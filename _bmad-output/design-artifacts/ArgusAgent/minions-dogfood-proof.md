# Minions Dogfood — Proof Artifact (Story 7.2, APAA CAPSTONE)

> AUTO-GENERATED by `minions_core/apaa/dogfood/proof_run.py` (`render_proof_markdown`). Reproducible + byte-stable for the same tracked Minions content — do NOT hand-edit. Drivers: APAA-FR-29 / APAA-FR-17 / APAA-FR-30 / APAA-FR-21 / APAA-NFR-D1 / APAA-NFR-S1 / APAA-AR4 / APAA-AR7.

## 1. Dogfood execution (AC-EXECUTE) — the frozen audit over the REAL Minions repo

The frozen `pipeline.run_audit_detailed` (REUSED — no fork) was run over the REAL Minions platform source (git-tracked `minions_core/`, excluding the self-audited `minions_core/apaa/` sub-tree — the SAME 7.1 scope), materialized into a CLEAN on-pin snapshot (the 6.5 `stage_cartridge` pattern) so the frozen `load_repo_at_commit` clean-tree precondition holds. The audited BYTES are the real Minions source at the tracked commit below.

- Commit descriptor (HEAD at generation): `7f8e1478573d3208c1df16aaaaa4f6f0bb0afea0`
- Source files audited: **135**
- Total physical LOC (build-cost proxy): **36712**
- Partition units (7.1 plan, CONSUMED): **4**
- **Verdict: `NOT_READY_FOR_RELEASE` (exit `2`)**
- Coverage-ledger deep-%: **`13/15`** (exact `Fraction`, never a float — AR4)
- Blocking (verdict-eligible) findings: **0**
- Total findings emitted: **2906**

## 2. Within the `$X` = 843 ceiling (AC-EXECUTE / FR21 / OI3) + the 3.2 halt

The run's V1 deterministic zero-token cost total is **675 credits** (folded via the 3.1 `account_spend` — no fork). It FITS under the 7.1 empirical ceiling `$X` = **843** credits.

- Under `BudgetConfig(ceiling_credits=843)` the run FITS (`ceiling_reached is False`): **True**
- Under a ceiling ONE credit below the total the run BREACHES (the >=-is-a-breach REUSE — the 3.2 halt->skip->downgrade->report path fires): **True**
- NFR-C1 baseline ratio (audit-cost / build-cost proxy): `675/36712` (`Fraction`/marker — never a float)

## 3. The SIGNED, source-free evidence bundle (AC-BUNDLE / FR29 / NFR-A1 / NFR-S1)

Exported via the done 4.3 `build_evidence_bundle` + persisted via `persist_evidence_bundle` (REUSED — no forked bundle model / serializer), serialized THROUGH the single 1.1 `canonical.dumps_bytes` and stamped by the 1.1 content-addressed, **prev-hash-chained** envelope (the APAA "signature"; the point-in-time stamp is the envelope `created_at`, EXCLUDED from the hash — NFR-A1/D3).

- Persisted bundle locator: `state/c0c4c35e1d32b5d435064bfdbf01550f2fb8acd16abde3413fc595dd7c72341b.json`
- Bundle content hash (the signature): `c0c4c35e1d32b5d435064bfdbf01550f2fb8acd16abde3413fc595dd7c72341b`
- Canonical bundle byte length: **1290935**
- Referential-integrity report consistent (4.2 lint): **True**
- **No-source-retention MOAT (NFR-S1 / NFR-S3):** the bundle retains NO Minions source byte and NO secret value — the moat is STRUCTURAL (no bundle field holds a source/secret value; only locations + redacted indicators). Proven over the REAL Minions tree by `tests/security/test_apaa_secret_containment.py`.
- **100% reproducibility (AC-REPRODUCIBLE / NFR-D1 / P1):** two dogfood runs on the same tracked content yield a BYTE-IDENTICAL verdict + bundle canonical bytes (the builder sorts/order-fixes every collection; no clock/float/set-order in the hashed payload). Demonstrated (RED against injected non-determinism, then green) in `tests/apaa/test_dogfood_proof.py`.

## 4. The `GitHub green · Sonar green · APAA 🔴` signature demo (AC-SIGNATURE)

APAA audits a vacuous test (the `vacuous_basic` cartridge — a test that runs green in CI while asserting nothing) and emits a **BLOCKING** `vacuous_test_ast` finding → verdict `NOT_READY_FOR_RELEASE` / exit `2` (the 🔴), reproducing the `GitHub green · Sonar green · APAA 🔴 tests appear vacuous` line as a real, repeatable committed artifact (the 1.7 `TC-APAA-PIPELINE-001-01` precedent). Asserted in `tests/apaa/test_dogfood_proof.py` (`test_signature_demo_vacuous_test_blocks`).

## 5. `grade: demo-heuristic-only` — the red-team honesty flag (AC-DEMO-GRADE)

- **`grade: demo-heuristic-only`**

> This dogfood run is a demo-heuristic-only (Tier-A) result: the frozen pipeline run_audit_detailed calls NO LLM (zero-token) and the AST-grounding deep-audit seam is NOT wired in, so every finding is advisory / verdict-ineligible (depth_supported is None). It is NOT presented as externalization or assurance evidence, and it does NOT clear the >=80%-precision gate — that requires the human TP/FP adjudication over these REAL findings (a documented human step, still open).

## 6. Adjudication-ready REAL findings (AC-ADJUDICATION-READY / OI1 / DF-6-6-A)

The REAL dogfood findings are laid out below by the 6.6 `finding_match_key` identity `(rule_id, verdict_eligible, advisory)` — one row per finding CLASS (two DISTINCT classes never collapse to one row — AI-E6-1). A human Eng-Lead + QA-Lead can tag each class TP/FP per `precision-validation-protocol.md` §4/§5 by inspecting the sample locators on the real repo. **The human TP/FP adjudication is NOT performed here (OI1 — it is the human step); the `TP/FP` column is left empty for the human.**

| rule_id | verdict-eligible (blocking) | advisory | count | sample locators | TP/FP (human) |
|---|---|---|---|---|---|
| `cross_partition` | False | True | 332 | `minions_core/agent_auth.py:1`; `minions_core/api/routes/capabilities.py:1`; `minions_core/api/routes/context.py:1`; `minions_core/api/routes/convoy.py:1`; `minions_core/api/routes/cost.py:1` | &nbsp; |
| `hardcoded_secret` | False | True | 2289 | `minions_core/agent_auth.py:103`; `minions_core/agent_auth.py:104`; `minions_core/agent_auth.py:128`; `minions_core/agent_auth.py:167`; `minions_core/agent_auth.py:173` | &nbsp; |
| `orphan_code` | False | True | 285 | `minions_core/agent_auth.py:123`; `minions_core/agent_auth.py:268`; `minions_core/agent_auth.py:274`; `minions_core/api/routes/capabilities.py:119`; `minions_core/api/routes/capabilities.py:127` | &nbsp; |

## 7. The ≥80%-precision gate STAYS PROVISIONAL (AC-PROVISIONAL / OI1 keystone)

The synthetic corpus (7.1: 5 distinct classes) bootstrapped a PROVISIONAL ≥80%-precision gate. **The gate is cleared ONLY by the human TP/FP adjudication over the REAL dogfood findings above** — a HUMAN step (Eng-Lead + QA-Lead), OUT of scope for this autonomous story. This proof presents NO ≥80% number as authoritative / cleared, does NOT flip `protocol_cleared`, and does NOT flip the 6.5 `precision_gate_status()` marker.

- Gate status: `provisional (Story 6.6 precision harness; precision=0/1 over FINDINGS not repos; N=0 labeled cartridges populated, floor N=5; the >=80% externalization gate stays PROVISIONAL until N>=5 with the validation protocol applied — this number is an EARLY/PROVISIONAL signal, NOT a cleared gate; adjudication method: _bmad-output/design-artifacts/ArgusAgent/precision-validation-protocol.md)`
- The still-open human-adjudication step is filed as a defer (six CC-3 fields, `target_story: epic-7-minions-dogfood-precision`) in `deferred-work.md`.
