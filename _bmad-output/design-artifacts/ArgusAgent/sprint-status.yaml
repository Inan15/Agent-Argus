# generated: 2026-06-18
# last_updated: 2026-08-23
# project: ArgusAgent (formerly APAA — AI Project Assurance Audit)
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/design-artifacts/ArgusAgent/stories
#
# PATH NOTE (2026-07-30, corrected 2026-08-03): the artifact folder was renamed
# APAA -> ArgusAgent, but the path references inside these documents were not
# updated at the time. The machine-read keys above now match the on-disk layout.
# Prose and frontmatter elsewhere in this folder may still say
# `design-artifacts/APAA/`; read those as `design-artifacts/ArgusAgent/`.
# Historical evidence text is left as written.
# (The 2026-07-30 wording of this note named the SAME path on both sides of the
# instruction — a partial rename edit — making it self-cancelling. Fixed here.)
#
# SCOPE NOTE: This is the ArgusAgent-LOCAL sprint tracker. ArgusAgent is a
# self-contained headless sub-tool whose planning lives under
# _bmad-output/design-artifacts/ArgusAgent/ (placement decision 2026-06-18) — it was
# kept SEPARATE from the Minions platform tracker at
# _bmad-output/implementation-artifacts/sprint-status.yaml (Epics 1–20). That
# platform path does NOT exist in this repo: ArgusAgent has since been extracted
# from the Minions monorepo and stands alone. The Minions sprint-status-integrity
# gate (scripts/check_sprint_status_integrity.py) targeted the Minions file only.
# Source epics: _bmad-output/design-artifacts/ArgusAgent/epics.md (10 epics / 45 stories).
#
# DELTA NOTE (2026-08-03): Epics 8-9 were added by the FR16/FR4 verdict-contract
# amendment + the Agent-Argus repo separation. Epics 1-7 are delivered and are NOT
# regenerated. The Minions-repo handoff (H0-H4) is deliberately NOT tracked here:
# it executes in the Minions repository and this repo's CI cannot verify it.
#
# DELTA NOTE (2026-08-09): Epic 10 was added by the repository audit of 2026-08-09
# (sprint-change-proposal-2026-08-09.md), which found work that entered the shipped
# contract through the separation seed commit (084c6a7) without passing a story gate.
# Epics 1-9 are delivered and are NOT regenerated or reopened — their retrospectives are
# signed, and editing their scope now would falsify the record. Epic 10 carries the
# corrective work as a delta, following the Epic 8/9 precedent. Ledger: DF-AUD-APAA-C..F.
# NOTE ON PLAN CLOSURE: the epic-9 retro declared the plan FINAL. Epic 10 re-opens it,
# which is the honest consequence of a plan closed over unspecified work.
# H0 (who FILES that handoff) is still UNOWNED — see epics.md frontmatter.
#   ⚠️ CORRECTION APPENDED 2026-08-17 (Story 13.3 / AC7) — the line ABOVE is STALE and is
#   retained unedited (§3.4) rather than rewritten: it is dated 2026-08-09 and PREDATES the
#   2026-08-10b closure. H0 is NO LONGER UNOWNED — CLOSED 2026-08-10b via the pre-authorised
#   epics.md option (b) (epics.md:30, :2642-2643; deferred-work.md:1575-1588). Story 10.5 already
#   recorded the same correction to a brief that said otherwise. THE SUBSTANCE IS UNCHANGED AND IS
#   THE POINT: H1-H4 are still NOT FILED, assumption A5 remains UNSUPPORTED, and H3's
#   blocking-vs-advisory policy decision is unmade. Re-derived list: epic-13-open-items.md.

# DELTA NOTE (2026-08-10): Epic 11 was added by the strategic pivot to a first PUBLIC
# release (sprint-change-proposal-2026-08-10.md). This is a MAJOR change: it amends the
# PRD's most-elevated constraint — the >=80%-precision externalization gate — by SPLITTING
# it into an ATTESTED tier (still gated, still NOT CLEARED) and a DISCLOSED tier (permitted
# pre-gate, on the condition that FR34 mechanically enforces the tool's own provisional
# status on every user-facing surface). Epic 11 is the disclosed release; FR34 is new.
# Epics 1-9 are delivered and are NOT reopened. Epic 10 is NOT redefined — Epic 11 DEPENDS
# on all four of its stories. Story 10.2's AC site list was CORRECTED in place (it named 4
# of 7 PRD/architecture sites and omitted FR7, the binding capability contract); 10.2 is
# unstarted, so this is an enumeration fix, not a scope change.
# SCOPE NARROWED FROM THE ORIGINAL ASK: the operator opened with Microsoft Store and other
# desktop software stores. Channels for THIS release are PyPI + GitHub Marketplace only.
# Desktop app stores, OS package managers (Winget/Chocolatey/Homebrew) and a hosted runner
# are DEFERRED, not rejected — recorded at PRD L317. The `store_compliance` PRD skip
# (L340) was re-validated and REMAINS CORRECT under the narrowed scope; it must be
# un-skipped before any desktop-store channel is scoped.
# OPEN AND UNOWNED, restated so it does not vanish behind a new epic: H0 (who FILES the
# Minions handoff H1-H4) and the DF-7-2-A human TP/FP adjudication — the ONLY step that can
# clear the attested gate. Neither is an Epic 11 story and neither is closed by shipping.
# ALSO SURFACED, NOT SCOPED HERE: argus/pipeline.py is 1331 lines against the NFR-M1 cap of
# 1200 — the DF-8-2-A breach predicted at 1199/1200 has HAPPENED, and no gate caught it
# (NFR-M1 is enforced per-module and ad hoc; there is no repo-wide sweep and no assertion
# covering pipeline.py). Not a release blocker — invisible to a user — but it taxes every
# Epic 10/11 fix that touches the file, and DF-8-2-A/DF-8-3-A/DF-8-3-C all gate on the
# extraction that has never been performed. Recommended as an ENABLER, not a gate.

# DELTA NOTE (2026-08-10b): SUPERSEDES the 2026-08-10 note above, which is retained unedited
# (§3.4). That proposal was never signed — its §6 approval box is still ⬜ and only its
# sprint-status edit ever landed: FR34 does not exist in the PRD, no 2026-08-10 entry exists in
# the PRD amendments block, and Epic 11 was never written into epics.md. This delta replaces it.
# Source: sprint-change-proposal-2026-08-10b.md (16 edits, approved by XAgent007 2026-08-10).
#
# WHAT CHANGED, AND WHY: the operator rejected the disclosed-provisional shape ("no provisional
# demos, only utility which helps end users and students"). The gate is NOT waived and NOT split
# into two permanent tiers. It is ABSOLUTE for attested use, and DEFERRED for a free public
# release under two binding FR34 conditions — enforced disclosure AND a committed programme to
# clear it. Epic 13 is that programme. FR34 is therefore TEMPORARY, with a removal condition.
#
# PERSONA CORRECTION (operator, 2026-08-10b): "student" is a CONTEXT, not a skill level — no
# procurement, no compliance mandate, no internal champion, no colleague who knows the tool. The
# tertiary persona is the INDEPENDENT DEVELOPER. Design for unassisted first-run utility because
# there is no one to ask, NOT because the user is inexperienced.
#
# EPIC 10 EXTENDED to 5 stories (10.5 added). NOT redefined — 10.1-10.4 survive unchanged.
# EPIC 11 REDEFINED (7 stories -> 5) as Release Integrity. Renumbering, recorded so the superseded
# entries can be traced: old 11.1 -> 11.1 (scope changed: gained the expiry clause) · old 11.3 ->
# 11.2 · old 11.4 -> 11.3 · old 11.5 -> 11.4 · old 11.6 -> 11.5 · old 11.7 -> 12.9 (the publish
# MOVED; publishing at the end of Epic 11 would ship a safe tool nobody wants) · OLD 11.2 IS
# ABSORBED into 12.2, which measures the same question as a precondition of the work rather than
# as a standalone spike. Its escalation discipline is preserved verbatim: a measured "no" is
# reported and escalated, never a licence to loosen a gate or soften Journey 6.
#
# EPIC 12 ADDED (9 stories) — The Useful Tool. Two capabilities are BUILT AND UNWIRED and this is
# the delivery gap that made the tool not worth installing: DeepAuditSeam (argus/audit/deep_audit.py:91,
# referenced only from argus/audit/* and dogfood/proof_run.py, NEVER from pipeline.py) and the
# memoization store (argus/cache/memo_store.py, never imported by pipeline.py). Adds NO new
# assurance capability (PRD §V1.5) — it delivers FR27/NFR-D1 already in the contract, plus new
# FR35/FR36/FR37 and NFR-P3/NFR-S6.
#
# EPIC 13 ADDED (3 stories) — Earn the Gate. The ONLY work that can remove the disclosure. NOT a
# build task. 🚩 CANNOT START WITHOUT A NAMED HUMAN: DF-7-2-A has been open and UNOWNED since
# Epic 7 and restated as unowned by Story 9.2 and the 2026-08-10 proposal. Story 13.2 does not
# begin until an adjudicator is named IN THIS FILE. Restating it a fourth time changes nothing;
# refusing to start the story does.
#
# NEW FINDING (a) — THE VALIDATION SET IS DEFINED TWICE, INCOMPATIBLY. PRD L161 says "N ~ 5-10
# REAL XAgents repos" and L156 requires findings judged real by an independent senior engineer;
# precision-validation-protocol.md §5 says "N >= 5 labeled planted-defect CARTRIDGES"
# (VALIDATION_SET_FLOOR_N = 5). These measure different quantities — cartridges measure RECALL
# against known plants (FR20, already delivered and CI-asserted); precision measures how often a
# 🔴 on UNPLANTED code is real. Architecture L152-154 called the validation-set input "the one
# open input that gates an ARCHITECTURE choice" and it is STILL marked OPEN at L328 — it was
# closed by IMPLEMENTATION rather than by DECISION. OWNED BY STORY 13.1, whose recommendation of
# record is that the PRD reading governs. Clearing an externalization gate on synthetic corpora
# Argus's own team authored would repeat the self-referential-evidence problem that already cost
# the Minions run.
#
# NEW FINDING (b) — A V1 CORE COMMITMENT WAS NEVER DELIVERED AND NEVER RECORDED AS DEFERRED.
# PRD §Product Scope L168 commits standards_refs[] + CWE-required-on-security-findings to V1 Core
# ("day-one additive"). Measured: ZERO occurrences of standards_ref/cwe/asvs/owasp anywhere in
# argus/**/*.py; FindingDraft (argus/detectors/base.py:63-87) carries no standards field.
# ROOT CAUSE: §Product Scope commits it, the BINDING FR CONTRACT NEVER LISTS IT (no FR1-33
# mentions standards mapping), and L384 says a capability not listed there will not exist. The two
# sections disagree, which is why no story ever built it. This is the EXACT INVERSE of
# DF-AUD-APAA-D — same root cause, a claim living in a section no story reads. OWNED BY STORY 10.5.
#
# V2 ROADMAP SWEEP (2026-08-10b, prompted by the operator): every §Product Scope V2 Growth Feature
# was swept against argus/**. MULTI-LANGUAGE IS THE ONLY V2 CAPABILITY THAT LEAKED INTO V1, and
# Story 10.2 already owns it. Absent: seam auditor, mutation-grade vacuous, multi-perspective
# panel, host-capability manifest, holdout rotation, PRR checklist, Minions cost-optimization (d).
# TWO V1 INVARIANTS VERIFIED HOLDING, not assumed: (1) argus/index/partitioner.py:233-238 records
# seam_analysis="v2-deferred" with cut edges "recorded, NOT analyzed"; (2) the partition_id-always-
# "root" ledger invariant holds — _DEFAULT_PARTITION_ID="root" at coverage_ledger.py:57 and
# recording.py:53, passed explicitly at pipeline.py:910 and :1189. The per-unit sha256 ids in
# dogfood/partition_plan.py belong to the WORK MANIFEST, a different contract, correctly separated.
#
# STORY 10.2 AC1 CORRECTED AGAIN — to 10 SITES, measured. The original AC named 4; the 2026-08-10
# "correction" named 7 and was ITSELF incomplete, omitting PRD L174, L317 and L375. Measured list:
# PRD L23, L116, L174, L180, L317, L375, L398 (FR7 — the binding capability contract), L476
# (NFR-P2) + architecture L220, L237. 10.2 is unstarted, so this remains an enumeration fix, not a
# scope change — but it now closes with a test that greps for the unamended claim shape, because
# hand-counting has been wrong twice.
#
# STATUS CHANGE — argus/pipeline.py NFR-M1 BREACH NOW HAS AN OWNER. The 2026-08-10 note above
# recorded 1331/1200 and recommended the extraction "as an ENABLER, not a gate". That was correct
# while nothing was landing in the file. Stories 12.2 and 12.3 BOTH land in it, so it is now a
# GATE: Story 12.1, FIRST in Epic 12. It also adds the repo-wide NFR-M1 sweep test (~10 lines)
# that would have caught 131 lines of silent drift — NFR-M1 is enforced per-module and ad hoc
# (tests/test_cache_invalidation.py:690, tests/test_cartridge_selfaudit.py:472) with no assertion
# covering pipeline.py. Closes/re-scopes DF-8-2-A, DF-8-3-A, DF-8-3-C, which all gate on it.
#
# ALSO CORRECTED IN THE ARCHITECTURE (recorded here because it becomes live in 12.2): §E justified
# NOT building fallback/circuit-breaking/cost-attribution on the grounds they came free from the
# Minions orchestrator. Story 9.1 REMOVED that orchestrator and nothing re-derived the reasoning.
# Costless today because the deep path is unwired; a live gap the moment 12.2 wires it, presenting
# as flaky audits rather than an obvious defect.
#
# OWNERSHIP RESOLVED 2026-08-10b (operator decisions; ledger appended append-only per §3.4):
#   - DF-7-2-A — ADJUDICATOR NAMED: **XAgent007**, filling the Engineering Lead role in
#     precision-validation-protocol.md §2 (primary adjudicator). The QA Lead second and external
#     tie-break stay unfilled until a borderline finding requires them (§4). Story 13.2's start
#     condition IS NOW MET. The ITEM IS NOT CLOSED — an owner is named, the measurement has not run.
#     DF-6-6-A/-P1/-P2 follow the same owner by inheritance (absorbed into Story 13.2).
#   - H0 — CLOSED via the pre-authorised epics.md option (b): the operator (XAgent007) records that FILING
#     H1-H4 against the Minions backlog is their OWN STEP, taken outside this workflow. No longer
#     UNOWNED. SCOPE OF THE CLOSURE: it ends the OWNERSHIP gap, NOT the execution — H1-H4 are still
#     NOT FILED, A5 remains UNSUPPORTED, and H3's blocking-vs-advisory policy decision is still needed.
#
# STILL OPEN, restated so nothing vanishes behind three new epics:
#   - The ≥80% precision gate — NOT CLEARED. protocol_cleared is False at
#     argus/precision/replay_harness.py:223 and has never been passed True at any call site.
#     Nothing in Epics 10-12 clears it. Shipping does not clear it. Usage is not evidence.
#
# SCOPE UNCHANGED FROM 2026-08-10: channels are the public index + GitHub Marketplace only.
# Desktop app stores, OS package managers and a hosted runner remain DEFERRED with reasons
# recorded; the PRD store_compliance skip was RE-VALIDATED and REMAINS CORRECT.
#
# ALL ARTIFACTS AMENDED 2026-08-10b — this delta did NOT stop at handoff, which is the failure that
# killed the 2026-08-10 proposal (approved edit-by-edit, routed onward, only its tracker edit ever
# landed). Applied and verified on disk: PRD (22 substitutions incl. the frontmatter amendments
# entry; FR count 33 -> 37; NFR-S6 + NFR-P3 added), architecture (10 substitutions), epics.md
# (Story 10.2 AC1 -> 10 sites + closing grep test; Story 10.5 added; Epics 11/12/13 written in
# full — 22 stories across 4 epics), and this file. YAML re-parsed clean after the splice.

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - in-progress: An INTERIM retrospective has been produced and a FINAL pass is
#     still REQUIRED. Added 2026-08-17 by the Epic-13 interim retrospective, which
#     covered 13.1 and 13.2 while 13.3 was blocked on a human act. The value already
#     exists in the Epic, Story and Action Item vocabularies above; this records what
#     it means for a RETROSPECTIVE, so the state is a documented one rather than an
#     undefined value. ⛔ It is NOT `done` on purpose: the dev-loop rolls an epic up to
#     `done` through this key, and a retrospective that never saw the epic's decisive
#     story must not be able to authorise that roll-up.
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-06-18
last_updated: 2026-08-23  # ADJUDICATOR NAMED 2026-08-22 (operator act, XAgent007): the QA Lead (second reviewer) role in precision-validation-protocol.md section 2 is FILLED by **Veer Pratap Singh**. Named AHEAD of Story 16.7 rather than during it - section 4's borderline ladder terminates at this role, and the only comparable population (the 2026-08-16 set of 31) produced 5 borderlines, so ~6 are expected over 16.7's 36. A role filled mid-adjudication is a role filled to unblock a result. Dispositions carry adjudicator id 'Veer Pratap Singh (QA Lead)'; the role string must match PROTOCOL_ADJUDICATOR_ROLES exactly or AdjudicationRow.__post_init__ raises UnregisteredAdjudicator. This supersedes the 2026-08-10b line above ('The QA Lead second and external tie-break stay unfilled...') by DATE, and does not rewrite it (section 3.4). The EXTERNAL ADJUDICATOR tie-break REMAINS UNFILLED - a persistent Engineering-Lead/QA-Lead disagreement still has nowhere to go and a story reaching that step must STOP. Recorded in the protocol as a fourth dated block under V1.3 with NO V1.4 row, so the change-log head is unmoved and -45/-63 stay green.  # EPIC 16 DELTA 2026-08-22: 16-4 closed BY DECISION (HALT-1 declined; DF-13-5-A OPEN and UNSPENT with a re-review trigger of 16.6+16.7 done, or 2026-11-22); 16-6 and 16-7 ADDED at backlog by sprint-change-proposal-2026-08-22.md, APPROVED 2026-08-22.  # dev-story 16-4 (2026-08-20): ready-for-dev -> in-progress, opened HALTED at Task 1 as the story requires.  # create-story 16-4 (2026-08-20): 16-4-ratify-run-adjudicate-and-let-the-arithmetic-decide backlog -> ready-for-dev, contexted at HEAD 3022415. The story that spends DF-13-5-A's ONE round is filed, and it is filed HALTED: it may not proceed past Task 1 without the operator. Two halts, not one - the section 6 R2 act, and a record-scoping decision found by execution that changes a six-for-six round from 1/1 to 3/16 depending on which record it folds into. See the story's section 0.2.  # code-review 16-3 (2026-08-20): PASS, zero decision-needed/patch/defer findings. review -> done for 16-3-a-detector-that-finds-nothing-has-not-passed; see the story's Review Findings for the full independent re-derivation.  # dev-story 16-3 (2026-08-20): 16-3-a-detector-that-finds-nothing-has-not-passed ready-for-dev -> in-progress -> review. Protocol section 5 gained a SEVENTH condition, the YIELD floor: the precision ratio is evaluable only over a verdict-eligible population of at least ceil(q/(q-p)) findings over PRECISION_GATE_THRESHOLD = p/q, which is 5 at the shipped 4/5 - the smallest denominator at which '>=80%' is not silently '100%'. Derived, never typed; stated in the general form because it equals q only when q-p==1 (it is 4 at 5/7 and 5 at 7/9); NOT derived from VALIDATION_SET_FLOOR_N, whose equality is a disclosed coincidence. Premise reproduced first: at HEAD 1ecf618 populations of 3 and 4 findings, all TP, returned CLEARED at 1/1 with all six conditions MET; they are BLOCKED now. Landed in 48e8ea6 (the sha 16.4 cites), after the UNDOCUMENTED split-first trigger on tests/test_gate_decision.py (1191/1200, no ledger entry) was discharged FIRST and alone in 01a2f48; artifacts regenerated in d26ffb6; an unreal guard my OWN mutation run caught was repaired in a0c74ae. THIRD dated block under V1.3 - no V1.4 row, adjudication-record.json byte-unchanged. The OI1 recall bullet at section 5 line 313 amended explicitly, struck-not-erased, saying what the floor is and is not; line 312 and section 7 untouched; the boundary enforced STRUCTURALLY by an AST walk of gate_yield.py. Full suite 1673/0/0/0 exit 0 with ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1 (baseline 1667 + the six new -95..-100), mypy 92 files, bandit clean, both builders --check exit 0, ceiling green with no new exemption; 15 executed mutations all observed RED with the tree restored byte-exact. PRE-ROUND DISCLOSURE carried forward, NOT resolved: the corrected detector's verdict-eligible yield over the gating corpus is 0 of 4284, the only population ever above this floor (31) came from the refuted pre-14 rule and adjudicated 0 TP / 26 FP / 5 BORDERLINE, and the sealed partition's achievable yield is UNMEASURABLE without the forbidden run - so a BLOCKED-on-yield round is the likely outcome and DF-13-5-A already routes it to option (b). A search for a STRUCTURAL cap found none: unmeasured, not impossible. DF-16-3-A filed (tests/test_gate_seal.py at 1145/1200), pure append. AC8.3 OPEN - instructed not to push, so no CI run covers these shas and the local gates are Windows-only. Nothing ratified, fetched, staged or run over any bench member; DF-13-5-A's ONE round UNSPENT; DF-16-1-A stays unlanded; amendment-A stays UNAPPROVED and unacted-on; no ledger entry disposed of; zero candidate-output paths touched, so the BINDING ORDERING CONSTRAINT is intact. No other key moved.  # dev-story 16-3 (2026-08-20): 16-3-a-detector-that-finds-nothing-has-not-passed ready-for-dev -> in-progress. Task 1 (the UNDOCUMENTED SPLIT-FIRST trigger: tests/test_gate_decision.py at 1191/1200 with no ledger entry) taken FIRST and in its own commit. No other key moved.    # create-story 16-3 (2026-08-20): 16-3-a-detector-that-finds-nothing-has-not-passed backlog -> ready-for-dev, contexted at HEAD 1ecf618 with the baseline measured GREEN by execution (1,667 collected exit 0 with ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1, mypy 91 files, bandit clean, both builders --check exit 0, tree clean, 20 ahead / 0 behind). THE PREMISE IS PROVED, NOT ASSERTED: driven through the SHIPPED decide_gate, a population of exactly THREE verdict-eligible findings - one per sealed member, all TP - returns CLEARED today with all six section-5 conditions MET, precision 1/1, and an outcome sentence reading 'Clearing authorises ATTESTED externalization'. Size 4 does too. That is the story title, measured. THE FLOOR IS DERIVED FROM THE ONE LOCKED THRESHOLD, NOT TYPED: ceil(q/(q-p)) over PRECISION_GATE_THRESHOLD = 4/5, i.e. the smallest denominator at which the >=80% gate admits ONE false positive - below 5, '>=80%' is silently a 100% requirement (executed: 0 FPs affordable at d=1..4, 1 at d=5). = 5. Stated in the GENERAL form because it equals the denominator only when q-p==1 and diverges at 5/7 and 7/9 - 16.1's 'strict majority' correction, pre-applied. VACUITY MEASURED FROM THE OTHER SIDE AND THE TWO AGREE: derive_concentration raises on an empty population so a floor of 1 could never fail, and the smallest population passing BOTH breadth and seal is 3, so any floor <=3 is an unreachable branch; 5 fires on exactly the sizes 3 and 4 that were measured wrongly CLEARED. NOT resolved from VALIDATION_SET_FLOOR_N although both are 5 - a disclosed coincidence of two independent locked quantities, not a derivation. THE SHUTDOWN CHECK IS ANSWERED AS FAR AS IT CAN BE, AND ITS LIMIT IS RECORDED UP FRONT: the corrected post-Epic-14 detector's verdict-eligible yield over the gating corpus is ZERO - counted directly, 4,284 findings across all five ratified members, verdict_eligible 0, blocking 0 - and the only population that ever exceeded a yield of 5 (31) came from the REFUTED pre-14 corroboration rule and adjudicated 0 TP / 26 FP. Achievable yield over the sealed partition (6 members, 431 co-occurrence files) is NOT measurable without running the detector, which is exactly the forbidden act, so unlike 16.1 and 16.2 this question cannot be closed by measurement. Recorded as a PRE-ROUND DISCLOSURE rather than a halt, because unmeasured is not impossible-by-construction: a low-yield 16.4 is already DF-13-5-A's pre-registered option (b), and the yield floor is that stopping rule made arithmetic. The dev escalates ONLY if a STRUCTURAL cap bounds yield below the floor. OI1 ANSWERED, NOT PAPERED OVER: recall needs FN, which is unknowable over a corpus with no golden key; this floor has no FN term and no bench-content term - it is a floor on the DENOMINATOR of the precision ratio derived from the THRESHOLD, i.e. a statement about the measurement's resolution, not about what was missed. That holds ONLY because of where the number comes from, so AC2.4 makes it MECHANICALLY CHECKABLE by an AST walk of the new module's own imports and names, and AC2.5 makes the bench-content framing an OPERATOR ESCALATION rather than a story decision. The OI1 bullet amended is section 5's Recall row at protocol line 313, EXPLICITLY and struck-not-erased; line 312 (cartridge corpus) and section 7 (which carries no recall bullet) are NOT touched. AC3 COMPOSITION IS A DRIVEN GUARD IN BOTH DIRECTIONS: 40 findings from 1 sealed member is BLOCKED today and must stay BLOCKED (quantity without breadth still fails), and sizes 3-4 with breadth AND seal MET must become BLOCKED on yield - with a mixed population pinning breadth and seal TRUE so the yield clause is DECISIVE and not lockstep. AN UNDOCUMENTED SPLIT-FIRST TRIGGER WAS FOUND AND IS TASK 1: tests/test_gate_decision.py is at 1,191/1,200 - NINE lines - with NO ledger entry at all (16.2 flagged it at 1,193, split only the production module, and filed no DF-16-2-* entry), and the edit is unavoidable because the section-5 dispatch mirror takes REQUIRED keyword terms so a seventh condition forces every caller to be edited; the cohesion boundary is measured by AST at -59..-64 (353 contiguous lines, the artifact guards) and must be re-confirmed by the dev. Guards go in a NEW tests/test_gate_yield.py at TC-ArgusAgent-PRECISION-001-95.., because test_gate_seal.py has 65 lines and shaving a file to fit is what MAINT-001's remedy forbids. NOT authorised and NOT taken: no run, no ratification, no fetch, no stage, no adjudication, no V1.4 row (the 31 judgements of 2026-08-17 keep V1.3 provenance - this is the THIRD dated block under V1.3), DF-16-1-A's rule-class arm stays unlanded, DF-13-5-A's ONE round stays UNSPENT, 16.4 is NOT contexted or started, and sprint-change-proposal-2026-08-20-amendment-A.md remains registered and UNAPPROVED and out of scope. Nothing was committed, staged or pushed by contexting; argus/, tests/ and scripts/ are byte-unchanged. No other key moved.  # code-review 16-2 (2026-08-20): 16-2-part-of-the-bench-is-sealed-before-anything-is-run review -> done. PASS on independent re-review; see the story's Review Findings section and this key's own comment for the full account. No other key moved.  # dev-story 16-2 (2026-08-20): 16-2-part-of-the-bench-is-sealed-before-anything-is-run ready-for-dev -> in-progress -> review. Seal landed in f89f028 (SEAL_COMMIT_SHA, recorded in the later 9d7f8b5 because a commit cannot cite itself); DF-16-1-B's SPLIT-FIRST trigger discharged first and in its own commit (95819bc); protocol section 5 gained a SIXTH condition by a SECOND dated block under V1.3 (no V1.4 row, adjudication-record.json byte-unchanged); gate-decision-record.json and the dogfood artifacts regenerated in fd20c32. Full suite 1667/0/0/0 exit 0 with ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1, mypy 91 files, bandit clean, both builders --check exit 0; 27 executed mutations all observed RED with the tree restored byte-exact. AC8.3 recorded OPEN - instructed not to push, so no CI run covers these shas and the local gates are Windows-only. Nothing ratified, fetched, staged or run over; DF-13-5-A's ONE round UNSPENT; DF-16-1-A stays unlanded; amendment-A stays UNAPPROVED and unacted-on; no ledger entry disposed of. No other key moved.  # create-story 16-2 (2026-08-20): 16-2-part-of-the-bench-is-sealed-before-anything-is-run backlog -> ready-for-dev. No other key moved.  # correct-course 2026-08-19 (sprint-change-proposal-2026-08-19.md, APPROVED by XAgent007): added 15-2-the-detector-and-the-index-agree-on-what-a-line-is at `backlog` under epic-15, positioned AFTER 15-1 and BEFORE epic-15-retrospective. Trigger: the AI-E14-2 / SD-1 vacuity sweep of epic 14's 35 guards (11 previously swept, the other 24 swept 2026-08-19 by mutation) returned 22 REAL / 1 VACUOUS / 1 WEAK and surfaced a live false-accusation defect in the shipped detector. Proposal registered in tests/test_status_document_registry.py (TC-ArgusAgent-DOCS-001-22 observed RED before and GREEN after); epics.md carries story 15.2 and the amended epic-15 Covers line; deferred-work.md received DF-15-2-A, DF-15-2-B, DF-15-2-C and an append-only amendment re-pointing DF-14-3-H at 15-2. NOTHING was closed. AI-E14-2 is NOT discharged - its verdict table has no durable home in the repository, which is what DF-15-2-C records. No code was written, argus/ and tests/ are byte-unchanged apart from the one registry line, and nothing was committed, staged or pushed.  # retrospective epic-13 FINAL (2026-08-19): epic-13-retrospective in-progress -> done. epic-13-retro-2026-08-19.md written, registered in tests/test_status_document_registry.py, and the full suite re-run green afterwards (1641/0/0, exit 0). It SUPERSEDES epic-13-retro-INTERIM-2026-08-17.md BY NAME and does not edit it. Gate outcome BLOCKED, precision UNEVALUABLE, over a pin-verified five-of-five zero; the FR34 disclosure STAYS and the tool's stated status is unchanged. epic-13 NOT touched — the roll-up is the orchestrator's step. Nothing committed, staged or pushed.  # code-review 13-5 (2026-08-19, Sonnet): review -> done. VERDICT pass, iteration 2, on commit ad838f6 (the single fix commit over 313c94b). Both iteration-1 Low findings independently re-verified as resolved: reverted the parser and confirmed -72 goes RED, restored it and confirmed all 6 tests green; re-measured all five corrected line counts (893/350/367/448/559) exactly; confirmed zero rename/copy records and identical old/new parsing on all five live checkouts with no write path into any of them; confirmed AR8 purity and the three distinct named refusals in -73. mypy/bandit/ruff clean, full suite 1641/0/0 exit 0 (result-marker count, matching claim). No High/Medium/Low findings on this diff. Findings written to the story file.  # dev-story 13-5 (2026-08-18) fix round on HEAD 313c94b: in-progress -> review. 2 of 2 code-review findings resolved (both Low); a real -z rename/copy parsing defect fixed at the real seam plus a second truncated-stream hole found by the new guard; three stated-as-measured line counts corrected (897->893, 297->350, 372->367). Full suite 1641 passed / 0 failed / 0 skipped, exit 0. No argus/ LOC changed, no artifact regenerated, no ledger entry written, no corpus checkout mutated.  # code-review 13-5 (2026-08-18) on commit range 63a0434..313c94b (d7886ba + 91b95e3 + 313c94b): review -> in-progress. VERDICT concerns: two Low-severity findings (a rename-record parsing gap in the new dirty_in_scope_paths evidence field, and a 4-line D-7 table inaccuracy for test_instrument_disclosure.py, 893 not 897), neither affecting gating/outcome correctness. No High/Medium findings. Independently re-verified: full suite 1639 passed / 0 failed / 0 skipped exit 0 (both before and after this review); corpus_read_proof figures (1960/828/5129/1249/4284/0) reconcile exactly from the D-4 per-member table; adjudication-record.json 31 rows byte-identical before/after; no corpus checkout worktree mutated (agent-smith still 16 dirty at 9ab774d7, minions has no extra worktree registered); GATE_OUTCOMES still exactly 3 members; protocol_cleared still literal False everywhere; expert_hours None not 0. Findings written to the story file Review Findings section for the next dev round.  # dev-story 13-5 (2026-08-18) on HEAD 63a0434: ready-for-dev -> review. Commits d7886ba (delta) + 91b95e3 (dogfood artifacts, separate). Full suite 1639 passed / 0 failed / 0 skipped, exit 0 (baseline 1632); mypy clean, bandit 0 High / 0 Medium. CI NOT ESTABLISHED - nothing pushed, origin/master still 47b6dbe.  # dev-story 14-3 (2026-08-18) on HEAD e2e278c: ready-for-dev -> review. Commits e252043 (delta) + d339076 (dogfood artifacts, separate). Full suite 1631 passed / 0 failed / 0 skipped, exit 0 (baseline 1621).  # create-story 14-3 (2026-08-18) on HEAD e2e278c: backlog -> ready-for-dev. Story file stories/14-3-the-assertion-vocabulary-crosses-the-languages-the-installer-ships.md. ALL PREMISES RE-DERIVED BY EXECUTION against the post-14.1/post-14.2 tree; TWO DID NOT SURVIVE and are recorded as failures rather than carried. (1) The named JS fixture is NO LONGER FLAGGED: 14.2's logical-statement denominator scores a K&R-brace JS function body at statement_count=0 and the flag predicate requires >0, so the epic's 're-measured and is NOT flagged' criterion is now VACUOUSLY TRUE and would pass with zero code change. The false accusation survives only in the ALLMAN-brace shape (brace on its own line -> stmts=1, asserts=0, FLAGGED) - reproduced RED and measured closed by the widening. Target re-stated as DN-14-3-4. (2) The proposal 17b section 2.3 corpus prediction is REFUTED, not merely unmeasured: over all three members at their pinned shas the widening moves 0 flags GAINED and 0 LOST - agent-smith scores 0 TypeScript test functions of 169 extracted, xagents-webapp exactly 1 of 410, because DF-14-3-A (case-sensitive startswith 'test') and DF-14-3-C (callback blocks yield no definitions) keep TS tests out of the scorer entirely. The story's rationale therefore narrows to the MECHANISM and the FIXTURE, which is the branch the proposal's section 3.3 pre-registered. Baselines re-measured: 1621 passed / 0 failed / 0 skipped exit 0, mypy Success-87, bandit 19L/0M/0H; flag rates 653/3551=18.4%, 295/1122=26.3%, xagents-webapp 17/73=23.3%; _ASSERTION_CALLEES holds 53 names (NOT 23 - that figure predates 14.2); _CORROBORATION_ASSERTION_CALLEES frozen at 23. DN-14-2-1 vs NFR-P2 resolved WITHOUT conflict and without a halt: 14.3 widens ONE table (the density numerator); the two tables are partitioned by QUESTION, never by language, so no language field enters the detector. The JS/TS verdict-ineligibility limitation was measured to be caused by _MOCK_CALLEES being Python-only and by fact (b)'s Python-shaped machinery - demonstrated to hold even under a hypothetical ONE-TABLE design carrying 14.3's full vocabulary - so it is NOT a trap DN-14-2-1 laid for this story; it is made AC6 (a new ledger entry with a named owner) rather than left unrecorded, because it bounds Epic 15: a TypeScript bench member can contribute advisory findings only. TC-ArgusAgent-DETECT-001-116 identified by execution as the ONLY guard that will go red (its cross_language literal is 14.2's 'this has not happened yet' assertion) - to be re-authored -86-style, never deleted. Dogfood artifacts PREDICTED UNCHANGED (0 delta over Argus's own 1,777 test functions), reversing the usual expectation. DF-14-2-A and DF-14-2-B both name this story as target_story and are in scope. DF-14-3-A/B/C cited, not fixed; A and B move together or not at all. || code-review 14-2 (Sonnet, iteration 3, 2026-08-18): review -> done. VERDICT pass -- documentation-accuracy finding confirmed closed by independent re-measurement (module sizes 926/929/1060/1128, deferred-work.md diff 287/0), DF-14-2-B confirmed genuinely OPEN in the ledger with a named owner and the dev's decision not to force a ledger closure adjudicated sound, full suite re-run after this review's own write at 1621 passed / 0 failed / 0 skipped exit 0. || dev-story 14-2 fix pass (Opus, dev pass 3, 2026-08-18) on HEAD e2e278c: in-progress -> review. Review iteration 2's ONE Low finding CLOSED, 1/1. DOCUMENTATION ONLY -- no argus/ or tests/ file opened, no test added or changed, no corpus measurement re-run, no dogfood artifact regenerated, nothing committed. The finding was reproduced before it was accepted rather than taken from the reviewer's table: each figure re-derived with len(Path(p).read_text(encoding='utf-8').splitlines()) -- the same method test_module_size_ceiling.py uses -- at HEAD e2e278c and cross-checked at every commit this story produced via git show. CORRECTED: provenance_scan.py 903 -> 926, vacuous_test.py 919 -> 929, test_vacuous_density.py 1053 -> 1060; test_vacuous_detector.py confirmed already exact at 1128. Root cause named rather than rounded off: 919 is vacuous_test.py's PRE-fix-pass count at a9cf25d transcribed forward as a post-pass number, and the whole fix-pass table was assembled from the pass-1 table rather than from the files; every remaining 919 in the record now carries its baseline explicitly and the File List entries read 815->926, 919->929, 814->1060. SWEEP of the same defect class across the record (the finding's real subject, not just the four line counts) found TWO FURTHER LIVE DEFECTS, one of them RED ON DISK. (i) deferred-work.md's working diff, claimed 269 insertions, re-measured 287, load-bearing 0 deletions (append-only) confirmed to hold. (ii) THE FIRST FULL-SUITE RUN OF THIS PASS CAME BACK 1620 PASSED / 1 FAILED -- TC-ArgusAgent-DOCS-001-78, 'a story record claims a ledger closure that deferred-work.md never received' -- and the claimant was the 14-2 story file itself: review iteration 2's own hand-off prose put the ledger id DF-14-2-B on the same line as the closure verb, which the line-scoped story_closure_claims analyzer reads as a claim the ENTRY was closed. It was not, and must not be: that entry is deferred and OPEN with a named owner, as both prior passes verified. Same defect class as the finding I was sent to fix (the record asserting what is not true of the world) through a different door, and the exact trap the story's own previous-story-intelligence names. It went unseen because iteration 2's gates ran BEFORE its findings were written into the story file, so the red was created by the hand-off prose itself and nothing re-ran -- AI-E13-1 in miniature. FIXED IN THE PROSE, DELIBERATELY NOT IN THE LEDGER: appending a ledger closure to green a guard would be AI-E12-3's defect committed inside the guard built to stop it, so deferred-work.md was NOT opened; the sentence now reads 'closed as a review item, explicitly not a ledger closure, the entry remains OPEN', annotated with what changed and why. (My first attempt reproduced the pattern by quoting the bad sentence verbatim and re-tripped the guard; recorded, because a fix that had to be fixed is part of the record.) The rest of the sweep re-derived as CORRECT: pass-1's four module sizes exact for their baseline (697->919, 610->815, 814 new, 1084->1128), argus/pipeline.py 1111 at all five commits and absent from the diff, thresholds byte-identical by git diff -U0 | grep (empty), dogfood provenance sha 2eeaace confirmed an ancestor of HEAD, git tag -l empty. NO CEILING IMPLICATION: all four files <=1200 (tightest test_vacuous_detector.py at 1128, 72 spare), test_module_size_ceiling.py green, no exemption added, registry unmoved. NO CURRENCY-GUARD IMPLICATION, and it is CONFIRMED rather than assumed: git status --porcelain -- argus/ is EMPTY at e2e278c, so the artifacts' inputs cannot have moved; the regenerator was deliberately NOT run, since re-rendering with no input change would rewrite a provenance sha for nothing. Full suite re-run after the last prose edit (AI-E13-1's lesson, third time): 1621 passed / 0 failed / 0 skipped / 0 errors, exit 0 -- delta 0 against the fix pass, the expected result for a change touching no executable line, and green only after defect (ii) above was repaired. LOCAL ONLY; CI evidence is NOT ESTABLISHED (Windows 11 / CPython 3.11; the ubuntu matrix and the 3.10/3.12 legs reasoned about, not executed). The story's substance, confirmed correct by two independent review passes, is byte-unchanged; only the record's own arithmetic about itself was repaired. Story file and sprint-status.yaml remain UNCOMMITTED per SS0.8, as in the previous two rounds. || code-review 14-2 (Sonnet, iteration 2, 2026-08-18): review -> in-progress. VERDICT concerns (one new Low finding: the Dev Agent Record's own AC9 module-size numbers for this pass do not match the files as committed -- provenance_scan.py claimed 903 vs measured 926, vacuous_test.py 919 vs 929, test_vacuous_density.py 1053 vs 1060, test_vacuous_detector.py exact at 1128; no ceiling implication, no functional impact). Scope: the fix round only (2eeaace/e2e278c against a9cf25d) -- iteration 1's own re-derivation stands, not repeated. Independently re-derived from a FRESH harness: CPython grounding confirmed for all five excluded/carried-negative cases; DN-14-2-5 verified on all three claims (scan/starts untouched in the diff, byte-identical by direct call, a hand-built wrong in-scan alternative shown to fail -122); RED-first reproduced row for row against a9cf25d (8-vs-5, 6-vs-4, 5-vs-4 patterns, -122's 7-vs-5); the structural GAINED=0 argument adjudicated sound by source read; corpus-wide re-derivation from an independent line-matched-ground-truth harness reproduces n=4673 exact=4672 over=0 under=1 EXACTLY; 15 hand-built adversarial predicate shapes found no defect; gates re-run independently at pytest 1621/0/0/0, mypy Success-87, bandit 19L/0M/0H, all exact; both iteration-1 findings confirmed CLOSED. Full detail in the story file's Review iteration 2 section and the 14-2 key's own comment above. || code-review 14-2 (Sonnet, iteration 1, 2026-08-18): review -> in-progress. VERDICT concerns. Independently re-derived by execution in two isolated git worktrees (966ceba/a9cf25d), own harness (not the dev's script): headline flags EXACT match (minions 1848/3551=52.0%% -> 660/3551=18.6%%, agent-smith 681/1122=60.7%% -> 297/1122=26.5%%, GAINED 0 both members, 1572 lost); CPython ast ground truth re-derived over both full before-flagged populations, 1.9071x -> 1.0052x confirmed exact, 1784/1848=96.5%% exact-match confirmed exact, agent-smith ground-truth divergence (6608 vs §0's 6600) independently reproduced. DN-14-2-1 load-bearing claim reproduced literally: corroboration path patched to read the widened table in an isolated worktree -> only -115 goes red, test_default_path_blocking_verdict.py (-30/-116/-117) stays 100%% green, revert restores byte-identical diff. Gates independently re-measured in ArgusAgent's own venv: pytest JUnit XML tests=1619 errors=0 failures=1 skipped=0 (the one failure is a verification-harness artifact -- a worktree literally named "after" trips a project-name-leak assertion, confirmed absent at 966ceba unpatched); mypy Success 87 files; bandit 19L/0M/0H conf 0/0/6/13. Thresholds byte-identical by diff (empty); pipeline.py diff empty; module sizes 919/815/1128/814 all <=1200, ceiling test diff empty (no exemption); cartridge/critical-eligibility suites clean; DF-14-2-A confirmed a real, well-scoped, honestly-bounded deferral with a named owner; stories/1-5-...md's 2026-08-18 correction independently checked -- struck text untouched, every re-measured figure matches exactly. ONE Low finding kept open: the AC1 docstring's claim about the residual error's DIRECTION is measurably wrong -- the dominant measured mechanism is an OVER-count from bare except/else/finally/case continuation-clause headers (each counted as its own statement though CPython's ast gives the whole compound ONE stmt), not the claimed under-count; reproduced with a minimal repro (try/except/else/finally scores 8 vs ast 5) and two real corpus instances named in the story's Review Findings. Severity capped Low because it can only feed the advisory heuristic path -- ast_corroborated/RULE_AST is independent of density and reads the FROZEN vocabulary via DN-14-2-1's separate path, so this cannot manufacture a verdict-eligible false accusation. One Low item deferred pre-existing (DF-14-2-B, hBcanchored _ASSIGNMENT_RE contradicting its own docstring, byte-identical since 14.1, not exploitable given call-site usage). No High/Medium findings; no unresolved decision-needed or patch items above Low; all ACs independently verified met; tests/mypy/bandit all green. Findings written to the story file's Review Findings section; DF-14-2-B filed in deferred-work.md. Sent back to in-progress per the concerns->in-progress mapping so the two Low items are picked up; the orchestrator may treat concerns as pass-eligible per its own cap. || create-story 14-2 (2026-08-18) on HEAD 966ceba: backlog -> ready-for-dev. || code-review 14-1 (Sonnet, iteration 3, 2026-08-18): review -> done (PASS; fix-loop cap reached). Independently re-verified by execution: git diff --name-only 47b6dbe..966ceba touches exactly the File List eleven files, nothing else (pipeline.py, cartridges, validation-corpus, governance artifacts untouched). Full suite 1611 collected / 1611 passed / 0 failed / 0 skipped exit 0; mypy Success 87 files; bandit 19L/0M/0H. vacuous_test.py diff against a727a80 is empty (byte-unchanged, confirmed directly). -111/-112/-117 independently reproduced RED against the actual pre-fix provenance_scan.py (swapped in via git show, restored after): -111 failed (2,0)!=(1,1); -112 failed on the SAME (1,1)-count assertion, not order-invariance -- the pre-fix code is already order-invariant (always searches from column 0) and simply wrong, confirming -112 catches an order-invariant-but-wrong fix against the real pre-fix code; -117 failed with the semicolon-compound arm promoted to a blocking verdict. AC3 re-scored independently (own script, own staging via git show at the pinned shas through build_ast_index + VacuousTestDetector): 0/31 verdict-eligible. Cartridge recall 7/7. VacuousTestScore confirmed still frozen extra=forbid, same fields. Dogfood/signature-demo modules re-run green. Order-invariance argument adjudicated and holds. Both moved behaviours (x = 1; sut(2, 3) -> corroborated; bare nested sut(sut(1,2),3) -> advisory) are defensible; the nested-call move is a second, previously-unreported instance of the same column-blindness defect fixed as a side effect. Adversarial hunt for a third false-accusation mechanism: 27 fixtures (walrus, lambda, f-string, async/await, try/except, with-as, chain-assignment, tuple/starred unpacking, global/nonlocal, decorators, comment/string lookalikes, CRLF, deep indentation, long lines, match/case, dict-comprehension) run through the real detector -- none produced a false promotion; two recall misses found, both in the safe direction. No third mechanism found (reported as a negative result). No High/Medium findings. See stories/14-1-...md "Review Findings - iteration 3" for the full record. || dev-story 14-1 iteration 3 (2026-08-17): 14-1 in-progress -> review; the ONE open High finding (column-blind occurrence resolution) is resolved. Occurrence resolution is stateful per (line, callee) and the classification unit is now the ;-delimited SIMPLE statement; CodeEdge and the 1.4 index are byte-unchanged. The suggested fix's premise that the index emits edges in source order was MEASURED FALSE (_extract walks right-to-left) and reported rather than converted into a schema change - the counts are bijection-invariant, so distinctness is all that is required, pinned by -112. Predicate not weakened: recall 3/3, adjudicated locators 0/31 verdict-eligible at the unchanged pinned shas, flag rate byte-identical (1,848/3,551 and 681/1,122). Guards -111/-112/-117 confirmed RED pre-fix. pytest 1611 passed / 0 failed / exit 0, mypy Success 87 files, bandit 19L/0M/0H. || code-review 14-1 (Sonnet, iteration 2, 2026-08-17): 14-1 review -> in-progress (FAIL) - a new High false-accusation finding (provenance_scan._locate_call is column-blind to a repeated callee on one physical line); see development_status[14-1-...] for the full reproduction. || dev-story 14-1 iteration 2 (2026-08-17): 14-1 in-progress -> review; both review findings resolved, AI-E12-11 sequence run so the dogfood guards are green. || dev-story 14-1 (2026-08-17): 14-1 in-progress -> review; see development_status[14-1-...] for the measured record and the AI-E12-11 hand-off. || correct-course 2026-08-17b (sprint-change-proposal-2026-08-17b.md, APPROVED by XAgent007): story 14-3 added to epic-14, runs STRICTLY AFTER 14-2. Trigger was a newly measured FALSE ACCUSATION found while scoping a multi-language bench - a JS test containing a real expect().toBe() assertion scores assertions=0 and is FLAGGED, because _ASSERTION_CALLEES holds 23 names and every one is unittest. Measured by execution over six fixtures through the SHIPPED index and scorer; argus/ byte-unchanged. Three further gaps filed as DF-14-3-A/B/C and deliberately NOT fixed - A and B are COUPLED and fixing A alone would convert Go's harmless silence into a fresh false accusation across the language. || OPERATOR DECISION 2026-08-17 (XAgent007), no proposal: epic-15 + 15-1 created on the Story 13.4 precedent - the bench that makes the gate EVALUABLE. || DF-13-5-A ANSWERED 2026-08-17 as a PRE-REGISTERED RULE, taken BEFORE 13.5 ran and before any repository was chosen: if 13.5 returns UNEVALUABLE we pursue ONE bench-expansion round; zero findings or sub-80% precision means the FR34 disclosure STANDS for V1.5 and the next attempt needs a materially better DETECTOR, not a bigger bench. ONE ROUND is load-bearing - without a stopping rule 'expand the bench' becomes 'keep expanding until it passes'. || code-review 13-4 (Sonnet): 13-4-split-the-status-document-registry review -> done. VERDICT pass, independently re-verified by execution against all ACs (test-id sets, assert counts, registry byte-diffs, gate numbers); one Low/cosmetic finding deferred. See development_status[13-4-split-the-status-document-registry] and the story file's Review Findings for the full record. || correct-course 2026-08-17 (sprint-change-proposal-2026-08-17.md, APPROVED): epic-14 + 14-1 + 14-2 + 13-5 added; Epic 13 RE-OPENED. || create-story 13-4 (2026-08-17) on HEAD 867a3bd: 13-4-split-the-status-document-registry backlog -> ready-for-dev. Epic 13’s OWN unblocker (AI-E13-2, retro critical-path #3), authorised directly by XAgent007 WITHOUT a change proposal because filing one is the very act it unblocks. The deadlock was REPRODUCED BY EXECUTION and a feasibility dry run of the split was executed and reverted — full record in development_status[13-4-split-the-status-document-registry]. Baselines re-measured green on this tree: pytest 1597 passed / 0 failed / 0 skipped, mypy 86 files, bandit 19 Low / 0 Med / 0 High. epic-13 already in-progress, unchanged. epic-13-retrospective stays in-progress: this story does NOT write, register or trigger the FINAL retrospective — it makes writing one possible. || code-review 13-3 (Sonnet) close-out: 13-3 in-progress -> done. VERDICT pass after two same-class Low findings (both hand-typed ledger figures disagreeing with the artifact, one with an accompanying wrong sha citation) were found across three review passes, fixed by the orchestrator, and independently re-verified by execution each time. See development_status[13-3-record-the-result-and-let-it-decide] and the story file's Review Findings for the full record. || code-review 13-3 (Sonnet): 13-3 review -> in-progress. VERDICT concerns (independently re-verified by execution; one Low finding, a hand-typed ledger-delta figure off by 25 lines). See development_status[13-3-record-the-result-and-let-it-decide] and the story file's Review Findings for the full record. || dev-story 13-3: 13-3 ready-for-dev -> review. Epic 13's final story is implemented and the gate decision is COMMITTED. The named human adjudicated all 31 findings (0 TP / 26 FP / 5 BORDERLINE); the run is NOT exhaustive under protocol §4, so the recorded outcome is BLOCKED — a §5 outcome was NOT taken, and BLOCKED is never rendered as 'the gate did not clear'. The disclosure stays; no threshold moved; nothing published. epic-13-retrospective still needs its FINAL pass. || epic-13 INTERIM retrospective: epic-13-retrospective backlog -> in-progress (DELIBERATELY NOT `done`). Covers Stories 13.1 and 13.2 only; 13.3 is ready-for-dev and BLOCKED on the named human's adjudication (DF-13-2-A, 31 rows all UNADJUDICATED). A FINAL pass is required after 13.3 completes, and epic-13 is NOT rolled up. Document: epic-13-retro-INTERIM-2026-08-17.md. 12 action items (AI-E13-1..-12) appended to the register. The retrospective applied NO re-plan; its one write outside its own document was registering that document in _STATUS_DOCUMENTS (AI-E12-1's second half), after which the full suite was re-run green (1585 passed / 0 failed / 0 skipped, Windows). See development_status[epic-13-retrospective] for the full record. || create-story 13-3: 13-3 backlog -> ready-for-dev. Epic 13's final story is contexted and BLOCKED ON ONE HUMAN ACT: the adjudication record still holds 31 UNADJUDICATED rows and zero judgements, so 13.3's required input does not exist yet (DF-13-2-A). Contexted with three terminal outcomes so a vacuous fold cannot be recorded as a measured shortfall. No threshold amended; two live defects in the flip path and four stale document references recorded for the dev. See development_status[13-3-record-the-result-and-let-it-decide] for the full record. || code-review 13-2 (Sonnet): 13-2 review -> done. CLEAN PASS, independently re-verified by execution (pytest full suite green, mypy clean 84 files, bandit 19/0/0, adjudication-record.json independently parsed: 31/31 UNADJUDICATED rows with no adjudicator id). AC7's HALT ruled a legitimate designed terminal state per the story's own ESCALATION section and the verified 12.9/AC9 precedent, not an unmet AC waved through. Protocol V1.3 amendment checked and found NOT loosened. See development_status[13-2-adjudicate-every-finding-by-a-named-human] and the story file's Review Findings for the full record. || dev-story 13-2 on HEAD 1816524: 13-2 ready-for-dev -> review. The adjudication INSTRUMENT is delivered and the JUDGEMENT is not: 31 blocking findings sit in a committed, append-only, git-tracked adjudication record as UNADJUDICATED with no adjudicator, and the fold reports Unevaluable/residual-31 rather than a number. Three gate-flip defects that let the gate read 'cleared' without a single adjudicated finding are closed additively. AC7 HALTED awaiting XAgent007. || create-story 13-1 on HEAD bc55e36: epic-13 backlog -> in-progress, 13-1 backlog -> ready-for-dev. First story of Epic 13 (Earn the Gate) contexted. Nothing outward-facing performed; git tag -l still empty, origin/master unmoved at bc55e36. See development_status[13-1-decide-what-validation-set-is-then-build-it] for the §0 premise re-measurement, which found four stale citations in the epic's own ACs plus two action items recorded open that are measurably already done (AI-E10-8, AI-E12-1 first half). || PRIOR: retrospective epic-12 (epic-12-retro-2026-08-15.md): epic-12-retrospective backlog -> done. 9/9 stories done; four of the outcome clause's five parts MET and the fifth ("the result is published") NOT - nothing is published and the repository is PRIVATE, re-measured live by read-only git/gh. 14 Epic-12 action items appended to action_items below; six Epic-11 entries updated per the retrospective's section 5 follow-through (AI-E11-2/-3/-5/-10/-12 -> done, AI-E11-1 -> in-progress; AI-E11-4/-6/-7/-8/-9/-11 deliberately left as found). No re-plan applied; development_status[epic-12] deliberately NOT rolled up by the retrospective. || PRIOR: code-review 12-9: review -> done. CLEAN PASS, independently re-verified by execution (pytest 1543/0 failed, mypy clean, bandit 19/0/0, argus/** byte-unchanged, no tag/push/release/visibility-change). See development_status[12-9-release-is-published-and-cites-its-gate] for the full record. AC9's HALT (DF-12-9-A, awaiting human authorisation) is the designed terminal state and does not block this pass. || PRIOR: dev-story 12-9: ready-for-dev -> review. AC1-AC8 COMPLETE, AC9 HALTED pending explicit human authorisation (the intended terminal state; the orchestrator halts before 12.9). Nothing was pushed, tagged, released, made public, listed or uploaded -- re-asserted by execution: git tag -l empty, origin/master unmoved at 00c8d1b, gh release list empty, repository still PRIVATE. Release status: NOT ESTABLISHED. Gates LOCAL: pytest 1543 passed/0 failed/0 skipped, mypy clean 83 files, bandit 19 Low/0 Med/0 High. See development_status[12-9-release-is-published-and-cites-its-gate] for the full record. || PRIOR: create-story 12-9: backlog -> ready-for-dev. THE LAST STORY OF EPIC 12 AND THE ONLY ONE THAT PUBLISHES ANYTHING -- see the development_status[12-9-release-is-published-and-cites-its-gate] entry below for the full record. Measured on de05dec by execution AND by live read-only gh API reads: NO CI run covers the release commit (origin/master is 34 commits behind local master; the newest audit-ci run is 31341363300 at 00c8d1b, 2026-08-09), so the release status is NOT ESTABLISHED and citing that run is forbidden by architecture.md:614-616's own worked example; the repository is measurably PRIVATE, so the documented VCS pin resolves for nobody with or without the tag; DOCS-001-55 guards the tag caveat on README ONLY while the pin sits on three surfaces, one of them added by 12.8 after the guard was written; release.yml's release note is a hand-typed literal nothing checks; 10.1's citation rule cannot see the consumer surfaces; and DF-10-3-A (which names this story) was already resolved by 12.8/AC8 and never closed. 9 ACs: AC1-AC8 need ZERO outward-facing acts; AC9 alone enumerates the seven irreversible/outward acts and IS A HALT REQUIRING EXPLICIT HUMAN AUTHORISATION -- the expected terminal state is AC1-AC8 complete / AC9 halted, per this epic's standing operator instruction that the orchestrator halts before 12.9. || PRIOR: code-review 12-8 iteration 2 (Sonnet 5, RE-REVIEW, final iteration): review -> done, VERDICT PASS -- see the development_status[12-8-the-tool-explains-itself] entry below for the full record. Gates green: pytest --junit-xml tests="1527" errors="0" failures="0" skipped="0"; mypy clean 83 files; bandit 19 Low / 0 Medium / 0 High, byte-identical to iteration 1. || PRIOR: dev-story 12-8 fix iteration 1: in-progress -> review (the single Low review finding on docs/first-run.md's self-description resolved by narrowing the prose to what the guards actually derive; gates green: pytest 1527/0, mypy clean, bandit 19 Low control-matched). || PRIOR: code-review 12-8 iteration 1 (Sonnet 5): review -> in-progress. VERDICT: CONCERNS (one Low finding on docs/first-run.md's self-description; see the 12-8 entry below for the full independent re-verification). || PRIOR: dev-story 12-8: ready-for-dev -> review (the OPERATOR-FACING EXPLANATION SURFACE shipped, and the load-bearing half is AC3's false-green closure: --passes/--skip-pass/--reports now REFUSE an unknown token inside parse_args against the ONE definition of each, so `--passes securty` - measured on 2f84a0b returning RELEASE_READY exit 0 with EVERY detector pass silently disabled - is a typed refusal instead of a fabricated green. generator.py gained the single RENDERED_REPORT_TYPES constant AC3 required (there was none, which is why nothing could validate a report token and why this repo's own argus-student-audit.yml:48 shipped `vacuous-tests`, now corrected). AC8: an argparse usage error returns the reserved 1 in main() only, so a typo no longer publishes verdict=NOT_READY_FOR_RELEASE assessed=true through action.yml; --help still exits 0 and build_parser().parse_args is byte-identical for every guard and for the second invocation surface. AC4/AC5: FR37's render_audit_failed_next_action has production callers for the first time (both CLI arms + the second surface), dispatching per typed class and raising on an unregistered one; DF-8-4-D CLOSED at the WRAP SITE via pipeline.UnexpectedStageError, because pipeline.py was pre-disguising internal defects as expected PipelineErrors and a CLI-only except split could not have closed it. AC6: the NFR-S1 absolute-host-path leak at source_state.py:122 removed and pinned as a property over BOTH streams in every spelling a path can wear. AC7: 12.5's handover wired - the grammar downgrade reaches the DEFAULT run from the SAME renderer via an additive AuditResult field, nothing new persisted; DF-10-4-C surface half CLOSED, class-payload half re-recorded with a reason. AC1/AC2: docs/first-run.md ships, linked, every checkable claim derived by test and registered in both release-surface registries + the -28 corpus by glob; every argument's --help now states its LIVE default, derived by formatter, parity asserted over derive_arguments' closure. TWO guards that could not fail as written were corrected with their new forms proven to bite, and REPORT-002-31's write-point floor was RE-DERIVED (not lowered) after the render loop. NFR-M1 forced 12.7's cohesion-split remedy -> tests/test_help_contract.py, no line shaved, no population narrowed. GATES: pytest 1524 passed / 3 failed - all three the known DF-10-4-D artifact-currency class (test_dogfood_plan.py x1, test_dogfood_proof.py x2) awaiting the commit-then-regenerate bootstrap this story may not perform; mypy argus clean on 83 files; bandit control-matched with ZERO new findings after two constants were renamed off the ...TOKEN suffix rather than suppressed (no nosec anywhere). Published NOTHING - 12.9's fence held: no tag, no index upload, no release, no release-status claim). PRIOR: create-story 12-8: backlog -> ready-for-dev (the operator-facing explanation surface - first-run page, --help parity, operator-error diagnosis; owns DF-8-4-D and DF-10-4-C. See the 12-8 entry below for the measured inventory: eight SILENT operator-error surfaces including a --passes typo that disables every detector pass and returns exit 0, FR37's AUDIT_FAILED next-action renderer with zero production callers, an absolute-host-path leak on the CLI failure line, and an argparse usage error exiting 2 which action.yml publishes as a NOT_READY_FOR_RELEASE verdict). PRIOR, same date: code-review 12-7 iteration 1 (Sonnet 5): review -> done. VERDICT: PASS. Full independent re-verification detailed at the 12-7 entry above (same date, same review) - pytest 1505/1505 green (both commits applied, DF-10-4-D two-step confirmed complete), mypy clean, bandit control-matched with zero findings in the new modules, a real wheel+sdist build confirmed the assets ship in both (91/90 entries), the three corrected guards reasoned through against their attacks, no live adapters/ reference remains, the FR34 disclosure confirmed rendered-not-committed, and the invocation-contract split confirmed no assertion dropped. PRIOR, same date: dev-story 12-7: in-progress -> review (RESUMED run; FR35 half two shipped - packaged command assets in wheel+sdist, `argus install-commands`, three commands proven through the real parser, published set == shipped set on every surface, and TWO never-executed guards corrected rather than satisfied. pytest 1499 passed / 6 failed, all six the DF-10-4-D artifact-currency class awaiting the commit-then-regenerate bootstrap; mypy clean; bandit matches control). PRIOR, same date: create-story 12-7: backlog -> ready-for-dev (FR35 half two, the packaged command assets + the documented install step 12.6 deferred by name; see the 12-7 entry below for the measured inventory of promised-but-absent commands, the three disagreeing published lists, and the two never-executed guards it must correct). PRIOR, same date: code-review 12-6 iteration 1 (Sonnet 5): review -> done. VERDICT: PASS. Full independent re-verification detailed at the 12-6 entry above (same date, same review). PRIOR, same date: create-story 12-6: backlog -> ready-for-dev (FR35 half one, the MCP stdio server; see the 12-6 entry below for scope, the two protocol decisions and the five committed gates it falsifies). PRIOR, same date: code-review 12-5 iteration 1 (Sonnet 5): review -> done. VERDICT: PASS. Independently re-derived on disk, not transcribed: python -m pytest -q 1477 passed / 0 failed / 0 error / 0 skipped (run twice, includes test_dogfood_plan.py/test_dogfood_proof.py both GREEN now that the 54b96d7 regeneration commit landed); mypy clean 75 source files; bandit 19 Low / 0 Medium / 0 High, matches claimed baseline exactly; uv lock --check resolved 81 packages, lock consistent/not stale; line counts re-measured under NFR-M1 1200 cap (generator.py 909, plain_english.py 543, test_grammar_runtime_validation.py 1148, test_spec_claim_scope.py 607, test_release_surface_honesty.py 483, architecture.md 1121). AC1/AC3: pyproject.toml [languages] alias diffed by hand against [project.dependencies] - specifier-for-specifier IDENTICAL for all 9 grammars; uv.lock carries them unconditionally plus extra=='languages'-marked duplicates. AC2: _render_grammar_downgrade_section and _render_readability_warning read side by side - mutual exclusivity holds on every path (nothing parsed -> only 10.4's callout; something parsed + a downgrade -> only the new section; something parsed + no downgrade -> correctly NEITHER fires, nothing to disclose). Minor note: the Dev Agent Record's/this file's own prior "exactly one of the two renders per run" phrasing is a slight overclaim vs. the code comment's own "at most one / never both" - DISMISSED as noise, appears only in internal dev-record prose, never in README/CHANGELOG/architecture.md. AC4: git log --all -- tests/test_default_install_languages.py is EMPTY - the file was never committed (untracked partial-start artifact), so nothing was lost in the fold into TC-ArgusAgent-DOCS-001-61. architecture.md restoration confirmed: both open-decision paragraphs present in STRUCK form (~~...~~) with dated reasoned resolutions, not deleted (§3.4). argus/cli.py scope claim confirmed: git diff 41b8024..HEAD -- argus/cli.py is empty, and epics.md:2431 does name "missing grammar" operator-error diagnosis as Story 12.8's AC by name. No High/Medium finding, no unresolved decision-needed/patch item, all four ACs independently verified met. See story's Review Findings section.  # PRIOR: dev-story 12-5: ready-for-dev -> review (NFR-P3 packaging + point-of-downgrade disclosure).  # code-review 12-3 iteration 1 (Sonnet 5): review -> done. VERDICT: PASS. Independently re-derived on disk, not transcribed: bare python -m pytest 1466 passed / 0 failed / 0 error / 0 skipped; mypy clean 75; bandit content-diffed against a fresh detached 58c8f6b worktree via my own JSON parse = 19/19, 0 new, 0 removed, identical sets; argus audit . = RELEASE_READY deep_ratio=65/181 blocking_findings=0 assessed_deep_ratio=65/81 scope=application held_out=100 exit 0 (byte-identical to the claim); a live cold/warm smoke run outside the test suite confirmed one .argus/cache/*.json slot written on run 1, not duplicated on run 2; git diff --numstat 58c8f6b HEAD -- deferred-work.md = 203/0 (append-only proven); git tag -l EMPTY, origin/master unmoved at 00c8d1b, HEAD d040cad; fab36e7 confirmed an ancestor of HEAD and git diff --quiet fab36e7 HEAD -- argus/ EMPTY (dogfood regeneration current, renderer-only); README/CHANGELOG figure corrections (74->75 modules, 79->80 wheel, 78->79 sdist) matched the artifact exactly. CENTRAL VACUITY QUESTION RE-DERIVED WITH MY OWN MUTATION, NOT THE DEV'S: patched MemoStore.lookup_stage to unconditionally return None (permanently-cold cache) and re-ran -81/-82 - both went RED for the stated reason, then reverted cleanly; separately read -82 end-to-end and confirmed it reads the key off the LIVE slot filename, writes through the REAL store, spies to prove zero recomputation, and asserts the verdict moves off RELEASE_READY with blocking_finding_count >= 1 - not vacuous. THREE ADJUDICATED REDS: (1) test_grammar_runtime_validation -125's _wipe_memo_cache addition - read the diff in full, no assertion touched, reasoning sound (in-process monkeypatch drift cannot move the 10.2 per-grammar key) - RULED legitimate fix, not weakened. (2) test_secret_containment isolation-collection failure - independently reproduced the ModuleNotFoundError in isolation, independently confirmed byte-identical to the 58c8f6b blob and that argus/cartridges never existed via git ls-tree - RULED pre-existing disposition correct (fixing it here would mix concerns, matches the DF-12-2-E precedent), but severity 🟢 UNDERSOLD - it is the NFR-S1 secret-containment CI-blocking suite and the failure mode is a silent collection error; recommended the Epic-12 retrospective give it a firmer owner than "next toucher." (3) The 7 dogfood currency guards red-by-design on the population move - remedy independently verified sanctioned (separate renderer-only commit, ancestor-verified provenance, README/CHANGELOG corrected to measured figures). SS8: no SOLID/DRY/coupling violation found; the MEMO_STORE_SCHEMA_VERSION 1->2 bump is paired with an explicit tested version-refusal in lookup_stage (the correct migration strategy for an advisory cache, needing no separate migration story given 0 persisted entries at bump time); two disclosed departures from generic best practice (D-1 over-keying, D-6 literal duplication for NFR-S6) are deliberate, reasoned and guarded by tests - accepted, not findings. Both dev-flagged open questions (AC5.4 scope reading, D-2 tool-version fold) adjudicated: AC5.4's own guidance text sanctions option (b); D-2 is a defensible correctness improvement under FR27's own wording. ONE non-blocking [Review][Decision] item recorded in the story (DF-12-3-C severity). No unresolved decision-needed/patch item, no unresolved High/Medium issue, all ACs met, tests/mypy/bandit/publication-fences all independently re-run and green. CI evidence: NOT ESTABLISHED (LOCAL, Windows/CPython 3.11.15). Prior dev-story note follows. # dev-story 12-3 (Opus 5): 12-3-a-re-run-returns-the-recorded-result in-progress -> review at HEAD d040cad (3 commits: fab36e7 feat, c9603ae chore(dogfood) renderer output only, d040cad docs). Suite 1466 passed / 0 failed / 0 error / 0 skipped. NOTHING PUBLISHED. Prior create-story note follows. # create-story 12-3 (Opus 5): 12-3-a-re-run-returns-the-recorded-result backlog -> ready-for-dev. Story file: stories/12-3-a-re-run-returns-the-recorded-result.md. Seven ACs, eight tasks. BASELINE RE-MEASURED BY EXECUTION ON THIS TREE, NOT TRANSCRIBED: bare python -m pytest = 1441 passed / 0 failed / 0 error / 0 skipped in 137.21s (NO sanctioned red); HEAD 58c8f6b, 15 ahead of origin/master UNMOVED at 00c8d1b, git tag -l EMPTY, NOTHING PUBLISHED; git ls-files -- argus = 74; argus audit . = RELEASE_READY deep_ratio=64/177 blocking_findings=0 assessed_deep_ratio=4/5 scope=application held_out=97 exit 0; dogfood artifacts CURRENT (provenance 7074c31 confirmed an ancestor of HEAD, git diff --quiet 7074c31 HEAD -- argus/ EMPTY; 74 files / 21569 LOC / 3 units / 70 cut edges; partition_ids 1a31dc9a9559, 619a713d53ca, aaec0673cdcf); .argus/cache/ holds ZERO files and .argus/ is gitignored with 0 tracked, so the zero-migration premise HOLDS; argus/pipeline.py is 1007 lines (193 of headroom, NOT the 256 that 12.1 left - 12.2 spent 63). AI-E10-3 FIRED 2-OF-7 PLUS ONE FALSE LEDGER TRIGGER. HELD: memo_store exists and pipeline.py imports no argus.cache module; derive_cache_key has ZERO production callers; no persisted cache entry to migrate; CACHE_KEY_SCHEMA_VERSION already corrected to "3" by 10.2. STALE (a): key.py:100 and architecture.md 395-396 say pipeline.py imports neither argus.cache.key nor memo_store and infer "no production caller" - Story 12.2 made argus.cache.key PRODUCTION-IMPORTED (argus/audit/deep_audit.py:49, argus/index/ast_index.py:66) and it IS in the static import closure from argus.cli today; the substantive claim survives (reachable != derives-a-key) but the sentence no longer describes the tree - AC7.4 corrects both sites. STALE (b): DF-12-1-B records tests/test_v1_commitment_closure.py at 1308 lines; measured 1419. THE FALSE TRIGGER, PROVEN BY EXECUTING THE GUARD'S OWN CODE RATHER THAN READING IT: DF-12-1-B states that wiring the memo store "flips a library-seam disposition and turns it red". IT DOES NOT. FR27 is disposed delivered-differently (test_v1_commitment_closure.py:447), and reachability_refutations refutes only wired-over-unreachable and library-seam-over-reachable - run directly, FR27 + memo_store REACHABLE returns NO REFUTATION while the library-seam control correctly fires. So after wiring, the delivery registry would go on asserting "the memoization MECHANISM is unwired ... deferred to Story 12.3" with NO test in the repository able to notice it is false - a disposition outliving the fact it disposed, arriving through the one door -35/-37 left open. AC6.3 closes BOTH halves (correct the entry AND close the refutation hole). SCOPE DECISIONS RULED AT CREATE-STORY TIME with rationale in the story's DN-1..DN-8: hook wraps the DETERMINISTIC detect/grade stage unit-level (per-file keying is a key-SHAPE change = the CACHE_KEY_SCHEMA_VERSION migration the 10.2->12.3 ordering exists to avoid); memoized payload carries entries+findings+candidates with MEMO_STORE_SCHEMA_VERSION "1"->"2" (findings-only memoization re-runs the same loop, saves nothing, and is a VACUOUS wiring every byte-identity test would pass); NO CLI flag (invocation contract LOCKED by 10.3, no requirement asks for one, and memo_store's own invariant makes an override unnecessary for correctness); CACHE_KEY_SCHEMA_VERSION stays "3". THE --deep-audit QUESTION SETTLED BY MEASUREMENT, NOT ASSERTION: the deep pass runs at pipeline.py:416-430 INSIDE _assemble_and_persist, DOWNSTREAM of the hook, so a hit serves no LLM-derived byte; the cache key's model_checkpoint/prompt_template_version are FIXED SENTINELS (v1-heuristic-no-llm / v1-no-prompt-template) that do not vary with the model actually dispatched, while the deep pass runs under DEEP_PROMPT_TEMPLATE_VERSION=argus-deep-v1 against a model resolved from ARGUS_LLM_MODEL/OLLAMA_MODEL/gpt-4o-mini - NEITHER reaches the key; build_closure_from_recording, the ONLY function that substitutes the captured checkpoint, has ZERO production callers. RULING: memoization is SCOPED to the deterministic stage and the exclusion is FENCED by a committed guard (AC6.1), because a future change that memoized deep output under this key would let two runs against two DIFFERENT MODELS collide on the SAME slot - exactly what key.py says it exists to make impossible; and because caching DF-12-2-D's empty-response degradations is "memoization caches errors -> reproducibility != correctness" by name. HONEST CONSEQUENCE DISCLOSED, NOT IMPLIED AWAY: PRD 501 (FR36's "determinism is preserved by the FR27/NFR-D1 memoization path") is NOT delivered by this story - under --deep-audit the guarantee holds for the deterministic component and NOT the deep one - filed by AC6.4 and disclosed by AC6.5 with a note for 12.4. PROPERTY ANALYSIS (the orchestrator's question, answered): 12.3's property is STRICTER than and NOT covered by either predecessor - Epic 3.5's TC-ArgusAgent-PIPELINE-001-37 varies the TREE and both runs execute the SAME code; 12.2's AC2.4 varies the BINARY and both runs execute the same code; 12.3 varies NOTHING and run 2 takes a DIFFERENT code path (serve vs compute), which is a claim about an equivalence between two mechanisms that no existing test makes. Measured: argus.cache.memo_store is NOT in the 58-module static closure from argus.cli, and NO test in this repository exercises a cache read or write on the pipeline path. VACUITY GATE (AI-E11-1, ninth consecutive story): a determinism guard is the MOST prone of all because THE PROPERTY IS ALREADY TRUE - a re-run byte-identity test passes on this tree today with no cache in existence. Five shapes named in the story; FOUR CONTROLS MANDATORY: prove the HIT (spy proving the detect stage did not execute), THE POISON POSITIVE CONTROL (a validly-enveloped, integrity-correct, schema-valid but DIFFERENT entry must CHANGE the verdict - the only assertion a permanently-cold cache cannot pass), red-first with the FINAL code against a mutation of the real seam, and closures over live structure never hand-lists. LEDGER RULED: DF-12-1-B IN but RESHAPED (its substance - the FR27 correction + refutation hole - is AC6.3; the 1200-line exemption stays, re-recorded with the corrected 1419 figure and the corrected trigger); DF-12-1-A OUT with the same measured reason 12.1 and 12.2 gave, but ESCALATED to the Epic-12 retrospective rather than re-homed a third time; DF-12-2-D OUT and NOT re-homed (owner/target already assigned to 6.2-style claim-grammar work) but CITED as load-bearing context for the deep-audit ruling; DF-12-2-E OUT unless the AC6.1 fence edits deep_pass.py, in which case its trigger fires and must be ruled; DF-10-2-A OUT - this would be the FOURTH consecutive story, already escalated by 12.2 to the Epic-12 retrospective with owner OPERATOR (XAgent007) per AI-E11-7, LEFT FLAGGED and deliberately NOT re-homed; DF-12-1-D hazard LIVE even if its trigger does not fire (the NFR-M1 sweep reads the git INDEX, so a new module must be git add-ed early); DF-12-1-E to be checked and recorded (three argus/pipeline*.py siblings today). MEASUREMENT TRAPS CARRIED INTO THE STORY so they are not re-bought: run pytest BARE (pytest.ini's own -q makes -qq and SILENTLY suppresses the summary at exit 0); core.autocrlf=true means a working-tree byte compare disagrees with a git-blob compare - state which you made; use a SHORT worktree path on Windows; .argus/ is gitignored so assert over the filesystem not git. FENCES CARRIED: nothing published (12.9 alone), no dogfood artifact hand-edited, no egress and no real network call in any test, deferred-work.md append-only. CI evidence: NOT ESTABLISHED - every figure LOCAL Windows/CPython 3.11.15, AI-E10-1's dated acceptance CARRIED FORWARD and NOT re-taken. || PRIOR: code-review 12-2 iteration 2 (Sonnet 5): review -> done. VERDICT: PASS. Narrow confirmation of the fix round (cap 2, not a full re-review) - did not relitigate AC5/AC5.4, the advisory-not-blocking degradation design, the two removed asserts, AC2.4's base byte-identity, the append-only ledger mechanics, the publication fences or bandit-zero-new (already independently re-derived and passed at iteration 1). Independently re-verified on disk, not transcribed: python -m pytest (bare) 1441 passed / 0 failed / 0 error / 0 skipped (142.36s); mypy Success on 74 source files; bandit 19 Low / 0 Med / 0 High, test_id set {B105x6, B603x5, B404x4, B607x4} diffed against a FRESH worktree checked out at 2bea92f (not the story's own prior figures) - byte-for-byte the same 19 findings, zero new, zero removed; argus audit . = RELEASE_READY deep_ratio=64/177 blocking_findings=0 assessed_deep_ratio=4/5 scope=application held_out=97 exit 0; git ls-files -- argus = 74. git diff --quiet 7074c31 HEAD -- argus/ empty and 7074c31 confirmed an ancestor of HEAD - the regeneration commit 58c8f6b touches only the three dogfood doc artifacts, nothing under argus/. git diff 64164bd 7074c31 --stat matches the story's own File List for the fix round EXACTLY: CHANGELOG.md, deferred-work.md (+84), argus/audit/deep_pass.py (docstring-only, verified by reading the diff - no logic changed), tests/test_deep_pass_wiring.py, tests/test_v1_commitment_closure.py - no undisclosed file moved. Ledger append-only: git diff --numstat 2bea92f HEAD -- deferred-work.md = 250/0 cumulative; git diff --numstat 64164bd HEAD = 84/0 this round - both proven programmatically. Publication fences intact: git tag -l empty, origin/master = 00c8d1b, git rev-list --count 00c8d1b..HEAD = 15. THE CENTRAL QUESTION - is TC-ArgusAgent-AUDIT-001-73/-74's discrimination real or asserted noise - was attacked with TWO SELF-CONSTRUCTED MUTATIONS, independent of the ones the story names: (1) edited open_llm_adapter.py::_dispatch_httpx's success branch to carry the response's actual message.content onto structured_output (built the 'real fix' shape by hand, not a literal revert-diff) - -73 FAILS exactly as designed ('OpenLLMAdapter now populates structured_output - DF-12-2-D IS CLOSED'), -74 stays green; reverted with git checkout --, git status --porcelain -- argus/ clean after. (2) edited deep_pass.py::_claim_is_ast_grounded to unconditionally return False - -74 FAILS exactly as designed (delivered_count=0, reasons=('claim-ungrounded',)), -73 stays green; reverted clean. BOTH DIRECTIONS INDEPENDENTLY REPRODUCED - the discrimination is real. Also independently confirmed by reading _resolve_dispatcher and pipeline.py that -73's 'no injected port' claim is literal (with port=None and an endpoint configured, the REAL unmodified OpenLLMAdapter is constructed, not a second fake), and that .invalid (RFC 6761) genuinely does not resolve on this host (socket.gethostbyname raises gaierror) - the 'two independent fences' claim is literally true. RULED SOUND: the NOT-VACUOUS/adapter-limited/fail-safe framing (read _dispatch_one directly: REASON_EMPTY_RESPONSE fires before grounding is ever consulted); option (a)'s five-finding rejection (read as an argument, not transcribed - genuinely grounded in a real NFR-S1 security contract and a real AR5 provenance mechanism, not scope-avoidance); the -69 vacuous-assert removal (confirmed removed, confirmed the property is proven more strongly by PIPELINE-001-11 in a fresh subprocess, searched all changed files for any surviving 'or True' shape - none found); DF-12-2-E's re-deferral (env-var lists confirmed byte-for-byte identical, drift confirmed safe-side). AC2.4 INDEPENDENTLY RE-DERIVED FROM SCRATCH (not merely re-read): reproduced with two clean synthetic repos, first across different paths (which reproduced a single differing .argus/state/*.json filename - confirmed to be an artifact of MY OWN methodology via git status on the reused directory, not the code), then correctly with an IDENTICAL repo path and IDENTICAL --report-dir path, sequential clean runs across a fresh 2bea92f worktree vs HEAD: stdout, stderr, exit 0/0, the full report tree, AND the .argus/ state directory (same 9 filenames) all byte-identical - the claimed caveat elimination CONFIRMED. ONE LOW, NON-BLOCKING FINDING: the Dev Agent Record's claimed sha256 round-trip states deep_pass.py 6cc79bf3...3ff7; independently recomputed under raw bytes, git blob hash, and LF/CRLF text-mode variants - NONE match (actual 47eadfad9683...8daa2 / blob 531a9c2772f0...33cca); open_llm_adapter.py's paired figure (4a820e77...97ca) DOES match exactly. Looks like a transcription error in the completion note, not an unreverted mutation - git status --porcelain -- argus/ is clean at HEAD and the underlying revert-clean property was independently re-verified via my own two mutation cycles above. Not blocking; correct if this note is touched again. PROCESS RULING: the story file (.../stories/12-2-deep-audit-is-wired-opt-in-and-honest.md) is confirmed UNTRACKED in git (git ls-files --error-unmatch fails on it) while the sibling 12-1 story file is tracked with its own commit history. RULED: this is a process note for the Epic-12 retrospective, NOT a blocker to done - nothing in this story's ACs, its DoD, or the project's stated gates requires the story markdown file itself to be git-tracked; the artifacts carrying the delivered behaviour and its evidence (code, tests, CHANGELOG.md, architecture.md, deferred-work.md) are all tracked and committed. DISPOSITION: PASS - no unresolved decision-needed/patch finding, no unresolved High/Medium issue, tests/mypy/bandit/self-audit all green and independently re-run (not carried forward), AC2.4 re-derived from scratch, publication fences intact. CI evidence: NOT ESTABLISHED (LOCAL, Windows / CPython 3.11.15). || PRIOR: dev-story 12-2 FIX ROUND iteration 1 (Opus 5): in-progress -> review. 1 of 1 blocking review findings resolved (DF-12-2-D); DF-12-2-E re-ruled and left deferred with reasons. THE DECISION OF THE ROUND - is the wiring VACUOUS? RULED NOT VACUOUS: adapter-limited and fail-safe. Option (a) (populate structured_output in the adapter) was MEASURED and rejected on five findings, not assumed: (1) _build_messages never asks the model for structured output - there is no claim grammar and nothing well-formed to parse; (2) structured_output's own contract in ports.py is claim/locator-shaped strings, NEVER raw prompt/response bytes (NFR-S1), so tipping completion text in is a documented contract VIOLATION, not a fix; (3) tests/test_open_llm_adapter.py asserts structured_output == () with the explicit rationale 'NFR-S1 - metadata only', so closing it requires re-baselining a committed NFR-S1 SECURITY assertion on the product's only egress path; (4) doing it honestly needs a claim grammar + prompt contract + redacting parser + a DEEP_PROMPT_TEMPLATE_VERSION bump, which is an AR5 cache-key closure input; (5) it could not be validated (Sec 0.3 forbids live dispatch; CI evidence NOT ESTABLISHED) and deep_audit.py and _claim_is_ast_grounded BOTH already name full claim-grammar grounding as Story 6.2's. Option (b) SHIPPED. Why not vacuous, asserted rather than argued: a vacuous guard is green when it should be red (over-claims); this is the mirror image. TC-ArgusAgent-AUDIT-001-73 asserts the egress path GENUINELY FIRES - the real pipeline builds the real adapter and the request really reaches the transport, with the right per-file targets and URL - so only the FAVOURABLE outcome is unreachable, and FR36's 'never produces a false deep claim' is made UNCONDITIONAL by the gap rather than weakened by it. TC-ArgusAgent-AUDIT-001-74 is the POSITIVE CONTROL required by this story's own Sec 0.6/AC3.3 rule: same adapter, same substituted transport, same response bytes, same real pipeline, same repo, the ONLY change being the discarded completion carried onto structured_output - and it DELIVERS. So the gap is exactly one field wide, it is the adapter's, and everything wired downstream of the port is proven on real provider-shaped input rather than only on a purpose-built fake. Both landed RED-FIRST WITH THE FINAL COMMITTED CODE, BOTH DIRECTIONS: mutating _dispatch_httpx to populate structured_output fails -73 while -74 stays green; mutating _claim_is_ast_grounded to return False fails -74 while -73 stays green; each mutation reverted and the file sha256 round-tripped (open_llm_adapter.py 4a820e77..97ca, deep_pass.py 6cc79bf3..3ff7). -73 is DESIGNED TO GO RED the day DF-12-2-D closes, with a failure message that says so, so the record cannot rot. HONESTY MADE EXPLICIT at all three sites a reader would be misled: the deep_pass.py module docstring, the FR36 _Delivery note WHERE THE WORD 'wired' IS ASSERTED (now scoped: wired means the seam is reached and the safety properties hold on it, NOT that the capability completes end to end through the shipped adapter), and a user-facing KNOWN LIMITATION in CHANGELOG.md telling an operator not to pay a provider for depth they will not get. Also removed 'assert ... or True' from -69 - an assertion that cannot fail, i.e. a vacuous guard, in the story chartered against vacuous guards; the property is held more strongly in a fresh subprocess by TC-ArgusAgent-PIPELINE-001-11. NO assertion was weakened, deleted or re-baselined. GATES RE-RUN, NOT CARRIED: python -m pytest (bare) 1441 passed / 0 failed / 0 error / 0 skipped (142.03s; 1439 -> 1441, +2); mypy Success on 74 source files; bandit 19 Low / 0 Med / 0 High, content-diffed against a 2bea92f worktree = 0 new, 0 removed, IDENTICAL SET (B105x6 B603x5 B404x4 B607x4); argus audit . = RELEASE_READY deep_ratio=64/177 blocking_findings=0 exit 0; git ls-files -- argus = 74 (unchanged); dogfood total LOC 21530 -> 21569 (+39, all partition 2) and ALL THREE partition_ids UNCHANGED (1a31dc9a9559 / 619a713d53ca / aaec0673cdcf); ledger append-only +84/0 this round, +250/0 vs 2bea92f. AC2.4 RE-DERIVED with iteration 1's one caveat ELIMINATED rather than attributed: the differing .argus/state record was first shown to differ in EXACTLY ONE LINE (report_dir), then the confound was removed by re-running both binaries against the SAME repo with the SAME --report-dir - stdout+stderr byte-identical, exit 0 both, all 2 report files and all 9 .argus/ files byte-identical with identical filenames. Commits: 7074c31 (fix) -> 58c8f6b (three dogfood artifacts regenerated THROUGH THEIR OWN RENDERERS at provenance 7074c31, merge-base ancestor-verified, argus/ diff empty, no hand-edits). The regeneration commit message was amended on the local unpublished tip because it stated 21540 where the artifact's prior figure was 21530 - corrected rather than left as an untrue committed figure. NO EGRESS OCCURRED: the two new tests substitute httpx.Client AND point at a .invalid host (RFC 6761), two independent fences. NOTHING PUBLISHED: git tag -l empty, origin/master 00c8d1b unmoved, HEAD 15 ahead, no push/tag/release/workflow_dispatch/index upload. New variance recorded: tests/test_deep_pass_wiring.py now 1104 lines (96 under the NFR-M1 cap) and tests/test_v1_commitment_closure.py 1412 -> 1419 (DF-12-2-C's figure now stale by 7; NOT edited - the ledger is append-only - the true number is in the story for 12.3 to size against). CI evidence: NOT ESTABLISHED (LOCAL, Windows / CPython 3.11.15). || PRIOR: code-review 12-2 iteration 1 (Sonnet 5): review -> in-progress, verdict CONCERNS. Independently re-derived on disk, not transcribed: python -m pytest (bare) 1439 passed / 0 failed (141.29s); mypy clean on 74 source files; bandit 19 Low / 0 Med / 0 High, and a byte-for-byte diff of the bandit JSON against a 2bea92f worktree shows the IDENTICAL 19 findings (same test_id/file/line/text) - ZERO new, confirmed by content not count. git ls-files -- argus = 74; 8a0bebc confirmed an ancestor of HEAD; git diff --quiet 8a0bebc HEAD -- argus/ empty. git tag -l empty; origin/master = 00c8d1b, unmoved. git diff --numstat 2bea92f HEAD -- deferred-work.md = 166 insertions / 0 deletions (append-only proven programmatically). AC2.4 byte-identity RE-DERIVED independently on a fresh synthetic repo across a 2bea92f worktree vs HEAD: stdout, stderr, exit code and every rendered report file byte-identical; the sole .argus/state/*.json filename difference was fully attributable to the different --report-dir paths used for the two runs. All three named judgement calls ADJUDICATED CORRECT: (1) AC5/AC5.4's re-grade-to-audited_shallow-via-grade_entry(claim_present=False) reading verified against the function's own code and confirmed live (a --deep-audit run with no provider configured against an otherwise-RELEASE_READY repo returns INSUFFICIENT_COVERAGE, deep_ratio=0, exit 3 - the downgrade demonstrably moves the FR16 input, no row/threshold/exit-code mapping touched); (2) advisory/non-blocking degradation findings verified structurally (is_verdict_blocking keyed on depth_supported, which _degradation_finding sets None) AND behaviourally (the adversarial repo the review brief asked for was constructed and run: the coverage downgrade IS the only signal and the operator is NOT misled - verdict/summary/depth-meaning callout all state the deep pass was requested but not completed); (3) the two removed production asserts left nothing load-bearing missing (mypy clean, explicit control flow in place, bandit-diff re-run confirms the RULE_DEGRADED_DEEP_READ rename held the security-scan delta at zero). ONE SUBSTANTIVE FINDING, not previously filed: [Medium, non-blocking] the deep pass's 'delivered' branch is provably unreachable through the real, unmodified OpenLLMAdapter - neither _dispatch_litellm nor _dispatch_httpx ever populates LLMRecording.structured_output (confirmed by reading both methods, independently corroborated by the PRE-EXISTING tests/test_open_llm_adapter.py:208 assertion that a real dispatch returns structured_output == ()), so deep_pass.py::_dispatch_one routes every real dispatch - success or failure - to REASON_EMPTY_RESPONSE before grounding is even checked. Fails safe (AC5.1's own 'malformed/empty response' mode is exactly what fires; no false claim is possible) and violates no written AC (every AC and test in this story is, correctly per Sec 0.3, restricted to injected fakes), but the positive half of the wired capability is dead code against the shipped adapter and this gap was not measured or filed alongside DF-12-2-A/-B/-C. Filed at review as DF-12-2-D (a [Review][Patch] item: file the DF entry + add a mocked-transport test) and DF-12-2-E (Low, PROVIDER_ENDPOINT_VARIABLES duplicates the adapter's _api_base derivation instead of deriving it - the [Review][Defer] item, already appended to deferred-work.md). Everything else reviewed and found sound: CLI wiring, derived forbidden-surface/entry-point populations, the AC2.3 environment gate, the AC2.5 ordering proof, the AC3 positive control, the FR36 not_built_refutations closure, the DOGFOOD_EXTERNALIZATION_GUARD repair, and every ledger ruling - no vacuous guard found among the ones this story added, no other engineering-principle violation found. VERDICT: CONCERNS (one Medium disclosure/filing gap on a pre-existing adapter limitation, not a defect in the code this story wrote; core delivery, safety properties and every measured claim otherwise hold). See the story's Review Findings section. || PRIOR: dev-story 12-2 (Opus 4.6): ready-for-dev -> review. FR36 DELIVERED: the deep pass is wired behind `--deep-audit` (store_true, default False, registered in the LOCKED invocation contract) — NOT a --passes token, NOT an env var, NOT a packaging extra, all three foreclosed by measurement. The LIVE FALSE DEEP CLAIM was reproduced on 2bea92f and closed RED-FIRST end-to-end with the final committed test code: the disclosure is now derived from WORK PERFORMED (three honest states), not from a CSV token. The seam is reached only via the injected LLMDispatchPort through a FUNCTION-LOCAL import — statically reachable (FR36 flipped not-built -> wired, PROVEN by the live import closure) yet runtime-inert (NFR-S6) — and AC3's POSITIVE CONTROL landed in the same change because that green is otherwise obtained by not executing. Forbidden-surface and entry-point populations DERIVED (the hand-written ones had omitted open_llm_adapter.py, the only module that can open a socket, and two thirds of the pipeline family); non-vacuity proven by GENERATING a new argus/audit module. Env-var population derived by ast from the adapter's own os.getenv calls (6). Honest degradation over a typed-error surface derived from the class tree, driven through the REAL pipeline entry point; an unconfigured provider REFUSES TO CONSTRUCT an adapter, so the fabricating _dispatch_httpx branch cannot reach the verdict (residual filed DF-12-2-B). Spend through the EXISTING --budget ceiling: no new module/threshold/schema/default. AC6 ANSWERED YES by independent re-derivation (bare `argus audit` reaches NOT_READY_FOR_RELEASE, exit 2, via vacuous_test RULE_AST) and COMMITTED as TC-ArgusAgent-VERDICT-001-30/-31 — the escalation branch does NOT fire, and the externalization guard's causal claim is refuted and repaired (honesty language strengthened, not softened). AC7.2's not_built_refutations FIRED on the live registry before the flip. Two local commits: 8a0bebc (implementation) + 64164bd (artifacts regenerated THROUGH THEIR OWN RENDERERS at provenance 8a0bebc, verified an ancestor of HEAD with an empty argus/ diff; no hand-edits). Suite 1439 passed / 0 failed / 0 error / 0 skipped (baseline 1418, +21). mypy clean over 74 files; bandit 19 low / 0 med / 0 high — ZERO new. DEFAULT RUN BYTE-IDENTICAL vs a detached 2bea92f worktree: stdout, stderr, exit code and all 14 artifact files (rollup 558c6852...). Ledger append-only proven by --numstat (166 insertions, 0 deletions); DF-12-1-A re-recorded to 12.3, DF-12-1-E did not fire, DF-10-2-A RULED OUT and ESCALATED to the Epic-12 retrospective (third consecutive story; needs a dated OPERATOR decision). NO EGRESS OCCURRED — every dispatch went through an injected FakeDispatch, zero tokens, no provider contacted. NOTHING PUBLISHED: git tag -l empty, origin/master unmoved at 00c8d1b. CI evidence: NOT ESTABLISHED (LOCAL, Windows / CPython 3.11.15). || PRIOR: create-story 12-2: backlog -> ready-for-dev. Baseline re-measured by execution on 2bea92f (1418 passed / 0 failed / 0 error / 0 skipped, 135.48s). Three inherited premises STALE (seam at :98 not :91; ZERO production callers; the [llm] extra is NOT an egress gate because httpx is a base dependency). Two live defects reproduced: the '--passes deep' FALSE DEEP CLAIM and the adapter's ambient-env egress absorption. Absorbed 11.2 question ANSWERED YES by measurement (a default invocation reaches NOT_READY_FOR_RELEASE, exit 2, via vacuous_test RULE_AST) - the escalation branch does NOT fire. Four blind guards named. DF-10-2-A ruled OUT and ESCALATED for the Epic-12 retrospective (third consecutive story; needs a dated OPERATOR decision). Nothing published; CI evidence NOT ESTABLISHED. || PRIOR: code-review 12-1 iteration 2 (Sonnet): review -> done. NARROW CONFIRMATION of fix round 1 (cap 2, not a full re-review) — did not re-litigate the pipeline_stages.py extraction boundary, the NFR-M1 sweep's/dogfood-currency closure's non-vacuousness, the append-only ledger check or the provenance-ancestor chain (already independently re-derived and passed in iteration 1). VERDICT: PASS. Independently re-derived Finding 1 with a fresh AST-span extractor over the git blobs (not the working tree): 29 top-level definitions in ca37283:argus/pipeline.py, 16 moved / 13 retained, 0 dropped/duplicated/added, 28 of 29 byte-identical, sole exception _assessment_scope_paths — exact match. Independently reproduced the working-tree-vs-blob trap (binary read off disk is CRLF, the ca37283 blob is LF, so 0 of 13 retained match on a naive working-tree read) and all three replacement digests (e478ec09... / 5ce703e8... / 9d72d850...) by joining moved-then-retained blob bytes in pre-split file order — all three match exactly. Confirmed Finding 2's docstring fix and Finding 4's pipeline_stages.py correction (only a stale .pyc references pipeline_resume.py). Read tests/test_pipeline_split_surface.py in full and attacked it with a SELF-CONSTRUCTED mutation outside the dev's four (addition/reorder/deletion/laundering): a SUBSTITUTION — renamed "resume_audit" to "resume_audit_v2" in argus/pipeline.py's __all__ (same length/position, unlike any of the dev's four) — all three new tests (-14/-15/-16) went RED correctly, reverted with git checkout --, sha256 round-tripped to the exact recorded value (28f11817...). Confirmed no other test references pipeline.__all__, supporting the "0 of 1415 pre-existing tests saw the addition" claim. Confirmed "29 of 29" appears 7x in the story and 2x here, every occurrence a quotation of the original error, none asserting current truth. GATES INDEPENDENTLY RE-RUN: pytest -q 1418 passed in 142.23s, 0 failed/error/skipped; mypy clean 73; bandit 0H/0M/19L; argus audit . = RELEASE_READY deep_ratio=21/58 blocking_findings=0 assessed_deep_ratio=63/79 scope=application held_out=95 exit 0 — byte-identical to the claim; git ls-files -- argus = 73 (unmoved). WRITE-SET CLAIM VERIFIED PROGRAMMATICALLY: git show --stat 2bea92f touches only sprint-status.yaml, the story file, tests/test_classification_word_boundary.py, tests/test_module_size_ceiling.py and tests/test_pipeline_split_surface.py — no argus/, scripts/, README.md, CHANGELOG.md, architecture.md or deferred-work.md touched this round; git diff --numstat ca37283 HEAD -- deferred-work.md still 301/0. PUBLICATION FENCE HELD: git tag -l empty, origin/master unmoved at 00c8d1b, no push/tag/release/workflow_dispatch/index upload. All figures LOCAL, Windows/CPython 3.11.15; CI evidence NOT ESTABLISHED. No High/Medium finding, no unresolved decision-needed/patch item, all ACs met. || dev-story 12-1 FIX ROUND 1 (2026-08-12, Opus): in-progress -> review. ALL THREE filed [Review][Patch] findings RESOLVED, plus a FOURTH of the same class this round found in the story's own write set and the review did not file. NOT PROSE-ONLY - it ships a committed guard. (1) THE BYTE-IDENTITY COUNT WAS RE-DERIVED BY EXECUTION AGAINST ca37283, NEITHER OF THE REVIEW'S TWO NUMBERS TRANSCRIBED: the truth is 28 OF 29, denominator = the 29 top-level definitions of ca37283:argus/pipeline.py (16 moved into argus/pipeline_stages.py + 13 retained; 0 dropped, 0 duplicated, 0 newly added - each count asserted, not eyeballed). The review's '15 of 16 moved' and its suggested '28 of 29' never actually conflicted: 15/16 moved AND 13/13 retained IS 28/29 - it simply never stated the denominator that joins them, and stating it was the fix. BOTH SIDES ARE READ AS GIT BLOBS: this checkout is core.autocrlf=true, so a working-tree read reports a FALSE 0 of 29 (one byte per line) and would have buried the one real change inside 28 artefacts - the first attempt did exactly that. The single exception, _assessment_scope_paths (1962B -> 2206B), is INTENTIONAL: this story's own DF-8-3-C dedup, already disclosed under AC4. Corrected in the story AND in BOTH sprint-status occurrences; the '29 of 29' strings that remain are the REVIEW'S OWN quotation of the error inside its finding text and are deliberately preserved, per the same DOCS-001-59 rule that keeps architecture.md's corrected false claim quoted inside its correction - deleting the quotation deletes the lesson. The digest c6edd6fa9ddd10... is WITHDRAWN, not restated: unreproducible under 48 span/line-ending/joiner conventions against either blob, and a both-sides-equal digest is arithmetically impossible over a set containing a changed body. Replaced by digests whose convention is stated (15 unchanged moved = e478ec09edb501..., 13 retained = 5ce703e8ca2e7b..., all 28 = 9d72d850905c31..., each equal on both sides). (2) PIPELINE-002-11's docstring said 'ENUMERATED FROM GIT' while _pipeline_family_sources() globs the FILESYSTEM - corrected to 'ENUMERATED FROM THE FILESYSTEM' with the DF-12-1-D reason inline so the two docstrings name ONE mechanism; prose only, 4/1, entirely inside the docstring, no predicate or population touched. (3) AC1's 'Pinned by test' HAD NO TEST; it does now: tests/test_pipeline_split_surface.py, TC-ArgusAgent-PIPELINE-002-14..-16, a DEDICATED file because the named DOGFOOD-001-45/-46 precedent is a dedicated file and the alternative homes would have coupled an import-surface pin to a file whose stated single subject is something else. PROVEN RED-FIRST WITH THE FINAL COMMITTED CODE against FOUR mutations of the real seam - addition, reorder, deletion and LAUNDERING (live __all__ and the pinned literal widened together) - with argus/pipeline.py sha256 ROUND-TRIPPED to 28f1181742b7f6... after every one. MUTATION A IS THE FINDING'S PREMISE PROVEN BY EXECUTION: full suite under it = 1418 collected / 1413 passed / 5 failed, and of the 1415 PRE-EXISTING tests exactly TWO went red - the two committed-artifact checks, which saw the added LINE (live LOC moved off the artifacts, i.e. this story's own widened -03/-20 working), NOT the added export. ZERO pre-existing tests saw the __all__ addition. Mutation D is why -15 exists: it re-reads __all__ OUT OF the immutable ca37283 blob by ast, so editing the literal to match a changed surface makes -14 green again and -15 stays RED - the pin cannot be laundered. -16 GENERATES 4n-1 = 27 mutants from the LIVE surface (every deletion, adjacent transposition, addition and duplication) and drives each through THE SAME predicate -14 asserts with, count asserted, never a hand-listed sample - under mutation A it correctly produced 31 for the 8-name surface, which is how it was confirmed to close over live structure rather than a constant. Both directions asserted (true surface ACCEPTED before mutants REJECTED); -15 fails rather than SKIPS when the blob is unreachable. -15 also pins two properties AC1 asserted in prose only: every re-export is the SAME OBJECT as pipeline_stages' (the no-fork property that keeps monkeypatch.setattr(pipeline, '_detect_per_file', ...) intercepting the real call) and none of the 16 private helpers LEAKED into __all__. Two warts in the new file were found by its own red output and NARROWED EXPLICITLY, never quietly: -16's assertion order (it misattributed 'the guard does not bite' when the truth was 'the live surface is wrong') and a -15 assertion whose message described the live surface while its predicate compared the blob to the literal - it now compares the LIVE MODULE TO THE BLOB, which is strictly stronger and is what makes mutation D red. (4) SELF-FOUND, NOT FILED: the MAINT-001 sweep's failure-message remedy told the next reader to follow the 'argus/pipeline_resume.py (Story 12.1) precedent' - a module that DOES NOT EXIST (it is the name SA.1 recommended; the shipped sibling is argus/pipeline_stages.py). Same defect class as findings 1 and 2, inside this story's own new guard, so it was fixed with them (1/1). GATES RE-RUN ON THE FINAL TREE, NOT CARRIED, all LOCAL Windows/CPython 3.11.15: pytest 1418 collected / 1418 passed / 0 failed / 0 error / 0 skipped (+3 = exactly the three new tests, no red anywhere); mypy clean on 73 (unmoved); bandit 0 High / 0 Medium / 19 Low (unmoved); argus audit . = RELEASE_READY deep_ratio=21/58 blocking_findings=0 assessed_deep_ratio=63/79 scope=application held_out=95, exit 0 - a POPULATION move, not a behaviour move: 21/58 IS 63/174 reduced (the Fraction effect this story already documented at 31/39), held_out 94->95 and total 173->174 are the ONE new test file, and assessed_deep_ratio is 63/79 UNCHANGED because a held-out file leaves 174-95=79 assessed and adds no deep-gradable source; verdict and blocking findings IDENTICAL. git ls-files -- argus = 73, UNMOVED - no argus/** file was added, changed or deleted this round, so all three partition_ids (45cbaf975494 / 477ef77d7b65 / 9408a0fe1acf), the unit LOC (14938 / 1341 / 4385), source_file_count 73 and total_loc 20664 are ALL UNMOVED, NO REGENERATION WAS REQUIRED OR PERFORMED, no artifact was hand-edited, and the AC3 currency guard stays green (git diff --quiet c4bd769 HEAD -- argus/ still empty). Write set PROVEN, five files: the new 290-line guard, two prose-only test edits (4/1 and 1/1), the story and this file - argus/, scripts/, README.md, CHANGELOG.md, architecture.md and deferred-work.md UNTOUCHED (git status --porcelain over the first four returns zero lines; deferred-work.md was not opened, so append-only holds trivially). NOTHING WEAKENED: no assertion anywhere deleted, loosened, skipped, xfail-ed or re-baselined; the only two assertions touched were STRENGTHENED, inside the new file. NOTHING PUBLISHED: git tag -l EMPTY, origin/master unmoved at 00c8d1b, no push/tag/release/dispatch/upload. CI EVIDENCE: NOT ESTABLISHED - every figure LOCAL, AI-E10-1's dated acceptance carried forward and NOT re-taken. # PRIOR NOTE FOLLOWS. code-review 12-1 (2026-08-12, Sonnet): 12-1-pipeline-stops-breaching-its-own-limit review -> in-progress, verdict CONCERNS. Independently re-derived on disk: 1415 passed in 361.39s; mypy clean 73; bandit 0H/0M/19L; `argus audit .` = RELEASE_READY 63/173 0 63/79 application 94 exit 0 (byte-identical to claim); deferred-work.md +301/-0 numstat-proven append-only; a9cc933 an ancestor of HEAD and `git diff a9cc933 93adc94 -- argus/` empty, both re-verified; git tag -l empty, origin/master unmoved at 00c8d1b, HEAD 10 ahead — nothing published. AST-diffed all 16 moved definitions against ca37283: 15/16 byte-identical; `_assessment_scope_paths` was intentionally rewritten by this story's own DF-8-3-C dedup, contradicting the story's "29 of 29 byte-identical" claim (Low finding). Two further Low findings filed (a self-contradicting "ENUMERATED FROM GIT" docstring in tests/test_classification_word_boundary.py that actually globs the filesystem; no dedicated `__all__` byte-identity test for AC1's "Pinned by test" claim, though `__all__` is independently confirmed unchanged). No High/Medium defect; core code, tests, guards, ledger and provenance chain are all independently verified correct and non-vacuous. See the story's Review Findings section for the three [Review][Patch] items. # dev-story 12.1 (2026-08-12): 12-1-pipeline-stops-breaching-its-own-limit in-progress -> review. dev-story 2026-08-12: in-progress -> review. Two LOCAL commits, NOTHING PUBLISHED (git tag -l EMPTY, origin/master unmoved at 00c8d1b, no push/tag/release/dispatch/upload). c4bd769 = the implementation; e5a8a88 = the regeneration, a SEPARATE commit of renderer output only, exactly as 93adc94 did for 10.4, under the operator ruling of 2026-08-12. AC1: argus/pipeline.py 1331 -> 944 (256 lines of headroom for 12.2/12.3/12.4) by extracting argus/pipeline_stages.py. THE BOUNDARY IS NOT THE ONE THE STORY RECOMMENDED, and the reason is measured rather than preferred: an ast walk showed the resume family (SA.1's 357-line recommendation) needs ELEVEN names that would stay behind while pipeline.py must keep re-exporting resume_audit from __all__ - a module-level import cycle survivable only by an import-order trick - whereas the derivation stages need THREE, all constants, which moved with them, so the dependency points strictly downward, there is no cycle, and 403 lines are shed. PURITY PROVEN MECHANICALLY: 28 of 29 moved-and-retained definitions byte-identical (figure RE-DERIVED BY EXECUTION in fix round 1 against ca37283, NOT transcribed; denominator = the 29 top-level definitions of ca37283:argus/pipeline.py, 16 moved + 13 retained, 0 dropped / 0 duplicated / 0 added; both sides read as GIT BLOBS because a core.autocrlf working-tree read falsely reports 0 of 29). The ONE exception, _assessment_scope_paths, was INTENTIONALLY rewritten by this same story's DF-8-3-C dedup and is disclosed under AC4. Re-derived digests, convention stated: the 15 unchanged moved bodies e478ec09edb501... and the 13 retained 5ce703e8ca2e7b..., both sides equal; the previously published c6edd6fa9ddd10... is WITHDRAWN - unreproducible under 48 span/line-ending/joiner conventions, and both-sides equality is impossible over a set containing a changed body), __all__ byte-identical, ZERO import sites edited, and monkeypatching argus.pipeline._detect_per_file still intercepts the real call. DF-8-2-A's OWN named remedy was measured INSUFFICIENT (~59 lines vs the >=131 required, leaving ~1272) and that is recorded in the ledger so the next reader does not repeat it. AC2: tests/test_module_size_ceiling.py (TC-ArgusAgent-MAINT-001-01..-05, a NEW verification area) sweeps EVERY tracked .py from git ls-files. LANDED RED-FIRST WITH THE FINAL COMMITTED CODE - 'NFR-M1 breach - argus/pipeline.py (1331 lines, 131 over)' - and with the registry emptied in-process it independently reproduced all four repo-wide breaches (1331/1326/1308/1203 of 169). The three test-file breaches are NAMED, DATED, REASONED exemptions filed as DF-12-1-A/-B/-C with owners and live target stories (12.2/12.3/12.5), never a narrowed population, and the registry SHRINKS (an exemption for a file that is gone OR no longer over the cap fails; argus/pipeline.py is asserted unaddable). Non-vacuity: the enumeration is asserted non-empty and two-sided, the 1200/1201 boundary is driven through the sweep's OWN predicate, and an over-cap adversarial variant is GENERATED from every one of the 173 files in the live population. AC3: THE 0.5 FINDING IS CONFIRMED INDEPENDENTLY - all three artifacts were stale at ca37283 while all five DF-10-4-D assertions were GREEN. tests/test_dogfood_artifact_currency.py (TC-ArgusAgent-DOGFOOD-001-49..-52) closes both directions: an artifact is current iff the sha it cites is a real ancestor of HEAD and git diff --quiet SHA HEAD -- argus/ is empty. RED FOR FREE on the live defect with the final code ('cites a9cc933; argus/ has moved since (7 files changed, 749 insertions, 78 deletions)'), and -52 refutes both degenerate predicates from LIVE HISTORY on every run by classifying every commit reachable from HEAD and requiring both classes to be non-empty. -03 and -20 were WIDENED to the derived figures their docstrings always promised (-20's red was the live 20152 LOC against the published 19783); NOTHING was loosened or deleted. All five committed-artifact failure messages now name scripts/regenerate_dogfood_artifacts.py, a REAL committed entry point that renders through the artifacts' own renderers, asserts each file equals the renderer's return value, and REFUSES to run on a dirty argus/ tree. DF-10-4-D's provenance/enumeration question is DECIDED and recorded: the label now tells the truth (population = git INDEX, descriptor = git rev-parse HEAD) rather than pinning the enumeration, because pinning is a verdict-adjacent behaviour change AC5 forbids - and the guard makes the divergence detectable rather than merely described. AC4: deferred-work.md +301/-0, APPEND-ONLY PROVEN. CLOSED: DF-8-2-A; DF-8-3-C (one partition_application_files helper in argus/detectors/vacuous_test.py called from both former verbatim copies - its recorded coordinates were stale AGAIN, the fourth in three days); DF-8-5-B + DF-10-4-D TOGETHER per DF-10-4-D's own instruction; DF-11-1-A; DF-11-5-A; DF-9-2-C (already true on arrival - zero tracked pycache under argus, verified, no git rm --cached run). RE-RECORDED: DF-8-3-A -> 12.4, its 'no room in pipeline.py' blocker EXPLICITLY DISCHARGED, the remaining reason being scope (a report-content change AC5 forbids by construction). RULED OUT OUT LOUD: DF-10-2-A stays OPEN and UNOWNED (AI-E11-7 wants a dated OPERATOR decision, type (H)); DF-11-4-D/AI-E11-6 re-targeted to 12.4 with its alternative DoD explicitly NOT taken. OPENED: DF-12-1-A/-B/-C (the exemptions), DF-12-1-D (the sweep's git-INDEX blind spot, which bit during this implementation), DF-12-1-E (three pipeline*.py siblings and no layering guard). AC5 BEHAVIOUR PROVEN UNTOUCHED, NOT ASSUMED: the in-place figures necessarily move (63/173, 63/79, held_out=94) because this story ADDS 4 tracked .py files (+2 deep-gradable source, +2 held-out test files), so the two variables were SEPARATED. The NEW code over the PRE-STORY population (a pristine detached worktree at ca37283) returns verdict=RELEASE_READY deep_ratio=61/169 blocking_findings=0 assessed_deep_ratio=61/77 scope=application held_out=92 exit 0 - byte-identical to the Task 0 fixture. An A/B over that one tree, pre-12.1 code vs post-12.1 code with an identical --report-dir, gives 4 BYTE-IDENTICAL report files and 848 BYTE-IDENTICAL .argus/ files, with no caveat and nothing to explain away. ZERO import-path edits were needed. Four test files were touched and every change is a STRENGTHENING: PIPELINE-002-11 went red because code it ast-parses BY NAME moved, and its REACH was widened to the globbed argus/pipeline*.py family rather than its CLAIM narrowed. AC6: DF-11-1-A closed FIRST and ALONE - suite 1405/1404/1 -> 1405/1405/0 BEFORE the extraction, ending a five-story node-id carve-out; registration verified inert (_status_assertions() = 0 for both retrospectives); NO retrospective edited and NO citation minted (that half of the close condition deliberately NOT taken, with the reason in the ledger). architecture.md registers both new rules AND CORRECTS its false claim that NFR-M1 was enforced by file-size CI under a tests/apaa/ directory that does not exist - TC-ArgusAgent-DOCS-001-59 holds both and permits the false sentence only as a quotation inside the correction blockquote. README/CHANGELOG derived figures 72 -> 73 shipped modules (DOCS-001-54: the artifact is the fact); no user-visible claim changed and no release-note section added (SA.6 honoured). REGENERATION: all three artifacts produced by scripts/regenerate_dogfood_artifacts.py at provenance c4bd769, which git merge-base --is-ancestor confirms is an ancestor of HEAD and git diff --quiet c4bd769 HEAD -- argus/ confirms is EMPTY. Every changed line is a DERIVED FIGURE or the new honest label: population 72->73, LOC 19783->20664, X 450->456, cut edges 57->67, critical set 51->52, deep 56/72->57/73, findings 108->111, plus the bundle hash and byte length. Unit count STAYS 3; two of three partition_ids moved to 45cbaf975494 and 9408a0fe1acf - EXACTLY the ids SB.3's simulation predicted for a new argus/pipeline_stages.py. Not one character was hand-edited. The frozen Story-7.2 superseded record was deliberately NOT regenerated. FINAL GATES, ALL LOCAL Windows/CPython 3.11.15: 1415 collected / 1415 passed / 0 failed / 0 error / 0 skipped (1405 baseline + 10 new); mypy clean on 73 source files (was 72, exactly as predicted); bandit 0 High / 0 Medium / 19 Low; argus audit . RELEASE_READY exit 0. git ls-files -- argus = 73; all tracked .py = 173; NO argus file over 1200 (next largest 944). CI EVIDENCE: NOT ESTABLISHED - committing is not evidence, and AI-E10-1's dated acceptance is carried forward, NOT re-taken (re-taking it for Epic 12 is AI-E11-4, the operator's). Publication is Story 12.9 alone.  # PRIOR NOTE FOLLOWS. create-story 12.1 (2026-08-12): 12-1-pipeline-stops-breaching-its-own-limit backlog -> ready-for-dev; epic-12 backlog -> in-progress (first story of the epic). SD-1's HARD STOP IS DISCHARGED: operator XAgent007 ruled 2026-08-12 that for ALL OF EPIC 12 the sequence implement -> commit -> regenerate the dogfood artifacts THROUGH THEIR OWN RENDERERS at a truthful provenance sha -> re-run the gates is PRE-AUTHORISED, and the Epic-11 'no new argus/** file' fence is LIFTED (the same sequence already ruled for 10.4 = commit 93adc94). STILL BINDING: nothing is published (12.9 alone), and hand-editing a dogfood artifact remains forbidden - regeneration is legitimate only through the renderers. AI-E10-3 RE-MEASUREMENT FIRED AGAIN, 4 STALE/FALSE PREMISES ON A 6-OF-6 RUN: (1) DF-8-2-A's OWN named remedy is now insufficient - `_critical_ineligibility` + siblings is ~59 lines against the >=131 required, leaving pipeline.py at ~1272, STILL OVER; (2) the NFR-M1 breach is NOT confined to argus/** - 4 of 169 tracked .py files exceed 1200 (argus/pipeline.py 1331, tests/test_pipeline_signature_demo.py 1326, tests/test_v1_commitment_closure.py 1308, tests/test_grammar_diagnosis.py 1203), so a naive repo-wide sweep is red on four; (3) architecture.md §Enforcement claims NFR-M1 is enforced by 'file-size CI committed under tests/apaa/' - tests/apaa/ DOES NOT EXIST and no workflow has a file-size step, i.e. the architecture asserts a guard never built; (4) THE HEADLINE - all three committed dogfood artifacts are STALE AT HEAD RIGHT NOW (total LOC 19783 -> 20454, cut edges 57 -> 64, unit2 14793 -> 14997, unit3 3660 -> 4127, NFR-C1 ratio 360/19783 -> 60/3409, provenance a9cc933 vs HEAD ca37283) WHILE ALL FIVE DF-10-4-D ASSERTIONS ARE GREEN - the guards' own docstrings claim 'the artifact cannot silently rot away from the generator' and they check only unit count, three partition_id prefixes, sized_ceiling and a verdict token. So DF-8-5-B/DF-10-4-D were UNDERSTATED: the class has a second, SILENT direction, and 'name a regeneration entry point in the failure message' improves a red that today never appears. HELD on re-measurement: pipeline.py 1331/1200 and the only argus file over; git ls-files -- argus = 72; suite 1405 collected / 1404 pass / 1 red (DF-11-1-A only); mypy clean on 72; bandit 0H/0M/19L; dogfood unit 2 = 14997/15000 with 3 lines of slack (and NEWLY measured: 39/40 files, one file of slack). CHANGED: the tracked tree is now CLEAN (Epic 11 committed as ca37283), so AI-E11-5 has taken effect. PARTITION SIMULATION (baseline arm reproduces the live map byte-for-byte): ANY new argus/** module changes at least one partition_id, so TC-ArgusAgent-DOGFOOD-001-03 WILL go red by construction; unit count stays 3. Story ACs: AC1 extraction (recommended: the 357-line resume family -> argus/pipeline_resume.py, 1331 -> ~980, following the pipeline_persist.py DN-PIPELINE-SPLIT precedent); AC2 repo-wide sweep over ALL tracked .py with the 3 test-file breaches as NAMED, DATED, FILED exemptions in a SHRINKING registry (never a narrowed population); AC3 an artifact-currency guard that is RED-first for free (it is red at ca37283 today) and would have been GREEN at 93adc94 - validated: `git diff a9cc933 93adc94 -- argus/` is EMPTY and a9cc933 IS an ancestor of HEAD; AC4 ledger rulings; AC5 behaviour proven untouched against the measured fixture RELEASE_READY deep_ratio=61/169 assessed 61/77 held_out=92 exit 0; AC6 closes DF-11-1-A. LEDGER RULINGS: DF-8-2-A CLOSE (recording that its own remedy was measured insufficient); DF-8-3-C CLOSE (coordinates re-measured - now pipeline.py:745 and generator.py:176, NOT the ledger's 686-694/86-93); DF-8-3-A RE-RECORDED -> 12.4 (its 'no room' blocker IS discharged here; what remains is scope - it is a report-content change this story's AC5 forbids); DF-8-5-B + DF-10-4-D CLOSED TOGETHER via AC3; DF-11-1-A CLOSED - registration VERIFIED GREEN BY EXECUTION before writing (both retros yield 0 status assertions), ending a five-story carve-out; DF-10-2-A RULED OUT with reasons (unrelated subject; AI-E11-7 wants a dated OPERATOR decision, type (H), not a dev's to take) - it stays OPEN and UNOWNED and the story says so; DF-11-4-D/AI-E11-6 RULED OUT of 12.1 -> 12.4 (12.1 adds no release-note section so the trigger does not fire; 12.4 owns the outcome-impact vocabulary the rank needs). AI-E11-1's guard-adequacy clause is binding on every AC that ships a test. AI-E10-1 carried forward as a dated risk acceptance, NOT re-taken (re-taking for Epic 12 is AI-E11-4, operator's). ALL FIGURES LOCAL Windows/CPython 3.11.15; CI evidence NOT ESTABLISHED; git tag -l empty and origin/master unmoved. || create-story 13-2 on HEAD bc55e36: 13-2 backlog -> ready-for-dev. Nothing outward-facing performed; git tag -l empty, origin/master == HEAD. || code-review 14-1 (Sonnet, iteration 1): 14-1 review -> in-progress (FAIL). See development_status[14-1-a-verdict-eligible-vacuous-finding-proves-vacuity-not-mocking] and the story file's Review Findings for the full record: one HIGH (backslash line-continuation lets a CONSUMED SUT result be misclassified as DISCARDED, wrongly promoting a test that constrains the real SUT result), one MEDIUM (provenance micro-parser should be extracted from vacuous_test.py before 14.2/14.3 land). All of the story's measured claims (recall 3/3, precision 0/31, flag rate, byte-unchanged 14.2 surface, script-generated dogfood artifacts, the 0.3 both-arms hardening) were independently reproduced by execution. || dev-story 14-2 (2026-08-18): in-progress -> review.  # dev-story 14-3 FIX round 1 (2026-08-18) on HEAD d339076: in-progress -> review. Review iteration 1 returned FAIL with two High findings; BOTH resolved, neither argued away. (1) The exclusion principle had been applied to match/doesNotMatch and to nothing else, leaving six ordinary non-assertion Python identifiers (ok, equal, Error, Equal, throws, rejects) in the density numerator - a wrong match RAISES density and REMOVES a flag, so the risk is silent recall loss, contained by the frozen corroboration table. DN-14-3-5 replaces the judgement with a RULE: admit a name when its measured Python collision is materially below its measured JS/TS benefit, counted with the stdlib ast module over 4046 independent Python files (three pinned members, Argus, site-packages, CPython 3.11 stdlib). It REPRODUCES both previously-ratified decisions (match out at 706 vs 476, expect in at 29 vs 6876) and drops exactly one name: Error, 164 measured Python call sites against a benefit of ZERO. ok (10 vs 764) and equal (34 vs 1548) KEPT with their cost recorded and asserted by execution; throws/rejects/Equal kept on 0 measured collisions. New guard -133 demonstrated RED against the shipped table. (2) TC-ArgusAgent-DETECT-001-131 was ITSELF a vacuous test: its fixture cleared the density floor, so _ast_corroborated returned at 'if not heuristically_vacuous' and provenance_evidence was never called. Re-authored with a non-vacuity floor mirroring -115, a fixture reaching the predicate through mock_ratio 4/7 > 1/2, and the mechanism asserted DIRECTLY (_mock_bound_names binds nothing in 'const fake = Mock()' and binds fake in 'fake = Mock()'). The sweep of -123..-132 found a SECOND instance, -132, which concluded silence from what an unparsed file also produces; floors added there and on -124. Re-measured: 0 flags GAINED / 0 LOST with byte-identical file::name@line fingerprint sets on all three pinned members, cartridge recall unchanged, _CORROBORATION_ASSERTION_CALLEES byte-unchanged at 23, thresholds byte-unchanged, pipeline.py untouched at 1111. The unreproducible sut=3 figure corrected in deferred-work.md and story 0.7(c), named to a fixture. DF-14-3-G and DF-14-3-H filed with named owners; test_vacuous_detector.py judged NOT an acceptable steady state at 1161/1200 but deliberately not split inside a fix round, zero lines added. Commits cce9897 (delta) + d563fa9 (dogfood artifacts, separate; LOC proxy only, NFR-C1 numerator unchanged at 435). Full suite 1632 passed / 0 failed / 0 skipped, exit 0. Gates LOCAL, CI evidence NOT ESTABLISHED.  # retrospective epic-14 (2026-08-18) on HEAD d563fa9: epic-14-retrospective CREATED (the key did not exist) and set to `done`; epic-15-retrospective CREATED at `backlog`; epic-13-retrospective and epic-14 NOT touched. Document epic-14-retro-2026-08-18.md, registered in _STATUS_DOCUMENTS. 10 action items appended (AI-E14-1..-10). Full suite re-verified 1632 passed / 0 failed / 0 skipped, exit 0. Gates LOCAL; CI evidence NOT ESTABLISHED for any commit in epic 14.  # create-story 13-5 (2026-08-18) on HEAD 63a0434: backlog -> ready-for-dev. Four premises refuted by execution (minions checkout off-pin; HEAD==pin does not imply pinned bytes; UNEVALUABLE is a CONDITION verdict not a gate outcome; both gate producers refuse an empty blocking population). Zero re-derived independently: 0 vacuous_test_ast over 1,960 files at the pins, 1,032 advisory, 0 of 31 locators still verdict-eligible. Full suite 1632 passed / 0 failed / 0 skipped, exit 0 (baseline 1632).  # create-story 2026-08-19 (Scrum Master): 15-2-the-detector-and-the-index-agree-on-what-a-line-is backlog -> ready-for-dev, and epic-15 backlog -> in-progress (first story of the epic to be created). Story file written; three premises from sprint-change-proposal-2026-08-19.md were re-measured and did NOT survive (secret_scan.py 575 not 583; 24 ids in tests/test_vacuous_detector.py not 'roughly thirty-five'; DF-15-2-B's reported line number is fixture-dependent; and the briefed working-tree list was stale by ten paths) and are corrected in the story rather than propagated. The defect itself REPRODUCED byte-for-byte and AC1's verdict-eligibility question is preserved as OPEN in both directions. Full suite re-run green after the story file was written (DOCS-001-78 lints story prose). Nothing closed, nothing committed, staged or pushed; argus/ and tests/ byte-unchanged.  # dev-story 15-2 review-fix 1 (2026-08-19): 15-2-the-detector-and-the-index-agree-on-what-a-line-is in-progress -> review.  # create-story 2026-08-19 (Scrum Master): 15-1-a-bench-with-the-defect-class-in-it-chosen-before-anyone-looks backlog -> ready-for-dev. See that key for the premise re-measurement: six premises survived, eight were corrected with the original named as wrong, and one new measurement (the mock-assertion co-occurrence sweep, 1 of 315 strict / 6 of 315 loose) was added. epic-15 unchanged (already in-progress). Nothing committed, staged or pushed.  # dev-story pass 2 2026-08-19: 15-1 in-progress -> review. Operator XAgent007 authorised the fetch (protocol section 6 R2); 14 candidates admitted from 20 fetched against criteria frozen in 16d7100 and applied UNCHANGED. Nothing ratified, N still 5, argus/ byte-unchanged. Suite 1651/0/0 exit 0.  # dev-story fix round 1 2026-08-19: 15-1 in-progress -> review. Code review iteration 1's ONE Low finding addressed, documentation only. The two D15 line counts re-derived by wc -l, _physical_line_count and git numstat (all agreeing on 927 and 698, matching the reviewer), struck rather than erased, recorded in the new D16. No code changed: argus/ and tests/ byte-unchanged, N still 5, nothing ratified. Suite 1651/0/0 exit 0, identical to baseline. Nothing committed, staged or pushed.  # retrospective epic-15 (2026-08-19): epic-15-retrospective backlog -> done. epic-15-retro-2026-08-19.md written and registered; 15 action items appended. epic-15 NOT touched.  # code-review 16-1 (2026-08-20, iteration 2, Sonnet): review -> done. VERDICT pass. Scope: all six commits 2ac1078..0733a33 (base 0a6e121), judged as a whole per the orchestrator's instruction; the fix under review is 7323f61/a20a0ef/0733a33 over round-3's response to iteration-1's ONE Med finding. Both of iteration-1's exact mutations (assess_breadth.holds forced True; both breadth dispatch branches in gate_decision.py disabled) were RE-RUN by this review, independently: they now turn NEW guard TC-ArgusAgent-PRECISION-001-86 RED where they previously left -55 GREEN, confirmed by execution, not by reading the story's transcript. A third, reviewer-authored mutation (the assess_breadth predicate changed from >= to > at the floor, an off-by-one the dev did not try) ALSO turned -86 RED, which is the evidence the shared expected_section_5_outcome() mirror is a genuine independent oracle over the fixture's own asserted contributing-member count and NOT a tautology that moves in lockstep with the predicate under test. -55 itself was confirmed to still be GREEN under both of iteration-1's mutations, exactly as its own docstring now discloses: its only fixture is the COMMITTED record (5 BORDERLINE rows, never Exhaustive), so its first dispatch clause fires before the breadth clause and -55 does not claim otherwise. -55's exhaustiveness tripwire was independently exercised by constructing an all-FP resolution of the committed record's BORDERLINE rows in isolation and confirming the tripwire assertion fires RED, proving it is reachable rather than decorative. No other guard imports the shared mirror besides -55 and -86, so nothing else inherits a weakened oracle. Production byte-unchanged confirmed by git diff 6766df7..HEAD over argus/, scripts/, README.md, CHANGELOG.md (empty). deferred-work.md's restoration confirmed byte-exact against 0a6e121 (git diff --numstat: 121 insertions, 0 deletions, single end-of-file hunk; the two backtick spans at the Story-15.x entry hold a literal CR and literal CRLF again, byte-compared against the 0a6e121 blob). Re-confirmed: no V1.4 row, protocol_version still V1.3, all 31 rows retain their 2026-08-17 XAgent007 provenance; no rule-class threshold anywhere in the tree; the >=80% Fraction, VALIDATION_SET_FLOOR_N, the five ratified members and MANIFEST_FIELDS untouched; gate_decision.py still 1,197/1,200. Independently executed: full suite 1658 passed / 0 failed / 0 skipped / 0 errors under ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1, exit 0; mypy argus Success (88 files); bandit -r argus --severity-level medium 0 medium / 0 high (20 low, unchanged); both builders --check exit 0. Tree restored exactly after every mutation, git status --porcelain empty before and after this review. No production or artifact file touched by this review; only the story file and sprint-status.yaml written.
project: ArgusAgent
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/design-artifacts/ArgusAgent/stories

development_status:
  # ── Epic 1: Signature-Demo Vertical Slice (Tier-A spine) ──
  epic-1: done
  1-1-canonical-serializer-content-hashed-envelope: done
  1-2-fixed-enum-coverage-ledger-frozen-recording-schema: done
  1-3-apaa-store-writer-reader-filesystem-containment: done
  1-4-tree-sitter-ast-index-repo-intake-python-stack-detection: done  # code-review iter-2 PASS: iter-1 Med (git ls-files non-ASCII drop) resolved + adversarially verified (git ls-files -z + UTF-8 decode; regression TC-APAA-INTAKE-001-78 RED-on-old); slice re-confirmed green; 291 passed, mypy clean
  1-5-heuristic-vacuous-test-detector-tier-a-vacuous-path-ast-subset: done  # code-review iter-1 PASS: advisory-by-contract / false-accusation moat verified non-bypassable (heuristic-only ⇒ vacuous_test_heuristic+advisory+depth_supported=None; only real two-fact AST corroboration over the unresolved 1.4 edge set promotes to vacuous_test_ast+AUDITED_SHALLOW, never fabricated); 1.2 Recording/grade_entry + 1.1/1.3 spine reused verbatim; Fraction-not-float proven; 307 passed, mypy clean
  1-6-pure-function-verdict-gate-finding-ordering-exit-code-mapping: done  # code-review iter-1 PASS: advisory-by-contract moat verified non-bypassable (is_verdict_blocking keyed on depth_supported is not None, NOT advisory — heuristic-only ⇒ RELEASE_READY, AST-corroborated ⇒ NOT_READY); determinism fuzzed (24 ledger-build orders ⇒ 1 byte string + 1 content_hash); Fraction canonical workaround sound across 1/1,0/1,7/11,1/2,1/7,13/17,100/100 (re-installed live Fraction ⇒ num/den; single 1.1 serializer, AST gate green); total==0 floor-first divide-by-zero guard; FR8 inferred-never-satisfies; exhaustive 0/2/3 map raises on unmapped, 1 reserved; PURE (imports only 1.2 ledger/finding models); critical-subsystem = additive 2.3 seam; 347 passed, mypy clean. 2 Low doc-precision nits (non-blocking)
  1-7-cli-invocation-contract-pipeline-signature-demo-vacuous-test-cartridge: done  # code-review iter-1 PASS: signature demo verified REAL non-bypassable (planted vacuous test ⇒ vacuous_test_ast/AUDITED_SHALLOW verdict-ELIGIBLE drives NOT_READY_FOR_RELEASE/exit 2 finding-first; clean control ⇒ RELEASE_READY/exit 0; advisory moat holds through full pipeline). AC5 byte-identity genuine (clock-free Fraction-not-float content-addressed envelope). Headless argv/stdout/exit-only; zero-token + web-free gates green; typed-error→exit-1 honest degradation. "grade non-test py audited_deep" = correctly-scoped FR6/Epic-1 claim-presence (FR7 AST-truth-grounding deferred to Epic-6 Story 6.2 [Tier B] per epics cut-order) — ACCEPTED not a defect. 368 passed, mypy clean. 2 Low non-blocking concerns deferred (DF-1-7-A persist OSError edge→Epic-3; DF-1-7-B interim deep over-grading→close before Epic-7 dogfood, owned by 6.2)
  epic-1-retrospective: done  # retrospective complete 2026-06-21 (epic-1-retro-2026-06-21.md): all 7 stories done/green (368 passed, mypy clean); determinism spine + single-serializer + false-accusation moat held across the slice; only review FAIL was 1.4 non-ASCII git ls-files (impure-shell encoding boundary) → fixed + adversarially verified; defer-inflow 5 (4×🟢 + DF-1-7-B 🟡 interim deep over-grading, MUST close before Epic-7 dogfood, owned by Story 6.2); 5 action items recommended as Epic-2 backlog (L1-E11); no significant discovery, Epic-2 plan sound

  # ── Epic 2: Full Coverage Ledger & Defect Detectors (Tier-A) ──
  epic-2: done
  2-1-complete-depth-state-semantics-inferred-never-satisfies-a-gate: done  # code-review iter-1 PASS: FR8 proven not promised — 5 named synthetic-ledger tests (TC-APAA-LEDGER-001-97..101) assert the REAL 1.6 evaluate_verdict (import-verified, NOT a fork) excludes inferred/skipped/tool_scanned_only from the deep-% numerator (100%-inferred→INSUFFICIENT_COVERAGE; 10 deep+7 inferred=10/17<60%→NOT_READY; 17/17→RELEASE_READY control); 1.2 enum/grade_entry + 1.6 gate UNCHANGED (untracked, no diff); assess_criticality defeats rename-gaming (utils_misc.py w/ auth content→CRITICAL; auth_guard.py arithmetic→NORMAL) + Unicode casefold non-ASCII café_guard→CRITICAL (AI-E1-1); pure (AST-scan-pinned)/frozen/no-float/typed-error/TYPE_CHECKING-only AST import; gate extended-not-forked; 396 passed, mypy clean, 337 lines. 1 Low non-blocking: assess_criticality substring match over-flags benign tokenize/policy substrings (safe direction; 2.3 operator-override seam; tighten to identifier-boundary in a precision pass)
  2-2-readable-per-file-coverage-ledger-surface: done  # code-review iter-1 PASS: pure FR9 readable surface ledger/coverage_report.py (build_coverage_report/render_text/render_json/render); frozen CoverageReport+DepthAggregate; deep-% reuses+cross-checks REAL 1.6 evaluate_verdict Fraction(deep,total) (TC-118, non-divergent, import-verified not forked); JSON via single 1.1 canonical.dumps (AST gate green, no float anywhere); secret-safe canary (TC-121, never sources file bytes); pure(AST-pinned)/frozen/byte-stable(twice+2 input orders)/typed-error(CoverageReportError); non-ASCII café/Cyrillic/JP path intact text+JSON + round-trip (AI-E1-1, TC-130); import-isolation gate EXTENDED not forked; frozen 1.2/1.6/1.1 contracts UNCHANGED (no working-tree diff); AC5 seam=option(a) pure-library-only (no wire-contract change); 417 passed, mypy clean, 313 non-blank lines. 1 Low non-blocking (markdown-cell robustness for pathological | / newline in path — cosmetic, JSON unaffected, no secret leak)
  2-3-critical-subsystem-identification-operator-designation: done  # code-review iter-1 PASS: SEAM-not-fork confirmed (pipeline feeds computed critical_subsystems_all_deep into the UNCHANGED 1.6 evaluate_verdict; advisory moat + thresholds + 0/2/3 map intact); byte-identity keystone genuinely pinned (TC-PIPELINE-001-12 compares content-addressed locator + on-disk bytes default-vs-empty-designation; predicate short-circuits empty-set→True); anti-gaming verified (force/exclude typo can only make gate STRICTER; deliberate exclude is the locked 2.1-correction lever AND is audited via persisted excluded_critical_paths provenance — not a silent bypass); additive AuditRequest fields + CLI flags only (AR3 wire contract unchanged); pure(AST-pinned)/frozen/no-float/typed-error/single-serializer; order-independence + non-ASCII AI-E1-1 intact; _MODULES_UNDER_GUARD extended-not-forked; 450 passed, mypy clean, ≤1200 lines. 1 Low non-blocking deferred (DF-2-3-B: persist computed CriticalSubsystemSet for negative-assurance, Epic-4 scope)
  2-4-repository-partitioning-bounded-audit-units-work-manifest-permission-boundary: done  # code-review iter-1 PASS: permission boundary (NFR-S4) verified EXACT-membership non-bypassable (is_in_scope = normalize(query) in frozenset(manifest.files), never prefix/substring; sibling-prefix auth/secrets_extra.py + auth/secrets truncation + bare-dir + ../traversal + cross-manifest non-ASCII all rejected; normalize_rel_path rejects absolute/UNC/drive-letter/.. before compare; read_in_scope off-scope→PartitionScopeError impossible). Determinism byte-stable (union-find smaller-root + sorted components/blobs + sha256-over-sorted-members id + reversed-input order-independence proof; no clock/uuid/random/os AST-pinned). Hard ceiling holds (bin-pack flushes at soft⊂hard; single oversized file = own context-pressured unit documented boundary). V1 honest (seam_analysis=v2-deferred; cut edges recorded-NOT-analyzed; under-merge never over-merges/drops). Total+disjoint 120-file proof; parse_failed placed; AI-E1-1 non-ASCII intact+round-trip. Single serializer (build_plan_payload thin model_dump, no json.dumps); reuses 1.4 AstIndex + 1.3 write_assignment + 1.1 envelope (no second writer/serializer); frozen extra=forbid no-float; typed errors; headless. dev-story: pure graph-derived partitioner (partition_repository over the 1.4 AstIndex + in-memory LOC map; union-find cohesion blobs → bin-pack ≤40 files/15k LOC soft, ≤60/25k hard; context_pressure auto-downgrade on capped/split units + single oversized file) + frozen Partition/PartitionPlan/WorkManifest/CutEdge (content-derived sha256 partition_id, extra=forbid, no float) + NFR-S4 work-manifest permission boundary (is_in_scope EXACT normalized membership, never prefix; read_in_scope off-scope→PartitionScopeError) + scope-fenced pipeline persistence (per-unit assignments/<partition_id>.json + plan snapshot via existing write_assignment, no verdict-math change) + V1 seam_analysis=v2-deferred provenance + recorded-not-analyzed cut edges; AI-E1-1 non-ASCII (café/Cyrillic) placed-intact + scoped; _MODULES_UNDER_GUARD extended; 484 passed, mypy clean, 645 lines
  2-5-hardcoded-secret-detector-producer-side-redaction: done  # code-review iter-1 PASS: security keystone verified non-bypassable — every leak path traced + closed (NO value field structurally on Recording/Locator/FindingDraft/SecretFindingEvidence; raw value held ONLY on a transient __slots__ _Match, computed→masked→discarded in _evidence_for; evidence in-memory-only NOT persisted; recording_id value-independent (hashes location/rule only); constant degraded reason "secret_scan_failed" + arg-only SecretScanError messages — no secret in any emitted field/log/exception). MANDATORY test_secret_containment.py re-run + proven NON-VACUOUS: ASCII (PLANTEDx...) + Cyrillic (parol_sekret...PLANTED) + bare PLANTED token ABSENT from every persisted .apaa/ byte AND in-memory finding bytes (UTF-8 searched), while findings ARE emitted (lines 5+10) w/ correct locators (redaction != silent suppression). Determinism: entropy exact Fraction via 1e-6 grid (never float; 1.1 serializer accepts), value-independent stable ids, sorted matches. Pure (no FS/clock/uuid/random/LLM); frozen extra=forbid evidence w/ localized schema_version; advisory=True+depth_supported=None moat (frozen 1.6 gate UNTOUCHED, NOT verdict-eligible); additive findings-only _detect_per_file fold (no ledger double-count, no verdict-math change, no-secrets repo byte-identical); single serializer (AST gate green); _MODULES_UNDER_GUARD extended-not-forked; AI-E1-1 non-ASCII path+value discharged. 503 passed (17 new), mypy clean, 351 non-blank lines (≤1200). 2 Low non-blocking deferred to 4.4/6.5 hardening (pattern_id granularity erased on persistence since evidence in-memory-only + recording_id excludes pattern_id → distinct families collapse to byte-identical Recording, idempotent overwrite, no leak; docstring overstates degraded-fold — only findings folded)
  2-6-zero-token-breadth-tool-runner-tool-failure-as-finding: done  # code-review iter-1 PASS (2026-06-24): pure/impure split (AR8) verified — radon library API behind an INJECTED tool_invoker seam (no subprocess/shell), PURE classify_outcomes (no clock/uuid/random/float; deterministic regardless of input order TC-14). TOOL-FAILURE-AS-FINDING non-bypassable: each injected mode (unavailable/crashed/timed_out/unparseable) → tool_failure Recording (advisory/depth_supported=None/≥1 locator) + SKIPPED downgrade + sanitized token DegradedCondition (TC-04); raising/misbehaving invoker → CRASHED never propagates (TC-05/07); malformed arg → typed ToolRunnerError(ValueError), arg-name-only message (TC-06). tool_scanned_only DENOMINATOR-ONLY proven via REAL 1.6 evaluate_verdict (import-verified NOT forked) over synthetic ledger → INSUFFICIENT_COVERAGE/deep_ratio 0/3 (TC-02); minted via reused 2.1 classify_depth(TOOL_BREADTH_ONLY)/1.2 grade_entry, no ad-hoc enum. NFR-S1 redaction STRUCTURAL: neither ToolRunOutcome NOR ToolInvocation has any raw-output field (TC-10); reason = closed _REASON_TOKEN_BY_OUTCOME token; radon_invoker drops raw error at boundary; planted ASCII+Cyrillic sentinel ABSENT from raw serialized bytes + full .apaa/ tree (TC-05/24/25, non-vacuous). AI-E1-1 MANDATORY suite genuine: café/Cyrillic paths intact + pipeline round-trip (TC-20/21), explicit-UTF-8 decode never raises out (TC-22/23). _MODULES_UNDER_GUARD extended-not-forked + test_pipeline_is_zero_token green; single-serializer AST gate green (no json.dumps/no float); reuses 1.1/1.2/1.5/2.1/2.5, frozen contracts UNCHANGED. 532 passed, mypy clean (--ignore-missing-imports), 455 lines. 2 Low non-blocking: ToolRunOutcome defined-but-unused (ToolInvocation is the working equivalent, AC4 permits "or equivalent", both no-raw-field) + pipeline tool_failure surface dormant for already-graded files (matches LOCKED AC5 no-double-count, V1-scope, detector contract fully tested).
  # dev-story 2026-06-23: zero-token breadth tool-runner detectors/tool_runner.py — V1 radon library API (radon.raw.analyze LOC + radon.complexity.cc_visit) behind an INJECTED tool_invoker seam (AR8 + AC6 failure-injection); PURE classify_outcomes maps captured outcomes → tool_scanned_only entries (via 2.1 classify_depth(EvidenceKind.TOOL_BREADTH_ONLY)/1.2 grade_entry, denominator-only/FR8 — proven via REAL 1.6 evaluate_verdict over a synthetic ledger) + tool_failure/traceability_not_establishable findings (1.5 build_recording, advisory=True/depth_supported=None, repo-anchor locator line 1) + downgrade, never crash/fabricated-pass/silent-skip (FR14/NFR-R1). Frozen redaction-safe ToolRunOutcome (NO raw-output field; files_scanned/total_loc/total_complexity int — no float; sanitized reason TOKEN only — tool output hostile, NFR-S1). ToolRunnerError (ValueError subclass, no leak). Scope-fenced pipeline wiring: breadth grades ONLY files NOT already depth/shallow-graded (no double-count; clean/no-new-grade run ledger+verdict byte-identical to pre-2.6). MANDATORY AI-E1-1 adversarial suite (non-ASCII path café/Cyrillic intact + round-trip; explicit-UTF-8 errors=replace; FAILURE-INJECTION crash/timeout/unavailable/unparseable each → tool_failure finding + planted sentinel ABSENT from every emitted/persisted byte; clean run no cry-wolf). _MODULES_UNDER_GUARD extended not-forked; test_pipeline_is_zero_token green. Reuses 1.1 serializer + 1.2 enum/grade_entry + 1.5 base/build_recording + 2.1 classify_depth + 2.5 producer discipline; frozen contracts UNCHANGED. PYTHONIOENCODING=utf-8 pytest tests/apaa/ tests/test_import_paths.py green; mypy clean.
  epic-2-retrospective: done  # retrospective complete 2026-06-24 (epic-2-retro-2026-06-24.md): all 6 stories done, 0 review FAILs (vs Epic-1's 1), 532 passed/mypy clean; determinism + single-serializer + advisory-moat held with zero erosion; 3 security keystones (2.4 permission boundary, 2.5 secret containment, 2.6 tool-output redaction) verified non-bypassable; AI-E1-1 (adversarial non-ASCII/locale/failure-injection) paid off — applied first at 2.6 impure-subprocess boundary, the class that caused Epic-1's only FAIL did not recur; defer-inflow 5→2 (DF-2-3-A 🟢 + DF-2-3-B 🟢, both in-scope-deferred); DF-1-7-B (🟡 interim deep over-grading) still open, owned by Story 6.2, MUST close before Epic-7 dogfood; 1 new process watch-item AI-E2-1 (premature status=review flip under 2.6 session interrupts → promote a pre-review test-existence guard); no significant discovery, Epic-3 plan sound

  # ── Epic 3: Honest Degradation & Cost Governance (Tier-A) ──
  epic-3: done
  3-1-budget-ceiling-configuration-cost-accounting: done  # code-review iter-1 PASS (2026-06-24): reuse-not-fork verified — ceiling_reached IS the imported BudgetGuardrails.evaluate_worker_spend >=-is-a-breach decision (asserted live + TC-13 against the imported guardrails across {0,50,99,100,101,500}; at-ceiling boundary breaches, TC-COST-001-46 parity), no parallel comparison, the policy's internal float never leaves _coerce_breach. OI3 no-numeric-default confirmed (AST scan TC-04 + BudgetConfig().ceiling_credits is None; 0→None first-class no-ceiling). No-float invariant proven non-bypassable (canonical serializer genuinely raises CanonicalSerializationError on a float leaf — the backstop is real; baseline an exact reduced Fraction; to_canonical_payload hands LIVE Fraction → 1/1 not model_dump's "1", round-trips to an EQUAL model via _coerce_baseline — 1.6/2.2 pattern; proxy==0→BASELINE_UNDEFINED total-safe). Scope fence holds: verdict+findings BYTE-IDENTICAL across no-ceiling vs ceiling runs (TC-PIPELINE-001-16 compares content-addressed names+bytes), ceiling_reached exposed but NEVER acted on (no halt/skip/verdict-math change — that's 3.2), LOC map computed once + shared with the 2.4 plan. Pure (AST-pinned no I/O/clock/uuid/random); typed BudgetGovernorError on float/neg/bool/non-int/non-mapping/non-str-axis; snapshot carries only int/Fraction/bool/str (closed key set, no abs-path/source/secret byte on-disk). 567 passed, mypy clean, 343 lines; single-serializer + web/LLM import-isolation gates green w/ the new module. AI-E2-1/E2-5/E1-1 discharged. 1 Low non-blocking: DF-3-1-A (BudgetConfig.ceiling_credits lacks ge=0 — defensive-only, operator seam fully guarded via budget_config_from_budget + AuditRequest ge=0; not reachable, not a defect).
  # dev-story: pure cost/budget_governor.py — frozen BudgetConfig (ceiling_credits int|None, 0→None no-ceiling, NO numeric default OI3) + pure account_spend fold (REUSES BudgetGuardrails.evaluate_worker_spend >=-is-a-breach BY IMPORT, no fork; at-ceiling boundary breaches) + frozen CostLedger (no-float; ceiling_reached exposed for 3.2) + NFR-C1 baseline_ratio (exact reduced Fraction over total-LOC proxy; proxy==0 total-safe BASELINE_UNDEFINED) + typed BudgetGovernorError. Config seam: cli --budget flipped to ceiling + models budget docstring (additive, byte-identity). Scope-fenced pipeline: build cost ledger from V1 zero-token contributions + persist snapshot additively to state/ via existing write_payload (LOC map computed once, shared w/ 2.4 plan); NO halt(3.2)/NO verdict-math change/NO single-pass split. Round-trip via to_canonical_payload (LIVE Fraction→num/den) + _coerce_baseline validator. Import-isolation gate extended-not-forked. 567 passed, mypy clean, 343 lines. AI-E2-1/E2-5/E1-1 discharged.
  3-2-halt-skip-downgrade-report-on-budget-exhaustion: done  # code-review iter-1 PASS (2026-06-24): honest-degradation keystone verified non-bypassable — un-audited remainder graded CoverageDepth.SKIPPED via the EXISTING grade_entry (claim_present=False), NEVER a fabricated audited_*; halted branch runs _detect_per_file ONLY over the assessed set + grades the DISJOINT remainder SKIPPED (disjointness structural + e2e-asserted TC-PIPELINE-001-17 reads the persisted ledger: depths[skipped]=="skipped"). skipped in total() not deep_count() drives deep-% down through the REAL frozen 1.6 gate (TC-78) — no RELEASE_READY reachable on exhaustion. Reuse-not-fork: would_breach/project_halt_point delegate to the imported 3-1 _coerce_breach (TC-70 asserts equality across {0,50,99,100,101,500} incl at-ceiling breach); verdict_gate UNCHANGED (floor=3.3). Determinism: pure projection, AST-pinned no clock/uuid/random/float, order-independence TC-74 + byte-stability TC-75; per-file proxy 1 non-Py/5 Py sums to the 3-1 whole-run total. No-halt BYTE-IDENTICAL on verdict/ledger/findings (TC-PIPELINE-001-19, content-addressed names+bytes budget=0 vs 100). HaltReport frozen/extra=forbid/no-float (TC-83)/secret+abs-path-safe (TC-84/94)/non-ASCII round-trip (TC-85); single 1.1 serializer; round-trip equal+byte-identical (TC-90..93). ExhaustionError typed (TC-87/88); 294 lines ≤1200; headless; import-isolation gate extended-not-forked. DF-1-7-A stays deferred (no scope expand). 595 passed, mypy clean. No findings, no new defer. dev-story 2026-06-24 (claude-opus-4-8, implement): FR22 halt→skip→downgrade→report shipped. NEW pure cost/exhaustion.py (CostUnit/HaltProjection/project_halt_point/would_breach/frozen HaltReport/typed ExhaustionError) — deterministic PRE-DISPATCH per-unit admission projection reusing 3-1 _coerce_breach >=-hard-ceiling BY IMPORT (no fork, no wall-clock interrupt; at-ceiling breaches). Locked: new exhaustion.py sibling (budget_governor frozen); _coerce_breach reuse via public delegating would_breach/project_halt_point; per-file halt granularity over the sorted index; per-file cost proxy 1 non-Py / 5 Py (sums to the EXACT 3-1 whole-run total → no-halt projection consistent); APAA-COST area. Scope-fenced pipeline.py: project halt → _detect_per_file over ASSESSED only → remainder graded CoverageDepth.SKIPPED via EXISTING grade_entry (NEVER fabricated audited_* — honest-degradation keystone) → PARTIAL ledger re-folded through UNCHANGED 1.6 gate (degraded, no crash; floor semantics deferred 3.3) → frozen HaltReport persisted additively to state/ via write_payload (single 1.1 serializer, content-addressed, 3.4 resume seam). No-halt run BYTE-IDENTICAL on verdict/ledger/findings (AC6, e2e-proven). DF-1-7-A stays deferred. 595 passed; mypy clean; exhaustion.py 294 non-blank lines; import-isolation + single-serializer gates green. Fences: floor=3.3, resume=3.4, host byte-parity=3.5, $X=7.1, LLM=Epic 6. AI-E2-1/E2-5/E1-1 discharged. No new defer.
  3-3-insufficient-coverage-floor-under-exhaustion: done  # code-review iter-1 PASS (2026-06-25): verify-and-lock confirmed — build_floor_report READS verdict.deep_ratio + INSUFFICIENT_COVERAGE_FLOOR (imported, no re-derive) + HaltReport.halted_on_exhaustion; 1.6 gate/budget_governor/coverage_ledger/canonical carry NO floor logic. NO FABRICATED READINESS keystone verified: below_floor=verdict==INSUFFICIENT_COVERAGE agrees with deep_ratio<floor AND across total==0 boundary (TC-111 pinned); AC1+e2e TC-PIPELINE-001-20 exit 3 never RELEASE_READY/never BLOCKED, floor-wins w/ blocking finding (TC-96). ADDITIVE/byte-identity: floor_report = pure AuditResult surface, no write (TC-PIPELINE-001-22 locator bytes identical + no state/ payload carries below_floor); whole-% int(ratio*100) exact-Fraction truncation (TC-115), no float (TC-107 "9/50"/"1/5" via single 1.1 serializer); secret-safe (TC-108) + non-ASCII café/Cyrillic round-trip (TC-109). PURE (AST scan TC-114)/frozen extra=forbid/typed ExhaustionError (TC-112/113). Reviewer re-ran: 619 passed, mypy clean, no-web-imports+single-serializer gates green, exhaustion.py 506 lines ≤1200. No findings any severity; DF-1-7-A stays deferred; no new defer. dev-story 2026-06-25 (claude-opus-4-8, implement): exhaustion-aware INSUFFICIENT_COVERAGE floor SEMANTICS shipped (verify-and-lock + additive PURE floor report). NEW frozen InsufficientCoverageFloorReport + PURE build_floor_report(verdict, halt_report) + _whole_percent render in cost/exhaustion.py (437 lines) READS the done 1.6 AuditVerdict.deep_ratio + INSUFFICIENT_COVERAGE_FLOOR (no re-derive) + the 3-2 HaltReport.halted_on_exhaustion (exhaustion-driven vs intrinsic); renders the PRD-J2 line "assessed 18% deep; no repo-wide verdict rendered (floor: 20%)". below_floor = verdict==INSUFFICIENT_COVERAGE (pinned to agree with deep_ratio<floor); message whole-percent int(ratio*100) exact-Fraction truncation (no float). Persist vs pure-render LOCKED = option (b) PURE surface on AuditResult.floor_report (no new write — derivable; YAGNI); non-floor run BYTE-IDENTICAL to 3-2 on verdict/ledger/findings/halt-report (AC6, no state/ payload carries below_floor). AC1 below-floor→INSUFFICIENT/exit3 [never RELEASE_READY/never BLOCKED, floor-wins even w/ blocking finding] synthetic+e2e; AC3 exit-3-vs-exit-2 routing distinct; AC4 above-floor no-over-fire (30%→NOT_READY, exactly-20% at floor, e2e budget=6→50% real verdict). Frozen extra=forbid/no-float (deep_ratio "9/50"+floor "1/5" via single 1.1 serializer)/secret-safe/non-ASCII round-trip; typed ExhaustionError on bad input; PURE (AST-scan). 1.6 gate/1.2 ledger/1.1 serializer/3.1 budget_governor/3.2 halt mechanism UNCHANGED (no working-tree diff). Fences: negative-assurance wrapper=4.1, resume=3.4, host-parity=3.5, $X=7.1, LLM=Epic6. DF-1-7-A stays deferred (pure surface, no _persist touch). 619 passed (24 new: TC-APAA-COST-001-95..115 + TC-APAA-PIPELINE-001-20..22), mypy clean, import-isolation+single-serializer gates green. AI-E2-1 test-existence gate honored (all test files exist+pass before review flip). create-story 2026-06-25: exhaustion-aware INSUFFICIENT_COVERAGE floor SEMANTICS/render (verify-and-lock + additive PURE floor report) — READS the done 1.6 AuditVerdict (floor MATH already ships: <20% deep → INSUFFICIENT_COVERAGE/exit 3, floor-wins) + the 3-2 HaltReport.halted_on_exhaustion (exhaustion-driven vs intrinsic); names "assessed X% deep; floor 20%" (PRD J2), proves exit 3 (not 0/2), NO fabricated RELEASE_READY; reuses deep_ratio+INSUFFICIENT_COVERAGE_FLOOR (no re-derive), single serializer, frozen extra=forbid, no float; FR17/NFR-A3 negative-assurance WRAPPER fenced to 4.1, resume 3.4, host-parity 3.5, $X 7.1; gate/halt/ledger/serializer UNCHANGED
  3-4-resumability-from-on-disk-apaa-state: done  # code-review iter-2 PASS (2026-06-26): all 3 iter-1 findings RESOLVED + reviewer-re-verified — [High] resume now carries forward EVERY prior-assessed path (all depths) via halt_report.assessed_files (ResumePlan.carried_forward_paths; _carried_forward_entries reuses ALL assessed entries verbatim); reviewer reverted to old deep-only behavior and confirmed TC-APAA-PIPELINE-001-30 RED (resumed deep_ratio 1/3≠1/2) + TC-APAA-COST-001-132 RED (carried_forward_paths ()≠('a_test.py',)), green on the fix; [Med] keystone now compares persisted halt-report bytes on ...-30 + ...-24; [Decision] resumed halt report re-projected via the identical build_halt_report(_project_halt(index, raised_config)) call — byte-identical all depths. Tamper(...-25 secret-safe msg)/unknown-field(...-26)/no-prior-state(...-27)/chained-partial→complete(...-28)/no-resume-byte-identity-AC6(...-29) all green; reuse-by-import no-fork; frozen surfaces+cli/AuditRequest unchanged; resume.py 251 / pipeline.py 920 ≤1200; headless; single-serializer+no-web-imports gates green. 644 passed (reviewer-run), mypy clean. No new findings, no new defer (DF-1-3-A/DF-1-7-A/DF-1-3-B stay deferred). # fix iter-1 (2026-06-26): all 3 review findings resolved — [High] resume now carries forward EVERY prior-assessed path (all depths) via prior_halt_report.assessed_files (ResumePlan.carried_forward_paths; _carried_forward_entries reuses ALL assessed entries verbatim), completing the interrupted-session partial fix by reconciling the renamed model field w/ the unit tests (AttributeError); [Med] added TC-PIPELINE-001-30 keystone fixture (aaa_test.py audited_shallow in the assessed prefix, RED on old behavior deep_ratio 1/3≠1/2) + TC-COST-001-132 unit regression, halt-report bytes now compared on ...-24/...-30; [Decision] resumed halt-report re-projected over the full current index (build_halt_report(_project_halt(index.entries, raised_config))) = the uninterrupted-run call, byte-identical all depths. 644 passed, mypy clean, frozen surfaces unchanged, no-web-imports+single-serializer gates green. # code-review iter-1 FAIL (2026-06-26): AC2/NFR-R2 keystone BROKEN — resume carries forward ONLY audited_deep, silently dropping prior assessed-but-non-deep coverage (audited_shallow test files / tool_scanned_only / assessed-skipped). Reproduced: halt(6)→resume(100) ledger deep_ratio 2/3 vs uninterrupted run(100) 1/2; verdict+ledger+halt-report bytes diverge (will flip verdict at the 20% floor). 642-green keystone test masks it (vacuous_basic test file always sorts into the skipped remainder). FIX: carry forward ALL prior-assessed entries (prior_halt_report.assessed_files), not only audited_deep (resume.py:217 + pipeline.py:968); add RED fixture w/ a non-deep file in the assessed prefix + compare halt-report bytes (TC-PIPELINE-001-24); re-derive resumed halt-report assessed_files to match an uninterrupted run (pipeline.py:1021). Reuse/purity/tamper-guard/no-resume-byte-identity all sound. # dev-story 2026-06-25 (claude-opus-4-8, implement): FR31/NFR-R2 resumability shipped. NEW pure cost/resume.py (frozen ResumePlan + build_resume_plan, divergence guard, prior-spend-seeded halt re-projection via 3-2 project_halt_point BY IMPORT) + impure resume_audit_detailed/resume_audit entrypoint + ResumeStateError in pipeline.py (tamper-guarded 1-3 read → plan → detect-remainder-ONLY → merge carried-forward audited_deep VERBATIM + new + still-skipped into single CoverageLedger.build → re-fold UNCHANGED 1.6 gate → persist via EXISTING _persist*; shared _assemble_and_persist extraction). KEYSTONE proven: halt(B1)→resume(B2) verdict+ledger+findings BYTE-IDENTICAL to uninterrupted run(B2) (TC-PIPELINE-001-24). AI-E1-1: tampered/unknown-field/missing/divergent-tree state → typed ResumeStateError(=PipelineError/ValueError)→exit 1, NEVER silent wrong resume/fabricated verdict/fallback-to-fresh (TC-25/26/27, secret-safe msg). Partial-state resume halts again honestly + chained halt→resume(partial)→resume(complete)==run(B2) (TC-28). No-resume run byte-identical to 3-3 (AC6, TC-29); AuditRequest+cli.py UNCHANGED. Discovery by PRODUCER token (sorted locators, most-progress selection — no clock). Reuse-not-fork (1-3 reader/StoreIntegrityError, 3-2 halt, 1.2 ledger, 1.6 gate, 3-1 budget, 3-3 floor — all BY IMPORT). Frozen surfaces UNCHANGED. cost/resume.py appended to _MODULES_UNDER_GUARD. 642 passed (16 TC-COST-001-116..131 + 7 TC-PIPELINE-001-23..29), mypy clean, resume.py 233 lines / pipeline.py 936 (≤1200), single-serializer + web-import gates green. DF-3-4-A filed (CLI --resume deferred); DF-1-3-A/DF-1-7-A/DF-1-3-B stay deferred. AI-E2-1/E2-5/E1-1 discharged. create-story 2026-06-25: deterministic resume-from-disk restore-and-continue loop (FR31/NFR-R2) — re-load prior ledger+halt-report+cost-snapshot via the 1-3 tamper-guarded ApaaStoreReader (StoreIntegrityError REUSED, no second reader/tamper-check), reuse prior audited_deep coverage (no re-audit), continue ONLY the remainder bounded by the RAISED budget (re-project halt via 3-2 project_halt_point), merge into a single CoverageLedger.build, re-fold through the UNCHANGED 1.6 gate. KEYSTONE: halt(B1)→resume(B2) final verdict+ledger BYTE-IDENTICAL to uninterrupted run(B2) — resume must not change the answer. AI-E1-1: tampered/corrupt/unparseable/divergent-tree state RAISES typed→exit 1, never silent wrong resume. Partial-state resume halts again honestly. NEW pure cost/resume.py (frozen ResumePlan + build_resume_plan, AR8 pure) + impure resume entrypoint in pipeline.py; no-resume run byte-identical to 3-3 (AC6). Fences: host-parity=3.5, memoization=Epic 5, integrity-lint=4.2, neg-assurance wrapper=4.1, $X=7.1, LLM=Epic 6. Single serializer, frozen extra=forbid, no float, ≤1200 lines, FastAPI-free.
  3-5-sequential-byte-identical-execution-least-capable-host: done  # code-review iter-1 PASS (2026-06-27): VERIFY-AND-LOCK confirmed — reviewer re-ran PYTHONIOENCODING=utf-8 pytest tests/apaa/ tests/test_import_paths.py → 661 passed (matches DAR), 17/17 portability legs RAN (zero skips on this host), mypy clean, single-serializer + no-web-imports gates green, both new files ≤1200 lines. ZERO product-code drift verified (newest apaa src mtime cost/resume.py 2026-06-26 predates the 3.5 test files 2026-06-27; only tracked apaa diff is __init__.py, pre-existing/unrelated). PYTHONHASHSEED genuinely differs across legs (hash('café') seed0≠seedX). Adversarial mutation: AC4 order-independence lock (TC-PORT-001-04) is SHARP — went RED when CoverageLedger.build sorted()→set() round-trip; spine has defense-in-depth (every serialize boundary re-sorts). No-parallelism AST source-scan lock robust (AST-over-sys.modules deliberate+documented — sys.modules false-positives on transitive 3rd-party threading/asyncio). Honest reframing (cross-ENV byte-identity + order-independent-merge lock for the V1 no-parallel-scheduler reality) documented + sound, not a dodge. AI-E1-1 café/Cyrillic proof REAL + runs (clean+halted+resumed, TC-PIPELINE-001-33..36). All 7 ACs met. 2 Low/informational non-blocking (cross-env e2e leg coarse for ledger-element ordering — backstopped by sharp AC4 unit lock; POSIX locale env vars weak on Windows host but PYTHONUTF8=0 carries the real encoding axis). No new defer. # dev-story 2026-06-27 (claude-opus-4-8, implement): VERIFY-AND-LOCK cross-ENVIRONMENT determinism property suite shipped — ZERO product-code edit (no drift surfaced; the expected verify-and-lock footprint). NEW tests/apaa/test_sequential_portability.py (areas APAA-PIPELINE …31-37 e2e + APAA-PORT …01-10) + tests/apaa/portability_runner.py (subprocess child-runner = the documented impure shell of the SUITE) + nonascii_unicode cartridge (café deep src + Cyrillic тесты vacuous test). AC1 no-parallelism/sequential-only lock = AST source scan over every minions_core/apaa/*.py (no threading/multiprocessing/concurrent/asyncio IMPORT + no fan-out construct/async def invoked) — deliberately NOT a sys.modules absence check (false-positives on third-party transitive imports; documented in DN/Completion Notes) — + sequential path completes to full verdict (nonascii BLOCKS exit 2 / clean RELEASE_READY exit 0). AC2 KEYSTONE: same cartridge audited in two FRESH-interpreter subprocess legs under differing PYTHONHASHSEED + LC_ALL/LANG/LC_CTYPE/PYTHONUTF8 + cwd → byte-identical .apaa/ trees (sorted locators + read_bytes + content_hash). AC3 AI-E1-1: C/POSIX(PYTHONUTF8=0) vs UTF-8 legs byte-identical + café/тесты intact UTF-8 (no octal-escape/drop) + reader round-trip, for clean + halted + resumed. AC4 order-independent merges (CoverageLedger.build sorted/reversed/rotated byte-identical; resume-merge; _list_locators sorted) = the forward-compat "parallel=pure speedup" lock (V1 has NO parallel scheduler — epic's literal "byte-identical to a parallel run" REFRAMED to cross-ENV byte-identity, project context wins over literal wording). AC5 cross-CWD no-abs-path/secret leak (NFR-S1 reuse; full randomized-canary=4.4). AC7 stack-agnostic-core AST lock (NFR-P2). 661 passed (+17), mypy clean, ≤1200 lines, single-serializer+no-web-imports gates green, all frozen spine contracts byte-unchanged. AI-E2-1 test-existence gate honored before review flip. Fences: memoization=Epic5, neg-assurance=4.1, integrity-lint=4.2, randomized-canary=4.4, $X=7.1, LLM=Epic6, --resume CLI=DF-3-4-A/7.1. No new defer.  # create-story 2026-06-27: VERIFY-AND-LOCK + cross-ENVIRONMENT determinism property suite (FINAL Epic-3 story) — the sequential-canonical spine (single 1.1 serializer/envelope, no-float, clock-free, sorted/order-independent merges, explicit-UTF-8 impure shell) ALREADY ships + is repeated-run byte-stable; net-new = cross-environment proof (same cartridge under differing PYTHONHASHSEED/locale-encoding/CWD → byte-identical .apaa/ trees) + no-parallelism/sequential-only lock (no threading/multiprocessing/asyncio fan-out on a write path, no-web-imports-gate style) + forward-compat order-independent-merge lock + cross-locale impure-shell/non-ASCII round-trip (AI-E1-1 Epic-1-FAIL class, clean+halted+resumed) + no-abs-path/secret cross-CWD guard + stack-agnostic-core lock (NFR-P2). KEY DECISION: V1 ships NO parallel scheduler (pipeline is sequential-canonical only; "parallel=pure speedup" is forward-looking arch §230), so the epic's literal "byte-identical to a parallel run" is REFRAMED to cross-ENVIRONMENT byte-identity of the single path (project context wins over literal wording; building a parallel scheduler to diff is speculative — CLAUDE.md §5). Expected code footprint = ZERO (tests-only) unless the suite surfaces a real drift → minimal targeted impure-shell hardening + RED-then-green. Scope-fences: memoization cache=Epic 5 (cross-RUN, not cross-ENV); neg-assurance wrapper=4.1; integrity lint=4.2; randomized-canary containment=4.4; $X=7.1; LLM=Epic 6; --resume CLI=DF-3-4-A/7.1. Frozen/reused: 1.1/1.2/1.3/1.6/2.x/3.1/3.2/3.3/3.4 (verify NO diff). Areas APAA-PIPELINE (e2e) + APAA-PORT (NEW). AI-E2-1 test-existence gate before review flip.
  epic-3-retrospective: done  # retrospective complete 2026-06-27 (epic-3-retro-2026-06-27.md): all 5 stories done (661 passed, mypy clean, ≤1200 lines); honest-degradation keystone verified non-bypassable (exhaustion→SKIPPED via existing grade_entry, no fabricated audited_*, floor→INSUFFICIENT_COVERAGE/exit3); determinism spine now byte-identical ACROSS environments (3.5 PYTHONHASHSEED/locale/CWD legs; AC4 order-independence lock proven SHARP); reuse-not-fork held at every cost seam (3.1 ceiling_reached IS imported BudgetGuardrails decision). ONLY review FAIL = 3.4 [High]: resume dropped prior non-deep coverage (would flip verdict at floor); the dev's 642-green keystone test MASKED it (vacuous_basic fixture always sorted into skipped remainder) — reviewer caught it by reproducing the divergence + reverting RED-then-green → KEY LESSON AI-E3-1 (keystone tests must be adversarially fixtured vs their own keystone bug). 2 transport-timeout interrupts mid-3.4 left tree broken/4-failing; dev-loop resumed cleanly from disk (process resumability mirrors FR31). AI-E1-1 cross-locale fixtures continued paying off → promoted to standing cross-env determinism suite (zero new encoding FAILs). Defer-inflow 2 (DF-3-1-A 🟢 defensive, DF-3-4-A 🟢 --resume CLI). Carry-forward DF-1-7-B (🟡 interim Python deep over-grading, owned by Story 6.2) STILL OPEN + increasingly urgent (Epic 4 next, then 5/6/7 before Epic-7 dogfood). 6 action items recommended as Epic-4 backlog (L1-E11); process items (test-existence guard AI-E3-2, defer consolidation AI-E3-3) slipping across epics → promote to engineered guards at 4.4. No significant discovery; Epic-4 plan sound

  # ── Epic 4: Negative-Assurance Verdict & Evidence Bundle (Tier-B) ──
  epic-4: done
  4-1-negative-assurance-verdict-semantics: done  # code-review iter-1 PASS (2026-06-27): honest-framing keystone (FR17/NFR-A3) VERIFIED — all 3 verdicts render scope-bounded negative assurance ("no blocking findings WITHIN THE ASSESSED SCOPE", never correct/certified/passed/defect-free); reviewer independently rendered all three + ran the forbidden-phrase scan = ZERO hits. AI-E3-1 keystone adequacy INDEPENDENTLY RE-VERIFIED RED via THREE real source mutations (over-claiming DISCLAIMER → AC2 test FAILS all 3 params; not_covered_inferred=0 → AC3 FAILS; dropped critical_not_examined_deep → AC3 FAILS), source restored byte-clean — assertions sharp not vacuous. No-fork (wraps 1.6/3.3/2.3 read-only; materiality_bar REUSED str); NFR-D3 stamp = envelope created_at, confirmed content_hash==compute_content_hash(payload), excluded from hash. Fresh==resumed byte-identity (3.4 keystone) proven via persisted-envelope read_bytes with the non-deep-in-assessed-prefix mask. DF-2-3-B CLOSED real (computed CriticalSubsystemSet persists + round-trips equal; closure note append-only). designated_but_unmatched correctly classified not-deep. Reviewer re-ran PYTHONIOENCODING=utf-8 pytest tests/apaa/ tests/test_import_paths.py → 694 passed; mypy clean on negative_assurance.py + pipeline.py; single-serializer + no-web-imports gates green; negative_assurance.py 322 / pipeline.py 923 ≤1200; frozen extra=forbid, no float, typed NegativeAssuranceError → PipelineError exit 1. No findings any severity; no new defer. # dev-story 2026-06-27 (claude-opus-4-8, implement): negative-assurance verdict WRAPPER shipped. NEW pure verdict/negative_assurance.py (frozen NegativeAssuranceVerdict + nested ScopeStatement [examined/sampled/each-not-deep-class-its-own-field/critical-deep-vs-not-deep/designated_but_unmatched] + PURE build_negative_assurance_verdict + fixed DISCLAIMER constant + typed NegativeAssuranceError) WRAPS the done 1.6 AuditVerdict + 3.3 floor report + 2.3 CriticalSubsystemSet — no verdict-math fork, no second serializer; materiality_bar REUSED from AuditRequest (str), point-in-time stamp = envelope created_at (NFR-D3, never hashed). Scope-fenced pipeline._assemble_and_persist (shared fresh+resume fold): build wrapper + persist it additively + persist the COMPUTED CriticalSubsystemSet additively (DF-2-3-B CLOSED, closure note appended to deferred-work.md) + expose on AuditResult.negative_assurance (additive __slots__). HONEST FRAMING (FR17/NFR-A3): negative assurance = absence of DETECTED defects within ASSESSED scope, never proven-defect-free. AI-E3-1 keystone-fixture-adequacy: no-over-claim (all 3 verdicts, forbidden-phrase substring scan), did-NOT-cover-every-not-deep-class (≥1 of every class), critical-not-deep narration, fresh-vs-resumed wrapper byte-identity (3.4 mask: non-deep file in assessed prefix) — each demonstrated RED against a REAL source mutation then green. no-float (deep_ratio canonical num/den), secret-safe + non-ASCII café/Cyrillic round-trip (AI-E1-1), AST purity scan, single-serializer + no-web-imports gates green (_MODULES_UNDER_GUARD extended). Areas APAA-VERDICT (TC-APAA-VERDICT-001-01..21, NEW) + APAA-PIPELINE e2e (TC-APAA-PIPELINE-001-31..36). 694 passed, mypy clean, negative_assurance.py 340 / pipeline.py 1006 ≤1200; 1.6 gate/vocabulary/exit-map/1.2 ledger/1.1 serializer/3.2 halt/3.3 floor/2.3 critical UNCHANGED (no working-tree diff). Fences: integrity-lint=4.2, evidence-bundle=4.3, secret-containment CI=4.4, Prosecutor/HITL=Epic6, --resume CLI=DF-3-4-A. AI-E3-2/E2-1 test-existence gate honored before review flip. # create-story 2026-06-27: negative-assurance verdict WRAPPER (FR17/NFR-A3) — NEW pure verdict/negative_assurance.py WRAPS the done 1.6 AuditVerdict + 3.3 floor report + 2.3 CriticalSubsystemSet (no verdict-math change, no fork) adding a structured scope_statement ("examined X, sampled Y, did NOT cover Z" + which critical subsystems were/weren't deep), materiality_bar (REUSED from AuditRequest), a fixed disclaimer constant (no certification/"correct"/"defect-free" over-claim), + the point-in-time stamp = envelope created_at (NFR-D3, NOT in the hashed payload); persist the wrapper + the COMPUTED CriticalSubsystemSet additively to .apaa/state/ (closes DF-2-3-B / AI-E3-5) via the EXISTING writer, expose on AuditResult; wired in the SHARED _assemble_and_persist fold so fresh==resumed bytes (3.4 keystone). Negative assurance = absence of DETECTED defects within ASSESSED scope, never proven-defect-free. AI-E3-1 keystone-fixture-adequacy: no-over-claim + did-NOT-cover-every-not-deep-class + fresh-vs-resumed-byte-identity fixtures each span every preserved class + demonstrated RED before trusted. Single serializer, frozen extra=forbid, no float, typed NegativeAssuranceError, FastAPI-free (joins _MODULES_UNDER_GUARD), ≤1200 lines. Area APAA-VERDICT + APAA-PIPELINE e2e. Fences: integrity-lint=4.2, evidence-bundle=4.3, secret-containment CI suite=4.4, Prosecutor/HITL=Epic6, --resume CLI=DF-3-4-A. 1.6 gate/vocabulary/exit-map/1.2 ledger/1.1 serializer/3.2 halt/3.3 floor/2.3 critical UNCHANGED
  4-2-referential-integrity-lint-of-on-disk-state: done  # code-review iter-2 PASS (2026-06-27): iter-1 [High] no-crash keystone FAIL (FR26/AC6/AR10) RESOLVED + adversarially re-verified — shell catch broadened FileNotFoundError→OSError genuinely subsumes IsADirectoryError+PermissionError+FileNotFoundError → each routes to an unreadable_artifact finding (NFR-S1 preserved: token=type(exc).__name__, no payload/path bytes); breadth NOT too broad (StoreIntegrityError/ValidationError still caught first + token-ized distinctly; WorkspaceContainmentError is ValueError-not-OSError so a containment escape still propagates; typed IntegrityLintError programmer-raise unchanged; 1.3 read_envelope/StoreIntegrityError semantics untouched). [Med] TC-APAA-STORE-001-108 plants a real state/evil.json DIRECTORY: weakened no-catch walk RAISES OSError (RED proof real), real lint RETURNS consistent=False + unreadable_artifact naming state/evil.json, does NOT raise (assertion sharp on kind+locator). NO REGRESSION: reviewer re-ran PYTHONIOENCODING=utf-8 pytest tests/apaa/ tests/test_import_paths.py → 721 passed (720→721); mypy clean; integrity.py 441 non-blank lines ≤1200; intact-passes floor (2 real cartridges) zero findings; no-web-imports+single-serializer AST gates green; reuse-not-fork; DF-1-3-A closure intact; frozen extra=forbid/no-float; frozen surfaces no tracked diff. All 7 ACs MET; no unresolved findings; no new defer. # dev-story FIX iter-1 (2026-06-27, claude-opus-4-8): resolved the iter-1 [High] no-crash keystone FAIL — broadened the enumerate-and-read shell catch from FileNotFoundError to OSError (subsumes FileNotFoundError raced-delete + IsADirectoryError a non-file <sha>.json directory + PermissionError), so a non-file *.json entry now converts to an unreadable_artifact finding instead of escaping (NFR-S1 preserved — detail names only the locator + error-class token; typed IntegrityLintError programmer-error raise unchanged). Added the planted-directory keystone test test_non_file_json_entry_is_a_typed_finding_not_a_crash (TC-APAA-STORE-001-108): on a real vacuous_basic tree it creates state/evil.json as a DIRECTORY, asserts the weakened no-catch walk RAISES OSError (RED proof real) and the real lint RETURNS a report (consistent=False, unreadable_artifact finding naming state/evil.json) and does NOT raise. RED-then-green demonstrated explicitly: with the original four-exception catch the new test FAILED with PermissionError; after broadening to OSError it passed. PYTHONIOENCODING=utf-8 pytest tests/apaa/ tests/test_import_paths.py → 721 passed (720→721, +1); mypy clean on store/integrity.py; integrity.py 438 non-blank lines (≤1200); frozen surfaces unchanged. Both ### Review Findings ticked. # code-review iter-1 FAIL (2026-06-27): one [High] KEYSTONE violation — the FR26/AC6/AR10 no-crash contract leaks an uncaught OSError on a non-file *.json entry. The enumerate-and-read shell catches only (StoreIntegrityError, CanonicalSerializationError, ValidationError, FileNotFoundError); _list_locators uses glob("*.json") which ALSO matches directories, so a directory (or permission-denied entry) named <sha>.json under .apaa/ — an interrupted/partial write, the exact corruption this lint exists to catch — is enumerated then read, raising IsADirectoryError (POSIX) / PermissionError (Windows), both OSError-not-FileNotFoundError, which ESCAPES lint_referential_integrity. REPRODUCED LIVE: planting state/evil.json as a directory on a real vacuous_basic run_audit tree crashes with PermissionError instead of returning an unreadable_artifact finding. The AC6 test set (tamper/corrupt-bytes/unknown-field) does NOT cover this class → AI-E3-1 recurrence. FIX: broaden the shell catch to OSError (subsumes FileNotFoundError/IsADirectoryError/PermissionError); _read_failure_finding already token-izes the generic case; NFR-S1 preserved. Add a planted-directory keystone test demonstrated RED. Otherwise pass-quality: all other ACs met, RED-then-green keystones genuine, intact-passes floor over 2 real cartridges green, DF-1-3-A closure real, reuse-not-fork, frozen surfaces unchanged, 720 passed/mypy clean/437 lines. Reviewer-re-ran PYTHONIOENCODING=utf-8 pytest tests/apaa/ tests/test_import_paths.py → 720 passed. # dev-story 2026-06-27 (claude-opus-4-8, implement): FR26/NFR-A2 referential-integrity LINT shipped — NEW pure store/integrity.py (frozen IntegrityFinding[closed-enum kind]/IntegrityReport[sorted findings + consistent bool + per-kind int counts]/typed IntegrityLintError + PURE _resolve_references resolver over an in-memory _ReadArtifact set + impure enumerate-and-read shell over the 1.3 reader) walks the on-disk .apaa/ tree + proves every cross-reference resolves: (a) prev_hash chain (every non-genesis prev_hash resolves to a present content_hash — V1 chain semantics LOCKED against the real genesis-headed content-addressed writer, NOT a single linear chain); (b) content-addressed filename↔internal content_hash for state/+findings/ <sha>.json [DF-1-3-A gap CLOSED as a lint finding; assignments/<partition_id>.json EXCLUDED — keyed by partition_id not a payload sha]; (c) verdict ordered_findings[].recording_id → findings/ Recordings (+ orphan); (d) partition-plan partitions[].partition_id → assignments/<partition_id>.json (+ orphan); (e) unreadable/tamper → typed finding. KEYSTONE (FR26/AR10/NFR-R1): a broken reference is a TYPED IntegrityFinding in the returned report, NEVER a raise; only raise = IntegrityLintError on a non-ApaaStoreReader arg. COMPLEMENTARY to 1.3 StoreIntegrityError (content tamper) — REUSES read_envelope/StoreIntegrityError/compute_content_hash/ApaaStorePaths + the _list_locators sorted pattern + mirrored pipeline producer tokens (no circular import, no second tamper-type/serializer/enumerator/producer-registry). AI-E3-1: keystone tests PLANT a REAL on-disk break of every class (deleted findings/assignment dangling, bogus non-genesis prev_hash, renamed <sha>.json, deleted plan orphan, tampered+corrupt+unknown-field artifact) on a real run_audit tree + demonstrate RED via DURABLE weakened-resolver/no-catch-walk helpers + an intact-store-passes floor over real vacuous_basic AND clean_control trees (consistent=True/empty). Pure (AST purity + banned-import scan)/frozen extra=forbid/no-float (int counts; single 1.1 serializer)/order-independent+byte-stable report/secret-safe (repo-relative locators/ids/kinds only; non-ASCII café/Cyrillic round-trip; planted leak token ABSENT; no abs-path)/read-only (no .apaa/ write). NO pipeline-stage/CLI/HTTP wiring (DN-WIRING; bundle-consumer fenced to 4.3). _MODULES_UNDER_GUARD extended-not-forked; single-serializer + no-web-imports gates green. Areas APAA-STORE (TC-APAA-STORE-001-82..107, 26 new). 720 passed, mypy clean, integrity.py 437 non-blank lines (≤1200); 1.1 envelope/1.3 reader+paths+StoreIntegrityError/1.2 recording+ledger/2.4 partitioner/4.1 wrapper+critical-set UNCHANGED (no working-tree diff). DF-1-3-A integrity gap CLOSED (append-only deferred-work note); DF-1-3-B left open with note. No new defer. AI-E3-2/E2-1 test-existence gate honored before review flip. Fences: evidence-bundle=4.3, secret-containment CI suite=4.4, Prosecutor/HITL/decision-record=Epic6, --resume CLI=DF-3-4-A. # create-story 2026-06-27: referential-integrity LINT of on-disk .apaa/ state (FR26/NFR-A2, Tier-B) — NEW PURE store/integrity.py walks the persisted tree + proves every cross-reference resolves (prev_hash chain intact; content-addressed filename==internal content_hash [DF-1-3-A gap closure]; run-state→findings/ledger recording_id refs; partition-plan→assignments/<id>; orphans). COMPLEMENTARY to 1.3 StoreIntegrityError (content-hash tamper) not a duplicate — REUSE read_envelope/StoreIntegrityError/compute_content_hash/ApaaStorePaths/_list_locators pattern + pipeline producer tokens, no fork/second-tamper-type. KEYSTONE: a broken reference is a TYPED IntegrityFinding in the returned IntegrityReport, NEVER a raise (FR26/AR10/NFR-R1); only raise=IntegrityLintError on a non-reader arg. Frozen IntegrityFinding(closed-enum kind)/IntegrityReport(sorted findings + consistent bool + per-kind counts); PURE _resolve_references core + thin impure enumerate-and-read shell (AR8); no float/clock/uuid/random; secret-safe (repo-relative locators/ids/kinds only, NFR-S1). V1 chain semantics = every non-genesis prev_hash resolves to a present content_hash (content-addressed spine, not single linear chain — lock vs real writer). decisions/→assignment fenced to Epic6 (decisions/ empty in V1). AI-E3-1: keystone tests PLANT a real break of every class (dangling/broken-chain/renamed-sha/orphan/tamper) + intact-store-passes over a REAL run_audit tree + demonstrate RED before trusting. Read-only (no .apaa/ write); no pipeline-stage/CLI/HTTP wiring (DN-WIRING; bundle-consumer fenced to 4.3). Areas APAA-STORE (TC-APAA-STORE-001-NN) + APAA-PIPELINE e2e. import-isolation gate extended. Fences: evidence-bundle=4.3, secret-containment CI suite=4.4, Prosecutor/HITL/decision-record=Epic6, --resume CLI=DF-3-4-A. 1.1 envelope/1.3 reader+paths+StoreIntegrityError/1.2 recording+ledger/2.4 partitioner/4.1 wrapper+critical-set UNCHANGED referential-integrity LINT of on-disk .apaa/ state (FR26/NFR-A2, Tier-B) — NEW PURE store/integrity.py walks the persisted tree + proves every cross-reference resolves (prev_hash chain intact; content-addressed filename==internal content_hash [DF-1-3-A gap closure]; run-state→findings/ledger recording_id refs; partition-plan→assignments/<id>; orphans). COMPLEMENTARY to 1.3 StoreIntegrityError (content-hash tamper) not a duplicate — REUSE read_envelope/StoreIntegrityError/compute_content_hash/ApaaStorePaths/_list_locators pattern + pipeline producer tokens, no fork/second-tamper-type. KEYSTONE: a broken reference is a TYPED IntegrityFinding in the returned IntegrityReport, NEVER a raise (FR26/AR10/NFR-R1); only raise=IntegrityLintError on a non-reader arg. Frozen IntegrityFinding(closed-enum kind)/IntegrityReport(sorted findings + consistent bool + per-kind counts); PURE _resolve_references core + thin impure enumerate-and-read shell (AR8); no float/clock/uuid/random; secret-safe (repo-relative locators/ids/kinds only, NFR-S1). V1 chain semantics = every non-genesis prev_hash resolves to a present content_hash (content-addressed spine, not single linear chain — lock vs real writer). decisions/→assignment fenced to Epic6 (decisions/ empty in V1). AI-E3-1: keystone tests PLANT a real break of every class (dangling/broken-chain/renamed-sha/orphan/tamper) + intact-store-passes over a REAL run_audit tree + demonstrate RED before trusting. Read-only (no .apaa/ write); no pipeline-stage/CLI/HTTP wiring (DN-WIRING; bundle-consumer fenced to 4.3). Areas APAA-STORE (TC-APAA-STORE-001-NN) + APAA-PIPELINE e2e. import-isolation gate extended. Fences: evidence-bundle=4.3, secret-containment CI suite=4.4, Prosecutor/HITL/decision-record=Epic6, --resume CLI=DF-3-4-A. 1.1 envelope/1.3 reader+paths+StoreIntegrityError/1.2 recording+ledger/2.4 partitioner/4.1 wrapper+critical-set UNCHANGED
  4-3-evidence-bundle-export-no-source-retention: done  # code-review iter-1 PASS (2026-06-28): reviewer re-ran PYTHONIOENCODING=utf-8 pytest tests/apaa/ tests/test_import_paths.py → 742 passed, mypy clean on evidence/bundle.py+__init__.py, single-serializer + no-web-imports (incl. NEW no-governance-evidence-coupling subprocess assertion) gates green, bundle.py 334 / pipeline.py 1160 ≤1200. NO-SOURCE-RETENTION keystone VERIFIED structural + non-bypassable: traced every EvidenceBundle leaf — str/int/bool/Fraction + frozen sub-models (4.1 NegativeAssuranceVerdict, 2.2 CoverageReport, value-free 1.6 Recording findings, 4.2 IntegrityFinding{detail=locator+error-token only}); NO field can hold a source byte/raw excerpt/secret value/abs-path; secret finding is value-free at source (secret_scan _evidence_for computes-then-DISCARDS raw value; FindingDraft carries only locator/rule_id/advisory). AC2 cartridge plants REAL distinctive source sentinel (EVIDENCE_SENTINEL_*) + secret (PLANTED*); full run_audit_detailed+export+persist proves BOTH ABSENT from bundle bytes + persisted artifact + whole .apaa/ tree WHILE bundle non-empty + secret finding present at src/config.py; RED demonstrated (payload-injection byte-absence check fails). Self-contained (no-coupling-to-governance.evidence asserted in CLEAN subprocess). Determinism: single 1.1 serializer (LIVE Fraction num/den, no 2nd json.dumps), byte-identity + order-independence proven, RED on arrival-order. Additive: coverage_report built in shared _assemble_and_persist fold (fresh==resumed), in-memory ONLY not persisted → .apaa/ bytes byte-identical to 4.1/4.2. PURE (AST scan)/frozen extra=forbid/typed EvidenceBundleError on all malformed inputs/no-over-claim 4.1 forbidden-phrase scan zero hits. 1 Low non-blocking (AC5 round-trip proves payload byte-identity not model reconstruction — persistence optional/unwired, no deserializer this story) + 1 informational (frozen-surface no-diff not git-verifiable: whole APAA tree still untracked; backstopped by 742-green suite). No new defer. # dev-story 2026-06-28 (claude-opus-4-8, implement): FR29 evidence-bundle export shipped. NEW pure evidence/bundle.py — frozen EvidenceBundle (frozen extra=forbid, localized EVIDENCE_BUNDLE_SCHEMA_VERSION) AGGREGATES BY REFERENCE the done 4.1 NegativeAssuranceVerdict (verdict+scope+disclaimer+materiality+deep-%) + 2.2 CoverageReport (per-file depth states + per-depth counts + exact-Fraction deep-%) + verdict-impact-ordered 1.6 Recording findings (locators+ids+advisory+contained_secret indicator ONLY, no value field) + 4.2 IntegrityReport + metadata (apaa_version/commit/materiality_bar); PURE build_evidence_bundle(result, integrity_report, *, commit, apaa_version) folds in-memory records (duck-typed vs AuditResult — no circular pipeline import); bundle_to_canonical_payload/_bytes re-install LIVE Fraction leaves through the single 1.1 canonical.dumps_bytes (no 2nd json.dumps — AST gate green); OPTIONAL impure persist_evidence_bundle → state/ via 1.3 writer (producer apaa.evidence.bundle, content-addressed, containment-checked, stamp = envelope created_at NFR-D3); typed EvidenceBundleError. NO-SOURCE-RETENTION moat is STRUCTURAL (AC3) — every leaf is a repo-relative POSIX locator/id/closed-enum kind/redacted indicator/deterministic statement/Fraction/int/bool; no field holds a source byte/raw excerpt/secret value; no repo_path/abs-path; no float. Additive in-memory AuditResult.coverage_report built in the SHARED _assemble_and_persist fold (fresh==resumed; NOT persisted — floor_report precedent; persisted .apaa/ bytes BYTE-IDENTICAL to 4.1/4.2, AC6). AI-E3-1 RED-then-green keystones: no-source-retention (NEW evidence_sentinel cartridge plants EVIDENCE_SENTINEL_* source byte + PLANTED* secret; full run_audit_detailed + export + persist proves BOTH sentinels ABSENT from serialized bundle bytes + persisted artifact + whole .apaa/ state WHILE bundle non-empty + secret finding present w/ correct locator; RED via leaking-builder injection + manual mutation); completeness over all 3 verdicts (RED if a section dropped → EvidenceBundleError); no-over-claim 4.1 forbidden-phrase scan (RED on injected phrase); determinism byte-identity + order-independence (RED on arrival-order render). Import-isolation _MODULES_UNDER_GUARD extended (no web/LLM) + NEW no-coupling-to-minions_core.governance.evidence assertion (APAA bundle is self-contained, NOT forked from the Minions governance bundle). Scope fences honored: 4.4 CI randomized-canary suite + operated-service lifecycle + new HTTP/CLI/pipeline-stage NOT pulled forward; 4.1/4.2/1.6/1.2/2.2/1.1/1.3 frozen contracts UNCHANGED. Area APAA-EVIDENCE (TC-APAA-EVIDENCE-001-01..20, 20 NEW). 742 passed (721→742), mypy clean on new+edited modules (residual radon import-untyped notes are pre-existing in tool_runner.py), bundle.py 334 / pipeline.py 1160 ≤1200. No new defer. AI-E3-2/E2-1 test-existence gate honored before review flip.
  4-4-secret-containment-property-suite-ci-blocking: done  # code-review iter-1 PASS (2026-06-28, claude-opus-4-8): security-keystone gate proven NON-VACUOUS. Reviewer re-ran 760 passed (742→760) + the new suite under the EXACT merge-blocking security-job flags (pytest tests/security/test_apaa_secret_containment.py -v --strict-markers → 18 passed); single-serializer + no-web-imports gates green; mypy clean. AC1 full-pipeline drive (run_audit_detailed→integrity lint→4.3 bundle build+persist) + REAL artifact-class union verified at runtime (bundle serialized + in-mem payload + _all_apaa_bytes over .apaa/** + response repr all genuinely included — not a token subset). AC3 marquee guard INDEPENDENTLY RE-VERIFIED: mutated the actual frozen bundle model (model_copy commit field) + ran the REAL _artifact_union_blob + _assert_canaries_absent end-to-end → RAISED "SECRET LEAK", proving a real producer leak makes CI red through the PRODUCTION assertion path (Epic-3 3.4 / AI-E3-1 "green keystone that could not catch its bug" class genuinely closed; both committed RED demos traverse the SAME _assert_canaries_absent). AC2 redaction≠suppression verified live (16 hardcoded_secret findings across all 4 planted files incl. non-ASCII src/café/модуль_секрет.py; bundle exports them; Finding structurally value-free). AC4 CI-blocking real (dedicated merge-blocking `security` job runs tests/security/ -v --strict-markers; new file lives there, no excluding marker). AC5 fixed-seed random.Random(4_4_2026) + bounded 3-iter matrix (reproducible not flaky; prints population on failure); non-ASCII Cyrillic secret VALUES contained, non-ASCII PATH round-trips intact into locator. AC6 verify-and-lock sound (zero producer diff = zero leak because the property holds for ARBITRARY seeded values across every detector shape, not just the fixed PLANTED token); 552 lines ≤1200; honest V1-N/A log/OTLP note accurate; DN-DEFER honored (DF-1-3-A lint-CI OUT). 1 informational note (shared module-level _RNG order-sensitivity) non-blocking; no new defer. # dev-story 2026-06-28 (claude-opus-4-8, implement): CI-BLOCKING property-based secret-containment suite SHIPPED at tests/security/test_apaa_secret_containment.py (area APAA-SECURITY, TC-APAA-SECURITY-001-01..16, 18 fns) + NEW cartridges/secret_canary cartridge (source sentinel + every secret shape + non-ASCII secret/path planted at every entry point: config/auth-critical/vacuous-test/non-ASCII-path). Drives FULL run_audit_detailed + integrity lint + 4.3 bundle export + persist; asserts every fixed AND RANDOMIZED (fixed-seed, bounded 3-iter matrix) canary value + source sentinel ABSENT from the artifact-class union {.apaa/ tree, coverage ledger, findings/recordings, evidence bundle in-mem+serialized+persisted, verdict+envelope+negative-assurance wrapper, captured logs, exception messages, response repr} — OTLP spans + app logs honestly N/A in V1 (no logging/opentelemetry import under apaa/; asserted empty-of-canary, documented in docstring not silently dropped). AC2 redaction≠suppression: secret findings PRESENT w/ correct locators (config.py/auth/guard.py/non-ASCII path), bundle non-empty; Recording structurally value-free. AC3 AI-E3-1 RED-then-green (the marquee guard): TWO committed leaking variants (bundle-payload commit-field copy + raw .apaa/ byte) traverse the SAME _assert_canaries_absent union check → RAISE on the leak, then real source-free pipeline PASSES — non-vacuous, the SAME assertion. AC4 durability verified LIVE under the merge-blocking `security` CI job's exact flags (pytest tests/security/ -v --strict-markers → 192 passed/1 skipped); CI-blocking on 3 paths (security job + test job + bulk-migration Layer-1), ZERO new CI job/marker (DN-CI locked). AC5 non-ASCII secret VALUE + PATH contained (explicit UTF-8 search; path locator round-trips intact); reproducible fixed-seed (not flaky), prints population on failure. AC6 verify-and-lock — randomized property surfaced NO leak → NO producer working-tree diff (secret_scan/tool_runner/bundle/pipeline/store/verdict/ledger UNCHANGED); single-serializer + no-web-imports + file-size gates green; mypy clean on new test file; ≤1200 lines; _MODULES_UNDER_GUARD unchanged (tests-only). 760 passed (742→760, +18). DN-DEFER honored: DF-1-3-A optional lint-CI-enforcement OUT (gap already closed by 4.2). No new defer. AI-E3-2/E2-1 test-existence gate honored before review flip. # create-story 2026-06-28: FINAL Epic-4 story — CI-BLOCKING property-based secret-containment suite (FR28 enforcement/NFR-S1/AR9). Consolidates the 2.5/2.6/4.3 per-story producer proofs into ONE durable randomized-canary whole-pipeline property suite at tests/security/test_apaa_secret_containment.py (DN-CI: reuses the merge-blocking `security` CI job — no new CI job/marker, simplest durable form). Asserts every canary secret value + source sentinel ABSENT from the artifact-class union {coverage ledger, findings/recordings, evidence bundle (in-mem+serialized+persisted), all .apaa/ bytes, verdict+envelope, logs, OTLP spans/traces, exception msgs, responses} while findings STILL emitted (redaction≠suppression); AI-E3-1 planted-leak RED-demonstration is itself AC3 (the suite must provably catch a leak); AI-E1-1 non-ASCII secret+path; reproducible-not-flaky (fixed seed/bounded matrix). Tests-only verify-and-lock (NO producer edit unless the property surfaces a real leak → then minimal RED-then-green). Scope-fenced: DF-1-3-A optional lint-CI-enforcement OUT (gap already closed by 4.2); Prosecutor/HITL/memoization/operated-service-lifecycle out. Area APAA-SECURITY (TC-APAA-SECURITY-001-NN). epic-4 already in-progress; epic-4 retro follows.
  epic-4-retrospective: done  # retrospective complete 2026-06-28 (epic-4-retro-2026-06-28.md): all 4 stories done (760 passed, mypy clean, ≤1200 lines); FIRST Tier-B epic; both keystones non-bypassable + STRUCTURAL — honest negative-assurance framing (4.1 forbidden-phrase scan ZERO hits, never correct/certified/defect-free) + no-source-retention (4.3 every bundle leaf is locator/id/enum/Fraction; 4.4 randomized-canary CI-blocking gate proves canaries ABSENT from whole artifact-class union while findings emitted). 4.4 consolidated 2.5/2.6/4.3 producer proofs into ONE durable CI-blocking suite reusing the merge-blocking security job (AR9; AC3 marquee guard verified to RAISE through the REAL _assert_canaries_absent path — Epic-3 "green keystone can't catch its bug" class closed for this epic's keystones). AI-E3-1 keystone-fixture-adequacy APPLIED + verified across 4.1/4.3/4.4 (each RED-then-green). ONLY review FAIL = 4.2 [High]: uncaught OSError on a non-file *.json dir/permission entry ESCAPED the no-crash keystone, missed by keystone tests, caught by reviewer reproducing it LIVE (planted state/evil.json directory); fixed (shell catch broadened FileNotFoundError→OSError) + adversarially re-verified RED-then-green (TC-APAA-STORE-001-108). This is the SAME class as Epic-3 3.4 (too-weak keystone test) — now 2 epics running → KEY LESSON: promote a NO-CRASH-KEYSTONE INPUT-SHAPE CHECKLIST now (AI-E4-1) + mechanize in Epic-6 cartridge harness (AI-E4-2); rule-of-three at occurrence 2→3. Defer-inflow 0 (lowest of project; net-negative-debt epic) — DF-2-3-B CLOSED in 4.1, DF-1-3-A integrity gap CLOSED in 4.2, DF-1-3-B left-open re-homed; inflow trajectory 5→2→2→0 (converged). Carry-forward DF-1-7-B (🟡 interim Python deep over-grading, owned by Story 6.2) now IMMINENT — runway Epic5→Epic6(6.2)→Epic7 dogfood; treat 6.2 as hard deliverable. 7 action items recommended as Epic-5 backlog (L1-E11); thrice-slipping committed test-existence guard (AI-E4-3) + defer back-fill (AI-E4-4, now cheap) carried. No significant discovery; Epic-5 plan sound (one fwd-coupling note: 5.1 cache-key model-checkpoint input owned by Epic-6 6.1 → use a testable V1 placeholder, don't block)

  # ── Epic 5: Reproducible Verdict & Memoization (Tier-B) ──
  epic-5: done
  5-1-cache-key-derivation-recording-producing-closure-ci-canary: done  # code-review iter-2 PASS (2026-06-28, claude-opus-4-8): DF-5-1-A fix verified adversarially — the prompt_template_version slot is genuinely in the key closure (two values→two keys, TC-APAA-CACHE-001-22), covered by the bidirectional canary's REAL-derivation _key_ignoring RED demo (the new _PERTURBATIONS leg), and a clean additive 6.1 placeholder (no live-LLM dep, purity preserved). CACHE_KEY_SCHEMA_VERSION 2 folded; golden regenerated to 2628b9a6…550ca87, re-derived byte-identically. Closure-COMPLETENESS re-swept vs architecture §77 — every enumerated input (content-hash, model checkpoint, prompt-template version, grammar/tool versions, budget/materiality, work-manifest scope, detector-set content-hash) now has a folded slot, NO remaining gap. No regression: single serializer/single hasher (no hashlib/json.dumps in key.py), purity, order-independence, non-ASCII, typed CacheKeyError, frozen extra=forbid, no float, no .apaa/cache/ write, ≤1200 lines (304/392), headless, _MODULES_UNDER_GUARD extended-not-forked. Tests GREEN: 783 apaa+import-paths, 48 cache+structural-gates, mypy clean. DF-5-1-A closure append-only/real. # dev-story fix iter-1 (2026-06-28, claude-opus-4-8): RESOLVED the sole CONCERNS finding DF-5-1-A (Low) — added the `prompt_template_version` closure slot (architecture §77 input) mirroring the model_checkpoint DN-PLACEHOLDER pattern (stable V1_PROMPT_TEMPLATE_VERSION="v1-no-prompt-template", additive-shaped for a clean 6.1 substitution), folded it into _closure_payload + a non-blank validator leg, bumped CACHE_KEY_SCHEMA_VERSION 1→2, regenerated the pinned golden to 2628b9a6…550ca87 (documented intentional invalidation). Added a prompt_template_version perturbation-matrix leg (RED-then-green via the _key_ignoring keystone demo) + TC-APAA-CACHE-001-21/22 (placeholder default + drift seam) — two prompt-template values now derive two keys, closing the future silent-cache-staleness hole at 6.1. DF-5-1-A CLOSED append-only in deferred-work.md. Preserved everything that passed (single serializer/single hasher, purity, order-independence, non-ASCII, typed CacheKeyError, frozen extra=forbid, no float, no .apaa/cache/ write, ≤1200 lines, headless). 783 passed (was 779; +4) / mypy clean / structural gates green. # code-review iter-1 CONCERNS (2026-06-28, claude-opus-4-8): tests independently GREEN (779 apaa+import-paths, 192+1-skip security, 44 cache+structural-gates, mypy clean; golden 14fc9123… re-derived byte-identical). All V1 ACs MET; single-serializer/single-hasher (no hashlib/json.dumps in key.py — composes compute_content_hash/dumps_bytes), purity (AST-scanned no clock/uuid/random/float/FS/LLM), order-independence, non-ASCII stability, typed CacheKeyError, frozen extra=forbid, DN-PLACEHOLDER + DN-DETECTORSET all verified; canary keystone-adequacy NON-VACUOUS (_key_ignoring traverses the REAL derivation, each leg RED-then-green). NO High/Med. ONE Low forward-coupling finding → DF-5-1-A: architecture §77 enumerates `prompt-template version` as a closure key input but it has NO key slot — out of V1 scope (no live LLM in Tier-A) yet, unlike model_checkpoint, given no placeholder slot, so 6.1 must fold it (CACHE_KEY_SCHEMA_VERSION bump lever permits a clean additive add) to avoid a future silent-cache-staleness hole. Interim doc-only note recommended in key.py. Status review→in-progress for the next dev round (apply the Low fix or accept-and-promote). Frozen Epic-1..4 surfaces unedited. # dev-story 2026-06-28 (claude-opus-4-8, implement): PURE cache-key derivation apaa/cache/key.py shipped over the FULL recording-producing closure (content-hash + detector-SET content-hash [DN-DETECTORSET frozen FROZEN_DETECTOR_SET of DetectorDescriptor(rule_id+code_identity+config), order-independent, empty→CacheKeyError — the AR6/5.3 lever] + grammar_version [1.4] + tool_versions + budget/materiality + sorted work-manifest+critical scope + model_checkpoint V1_MODEL_CHECKPOINT="v1-heuristic-no-llm" [DN-PLACEHOLDER, shaped for additive 6.1 substitution]) COMPOSING the single 1.1 dumps_bytes+compute_content_hash (no 2nd hasher; hashlib not imported) + bidirectional CI CANARY (golden 14fc9123… + 10-input perturbation matrix closure→key, each leg RED vs an ignoring-derivation per AI-E4-1; same-closure→same-key; order-independent; non-ASCII AI-E1-1). PURE/no-float/typed CacheKeyError(ValueError) AR8/AR10; checkpoint_drift DETECTION SEAM (two values→two keys), live capture deferred to 6.1. _MODULES_UNDER_GUARD extended-not-forked; single-serializer+no-web-imports+file-size gates green; mypy clean. NO .apaa/cache/ byte, NO cli/HTTP/new-CI-job. Area APAA-CACHE TC-APAA-CACHE-001-01..20. 971 passed/1 skipped (760 Epic-4 baseline + new). Scope fences 5.2 store/5.3 invalidation/6.1 live-capture OUT; no new defer. AI-E4-1/E4-7/E1-1 discharged. # create-story 2026-06-28: PURE cache-key derivation apaa/cache/key.py over the FULL recording-producing closure (content-hash + detector-SET content-hash [DN-DETECTORSET: declared frozen descriptor set, NOT a human apaa_version string — the AR6/5.3 invalidation lever] + tree-sitter grammar_version [REAL, recorded by 1.4] + tool versions + budget/materiality + work-manifest scope + model_checkpoint V1 PLACEHOLDER [DN-PLACEHOLDER: live API-response capture owned by Epic-6 6.1; stable testable constant shaped for clean additive substitution — do NOT build/block on 6.1]) COMPOSING the single 1.1 serializer dumps_bytes + compute_content_hash (no 2nd hasher) + CI CANARY (golden key + per-input perturbation matrix: closure changes→key changes / same closure→same key — the AR5/AI-E4-1 honesty guard, each leg RED-then-green vs a derivation that ignores that input). PURE/no-float/clock-free/typed CacheKeyError (AR8/AR10); checkpoint_drift DETECTION SEAM here (two values→two keys), live capture deferred to 6.1. SCOPE FENCE: 5.2 memo store + 5.3 invalidation/rejected-finding key-busting + 6.1 live LLM capture OUT; NO .apaa/cache/ write, NO cli/HTTP/new-CI-job. Area APAA-CACHE (TC-APAA-CACHE-001-NN). _MODULES_UNDER_GUARD extended; structural gates stay green. epic-5 flipped in-progress (first story). AI-E4-1/E4-4/E4-7/E1-1 carried.
  5-2-content-addressed-memoization-store: done  # code-review iter-1 PASS (2026-06-28, claude-opus-4-8): tests independently GREEN (1000 passed / 1 skipped / 4 subtests over tests/apaa/+tests/security/+tests/test_import_paths.py; memo_store.py 23 passed; mypy clean). ALL 6 ACs met. Keystones verified adversarially: AC2 HIT==MISS byte-identity SOUND (canonical bytes of HIT reconstruction == MISS recompute; RED-against-mutation leg has teeth; idempotent re-store byte-identical; NFR-D3 run_id/created_at excluded — confirmed in envelope.py); AC3 tamper→MISS LOAD-BEARING (named typed swallow set, no bare/Exception except; RED leg proves verify_hash=False WOULD serve poison so removing the verify reddens TC-31; wrong-schema re-stamps content_hash so ValidationError trips distinctly; poisoned MISS re-derives correct result); AC4 LOCAL-only + correct-if-wiped + library-only (pipeline.py byte-identical); secret-containment sweep EXTENDED to .apaa/cache/ (TC-SECURITY-17/18, planted-leak CAUGHT). reuse-not-fork ENFORCED (AST single-serializer gate rglobs apaa/**, green; no json/hashlib import; composes canonical/EnvelopeWriter/ApaaStorePaths/ApaaStoreReader/derive_cache_key; key.py unmodified; frozen extra=forbid; no float; ≤1200; headless; import-isolation extended). 5.3 invalidation NOT pulled forward. TWO Low non-blocking forward-flags (no High/Med, no decision-needed/patch): (1) lookup raises WorkspaceContainmentError on a traversal key vs the "lookup never raises" docstring claim — negligible risk (real keys are 64-char sha256 hex, traversal structurally unreachable); test_lookup_containment_traversal_key_is_a_miss is misnamed (asserts a raise). (2) store() inlines the write instead of reusing ApaaStoreWriter (defensible — key-addressed vs content-hash-addressed filename — but docstring/Completion-Notes inaccurately claim "via ApaaStoreWriter"); serializer/hasher NOT forked. Both written to the story Review Findings for the next round; neither blocks done. # dev-story 2026-06-28 (claude-opus-4-8, implement): IMPURE content-addressed memo STORE apaa/cache/memo_store.py (MemoStore.store/lookup over .apaa/cache/<key>.json) COMPOSING 1.1 dumps_bytes/compute_content_hash/EnvelopeWriter + 1.3 ApaaStorePaths/ApaaStoreReader(read_envelope verify_hash=True tamper guard) + 5.1 derive_cache_key (CONSUMED, no re-derive/fork). KEYSTONES proven RED-then-green: AC2 HIT==MISS byte-identity (RED vs a mutating round-trip — drop/reorder) + cache-NEVER-changes-verdict cold-vs-warm + idempotent byte-identical re-store (NFR-D3 run_id/created_at excluded from content-hash); AC3 tamper/content-hash-mismatch/corrupt/non-UTF8/wrong-schema(extra=forbid)/non-file/permission-denied → MISS, the NAMED typed set (StoreIntegrityError/CanonicalSerializationError/ValidationError/FileNotFoundError/PermissionError/OSError) SWALLOWED into a MISS (DN-MISS — the ONE place a store error is swallowed vs 1.3 which raises; NO bare except, RED vs a verify_hash=False trusting reader; poisoned MISS re-derives the CORRECT result = reproducibility≠correctness); AC4 LOCAL-only + correct-if-cache-wiped; AC5 non-ASCII café/Cyrillic HIT==MISS + containment-rejects-traversal-key (WorkspaceContainmentError before any write) + ≤1200 (211 lines) + impure-shell. EXTENDED the 4.4 secret-containment suite to sweep .apaa/cache/ (TC-APAA-SECURITY-001-17/18: memo-cache entry canary-absent + planted-cache-leak CAUGHT) + import-isolation gate for apaa.cache.memo_store (_MODULES_UNDER_GUARD extended-not-forked, impure so NOT a purity guard). DN-WIRING = library-only (pipeline.py UNMODIFIED, byte-identical to today; live short-circuit deferred to Epic-6). Single-serializer AST gate green (no 2nd json.dumps/hashlib). Area APAA-CACHE TC-APAA-CACHE-001-23..45. Tests: 1000 passed / 1 skipped / 4 subtests; mypy clean on memo_store.py. NO new defer; 5.3 invalidation/key-busting + 6.1 live-LLM/checkpoint + shared/network cache + cli/HTTP/new-CI-job all OUT. # create-story 2026-06-28: content-addressed memo STORE (apaa/cache/memo_store.py — IMPURE shell) over the fixed .apaa/cache/ tree, keyed on the 5.1 derive_cache_key(closure). KEYSTONE ACs (the brainstorm reproducibility floor — the cache is an OPTIMIZATION not the sole guarantee): AC2 HIT==MISS byte-identity (a served hit is byte-identical to a recompute) + cache-NEVER-changes-verdict (cold-cache vs warm-cache → byte-identical verdict/ledger); AC3 tamper/content-hash-mismatch/corrupt/non-UTF8/non-JSON/wrong-schema(extra=forbid)/non-file/permission-denied → MISS (reuses the 1.3 read_envelope(verify_hash=True)→StoreIntegrityError tamper guard but SWALLOWS the typed set into a MISS — the ONE place a store error is swallowed, vs 1.3 which raises; AR10/AI-E4-1 no-crash on the cache FS shell — the exact surface 5.1 flagged forward); AC4 LOCAL-only + correct even if cache wiped (G4 shared cache is V4, never the sole guarantee). REUSES 1.1 dumps_bytes/compute_content_hash/EnvelopeWriter + 1.3 ApaaStorePaths/ApaaStoreWriter/ApaaStoreReader containment+tamper-guard + 5.1 cache/key.py (CONSUMES derive_cache_key, no re-derive, no fork). IMPURE (FS I/O — NOT a purity guard module); cached payload pure/no-float, NFR-D3 volatile run_id/created_at excluded from content-hash so content-addressing is cross-run stable. EXTENDS the 4.4 secret-containment suite to sweep .apaa/cache/ (5.1 forward-flag) + import-isolation gate for apaa.cache.memo_store. Area APAA-CACHE TC-APAA-CACHE-001-NN (after 5.1's …-01..22). SCOPE FENCE: 5.3 active invalidation/rejected-finding key-busting (only the NATURAL miss from a changed key is in scope) + 6.1 live LLM/real checkpoint capture + shared/network cache + cli/HTTP/new-CI-job all OUT. DN-WIRING: memo store is library-only OR a thin additive default-equivalent pipeline seam (every existing pipeline test byte-identical). AI-E4-1/E4-4/E4-7/E1-1 carried.
  5-3-cache-invalidation-rejected-finding-key-busting: done  # code-review iter-1 PASS (2026-06-29, claude-opus-4-8, adversarial): independently GREEN (1032 passed / 1 skipped / 4 subtests over tests/apaa/+tests/security/+tests/test_import_paths.py; mypy clean on invalidation.py; single-serializer AST gate green). ALL 6 ACs met. Keystones verified ADVERSARIALLY: under-bust-forbidden NON-VACUOUS (monkey-patched bust_key→no-op makes the AC1 GREEN-leg `lookup(K) is None` fail RED — the bust is load-bearing); over-bust SAFE (recompute byte-identical to cold, 5.2 HIT==MISS); containment empirically rejects sibling-prefix `../../.apaa-evil/...` via is_relative_to BEFORE any delete while keeping contained `../.apaa-evil/...` inside `.apaa` (the NFR-S5 distinction startswith gets wrong); §3.4 immutability — Recording frozen/no `rejected` field, NEVER mutated (rejection is a separate frozen RejectedFinding citing recording_id+key+redacted meta, no source bytes); no-crash named-typed-set swallow on the seam read (corrupt/non-UTF8/tampered/wrong-schema/missing→empty safe-skip, no bare except); reuse-not-fork (no 2nd json/hashlib — grep-confirmed; composes 5.1 key + 5.2 MemoStore + 1.3 ApaaStorePaths/reader + 1.1 serializer/envelope); pipeline.py byte-identical (no invalidation import — DN-WIRING library-only); BustOutcome typed-not-bool; AR5 consumes handed key (locator==cache/<key>.json); ≤1200 lines (447/695); LOCAL-only (repo A bust ≠ repo B); non-ASCII byte-stable (AI-E1-1). 4.4 secret-containment suite extended NON-VACUOUSLY (.apaa/decisions/ rejection class swept: TC-19 asserts decisions_blob non-empty+canaries absent, TC-20 catches a planted leak); _MODULES_UNDER_GUARD extended (import-isolation, impure→not a purity guard). AR6 detector-set lever fully live (known_keys seam, story-sanctioned); live Prosecutor 6.4 / HITL 6.7 trigger is a V1 placeholder seam, NOT a hidden Epic-6 dep. ONE Low doc-precision nit (RejectionLedger docstring "reuses ApaaStoreWriter" but _write inlines the fixed-name-slot byte write — defensible, no serializer fork, same nit accepted on 5-2; non-blocking, written to story Review). No High/Med, no decision-needed/patch, no new defer. Scope fences honored (no 6.4/6.7/6.1, no shared-network cache, no cli/HTTP/CI-job, headless). FINAL Epic-5 story → Epic-5 retro UNBLOCKED. # dev-story 2026-06-29 (claude-opus-4-8, implement/resume — prior session interrupted by transport timeout after writing invalidation.py + status in-progress, BEFORE the test suite): CONFIRMED apaa/cache/invalidation.py complete + ACs-compliant (CacheInvalidator.bust_key/bust_rejected_finding/invalidate_rejections/invalidate_on_detector_set_change → typed BustOutcome; V1 SEAM RejectedFinding [frozen extra=forbid, cites recording_id+key+redacted meta, NO source bytes] + RejectionLedger append-only over decisions/rejection_ledger.json via 1.1 envelope + read_envelope(verify_hash=True) tamper guard; COMPOSES 5.1 derive_cache_key/detector_set_content_hash + 5.2 MemoStore cache/<key>.json slot + 1.3 ApaaStorePaths containment — NO 2nd serializer/hasher) and WROTE the missing tests/apaa/test_cache_invalidation.py (30 proofs, TC-APAA-CACHE-001-46..75). KEYSTONES RED-then-green: AC1 bust_rejected_finding→MISS (under-bust RED: naive no-bust re-serves the stale 🔴 on the SAME lookup the green leg MISSes); AC2 detector-set code_identity edit moves detector_set_content_hash→moves key (NATURAL MISS from 5.1) + invalidate_on_detector_set_change ACTIVELY deletes the orphaned OLD-hash slot (RED vs a re-key-only path leaving it reachable), no-change=no-op, recompute byte-identical; AC3 over-bust SAFE (recompute byte-identical to cold per 5.2 HIT==MISS) vs under-bust FORBIDDEN; AC4 idempotent (absent/deleted/absent, never raises), containment (../../escape + sibling-prefix ../../.apaa-evil raise WorkspaceContainmentError BEFORE any delete, outside file survives — is_relative_to not startswith), sibling-survival, perm-denied delete→BustOutcome(os_error) RED vs propagating Path.unlink, corrupt/non-UTF8/tampered(content-hash mismatch)/wrong-schema(extra=forbid)/missing rejection-ledger→EMPTY safe-skip (named typed set only), pure payload (no float/clock/uuid; envelope hash excludes run_id/created_at NFR-D3), frozen+extra=forbid record, append-only ledger; AC5 stable-AND-correct→after-bust recompute→wiped-cache cold rebuild SAME result, LOCAL-only (repo A bust ≠ repo B), non-ASCII café/Cyrillic key+ложноположительный reason byte-stable (AI-E1-1); AC6 typed BustOutcome (not bare bool), CONSUMES handed key (locator==cache/<key>.json, never re-derives AR5), ≤1200 lines, single-serializer AST gate green (no json.dumps/hashlib). Extended _MODULES_UNDER_GUARD (import-isolation; impure→NOT a purity guard) + 4.4 secret-containment suite (.apaa/decisions/ rejection-record class swept: TC-APAA-SECURITY-001-19/20, planted leak CAUGHT). DN-WIRING=library-only (pipeline.py byte-identical). Tests: 1032 passed / 1 skipped / 4 subtests (was 1000/1; +32) over tests/apaa/+tests/security/+tests/test_import_paths.py; mypy clean on invalidation.py. NO new defer. Scope fences honored: live Prosecutor 6.4 / HITL 6.7 trigger / 6.1 live-LLM+checkpoint / shared-network cache / cli/HTTP/new-CI-job all OUT. FINAL Epic-5 story → review (Epic-5 retro unblocked once 5.3 closes). # create-story 2026-06-28: AR6 ACTIVE-invalidation layer (FINAL Epic-5 story) — apaa/cache/invalidation.py (IMPURE shell) busting cache slots, COMPOSING 5.1 derive_cache_key/detector_set_content_hash (the AR6 lever) + 5.2 MemoStore (cache/<key>.json slot) + 1.3 ApaaStorePaths containment + read_envelope tamper guard; no 2nd serializer/hasher. TWO levers: (a) detector-set-change invalidation FULLY LIVE NOW (5.1 key already folds the detector-set hash → natural MISS; this story ADDS the active delete of orphaned OLD-hash slots + proves recompute-not-re-serve); (b) rejected-finding key-busting via a V1 SEAM — a frozen RejectedFinding record (cites recording_id + cache key + redacted metadata, NO source bytes; Recording stays frozen/immutable, NOT mutated) + RejectionLedger an Epic-6 caller populates. KEYSTONE asymmetry: over-bust SAFE (recompute byte-identical to cold per 5.2 HIT==MISS) / under-bust FORBIDDEN (re-serving a stale rejected 🔴 is the failure to prevent) — both RED-then-green. Idempotent + containment-safe (WorkspaceContainmentError before any delete) + leak-free + no-crash on DELETE/seam edges (perm-denied delete + corrupt RejectedFinding read → typed BustOutcome/skip, never raise — AR10). Stable-AND-correct (clean-repo flaky-vs-stable). Extends 4.4 secret-containment suite (rejection-record class swept) + import-isolation gate (IMPURE, not a purity guard). Area APAA-CACHE TC-APAA-CACHE-001-46..NN (after 5.2's …-45). SCOPE FENCE: live Prosecutor (6.4) / HITL STOP-PROCEED + decision-record writer (6.7) / 6.1 live-LLM+checkpoint / shared-network cache / cli/HTTP/new-CI-job all OUT — V1 builds only the rejection SEAM the Epic-6 trigger populates. Library-only OR thin default-equivalent pipeline wiring (every existing pipeline test byte-identical). epic-5 already in-progress. AI-E4-1/E4-4/E4-7/E1-1 carried.
  epic-5-retrospective: done  # retrospective complete 2026-06-29 (epic-5-retro-2026-06-29.md): all 3 stories done (1032 passed/1 skipped/4 subtests, mypy clean, ≤1200 lines); SECOND Tier-B epic delivered APAA's core promise — reproducibility as a system property (FR27/NFR-D1) — STRUCTURAL + non-bypassable: HIT==MISS byte-identity (a served hit byte-identical to a cold recompute; the memo cache is an OPTIMIZATION that can never change the verdict), tamper→MISS via a NAMED typed swallow set (poisoned MISS re-derives CORRECT — reproducibility≠correctness), over-bust-SAFE/under-bust-FORBIDDEN (a stale rejected 🔴 is never re-served; AR6). 5.1 cache-key folds the FULL recording-producing closure re-swept vs architecture §77 (NO gap). 0 hard review FAILs (5.1 CONCERNS→PASS iter-2 closing DF-5-1-A; 5.2/5.3 PASS iter-1). HEADLINE: AI-E4-1 no-crash input-shape checklist applied PROACTIVELY to the new cache impure shells (the exact surface that FAILed in 3.4 AND 4.2) → the "too-weak keystone test" class did NOT recur — cleanest L1-E11 loop outcome yet. Keystone-adequacy paid off at the DESIGN level (5.1 CONCERNS = closure-completeness gap caught by re-sweeping architecture §77's enumerated inputs; prompt_template_version slot added + CACHE_KEY_SCHEMA_VERSION bumped + golden regenerated). 4.4 secret-containment suite extended to .apaa/cache/ (5.2) AND .apaa/decisions/ (5.3); planted leaks CAUGHT; §3.4 immutability held (Recording never mutated; rejection is a separate frozen RejectedFinding). Recurring Low doc-precision nit across 5.2+5.3 (docstring "reuses ApaaStoreWriter" but inlines a fixed-name slot — defensible, no serializer fork; partial-reuse docstring convention noted AI-E5-7). 2 mid-story session interruptions (5.1 pre-code, 5.3 pre-tests) → orchestrator resumed cleanly from disk, 0 rework (dev-loop resumability robust). Defer-inflow: DF-5-1-A opened AND closed same-epic → net ≈ 0 (trajectory 5→2→2→0→0-net, converged). Carried 🟡 DF-1-7-B (interim Python deep over-grading) now ONE EPIC AWAY — owned by Epic-6 Story 6.2 (full FR7 AST grounding); flagged HARD Epic-6 deliverable before the Epic-7 dogfood (AI-E5-5), 7.2 grade:demo-heuristic-only red-team flag the backstop if it slips. 7 action items recommended as Epic-6 backlog (L1-E11): generalize keystone-adequacy to a "complete-the-declared-set" checklist (AI-E5-1), mechanize fixture-shape coverage in 6.5 cartridge harness (AI-E5-2), BUILD the 4-epochs-slipping committed test-existence guard (AI-E5-3), cheap defer back-fill (AI-E5-4), DF-1-7-B hard close in 6.2 (AI-E5-5). No significant discovery; Epic-6 plan sound — DF-5-1-A closure means 6.1 substitutes real model_checkpoint+prompt_template_version VALUES into the existing slots (no key-shape change)

  # ── Epic 6: Trust Substrate — Self-Audit, Prosecutor & Precision (Tier-B; cut-order boundary) ──
  epic-6: done
  6-1-llm-dispatch-port-minions-orchestrator-adapter: done  # code-review iter-1 PASS: determinism quarantine proven by two orthogonal subprocess gates (pure seam ports/deep_audit ⊬ providers; adapter is the carve-out + ⊬ FastAPI), reuse-by-import no-fork verified, full no-crash matrix RED-then-green + complete-the-declared-set, additive 5.1 substitution (V1 golden byte-identical, cache/key.py untouched), NFR-S1 structural redaction (no source-byte field), 1062 passed/1 skipped, mypy clean, ≤1200 lines, headless. dev-story: PURE LLMDispatchPort + frozen DTOs (ports.py), thin MinionsLLMAdapter reusing LLMProviderOrchestrator.execute_llm BY IMPORT + checkpoint capture (model_checkpoint ← LLMResponse.model), full no-crash matrix RED-then-green (exhaustion/timeout/malformed/budget-halt → LLMDispatchError, drift → CheckpointDriftError), FakeDispatch zero-token double, deep_audit DIP seam + additive 5.1 cache-key substitution (no key-shape change, V1 golden byte-identical); web+no-LLM import gates extended-not-forked; 1062 passed/1 skipped, mypy clean on new files, frozen Epic-1..5 surfaces byte-identical, no .apaa/ write, DF-5-1-A stays closed
  6-2-full-python-ast-grounding-of-audited-deep-claims: done  # code-review iter-1 PASS: DF-1-7-B GENUINELY CLOSED (not relabel) — RED-then-green TC-APAA-AUDIT-001-50 + observable end-to-end verdict migration TC-APAA-PIPELINE-001-41 (zero-def repo flips to INSUFFICIENT_COVERAGE/exit 3); ledger byte-identical (grade_entry signature unchanged, DN-GROUNDED); honest migration (signature-demo moat preserved empirically for the right reason — calculator.py has real defs, deep-% 1/2, not gamed); validator pure/zero-token/never-raises (AR10 matrix), no re-parse (AR7), no-LLM gate extend-not-fork correct; 1083 passed/1 skipped (one transient git-timeout flake unrelated to 6.2, passes on re-run), mypy clean, ≤1200 lines (pipeline 1190 — advisory forward note), headless. # dev-story: FR7 AST-grounding validator (PURE audit/grounding.py::is_deep_claim_grounded, stack-agnostic claim→validated?, Python impl #1, ≥1-Definition grounding fact, no re-parse — AR7) wired into pipeline._grade_non_test_python via claim_present=(claim_emitted AND claim_grounded) into UNCHANGED grade_entry (DN-GROUNDED, ledger byte-identical); zero-def module → audited_shallow. CLOSES DF-1-7-B (AI-E5-5): RED-then-green closure TC-APAA-AUDIT-001-50 + observable verdict migration TC-APAA-PIPELINE-001-41. Honest migration = net-zero cartridge change (all SUTs have real defs → stay deep → signature-demo moat preserved TC-APAA-PIPELINE-001-40). Web+no-LLM gates extended-not-forked; frozen Epic-1..5 byte-identical; 1083 passed/1 skipped, mypy clean (grounding.py), ≤1200 lines, zero-token. DF-1-7-B back-filled+closed in deferred-work.md (AI-E5-4)
  6-3-orphan-dead-code-detector: done  # code-review iter-1 PASS: no-false-accusation moat held by 3 guards (name-match miss + exclusion set [dunders/test_*/ORPHAN_EXCLUSION_NAMES] + name-collision-twin guard, RED-first); attribute-call references captured as callees so a method-via-attribute is never falsely accused; advisory-by-contract (advisory=True, depth_supported=None) proven non-verdict-eligible (TC-APAA-PIPELINE-001-51). DN-PIPELINE-SPLIT pure refactor verified (pipeline_persist.py 268L verbatim lift, pipeline.py 1071<1200, both docstrings, suite byte-green). DN-WHOLE-INDEX no-orphan byte-identity + resume drop+recompute cross-file fix correct (both named byte-identity tests green, 28-test resume suite green). 1112 passed/1 skipped/4 subtests, mypy clean on new modules (only pre-existing radon-stub noise in tool_runner), no-web/single-serializer/file-size gates green, ≤1200, headless, no cli/HTTP/CI/LLM. DF-1-4-A back-filled+CONSUMED + DF-6-3-A filed (6 CC-3 fields). No action items. # dev-story: FR12 orphan/dead-code detector DELIVERED + DF-1-4-A CONSUMED. DN-PIPELINE-SPLIT done FIRST (pure refactor) — persist family extracted to pipeline_persist.py (268 lines), pipeline.py 1190→1071 (<1200), both docstrings document the split, suite byte-green. DN-CONSERVATIVE detector detectors/orphan_code.py::OrphanCodeDetector (rule_id orphan_code, pure run(*, index)->DetectorResult): one whole-index pass derives global callee-name set + name-collision histogram; orphan iff name-not-in-callee AND not-excluded (dunders/test_*/ORPHAN_EXCLUSION_NAMES framework hooks) AND not a name-collision twin → advisory=True, depth_supported=None, composes EXISTING build_recording (FR13), NO coverage entry, sorted findings (AR11), never raises (AR10 typed OrphanCodeError only on non-AstIndex). DN-WHOLE-INDEX wired via _orphan_findings after _detect_per_file in run_audit_detailed (assessed entries on halt) + resume (drop+recompute cross-file over carried∪target — byte-identity fix). Tests: TC-APAA-ORPHAN-001-01..17 (complete-the-declared-set incl. RED-first name-collision guard + no-crash + non-ASCII) + TC-APAA-PIPELINE-001-50..52 (end-to-end + no-orphan byte-identity). 1112 passed/1 skipped/4 subtests, mypy clean, no-web-imports+single-serializer+file-size gates green, all files ≤1200, no cli/HTTP/CI-job change, no live LLM. DF-1-4-A back-filled+CONSUMED + DF-6-3-A (resolved call graph→epic-6-resolved-call-graph) filed with 6 CC-3 fields.
  6-4-adversarial-prosecutor-cut-edge-pass: done  # code-review iter-1 PASS: downgrade-only/never-upgrade CONFIRMED (_CONSERVATISM_RANK RR<NRFR<INSUFF; re-fold only adds blocking weight; critical_subsystems_all_deep preserved from candidate; else-branch a defensive floor that cannot upgrade; -03/-10/-11). PROMOTION FLOOR CONFIRMED (promote IFF advisory∧ast_span∧sign_off; heuristic-only never promoted -04 RED-first; corrob-without-signoff not promoted -06; original never mutated model_copy -05; 1.6 gate predicate/thresholds byte-unchanged). UN-PROSECUTED BYTE-IDENTICAL CONFIRMED (pipeline never passes sign_offs → no promotion reachable in V1; wired once in _assemble_and_persist, run+resume route through; -60/-61). DETERMINISM QUARANTINE CONFIRMED (⊬ providers/apaa.audit via _MODULES_UNDER_GUARD + -20; pure/zero-token; -17 byte-stable). AR10 never-raises (typed ProsecutorError -12; DegradedCondition -13). 1132 passed/1 skipped/4 subtests, mypy clean (pre-existing radon-stub noise only), pipeline.py 1090≤1200, headless, no cli/HTTP/CI/LLM. DF-6-4-A (V2 seam auditor) + 1 Low Review finding DF-6-4-B filed (synthetic cross_partition ast_span passes _has_ast_corroboration — harmless in V1 since no sign_offs; doc/V2-hardening note). # dev-story: implemented verdict/prosecutor.py (PURE FR19 adversarial Prosecutor) + CC#4 cross_partition cut-edge pass + DN-PROMOTE promotion authority. DN-PROMOTE: promote advisory→verdict-eligible IFF AST corroboration (non-empty Locator.ast_span) AND sign-off (recording_id in explicit sign_offs); heuristic-only NEVER promoted (false-accusation floor); corroboration-without-sign-off NOT promoted; original Recording never mutated (model_copy). FR19 downgrade only-ever-more-conservative (re-fold through UNCHANGED evaluate_verdict, min-conservative vs candidate, never upgrade). cross_partition pass re-reads 2.4 cut_edges (SURFACE not PROVE — V1 mitigation; full seam auditor V2/OI2 = DF-6-4-A). DN-WIRE minimal in _assemble_and_persist (run+resume route through), un-prosecuted repo byte-identical. DN-PIPELINE-SIZE: pipeline.py 1071→1090 (measured, NO split). REUSE 1.6 evaluate_verdict/order_findings + 1.5 build_recording (no second verdict math/no fork/no new serializer). Pure zero-token (⊬ providers/FastAPI). Tests TC-APAA-PROSECUTOR-001-01..17,-20 + TC-APAA-PIPELINE-001-60/61; pytest tests/apaa/+security/+import_paths green, mypy clean. DF-6-4-A filed. Status → review.
  6-5-defect-cartridge-self-audit-harness-holdout-clean-controls: done  # code-review iter-1 PASS (2026-06-30): all 6 keystones verified adversarially — golden keys are SETS of (rule_id,verdict_eligible,advisory) never byte/count (NFR-S1/AR4); holdout_vacuous genuinely un-tuned (total_stock/Mock.tally vs tuned compute_total/Mock.calculate) + caught by SAME vacuous_test_ast with NO detector change (frozen surfaces mtime 06-21..06-29 < 6-5 06-30); PRECISION_GATE_STATUS computes NO ≥80% number, % absent, PRECISION_VALUE/MEASURED_PRECISION unbound — OI1 honesty NOT softened; ZERO frozen-contract diff (only tracked apaa diff is __init__.py story-1.1 __version__, mtime-verified); 2-run byte-identical content_hash per cartridge; secret-containment non-vacuous (planted tokens grep-confirmed present in cartridges). Re-ran: cartridge harness 33 passed; full tests/apaa+security+import_paths 1165 passed/1 skipped/4 subtests; files ≤1200 (pipeline 1090, registry 240, harness 475). REUSE-only (stage_cartridge+run_audit_detailed+ApaaStoreReader, no fork), no detector/Prosecutor edit, no cli/HTTP/CI/LLM. No new defer (corpus→N=5+precision number = planned 6.6). # dev-story 2026-06-30: FR20 defect-cartridge self-audit harness DELIVERED. Registry tests/apaa/cartridges/_registry.py (frozen CartridgeSpec/GoldenFinding, CARTRIDGE_REGISTRY designed for N=5, golden keys = SETS of (rule_id,verdict_eligible,advisory) never counts/bytes) + harness tests/apaa/test_cartridge_selfaudit.py (33 cases, area APAA-CARTRIDGE TC-...-001-01..15) assert golden-key true positives (vacuous_test_ast verdict-eligible+sorts-first/FR33, redacted hardcoded_secret value-absent, orphan_code advisory) + clean_control zero-blocking (any 🔴 = NAMED instant fail) + NEW holdout_vacuous cartridge caught with NO detector change (overfitting defense) + evidence_sentinel citation-gaming trap + tool_breadth no-crash row (AI-E4-2/AR10) + 2-run byte-identical content_hash + NFR-S1 secret-containment (planted secrets/sentinels absent from .apaa blob + reprs) + non-ASCII (AI-E1-1). OI1 keystone: PRECISION_GATE_STATUS mechanized marker reports gate PROVISIONAL unconditionally, precision over FINDINGS not repos, NO ≥80% number computed (6.6 owns it). REUSE-only (stage_cartridge+run_audit_detailed+ApaaStoreReader, no fork); NO detector/Prosecutor edit, NO frozen-contract diff (verified empty git diff over coverage_ledger/recording/verdict_gate/detectors/prosecutor/pipeline/store/cache/models), NO cli/HTTP/CI-job/LLM. 1165 passed/1 skipped/4 subtests, no-web+single-serializer+file-size+4.4-secret-containment gates green, mypy clean, files ≤1200 (475+240). No new defer (corpus→N=5+precision number = planned 6.6). AI-E5-1/E5-2/E5-3/E4-2/E1-1. # create-story 2026-06-30: FR20 defect-cartridge self-audit harness (Tier-B) — parametrized cartridge registry (designed for N=5, populated phased 3→5) with per-cartridge golden expected-findings keys + kind∈{planted_defect,clean_control,holdout,trap}; harness tests/apaa/test_cartridge_selfaudit.py runs FULL deterministic pipeline over corpus asserting golden-key true positives (vacuous_test_ast/secret/orphan_code) + clean-control zero-blocking (any 🔴 = instant CI fail, false-accusation floor) + hidden holdout (overfitting defense) + citation-gaming trap + no-crash row (AI-E4-2 mechanized) + determinism (2-run byte-identical content_hash) + NFR-S1 secret-containment + non-ASCII (AI-E1-1). OI1 keystone: precision measured over FINDINGS not repos, ≥80% gate mechanically reported PROVISIONAL until N≥5 (PRECISION_GATE_STATUS marker) — NO precision number computed (deferred to 6.6). REUSE stage_cartridge + run_audit_detailed + ApaaStoreReader (no fork); NO detector/Prosecutor edit, NO frozen-contract diff, NO new CI job/HTTP/cli flag. AI-E5-1/E5-2/E4-2/E1-1. Scope-fenced vs 6.6 (precision replay/number/validation protocol) + 6.7 (HITL). Area APAA-CARTRIDGE.
  6-6-precision-replay-harness-validation-protocol: done  # code-review 2026-07-01: PASS (clean, 0 findings). Verified INDEPENDENTLY: 1181 passed/1 skipped/4 subtests re-run green; precision=1/1 exact-Fraction (no float, AR4); FP-def correct (clean-repo advisory findings NOT miscounted, clean_repo_fp=0; RED-first -05 proves a synthetic blocking clean-repo finding depresses precision); OI1 KEYSTONE HELD — gate NOT flipped (protocol_cleared=False; 5 rows but only 3 distinct defect-rule classes, honestly deferred in DF-6-6-A); zero-token provider-free guard passes; match-key byte-identical to 6.5 (no fork); mypy clean; frozen surfaces untouched (mtime); DF-6-6-A carries 6 CC-3 fields, defer-schema exits 0. Senior Developer Review (AI) written into story file. # dev-story 2026-06-30: DELIVERED. PURE precision/replay_harness.py (compute_precision → TP/FP/FN over the 6.5 golden keys → exact-Fraction precision string/AR4) + frozen PrecisionResult/CartridgePrecisionRow (DN-RESULT-SCHEMA) + finding_match_key/golden_match_key (SHARED 6.5 key/DN-MATCH-KEY-REUSE) + precision_gate_status_for (REUSES 6.5 marker, no fork). Committed precision-validation-protocol.md (who-validates/expert-hours/adjudication-method/per-metric-pass-fail/phased-3→5). Test tests/apaa/test_precision_replay.py (area APAA-PRECISION, TC-APAA-PRECISION-001-01..15) RED-first clean-repo FP denominator (FP=emitted-not-golden BLOCKING key; advisory over-emissions NOT false accusations, matches 6.5 max_blocking==0 floor) + RED-first no-overclaim provisional gate. OI1 KEYSTONE — gate NOT flipped: computed precision 1/1 reported PROVISIONAL (protocol_cleared=False; no human adjudication; only 3 distinct defect-rule classes across 5 cartridge rows). DF-6-6-A filed (grow corpus to N=5 distinct classes + run adjudication → flip). REUSE-only; frozen Epic-1..6 + _registry.py shape unchanged (mtime-verified); no detector/Prosecutor/cli/HTTP/CI/LLM edit. 1181 passed/1 skipped/4 subtests, mypy clean, no-web/single-serializer/file-size gates green, files ≤1200. # create-story 2026-06-30: precision replay harness + validation protocol (Tier-B, OI1 N=5/phased/precision-over-findings/provisional). Pure precision/replay_harness.py diffs emitted findings vs the 6.5 CARTRIDGE_REGISTRY golden keys → TP/FP/FN → fixed-precision precision number (Decimal/Fraction string, NO float/AR4); clean repos (clean_control/trap/no_crash) supply the false-positive denominator (R6 — blocking-on-clean = FP). Committed validation protocol precision-validation-protocol.md fixes who-validates/expert-hours/adjudication-method/per-metric-pass-fail/phased-population. REUSE/extend the 6.5 precision_gate_status() marker — gate reported PROVISIONAL until N≥5 with protocol applied (no over-claim, OI1 honesty keystone). REUSE-only over 6.5 (registry+stage_cartridge+run_audit_detailed+ApaaStoreReader+match-key, no fork); NO detector/Prosecutor edit, NO frozen-contract/_registry.py-shape change, NO new CI job/HTTP/cli flag. Scope-fenced vs 6.7 (HITL) + Epic 7 (Minions dogfood real-repo precision). Area APAA-PRECISION (TC-APAA-PRECISION-001-NN).
  6-7-hitl-stop-proceed-escalation-append-only-decision-record: done  # code-review 2026-07-02 iter-2 PASS: the iter-1 blocking [Review][Patch] append-only content-address collision is GENUINELY FIXED — verified INDEPENDENTLY. Reviewer reverted the chain_prev_hash fold in a scratch copy → regression test RED (2 files, assert 2==3); with the fold, green (3 distinct files). Three fresh adversarial probes (first-two-identical head-genesis edge; three-identical-in-a-row; interleaved identical/different) all yield distinct content-addressed files + an ordered genesis-rooted NON-cyclic chain whose head never resets — no residual collision path (append is synchronous so each identical re-log resolves a distinct tail as chain_prev_hash). Keystones re-verified non-regressed: escalation.py PURE/zero-LLM-token/DEFAULTS-TO-STOP/PARKS-AT-STOP/no-PROCEED-without-HumanDecision (escalation.py + __init__.py untouched, mtime 07-01; only decision_record.py changed 07-02); no forked persistence/no UPDATE-DELETE/no LLM-web imports; chain_prev_hash is a 64-hex hash (secret-free, 4.4 suite sweeps decisions/); headless; files ≤1200. Re-ran tests/apaa/+security/+import_paths = 1220 passed/1 skipped/4 subtests. All ACs met. Senior Developer Review (AI) iter-2 appended to story file. Status → done. # dev-story 2026-07-01 (fix iter-1, claude-opus-4-8[1m]): RESOLVED the blocking [Review][Patch] append-only content-address collision. DecisionRecordWriter.append() now folds the resolved chain position (prev_hash) into the HASHED persisted payload under CHAIN_PREV_HASH_KEY="chain_prev_hash", so each chain link is a DISTINCT content-addressed decisions/<content_hash>.json even for byte-identical resolutions — the AC4 re-log-same-deferred-STOP no longer collides/overwrites the prior file, no self-cyclic prev_hash, no genesis-orphan (reviewer's PREFERRED "fold chain-position in" option). REUSE preserved: no fork of 1.1 serializer/envelope or 1.3 writer/reader; NO UPDATE/DELETE; EscalationResolution.to_payload() 6-key schema UNCHANGED (fold is at the RECORD/writer layer); envelope prev_hash read-side chain unchanged; secret-free (folded value is a 64-hex content-hash/genesis token, NFR-S1, swept by 4.4). RED-first regression test_identical_resolutions_append_two_distinct_files_and_intact_chain (TC-APAA-HITL-001-31): RED on old code (2 files/2-cycle/genesis-reset), green with fix (3 distinct files + ordered genesis-rooted non-cyclic chain). Full suite tests/apaa/+security/+import_paths = 1220 passed/1 skipped/4 subtests; mypy clean on the 3 governance modules; decision_record.py 253/escalation.py 396/__init__.py 24/test 621 all ≤1200; other keystones (default-STOP/park-at-STOP/zero-LLM/STOP-logged-if-deferred/frozen-surfaces) untouched. Status → review. # code-review 2026-07-01 iter-1 FAIL (concerns→blocking): safety keystone GENUINELY held + re-verified — gate DEFAULTS-TO-STOP, PARKS-AT-STOP on timeout, NO PROCEED path without an explicit HumanDecision; PURE zero-LLM-token (import loads 0 providers/fastapi); STOP-logged-if-deferred; secret-containment RED-then-green; frozen Epic-1..6 untouched; 1220 collected/exit 0/1 skipped/34 HITL pass/mypy clean/≤1200. BUT FR24 append-only BROKEN for identical resolutions: content-addressed filename = hash(payload-only, excludes prev_hash), so two identical EscalationResolutions (the AC4 re-log-same-STOP case) collide on decisions/<hash>.json → 2nd append OVERWRITES prior (AC3 "never overwritten" violated) + self-cyclic prev_hash → _resolve_chain_head returns genesis, orphaning the chain (reproduced: 3 appends→2 files→2-cycle→head resets). [Review][Patch] filed in story ### Review Findings (fold chain-position into hashed payload OR reject dup filename + RED-first test). Status → in-progress for next dev round. # dev-story 2026-07-01 (claude-opus-4-8[1m], implement): NEW governance/ sub-package DELIVERED — PURE pattern-matched escalation.py (escalation_fires + resolve_escalation over frozen findings/verdict-state, NOT LLM-judgment; frozen EscalationRule/EscalationTrigger/HumanDecision/EscalationResolution + EscalationOutcome/ResolutionKind; DEFAULTS TO STOP; timeout PARKS AT STOP never auto-PROCEED — DN-RESOLUTION; content-derived decision-id via 1.1 compute_content_hash, no uuid/clock/float; zero-LLM-token) + IMPURE decision_record.py (DecisionRecordWriter.append → append-only content-addressed decisions/<content_hash>.json, prev-hash-chained to prior decision, producer-scoped chain-head resolution ignoring the 5.3 rejection_ledger + corrupt artifacts; REUSES 1.1 serializer/envelope + 1.3 ApaaStoreWriter/ApaaStoreReader/ApaaStorePaths — NO fork). STOP logged even if full record deferred (FR24, RED-first). Complete-the-declared-set (AI-E5-1) all 4 HITL members RED-first + mechanized resolution matrix (AI-E5-2) + no-crash edges (AI-E4-2: empty/None findings, malformed element skipped, genesis, foreign/corrupt decisions artifact, typed NAMED EscalationError/DecisionRecordError) + non-ASCII round-trip (AI-E1-1) + determinism/tamper-evidence (AC6). Extended no-web-imports gate (+2 governance modules, +2 provider-free legs TC-APAA-HITL-001-20/21) + 4.4 secret-containment suite (+HITL decision-record sweep TC-APAA-SECURITY-001-21/22, RED-then-green). Area APAA-HITL TC-APAA-HITL-001-01..30. Tests 1045 passed (was 1007; +34 HITL +2 security +2 no-web) / mypy clean / structural gates green / files ≤1200 (escalation 396, decision_record 216) / frozen Epic-1..6 surfaces mtime-unchanged / headless / no cli/HTTP/CI-job/LLM. DF-6-7-A (live pipeline/CLI wiring → epic-7-minions-dogfood-proof-run) filed append-only with 6 CC-3 fields. AI-E5-1/E5-2/E5-3/E5-4/E5-7/E4-2/E1-1 discharged. # create-story 2026-07-01: HITL STOP/PROCEED escalation (FR23) + append-only decision record (FR24), Tier-B, Epic-6 LAST story. NEW governance/ sub-package: PURE pattern-matched escalation.py (deterministic rule match over frozen findings/ledger/verdict-state, NOT LLM-judgment; DEFAULTS TO STOP; on gate-timeout PARKS AT STOP, NEVER auto-PROCEED) + append-only decision_record.py that appends each decision to the already-reserved .apaa/decisions/ subdir REUSING the 1.1 canonical serializer + content-hashed prev-hash-chained envelope + 1.3 ApaaStoreWriter.write_payload (no forked persistence; prev_hash chains to prior decision's content_hash; STOP logged even if full record deferred). Zero-LLM-token, secret-free (NFR-S1: only decision/trigger-provenance/finding-id/decider-id token/content-derived id, never source bytes), content-derived ids (AR4/AR11, no uuid/clock/float). REUSE-only over frozen Epic-1..6 (mirrors 6.4 Prosecutor pure-consumer shape); NO detector/Prosecutor edit, NO frozen-contract diff, NO new CI job/HTTP/cli flag (minimal invocation seam only if AC proof requires, else AI-E5-4 defer to Epic 7). Scope-fenced vs Epic 7 (Minions dogfood live-run wiring) + any LLM-driven escalation (V1 pattern-matched only). Area APAA-HITL (TC-APAA-HITL-001-NN). AI-E5-1/E5-2/E5-3/E4-2/E1-1.
  epic-6-retrospective: done  # retrospective complete 2026-07-02 (epic-6-retro-2026-07-02.md): all 7 stories done (1220 passed/1 skipped/4 subtests, mypy clean, ≤1200 lines); THIRD Tier-B epic + explicit cut-order boundary + largest to date (7 stories) delivered the validation-grade TRUST SUBSTRATE as proven properties — LLM behind ONE injectable determinism-quarantine port (6.1, two orthogonal subprocess import gates: pure seam ⊬ providers, adapter is carve-out but ⊬ FastAPI), audited_deep GROUNDED-not-assumed (6.2, FR7 — CLOSES the four-retro-old DF-1-7-B: zero-def module → audited_shallow, verdict migration observable end-to-end, ledger byte-identical, moat preserved for the right reason), false-accusation moat over the unresolved-name substrate (6.3 orphan 3 silence guards + 6.4 Prosecutor downgrade-only/promotion needs advisory∧AST-corrob∧sign-off), precision MECHANIZED + HONESTLY PROVISIONAL (6.5 harness measures over findings not repos + un-tuned holdout caught + OI1 no-≥80%-number; 6.6 exact-Fraction precision but gate UNCONDITIONALLY provisional — 3 distinct classes, no human adjudication = DF-6-6-A), HITL DEFAULTS-TO-STOP + append-only hash-chained decision record (6.7). REVIEW: 6.1-6.6 PASS iter-1 (6.6 clean/0-findings); 6.7 FAIL iter-1 → PASS iter-2 = the FIRST Tier-B hard FAIL and a GENUINE data-integrity catch (append-only content-address collision on identical resolutions → overwrite + self-cyclic prev_hash + genesis-orphan; reviewer INDEPENDENTLY reproduced, existing tests missed it because every fixture varied decider tokens; iter-2 chain_prev_hash fold RED-first + 3 adversarial probes). HEADLINE follow-through (strongest yet, 4 fully completed): AI-E5-1 complete-the-declared-set applied PROACTIVELY to the predicted-risk 6.1 LLM no-crash matrix → predicted FAIL did NOT occur; AI-E5-5 closed DF-1-7-B in 6.2 exactly as sequenced; AI-E5-2 mechanized in 6.5 cartridge harness. Defer-inflow: 5 new (DF-6-3-A/6-4-A/6-4-B/6-6-A/6-7-A — 4×🟢 scoped V2-boundary + 1×🟠 DF-6-6-A precision-flip) / 2 CLOSED (DF-1-7-B + DF-1-4-A consumed — the two oldest dated items retired); trajectory 5→2→2→0→0-net→5-filed/1-closed — the rise is V1-vs-V2 scoped discipline not regrowth (read with severity breakdown). 8 action items recommended as Epic-7 backlog (L1-E11): payload/event-identity checklist leg (AI-E6-1, the 6.7-FAIL class, §9.2 occ-2 promotion), DF-6-6-A as HARD Epic-7 pre-condition (AI-E6-2, non-code: N=5 distinct-class corpus + recorded human adjudication before 7.2 presents externalization evidence), wire DF-6-7-A live HITL in 7.2 (AI-E6-3), proactive pipeline.py split before live-wiring (AI-E6-4, 1090/1200), BUILD-OR-RETIRE the 5-epochs-slipping test-existence guard (AI-E6-5), defer back-fill (AI-E6-6). SIGNIFICANT DISCOVERY: none re-scopes Epic 7 — but DF-6-6-A (precision-gate flip) is the DF-1-7-B analogue for the Epic-7 runway and needs a non-code deliverable before the dogfood can present a CLEARED gate. Epic-7 plan sound as written (OI2 full-repo multi-partition + OI3 $X sized in 7.1 both LOCKED; V1 no-cross-partition-seam-analysis honesty limitation must appear in the 7.1 proof scope statement). 0 session interruptions, 0 HALTs.

  # ── Epic 7: Minions Dogfood Proof Run (capstone) ──
  epic-7: done
  7-1-minions-full-repo-partition-budget-sizing-plan: done  # code-review 2026-07-02 iter-1 PASS: verified INDEPENDENTLY (not trusting Dev record) — full suite re-run 1243 passed/1 skipped/4 subtests; the two new golden rule_ids CONFIRMED-emitted by directly auditing the staged cartridges through the REAL run_audit_detailed (vacuous_heuristic_basic→['vacuous_test_heuristic'], cross_partition_seam→['cross_partition','orphan_code']) — NOT fabricated (the -09 test also asserts row.tp>=1, a permanent FN if synthetic); cross_partition_seam is a genuine 45-file cohesion-chain HOLDOUT the real 2.4 partitioner splits; 5 DISTINCT classes / 7 labeled rows honest; OI1 KEYSTONE HELD — protocol_cleared default False, NO prod =True, precision_gate_status() unflipped, no ≥80% number presented as authoritative; reuse-by-import (partition_repository/build_ast_index/account_spend), no forked partitioner/accountant/registry SHAPE (frozen CartridgeSpec/GoldenFinding byte-unchanged, only rows+2 additive helpers); AR4 int/Fraction only (843/675/675:36712, no float); no secret leak in plans/precision (-15); no cli-flag/HTTP/CI-job/detector edit; files ≤1200; mypy clean on dogfood/*; DF-6-6-A-P1 note append-only with 6 CC-3 fields; sprint-status parses UTF-8. Senior Developer Review (AI) written into story file. 0 findings. Status → done. # dev-story 2026-07-02 (claude-opus-4-8[1m], implement): all 8 ACs delivered. (1) reproducible full-repo Minions partition map — minions_core/apaa/dogfood/partition_plan.py REUSES 2.4 partition_repository + 1.4 build_ast_index/_SOURCE_SUFFIXES (no fork) → 4 bounded units over 135 modules/36.7k LOC (all ≤ hard 60/25k ceiling), 332 cut edges; committed minions-dogfood-partition-plan.md, byte-reproducible (AC1/AC2/OI2). (2) empirically-sized $X=843 credits (V1 deterministic total 675 × 5/4 headroom, int, no float/AR4) REUSING 3.1 account_spend/BudgetConfig; NFR-C1 baseline 675/36712; 3.2 halt demonstrated (fits under $X, breaches below total); committed minions-dogfood-budget-plan.md; OI3 "no numeric default" preserved (budget_governor.py keeps ceiling_credits=None — AC3). (3) DF-6-6-A autonomous half: corpus grown 3→5 DISTINCT classes via 2 new additive registry rows (frozen CartridgeSpec shape) vacuous_heuristic_basic (planted, vacuous_test_heuristic) + cross_partition_seam (HOLDOUT, cross_partition) — both rule_ids CONFIRMED-emitted by REAL detectors (staged + run, NOT assumed; never synthetic); each produces own TP in UNCHANGED 6.6 compute_precision roll-up; tool_failure/traceability_not_establishable honestly ruled out (breadth skips depth-graded files) — AC5. OI1 KEYSTONE HELD: ≥80% gate STAYS PROVISIONAL, protocol_cleared NOT flipped, marker NOT flipped; human adjudication = 7.2 + human step (AC6). Plan STATES V1 no cross-partition seam analysis (6.4 cross_partition = V1 mitigation; V2 full seam) — AC4. DF-6-6-A-P1 progress note filed append-only (6 CC-3 fields, target epic-7-minions-dogfood-precision). Area APAA-DOGFOOD TC-APAA-DOGFOOD-001-01..17. Suite 1243 passed/1 skipped/4 subtests; mypy clean on dogfood/*; no-web-imports + single-serializer + file-size + 4.4 secret-containment green; frozen Epic-1..6 + 6.5 registry SHAPE unchanged (rows+2 helpers appended); files ≤1200; NO cli/HTTP/CI-job/detector/Prosecutor/partitioner/budget-core edit; NO live LLM. # create-story 2026-07-02 (claude-opus-4-8[1m]): Epic-7 capstone FIRST story — PLAN/sizing (not execution; 7.2 executes). THREE coherent "prepare the dogfood" deliverables, single-purpose: (1) reproducible FULL-REPO Minions partition map (~70 modules → multiple bounded ≤40-file/15k-LOC units) REUSING 2.4 partition_repository/PartitionPlan (no fork), each targeted unit clears the 20%-deep floor (AC1/AC2/OI2); (2) empirically-sized budget ceiling $X REUSING 3.1 account_spend/BudgetConfig/CostLedger (int credits, no float/AR4; OI3 "no numeric default" resolved-for-dogfood in the plan artifact, budget_governor.py keeps no hardcoded default; 3.2 halts+downgrades if breached — AC3); (3) DF-6-6-A AUTONOMOUS half (AI-E6-2 HARD Epic-7 pre-condition) — grow synthetic cartridge corpus toward N=5 DISTINCT defect-rule CLASSES (currently 3: vacuous_test_ast/hardcoded_secret/orphan_code) via ≥2 new distinct-class cartridges incl. ≥1 holdout EXTENDING 6.5 CARTRIDGE_REGISTRY (frozen CartridgeSpec shape, no fork; rule_ids CONFIRMED-emitted e.g. cross_partition/vacuous_test_heuristic — never synthetic; AC5). OI1 KEYSTONE (non-negotiable): ≥80%-precision gate STAYS PROVISIONAL — protocol_cleared=False, marker NOT flipped; human TP/FP adjudication over real dogfood findings (DF-6-6-A human half) is 7.2 + a human step (AC6). Plan STATES V1 NO cross-partition seam analysis honestly (6.4 cross_partition pass = V1 mitigation; full seam auditing V2 — AC4). 8 ACs. REUSE-only over frozen Epic-1..6; NO detector/Prosecutor/partitioner/budget-core/registry-SHAPE edit, NO cli/HTTP/CI-job/LLM. Area APAA-DOGFOOD (TC-APAA-DOGFOOD-001-NN). epic-7 flipped in-progress (first story).
  7-2-execute-minions-audit-proof-artifact-evidence-bundle-signature-demo: done  # code-review 2026-07-03 iter-1 PASS (APAA CAPSTONE): verified INDEPENDENTLY (not trusting Dev record) — full suite re-run 1262 passed/1 skipped; live dogfood re-run reproduces every headline figure byte-for-byte (NOT_READY_FOR_RELEASE/exit 2, 135 files/36712 LOC/4 units, deep-% 13/15, 0 blocking, 675 credits within $X=843 + 3.2 halt, grade demo-heuristic-only, integrity consistent, 3 classes cross_partition×332/hardcoded_secret×2289/orphan_code×285); mypy clean on proof_run.py (only pre-existing radon-stub from FROZEN tool_runner.py). KEYSTONE-1 no-source bundle CLEAN — structural moat (EvidenceBundle/Recording carry no source-body/secret-value field; ast_span=FR13 symbol location NOT source bytes); TC-22/23 + 4.4 canary prove absence over the REAL tree; hardcoded_secret findings are advisory locators not retained secret values; BUNDLE_LEAK none. KEYSTONE-2 OI1 HELD — protocol_cleared=True nowhere (grep-guarded), precision_gate_status_for(provisional=True) honest 'NOT a cleared gate', 6.5 marker unflipped, grade+externalization-guard present, no over-claim phrase. Reuse-by-import no fork; frozen seams mtime-unchanged (06-27..30 < proof_run 07-03); no float/json.dumps/new hasher; RED-first non-determinism non-vacuous (2906 findings); files ≤1200; headless; both CC-3 defers 6-field-complete, DF-6-7-A open. 1 Low note (DOGFOOD_APAA_VERSION 1.43.0 vs __init__ 0.1.0 — cosmetic non-gating). Senior Developer Review (AI) written into story file. Status → done. APAA project CAPSTONE COMPLETE. # dev-story 2026-07-02 (claude-opus-4-8[1m], implement): all 8 ACs DELIVERED — the APAA CAPSTONE dogfood EXECUTED. (1) ran the FROZEN pipeline.run_audit_detailed (REUSED, no fork) over the REAL Minions source (135 tracked minions_core/ modules / 36712 LOC / 7.1 4-unit plan, materialized into a clean on-pin snapshot via the 6.5 stage_cartridge pattern — DN-DOGFOOD-REUSE, since load_repo_at_commit refuses the drifted/untracked live tree) under $X=843 → verdict NOT_READY_FOR_RELEASE/exit 2, deep-% 13/15, 0 blocking (all findings advisory/verdict-ineligible = the structural Tier-A heuristic-only signal), 675 credits WITHIN the ceiling + the 3.2 halt-if-breached demo (REUSING 3.1 account_spend) → committed minions-dogfood-proof.md (AC-EXECUTE). (2) SIGNED source-free bundle via the done 4.3 build_evidence_bundle/persist_evidence_bundle + the 4.2 lint (impure call site) + the 1.1 prev-hash-chained content-addressed envelope (the signature; created_at excluded from hash); NO forked bundle/serializer; no-source moat proven over the REAL tree — source-BODY sentinels (import hashlib/raise ValueError/HMAC-SHA256) absent from the bundle + every persisted .apaa/ artifact while non-empty + verdict+findings present (AC-BUNDLE). (3) reproduced the `GitHub green · Sonar green · APAA 🔴` signature demo (vacuous_basic → vacuous_test_ast FIRST, blocking, exit 2) (AC-SIGNATURE). (4) 100% reproducibility — byte-identical verdict + bundle bytes across two runs, RED-first vs injected non-determinism (AC-REPRODUCIBLE). (5) grade: demo-heuristic-only flag + externalization guard on the artifact + the pure DogfoodProofRun wrapper — NO frozen 4.1/4.3 SHAPE mutation (DN-GRADE); RED-first dropped-flag/over-claim (AC-DEMO-GRADE). (6) REAL findings laid ADJUDICATION-READY by the 6.6 finding_match_key shape (3 classes: cross_partition ×332, hardcoded_secret ×2289, orphan_code ×285 — each rule_id + verdict-eligibility + sample locators + empty human TP/FP column), RED-first no-collision (AI-E6-1) (AC-ADJUDICATION-READY). **OI1 KEYSTONE HELD (non-negotiable): the ≥80%-precision gate STAYS PROVISIONAL — proof_run.py NEVER passes protocol_cleared=True (grep-guarded), no cleared-gate field, no ≥80% number presented as authoritative, the 6.5 precision_gate_status() marker NOT flipped, Tier-A run NOT presented as externalization evidence; DF-6-6-A-P2 progress note + the NEW DF-7-2-A human-adjudication defer filed append-only (6 CC-3 fields, target epic-7-minions-dogfood-precision); DF-6-7-A HITL wiring stays OPEN — DN-ESCALATION out of scope (AC-PROVISIONAL).** New minions_core/apaa/dogfood/proof_run.py (750) + tests/apaa/test_dogfood_proof.py (TC-APAA-DOGFOOD-001-18..34) + minions-dogfood-proof.md; extended tests/security/test_apaa_secret_containment.py (TC-APAA-SECURITY-001-23) + the no-web-imports import-isolation coverage (proof_run as IMPURE shell). Suite tests/apaa/+security/+import_paths = 1262 passed/1 skipped (was 1243 at 7.1); mypy clean on proof_run.py (radon-stub note is from the FROZEN tool_runner.py, not this module); single-serializer + no-web + file-size + 4.4 gates green; frozen Epic-1..6 + 7.1 + the 4.3/6.5 SHAPES mtime-unchanged (consumed by import, edited none); files ≤1200; NO detector/Prosecutor/partitioner/budget-core/bundle-core/registry-SHAPE edit, NO cli-flag/HTTP/CI-job/live-LLM. Status → review. # create-story 2026-07-02 (claude-opus-4-8[1m]): CAPSTONE / FINAL story of Epic 7 + the whole APAA project. EXECUTES the 7.1-planned Minions dogfood (7.1 sized the plan; 7.2 RUNS it). SINGLE-PURPOSE "execute the dogfood + produce the signed proof": (1) run the frozen pipeline.run_audit_detailed over the real Minions repo @ the pinned commit under $X=843 (the 7.1 empirical ceiling; 4-unit plan) → coverage ledger + findings + negative-assurance verdict WITHIN the ceiling (+ 3.2 halt-if-breached demo) → committed proof artifact minions-dogfood-proof.md (AC-EXECUTE); (2) export + persist a SIGNED, source-free evidence bundle REUSING the done 4.3 evidence/bundle.py build_evidence_bundle/persist_evidence_bundle + the 4.2 lint + the 1.1 canonical serializer + the content-addressed prev-hash-chained envelope (no fork; no Minions source/secret byte over the REAL repo — 4.3 structural moat + NFR-S1; round-trips) (AC-BUNDLE); (3) reproduce the `GitHub green · Sonar green · APAA 🔴 tests appear vacuous` signature demo as a real repeatable artifact (AC-SIGNATURE); (4) prove 100% reproducibility on the same commit (byte-identical verdict + bundle bytes — NFR-D1, RED-first non-determinism) (AC-REPRODUCIBLE); (5) carry the honest `grade: demo-heuristic-only` red-team flag (run cuts LLM AST-grounding = Tier-A only → NOT externalization evidence; DN-GRADE: record on the proof artifact / a wrapper, a default-preserving additive frozen-model field ONLY if unavoidable) (AC-DEMO-GRADE); (6) lay the REAL dogfood findings ADJUDICATION-READY (per-finding rule_id + locators + advisory-vs-blocking verdict-eligibility, mapped to the 6.6 finding_match_key shape) so the later human Eng-Lead+QA-Lead TP/FP adjudication can clear the ≥80% gate on REAL data (AC-ADJUDICATION-READY). **OI1 HONESTY KEYSTONE (operator epic-boundary decision 2026-07-02, NON-NEGOTIABLE): the ≥80%-precision gate STAYS PROVISIONAL — protocol_cleared NOT flipped, marker NOT flipped, NO fabricated cleared gate, NO externalization over-claim; the human TP/FP adjudication is a HUMAN step OUT of scope here + a follow-up defer — 7.2 ENABLES it, never runs it or flips the gate (AC-PROVISIONAL).** 8 ACs (EXECUTE/BUNDLE/SIGNATURE/REPRODUCIBLE/DEMO-GRADE/ADJUDICATION-READY/PROVISIONAL/+COMPLETE-SET + NO-REGRESSION). REUSE-only over frozen Epic-1..6 + the done 7.1 plan (run_audit_detailed/build_evidence_bundle/persist_evidence_bundle/lint_referential_integrity/compute_precision/dogfood.partition_plan — no fork); NO detector/Prosecutor/partitioner/budget-core/bundle-core/registry-SHAPE edit, NO cli-flag/HTTP/CI-job/live-LLM. DF-6-6-A progress note + a NEW human-adjudication defer (6 CC-3 fields, target epic-7-minions-dogfood-precision) filed before review; DF-6-7-A HITL wiring stays open (DN-ESCALATION — out of scope). Area APAA-DOGFOOD (TC-APAA-DOGFOOD-001-18..NN, continuing from 7.1's ..17). New: dogfood/proof_run.py + tests/apaa/test_dogfood_proof.py + minions-dogfood-proof.md. epic-7 already in-progress (unchanged).
  epic-7-retrospective: done  # retrospective complete 2026-07-04 (epic-7-retro-2026-07-04.md) — FINAL epic + PROJECT-CLOSE reflection. 2/2 stories done (7.1 clean iter-1; 7.2 PASS iter-1, 1 Low non-gating). Defer-inflow 3 rows (2 DF-6-6-A progress notes + 1 net-new DF-7-2-A), all 🟠, fell toward the converged floor. Capstone dogfood EXECUTED honestly: NOT_READY_FOR_RELEASE/exit 2, 0 blocking / 2906 advisory, within $X=843, signed source-free bundle (BUNDLE_LEAK none), signature demo 🔴, 100% byte-reproducible, grade demo-heuristic-only, gate held PROVISIONAL. HANDOFF (demo-grade→externalization): AI-E7-1 human TP/FP precision adjudication (DF-7-2-A — the one gate-clearing step, Eng+QA Lead); AI-E7-2 live HITL wiring + pipeline split (DF-6-7-A, Delivery Orch); AI-E7-3 package V2 resolved-graph cluster; AI-E7-4/5 hygiene + externalization CI backstops. AI-E6-5 test-existence guard RETIRED-WITH-RATIONALE (7-epic clean in-session record). APAA build complete.

  # ── Epic 8: The Honest Verdict — no block without a finding (delta, Argus repo) ──
  # Covers DR-1…DR-11 (DR-7 verify-only) + RS-4a · FR16 + FR4 (amended 2026-08-03)
  # Story ordering constraint: 8.5 (dogfood re-derivation) is LAST by design, so a slip is visible.
  epic-8: done  # all 5 stories done + retrospective complete 2026-08-08 (epic-8-retro-2026-08-08.md); rolled up by the dev-loop orchestrator
  8-1-findings-before-coverage-binding-decision-table: done  # code-review 2026-08-04 iteration 2: PASS. All 5 iteration-1 findings verified resolved on disk (R2 else-branch at prosecutor.py:489-496 proven EXECUTED by line-trace, not just asserted; R3 blast-radius annotated in place; R4 both evidence files staged A, check-ignore exit 1; R5 verdict_changed + one-token-per-move, ProsecutionResult never serialized — sole consumer pipeline.py:734 reads .verdict; R1 correctly left untouched per the AC15 fence, logged DF-8-1-A -> Story 8.3/DR-11). Reviewer re-run: 1075 collected / 1072 passed / 3 failed / 0 skipped / 0 errors (junit), mypy clean (69 files), the only reds are the 3 user-adjudicated carve-outs (2 inherited test_dogfood_plan + 1 deliberate test_dogfood_proof owned by Story 8.5/DR-10). No test deleted, skipped or weakened (test_prosecutor.py 17->21, zero names lost); AC18 fence empty-diff. # create-story 2026-08-04: DR-1/DR-2/DR-3/DR-4/DR-9 — 4-row FR16 table (findings before the GATES, floor still first), DecisionRow disclosure w/ default (A8), VERDICT_SCHEMA_VERSION "1"->"2". Blast radius MEASURED on a scratch tree: 23 delta-caused failures / 9 files (7 verdict_gate incl. golden bytes, 3 verdict_scope, 3 insufficient_coverage_floor, 3 cartridge golden keys, 2 report_honesty, 2 pipeline_signature_demo, 1 each critical_subsystems/depth_semantics/prosecutor); 4 further failures confirmed environment-only. ACs 12-16 ADDED at story design for consumers the reorder breaks: prosecutor _CONSERVATISM_RANK would silence a promoted blocking finding (row-4 candidate outranks its own row-2 refold), exhaustion.below_floor equivalence falsified, negative_assurance row-4 statement false, generator.py drops the critical-blockers block. plain_english.py/cli.py/critical_subsystems.py/__init__.py/dogfood artifacts explicitly OUT of scope (8.2-8.5)
  # dev-story 2026-08-04: DR-1/2/3/4/9 landed — 4-row FR16 fold (floor -> findings -> gates -> otherwise), DecisionRow(4) + AuditVerdict.decision_row (default None, A8) + is_below_floor, VERDICT_SCHEMA_VERSION 1->2 (no migration). 4 consumers fixed (prosecutor rank -> partial order; exhaustion below_floor -> row 1; negative-assurance row-1/row-4 split, row-1 byte-identical; generator branches on the row, NO rendered string changed) + 3 cartridge golden keys re-pointed. 1070 passed / 3 failed: 2 INHERITED test_dogfood_plan + 1 delta-caused test_dogfood_proof DEFERRED to 8.5 by AC18 (Variance V2 in the story). mypy clean.
  # code-review 2026-08-04 (iteration 1): VERDICT=CONCERNS -> in-progress. Reviewer RE-RAN everything: suite 1073/1070 pass/3 fail/0 skip (matches the Dev exactly); mypy clean (69 files); the 2 test_dogfood_plan reds VERIFIED inherited in a detached 9109e16 worktree and test_dogfood_proof VERIFIED green at baseline (so Variance V2 is right and the story's blast-radius table is wrong); collect-only 1051->1073, no file lost a test, zero skips, no weakening; the AC11 fixture REGENERATED at 9109e16 and byte-identical incl. the content-addressed locator (proof is genuine, not circular); FR16 row 4 proven END-TO-END on the orphan_basic cartridge (INSUFFICIENT_COVERAGE/exit 3 with 0 findings + honest negative-assurance string); is_below_floor adds no canonical key; no-migration safe (no schema-version gate in the store reader; one AuditVerdict construction site); _CONSERVATISM_RANK partial order proven cannot-silence and never-upgrade. 4 open items in the story's Review Findings: R1 DEFER (row-4 final-verdict.md still prints "Repository is NOT ready for release" beside INSUFFICIENT_COVERAGE - correct per AC15's fence, owned by 8.3, logged as DF-8-1-A), R2/R3/R4/R5 low patches (AC12 no-upgrade asserted over the private rank map instead of through prosecute(); stale blast-radius classification; the AC11 test+fixture are untracked by git; a row-4->row-2 move leaves no structured signal on ProsecutionResult).
  # dev-story 2026-08-04 (fix iteration 2): 4 of 5 review findings RESOLVED -> review. R2 the FR19 no-upgrade guarantee is now BEHAVIOURAL through prosecute() (the reviewer was right that the "impossible" case IS constructible via the independent scope_paths arg; the enforcing else-branch at prosecutor.py:474-483 is now actually executed, the false docstring corrected, the rank-map white-box pin KEPT as -32, and the rejection path's refined-finding carry-over newly pinned). R3 the blast-radius section annotated in place (test_dogfood_proof.py struck from the environment-only list, cross-ref V2 + 8.5/DR-10). R4 test_verdict_schema_bump.py + tests/fixtures/verdict_schema_v1_row2_artifacts.json git-added (A); no .gitignore rule matches. R5 ProsecutionResult.verdict_changed + a mirroring reclassified:<from>-><to> rationale token (one token per move); ProsecutionResult is never serialized, so no canonical payload / content hash / cache key moves. R1 NOT fixed by design (AC15's no-rendered-string fence; DF-8-1-A, owned by 8.3/DR-11). Suite 1075 collected / 1072 passed / 3 failed / 0 skipped - the SAME 3 adjudicated reds, zero new reds, no test deleted/skipped/weakened; mypy clean; AC18 fence still empty-diff.
  8-2-critical-subsystem-gates-operator-can-actually-satisfy: done
  # code-review 2026-08-05 (iteration 2): VERDICT=PASS -> done. All 3 open findings CLOSED, each falsified by the reviewer against the working tree, nothing taken on the Dev record's word. HIGH (D3/AC3 false green) CLOSED: reviewer re-ran the seam end-to-end through _detect_per_file - parse-failed app/auth_test.py and app/broken_auth.py now BOTH report ineligibility=None / depth=SKIPPED and BOTH stay in critical.paths with neither disclosed as 'test_file', while parse-failed tests/test_broken.py is still TEST_FILE so DR-5's deleted unsatisfiable gate stays deleted. The is_test_file RESTRUCTURE was PROVED behaviour-preserving, not accepted: the HEAD (9109e16) function body was rebuilt verbatim from git show and differentially compared against the new one over 220 paths x 12 ast_entry variants = 2567 comparisons -> 0 mismatches (corpus = all 147 real indexed paths + 73 adversarial synthetics incl. empty/'/'/backslash, no-extension, dotfiles, conftest.py, nested+cased tests/Tests/TESTS/__tests__/spec/specs, test_*/*_test.py, every unambiguous suffix and its near-miss, spaces, non-ASCII/emoji; variants = None, non-entry object, clean-zero-def, clean-testfunc, clean-TestCase, clean-prod-defs, parse_failed, ast_ineligible, both-bad, parse-failed-with-defs, the file's own real entry, no-kwarg call). is_test_classification_content_dependent verified BEHAVIOURALLY to be exactly 'tier 3 decided' (0 inconsistencies: where True the answer varies with the entry, where False it is invariant across all 12 variants). New-seam probe found no remaining path where a shallow-BY-CIRCUMSTANCE file exits the set, and the opposite error is bounded - an unreadable genuine *_test.py outside a test dir is now RETAINED, which is over-inclusion (AC7's sanctioned stricter direction) and is TRANSIENT, since repairing the parse restores the TEST_FILE exclusion. MED1 CLOSED and the discrimination claim INDEPENDENTLY VERIFIED by injecting the historical implementations at runtime (pytest plugin rebinding argus.pipeline._critical_ineligibility; no source edited): against the PRE-FIX body exactly -002-01[ambiguous-test-suffix] + -002-09 fail while -002-01[production-name] stays GREEN; against the FORBIDDEN naive hoist only -002-09 fails, on its tests/test_broken.py row. So -002-09 discriminates the correct fix from the forbidden one in BOTH directions - a real regression test, not a rubber stamp. -158's docstring now states honestly that it does NOT pin D3, why a fold test cannot, and where D3 lives. MED2 CLOSED: git diff --cached shows A for both new test modules, git check-ignore exits 1, worktree clean. LOW/NFR-M1 verified at exactly 1199 lines (no argus/ file over 1200; next largest proof_run.py 749) and the mass moved into vacuous_test.py (488->533, 533/1200) judged LEGITIMATE placement, not cap-gaming - the tier table and the which-tier question belong in the module that owns the classification (AR7 no-fork was the reviewer's own iteration-1 prescription); DF-8-2-A stands for 8.3, not re-litigated. REGRESSION SWEEP: critical_subsystems.py untouched this iteration (mtime 08-04 21:48 vs first fix write 08-05 07:38), _matches_exclusion AST-extracted from 9109e16 and worktree = 1424 chars, IDENTICAL SHA-256, fnmatchcase present / bare fnmatch absent; D1 purity re-verified by AST (7 module imports, ZERO function-local imports); figures re-derived independently = 147 indexed / 116 heuristic CRITICAL / 56 test_file + 10 zero_def / 50 eligible survivors / 0 unreachable / 0 parse-failed criticals, both AC-asserted figures (0 and 50) unmoved; the High was proved LATENT by running pre-fix vs post-fix _critical_ineligibility over all 147 entries -> 0 files change answer, so no verdict/artifact/byte-delta evidence could have moved; all 66 disclosures re-audited for over-breadth -> 0 violations; repo verdict still the EARNED RELEASE_READY/exit 0; AC12 fixture NOT regenerated (staged A, clean worktree diff, mtime 08-04 14:20); AC16 fence intact by mtime, dogfood-proof.md and both final-verdict.md empty-diff. Reviewer's OWN suite re-run: pytest tests/ -p no:randomly -> 3 failed, 1108 passed in 141.93s; --collect-only -> 1111 collected; 0 skipped / 0 xfail / 0 deselected; only reds are the 3 adjudicated carve-outs, left red and unmodified; mypy clean (69 files); nothing deleted/skipped/weakened. ONE new LOW DEFER logged (DF-8-2-B, non-blocking, PRE-EXISTING): _UNAMBIGUOUS_TEST_SUFFIXES lists "test.java"/"spec.rb" without a word separator, so svc/latest.java and svc/myspec.rb classify as tier-2 test files by name alone - byte-identical at 9109e16, unchanged by this story, not a false green because the grading stage misclassifies them identically, and zero instances in this repo.
  # dev-story 2026-08-05 iteration 2 (mode=fix): ALL 3 open review findings resolved. HIGH (D3/AC3 false green) CLOSED without the fix the reviewer forbade: vacuous_test.py now declares its 3 tiers ONCE (_is_unambiguous_test_path + _lower_basename, is_test_file re-expressed in terms of them - no fork, AR7) and exports is_test_classification_content_dependent; pipeline._critical_ineligibility withholds TEST_FILE only when the entry is UNREADABLE AND the label came from the AMBIGUOUS tier. Proved end-to-end on BOTH reviewer cases: parse-failed app/auth_test.py and app/broken_auth.py now land on the SAME eligible side (neither removed, neither falsely disclosed as 'test_file'), while parse-failed tests/test_broken.py stays TEST_FILE so the DR-5 gate stays satisfiable. MED1 CLOSED: -158 rescoped to the fold contract it actually pins; D3 moved to the seam - -002-01 parametrized over both filename shapes + new -002-09; both verified RED against pre-fix code AND -002-09 verified RED against the forbidden naive hoist (it discriminates the correct fix, not the implementation); new DETECT-001-95 pins the tier predicate. MED2 CLOSED: both new test modules git add-ed (now 'A', not committed). LOW/DF-8-2-A respected: pipeline.py ends at 1199 lines - exactly the reviewer's count, not blown; the substance lives in vacuous_test.py (488->533). critical_subsystems.py NOT touched, so D1 purity + byte-identical _matches_exclusion are untouched by construction; re-measured 147 indexed / 116 crit / 56 test_file + 10 zero_def / 50 eligible survivors / 0 unreachable / 0 parse-failed criticals (115->116 drift is this iteration's own edit to tests/test_vacuous_detector.py flipping it NORMAL->CRITICAL; both AC-asserted figures 0 and 50 unmoved; the High was latent so no verdict/artifact/byte-delta evidence moved). Suite 1111 collected / 1108 passed / 3 failed / 0 skipped (+3 tests) - only the 3 adjudicated carve-out reds, left red and untouched; mypy clean (69 files).
  # dev-story 2026-08-04 (implement): DR-5 + DR-6 + DR-7 landed in TWO source files, RED-first. critical_subsystems.py gained the closed CriticalIneligibility vocabulary (test_file, zero_definition_module), CriticalCandidate.ineligibility (default None = ELIGIBLE), CriticalSubsystemSet.heuristic_excluded_ineligible (default_factory dict), the eligibility partition applied to the HEURISTIC term ONLY, and CRITICAL_SUBSYSTEMS_SCHEMA_VERSION "1"->"2"; merge formula, _matches_exclusion (BYTE-IDENTICAL - DR-7 verify-only as required), exclude-wins tie and the conservative unmatched policy all verbatim; module gained NO import (D1 honoured - it stays PURE and in _MODULES_UNDER_GUARD, the -146 AST purity scan and test_no_web_imports green UNMODIFIED). pipeline.py gained _critical_ineligibility reusing is_deep_claim_grounded by import, with is_test_file hoisted to ONE per-file local driving BOTH eligibility and grading (AC7 - no second predicate anywhere); the clean-parse guard keeps parse-failed/AST-ineligible files ELIGIBLE (D3). Precedence (D2) pinned by an 8-row truth table. AC10/F1 counterweight proves the loosening against the REAL pre-filter fold (same candidates re-folded with the fact stripped): leg (a) under-assessed repo still withheld (row 4, above the floor), leg (b) the identical repo now reaches RELEASE_READY/exit 0/row 3, and --critical-subsystem restores the block. AC12 EXTENDED not relaxed: -003-01 names the critical-subsystem envelope as the second permitted mover (looked up by producer) with every other artifact byte-identical + unchanged count, and the new -003-04 applies -003-02's revert-proof shape (drop the key, revert the stamp, canonical bytes equal the pre-amendment payload); fixture NOT regenerated. AC14 holdout_vacuous: critical set emptied + disclosed, all_deep True, verdict UNMOVED at NOT_READY_FOR_RELEASE/exit 2/row_2_blocking_findings, _registry.py untouched. AC8/B3: an emptied set is distinguishable on disk, and the persisted assurance_statement, all three scope_statement critical tuples, the final-verdict report and the ship-readiness summary make NO positive "all criticals examined" claim (confirms D5 - the report surfaces OMIT, they do not assert a falsehood, so the prose is genuinely 8.3's). Re-measured IN PLACE on the real tree: 147 indexed / 115 heuristic CRITICAL / 55 test files + 10 zero-def = 65 unreachable -> 0, and 50 deep-gradable survivors (the SM's 50 confirmed EXACTLY; the +2 indexed / +3 critical deltas are this story's own new+edited test files, reconciled file-by-file in the Dev Agent Record incl. test_verdict_schema_bump.py flipping NORMAL->CRITICAL, verified against its pre-edit git blob). argus audit . RECORDED not pinned (boundary B1): RELEASE_READY / exit 0 / row_3_gates_met / assessed deep 57/73 / 0 blocking / 50 critical paths / 65 disclosed / schema_version "2"; nothing was adjusted to obtain it and no proof artifact was touched. Suite 1108 collected / 1105 passed / 3 failed / 0 skipped - EXACTLY the 3 user-adjudicated carve-out reds, left red and untouched (test_dogfood_proof's message moved INSUFFICIENT_COVERAGE -> RELEASE_READY as the story predicted; 8.5/DR-10 still owns it). mypy clean (69 files). No test deleted/skipped/weakened/re-pointed to assert less. NFR-M1 WATCH: pipeline.py finished at 1199 lines - ONE line of headroom, so the next story touching it should extract a shell-helper module rather than add. AC16 fence verified per path (the generator/verdict_gate/prosecutor/_registry diffs against HEAD are 8.1's pre-existing uncommitted delta, unchanged by this session). DF-8-1-A left to 8.3. One recorded fence tradeoff: AC10(b)/Task 6 required the accepted residual exposure in Dev Notes, which is outside dev-story's permitted sections - the AC won, and exactly one clearly-labelled dev-attributed block was ADDED with nothing existing altered.
  # create-story 2026-08-04: DR-5 (heuristic critical-set eligibility filter) + DR-6 (operator --critical-subsystem exempt) + DR-7 (verify-only regression pin on the fnmatchcase exact/prefix/glob --exclude-critical) + boundaries B3 (a vacuously satisfied gate must be visible) and B5 (one test-pinned precedence order) + inversion F1 (the false-green counterweight). Blast radius MEASURED IN PLACE on the real working tree (HEAD 9109e16 + 8.1's UNCOMMITTED delta, .git and _bmad-output/ present) via the shipped argus functions by import - deliberately NOT on a scratch copy, the method that caused 8.1's misclassification of test_dogfood_proof.py: 145 indexed files, 112 heuristic CRITICAL hits, of which 52 test files + 10 clean-parse zero-definition __init__.py = 62 permanently-unreachable blockers -> 0 after the filter, 50 deep-gradable survivors (all already audited_deep), application deep ratio 57/73. Epic arithmetic corrected by measurement (epic said "51 test files + 10" for a total of 62, which did not sum). All 10 self-audit cartridges run live through run_audit_detailed: exactly ONE (holdout_vacuous) has its critical set emptied and its verdict must NOT move (FR16 row-2 precedence - it carries a real blocking finding); CARTRIDGE_REGISTRY needs no edit and must not grow (it is the 6.6 precision-replay ground truth). Baseline suite re-measured: 1075 collected / 3 failed = the adjudicated carve-out only (2 inherited test_dogfood_plan + 1 deferred test_dogfood_proof, Story 8.5/DR-10) - all three left red and untouched. 7 LOCKED decisions recorded: D1 eligibility computed in the impure shell and carried as DATA on CriticalCandidate (critical_subsystems.py is PURE, in _MODULES_UNDER_GUARD, and its docstring LOCKS its import set - no detectors/audit import; follows the decided precedent at pipeline.py:630-632), D2 precedence = eligibility -> designation -> exclusion, D3 parse-failed/AST-ineligible files stay ELIGIBLE (FR4's literal principle would drop them, but that is a false-green: shallow-by-construction vs skipped-by-circumstance), D4 disclosure as a path->closed-reason map on CriticalSubsystemSet + CRITICAL_SUBSYSTEMS_SCHEMA_VERSION "1"->"2" (the model has no omit-when-empty machinery so bytes move regardless), D5 the 8.3 boundary (no plain_english.py / generator.py edit - verified nothing ASSERTS a falsehood, only omits), D6 no new CLI flag, D7 the corrected epic arithmetic. DF-8-1-A explicitly NOT pulled in - the ledger entry carries target_story 8-3 and the epic's own 8.3 AC block owns generator.py.
  # NOTE for the dev: the working tree carries Story 8.1's delta UNCOMMITTED (7 modified + 2 staged-new files). Do NOT stash / checkout -- / reset; it is not recoverable from a commit.
  8-3-plain-english-report-stops-describing-impossible-state: done
  # code-review 2026-08-06 (iteration 2): VERDICT=PASS -> done. Both iteration-1 findings genuinely closed; no open findings; nothing regressed. Reviewer re-derived everything and FALSIFIED the load-bearing claims rather than reading them. R1 (Med) CLOSED and the RED claim is REAL: the round-1 implementation was reconstructed textually (_render_critical_blockers called only from the row-4 arm, causal lead hard-wired, no hoist), compiled and bound over generator.render_final_verdict_report — against it the extended -002-21 goes RED ("row_1_below_floor: `Next:` points at a section this run's document does not contain -> '### Critical subsystems below `audited_deep`'") and -002-24 goes RED on the work-list assertion; both green against current code. TWO-SURFACE AGREEMENT VERIFIED FOR EVERY REACHABLE ROW/STATE: every named quantity on render_ship_readiness has a document counterpart (blocking count -> **Blocking Findings**; deep ratio -> **Deep Coverage Ratio**, scoped and unscoped; "Critical files not examined deeply: N" -> "### Critical subsystems below `audited_deep` (N)", now on EVERY row that carries one), and the only unregistered `Next:` line points at a FLAG (--coverage-scope), not a document section. The single asymmetry the reviewer could construct (all_deep False with an EMPTY not_deep tuple) has NO PRODUCER — pipeline.py:730-732 derives all_deep = not not_deep from one call and verdict_gate.py:663 forces not_deep=() when all_deep is True — so it is reachable only by the same contract violation ShipReadinessError already covers. Row 4's causal lead "These withheld `RELEASE_READY` (FR16)." is BYTE-IDENTICAL to 9109e16's, and -002-21 pins it to row 4 and only row 4; the non-causal lead is TRUE for rows 1 and 2 (both fold before the critical clause is evaluated) and asserts no causation. AC6's cleanup INTACT — the row-2 arm appends the CAUTION finding sentence and nothing else, no dilution hint call, and -002-24 pins all three negatives PLUS the new positives. No new dead code: both new branches exercised (`if critical_blockers:` either way; `lines[-1] != ""` True on row 2, False on rows 1/4), and the row is still decided in exactly one place. THE DEV'S SECOND (ROW-1) INSTANCE IS REAL — verified independently: against the reconstructed round-1 code a below-floor fold emits the counter and the pointer while the document contains ZERO occurrences of "critical"; the widened scope rested on a true premise. ONE RECORD CORRECTION (not an action item, no code impact): the Dev Record dates it to Story 8.1, but it predates 8.1 — at 9109e16 the arm was `elif verdict.verdict.value == "INSUFFICIENT_COVERAGE"` and equally omitted the section, and HEAD's plain_english.py:178-180 already emitted the pointer; 8.1 only changed the predicate. Corrected in the story's Review Findings, original text left standing (§3.4). R2 (Low) CLOSED: every TC id extracted from every test FUNCTION NAME in tests/ — zero duplicates suite-wide (plain_english -002-10..-17, honesty -002-18/-19/-24, surface-consistency -002-20..-23); ledger byte-unchanged, as claimed. REGRESSION SWEEP CLEAN: DF-8-1-A stays closed BOTH halves (pre-8.3 code rebuilt and injected — the 2/5 cause renders the false "deep coverage `2/5` is below the `3/5` release threshold" block and the 5/5 cause, deep_ratio==1, renders "at least one critical subsystem is not audited deep (FR16)", both under the row-1 "too little of the code was examined deeply" headline; -002-20 RED against it); the NOT VOUCHED deletion stays safe (still ONE AuditVerdict construction site at verdict_gate.py:651, prosecutor.py:494 model_copy updates ordered_findings only, no model_validate, no read-back); rows 1/2/3 headlines byte-identical vs HEAD:plain_english.py:95-123 (row 1's BODY legitimately gained the work list, its HEADLINE did not move); AC14 fences held — pipeline.py 1199 lines md5 399d6da1d36d668352fd7b0d539cc307 (matches the orchestrator's independent measurement), cli.py and __init__.py empty-diff, and test_dogfood_proof.py / minions-dogfood-proof.md / both final-verdict.md artifacts absent from git diff entirely, so the DR-10 artifact was NOT re-derived and the rot check NOT weakened. CONCURRENT-SESSION ACCOUNTING IS HONEST: tests/test_no_web_imports.py holds 12 test defs at HEAD and 13 in the worktree (the +1 collected / +1 passed, foreign Epic 9), while the two files this round touched hold 12 and 4 test defs — unchanged from round 1 — so this round added ZERO test functions and absorbed nothing. REVIEWER'S OWN RUNS: pytest tests/ -q --tb=no -> 1127 collected / 1124 passed / 3 failed / 0 skipped (collection re-counted independently at 1127; progress map carries no `s`), the 3 reds EXACTLY the adjudicated carve-out; mypy clean (69 files); generator.py 613 / plain_english.py 258 / pipeline.py 1199 lines, all under NFR-M1; live `argus audit .` -> verdict=RELEASE_READY deep_ratio=57/148 blocking_findings=0 assessed_deep_ratio=57/73 scope=application held_out=75, exit 0 — identical to the Dev record and to iteration 1, nothing adjusted (B1). DF-8-3-B / DF-8-3-C remain open and deferred; DF-8-2-A / DF-8-2-B not re-litigated.
  # dev-story 2026-08-06 iteration 2 (mode=fix): BOTH open findings resolved (R3/R4 were DEFERRED by the reviewer as DF-8-3-B/DF-8-3-C and were not touched). R1 (Med) CLOSED via ROUTE (a), recorded: _render_critical_blockers is HOISTED out of the four-row chain and rendered for EVERY row with a non-empty critical set, with the only row-dependent sentence — its LEAD — supplied by the arm, so the row is still decided in exactly one place (no second row derivation inside the helper, §3.3/AR7). Row 4 keeps the byte-identical causal lead "These withheld `RELEASE_READY` (FR16)."; rows 1 and 2 get _CRITICAL_LEAD_NOT_THE_CAUSE ("Not the reason for this verdict — that is stated in the callout above. Listed because the clause is still unmet and will withhold `RELEASE_READY` once the stated reason is resolved."), which is what keeps AC6 intact: a work LIST is not a reason CLAUSE and the lead says so, so the row-2 document still carries NO coverage clause, NO "critical subsystem is not audited deep" clause and NO dilution NOTE. Route (b) (suppressing plain_english's Next: pointer) was weighed and REJECTED — the pointer fires on rows 1, 2 AND 4, so (b) would delete the only actionable next step from the honest surface on two rows while leaving the "Critical files not examined deeply: N" counter naming a quantity with nothing behind it: a quieter contradiction, not a resolved one. MEASURED WHILE FIXING, WIDER THAN REPORTED: ROW 1 has the identical dangling pointer and has had it since Story 8.1 split the below-floor arm out of the `else` — fixed too, because DR-11/B4 require agreement on EVERY row. REPRODUCED FIRST (row-2 fold 3/5 deep, 1 AST finding, 1 unmet critical -> ZERO occurrences of "critical" in the whole document while the ship-readiness block printed the count and the pointer), then captured RED through the extended -002-21, verbatim in the Dev Agent Record. The requested generalisation landed: -002-21 now puts an unmet critical clause on every row that can carry one (row 3 cannot — RELEASE_READY requires critical_subsystems_all_deep) and asserts via _REPORT_POINTERS / _assert_every_pointer_resolves that EVERY `Next:` line referencing the final-verdict report resolves inside the same run's document, FAILING an unregistered pointer so a future one cannot slip through; it also pins the causal lead to row 4 and only row 4. -002-24 additionally pins that the row-2 work list IS present with the non-causal lead. Row-4 and row-1 documents are BYTE-IDENTICAL to before the fix (the separating blank line is added only when the previous line is not already blank). Incidental and recorded: the AC7 guidance was re-flowed from eight hard-wrapped list items into ONE Markdown paragraph constant (_CRITICAL_LIST_GUIDANCE), words unchanged, because every phrase -002-19 asserts straddled a newline; PIPELINE-002-07 (the _FALSE_POSITIVE_CLAIMS keystone) green. R2 (Low) CLOSED: test_report_honesty.py's TC-ArgusAgent-REPORT-002-17 renumbered to -002-24 (test_plain_english.py keeps -002-17), restoring TC-<AREA>-<SEQ>-<SUBSEQ> uniqueness and disambiguating the AC map (AC4 -> -002-17, AC6 -> -002-24); deferred-work.md names -002-20 for the DF-8-1-A closure so no ledger edit was needed and the ledger is UNTOUCHED this round. argus/reports/plain_english.py UNCHANGED this round — the fix is entirely generator + tests. WHOLE SYSTEM: pytest tests/ --tb=no -> 3 failed, 1124 passed, 1127 collected, 0 skipped; the 3 reds are EXACTLY the adjudicated carve-out, left red and unmodified; mypy clean (69 files); generator.py 613 / plain_english.py 258 / pipeline.py 1199 lines. Live `argus audit .` -> verdict=RELEASE_READY deep_ratio=57/148 blocking_findings=0 assessed_deep_ratio=57/73 scope=application held_out=75, exit 0 — identical to the reviewer's run, nothing adjusted (B1). AC14 fences re-verified: pipeline.py md5 399d6da1d36d668352fd7b0d539cc307 (1199 lines, unchanged), vacuous_test.py md5 8a0705030391df92ad1404af9d044758, cli.py and __init__.py empty-diff vs 9109e16, both final-verdict.md and minions-dogfood-proof.md untouched. WARNING FOR THE REVIEWER: the counts moved from 1126/1123 to 1127/1124 and NOT because of this round — a CONCURRENT session edited this working tree while the fix ran (argus/audit/minions_llm_adapter.py and tests/test_no_web_imports.py became modified and a Story 9.1 file appeared, none dirty at the start of this round); tests/test_no_web_imports.py went 12 -> 13 test functions at HEAD-vs-worktree, which is the +1 collected and +1 passed. This round added ZERO test functions (the two files it touched hold 4 and 12 test defs both before and after; -002-17 -> -002-24 is a rename and -002-21 was extended in place).
  # code-review 2026-08-06 (iteration 1): VERDICT=FAIL -> in-progress. ONE Medium regression; everything else verified clean. Reviewer re-ran everything and falsified the load-bearing claims rather than reading them. DF-8-1-A CLOSURE CONFIRMED, both halves: the pre-8.3 implementation of BOTH surfaces was rebuilt and INJECTED AT RUNTIME (plain_english._headline rebound + render_final_verdict_report re-compiled with the historic three-arm block), and against it -002-20 fails for BOTH row-4 causes — at 2/5 deep ("[!CAUTION] Repository is NOT ready for release — deep coverage `2/5` is below the `3/5` release threshold.") and at 5/5 deep / deep_ratio==1 ("… at least one critical subsystem is not audited deep (FR16)."), with the headline rendering row 1's "too little of the code was examined deeply" in both. Real regression test, not a rubber stamp. NOT-VOUCHED DELETION VERIFIED SAFE, re-derived STRUCTURALLY and independently of the SM's 270-fold sweep: AuditVerdict has exactly ONE construction site in argus/ (verdict_gate.py:651), where NOT_READY_FOR_RELEASE is set only in the `elif blocking >= 1` arm and blocking_finding_count=blocking; there is NO AuditVerdict.model_validate and no rehydration of a persisted verdict anywhere (store/reader.py rehydrates only CoverageLedger/Recording; the resume path re-folds), so on-disk v1 state cannot reach the renderer; the prosecutor's second path (prosecutor.py:494 model_copy) updates ONLY ordered_findings and so preserves NOT_READY => blocking>=1; exhaustion/escalation only read. No reachable path raises ShipReadinessError where the old code rendered prose. Rows 1/2/3 headlines proven BYTE-IDENTICAL by differential render (historic vs current compare equal); only row 4 changed and its new prose is true of BOTH causes. Row 2 clean: a diluted 20/63 row-2 render names only "1 verdict-blocking finding(s)." with no coverage clause, no critical clause and no dilution NOTE; zero _FALSE_POSITIVE_CLAIMS substrings in any new prose. No new dead code (all four generator arms, all five _headline paths and every surviving dilution-hint guard verified reachable). AC14 fence held: pipeline.py 1199 lines md5 399d6da1d36d668352fd7b0d539cc307, cli.py + __init__.py empty-diff vs 9109e16, vacuous_test.py md5 8a0705030391df92ad1404af9d044758 with DF-8-2-B's unseparated "test.java"/"spec.rb" STILL PRESENT (so DF-8-2-B genuinely not pulled in), minions-dogfood-proof.md and both final-verdict.md empty-diff — the DR-10 artifact was NOT re-derived and test_dogfood_proof.py is unmodified, its red still asserting the full '`RELEASE_READY` (exit `0`)' string (check not weakened). D5's exclusion respected, DF-8-3-A filed with all six CC-3 fields, DF-8-2-A not re-litigated. tests/test_report_surface_consistency.py confirmed staged (A) and not ignored. Every re-point in test_plain_english.py / test_report_honesty.py / test_cli.py read line by line — each keeps its subject and asserts strictly MORE ("coverage gate" -> "coverage or critical-subsystem gate was not met"; "would still block" -> "would still withhold `RELEASE_READY`" PLUS a new negative pin; the row-1 literal newly pinned byte-exact); nothing asserts less. Reviewer's OWN runs: pytest tests/ -q -> 1126 collected / 1123 passed / 3 failed / 0 skipped (progress map carries no `s`), the 3 reds EXACTLY the adjudicated carve-out; mypy clean (69 files); no argus/ file over 1200 lines; live `argus audit .` -> verdict=RELEASE_READY deep_ratio=57/148 blocking_findings=0 assessed_deep_ratio=57/73 scope=application held_out=75, exit 0 — identical to the Dev record. THE ONE MEDIUM (R1): the four-arm split silently dropped _render_critical_blockers from FR16 ROW 2 (pre-8.3 it lived in the `else` arm covering rows 2 AND 4). Reproduced both ways on a row-2 fold with two unmet criticals — the historic renderer emits "### Critical subsystems below `audited_deep` (2)" and names both files, the current one emits NO occurrence of the word "critical" anywhere in the document — while render_ship_readiness for that same run still prints "Critical files not examined deeply: 2" and "Next: see the final-verdict report for the named critical files and their actual depth". One run's two human surfaces disagree, which is the exact DR-11 defect class and the same one Story 8.1 already had to fix once ("generator.py drops the critical-blockers block"). No AC asked for the removal (AC6 forbids the row-2 REASON CLAUSES and the dilution NOTE, not the work list) and the story's own branch-by-branch audit does not mention it; AC11's net (-002-21) misses it because it only checks block-assertion consistency, never that a `Next:` pointer resolves. Two LOWs also open: R2 duplicate test id TC-ArgusAgent-REPORT-002-17 (used in BOTH test_plain_english.py and test_report_honesty.py — breaks the TC-<AREA>-<SEQ>-<SUBSEQ> uniqueness convention and the story's own "do not restart a sequence", and makes the AC map ambiguous: AC4 -> -002-17 AND AC6 -> -002-17); R3/R4 filed as DEFERS because both touch AC14-fenced files — DF-8-3-B (render_ship_readiness's new raise site sits OUTSIDE cli.py's except ValueError, so with no --report-dir a ShipReadinessError would escape as an uncaught traceback instead of the AR10 exit 1; CLI-001-32 monkeypatches run_audit so it proves the AC's letter without exercising the real site; unreachable today, so defence-in-depth only) and DF-8-3-C (the ast-index -> application/test partition plumbing is now written twice, pipeline.py:686-694 vs generator.py:86-93 — the PREDICATE is correctly reused, the derivation around it is duplicated).
  # dev-story 2026-08-05 (implement): DR-11 landed on BOTH report surfaces in TWO source files, RED-first, with every SM figure RE-DERIVED IN PLACE before any code changed (270-fold sweep identical, 0 occurrences of NOT_READY ∧ blocking==0; the 4×2 rendered-row table, the row-2 contamination and the 1-of-147 classification disagreement all reproduced verbatim). plain_english.py: _headline is now one branch per FR16 row, splitting INSUFFICIENT_COVERAGE on is_below_floor (D1); the unreachable NOT VOUCHED predicate is deleted and its prose RELOCATED to row 4 (D2) with the widening the 5/5-deep case forces; the impossible NOT_READY ∧ blocking==0 input is the new typed ShipReadinessError(ValueError), never a silent default; rows 1/2/3 byte-identical. generator.py: four arms that ARE the four FR16 rows — row 2 names ONLY the finding and emits no dilution NOTE (D3), and row 4's "[!CAUTION] Repository is NOT ready for release …" is replaced by "[!WARNING] Release readiness is NOT VOUCHED — Argus found nothing blocking, but …", matching negative_assurance's row-4 register (D4). AC8: the dilution hint now classifies with is_test_file(…, ast_entry=…) off the ast_index it already received — no pipeline.py edit, no second predicate. AC7 guidance corrected incl. the DR-6 exemption. FOUR branches this delta made unreachable were REMOVED (not traded for new dead code) and every retained guard is proven reachable by a named test; the audit is recorded branch-by-branch in the Dev Agent Record. Tests: TC-ArgusAgent-REPORT-002-10…-23 (new module tests/test_report_surface_consistency.py, git add-ed, carries the four-row cross-surface net + the generator determinism/secret pin — a measured gap) and TC-ArgusAgent-CLI-001-31/-32; -002-05 re-pointed, -002-03 strengthened, the zero-finding keystone re-pointed to a real row-4 fold with its subject intact. WHOLE SYSTEM: 1126 collected / 1123 passed / 3 failed / 0 skipped — the 3 reds are EXACTLY the adjudicated carve-out (2 inherited test_dogfood_plan + 1 deliberate test_dogfood_proof owned by Story 8.5), nothing deleted/skipped/weakened; mypy clean (69 files); no file over 1200 lines. Live `argus audit .` recorded verbatim and UNADJUSTED (B1): RELEASE_READY / exit 0 / row_3_gates_met / assessed 57/73 / 0 blocking.
  # DEFERRED-ITEM DISPOSITIONS: DF-8-1-A is CLOSED here (both halves — generator.py's row-4 callout and plain_english.py's row-4 headline), closing evidence TC-ArgusAgent-REPORT-002-20, demonstrated RED against the pre-fix code for BOTH row-4 causes, plus the -002-21 four-row net. DF-8-2-A CARRIED FORWARD, NOT closed — argus/pipeline.py was NOT edited at all (AC14 fence held; still 1199 lines, byte-identical, mtime predating this session), so the extraction trigger never fired; append-only note added. DF-8-2-B CARRIED FORWARD untouched — its target is conditional on editing argus/detectors/vacuous_test.py, which this story READ (reusing is_test_file rather than forking) but did not modify. NEW: DF-8-3-A filed with the six CC-3 fields for D5's declined vacuity prose, targeted at the story that performs the DF-8-2-A extraction.
  # create-story 2026-08-05: DR-11— the report-surface reconciliation, BOTH surfaces. Blast radius MEASURED IN PLACE on the real tree (HEAD 9109e16 + 8.1's and 8.2's uncommitted deltas, .git and _bmad-output/ present) by importing the shipped argus functions — NOT on a scratch copy, the method that cost 8.1 a review round. Measured: all 4 FR16 rows rendered through plain_english.render_ship_readiness AND generator.render_final_verdict_report. Row 4 renders the ROW-1 sentence "too little of the code was examined deeply" on both surfaces — reproduced at 2/5 deep AND at 5/5 deep (100% of files audited_deep, and the report still says too little was examined). DF-8-1-A reproduced verbatim (generator.py:337-339 "[!CAUTION] Repository is NOT ready for release" six lines under "INSUFFICIENT_COVERAGE (Exit Code 3)"). The plain_english "NOT VOUCHED" else-branch proven UNREACHABLE by a 270-fold exhaustive evaluate_verdict sweep (0 occurrences of NOT_READY_FOR_RELEASE with blocking==0; only 5 (verdict,row,has-blocking) triples occur), and evaluate_verdict is the ONLY producer of any verdict either surface renders (no code path in argus/ re-renders a verdict read back from disk). TWO further defects found by measurement, neither previously named: (a) a ROW-2 run appends coverage/critical clauses the short-circuited table never evaluated ("1 verdict-blocking finding(s); deep coverage 20/63 is below the 3/5 release threshold") plus a false "this coverage result is driven by test-file dilution" NOTE; (b) generator.py:67 classifies test files by NAME ONLY while pipeline.py:692 uses ast_entry= — measured to disagree on exactly 1 of 147 indexed files on this repo (argus/detectors/vacuous_test.py), so the report's APPLICATION denominator and the verdict's assessed population differ. ACs 6, 8, 10, 11, 12-15 ADDED at story design over the epic's six, each measured. 8 LOCKED decisions D1-D8 with rejected alternatives: D1 split on AuditVerdict.is_below_floor (the documented §3.3 single source of truth, carries the pre-amendment decision_row=None fallback), NEVER on decision_row directly and never on a re-derived ratio; D2 the NOT VOUCHED branch is resolved by RELOCATION (its prose IS the correct row-4 message) + a typed ValueError subclass on the impossible input, so the original bug string "BLOCKED — 0 verdict-blocking finding(s)" can never be reintroduced (exit_code_for_verdict / NegativeAssuranceError house pattern; a raise is testable, which is what DR-11 demands instead of untested dead code); D3 a row-2 report names ONLY the finding; D5 naming the VACUOUSLY-satisfied critical gate in prose is OUT (needs CriticalSubsystemSet threaded from pipeline.py, which DF-8-2-A forbids); D6/D7 the two open defers. Baseline re-measured: 1111 collected / 1108 passed / 3 failed / 0 skipped (the 3 adjudicated carve-outs only), mypy clean (69 files), pipeline.py 1199 lines.
  # DEFERRED-ITEM DISPOSITIONS: DF-8-1-A is PULLED IN and closed here (confirmed against epics.md — its ledger target_story is 8-3 AND the epic's own 8.3 AC block independently owns generator.py; both halves, generator.py:337-339 and plain_english.py:109-113, are named in AC5/AC2). DF-8-2-A is NOT closed and NOT triggered — this story does not edit argus/pipeline.py at all (every surface it changes is reachable from arguments generate_reports already receives, incl. ast_index), so the file stays at 1199/1200; AC14 FENCES pipeline.py so the cap cannot be breached, and AC15b carries the item forward with an append-only note. DF-8-2-B NOT pulled in — its ledger target_story is conditional ("or the first story that edits argus/detectors/vacuous_test.py") and this story does not edit that file; it is a detector-semantics defect (green, zero live instances in this repo), carried forward append-only per AC15c.
  # NOTE for the dev: the working tree carries Stories 8.1 AND 8.2's deltas UNCOMMITTED. Do NOT stash / checkout -- / reset; they are not recoverable from any commit. Two untracked root files (action.yml, .github/workflows/argus-student-audit.yml) are the USER's (Epic 9 packaging/CI) — out of scope, do not touch. Run tests as PYTHONIOENCODING=utf-8 python -m pytest tests/ -q (Windows cp1252 stdout raises UnicodeEncodeError on the em-dashes these very modules emit); pytest-timeout is NOT installed; a full run takes ~3.5 min.
  8-4-tell-integrators-what-changed: done  # code-review iteration 2 (2026-08-06) PASS after reviewer-applied fix round 3. All 13 ACs MET; R1-R4 from iteration 1 independently confirmed fixed (HEAD's fold + _assurance_statement EXECUTED, not read - R1's corrected before-string reproduces byte-for-byte). 10 patches applied, 3 defers filed as DF-8-4-B/C/D. Headline: the rot check had live routes around it - -05 was a whole-document substring satisfied by the note's own quoted 'before' (a straight revert of the amendment's flagship string was invisible); -06's guard was the bare word 'before' in a 2-line window, so stale wording republished under the unchanged-list header passed; the 1/5 floor was published 6x and pinned by nothing; row 4 renders THREE callouts and -15 forbade publishing the other two. All closed and shown RED-first (7 mutations RED, 5 controls GREEN). Also D1: a successful audit returned exit 1 / 'audit failed' on a cp437 stderr (em dash + UnicodeEncodeError-is-a-ValueError) while stdout carried verdict=RELEASE_READY - contradicting CHANGELOG's own exit-1 contract; handler split + stderr reconfigure, code brought UP to the published contract rather than the contract down to the code (TC-ArgusAgent-CLI-001-34). Suite 1150 collected / 1147 passed / 3 failed / 0 skipped - the three adjudicated carve-out reds only, no fourth red; mypy clean over 69 files; fences byte-identical by md5.
  # create-story 2026-08-06: DR-8 + RS-4a — the paper trail. Delta MEASURED IN PLACE on the real tree (HEAD 9109e16 + 8.1/8.2/8.3's uncommitted deltas) with TWO instruments: (a) `git diff HEAD -- argus/`, which IS the integrator-visible delta because none of Epic 8 is committed, so every "before" string is quoted from the `-` side rather than from memory; (b) importing the shipped argus functions and folding real evaluate_verdict inputs through the real render_ship_readiness / render_final_verdict_report. Foreign Epic-9 files excluded from every figure. ENUMERATED, NOT DESCRIBED: exit-code change is exactly ONE run class (0 blocking findings + unmet coverage/critical gate at or above the 1/5 floor: 2 -> 3); TWO schema bumps, not one — VERDICT_SCHEMA_VERSION "1"->"2" (verdict_gate.py:182) AND CRITICAL_SUBSYSTEMS_SCHEMA_VERSION "1"->"2" (critical_subsystems.py:146) — with DIFFERENT compat behaviour: decision_row is OMITTED from a v1 payload (measured: v1 read-back key list is the old ten, re-serializes byte-identically) while heuristic_excluded_ineligible is emitted UNCONDITIONALLY even when empty, so every critical-subsystem artifact changes bytes every run and, since .argus/ filenames are content-addressed, changes FILENAME; 9 added public names incl. ShipReadinessError (a NEW RAISE where prose used to render — verified raising, ValueError subclass); 5 changed rendered strings incl. a callout-level change CAUTION->WARNING on row 4, row 2 losing its appended coverage/critical clauses, and the PERSISTED negative-assurance row-4 sentence; stdout summary line + exit-code VALUES + the verdict enum unchanged (cli.py byte-identical to HEAD). --coverage-scope=application measured already present at HEAD, i.e. it PREDATES this delta — AC3 says so honestly. RS-4a scoped to the FILE argus/__init__.py (D3): 8 measured falsehoods beyond line 37, incl. "RESERVED PACKAGE SHELL — no business logic yet" and "reuses proven Minions infrastructure via direct import" (the latter falsified outright by Story 9.1); __version__/__status__/__all__ FENCED byte-identical (argus_version is folded into every content hash, NFR-P1). 13 ACs (epic's five carried as AC1/2/3/8/10; AC4-7, AC9, AC11-13 added after measurement, each justified). 8 LOCKED decisions D1-D8: D1 the note is CHANGELOG.md at the repo root (docs/ and the planning folder rejected — an integrator never reads them; the PRD addendum already states the migration and is exactly what DR-8 calls unpublished); D2 headed UNRELEASED with an explicit honesty preamble, because argus-agent 0.1.0 is on NO index (A1 falsified) and heading it as a release would make this very note a published Argus artifact asserting something untrue — pinned by AC9 so a later edit cannot quietly turn it into a release claim; D3 RS-4a = the file; D4 the proposed section shape ending in a 4-line "do I need to change anything?" list; D5 DF-8-3-B pulled in; D6 the four carried defers; D7 action.yml FILED not fixed; D8 the note documents the CONTRACT, never this repo's own RELEASE_READY verdict (that is 8.5/DR-10, grade demo-heuristic-only, never clearance). AC9 adds a RED-first ROT CHECK (tests/test_release_note.py, new area TC-ArgusAgent-DOCS-001-NN) that imports the shipped constants and re-renders the shipped strings, so the note cannot silently drift — an unpinned hand-written note is correct exactly once and would become the NEXT artifact contradicting the contract.
  # DEFERRED-ITEM DISPOSITIONS: DF-8-3-B is PULLED IN and closed here — its ledger target_story reads "8-4" first and unconditionally, the fix is ~5 lines in a 298-line cli.py plus one test, and it is in-domain: AC1/AC5 publish the AR3/AR10 contract including "typed failure -> exit 1", which the DEFAULT `argus audit <path>` invocation (no --report-dir) does not currently honour because render_ship_readiness at cli.py:292 sits OUTSIDE the try/except ValueError at :270-277 (verified by reading both). Publishing a contract the default path does not keep would be an over-claim in the document written to stop over-claims. AC11 requires the REAL render_ship_readiness to raise on the way out of main() with no --report-dir (8.3's CLI-001-32 monkeypatches run_audit and proves only the AC's letter). DF-8-2-A CARRIED FORWARD, not closed — target_story does not name 8-4 and this story needs nothing from argus/pipeline.py; AC12 FENCES it at 1199 lines / md5 399d6da1d36d668352fd7b0d539cc307 so the NFR-M1 cap cannot be breached. DF-8-2-B CARRIED FORWARD — conditional target (first story editing argus/detectors/vacuous_test.py) does not fire; fenced at md5 8a0705030391df92ad1404af9d044758. DF-8-3-A and DF-8-3-C CARRIED FORWARD — both target "the story that performs the DF-8-2-A pipeline.py extraction", which is not this one, and their only sensible homes are the two fenced files. NEW FILINGS required by AC10: RS-4b with the six CC-3 fields and origin_story 8-4, carrying the MEASURED surface (15 refs across 8 argus/*.py files, foreign minions_llm_adapter.py excluded, enumerated with line numbers) and the confirmed "not prose only" finding — dogfood/partition_plan.py:481,554 and dogfood/proof_run.py:597 are lines.append(...) calls emitting the stale path INTO generated Markdown, so a prose-only sweep misses them, and those generators produce Story 8.5/DR-10's artifacts so the sweep must FOLLOW 8.5; plus DF-8-4-A for action.yml's `else -> INSUFFICIENT_COVERAGE` swallowing exit 1 and publishing a crashed run as an under-covered one (pre-existing, untracked FOREIGN Epic-9-owned file), targeted at 9-2.
  # EPIC 9 DELIBERATELY EXCLUDED: no release workflow, no tag, no pyproject version bump, no action.yml or .github/workflows edit, and no prose claiming argus-agent is published — IN-0 / Story 9.2 owns all of it. AC12 fences those surfaces plus the FOREIGN concurrent-session files (argus/audit/minions_llm_adapter.py, tests/test_no_web_imports.py, action.yml, .github/workflows/argus-student-audit.yml, stories/9-1-*.md) and Story 8.5's dogfood/final-verdict artifacts. NOTE for the dev: 8.1+8.2+8.3 are UNCOMMITTED — do NOT stash / checkout -- / reset. A CONCURRENT session shares this tree: re-read deferred-work.md and sprint-status.yaml immediately before writing either, and change ONLY the 8-4 key. Known-red carve-out is exactly 3 (test_dogfood_plan.py x2 inherited, test_dogfood_proof.py deliberate = 8.5's deliverable); baseline 1127 collected / 1124 passed / 3 failed / 0 skipped, mypy clean (69 files). Any FOURTH red is yours. Run tests as PYTHONIOENCODING=utf-8 python -m pytest tests/ -q; pytest-timeout is NOT installed; ~3.5 min.
  # ORIGINAL code-review iter-1 CONCERNS (2026-08-07): DR-10 delta is CORRECT and independently verified — reviewer re-ran the full suite himself (1157 passed / 0 failed / 0 skipped, progress census Counter({'.': 1157}), exit 0) + mypy clean (69 files); RE-RENDERED all three dogfood artifacts from the live generators and confirmed each is BYTE-IDENTICAL to its committed copy (no hand-edit anywhere, so every published figure is a measured one: 69 files / 18206 LOC / 3 units / RELEASE_READY exit 0 / row_3_gates_met / deep 53/69 / blocking 0 / 101 findings / critical set 48 with 10 DR-5-ineligible + 0 unmatched / $X 843 + live sizing 431, fits under both); re-ran `argus audit .` at BOTH scopes and reproduced F5 verbatim (RELEASE_READY exit 0 · INSUFFICIENT_COVERAGE exit 3 — symptom NOT reproducible); confirmed `git diff -U0` shows ZERO deleted lines in both test modules (the 3 reds went green honestly, purely additively, 2 pre-existing + 1 delta-caused as required); confirmed the Story-7.2 record is preserved with the original 6994 bytes VERBATIM as the file tail; independently folded 13/15 + 0 blocking through the real evaluate_verdict and got exactly the supersession header's two rows (row_4/exit 3 · row_3/exit 0); re-measured the RS-4b split and found exactly 9 minions_core refs remaining at exactly the 9 lines the ledger names, partition_plan.py at zero; TC ids -35..-41 each used once, no reuse; AC13 fences all clean by git diff --stat. NOT `done` because 5 unresolved Low patch findings remain, written to the story's Review Findings section: (1) the republished §7 `N=0` understatement was neither fixed nor filed — DF-6-6-A-P1 does NOT cover it (verified at deferred-work.md:362-394), so the known-wrong published number has no ledger trace; (2) all three artifacts render `excluding argus/tests/` — a path that DOES NOT EXIST — and the new AC11b citation guard's regex only matches file-suffixed tokens, so it structurally cannot catch directory citations; (3) CostSummary.live_sized_ceiling uses 0 as an "absent" sentinel and _render_ceiling_pair branches on truthiness, so a zero sizing would publish "not supplied" (inconsistent with the correct set_retrieved pattern two dataclasses up); (4) _read_critical_subsystem_set turns an OPTIONAL disclosure read into a fatal DogfoodProofError on ANY unreadable state/*.json (a store the 4.2 lint would report as inconsistent can no longer produce the artifact that reports it) and its first-match-wins scan over content-addressed filenames has no recency ordering; (5) proof_run.py at 1154/1200 — the rejected renderer extraction is judged SOUND for this story (AC13 does not authorise it) but has no ledger id, unlike DF-8-2-A for pipeline.py at 1199.
  8-5-re-derive-proof-evidence-matches-tool: done  # code-review 2026-08-08 iteration 2: PASS. All FIVE iteration-1 Low findings adjudicated GENUINELY RESOLVED, none papered over — verified on disk, not read off the story. F1/F5 FILED as the review itself prescribed: DF-8-5-C's cited facts all re-verified (proof_run.py:764-765 IS the literal precision=Fraction(0,1)/n=0 call, artifact line 87 IS the N=0 gate string, and EXECUTING the shipped registry here returns 5 distinct classes / 7 populated rows) and DF-8-5-D carries the corrected softer blocker (sibling + re-export, no cycle) plus a constraint the review did not have (the unit_count>=3 knife edge), filed at orange with the 4-line headroom disclosed openly. F2 scrutinised hardest and holds: the emptiness is MEASURED truth — `git ls-files argus/tests` returns 0, the directory does not exist, a live re-derivation prints effective_exclude=() for BOTH generators, and grep over the three artifacts returns 0/0/0; NO reader information was dropped because the surviving clause 'tracked `argus/`: 69' now describes the COMPLETE population (69 = every tracked .py under argus/, counted independently) and the configured set is still recorded on the run; the new generic _DIR_TOKEN guard was proven RED by the REVIEWER against the real pre-fix HEAD artifacts (it flags the directory citations minions_core/ and minions_core/apaa/ that the old suffix-only regex was structurally blind to) and returns [] on all three current artifacts. F3 (int|None sentinel) and F4 (degrade-not-abort + typed >1-producer refusal, retrieval_note wired end-to-end) both fixed and pinned by TC-...-43/-44. Disclosure claims checked with git, not trusted: `git diff -U0 HEAD` shows ZERO deleted lines in both test modules, and proof_run.py's 22 deletions were each read — all are the AC2 false-subject/false-citation strings plus three signature lines, so NOTHING pre-existing was trimmed to fit 1196/1200; TC-...-41 is STRENGTHENED not loosened (derives the subject line from the recorded scope + measured exclusions, ADDS a negative, and cross-checks against a fresh un-excluded enumeration). Reviewer's own instruments: full suite exit 0 with progress census Counter({'.': 1160}) — 1160 passed / 0 failed / 0 skipped; mypy clean at 69 files; all three dogfood artifacts RE-RENDERED and sha256-compared BYTE-IDENTICAL to their committed copies; `argus audit .` re-run at both scopes with exit codes captured directly — RELEASE_READY exit 0 / INSUFFICIENT_COVERAGE exit 3, F5 symptom NOT reproducible; AC5 supersession tail byte-identical at 6994 chars and the analytic fold independently reproduced through the shipped evaluate_verdict (row_4/exit 3 · row_3/exit 0, NOT_READY_FOR_RELEASE unreachable); every path cited by all three artifacts resolves on disk and every cited TC id exists. AC1-AC13 all MET; AC13 fences clean by git diff --stat; sprint-status.yaml touched only at this key + last_updated. TWO Low NON-BLOCKING items written to the story for the next editor, neither withholding the pass: (a) the RS-4b progress note's post-change citation proof_run.py:632 went stale when fix it.1 inserted 42 lines above it — the reference is now at :666 (the note's other 8 line numbers and its 9-remaining count are exact); (b) build_dogfood_proof records the artifact subject from _DEFAULT_SCOPE_PREFIX instead of the enumerator's argument, unlike build_full_repo_plan one module over — not a falsehood today (neither entry point takes a scope parameter) but a latent trap of exactly the AC2 shape.
  # create-story 2026-08-07: DR-10 — the evidence. LAST story of Epic 8 by design. Every figure MEASURED IN PLACE at HEAD be9d744 by importing the shipped argus functions and running the shipped CLI (git diff HEAD is EMPTY now — Epic 8 is committed, so it is no longer the instrument). Baseline: 1147 passed / 3 FAILED, mypy clean (69 files). The 3 reds ARE the deliverable: two are pre-existing repo-growth drift on the committed plan artifacts (red since ae5f00c, unrelated to the delta) and the THIRD is delta-caused (8.1's reorder + 8.2's eligibility filter moved the live dogfood verdict, so the committed proof stopped matching). The epic's precondition says two; it is now three. AC6/D6 require them reported as 2 + 1, never as an undifferentiated "3 tests fixed".
  # MEASURED re-derivations: (a) live dogfood build_dogfood_proof('.') -> RELEASE_READY / exit 0 / decision_row=row_3_gates_met / deep 53/69 / blocking 0 / coverage_scope None / critical_subsystems_not_deep=() — boundary B1 held exactly as the epic predicted, and the story pins the METHOD not the verdict (D11). (b) the historical 7.2 Minions run folded through the REAL evaluate_verdict: 13/15 deep + 0 blocking cannot produce NOT_READY_FOR_RELEASE under ANY branch — it is row_3_gates_met/exit 0 if DR-5 clears the critical clause and row_4_gate_unmet_no_findings/exit 3 if it does not, and which one fires is NOT determinable here because Minions source is not in this repo. The artifact must say so rather than guess (AC5).
  # (c) Inversion F5, the delta's own acceptance test: `argus audit .` -> verdict=RELEASE_READY deep_ratio=57/149 blocking_findings=0 assessed_deep_ratio=57/73 scope=application held_out=76, exit 0; `argus audit . --coverage-scope repository` (the population the symptom was ORIGINALLY reported under) -> verdict=INSUFFICIENT_COVERAGE deep_ratio=57/149 blocking_findings=0, exit 3. The symptom — a blocking verdict carrying zero findings — is NOT reproducible at either scope. AC10 requires BOTH runs, because the default flipped to 'application' in CR-2 and the symptom was originally measured under the whole-repository denominator; running only the default would leave the F5 evidence one flag away from vacuous.
  # BIGGEST TRAP, and the reason AC2 exists: the dogfood generator's PROSE says Minions, its CODE does not. enumerate_tracked_sources defaults to scope_prefix="argus" — it self-audits THIS repo's argus/ package. Regenerating naively publishes a document asserting "the frozen audit over the REAL Minions repo" about a self-audit, plus three cited file paths that do not exist (tests/security/test_argus_secret_containment.py, tests/argus/test_dogfood_proof.py x2) and an AUTO-GENERATED banner naming a module that does not exist. That would be a NEW published falsehood authored by the story whose whole purpose is that no Argus artifact lies — worse than leaving it stale. Same defect in the partition/budget renderers.
  # LOCKED at story design (D1-D11, rationale in the story file): re-run for the live artifact + ANALYTIC for the historical one, in SEPARATE files; the 7.2 Minions record is PRESERVED verbatim at minions-dogfood-proof-story-7-2-superseded.md (RS-3 supersede-don't-erase — regeneration would otherwise destroy the only surviving record of the 332/2289/285 finding classes DF-7-2-A's human adjudication is defined over); canonical filenames do NOT change and no generated file is ever hand-edited; $X=843 stays frozen but the artifact must also render the LIVE 7.1 sized_ceiling (431 today) so two artifacts published in the same change cannot contradict each other; a worktree/-dirty report identity is ACCEPTED and disclosed, never engineered away (--strict cannot run on a dirty tree); no CHANGELOG entry.
  # SCOPE BOUNDARY vs RS-4b, measured: this story consumes 6 of RS-4b's 15 minions_core references (partition_plan.py:481,490,554 + proof_run.py:597,609,610 — the ones EMITTED INTO a committed artifact) and leaves the other 9; the bare-word "Minions" subject claims are not on RS-4b's list at all and are wholly 8.5's. Rule: if the string reaches a committed .md it is 8.5's; if it only reaches a Python reader it is RS-4b's. AC8 requires an append-only RS-4b progress note recording exactly which were consumed, so RS-4b's remaining scope stays honest.
  # NOT FIXED HERE, filed instead (AC8): DOGFOOD_ArgusAgent_VERSION "1.43.0" vs shipped 0.1.0 stamped into the SIGNED evidence bundle (target 9-2 — changing it changes the bundle content hash); the §7 gate-status string understating N=0 when the corpus is 5 classes / 7 rows; and the structural fragility that the committed-artifact rot checks re-break on any later commit changing argus/** LOC composition or partition membership.
  # FENCED (AC13): argus/verdict/**, argus/ledger/**, argus/reports/**, pipeline.py (1199 lines — one under the NFR-M1 ceiling), pipeline_persist.py, cli.py, argus/__init__.py, CHANGELOG.md, README.md, pyproject.toml, action.yml, .github/workflows/**, bmad-dev-loop-pack/ (orchestrator's, untracked — never git add it). sprint-status.yaml: 8-5 key only. DF-8-2-A / DF-8-3-A / DF-8-3-C / DF-8-4-A..D all untouched.
  # NOTE for the dev: new tests continue TC-ArgusAgent-DOGFOOD-001 at -35 (-01..-34 taken). Story Context §H tables every string and heading stem the EXISTING tests already pin — read it before editing a renderer, or 3 reds become 8. Regenerate the artifacts LAST, after all source edits: they are derived from argus/** bytes. Run everything as PYTHONIOENCODING=utf-8 python -m pytest tests/ -q and write every file with encoding="utf-8" (Windows cp1252 mangles the artifacts' non-ASCII characters). Full suite ~173s. Target final state: 1150 passed / 0 failed, mypy clean.
  # dev-story FIX ITERATION 1 (2026-08-07) -> back to review. ALL FIVE Low review findings resolved; none silently dropped, each with a per-finding resolution note in the story file. THREE FIXED IN CODE, each demonstrated RED-FIRST with the failure pasted into the Dev Agent Record: (2) the unmatched `argus/tests/` exclusion — the enumerators now render only the exclusions they MEASURABLY applied (`effective_exclusions` over the un-excluded enumeration, already present in partition_plan.py, now also on DogfoodProofRun.effective_exclude_prefixes), and MEASURED on this tree that subset is EMPTY (`argus/tests/` held out zero tracked files, which is exactly the finding), so all three artifacts now render no exclusion clause at all; the AC11b citation guard gained a generic `_DIR_TOKEN` directory pattern (no special-casing of `argus/tests/`) and caught the real pre-fix artifact RED first. (3) `CostSummary.live_sized_ceiling` is `int | None = None` and `_render_ceiling_pair` branches on `is not None`, so a genuinely-zero sizing is no longer published as "not supplied". (4) `_read_critical_subsystem_set` now returns `(set | None, measured_reason)`: an unreadable UNRELATED `state/*.json` DEGRADES to `set_retrieved=False` with the reason rendered into the artifact instead of aborting the whole proof run, and >1 producer-matching envelope raises the typed DogfoodProofError rather than disclosing whichever content-addressed filename sorts first. TWO FILED AS LEDGER ENTRIES, which was the review's own remedy for each: DF-8-5-C (the republished `N=0` understatement — call site `proof_run.py:764-765`, artifact line 87, true values re-measured by EXECUTING the registry: 5 distinct rule classes / 7 populated rows; understates, so not an over-claim) and DF-8-5-D (the rejected renderer extraction, with the real softer mechanism named — move the five dataclasses to a sibling and RE-EXPORT from proof_run.py, no cycle — plus a newly measured constraint the review did not have: a new module changes the partition input set while TC-...-18 pins unit_count >= 3 at exactly 3). HONEST DISCLOSURE the next reviewer must weigh: proof_run.py went 1154 -> 1196 / 1200, so NFR-M1 headroom is now FOUR lines; the extraction was still not performed (AC13 does not authorise it and the reviewer judged the rejection sound), which is why DF-8-5-D is filed at severity orange with target_story = the first story that edits the module. All three dogfood artifacts REGENERATED from the live generators after the source edits (never hand-edited; 69 files / 18276 LOC / 3 units / RELEASE_READY exit 0 / row_3_gates_met / deep 53/69 / blocking 0 / 101 findings / critical set 48 with 10 DR-5-ineligible / $X 843 + live sizing 431, fits both), both report directories re-run from the recorded CLI invocations, and F5 re-run at BOTH scopes with the symptom still NOT reproducible (RELEASE_READY exit 0 · INSUFFICIENT_COVERAGE exit 3). FINAL: 1160 passed / 0 failed / 0 skipped (census Counter({'.': 1160}), exit 0), `python -m mypy argus` clean at 69 files; `git diff -U0` shows ZERO deleted lines in both test modules vs HEAD; the one pre-existing assertion touched is TC-...-41's subject-line pin, STRENGTHENED from the configured exclusion to the measured one (it encoded the very defect finding 2 names) — not deleted, not loosened, not skipped. New ids: TC-ArgusAgent-DOGFOOD-001-42/-43/-44. AC13 fences re-verified clean by git diff --stat.
  epic-8-retrospective: done  # retro 2026-08-08 (epic-8-retro-2026-08-08.md): 5/5 stories done, outcome statement fully met (originating symptom not reproducible at either scope; 62 unreachable critical blockers -> 0; all 3 dogfood artifacts re-render byte-identical; precision gate still PROVISIONAL). Cost, honestly: 0 of 5 stories passed review iteration 1 (38 items, 1 High, all latent-verified); ALL FIVE shipped a guard narrower than its own AC; 4 of 5 were caught publishing an unmeasured figure -- every time by independent re-derivation, never by self-check. 14 defers filed / 2 closed / 12 open (2 orange). pipeline.py 1199 + proof_run.py 1196 of 1200 -- 4 ledger items now gate on those extractions. Epic-7 handoff backlog: 0 of 5 items moved. SIGNIFICANT DISCOVERY (SD-1): three ledger items name story 9-2 as target (DF-8-5-A orange, DF-8-4-A, RS-4b) and appear in NONE of its ACs; DF-8-5-A (signed bundle stamps 1.43.0 vs shipped 0.1.0) chains into DF-8-5-D's proof_run.py extraction -> partition input set moves -> artifact regeneration -- recommend the operator re-context/re-scope 9.2 before dev. CRITICAL PATH: the preserved minions-dogfood-proof-story-7-2-superseded.md (only surviving copy of the independent Minions run, un-re-derivable here) and the 8.5 story file are UNTRACKED by git -- git add before committing the 8.5 delta. H0 (who files the Minions handoff) still UNOWNED, deliberately untracked here.

  # ── Epic 9: Make Argus Consumable — stand alone, then ship a release (delta, Argus repo) ──
  # Covers IN-2 + RS-1 (9.1), IN-0 (9.2) · Depends on Epic 8 (a correct package to release)
  epic-9: done  # both stories done + retrospective complete 2026-08-09 (epic-9-retro-2026-08-09.md); rolled up by the dev-loop orchestrator
  9-1-argus-stops-importing-thing-it-audits: done  # code-review 2026-08-06 PASS: Argus stops importing minions_core (IN-2 / RS-1)
  9-2-ship-distribution-another-repo-can-actually-resolve: done  # code-review 2026-08-09 iter-2 PASS: all 7 iter-1 findings (R1-R7) independently re-verified and CLOSED on the merits, not taken on trust. R1 import census re-derived via the committed static AST walk (_modules_reaching) -- identical 5-file set, wheel independently opened via zipfile (76 = 71 .py + 5 dist-info), all 5 prose sites confirmed on disk. R2 both README/CHANGELOG carry an adjacent unresolvable-tag warning; git tag -l re-confirmed empty. R3 GH_TOKEN confirmed on the pre-build preflight step; _published_release_tags None/() distinction read in full; UNKNOWN (not ok) confirmed printed. R4 verified EXHAUSTIVELY: ran the committed _run_block_bodies extractor against the real workflow -- 7 run: bodies, 0 contain ${{; grep of all 9 ${{ occurrences confirms every one is a with:/env:/ref: key, none in a run: body. R5 all 3 SHAs cross-checked read-only against the live GitHub API and match v4.4.0/v5.6.0/v4.6.2 exactly. R6/R7 arguments accepted on the merits (mechanically confirmed: --creating-tag would contradict the input's own description; a pure precede-rule was hand-simulated and does false-flag the honest "ONLY by" sentence). Independently re-ran: pytest 1192/0/0 exit 0 (progress-census counted), mypy clean 71 source files + release_preflight.py, git diff --cached confirms only the claimed argus/** files changed and pipeline.py stayed at 1199, deferred-work.md +323/-0 zero deletions, no tag/push/publish occurred. One Low item remains, disclosed and non-blocking (action.yml:127 pre-existing shell interpolation, still deferred, byte-identical to 7be90f7). This CLOSES the last story of Epic 9 and the entire ArgusAgent plan.
  # dev-story 2026-08-09 fix-iter-1 (superseded by the code-review above, retained as the record): ALL 7 review patch items addressed (4 Med + 3 Low), 0 deferred. R1 import figures RE-MEASURED from scratch, not copied: 66 of the 71 wheel modules import, 5 do NOT (argus/precision/__init__.py, argus/precision/replay_harness.py, argus/dogfood/proof_types.py, argus/dogfood/proof_render.py, argus/dogfood/proof_run.py) -- this SUPERSEDES the '4 of 69' figure in the 2026-08-08 comment below, which used the PRE-split denominator and omitted replay_harness.py itself; corrected at all 5 sites (CHANGELOG, README, DF-9-2-A body, story Completion Note 7, test_release_preflight.py header). R2 the v0.1.0 install command is now marked UNRESOLVABLE adjacent to every code block (git tag -l is empty; prepared, not exercised). R3 GH_TOKEN added to the pre-build preflight step + E4 now returns Unevaluable and the report prints UNKNOWN instead of ok. R4 script-injection closed: every untrusted value bound via env: and quoted, tag validated against ^v\d+\.\d+\.\d+$ in the FIRST step. R5 all 3 actions pinned to full SHAs. R6 E2's CI-unreachability disclosed in CI_UNREACHABLE and pinned. R7 denial filter now requires the negation to PRECEDE the phrase. RED demonstrated for every behavioural fix against the pre-fix staged code. Suite 1192 passed / 0 failed / 0 skipped exit 0 (+7 tests, 0 removed); mypy clean 71 argus files + the preflight. NO argus/** byte changed and NO artifact regenerated -- none was implicated.
  # code-review 2026-08-09 iter-1 (SUPERSEDED by the fix round above, retained as the record): code-review 2026-08-09 iter-1 FAIL: 4 Med. Core VERIFIED by the reviewer: 1185/0/0 exit 0, mypy clean 71files, pipeline.py 1199 untouched, DF-8-5-D split proven a PURE MOVE at AST level (0 symbols lost, __all__ identical 17/17, 4 disclosed deltas only), all 3 dogfood artifacts re-render BYTE-IDENTICAL, ledger append-only +314/-0, minions_core down to the 2 allowlisted decoys, DF-9-2-A deferral judged DEFENSIBLE (fence honoured, pre-existing, argus audit runs from the wheel). Blocking: R1 published import figure "65 of 69" is a stale pre-split denominator (re-measured 66 of 71, 5 files) and self-contradicts CHANGELOG.md:105 + the test pin's 5; R2 the documented install pins tag v0.1.0 which does not exist and README's lead-in contradicts its own command; R3 E4 inert in CI (no GH_TOKEN on the preflight step) yet prints "ok"; R4 script injection via ${{ inputs.tag }} in run: on a contents:write job. All 4 are bounded prose/config patches.
  # dev-story 2026-08-08: IN-0 DELIVERED up to the outward-facing boundary. CLOSED DF-8-5-A (DOGFOOD_ArgusAgent_VERSION now derives from argus.__version__ — RED-first proof: the persisted envelope read 0.1.0 while its own hashed payload read 1.43.0; bundle content hash a1e76c01... -> b3588816... -> da17b0fe..., the first re-derived and confirmed against the committed signature line before it was trusted), DF-8-5-D (proof_run.py 1196 -> 679, split into proof_types.py 207 + proof_render.py 447 with a re-export shim; PURE MOVE proven byte-identical at 9927/9927 for a real run and 55101/55101 across 6 synthetic branch-covering runs), DF-8-4-A (action.yml exit 1 gets its own AUDIT_FAILED arm + a new `assessed` output; catch-all maps to the failure token so an unmapped code can never render as an assessment), RS-4b (9 swept, the 2 minions_llm_adapter true-negative decoys allowlisted, guard is an allowlist + a denial-check, never a zero-count).
  # PARTIALLY CLOSED: DF-8-4-B (heading half closed by TC-ArgusAgent-DOCS-001-16; bytes-example half explicitly left OPEN with its reason). OPENED: DF-9-2-A (4 of 69 modules unimportable from the built wheel — argus.precision + the 3 dogfood proof modules, all via replay_harness.py's unconditional `_registry` import from tests/; PRE-EXISTING at 7be90f7, verified not assumed; argus/precision/** is fenced so it is filed, disclosed in README+CHANGELOG, and pinned in BOTH directions by TC-ArgusAgent-RELEASE-001-11), DF-9-2-B (23 bare-word "Minions" claims outside dogfood/), DF-9-2-C (tracked .pyc files).
  # MEASURED CORRECTIONS to the story context, recorded because the context asked for re-derivation: (a) the DF-8-5-A fix is +3 lines net and left proof_run.py at 1199/1200 — it FIT, so the NFR-M1 fence did NOT force the extraction; the LEDGER did, plus the absence of any later story. (b) The §F simulation predicted ONE partition_id would move; the real split moved ALL THREE (2c0f52f60457/681c496d09ed/973f3f199d1c -> 085854c90586/477ef77d7b65/bde14bbf3bcf). unit_count HELD AT 3 as predicted and was never loosened. (c) The renderer block measured 391 lines, not the context's 367 nor the ledger's ~390. (d) Bare-word "Minions" ran 45x/17 files at HEAD, not 44. (e) TC-ArgusAgent-DOGFOOD-001 is taken through -44, not -36; new dogfood tests start at -45.
  # REGENERATION ran ONCE, LAST, after every argus/** edit and AFTER staging the two new modules (git ls-files, not git diff, was the instrument). RED-first verified before regenerating: all three live partition_ids absent from the committed plan and 443 absent from the committed budget plan, so -03 would have failed 3 ways and -06 once. Both green after, with tests/test_dogfood_plan.py COMPLETELY UNTOUCHED (empty git diff) — no assertion weakened to reach green. Figures: 69->71 files, 18276->18418 LOC, 345->355 credits, ceiling 431->443, headroom 86->88, verdict RELEASE_READY / row_3_gates_met UNCHANGED, DOGFOOD_BUDGET_CEILING=843 deliberately NOT re-pointed (D7).
  # WHOLE-SYSTEM: pytest 1185 passed / 0 failed / 0 skipped, exit 0 (baseline 1160; +25 tests, 0 removed, 0 skipped); mypy clean over 71 source files; no argus/** file over 1200 lines; pipeline.py UNTOUCHED at 1199/1200. AC15 fences verified with BOTH `git status --porcelain` AND `git diff --cached --stat` (AI-E8-2). Two carve-outs, both docstring-only and both mandated by AC11: store/envelope.py:25 (AC15 exempts it) and verdict/prosecutor.py:36 — NOTE the story's AC15 fences argus/verdict/** while AC11 names that exact line; the more specific instruction was followed and the conflict is disclosed in the Dev Agent Record.
  # PREPARED, NOT EXECUTED (orchestrator boundary — publication is the operator's call): NO tag created, NO push, NO GitHub Release, NO PyPI upload. `git tag -l` is still empty. The release workflow is COMMITTED AND HAS NEVER RUN, and every document says so — no README, CHANGELOG, workflow or story line claims a published release, per AC2's evidence rule (a release is a URL or an Actions run id). AC2 is satisfied for "a workflow exists" and prepared-not-executed for "attaches to a GitHub Release". AC3 records the repository's VISIBILITY as measure-this rather than as a fact, because measuring it needs a network call the boundary reserves for the operator. AC4 is FULLY satisfied: the build and the fresh-venv proof are local and were executed in full (argus --help exit 0; argus audit <fixture> -> RELEASE_READY exit 0; no minions_core; version 0.1.0 — all from a directory that is not this repository).
  # H0 REMAINS UNOWNED (AC16). This story did NOT file, claim or close H1-H4. A5 stays UNSUPPORTED and IN-1 must be an optional extra. AFTER THIS STORY CLOSES THE DEFERRED-WORK REGISTER HAS NO OWNER: 12 entries OPEN plus half of DF-8-4-B plus the 3 new ones, and every "the first story that..." target now points at a story that does not exist. pipeline.py sits at 1199/1200 with DF-8-2-A / DF-8-3-A / DF-8-3-C all gating on an extraction nobody is queued to perform. Recorded rather than papered over (D11).
  # create-story 2026-08-08: IN-0 — the distribution. LAST story of Epic 9 AND of the entire ArgusAgent plan; there is no successor to inherit a deferral, which is why AC14 forces a disposition on every ledger id rather than allowing a re-target. RE-SCOPED BY OPERATOR DECISION acting on the Epic-8 retro's SD-1 / AI-E8-4: 9.2 ABSORBS the three ledger entries that name it, as ONE story, not a split. epics.md gives 9.2 five ACs (all release mechanics); the story carries SIXTEEN. The variance and its authority are recorded IN THE STORY FILE (§Variance from the epic) — epics.md is deliberately NOT edited.
  # ABSORBED with dispositions: DF-8-5-A (AC7, the signed-bundle version token) · DF-8-5-D (AC8, the proof_run.py extraction) · DF-8-4-A (AC10, action.yml exit-1 arm) · RS-4b (AC11, the 9 remaining minions_core references). A FIFTH, DF-8-4-B, fires by its own target_story wording the moment AC6 edits tests/test_release_note.py — dispositioned in D12. STAY OPEN and fenced: DF-8-5-C (republished knowingly, AC13) · DF-8-5-B · DF-8-4-C · DF-8-4-D · DF-8-2-A / DF-8-3-A / DF-8-3-C (all gate on the pipeline.py extraction; AC14 requires the story to RECORD that they become UNOWNED when this story closes).
  # BASELINE MEASURED IN PLACE at HEAD 7be90f7 (not taken from the retro): pytest 1160 passed / 0 failed / 0 skipped, exit 0, progress census counted character-by-character = Counter({'.': 1160}); mypy clean over 69 source files; `git tag -l` EMPTY; `.github/workflows/` holds exactly 2 files (audit-ci.yml, argus-student-audit.yml) and NEITHER builds, tags or publishes; branch is fix/honest-verdict-reporting while both workflows trigger only on master/main. The story starts on ZERO red.
  # LOCKED at story design (D1-D13, rationale in the story file): release 0.1.0, NOT bumped (epics.md:1669 pins the value; TC-ArgusAgent-DOCS-001-10 asserts it; __version__ is the default envelope argus_version so a bump moves the bytes of every persisted artifact for no requirement). Distribution target = a GitHub Release carrying sdist+wheel for tag v0.1.0, plus the `git+https://github.com/Inan15/Agent-Argus.git@v0.1.0` VCS pin marked EXPLICITLY INTERIM with a NAMED exit condition. PyPI is NOT attempted: publishing a name+version is IRREVERSIBLE, needs a credential this story cannot prove exists, and cannot be verified from this repo — escalate rather than assume.
  # MEASURED CORRECTION #1 to the inherited SD-1 narrative: the NFR-M1 fence does NOT force the DF-8-5-D extraction. The DF-8-5-A fix is ~+1 line (1196 -> ~1197 of 1200) and FITS. What forces the extraction is the LEDGER — DF-8-5-D's target_story is literally "the first story that edits argus/dogfood/proof_run.py for any reason" — plus AI-E8-3 and the fact that no later story exists. Decisive efficiency argument recorded in D4: DF-8-5-A alone already moves the published bundle signature and forces a proof regeneration, so doing both together costs ONE regeneration instead of two over rot checks with a measured history of four consecutive red commits (DF-8-5-B).
  # MEASURED CORRECTION #2: a LABELLED SIMULATION of the extraction over an ISOLATED tempfile copy of argus/ (real tree never mutated; git status re-verified) held unit_count at 3 — it did NOT drop to 2. Unit 681c496d09ed sits at EXACTLY 40 files = DEFAULT_SOFT_FILE_LIMIT (partitioner.py:107), so adding modules pushes toward a SPLIT, not a merge. What DID move: partition_id 973f3f199d1c -> 31483e58e318 (ids are sha256 over sorted member PATHS, proven by 681c496d09ed keeping its id while its LOC moved 14529 -> 14093) and sized_ceiling 431 -> 443. So TC-ArgusAgent-DOGFOOD-001-03 and -06 WILL go red and all three artifacts must regenerate. The "a drop to 2 is a finding, never a licence to loosen the pin" instruction STANDS and AC9 still requires verifying it.
  # THE CONTRADICTION, TRACED THROUGH SHIPPED CODE (why DF-8-5-A is not cosmetic): run_dogfood passes the "1.43.0" literal to build_evidence_bundle (proof_run.py:683-684) -> bundle_to_canonical_payload puts argus_version INTO THE HASHED PAYLOAD (evidence/bundle.py:288) -> persist_evidence_bundle does NOT pass argus_version to write_payload (bundle.py:329-334) -> EnvelopeWriter.build defaults the ENVELOPE field to argus.__version__ = "0.1.0" (writer.py:110-118, envelope.py:99). One persisted .argus/state/<hash>.json therefore says 0.1.0 at the envelope level and 1.43.0 in the payload it wraps, and the fix MOVES the content hash and its locator — i.e. the signature published at minions-dogfood-proof.md:57 (a1e76c01cbd29241a928f71b724b4c4c01d1211e0a4ae8a6e266386f811e0c0e). The package already forbids this in writing: argus/__init__.py:56-58 "Never hardcode this literal at call sites"; envelope.py:106-107 "callers must not pass a literal".
  # CORRECTION TO A DOCSTRING THE DEV WILL READ: tests/test_release_note.py:782-783 claims __version__ is "folded into EVERY content hash". It is not — compute_content_hash hashes the PAYLOAD ONLY, so __version__ changes an artifact's BYTES but not its hash or filename; the evidence bundle is the sole exception because it also carries the version inside its payload. Load-bearing for AC7's blast radius. Docstring only, nothing goes red.
  # RS-4b RE-MEASURED on this tree: exactly 9 references remain, at exactly the lines the ledger names — EXCEPT proof_run.py, where the ledger's :632 is now :666 (AI-E8-7's correction confirmed). AC11 closes it by sweeping rather than filing a fourth line number, since the DF-8-5-D extraction will move it again. TWO DECOYS ARE ALLOWLISTED, NOT SWEPT: minions_llm_adapter.py:5 and :29 are Story 9.1's TRUE NEGATIVE statements ("zero dependency on minions_core") — deleting them would delete the documentation of RS-1/IN-2, so the AC11 guard is an allowlist, never a zero-count assertion. Bare-word "Minions" (44 occurrences / 17 files) is NOT RS-4b; bounded to argus/dogfood/** by D10, rest filed.
  # AI-E8-1 IS CLOSED, re-measured not assumed: `git ls-files` returns BOTH minions-dogfood-proof-story-7-2-superseded.md and stories/8-5-...md — the retro's 🔴 untracked-evidence item was resolved by commit 7be90f7. FENCED by AC15: pipeline.py (1199/1200 — do not add a line), pipeline_persist.py, verdict/**, ledger/**, reports/**, cli.py, detectors/**, index/**, evidence/**, precision/**, cache/**, the preserved 7.2 record, every retro, epics.md, E-PRD/**. Fence checks must use `git status --porcelain` AND `git diff --stat` — `git diff` alone cannot see an untracked path, which is how Epic 8's third recurrence was missed (AI-E8-2).
  # H0 IS EXPLICITLY NOT ABSORBED (AC16). It remains UNOWNED — epics.md:1693, readiness F5 (LIVE, 2026-08-03), AI-E8-10 — and is deliberately outside this tracker per the DELTA NOTE above, because it executes in the Minions repo and this repo's CI cannot verify it. The story must state that it did NOT file H1-H4, and must restate that assumption A5 stays UNSUPPORTED (post-amendment Minions lands on exit 3, which still fails an unconfigured blocking gate, so H3 needs a policy decision) and that IN-1 must be an optional extra because Minions declares dependencies = [].
  # NOTE for the dev: AI-E8-6 is applied as a story-authoring rule — AC5 (release edge cases E1-E6), AC11, AC12 and AC15 are written as ENUMERATED SPACES that must FAIL on an unenumerated member; discharging one with a single sample is a breach, not a pass. New test ids continue TC-ArgusAgent-DOGFOOD-001 at -37 (-36 taken) and TC-ArgusAgent-DOCS-001 at -14 (-13 taken). TWO THINGS THE SM COULD NOT MEASURE and did not assert, each with an AC forcing the dev to measure them: the contents of a built sdist/wheel (flit_core is NOT installed here) and whether the GitHub remote is public / whether any PyPI credential exists.
  epic-9-retrospective: done  # retrospective complete 2026-08-09 (epic-9-retro-2026-08-09.md) — FINAL epic and FINAL retro of the plan; 2/2 stories done (9.1 PASS iter-1; 9.2 FAIL iter-1 on 4 Med -> 1 fix round -> PASS iter-2). Epic-8 follow-through 3 done / 4 partial / 3 not-addressed — every item that became an AC landed (AI-E8-4's SD-1 re-scope closed DF-8-5-A, DF-8-5-D, DF-8-4-A, RS-4b), every governance-only item did not (AI-E8-8/-9/-10, H0 still UNOWNED). SD-1: the release is PREPARED, NOT EXECUTED — git tag -l empty, release.yml committed and never run, so epics.md:1620's "another repository can depend on" clause and the Minions handoff's "published argus-agent" prerequisite are both unmet by one operator action. SD-2: DF-9-2-A ships 5 of 71 wheel modules unimportable (consumer `argus audit` path proven clean), owned by nobody. SD-3: the repo gained CI/CD supply-chain surface this epic with no gate for the class; injection fixed in release.yml, DF-9-2-D still open in action.yml. Register closes with 16 entries reading target_story: NONE; pipeline.py ends at 1199/1200 with no story queued. 10 action items (AI-E9-1..-10), each targeted at the operator runbook, a named human on a ledger entry, an explicit follow-up story, or retire-with-rationale — none parked in a nonexistent Epic 10.

  # ── Epic 10: Specification Debt from the Separation — close the gate that let it through (delta, Argus repo) ──
  # Covers DF-AUD-APAA-C (10.1), -D (10.2), -E (10.3), -F (10.4) · Depends on Epic 9 (a released artifact worth correcting)
  # Sequence is GATE-FIRST: 10.1 is the control that would have caught 10.2-10.4; correcting artifacts
  # while the gate still accepts self-attestation invites recurrence. 10.4 is independent and may land anytime.
  # Source: sprint-change-proposal-2026-08-09.md. Contract edits (PRD FR30, PRD V1/V2, architecture R3,
  # README/CHANGELOG) remain PROPOSED pending PO/PM + Architect approval — mirroring how 2026-08-03 handled CR-1/CR-3.
  epic-10: done  # added 2026-08-09 by sprint-change-proposal-2026-08-09.md; EXTENDED to 5 stories 2026-08-10b (10.5 added); 0/5 stories. backlog -> in-progress 2026-08-10 on creation of the first story (10.1). in-progress -> done 2026-08-11 (dev-loop epic roll-up): all 5/5 stories done and epic-10-retrospective done (epic-10-retro-2026-08-11.md).
  10-1-release-status-must-cite-evidence: done  # code-review 2026-08-10 iteration 1: PASS, no unresolved findings. Independently re-derived, not read off the story: all 7 ACs verified against the actual files (AC1 :63 struck intact + dated §5 correction; AC2 architecture.md §H rule + §Enforcement; AC3 guard read in full, non-vacuous glob closure and positive control confirmed both directions; AC4 DF-AUD-APAA-C byte-intact original + append-only closure note; AC5 both epics.md fenced locations amended, nothing else touched; AC6 LOCAL gates re-run by the reviewer — mypy 0 issues/71 files, bandit 0 Med/0 High (18 Low), pytest 1235 collected/all green/coverage 94.97%, all matching the dev's figures exactly; AC7 git diff --stat HEAD -- argus/ empty, deferred-work.md numstat 161/0, audit-ci.yml untouched (DN-6), no tag/push (DN-7), tests/ diff shows only the new file — no existing test touched). DN-1..DN-8 all honored. Two theoretical Low observations (an exclusion dict's combined key; a narrow sha/run-id-citation regex edge case) were dismissed as noise, not filed. Status -> done.
  # create-story 2026-08-10: story file written; status backlog -> ready-for-dev. MEASURED LIVE against the GitHub Actions API while contexting (gh is authenticated on this host; api.github.com is reachable only with the tool sandbox disabled). TWO FINDINGS THAT CHANGE THE STORY. (1) AC3's evidence ALREADY EXISTS: audit-ci.yml run 31341363300 on master, sha 00c8d1b (= current HEAD = origin/master), conclusion success, ALL THREE matrix legs (3.10/3.11/3.12) success, 2026-08-09T23:13:27Z. So 10.1 is NOT a "make CI pass" task — it is "cite the run that already passed, and scope the citation to the sha it covers", because the story's own commit moves HEAD off 00c8d1b and a run id without a sha is the next version of the defect under repair. (2) NEW, NOT IN THE EPIC AS WRITTEN: the ledger entry REPEATS THE CLASS IT FILES. DF-AUD-APAA-C (deferred-work.md:1336-1337) states "Both were repaired 2026-08-09 and a clean-venv reproduction of every step passes on 3.12" — but the repair commit cd60dbb's OWN run 31322881580 FAILED (3.11 leg died at the pytest step; 3.10 and 3.12 were CANCELLED by fail-fast, so the 3.12 claim was never executed on 3.12 either). Measured cause: `git diff cd60dbb 00c8d1b -- .github/workflows/audit-ci.yml pyproject.toml` is EMPTY — the workflow repair was correct; what was still broken was PRODUCT CODE ON POSIX ("Failed: DID NOT RAISE WorkspaceContainmentError"), closed by the six POSIX/non-ASCII fixes in cd60dbb..00c8d1b (d0e0a5c, ebdca75, f7c666e, 266bb28, f85fe76, 40c0727). Folded into AC4 as an APPEND-ONLY correction (§3.4) — quietly fixing the entry would be the third instance. 7 ACs: AC1 correct the 2026-07-28 record in place (strike :63, dated+reasoned block, cite 30774175196 failure AND 31341363300 success with the sha each covers); AC2 the rule lands in architecture.md §H + §Enforcement AND a committed guard (both halves — prose-only is unenforced, test-only is not a rule); AC3 the guard bites (positive control both directions + GLOB CLOSURE so a new proposal cannot escape + non-vacuity); AC4 DF-AUD-APAA-C closed append-only incl. the finding above; AC5 epics.md:1748-1750/:1808 told to cite SHA-SCOPED (fenced to those two locations only); AC6 local gates re-run and LABELLED LOCAL (necessary, not sufficient — every one of the six fixes above was a host-portability defect invisible on this Windows host); AC7 whole-system, ZERO argus/** diff, deferred-work.md +n/-0. LOCKED: DN-1 rule in architecture.md §H not a new governance doc (AI-E9-7 names it; AI-E9-8 says a register with no reader evaporates) · DN-2 NEW tests/test_evidence_citation.py, area ArgusAgent-DOCS ids -20..-23 (-01..-19 taken) · DN-3 citation = run URL/id PLUS the sha it covers · DN-4 scanned set = sprint-change-proposal-*.md + epic-*-retro-*.md · DN-5 stories excluded BY NAME with the reason · DN-6 audit-ci.yml NOT edited (already correct and green) · DN-7 no push/tag/workflow_dispatch — triggering a run to manufacture a citation is manufacturing evidence · DN-8 the ledger's wrong sentence is corrected append-only, never edited. Guard shape REUSED from tests/test_release_surface_honesty.py (-17/-17b/-18): registry + glob closure + sentence-scoped scan whose negation must PRECEDE the claim + positive control. RED-first mandated (AI-E3-1: story 3.4's keystone test was green over its own keystone bug). Baseline 1230 tests collected; Python 3.11.15 local vs 3.10/3.11/3.12 CI; mypy 2.3.0 local vs >=1.0 in CI (divergence recorded, CI wins). Fences: DF-9-2-D + argus-student-audit.yml -> 11.3 · pipeline.py 1331/1200 -> 12.1 · publish -> 12.9/operator AI-E9-1 · precision gate -> Epic 13, NOT CLEARED by anything here.
  # dev-story 2026-08-10: ready-for-dev -> in-progress -> review. All 7 ACs met; 8/8 tasks complete. EVIDENCE RE-DERIVED LIVE, not transcribed (T1): gh reached api.github.com without disabling the sandbox, and all four figures reproduce — 31341363300 success/3 legs success/sha 00c8d1be...19fe0; 31322881580 failure, 3.11 leg failed at "Run Pytest & Coverage Assurance Gate" with 3.10+3.12 CANCELLED, sha cd60dbb; 30774175196 failure in 40s at "Run Static Analysis & Security Scans" on the 3.12 leg, sha ae5f00c (the sha the story did not name — cited here per DN-3); `git diff cd60dbb 00c8d1b -- .github/workflows/audit-ci.yml pyproject.toml` EMPTY (0 bytes). DELIVERED: AC1 sprint-change-proposal-2026-07-28.md :63 struck (not deleted) + dated §5 correction block naming the failed run, the superseding run, the sha each covers, what run 31341363300 does NOT cover, and the corrected status NOT ESTABLISHED; :55 annotated LOCAL. AC2 rule written in architecture.md §H (cross-referencing AUDIT_FAILED-is-not-a-verdict, action.yml:33-48) AND named in §Enforcement, AND enforced by the new tests/test_evidence_citation.py. AC3 guard bites both directions. AC4 DF-AUD-APAA-C closed APPEND-ONLY in 6 parts (a)-(f) incl. the §A.3 finding; original entry byte-intact. AC5 epics.md amended at EXACTLY the two fenced locations. AC6/AC7 below.
  # RED-FIRST DISCHARGED (AI-E3-1), and re-discharged after the test file changed: with the FINAL test code restored against the pre-edit documents, -20/-21/-23 FAIL — -21 fails on the verbatim historical defect line — and -22 was separately proven to bite by planting an unregistered sprint-change-proposal-9999-01-01.md (caught, then removed). -21b/-22 pass in the RED state by design: they are pure-function/closure controls, not tests of the corrections. Documents restored byte-identical (sha256 round-trip verified on all four).
  # GATES, ALL LOCAL — NECESSARY, NOT SUFFICIENT (AC6): mypy argus = Success, 0 issues in 71 files (local mypy 2.3.0; CI resolves mypy>=1.0 and may differ — if they disagree the EXECUTED CI RUN is the evidence). bandit -r argus --severity-level medium = exit 0, 0 Medium / 0 High (18 Low, below threshold). pytest tests/ --cov=argus --cov-fail-under=80 = 1235 passed, 0 failed, 0 skipped, 484.99s, coverage 94.97%. Baseline was 1230 collected, so the count grew by EXACTLY the 5 new cases; no test removed, skipped or weakened. THE EXECUTED-GATE CITATION FOR THIS TREE DOES NOT EXIST: run 31341363300 covers 00c8d1b only, and this delta is not in it. Per DN-7 nothing was pushed, tagged or workflow_dispatch'd — the run that evidences this commit is created when the operator pushes (AI-E9-1), and until then this story's own tree status is NOT ESTABLISHED, which is the rule applied to itself.
  # AC7 fence VERIFIED: `git status --porcelain -- argus/` is EMPTY — zero product-code change. My delta touches exactly the 7 permitted paths. deferred-work.md is +161/-0 vs HEAD (was +114/-0 before this story; +47 appended, mechanically append-only). epics.md +823/-29 (was +802/-28: +21 lines and exactly ONE deletion, the AC5-sanctioned struck clause). architecture.md +273/-35 (was +247/-35: +26, zero new deletions). sprint-change-proposal-2026-07-28.md +45/-2, and BOTH "deletions" are in-place line replacements whose original text is preserved verbatim inside the replacement (the ~~strike~~ and the LOCAL annotation) — nothing was removed. Story file and tests/test_evidence_citation.py were `git add`-ed (AI-E8-1: git diff cannot see an untracked path). NO new dependency; the guard is pure pathlib + re. NOTE for review: the Edit tool normalised 64 tail lines of deferred-work.md from LF to CRLF; git's own normalisation makes this invisible (-0 confirmed) and no content changed, recorded rather than hidden.
  10-2-multi-language-grounding-is-v1-in-the-specs: done  # code-review 2026-08-10 iteration 1: PASS, no unresolved findings. Verified independently: all 8 ACs checked against the files on disk (12 amendment sites confirmed by content, exemptions at architecture :517-518/:812-813 confirmed correctly untouched); FR7/README/CHANGELOG confirmed to disclose the DF-10-2-A shortfall immediately beside the delivered-in-V1 claim, so the amended contract does not oversell in a subtler form. Mechanical claims re-derived by the reviewer: pytest tests/ (ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1) -> 1272 passed/0 failed/0 skipped (exact match); mypy 0/71 files; bandit 0 Med/High; coverage 95.06%; git diff on pipeline.py and audit-ci.yml both empty (byte-unchanged, confirmed); deferred-work.md numstat 257/0 (append-only, confirmed); sha256 on the two claimed-unchanged PRD companions matches; file mtimes partition cleanly at the session boundary confirming zero write-set leakage; no push/tag found in reflog, matching the recorded NOT ESTABLISHED CI status. The three new guards (spec-claim closure, grounding matrix, cache-key bidirectional) read in full and independently re-run green; no existing test found deleted/skipped/xfailed. except(ImportError, Exception) confirmed structurally untouched and correctly fenced to 10.4. Status -> done.  # DF-AUD-APAA-D · 🟡 · Engineering Lead. Amend PRD (L23/L116/L174/L180) + architecture (L77-78/L200-202/L220/L237) to record multi-language grounding as delivered in V1; remove it from the V2 roadmap so V2 cannot re-scope delivered work; make grammar provenance PER-GRAMMAR (R3 design change — the key was specified for one grammar) BEFORE the Epic-5 store is wired, while it is still free; document the [languages] extra in README + CHANGELOG. Sibling of DF-5-1-A (same silent-cache-staleness class). Does NOT close DF-AUD-APAA-A.
  # create-story 2026-08-10: story file written; status backlog -> ready-for-dev. TWO MEASUREMENTS THAT CHANGE THE STORY, both re-derived on this tree, neither read off a document. (1) THE SITE ENUMERATION IS WRONG A THIRD TIME. The 2026-08-10b "measured 10-site list" (epics.md:1840-1845) is itself incomplete AND its line numbers have all drifted, because the 10b cascade inserted ~95 lines into prd.md and ~90 into architecture.md after the count was taken. Measured by grep on this tree: PRD is COMPLETE at 8 sites but they now live at :23/:121/:193/:214/:364/:438/:469(FR7)/:571(NFR-P2), not L23/L116/L174/L180/L317/L375/L398/L476. ARCHITECTURE names 2 (L220->:267, L237->:317) and measurement finds TWO MORE that all three enumerations missed — :89-90 ("AST = Python in V1") and :800 ("Future enhancement: multi-language AST (V2)"). TOTAL = 12 TO AMEND (8 PRD + 4 arch), plus 2 EXEMPT BY NAME WITH THE REASON: architecture :471 and :766 say "the default public install grounds Python only", which is TRUE (the 9 grammars are an optional extra) and is Story 12.5's NFR-P3 decision — amending them would replace a true sentence with a false one. Consequence recorded as DN-4: the closure GUARD (AC2), not the site list (AC1), is the load-bearing AC; a hand-list closes today's instance, only the guard closes the class. (2) ONLY 8 OF 10 LANGUAGES ACTUALLY GROUND — measured by running build_ast_index over a ten-language fixture: TypeScript and PHP return ast_eligible=False with reason grammar_missing_typescript / grammar_missing_php WHILE BOTH GRAMMARS ARE INSTALLED AND DECLARED IN [languages] (pyproject:47-56). Cause is ast_index.py:257-268, which resolves only `getattr(mod, "language")`; tree_sitter_typescript exports language_typescript/language_tsx and tree_sitter_php exports language_php/language_php_only — nothing raises, getattr returns None, the code falls through. So the reason token tells an operator to install a package they already have: the SAME harm DF-AUD-APAA-F/10.4 files, from a THIRD cause that splitting `except (ImportError, Exception)` does not catch. This constrains AC1 hard: FR7 is the BINDING capability contract, and writing "10 languages delivered in V1" into it while 2 return False would replace one false spec claim with another, in the OVERSELL direction the ledger notes the original was not — Story 10.5's defect class manufactured inside the story closing its inverse. LOCKED DN-3: this story FIXES the entry-point resolution (declarative per-language map, verified: language_typescript/language_tsx/language_php all construct and parse cleanly on the installed runtime; blast radius measured ZERO — `git ls-files` finds 0 tracked .ts/.tsx/.mts/.cts/.php files, so the dogfood verdict cannot move); FALLBACK if non-trivial is record the measured 8/10 and FILE the two, never a silent 10. ALSO MEASURED: grammar_version records '0.25.0' (tree-sitter-python) for a build that parsed Rust with 0.24.2, Java 0.23.5, Ruby 0.23.1 — versions genuinely differ across the 10 installed grammars (0.23.1..0.25.0), so provenance is wrong for 7 of the 8 that ground, and the CACHE_KEY_SCHEMA_VERSION bump is FREE today (grep proves no production caller derives a key: pipeline.py imports neither cache.key nor cache.memo_store). 8 ACs: AC1 amend all 12 sites dated+attributed with V2 lists dropping only multi-language · AC2 the closure guard (registry + GLOB CLOSURE + pattern claim-shape + positive control both directions + non-vacuity + reasoned exemptions) · AC3 make the claim TRUE (10/10 grounding matrix, enumerated so language #11 cannot escape) · AC4 per-grammar provenance, ADDITIVE (grammar_version retained, schema_version bumped, description corrected) · AC5 key moves for a grammar that PARSED and NOT for one that did not (both directions pinned), CACHE_KEY_SCHEMA_VERSION 2->3, architecture R3 amended at :83-88/:246-249/:327-328 as a DESIGN CHANGE · AC6 store NOT wired, as a POSITIVE requirement · AC7 README + CHANGELOG name the [languages] extra (measured: zero occurrences in either today) · AC8 ledger append-only + LOCAL-labelled gates + 10.1's sha-scoped citation or NOT ESTABLISHED + write-set fence. RED-first mandated on all three new guards (AI-E3-1). Baseline 1235 collected (summed per-file on this tree); Python 3.11.15 local vs 3.10/3.11/3.12 ubuntu CI. New ids: INTAKE-003-07.. / INDEX-001-105.. / CACHE-001-76.. / DOCS-001-24.. (maxima measured -06/-104/-75/-23). FENCES: pipeline.py 1331/1200 byte-unchanged -> 12.1 · `except (ImportError, Exception)` at ast_index.py:266 -> 10.4 · MemoStore wiring -> 12.3 · runtime tree-sitter version bound -> 11.4 · architecture :471/:766 and grammars-to-base-deps -> 12.5 · epics.md NOT in the write set (its 10.2 AC line numbers are STALE and are recorded in the story, not corrected there).
  10-3-invocation-contract-says-what-the-cli-accepts: done  # code-review 2026-08-10 iteration 1: PASS, no unresolved findings. Independently re-derived by EXECUTION, not read off the story: Live-Key Safeguard fuzzed adversarially (4 live keys x 12 hostile --ignore-pattern values x 7 hostile --ignore-path globs + combined-flag + case-variant probes, 0 escapes in every sweep); AC6 guard proven to catch a rogue flag injected into a live (monkeypatched) build_parser, independent of the story's own synthetic-parser unit test; fence D confirmed by git diff --quiet on pipeline.py/models.py (clean) and epics.md confirmed unchanged by mtime (predates this story's session); action.yml/audit-ci.yml/pyproject.toml/argus-student-audit.yml/README:178-190 all confirmed untouched; Story 1.7's amendment read in full and confirmed genuinely append-only (original AC3 byte-preserved, dated+reasoned note appended, not a rewrite); deferred-work.md numstat +404/-0 confirmed, DF-AUD-APAA-E closed with its original entry byte-intact, DF-10-3-A/B/C filed with named owners; all three README:192-196 invocations parse for real through build_parser().parse_args(); dogfood `argus audit .` re-run fresh -> RELEASE_READY exit 0, blocking_findings=0, deep_ratio=60/161, assessed 15/19 scope=application, byte-identical to the claimed baseline, disclosure line correct and leak-free; full suite run to completion three times (plain, -x --tb=short, -u --no-header) each producing exactly 1297 dots and zero F/E/x/s markers (host redirect truncates pytest's own final summary line on this Windows shell — a host quirk, verified by direct character count, not a test failure) matching the claimed 1297/0/0 exactly; mypy re-run clean on 71 files; bandit re-run 0 Med/0 High; no tag/push found (git tag -l empty), consistent with the claimed NOT ESTABLISHED CI status. New test files (test_invocation_contract.py, test_cli_flag_contract.py, test_secret_suppression_recording.py) read in full: AC6 registry genuinely derived from the live parser, non-vacuity (-39) and both-direction positive control (-40) are real, documented-invocation extractor correctly scoped to README/action.yml/workflows only (10.2's D2 lesson honored), redaction in the suppression-recording tests confirmed absolute (secret, operator pattern, host path all absent from the serialized record). secret_suppression.py's reordering and secret_scan.py's new rule-id/Recording fold stay inside the existing pure/impure boundary with zero new pipeline.py lines; design (a) over (b) justified by a measured fact, not a preference. All 8 ACs independently confirmed against the files. DN-1..DN-12 honored, none re-litigated. No High/Medium findings; no Low finding rose to actionable (the two dev-flagged judgement calls are correct engineering, not defects). Status -> done. Rule on --passes / --skip-pass / --ignore-path / --ignore-pattern: bless with ACs + a CHANGELOG entry, or remove. A threat model is REQUIRED before blessing either --ignore-* flag (they suppress security findings). Bind FR30 + architecture L226 + Story 1.7's LOCKED list to the parser and pin equality with a test. Flags were inert from 084c6a7 until 2026-08-09, so neither outcome is a behavioural break.
  # create-story 2026-08-10: story file written (stories/10-3-invocation-contract-says-what-the-cli-accepts.md); status backlog -> ready-for-dev. THREE MEASUREMENTS THAT CHANGE THE STORY, all re-derived on this tree, none read off a document. (1) THE ENUMERATION IS SIX, NOT FOUR. DF-AUD-APAA-E and the epic AC name --passes/--skip-pass/--ignore-path/--ignore-pattern. Differencing the LIVE parser (cli.build_parser() walked at runtime: 15 actions on `audit` = -h + repo + 13 optional flags, confirming the epic's "13 flags") against the binding contract corpus (E-PRD/prd.md, E-PRD/addendum.md, architecture.md, epics.md, CHANGELOG.md, README.md) finds TWO MORE with ZERO occurrences anywhere in it: --reports (entered in 084c6a7, the SAME 426-file separation seed as the other four, so identical defect class -- and WORSE, a committed workflow depends on it: .github/workflows/argus-student-audit.yml:48; also conditionally INERT, since pipeline.py:848 only renders when --report-dir is set) and --strict (entered in ae5f00c with --coverage-scope, but only --coverage-scope got a CHANGELOG entry; cli.py:31-41 names --strict as the BINDING statement of the FR1 determinism pin). The four hits for the ledger's flags in epics.md are Story 10.3's own AC text -- the audit finding, not a specification. Provenance measured with `git log -S -- argus/cli.py`. (2) --ignore-pattern DEFEATS THE LIVE-KEY SAFEGUARD, and this decides the story. secret_suppression.py:1-9 states the designed layering ("live production key signatures OVERRIDE folder glob exemptions"), but evaluate_suppression runs the CLI-supplied ignore_patterns arm at step 3, ABOVE the step-4 safeguard, matching by bare substring (`pat in snippet`, :136). Executed on this host: --ignore-path 'argus/**' does NOT suppress AKIAABCDEFGHIJKLMNOP (safeguard holds, (False,None)); --ignore-pattern 'AKIA' DOES; and --ignore-pattern 'A' -- ONE CHARACTER -- suppresses every live AWS key, GitHub PAT, Slack token and BEGIN RSA PRIVATE KEY block in the repo. AND NOTHING IS RECORDED: secret_scan.py:426 binds the reason to `_reason` and `continue`s, so the operator's INPUTS are persisted (to_provenance_payload) while the EFFECT leaves no trace. The epic treats the two flags as a pair; measurement separates them. (3) THE REVERSE DIRECTION EXISTS AND THE LEDGER MISSED IT. README.md:195 documents `argus --budget 500 --materiality critical`; executed, it is `argus: error: argument command: invalid choice: '500'` / SystemExit 2 -- no `audit` sub-command and `--materiality` is not a flag this parser has ever accepted. The first command a new user copies fails. (README:178-190, the slash commands, stays Story 12.7's -- two adjacent blocks, two owners.) SHAPE: 8 ACs. AC6 (a parser-vs-contract equality guard) is LOAD-BEARING, not AC5 (the amendments) -- reusing 10.1/10.2's registry + closure + both-direction positive control + non-vacuity idiom, but DERIVED from the live parser rather than heuristic, because unlike a prose corpus the parser is exactly enumerable. Both directions asserted, plus defaults and shape, plus every `argus audit ...` line committed in README/action.yml/workflows must parse. DISPOSITIONS LOCKED (DN-3..DN-7): --passes/--skip-pass/--reports/--strict/--ignore-path BLESSED with per-flag behavioural ACs + a CHANGELOG entry in 10.2's documented-not-new register; --ignore-pattern BLESSED ONLY IF AC4 lands (layering fixed so it sits BELOW the safeguard, + an operator-attributable suppression recorded and disclosed, redacted, in the --coverage-scope permitted-disclosed-never-lowers-a-bar register), ELSE REMOVED FROM THE PARSER -- with AuditRequest.ignore_patterns RETAINED defaulted-empty, because prd.md:393's ADDITIVE-ONLY schema policy binds to_provenance_payload's persisted shape and forbids deleting a field; the retained seam is then recorded BY NAME for Story 10.5's no-reachable-call-site sweep rather than left to be discovered (the FR23/DF-6-7-A lesson, one story early). OTHER RULINGS: DN-8 -- the ONE measured default divergence (cli.py:196 coverage_scope='application' vs models.py:125 'repository', each announced in its own place) is ruled DELIBERATE, documented and pinned both ways; NEITHER default is changed, because both are shipped, announced surfaces and changing one is behavioural, not a specification correction. DN-11 -- argparse's usage error exits 2, which IS the BLOCKED code (architecture.md:306-307), so a CI step branching on 2 reads a typo in its own workflow as a blocking finding; FILED (AC8.3), NOT fixed -- an exit-code change hits action.yml, both workflows and every integrator. CITATIONS RE-MEASURED: the epic's "architecture L226" has DRIFTED to :303-304 (L226 is now inside a Story-7.1 risk-closure note); FR30 is prd.md:541; Story 1.7's LOCKED list is amended APPEND-ONLY (it is signed/done, S3.4); and cli.py:26-56's own "LOCKED CLI contract" block documents 7 of 13 flags -- commit 230bf5c repaired the --commit paragraph in that very block and left the omission. FENCES HELD: pipeline.py byte-unchanged (NFR-M1 1331/1200, Story 12.1) -- every AC is reachable without it; --help prose + the cli.py:337/:368-372 ValueError split are 12.8's; README:178-190 + pyproject aliases are 12.7's; grammar messaging 10.4's; publishing 12.9's; the precision gate Epic 13's. BASELINE RE-VERIFIED, NOT TRUSTED: 1272 collected and ALL PASSING under ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1, mypy clean on 71 source files -- both match the figures handed in. Evidence status for this story: NOT ESTABLISHED by CI (run ids are sha-scoped; 31341363300 covers 00c8d1b only, and 10.1's + 10.2's deltas are still uncommitted in this tree), which is 10.1's rule applied to story creation itself.
  # dev-story 2026-08-10: IMPLEMENTED, status ready-for-dev -> in-progress -> review. All 8 ACs met. T1 RE-MEASURED EVERY FIGURE BEFORE ANY EDIT and the tree agreed with the story on all of it: 15 actions / 13 optional flags with every default and shape matching; SIX unspecified flags confirmed (--strict 0 hits, --reports 0, and the epics.md hits for the ledger's four are this story's own AC text); --report-dir thin-but-present at README:134 + action.yml:80, so its exclusion from the six is reasoned; the §A.2 probe reproduced VERBATIM; README:195 executed to SystemExit 2. TWO §B LINE NUMBERS HAD DRIFTED and are recorded rather than transcribed: architecture's exit-code wire contract is :305-306 (not :306-307) and secret_scan's evaluate_suppression call site is :422-430 (not :426-434). RED-FIRST DEMONSTRATED FOR ALL THREE KEYSTONES (trap E.1 / AI-E3-1), captured against the pre-change tree. (a) The equality guard failed with 16 findings across EIGHT flags -- §A.1's six on the contract-corpus half, plus --report-dir and --coverage-scope on the in-code half ("absent from cli.py's own LOCKED CLI contract docstring block"), which is §B's measured 7-of-13 under-count surfacing MECHANICALLY rather than by hand. -35/-36/-37/-37b/-39/-40 were green from the first run BY DESIGN: the registry was authored to match the live parser, so the redness sits exactly where the defect was -- in the documents. (b) The live-key assertions failed on 11 of 24 (LIVE_KEY_PATTERNS x hostile-pattern) combinations, including --ignore-pattern 'A' and '*' against ALL FOUR key classes; --ignore-path escaped 0 of 4, which is the measured basis on which DN-5 and DN-6 split the two flags. (c) The documented-invocation test failed on README:195 -- and it caught it only because the extractor matches ANY console-script line in an executable block, not just `argus audit ...`; scoped to `argus audit` the one broken invocation in the repo would have escaped the guard written to catch it. DN-6's CONDITION WAS MET, SO --ignore-pattern WAS BLESSED, NOT REMOVED: the Live-Key Safeguard now sits ABOVE both operator arms (inline annotation deliberately keeps top precedence -- it is reviewable in-diff, an argv flag is not), an operator-caused suppression is recorded as a non-blocking redacted `operator_suppressed_secret:<reason>` Recording and DISCLOSED on stderr on EVERY run including when the answer is none, and attribution is CONSERVATIVE (built-in DEFAULT_TEST_PATH_PATTERNS tested first, so a redundant --ignore-path is not credited with a suppression it did not cause -- over-attributing is as dishonest as recording none). Consequence of the branch not taken, recorded rather than left implicit: AuditRequest.ignore_patterns untouched, models.py BYTE-UNCHANGED, AUDIT_REQUEST_SCHEMA_VERSION not bumped, and NO unreachable field to hand to Story 10.5's sweep. AC4.4 DESIGN (a) CHOSEN BECAUSE (b) IS UNREACHABLE UNDER FENCE D, a fact about the code not a preference: a new DetectorResult field must be read by whoever folds the result, and the only folder is pipeline.py:539, byte-fenced to 12.1 at 1331/1200 -- design (a) needs ZERO lines there because findings.extend(secret_result.findings) already carries it. The reason token travels IN the rule_id because Recording is frozen extra="forbid" with no free-text slot and widening it would be a schema change this story has no licence to make. ZERO VERDICT DRIFT PROVEN THREE WAYS: depth_supported=None makes the record ineligible by construction (pinned by SECRET-001-22), the blocking count is asserted equal with and without the flag, and generator.py:556 filters the security report to (secret_scan, hardcoded_secret) so no rendered byte moves -- and above all the record fires ONLY on an operator-supplied rule, which the dogfood passes none of. SIX CONTRACT SITES AMENDED, each dated/attributed/struck-not-deleted: FR30 and architecture §A now name argus/cli.py::build_parser as the SOURCE OF TRUTH and commit to CATEGORIES rather than a hand-typed list (trap E.5 / the FR7 source_languages precedent); cli.py's LOCKED block grew from 7 arguments to all 14 with defaults and owning stories, finishing what 230bf5c started; Story 1.7 amended APPEND-ONLY under its signed AC3; README:192-196 replaced with three invocations that actually run; CHANGELOG got three sections in 10.2's documented-not-new register. Threat model written into architecture §G answering all four AC3 questions (the key one: suppression is not an access-control question but an EVIDENCE question, because there is only one principal -- so the answer is recording, not permission); guard registered in §Enforcement. A PRE-EXISTING CLOSURE GUARD FIRED AND WAS RIGHT TO: DOCS-001-16 refused the three new CHANGELOG sections until registered in _NOTE_SECTIONS -- registered deliberately with a comment saying what each claims; second piece of evidence in this story that the registry-plus-closure idiom works. DF-AUD-APAA-E CLOSED APPEND-ONLY (numstat -0) with all THREE of its own errors named -- four-vs-six, the safeguard bypass with the discarded reason token, and the reverse direction it never looked for. THREE DEFERRALS FILED WITH OWNERS, none with a bare target_story: NONE: DF-10-3-A (argparse exit 2 vs BLOCKED, Engineering Lead -> 12-9), DF-10-3-B (built-in suppressions undisclosed), DF-10-3-C (--ignore-pattern's bare-substring matching, AC3.4's accepted residual). FENCES HELD: pipeline.py and models.py byte-unchanged by git diff --quiet; epics.md sha IDENTICAL to the pre-edit snapshot (its stale "architecture L226" recorded here, not corrected there -- DN-10); audit-ci.yml, action.yml, pyproject.toml, README:178-190 untouched; test_sequential_portability.py's model inventory (trap E.4) neither weakened nor duplicated -- it pins the MODEL, the new guard pins the PARSER, which is the exact gap §A.1 survived two audits through. FOR STORY 12.8: the cli.py except-ValueError arms are STRUCTURALLY UNCHANGED but have MOVED DOWN ~60 lines behind the docstring amendment -- re-locate by anchor, not by :337 / :368-372. GATES, ALL LOCAL (Windows / CPython 3.11.15 vs a CI matrix of ubuntu x 3.10/3.11/3.12): 1297 passed / 0 failed / 0 skipped, baseline 1272 -> +25 accounted for EXACTLY (9 + 8 + 8) with no test removed, skipped, weakened or xfailed; mypy clean on 71 source files; bandit 0 Medium / 0 High; coverage 95.16%; sha256 round-trip clean over all 11 documents the guards read (0 mutated); dogfood `argus audit .` -> RELEASE_READY, exit 0, blocking_findings=0, deep_ratio=60/161, assessed 15/19 scope=application -- IDENTICAL to the recorded baseline. Write set verified with git status --porcelain AND git diff --stat AND an mtime partition that cuts cleanly at this session's 19:01 start, with nothing outside the permitted table. EVIDENCE STATUS: NOT ESTABLISHED by CI -- run 31341363300 is sha-scoped to 00c8d1b and cannot evidence a tree carrying 10.1's, 10.2's and this story's uncommitted deltas; the six commits behind 00c8d1b are ALL host-portability defects invisible on this Windows host, which is exactly why local is necessary and not sufficient. Nothing pushed, tagged or workflow_dispatched to manufacture a citation (10.1's DN-7).
  10-4-a-grammar-that-fails-to-load-names-why: done  # code-review 2026-08-11 iteration 1: PASS, review -> done. Provenance chain (the centre of this review) verified by git forensics, not trusted: a9cc933 genuinely contains the 10.1-10.4 deltas (git show --stat), all three regenerated dogfood artifacts cite `Commit descriptor: a9cc93398c1b1937d981e8f22108aa72cad157ba`, which was truthfully HEAD at generation time and is an ancestor of current HEAD 93adc94 (which adds only the artifacts themselves); nothing pushed, tagged or dispatched (git tag -l empty, ahead-of-origin 6), so CI evidence honestly stays NOT ESTABLISHED. Independently re-run, not read off the story: full suite 1324/1324 (0 fail/error/skip, ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1); dogfood subset (test_dogfood_plan.py + test_dogfood_proof.py + test_dogfood_module_split.py) 48/48; mypy clean 72 files; bandit 0 High/0 Medium (19 Low); coverage 95.51% (>=80% gate); `argus audit .` -> verdict=RELEASE_READY deep_ratio=61/163 blocking_findings=0 assessed_deep_ratio=61/77 scope=application held_out=86 exit 0, byte-identical to the story's own figures (DN-9/AC4.4 held). AC5's ast-closure guard proven to bite LIVE: a scratch fifth unregistered except arm was injected into the real `_get_parser_for_lang` in argus/index/ast_index.py, and `test_loader_has_no_unnamed_swallowed_or_redundant_arm` turned RED exactly as claimed (unregistered GrammarFailure member rejected); the file was restored and verified byte-identical via `git diff --quiet`. AC6 verified against the actual `_render_grammar_remedy`: cause 1 pip-installs only the missing package, cause 2 says plainly it is an Argus defect with no install command, cause 3 says reinstall/rebuild not fresh-install, cause 4 names the core package only; mixed case renders one bullet per class. deferred-work.md's +585/-0 confirmed genuinely append-only (diffed programmatically against its immediate parent commit: zero removed or changed lines; DF-AUD-APAA-F's original entry stays byte-intact, the two-causes-to-four correction and DF-10-4-A/B/C/D are pure insertions, matching 10.1/10.2's established closure-note convention). Fences verified held: git diff --quiet confirms pipeline.py (1331 lines, NFR-M1), models.py, cache/key.py, cli.py, pyproject.toml, epics.md, README.md, CHANGELOG.md, action.yml, audit-ci.yml, E-PRD/** untouched by either the 10.4 commit or the regeneration commit; test_ast_index.py -73/-74 and test_multilanguage_audit.py INTAKE-003-07/-08 confirmed unmodified by 10.4's own commits and green. No new field on AstIndexEntry; schema_version still "2"; CACHE_KEY_SCHEMA_VERSION still "3" (DN-5 held); grammar_status.py confirmed pure (AR8 held). ONE Low/deferred finding filed, non-blocking: DF-10-4-E — `_render_grammar_remedy` (generator.py:295-332) has no exhaustiveness guard mirroring the loader's AC5.2 closure for a hypothetical future fifth GrammarFailure member; not a defect today, filed unscheduled with Engineering Lead as owner for whichever future story next extends the enum. Findings written to the story's Review Findings section. (prior: dev-story FIX ROUND 2026-08-10: in-progress -> review. HALT RESOLVED by operator (XAgent007) decision, recorded in the story's Operator-decisions section. DECISION 1: commit 10.1-10.4's deltas so HEAD genuinely contains them, THEN regenerate the Epic-7 dogfood artifacts via their OWN renderers, THEN re-run - git authorised COMMIT ONLY (no push, no tag, no workflow_dispatch; 10.1's DN-7 still binds and CI stays NOT ESTABLISHED). DECISION 2: file the systemic sub-finding round 1 left to the operator. EXECUTED: DF-10-4-D filed APPEND-ONLY (deferred-work.md now +585/-0), owner Engineering Lead, target_story 12-1-pipeline-stops-breaching-its-own-limit, developer-experience, severity yellow - it records a mechanism STRICTLY STRONGER than DF-8-5-B's: enumerate_minions_source_files reads `git ls-files`, which reports the INDEX, so the five guards break on `git add` ALONE, mid-implementation, at the exact moment AI-E8-1 REQUIRES the git add - two repo requirements in direct mechanical conflict that no story can satisfy both of. FILED ONLY, deliberately NOT fixed (the coupling lives in argus/dogfood/, outside this story's fence, and changing what the dogfood enumerates is verdict-adjacent, DN-9's fence to Epic 11/12); 12.1 already absorbs DF-8-5-B and now carries a bootstrap ordering hazard - it owns the remedy AND is the story most certain to trip the defect before the remedy exists. Six LOCAL commits on master, each in the repo's message style with the Co-Authored-By trailer: e59c2e1 (the untracked correct-course change signal - AI-E8-1 in documentary form), ce2cc7e (10.1), ae5c6a3 (10.2), 58821c3 (10.3), a9cc933 (10.4 - THE PROVENANCE SHA the three artifacts now cite), and the regeneration commit. Four stories were implemented against ONE uncommitted tree, so eight co-edited files are carried by the LAST story that touched them - stated in the commit messages rather than hidden, and the consequence (only the SET is complete; an intermediate commit is not expected to pass its own suite) is recorded, not papered over. DEV-6, the one call the operator delegated - MASTER over a branch, decided on a MEASUREMENT not a preference: the artifact being replaced already cited 7be90f77, which is NOT an ancestor of this history (it survives only on release/epics-8-9 and on a branch literally named backup/pre-rebase-fix), so the orphaned-provenance failure the decision exists to prevent has already happened here once, by rebase; ordinary branch-first hygiene is a real standard but is outbid by the explicit project constraint that a cited provenance sha must stay valid, which is the section-7 conflict rule, and the tradeoff (no pre-merge review) is recorded with its mitigations rather than waved away. Three artifacts (minions-dogfood-partition-plan.md, -budget-plan.md, -proof.md) regenerated through render_partition_plan_markdown / render_budget_plan_markdown / render_proof_markdown and ASSERTED EQUAL to the renderer's return value after writing, with the pre-write diff printed and inspected - every changed line is a DERIVED FIGURE (population 71->72, LOC 18418->19783, $X 443->450, two partition_ids, the bundle hash, critical set 50->51), never a claim, a narration or an assertion; plan.commit_descriptor == proof.commit_descriptor == HEAD == a9cc933 asserted in the script, so each artifact cites a sha that CONTAINS the deltas it describes. AI-E3-1 checked MECHANICALLY, not promised: `git diff HEAD -- tests/` empty after regeneration and the suite count unchanged at 1324, so no test was edited to match broken output; none removed, skipped, weakened or xfailed. minions-dogfood-proof-story-7-2-superseded.md (Story 8.5's preserved historical record) and _bmad-output/audit-reports/* deliberately NOT regenerated. Gates re-run, all LOCAL: 1324 collected / 1324 pass / 0 fail / 0 error / 0 skip (counts taken from --junit-xml because this Windows shell truncates pytest's summary line - the host quirk 10.3's review already measured), mypy clean on 72 files, bandit 0 High / 0 Medium (19 Low), coverage 95.51%. DOGFOOD VERDICT BYTE-IDENTICAL: RELEASE_READY, exit 0, blocking_findings=0, scope=application - and round 1's unexplained divergence is RESOLVED: 15/19 is the reduced form of 60/76, so the story's recorded 60/161 baseline DOES reproduce once the tree is committed, off by exactly +1 assessed-and-deep (argus/shared/grammar_status.py) and +1 held-out (tests/test_grammar_diagnosis.py) - this story's own two files; the round-1 31/82 was an artefact of auditing a drifted, half-untracked tree. NO baseline figure was edited anywhere (AC4.4 stop-and-report). AC7.6's write set widened by exactly the three artifacts, recorded in the Dev Agent Record rather than by editing the AC, because a dev may not amend the criterion that constrains them. All 7 ACs satisfied; fences held (pipeline.py, models.py, cli.py, cache/key.py, pyproject.toml, epics.md, E-PRD/**, README.md, CHANGELOG.md, action.yml, audit-ci.yml all byte-unchanged by `git diff --quiet HEAD`). CI evidence remains NOT ESTABLISHED - committing is not evidence; HEAD is a sha no CI run has seen, and run 31341363300 is still scoped to 00c8d1b. The command a human runs: push and cite the audit-ci.yml run WITH THE SHA IT COVERS. Status -> review.  # PRIOR ROUND: dev-story 2026-08-10: ready-for-dev -> in-progress, and HELD there — NOT advanced to review. Implementation is COMPLETE (T1-T8, all 7 ACs satisfied) and 1319 of 1324 tests pass; the story is halted on ONE red test whose only remedy is fenced. Delivered: argus/shared/grammar_status.py (NEW, pure token contract; mypy 71->72 exactly as AC3.5 predicted), a four-arm _get_parser_for_lang returning _ParserLoad(parser, failure) so the emitted token is the ARM's, _render_readability_warning routed through the shared classifier with a per-class remedy (the all-or-nothing trigger pinned UNCHANGED, 12.5's fence), and tests/test_grammar_diagnosis.py (NEW, 27 tests) whose load-bearing half is an `ast` CLOSURE over the loader's own control flow — no bare/lone-pass/redundant-tuple/signal-catching handler, and every exit must return a REGISTERED GrammarFailure, so a fifth arm turns it red until registered AND driven. Every §A measurement re-derived by EXECUTION before any edit and all held: anchor at :350 (ledger's :266 and 10.2's :294 both stale — three coordinates in two days), all four causes emitting grammar_missing_go, generator.py:322-336 never executed, KeyboardInterrupt propagating, baseline 1297/71. RED captured FIRST with ast_index.py and generator.py sha256-verified byte-identical (15 of 27 failed, incl. the callout going SILENT on cause 2 and the mixed case counting 2 of 4 files). DF-AUD-APAA-F closed APPEND-ONLY (+540/-0), correcting its enumeration from two causes to four; DF-10-4-A (partial-failure blind spot -> 12-5), DF-10-4-B (DetectorResult.degraded has ZERO production readers -> 10-5, Governance Owner) and DF-10-4-C (exception class -> 12-8) filed with real target_storys. Gates LOCAL: mypy clean 72 files, bandit 0 High/0 Med, coverage 95.16->95.51%, dogfood verdict UNCHANGED by this delta (proved by re-running argus audit . against a reconstructed pre-story tree — identical line; the story's recorded 60/161 figure does NOT reproduce on this tree WITH OR WITHOUT the delta, reported per AC4.4 as stop-and-report, not updated). CI evidence NOT ESTABLISHED; nothing pushed, tagged or dispatched (10.1 DN-7). HALT: FIVE test_dogfood_* guards are RED (1319/1324 pass), each saying a committed AUTO-GENERATED Epic-7 artifact no longer matches what the generator derives: minions-dogfood-partition-plan.md, -budget-plan.md and -proof.md. TWO root causes, BOTH MANDATED BY THIS STORY: (a) the added LOC tips an NFR-SC1 bin-packing boundary (unit 40 files/14100 LOC -> 39/14793), changing two of three partition_ids; (b) the REQUIRED `git add` of the new module (E.5 / AI-E8-1) makes it tracked, and enumerate_minions_source_files reads `git ls-files` (which includes the INDEX), so the planned/audited population moves 71 -> 72, moving total LOC, the budget $X (450/843) and the critical-set size (51). Proved: reverse-applying my edits reproduced the committed ids EXACTLY, then I restored byte-identically (sha256 round-trip); and the suite had ONE failure before the git add and five after. NOT fixed because AC7.6 fences all four artifacts by name ('every Epic 1-9 artifact') AND regenerating them now would stamp HEAD 00c8d1b — a sha containing NONE of 10.1-10.4's uncommitted deltas — as their provenance, manufacturing the false-citation class Epic 10 exists to close. Four named options in the story's HALT block; recommended: commit 10.1-10.4's deltas, THEN regenerate via the artifacts' own renderers (partition_plan.render_partition_plan_markdown / render_budget_plan_markdown, proof_render.render_proof_markdown), then re-run test_dogfood_plan.py + test_dogfood_proof.py. NOTE FOR THE ORCHESTRATOR: these five guards fire on ANY story that adds a module to argus/ — no story before this one had to — which may itself deserve a ledger entry; deliberately NOT filed on dev authority. DF-AUD-APAA-F · 🟢 · Engineering Lead. Split ast_index.py:266 `except (ImportError, Exception)` so a MISSING grammar and a BROKEN one report distinct reason tokens; the current shape gives an operator a remedy that cannot work. Only bare except:pass in argus/ — AR10 / Story 4.3 forbid it. Degradation is already correct (ast_eligible=False, never a false deep claim), so this is diagnosis quality, not correctness. Independent — may land at any point.)
  # create-story 2026-08-10: story file written (stories/10-4-a-grammar-that-fails-to-load-names-why.md); status backlog -> ready-for-dev. FOUR MEASUREMENTS THAT CHANGE THE STORY, all re-derived on this tree by EXECUTION, none read off a document. (1) THE LEDGER SAYS TWO CAUSES; MEASUREMENT FINDS FOUR, AND ALL FOUR EMIT THE SAME TOKEN. Driving build_ast_index over a .go fixture with only `ast_index.importlib.import_module` monkeypatched: (a) package not importable, (b) package imports but `getattr(mod, entry_point)` returns None — nothing raises, (c) entry point present but parser construction raises, (d) `from tree_sitter import Language, Parser` itself raises — ALL FOUR record `grammar_missing_go`, and in THREE of the four the implied remedy is wrong. Cause (b) is the one the ledger's own stated repair does NOT catch (splitting the except does nothing where nothing raises) — Story 10.2 measured this independently and handed it forward BY NAME after fixing only its two instances (TypeScript/PHP) with the entry-point map. Cause (d) appears in no prior document and is the maximally wrong message: "install tree-sitter-go" when the CORE runtime is broken and every language is down. (2) 🚩 THE FINDING THAT FORCES AC6: the ONLY operator-facing consumer of the token — `reports/generator.py:322-336`, the CAUTION callout naming the pip command — IS NEVER EXECUTED BY ANY TEST. `pytest --cov=argus.reports.generator` across EVERY test file that imports the generator reports `Missing … 322-336`; line 318-320 (`if eligible: return []`) is covered, i.e. every test returns before reaching the branch. So renaming the token would SILENTLY DISABLE the one message an operator ever sees and NO test in the repo would turn red — the story eating its own tail. Worse, that callout fires ONLY when ZERO files parsed, so in the polyglot repository this story's own persona owns (Python parses, Go does not) it never fires at all. AND its language recovery is string arithmetic (`reason[len("grammar_missing_"):]`), which a naive widening would turn into `pip install tree-sitter-entrypoint_missing_go`. (3) `DetectorResult.degraded` HAS ZERO PRODUCTION READERS — three detectors build it, base.py declares it, 21 tests assert on it, and no code in argus/ reads it, so vacuous_test.py:400's careful propagation of parse_failure_reason is written into a structure nothing consumes. Same class as DF-6-7-A; FILED for 10.5's reverse sweep with an owner, NOT fixed. (4) COORDINATE DRIFT, THIRD VALUE IN TWO DAYS: the blanket catch is at ast_index.py:350 on this tree — the ledger says :266, 10.2's own Dev Agent Record says :294. Locate by anchor. SHAPE: 7 ACs. AC5 (a guard that parses ast_index.py with the stdlib `ast` module, walks `_get_parser_for_lang` and asserts no bare except, no lone-`pass` handler, no redundant exception tuple where one member subclasses another, no BaseException/KeyboardInterrupt/SystemExit arm, and every failure path accounted for by a registered token) is LOAD-BEARING, not AC1 — reusing 10.1/10.2/10.3's registry + closure + both-direction positive control + non-vacuity idiom, but closing over the CODE rather than a glob, because unlike a prose corpus the failure modes are exactly enumerable from the function's own control flow. TOKENS LOCKED (DN-1): grammar_missing_<lang> UNCHANGED · grammar_entrypoint_missing_<lang> · grammar_load_failed_<lang> · tree_sitter_runtime_missing (no lang suffix). OTHER LOCKED RULINGS: DN-3 one PURE definition in a new argus/shared/grammar_status.py imported by both index and reports (source_languages.py precedent; reports importing the impure index/ for a constant inverts AR8) — mypy goes 71->72 files, recorded so it is not read as drift · DN-4 classify by ARM POSITION, never by exception message and never by exception type (measured: the same cause surfaces as ValueError / TypeError / OSError, and the ABI text `Incompatible Language version %u. Must be between %u and %u` is a C format string extracted from the shipped _binding.pyd — recorded as a string NOT to match on) · DN-5 NO new field on AstIndexEntry, schema_version stays "2", CACHE_KEY_SCHEMA_VERSION stays "3" (verified: cache/key.py folds grammar_versions, never entries), and the exception detail is DEFERRED to 12.8 — a persisted free-text slot's only natural filler is str(exc), i.e. a host path (NFR-S1), argus/ has no logging facility, and manufacturing an unread seam would be this repo's THIRD instance of the class filed in measurement (3) · DN-6 parse_failed stays False for all four causes (the flag means a parse was ATTEMPTED and failed; pipeline.py:391/426 branches on it and flipping it would move the coverage denominator) · DN-7 the report AC is IN scope (do not go silent) but the all-or-nothing trigger is OUT (do not add a surface) · DN-9 ZERO verdict/gate/version-comparison change. SEVERITY HELD AT 🟢 DELIBERATELY: pipeline.py:391/:426, audit/grounding.py:88 and vacuous_test.py:228/:399 branch on parse_failed/ast_eligible BOOLEANS ONLY, never on the token, so the split cannot move a grade, a deep ratio or a verdict — the harm is in the EVIDENCE (a record naming a cause that is not the cause) and in the operator's reading of an ordinary-looking INSUFFICIENT_COVERAGE, NOT a false green in the verdict. Overselling it as a correctness defect would manufacture the exact class 10.2/10.5 exist to close. RED-first mandated on BOTH new guards (AI-E3-1), and mandatory for the report half specifically because those lines have never executed. FENCES STATED PRECISELY WHERE THEY ARE ADJACENT: 11.4 — its danger is the SILENT case (on tree-sitter 0.26.0 the grammar LOADS and corroboration merely stops firing, so this story's loader never sees it, which is exactly why 11.4 needs a RUNTIME version assertion and 10.4 cannot substitute); where they touch, an out-of-bound core that makes Language() RAISE now records grammar_load_failed_<lang>, a signal 11.4 may key on — 10.4 records, 11.4 decides what a record may vouch for. 12.5 — owns the packaging decision and the NEW per-file point-of-downgrade output; 10.4's only report obligation is that the ALREADY-EXISTING whole-report callout does not go silent or misdirect under the new tokens, and the measured partial-failure blind spot is FILED for 12.5, not fixed. ALSO FENCED: 12.8 (operator cause-and-fix messages, --help prose), 12.1 (pipeline.py byte-unchanged, NFR-M1 1331/1200), 12.9 (publishing), Epic 13 (precision gate); no contract document is edited — architecture.md §Error/Degradation + §Enforcement is the ONLY document in the write set besides deferred-work.md. BASELINE RE-VERIFIED, NOT TRUSTED: 1297 collected under ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1 and mypy clean on 71 source files — both match the figures handed in; coverage 95.16% carried from 10.3 for the dev to re-measure. Evidence status for this story: NOT ESTABLISHED by CI (run 31341363300 is sha-scoped to 00c8d1b and 10.1's + 10.2's + 10.3's deltas are still uncommitted in this tree), which is 10.1's rule applied to story creation itself.
  10-5-a-v1-commitment-is-delivered-or-explicitly-not-v1: done  # code-review 2026-08-11 iteration 1 (Sonnet): PASS, review -> done. Independently re-executed, not read off the story: mypy clean 72, bandit clean, full suite 1336 collected / 83 files, exit 0 (0 fail/error), coverage 95.51% unmoved, `argus audit .` byte-identical to the cited figures (verdict=RELEASE_READY deep_ratio=61/164 blocking_findings=0 assessed_deep_ratio=61/77). New guard (12 tests, DOCS-001-30..-41) green standalone; the four sibling guards + 49 dogfood/staleness tests green standalone. git diff --numstat matches claims exactly (prd.md +28/-6, architecture.md +4/-0); deferred-work.md +240/-0 append-only verified programmatically (after.startswith(before)); argus/** byte-unchanged (git status --porcelain and git diff --quiet both clean); write-set fence held exactly as declared; PRD frontmatter amendments YAML re-parses valid. One Low/dismissed note: the new test file (1308 lines) exceeds the story Dev Notes' suggested 863-1100 band, but architecture.md scopes NFR-M1's 1200-line cap to argus/** modules, not tests/, so this is not a binding-constraint violation — not actionable. (prev: 10-5 dev-story 2026-08-11: in-progress -> review. THE LAST EPIC-10 STORY. Record-only, ZERO argus/** changes (verified: `git status --porcelain -- argus/` EMPTY, `git diff --cached --name-only -- argus/` 0 files) - so DF-10-4-D's five dogfood staleness guards could not fire, and did not (49/49 green). AC1: standards_refs[] + CWE-on-security DECIDED V2 (DN-1) and applied struck-not-deleted at ALL THREE measured PRD sites, not the one site every planning document named - prd.md:187 (V1 Core), prd.md:309 (Compliance & Regulatory 'Standards anchoring', a SECOND independently-binding V1 site carrying a ^CWE-\d+$ FORMAT commitment, invisible to every planning document since 2026-08-03) and prd.md:214, where the reclassification is recorded as a MERGE AT THE DESTINATION so it cannot read as 'it was always V2'. Frontmatter amendments entry added (YAML re-parsed). AC2: Journey 4 gains the consequence as a known trade naming FR11 as the one V1 security-category finding producer; the >=80% gate is untouched and the note is written so it cannot read as clearing it. AC3 forward sweep: 20 V1 commitment atoms derived BY CLAIM SHAPE ACROSS THE WHOLE PRD (not by heading - that is the whole argument, and :309 is its proof), all disposed 11 fr-backed / 3 constraint / 2 nfr-backed / 2 reclassified-v2 / 1 specified-not-built / 1 delivered-differently. AC4 reverse sweep: FR1-37 all disposed 27 wired / 4 library-seam / 2 delivered-differently / 4 not-built, every 'wired' PROVEN against a static ast import closure rather than asserted. FR23 decided BY NAME as library-seam (DN-3) with BOTH halves of the reason (pipeline.py 1331/1200 fenced to 12.1; unattended-CI default path has no human to answer a default-STOP gate) AND its cut-order cost stated rather than hidden. FR24/FR26/FR29 amended and FILED FOR THE FIRST TIME IN FIVE WEEKS (DF-10-5-A/-B/-C); FR29 is the sharpest - its text says 'an operator can export an evidence bundle' and no operator can. FR27 got the trap right: delivered-differently by determinism, NOT a seam - calling it one would have manufactured a false accusation. architecture.md's 'No FR is unsupported' caveated: it certifies module PLACEMENT, not reachability (53/72 modules reachable). AC5 (LOAD-BEARING, DN-2): new guard tests/test_v1_commitment_closure.py, 12 tests DOCS-001-30..-41, two closures both directions each, wired AND library-seam both mechanically refutable, no `import argus` (DN-6), stdlib only, no new dependency, registered in architecture.md Enforcement with the rule. RED-FIRST DISCHARGED TWICE: once against the genuinely unamended documents (prd.md:187 AND :309 unclassified + FR23's wired claim refuted), once as a LIVE BITE on the finished tree - and the live bite found a real AI-E3-1 bug in this story's OWN guard (a half-struck commitment passed `'~~' in text`), now fixed by struck_spans() with four synthetic regressions. Both files restored byte-identically, sha256 verified. AC6: DF-6-7-A closed by disposition, DF-10-4-B RE-TARGETED OFF THIS STORY onto 12.4 (no fifth orphan manufactured), DF-AUD-APAA-A/-B un-orphaned off the `done` Epic 7 they had pointed at since 2026-07-04, four new filings, and the closing statement of the class written as one paragraph: filed 4x over five weeks, swept 0x. Ledger append-only +240/-0 proven programmatically. NOTHING OPEN WAS CLOSED: H0's filing stays open (A5 still UNSUPPORTED), DF-7-2-A's adjudication stays open, >=80% gate stays NOT CLEARED - first two pinned MECHANICALLY by -38. MEASURED CORRECTION recorded rather than adopted: the story's brief said H0 is 'OPEN and UNOWNED'; on this tree its OWNERSHIP was closed 2026-08-10b via option (b) and only the EXECUTION is open, so the guard pins the residual that is still true instead of a fact that expired the day before. Gates ALL LOCAL: 1336/1336 pass (0 fail/error/skip/xfail; +12 = exactly the new cases, 83 files), coverage 95.51% UNMOVED (guard executes no argus line), mypy clean 72, bandit 0 High/0 Med/19 Low, `argus audit .` -> RELEASE_READY blocking_findings=0. CI evidence NOT ESTABLISHED per 10.1's rule - no run has ever covered a9cc933/93adc94 or this tree; nothing pushed, tagged or dispatched. Interrupted TWICE by infrastructure faults and resumed; T7 verified not-partially-landed before re-writing, so no duplicate ledger entry. Stray untracked root file `NUL` deleted (837 bytes of captured audit stdout from a `>NUL` bash redirect). Epic 10 now 4 done + 1 review; the epic retrospective runs once this closes. (prev: 10-5 dev-story 2026-08-11: ready-for-dev -> in-progress) (prev: ADDED 2026-08-10b · 🟡 · Governance Owner. PRD §Product Scope L168 commits standards_refs[] + CWE-required-on-security-findings to V1 Core ('day-one additive') while NO FR lists it and ZERO occurrences of standards_ref/cwe/asvs/owasp exist in argus/**; FindingDraft (argus/detectors/base.py:63-87) carries no standards field. The two sections DISAGREE, which is why no story built it — the EXACT INVERSE of DF-AUD-APAA-D, same root cause. DECIDE: add an FR and ship the field, OR amend §Product Scope to V2, dated and reasoned. Leaving them in disagreement is NOT acceptable. Record the consequence for Journey 4 either way (a security finding with no standards ref is weaker compliance evidence). THIRD AC IS THE IMPORTANT ONE: sweep whether any OTHER §Product Scope V1 Core item is missing from FR1-37 — one instance found by accident implies the class was never checked. Recommendation of record: decide it V2. ALSO ABSORBS DF-6-7-A (2026-08-10b triage) as a REVERSE SWEEP: FR1-37 for requirements with NO reachable production call site. Known instance — FR23 (HITL escalation) ships as a LIBRARY SEAM that nothing in pipeline.py or cli.py invokes. IDENTICAL defect class to FR27/NFR-D1, which this proposal made Story 12.3 while leaving FR23 unlisted — an inconsistency the proposal itself introduced, recorded not quietly fixed. FR23 takes a DATED disposition BY NAME: wired, or recorded as a library seam for V1.5 with the call site deferred and a reason.))
  # create-story 2026-08-11: story file written (stories/10-5-a-v1-commitment-is-delivered-or-explicitly-not-v1.md); status backlog -> ready-for-dev. FIVE MEASUREMENTS THAT CHANGE THE STORY, all re-derived on this tree at HEAD 93adc94, none read off a document. (1) THE SITE ENUMERATION IS WRONG A FOURTH TIME. This annotation, the epic AC (epics.md:1932) and the proposal all name ONE site, 'PRD Product Scope L168'. Measured: THREE, and the named one is at :187 not :168 - prd.md:187 (V1 Core), prd.md:309 (Compliance & Regulatory, 'Standards anchoring (phased). V1: ...', a SECOND independent V1 binding that adds a ^CWE-d+$ FORMAT commitment and has been named in no planning document since 2026-08-03), and prd.md:214 (the V2 Growth Features line, which ALREADY CONTAINS the destination, so the move is a recorded MERGE, not a new bullet). A sweep anchored on the 'Product Scope' heading would MISS :309 - which is why the guard closes on CLAIM SHAPE across the whole PRD, not on a section heading. (2) THE REVERSE SWEEP IS NOT ONE HIT, IT IS FIVE, AND FOUR WERE FILED AND NEVER SWEPT. Built the argus/** import graph with stdlib ast and took the closure from argus/cli.py (the only entry point; pyproject.toml:59-62 = 3 aliases -> argus.cli:main): 43 of 72 modules reachable. Unreachable while carrying a binding FR: governance/escalation.py (FR23, DF-6-7-A), governance/decision_record.py (FR24 [Tier B], NEVER FILED), evidence/bundle.py (FR29 'an operator can export an evidence bundle' - there is no CLI subcommand; the only caller is dogfood/proof_run.py; NEVER FILED), store/integrity.py (FR26/NFR-A2 [Tier B], NEVER FILED), plus cache/* (FR27 mechanism, DF-AUD-APAA-A) and audit/* (FR36 -> 12.2). All three new hits are BUILT, TYPED AND TEST-PROVEN (test_hitl_escalation.py, test_store_integrity_lint.py, test_evidence_bundle.py) - so 'it has tests' is NOT evidence of delivery, which is half the point of the story. DF-AUD-APAA-A and -B both carry target_story: epic-7-..., an epic that is DONE, so both targets are ORPHANED and unclosable as written. The class has been filed FOUR times over five weeks (DF-AUD-APAA-A, -B, DF-6-7-A, DF-10-4-B) and swept ZERO times. That, not the standards_refs instance, is what this story is for. (3) THE ARCHITECTURE ALREADY CERTIFIES THE FALSE THING, IN ONE SENTENCE. architecture.md:905 - 'All 33 FRs of the base contract map to a concrete module. No FR is unsupported.' Its FR-cluster table (:884) maps Governance/Integrity FR23-29 to governance/, cache/, evidence/. Every module named exists; FOUR of that row's seven FRs have no production call site. MAPPING TO A MODULE IS NOT DELIVERY. AC4.5 caveats that sentence in ONE paragraph (the table itself stays read-only) and AC5.3 makes a 'wired' disposition MECHANICALLY REFUTABLE by the reachability walk, in both directions - a library-seam disposition over a module that later BECOMES reachable also fails, so 12.1/12.3 turn it red until the disposition is updated. (4) THE CLASS WAS NAMED AND SCORED ON 2026-08-03 AND NEVER ACTED ON. implementation-readiness-report-2026-08-03.md:400: 'the failures are all in the same blind spot: obligations the PRD binds without an FR number. The requirement-ID pass scores 100%; the unnumbered-obligation pass scores 68%.' Its F2 (:351-358) is this story's AC1 verbatim, recommending SHIP because it was 'far cheaper now than after the finding schema is frozen'. It then became AI-E8-9 and was measured NOT DONE twice (epic-9-retro:130: 'grep CWE over the 9.2 story: 0 hits'). The schema is now frozen, content-hashed and shipped, so that premise EXPIRED - which is exactly why the recommendation of record inverted. (5) BASELINE RE-VERIFIED, NOT TRUSTED: 1324 tests collected across 82 files under ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1; mypy clean on 72 source files; pipeline.py 1331 lines (NFR-M1 cap 1200, fenced to 12.1); cli.py 479; grep -rniE 'standards_ref|cwe|asvs|owasp' over argus/**/*.py = 0 occurrences, reproducing the proposal exactly. SHAPE: 7 numbered, mechanically-verifiable ACs. AC5 is the load-bearing one (DN-2): tests/test_v1_commitment_closure.py, ids TC-ArgusAgent-DOCS-001-30.., reusing the 10.1-10.4 guard idiom (registry + closure + both-direction positive control + non-vacuity) with TWO closures that meet in the middle - a claim-shape pattern over the whole PRD (forward) x a STATIC import-reachability walk of argus/** (reverse). Neither closure alone closes the class. RED-first mandated (E.1/AI-E3-1). LOCKED DECISIONS: DN-1 - standards_refs decided V2 at all three sites, struck-not-deleted and dated (the 2026-08-03 cost premise expired; a persisted field widens the NFR-S1/S2 redaction surface; the Journey-4 audience is gated NOT CLEARED by Epic 13 anyway, so shipping it today serves NO reachable user; and the binding FR contract at :457 never listed it, so its absence is already contract-correct). DN-3 - FR23 disposed library-seam BY NAME, with its FR text amended on the FR7/FR30 precedent, and the amendment MUST STATE THE COST IT INCURS: the PRD cut-order marks FR23 non-negotiable core (only FR24 is [Tier B]) and readiness-report:365 already flagged it as stranded. Wiring is impossible inside the fences (every call site lands in pipeline.py at 1331/1200, which is 12.1's and byte-fenced) and a default-STOP gate in unattended CI would deadlock Journeys 3 and 5. Owner XAgent007; target_story NONE-with-a-named-human (the DF-10-4-E form). THIS IS THE ONE DECISION AN OPERATOR MAY WANT TO OVERTURN and it is surfaced as open question 1. DN-6 - the guard must NOT import argus (static ast over source text): lazy imports would defeat a runtime walk (replay_harness.py:87-90), an import makes the result depend on installed extras so CI and this host could disagree, and a test that executes no argus line cannot perturb the 95.51% coverage figure. DN-7 - ZERO argus/** changes, not 'few'. Epic 10 closes the RECORD and ships nothing; it is also what keeps this story clear of DF-10-4-D, which halted 10.4 on five reds the moment it STAGED one new argus/ module (git ls-files argus reports the INDEX). A tests/-only delta cannot move the dogfood population, and AC7.3 verifies that rather than assuming it. FENCES RESTATED AND ASSERTED BY TEST (AC5.5, so the guard defends against its own author): H0 stays OPEN and UNOWNED - it WILL surface in the forward sweep, because PRD L196 commits the Minions consumption-contracts and PRD :222-232 specifies them, but FILING them is H0 and nobody owns it; DF-7-2-A's adjudication stays OPEN (owner named 2026-08-10b != done); the >=80% precision gate stays NOT CLEARED and nothing here clears it. Fenced OUT: pipeline.py's NFR-M1 breach (12.1), --help prose and the cli.py except-ValueError split (12.8), README:178-190 (12.7/11.5), default-install grounding (12.5), wrong-grammar-version false-green (11.4), publishing (12.9), FR34's disclosure (11.1), DF-10-2-A. WRITE SET: E-PRD/prd.md; architecture.md (Requirements-Coverage caveat + Enforcement registration ONLY); deferred-work.md (append-only, +n/-0); tests/test_v1_commitment_closure.py (NEW); this story file; this tracker. epics.md is FENCED, so its stale L168 citation is left as written (3.4 evidence immutability) and recorded as the epic's own coordinate-drift instance - the fifth in three days. CI EVIDENCE: no CI run has ever covered a9cc933 or 93adc94, so 10.1's citation rule reaches the dev as NOT ESTABLISHED unless it cites a run covering its own HEAD with the sha it covers; do not push, tag or workflow_dispatch.
  epic-10-retrospective: done  # retro 2026-08-11 (epic-10-retro-2026-08-11.md): 5/5 stories done, outcome clause MET except its own CI condition. Gate-first sequencing worked and was tested by the epic that built it (10.4 refused to regenerate evidence artifacts at a sha not containing the deltas they describe -> HALT -> operator ruling -> commit-then-regenerate). Delivered inside a "specification" epic: multi-language grounding 8/10 -> 10/10, the Live-Key Safeguard moved above both operator suppression arms, suppressions disclosed, four grammar-failure causes with four remedies. 1230 -> 1336 tests (+106, 0 removed/skipped/weakened), coverage 94.97 -> 95.51%, mypy clean 71 -> 72 files, verdict RELEASE_READY invariant across all five stories. Ledger: 5 closed (DF-AUD-APAA-C/-D/-E/-F + DF-6-7-A), 13 filed ALL with named owners (AI-E9-8 adopted as convention), 3 re-orphaned entries repaired. Epic-9 follow-through 5 done / 3 partial / 2 not-addressed — the best on record; everything converted into a RULE or a STORY moved, both items left as an operator runbook step did not. COST, honestly: NO CI run covers any Epic 10 sha (HEAD 93adc94, 6 ahead of origin/master, git tag -l empty, every figure LOCAL Windows/3.11.15); four stories shared one uncommitted tree so eight documents carry multi-story hunks; commits landed on master with pre-merge review bypassed (reasoned, recorded); pipeline.py breaches NFR-M1 at 1331/1200 with no repo-wide assertion; 9 of the 13 new ledger entries carry a CONDITION, not a trigger. FOUR SIGNIFICANT DISCOVERIES recommended to the operator, NONE applied here — SD-1 Epic 11 depends on a tree no gate has executed; SD-2 three Epic 11 premises are stale (11.5's "5 of 71" is now 72 modules + a [languages] extra; 11.4's tree-sitter flip must be re-measured on 10.2/10.4's rebuilt loader and should EXTEND GrammarFailure; 11.2's polyglot audience is now the advertised one); SD-3 DF-10-4-D fires on any story that STAGES an argus/** module and its remedy is scheduled for 12.1, AFTER Epic 11; SD-4 DF-10-2-A (C/C++/Ruby/Rust ground but extract zero definitions) has no owning story in the epic about publishing nothing untrue. 10 action items, 5 critical-path items — see the retro doc. (was: backlog # ADDED 2026-08-10 (consistency fix, not a scope change). Every epic 1-9 carries this key and the dev-loop orchestrator rolls the epic up through it; epic-10 was registered on 2026-08-09 WITHOUT one, so its retrospective had no tracked slot to complete into. Restored here so Epic 10 closes the way Epics 1-9 did.)
  # ── Epic 11: Release Integrity — nothing unsafe or untrue can be published (delta, Argus repo) ──
  # Covers FR34 (11.1), DF-8-2-B (11.2), DF-9-2-D (11.3), tree-sitter runtime bound (11.4),
  # DF-9-2-A + DF-9-2-B (11.5) · Depends on Epic 10 (ALL FIVE stories, not merely the epic).
  # NO STORY IN THIS EPIC PUBLISHES ANYTHING — the publish is 12.9, by design.
  epic-11: done  # REDEFINED 2026-08-10b by sprint-change-proposal-2026-08-10b.md (was 7 stories, 'The Disclosed Release'); ALL 5 STORIES DONE + retrospective done (epic-11-retro-2026-08-12.md) -> in-progress -> done 2026-08-12 by the bmad-dev-loop orchestrator roll-up. Story closes: 11.1 done 2026-08-11 (review iter-1 pass); 11.2 done 2026-08-12 (iter-2, one Low prose-accuracy fix); 11.3 done 2026-08-12 (iter-2 - iter-1 FAILED on a vacuous run-block guard blind to `run: | # comment`; fix proved 9 blind header spellings); 11.4 done 2026-08-12 (iter-1); 11.5 done 2026-08-12 (iter-1). backlog -> in-progress 2026-08-11 when 11.1 was contexted (create-story). SIGNIFICANT DISCOVERY carried to Epic 12 (retro AI-E11-2, RECOMMENDATION not applied): Story 12.1 CANNOT execute under Epic 11's regime - unit 2 is at 14997/15000 (3 lines slack) and pipeline.py (1331/1200) is in that unit, so 12.1 fires both DF-10-4-D triggers at once in the FIRST story of the epic; the blanket 'no new argus/** file' fence is logically inapplicable to a story whose deliverable IS a new module and must be lifted/replaced before 12.1 runs. Epic 12 ends in Story 12.9, an irreversible PUBLISH, and no CI gate has executed for three consecutive epics.
  11-1-tool-discloses-its-status-with-an-expiry: done  # code-review 11-1 iteration 1 (Sonnet): PASS, status review -> done. Independently re-verified on disk, not read off the story: DECIDING QUESTION adjudicated by a positive git-stash isolation experiment — reverted all nine of this story's touched files to pre-11.1 state, re-ran tests/test_evidence_citation.py, and the identical -22 failure reproduced byte-for-byte from the untracked epic-10-retro-2026-08-11.md alone; stash popped, all files re-hashed content-identical (two hashes differed only by CRLF/LF stash-roundtrip noise, confirmed by normalizing line endings). The red is PROVEN unattributable to this story's delta, and deferring it as DF-11-1-A (named owner, append-only) is judged legitimate — the fix sits outside AC6.5's write-set fence and would close an Epic-10 item from inside Epic 11, mirroring Story 8.1's AC18 carve-out precedent. Independently re-run: pytest tests/ = 1352 collected/1351 passed/1 failed(the attributed one)/0 errors/0 skipped (junit-xml); the 8 sibling guard files green standalone; mypy clean 72; bandit 0H/0M/19L exact match; `argus audit .` RELEASE_READY/blocking_findings=0/exit 0, stdout byte-unchanged. DF-10-4-D fence confirmed (git ls-files -- argus = 72; git status --porcelain -- argus/ = exactly 3 M lines). AC6.3 confirmed by direct re-run of build_full_repo_plan('.'): partition ids unchanged, unit 2 14867 = +74/207. Nothing published (no tags, unpushed, no dispatch). Both closures read in full and confirmed non-vacuous with real positive/negative controls; the ast-based protocol_cleared call-site walk independently re-verified against a raw grep (3 real call sites across 2 test files, 0 production, exactly as the guard's registry names). Both judgement calls (ast call-site walk for the harness guard; CLI invariant keyed on the verdict= line) verified correct by reading argus/cli.py::main directly. Two Low, non-blocking observations recorded in the story's Review Findings for a future SM pass: AC6.6's "full suite green" wording should explicitly carve out DF-11-1-A (as Story 8.1's AC18 did for its carve-outs); AC6.5's byte-unchanged fence table is inherited-dirty for architecture.md/E-PRD/prd.md from Story 10.5's already-reviewed uncommitted work, not caused by 11.1. All ACs met; SOLID/DRY/KISS/YAGNI, security and testability reviewed with no findings. || dev-story 11-1 2026-08-11: ready-for-dev -> in-progress -> review. FR34 shipped: one closed two-member InstrumentStatus vocabulary + one constant + one pure exhaustive renderer in the EXISTING argus/verdict/negative_assurance.py beside DISCLAIMER (DN-1/DN-2 honoured: ZERO new argus/** files; git ls-files -- argus still 72; git status --porcelain -- argus/ shows only the three permitted M lines). DF-10-4-D FENCE HELD AND WAS VERIFIED, NOT ASSUMED: dogfood unit 2 went 14793 -> 14867 = +74 of the 207-line budget (133 spare) and all three partition_ids are byte-unchanged (477ef77d7b65 / 82a3d605e61e / ed6d08f25ce3) AFTER staging the new test; no dogfood artifact regenerated. Two closures, both RED-first and re-demonstrated with the FINAL test code by sha256 round-trip (8 surfaces/sites, every one bit, every one restored byte-identically): an ast walk of generate_reports' own body requiring all four write_text calls to flow through _with_instrument_disclosure (a fifth report goes RED), and a glob-resolved surface registry + an MCP pin that goes RED the day 12.6 lands (no MCP stub built). The expiry is mechanised: the declared status must be not-independently-validated iff no production call site passes protocol_cleared=True. MEASURED DIVERGENCE recorded, not adopted: the story named 2 test-side occurrences in 1 file; the real CALL SITES are 3 across 2 files (a substring scan also false-positived on 2 argus/** docstrings, so the guard is an ast walk) - the sixth wrong hand-count in this project. LOCAL gates (Windows/CPython 3.11.15, NO CI): 1352 tests = 1336 baseline + exactly 16 new, 0 skipped; mypy clean 72 sources; bandit 0 High / 0 Medium (19 Low); coverage 95.77% (gate 80); `argus audit .` RELEASE_READY / blocking_findings=0 / exit 0, stdout byte-unchanged. CI evidence: NOT ESTABLISHED (nothing pushed, tagged or dispatched - 10.1 DN-7 / Epic 11 publishes nothing). ONE PRE-EXISTING RED, NOT CAUSED BY AND NOT CLOSED BY THIS STORY: test_evidence_citation.py::-22 fails because the untracked epic-10-retro-2026-08-11.md is an unregistered status document (that guard's closure working as designed on Epic 10's delta); attribution proven - test_evidence_citation.py is unmodified and the retro file predates this story's first write by ~75 min. Filed as DF-11-1-A, owner XAgent007, ledger append-only verified programmatically (+32/-0). NOT COMMITTED: story task T7 asks for a commit, but committing was not requested by the operator in this run and no AC requires it - the delta is STAGED (git add of the new test + the story file, E.5) and left for the operator/review gate. || create-story 2026-08-11: backlog -> ready-for-dev. THE THREE EPIC-10 RETRO GATES ARE DISCHARGED IN THE STORY FILE, NOT HERE. (1) AI-E10-1: a DATED RISK ACCEPTANCE (2026-08-11, XAgent007) is recorded in story §0.1 — no CI run covers any Epic 10 sha, HEAD 93adc94 is 6 ahead of origin/master, git tag -l empty, and every baseline figure is LOCAL Windows/CPython-3.11.15. (2) AI-E10-2: the operator's DF-10-4-D ruling — NO Epic 11 story may create or stage an argus/** file — is recorded as story §0.2 + DN-2 and CONFIRMED SATISFIABLE: every line of 11.1 lands in files that already exist (argus/verdict/negative_assurance.py, argus/cli.py, argus/reports/generator.py); git ls-files -- argus stays at 72. ⚠️ The SM ALSO measured DF-10-4-D's SECOND, unnamed trigger: dogfood unit 2 (partition 82a3d605e61e) is at 14793/15000 physical LOC, so the story carries a HARD BUDGET OF 207 ADDED LINES inside that unit before the bin-packer re-flows and two partition_id sha256s change — the story fences it (§A.5, AC6.3) and mandates a HALT rather than a regeneration if it tips. (3) AI-E10-3: all three premises re-measured on this tree — `demo-heuristic-only` HELD (8 occurrences, dogfood only); `provisional` DIVERGED (24 occurrences in argus/precision/replay_harness.py, which is the ORIGIN — and is UNSAFE TO IMPORT on a user path because its module-level sys.path/_registry insert is 11.5's wheel defect); the MCP surface DIVERGED (0 occurrences — it does not exist and is Story 12.6's, so 11.1 makes the closure FAIL when it appears rather than building it). Baseline re-measured: 1336 tests / 83 files, mypy clean on 72 source files, git status --porcelain -- argus/ EMPTY. || FR34 · 🟠 · Engineering Lead. Tokens exist ONLY in argus/dogfood/proof_run.py + proof_render.py today — an internal artifact, not a user surface. EXTEND the two-sided DOGFOOD_EXTERNALIZATION_GUARD (AR7/§3.3), never a second mechanism. Surface set ENUMERATED so an unenumerated member FAILS CI. NEW vs the superseded 11.1: the disclosure records WHAT REMOVES IT (Epic 13) and is written to be REPLACED by the cleared status, never deleted — with a test asserting the guard cannot pass vacuously once the token changes.
  11-2-polyglot-repository-is-classified-correctly: done  # code-review 11-2 iteration 2 (Sonnet): PASS, status review -> done. See the last_updated entry above for the full narrow-confirmation record (count re-derived a third time = 32; both sprint-status.yaml occurrences corrected; guard's `assert pairs >= 30` NOT re-baselined, tests/test_classification_word_boundary.py byte-unchanged; git ls-files -- argus = 72, four M no A; sha256 unchanged; dogfood unit 2 = 14900, partition_ids byte-identical; suite 1362/1 failure (DF-11-1-A only); mypy clean 72; bandit 0H/0M/19L; YAML parses clean, STATUS DEFINITIONS intact; nothing published). All ACs met. || dev-story 11-2 FIX ITERATION 1 2026-08-12: in-progress -> review. THE ONE Low review finding is RESOLVED, 1 of 1 - and the correction was RE-DERIVED BY EXECUTION, not copied from the review (mandatory: the finding is itself about asserting a number that was never measured). Completion Note 3 claimed TC-ArgusAgent-DETECT-001-98 generates 60 adversarial pairs; it generates 32. Two independent routes to that number: (a) sys.settrace over a run of the REAL -98 test function, capturing its own `pairs` local at frame return = 32 with the test passing (the loop executed, not re-implemented); (b) _all_registrations() live = _UNAMBIGUOUS_TEST_SUFFIXES 13 + _AMBIGUOUS_PYTHON_TEST_SUFFIXES 1 + _CASE_SENSITIVE_TEST_SUFFIXES 1 + _AMBIGUOUS_PYTHON_TEST_BASENAMES 1 = 16 entries x 2 synthesized prefixes ('a','9') = 32. THE GUARD WAS NOT TOUCHED: its `assert pairs >= 30` floor is correct and non-vacuous (32 >= 30), and re-baselining a threshold to agree with a corrected count is the antipattern this project has refused before - tests/test_classification_word_boundary.py is BYTE-UNCHANGED. NO argus/** BYTE MOVED: vacuous_test.py sha256 still 37c82e39003a1ad3074600d7754eee2749a59263b01125c67f85dcb603793e48. The round's whole write set is PROSE IN TWO FILES (this story's Completion Note 3 + record, and the two copied occurrences here). GATES RE-RUN, ALL UNMOVED (LOCAL Windows/CPython 3.11.15): --junit-xml tests=1362 failures=1 errors=0 skipped=0 with the one failure being tests/test_evidence_citation.py::test_TC_ArgusAgent_DOCS_001_22_the_status_document_set_is_closed (DF-11-1-A, carved out by node id, NOT closed and NOT touched) - NO SECOND RED; mypy clean 72; bandit 0 High / 0 Medium (19 Low); git ls-files -- argus = 72 with exactly four M lines and no A line (DF-10-4-D fence holds); dogfood unit 2 = 14900/15000 = 33 consumed of 133, 100 remaining, all three partition_ids byte-unchanged (477ef77d7b65 / 82a3d605e61e / ed6d08f25ce3); `argus audit .` field-for-field identical - verdict=RELEASE_READY deep_ratio=61/166 blocking_findings=0 assessed_deep_ratio=61/77 scope=application held_out=89, exit 0. CI evidence NOT ESTABLISHED; nothing pushed, tagged or dispatched (Epic 11 publishes nothing). || code-review 11-2 iteration 1 (Sonnet): CONCERNS, status review -> in-progress. Independently re-verified on disk (fence, LOC budget/partition ids, test ledger via --junit-xml, mypy/bandit/coverage, `argus audit .` field-by-field, sha256 round-trip, append-only ledger, DN-1/DN-2 code, AC4 ast walk). No second red; no High/Medium findings; all ACs/fences/gates held. ONE Low finding filed as [Review][Patch]: Completion Note 3's "60 pairs generated" is wrong (real count 32, re-derived by execution) — a prose inaccuracy only, the guard's own `>= 30` floor still holds and is non-vacuous. See story's Review Findings for full detail. [RESOLVED in dev-story fix iteration 1, 2026-08-12 — see the head of this entry.] || dev-story 11-2 2026-08-11: ready-for-dev -> in-progress -> review. DF-8-2-B CLOSED. The whole production change is 33 physical lines in the ONE existing file argus/detectors/vacuous_test.py (DN-4 / DF-10-4-D fence HELD AND VERIFIED AFTER STAGING: git ls-files -- argus = 72, git status --porcelain -- argus/ = exactly 4 M lines, 11.1's three plus this one, and NO A line). Three separator-less entries removed ('test.java', 'spec.rb', bare 'test.py' - the story's re-measured count of THREE held); _CASE_SENSITIVE_TEST_SUFFIXES = ('Test.java',) added INSIDE tier 2 and matched against the ORIGINAL-CASE basename before the lowercasing (DN-1 - the capital IS Java's separator; '_test.java' would have deleted every Java true positive); _AMBIGUOUS_PYTHON_TEST_BASENAMES = ('conftest.py',) added INSIDE tier 3 (DN-2 - the whole-basename rule the bare 'test.py' was standing in for). Predicates stay pure, no new import, no new dependency, tier ORDER and the tier-3 unreadable-defaults-to-True direction unchanged, and TC-ArgusAgent-DETECT-001-95 passes UNMODIFIED. ZERO EXISTING TEST ASSERTIONS CHANGED (the six-item watchlist re-run individually, all green). RED-FIRST DONE TWICE WITH THE FINAL TEST CODE (E.1): (a) against the genuinely unfixed tree, AC3's closure failed naming EXACTLY THREE offenders - test.java, spec.rb, test.py - and PIPELINE-002-12 reproduced the false green (latest.java inelig=TEST_FILE, depth=audited_shallow); (b) after the fix the three defective entries were restored into the FINAL module structure, both closures bit again (3 offenders + 'svc/atest.java' classified a test), and the file was restored with sha256 ROUND-TRIPPED byte-identically (37c82e39003a before and after; pre-story sha was the story's own 85af39dd98e4). LOAD-BEARING ACs DELIVERED AS CLOSURES, NOT LISTS (AI-E10-5): DETECT-001-97 reads both tables OUT of the module and fails on any entry carrying no registered boundary; -98 SYNTHESIZES every adversarial near-miss from those tables and asserts BOTH directions (32 pairs generated = 16 registered entries x 2 synthesized prefixes; CORRECTED in fix iteration 1 from a wrong "60" - re-derived by execution, not copied from the review); -99 closes over LANGUAGE_BY_SUFFIX so all 10 grounded languages are decided - 8 carry a convention, c and php are REGISTERED reason-carrying exemptions - and it goes red in both directions (an 11th language, or an exemption that acquires a convention). AC4's AC7 RE-PROOF IS BEHAVIOURAL *AND* STRUCTURAL: PIPELINE-002-10 drives the real pipeline over an 8-file polyglot fixture spanning BOTH constants plus the unreadable tier-3 case, and PROVES ITS OWN ASSERTION FIRES against a synthetic disagreement; -11 walks argus/pipeline.py with stdlib ast (READ-ONLY - the file stays byte-fenced to 12.1) and pins the TWO construction sites, requiring is_test_file to be evaluated ONCE per site and that exact bound name to reach _critical_ineligibility (a third derivation goes red). THE FALSE GREEN IS PROVEN CLOSED END-TO-END, not asserted: on the fixture svc/latest.java is now CRITICAL + ELIGIBLE + audited_deep and the critical set is NON-EMPTY, and svc/myspec.rb's exclusion reason moved from the FALSE test_file to the TRUE zero_definition_module (DF-10-2-A observed, NOT fixed). LOCAL GATES (Windows/CPython 3.11.15 - NO CI RUN COVERS THIS TREE, AI-E10-1 carried forward): 1362 tests = 1352 baseline + EXACTLY 10 new, 0 skipped, and EXACTLY ONE failure - tests/test_evidence_citation.py::test_TC_ArgusAgent_DOCS_001_22_the_status_document_set_is_closed, carved out BY NODE ID as DF-11-1-A (§0.4/DN-7: NOT closed, NOT touched, retro neither added nor deleted); mypy clean on 72 source files (count did not move); bandit 0 High / 0 Medium (19 Low); coverage 95.82% (gate 80; +0.05 from 95.77% - new lines are covered). BUDGET RE-RUN, NOT ASSUMED (AC7.4): dogfood unit 2 14867 -> 14900 = 33 CONSUMED of 133, 100 REMAINING, and all three partition_ids BYTE-UNCHANGED (477ef77d7b65 / 82a3d605e61e / ed6d08f25ce3). NO DOGFOOD ARTIFACT REGENERATED. `argus audit .` re-run and compared field by field: verdict=RELEASE_READY, blocking_findings=0, assessed_deep_ratio=61/77, scope=application, exit 0 - ALL UNCHANGED; deep_ratio 61/165 -> 61/166 and held_out 88 -> 89, +1 each and ARITHMETICALLY EXPLAINED by the ONE new test file, which closes exactly (166 - 89 = 77) and confirms the zero-instance claim (re-verified: no .java, .rb, conftest.py or bare-suffix *test.py is tracked). ONE DEVIATION FROM THE STORY'S §F WRITE SET, RECORDED NOT HIDDEN: tests/test_release_surface_honesty.py gained ONE additive _NOTE_SECTIONS registry entry, because AC6.4 mandates a new CHANGELOG ### section and that registry is designed to go RED until a new section is registered deliberately (Story 11.1 did the same). No assertion was weakened. The placement was DECIDED as that registry's own comment demands: 11.1's instrument disclosure stays FIRST, this section second. CI evidence: NOT ESTABLISHED - no audit-ci.yml run covers this HEAD; a human runs `gh workflow run audit-ci.yml --ref master` after pushing. NOTHING PUBLISHED: no push, no tag, no workflow_dispatch (Epic 11 publishes nothing - that is 12.9). NOT COMMITTED: the delta is STAGED (git add of the new test + this story file, E.5) and left for the review gate; the inherited-dirty paths named in the story frontmatter were not committed, reverted or restaged. || create-story 2026-08-11: backlog -> ready-for-dev; story file stories/11-2-polyglot-repository-is-classified-correctly.md. See the last_updated entry for the full create-story record. HEADLINES the dev must not miss: (a) THREE separator-less entries, not two - the bare "test.py" in _AMBIGUOUS_PYTHON_TEST_SUFFIXES has never been named by any document; (b) DN-1 Java's word separator is a CASE boundary (Surefire's four defaults are all CamelCase), so "_test.java" - the fix DF-8-2-B's own close condition suggests - would delete every Java true positive; (c) DN-2 conftest.py ends with "test.py" and DETECT-001-95 pins it as TIER 3, so the bare entry is REPLACED by a whole-basename rule, not dropped; (d) DF-8-2-B's "not a false green" premise EXPIRED when Java gained a grammar - measured, a CRITICAL production .java file is excluded from the FR4 set under the false reason test_file and the critical set comes out EMPTY, so FR16's critical clause is satisfied vacuously (inversion F1); (e) the whole change lands in ONE existing file (DF-10-4-D fence, git ls-files -- argus stays 72) inside a 133-line dogfood budget with a mandated HALT and NO artifact regeneration; (f) DF-11-1-A stays deferred and is carved out BY NODE ID - any second red is the dev's; (g) false NEGATIVES (_test.rb, *Tests.java, *TestCase.java, Test*.java, *Test.php, _test.c, and c/php having no convention at all) are FILED, NOT FIXED. Candidate fix proven viable by a reversible sha256-round-tripped experiment: full suite 1352 with only the pre-existing red, zero existing tests modified, +7 LOC, partition ids unchanged. (prev: DF-8-2-B · 🟢 internally, RELEASE-BLOCKING for this audience · Delivery Orchestrator. 'test.java'/'spec.rb' at argus/detectors/vacuous_test.py:198 carry no word separator, so latest.java/myspec.rb classify as tests BY NAME with no content check to correct it. Zero instances in THIS repo — the ledger called it 'latent, lands only on a polyglot target repo'. The public audience IS that target. Add separators + pin a near-miss corpus; RE-PROVE the AC7 two-stages-cannot-disagree invariant.)
  11-3-published-action-cannot-execute-consumer-input: done  # code-review 11-3 iteration 2 (Sonnet): PASS, status review -> done. FOCUSED CONFIRMATION of fix iteration 1's single High finding (not a full re-review; the six sites, the env:/quoting closure, the AR7 no-fork property, the documented-invocation set, both corpus-glob fences and the resumed-session diff coherence were already independently verified in iteration 1 and not re-derived). (1) THE FIX IS GENERAL, NOT A WIDER ENUMERATION: constructed the reviewer's OWN adversarial spellings outside the dev's 108-generated set - digit indicators 0/9 and combined chomp+digit orders the dev did not sample, a comment glued to the indicator with no space (`|#nospace`, `>-#c`), CRLF line endings throughout a synthetic document, a tab-only trailing-whitespace header, and a single-line command with a literal `#` INSIDE a double-quoted string (confirms a quoted `#` is not misread as a YAML comment) - every one classified correctly, no blind spelling found; the `[0-9+\-]*` character class is genuinely order/count-agnostic, keyed on `block is not None`, never on the content of `rest`. (2) RED-FIRST PROOF INDEPENDENTLY REPRODUCED: reconstructed the reviewed pre-fix resolver in memory (monkeypatch, no file touched) and ran the FINAL -29/-32 test bodies against it directly - -32 failed with the exact claimed message "THE GUARD IS BLIND TO 102 OF 108 LEGAL run: BLOCK HEADERS"; sha256 of the current file independently computed as 55b3efa15c18ea09a6c0a89b8ea7eb1260fa533eab448d60171bf6456a0d0974, byte-identical to the claimed FIXED/RESTORED hash. (3) NO FORK, NO WEAKENING: grep confirms exactly one `executable_line_numbers` definition and no private re-implementation in the guard file; extract_documented_invocations() re-run directly still returns the same 5 invocations element-for-element (action.yml at :102); git ls-files -- argus = 72, git status --porcelain -- argus/ = the same four ` M` lines, zero ` A`. (4) CONTINUATION-ABSORPTION VERIFIED CORRECT: a synthetic folded `${{` followed by a SIBLING `env:` at the same indent as `run:` is correctly NOT absorbed (result {6,7} only). (5) TEST LEDGER INDEPENDENTLY RE-RUN: full suite with --junit-xml gives tests="1371" failures="1" errors="0" skipped="0", the one failure attributed exactly to tests.test_evidence_citation::test_TC_ArgusAgent_DOCS_001_22_the_status_document_set_is_closed (DF-11-1-A, no second red); 1371 cross-checked independently by summing this repo's own per-file --collect-only report across 86 files; mypy clean 72; bandit 0H/0M/19L; argus audit . field-identical (RELEASE_READY, blocking_findings=0, assessed_deep_ratio=61/77, deep_ratio=61/167, held_out=90, exit 0). (6) FENCES RE-MEASURED after git add: git ls-files -- argus = 72 zero A; dogfood unit 2 = 14900 (0 of 100 consumed), all three partition_ids byte-unchanged; deferred-work.md's Story-11.3 append-only paragraph (g) confirmed +32/-0 by inspection of the diff boundary; HEAD = 93adc94, git tag -l empty, 6 commits unpushed - nothing pushed, tagged, released or dispatched. (7) sprint-status.yaml re-read directly: STATUS DEFINITIONS block and prior comment history intact. No High/Medium finding survives; no unresolved decision-needed or patch item. argusdemo/demo.sh unchanged from iteration 1's assessment (breaks nothing, held for the epic checkpoint by the operator) - not re-litigated, not actioned. All ACs, fences and gates held. || dev-story 11-3 (mode fix, iteration 1) 2026-08-12: in-progress -> review. The single [High] finding is RESOLVED (1 of 1). Reproduced RED first with the reviewer's own action.yml-shaped synthetic (interpolations() -> ()), then swept the neighbourhood instead of the reported shape: NINE ordinary `run:` header spellings were measured blind, including `run: |2` and `run: >-2` where no comment is involved at all (a digit is not [-+]), so a comment-stripping fix would have left a neighbour broken. Repaired IN PLACE (AR7/3.3 - no fork, still exactly one definition and one call site) by capturing the block header as its own regex group and classifying on the PRESENCE of the |/> indicator rather than on whether the line's remainder is empty; YAML permits only a comment after a block header, so the remainder is never the command, which makes the recognised set the c-b-block-header GRAMMAR rather than an enumeration (C.6 closure-not-list). A single-line `run:` carrying an unclosed ${{ now also absorbs its continuation lines. -29 gained four shapes plus a commented-header step in its CLEAN corpus (no false positive on the shape authors should write); NEW -32 asserts the GENERATED cross product of the header grammar (108 spellings). Proven RED-first with the FINAL test code against the reverted resolver - -29 and -32 both failed, -32 naming 102 of 108 blind spellings - with a sha256 round-trip on the reverted file (55b3efa15c18ea09 -> 6025e2d0f8298015 -> 55b3efa15c18ea09); no real file was mutated to produce a control. No existing assertion weakened, deleted or re-baselined. Provably INERT on the real corpus: zero executable lines added or removed in action.yml, the three workflows or README.md; extract_documented_invocations() still element-identical 5 -> 5 (0 added, 0 removed); surviving hit-set still exactly the two DN-6 github.sha exemptions. deferred-work.md gained an APPEND-ONLY paragraph (g) superseding its -24..-31 / 8-test figure (+32/-0, after.startswith(before) True). LOCAL gates (Windows, CPython 3.11.15): 1371 tests = 1370 + 1 new, EXACTLY ONE failure - tests/test_evidence_citation.py::test_TC_ArgusAgent_DOCS_001_22_the_status_document_set_is_closed (DF-11-1-A, carved out by node id, still not closed/touched), parsed from --junit-xml not read off the terminal; 0 errors, 0 skipped; mypy clean on 72 sources; bandit 0H/0M/19L; coverage 95.82%; `argus audit .` field-identical with NO movement at all (verdict=RELEASE_READY blocking_findings=0 assessed_deep_ratio=61/77 scope=application deep_ratio=61/167 held_out=90, exit 0) because this iteration added no file. Fences re-measured AFTER git add: `git ls-files -- argus` = 72, zero ' A' lines, dogfood unit 2 = 14900 (0 of 100 consumed), all three partition_ids byte-unchanged, no artifact regenerated. argusdemo/demo.sh left untouched per the operator's held decision. CI evidence NOT ESTABLISHED (no audit-ci.yml run has ever executed any Epic 10/11 sha; the artifact hardened here runs ONLY in GitHub Actions). Nothing published: HEAD = 93adc94, 6 commits unpushed, `git tag -l` empty; delta STAGED not committed. || code-review 11-3 iteration 1 (Sonnet): FAIL, status review -> in-progress. Independently re-verified on disk, not transcribed: the action.yml diff is coherent (sha256 45341dbc3a289f0f... matches the dev's own figure exactly; working tree == index; no half-applied/duplicated fragments from the interrupted/resumed dev session); all six sites genuinely closed (bash "$VAR" parameter expansion does not re-scan its own value for shell syntax, so env:-binding + double-quoting is a real fix, not a relocation); :135 unchanged and pinned; executable_line_numbers is a true rename with no fork (grep confirms exactly one definition, no _executable_line_numbers survives, one caller, guard imports it); extract_documented_invocations() re-run independently returns the same 5 invocations element-for-element (action.yml member now at :102); --junit-xml re-run independently: 1370 tests, 1 failure, 0 errors, 0 skipped (the one failure is exactly DF-11-1-A, confirmed by parsing the XML); mypy clean 72; bandit 0H/0M/19L; coverage re-run 4536/4734=95.8175% (96%), matching baseline; argus audit . re-run field-identical to the dev's D7 table (RELEASE_READY, blocking_findings=0, assessed_deep_ratio=61/77, deep_ratio=61/167, held_out=90, exit 0); fences re-measured after git add: git ls-files -- argus = 72, four M no A, unit 2 = 14900, all three partition_ids byte-unchanged; git log origin/master..HEAD = 6, git tag -l empty, HEAD 93adc94 - nothing pushed/tagged/dispatched. -31's AC1.6-freeze addition judged legitimate (not scope creep): no new file, disclosed, severable. test_release_surface_honesty.py's new registry entries are a correctly-ordered registration, not a bent assertion. argusdemo/demo.sh read in full: it is exactly the SM's §A.2 bash demo, not collected by pytest, not reached by any corpus glob (test_workflow_input_containment.py/test_release_surface_honesty.py/test_evidence_citation.py are all scoped to specific dirs, none to the repo root), outside argus/ so the dogfood partitioner never sees it - does NOT currently break anything, but is the same SHAPE of risk as DF-11-1-A (an unfenced untracked root artifact) and should be resolved (gitignored/moved/deleted) before Epic 11 closes. ONE HIGH FINDING BLOCKS PASS: tests/test_invocation_contract.py::executable_line_numbers (the SINGLE run-block resolver this guard imports, AR7/3.3) is genuinely vacuous against a run: | # <comment> block-scalar header - an ordinary, non-adversarial YAML edit the shipped -29 (three shapes: single-line, block-scalar-no-comment, line-wrapped) does not cover. Reproduced independently this session with an action.yml-shaped synthetic document (run: | # scrub inputs before use / echo "${{ inputs.strict }}"): tests.test_workflow_input_containment.interpolations() returns () - EMPTY, the injection is UNDETECTED. Root cause: _RUN_HEADER_RE captures the trailing comment into group(1); executable_line_numbers finds group(1) non-empty and misclassifies the line as single-line run: cmd (never setting run_indent), so the indented block-scalar body - where the real script and the injection live - is never scanned. This is exactly the "-17b lesson" / E.3 trap the story itself names, one shape further out than the three it tested; AC3.2's "recognises BOTH shapes" claim and AC3's closure-not-list mandate (AI-E10-5) are both violated by this gap even though today's six action.yml sites are genuinely fixed. See the story's Review Findings for the full reproduction and suggested fix (strip a well-formed trailing YAML comment before testing whether group(1) is empty; add a fourth negative control to -29 for this shape). || create-story 2026-08-12: backlog -> ready-for-dev; story file stories/11-3-published-action-cannot-execute-consumer-input.md. FIVE PREMISES RE-MEASURED BY EXECUTION on this tree (AI-E10-3), and for the first time in Epic 11 the epic's HEADLINE measurement HELD: the five ${{ inputs.* }} sites are EXACTLY at action.yml:74,78,79,80,126, identical in the working tree AND at HEAD (11.1's action.yml edit replaced line 2 and moved nothing). HEADLINES the dev must not miss: (a) DF-9-2-D's single coordinate ':127' is OFF BY ONE - the site is :126, :127 is the echo beneath it (seventh stale coordinate in this project); (b) there is a SIXTH interpolation inside a run: body that NO document names - action.yml:68 pip install "${{ github.action_path }}"[languages] - runner-provided, NOT the vulnerability, but swept anyway (DN-3) so the PUBLISHED artifact carries a zero-exemption claim; (c) action.yml:135 with: path: is an ACTION input, not shell - deliberately NOT changed (DN-4, release.yml:84-87 precedent); (d) the epic's 'compared as quoted shell variables' is imprecise - only strict is COMPARED, the other three are passed as ARGUMENTS, so the property is bound-through-env AND double-quoted; (e) THE MEASUREMENT THAT DECIDES THE GUARD: this repo's existing run-block resolver (tests/test_invocation_contract.py::_executable_line_numbers, which AR7/3.3 forbids duplicating) is BLIND to single-line `run: cmd` - measured, it returns an EMPTY line set for a synthetic `- run: echo "${{ inputs.evil }}"`, so the obvious guard is VACUOUS against the cheapest reintroduction path; the generalized resolver was proven in a REVERSIBLE in-memory experiment to leave the documented-invocation set element-identical (5 = 5, 0 added, 0 removed) and to detect the single-line form. EXPLOIT DEMONSTRATED, NOT ASSERTED: the :126 template was rendered with a crafted strict value and executed through a real bash - an injected `id -un` RAN and printed the user; the same value through the env-bound form returned GATE_OFF, rc 0, nothing executed. The committed guard is nevertheless PURE TEXT, stdlib only - no bash, no subprocess, no pytest.skip, and NO PyYAML (measured: PyYAML is NOT declared in pyproject.toml dependencies OR the [dev] extra; it is in this venv only transitively via bandit/markdown-it-py, so `import yaml` would be a CI coin-toss). BOTH EPIC-11 FENCES WERE MEASURED AND NEITHER BINDS, which is itself the finding: build_full_repo_plan('.') reports scope_prefix='argus/', so action.yml, .github/workflows/** and tests/** are NOT in the dogfood-audited population - this story consumes 0 of the 100 remaining lines in unit 2 (re-derived independently: unit 1 1330 / unit 2 14900 / unit 3 3804 = 20034 over 72 files) and cannot move `git ls-files -- argus` (= 72, four M, no A) because it writes no file under argus/. Both are retained as verify-with-HALT ACs (AC6.1/AC6.2) with NO artifact regeneration permitted. DF-11-1-A stays deferred and is carved out BY NODE ID (third consecutive story - operator open question 1); DF-11-2-A/-B were read in full and RULED OUT - they are argus/** classification defects the ledger already targets at 12.5 with owner Delivery Orchestrator, and absorbing them would breach the argus/ fence anyway. NOTHING IS PUBLISHED (12.9 owns the publish) - pointed here because the story hardens the published action; and the irony is recorded explicitly: this is CI-executed code that NO CI run has ever executed (AI-E10-1, LOCAL Windows/CPython 3.11.15 only). Baseline re-measured this session: 1362 collected / 85 test files, the ONE red reproducing identically, the three guard files this story touches green (33 passed) || (prev: DF-9-2-D · 🟢 internally, RELEASE-BLOCKING once published · Engineering. FIVE ${{ inputs.* }} sites in run: bodies (action.yml:74,78,79,80,126) — the ledger named only :127. Publishing converts a latent finding into a live one in EVERY consuming repository. Bind via env:, compare "$VAR", sweep in ONE pass, guard test fails on any ${{ inputs. inside a run: block. HARD PRECONDITION on the marketplace channel in 12.9.) || dev-story 11-3 2026-08-12: ready-for-dev -> review. Six interpolations removed from run: bodies in action.yml (the five inputs.* at :74/:78/:79/:80/:126 plus DN-3's undocumented :68 github.action_path), each bound through a step-level env: map and referenced as a DOUBLE-QUOTED shell variable in release.yml's shape; :135 (with: path:) deliberately unchanged per DN-4 and PINNED so the non-change cannot be silently 'fixed'. The existing run-block resolver was generalized IN PLACE and renamed public (executable_line_numbers) rather than forked (AR7/3.3): it was re-measured blind to single-line `run:` (empty set for `- run: echo "${{ inputs.evil }}"`), which would have made the obvious guard VACUOUS; the documented-invocation set was proven element-identical across that change (5->5, 0 added, 0 removed) with action.yml still UNFIXED, isolating the resolver's effect. New guard tests/test_workflow_input_containment.py, 8 tests (TC-ArgusAgent-SECURITY-001-24..-31): the committed property is stdlib-only script-text INVARIANCE over an adversarial corpus (no bash, no PyYAML, no subprocess, no pytest.skip), with a mandatory positive control on the pre-fix literal, three negative controls (single-line, block scalar, line-wrapped), a both-directions exemption registry (exactly two entries, both argus-student-audit.yml github.sha), and a no-second-resolver assertion. Proven RED FIRST against the unfixed tree naming all six sites, then GREEN, with NO file mutated (action.yml sha256 byte-identical across the round trip). DF-9-2-D closed by an APPEND-ONLY ledger note (after.startswith(before) True, +90/-0) recording the corrected count (FIVE, not one), the off-by-one :127->:126, the sixth site, the :135 non-change, and the demonstrated exploit. LOCAL gates (Windows/CPython 3.11.15): 1370 tests = 1362 + 8 new, EXACTLY ONE failure - test_evidence_citation.py::test_TC_ArgusAgent_DOCS_001_22_the_status_document_set_is_closed (DF-11-1-A, carved out by node id, NOT closed/touched) - 0 errors, 0 skipped; mypy clean on 72 sources; bandit 0H/0M/19L; coverage 95.82%; argus audit . field-identical (RELEASE_READY, blocking_findings=0, assessed_deep_ratio=61/77, scope=application, exit 0) but for deep_ratio 61/166->61/167 and held_out 89->90, +1 each, explained exactly by the one new test file. Fences re-measured AFTER git add: git ls-files -- argus = 72, no A line, unit 2 = 14900 (0 of 100 consumed), all three partition_ids byte-unchanged; no dogfood artifact regenerated. CI evidence NOT ESTABLISHED - no audit-ci.yml run has ever executed this action, before or after the fix (AI-E10-1 carried forward). NOTHING pushed, tagged, released or dispatched; delta STAGED not committed. Note for the reviewer: an untracked argusdemo/demo.sh at the repo root is the SM's A.2 bash exploit demo, NOT the dev agent's - left untracked and unstaged rather than deleting someone else's evidence; needs an operator decision (delete, or keep with a .gitignore entry).
  11-4-wrong-grammar-version-cannot-produce-false-green: done  # code-review 2026-08-12 (iteration 1, model sonnet): PASS, review -> done. Reviewer independently re-derived every load-bearing claim on disk rather than trusting the story's own figures: re-ran the full suite via --junitxml (1395/1394/1, sole red DF-11-1-A by node id, deselect run 1394/0), mypy (clean/72), bandit (0H/0M/19L) and the dogfood self-audit (RELEASE_READY exit 0, assessed_deep_ratio 61/77); independently executed build_full_repo_plan('.') and reproduced units 1330/14987/4116 with all three partition_ids byte-unchanged and 13 lines of headroom left in unit 2 (confirms the operator-facing headroom figure); confirmed pipeline.py/verdict_gate.py/cache/key.py byte-unchanged via git diff --numstat; confirmed pyproject.toml's specifiers byte-unchanged (only the comment was rewritten); injected a real hypothetical sixth GrammarFailure member and watched -115/-116/-118 and REPORT-002-33 all go RED as claimed, then reverted byte-identically, proving DF-10-4-E's exhaustiveness closure genuinely bites; read _disable_the_fix and confirmed the RED-first false-green reproduction (-125) disables the real predicate at the real seam rather than a strawman; confirmed DEV-1's canary_matches is a total-equality check (stronger than "non-empty intersection"), noting only the inherent, disclosed residual limitation that any finite canary cannot prove universal grammar correctness (Low, accepted). Two Low/process items filed to deferred-work.md (DF-11-4-C canary finite-sample limitation, accepted; DF-11-4-D the note-section registry in tests/test_release_surface_honesty.py has now been touched by three consecutive stories - this instance is a clean, justified, zero-deletion insertion, but the pattern is flagged for the epic checkpoint). No unresolved High/Medium finding; no decision-needed or patch items outstanding. Findings written to the story file under ### Review Findings. # dev-story 2026-08-12: IMPLEMENTED, ready-for-dev -> review. The false green was reproduced RED-FIRST against the unmodified tree (NOT_READY_FOR_RELEASE/exit 2 -> RELEASE_READY/exit 0 with deep_ratio 5/6 in BOTH runs, at an IN-BOUND tree-sitter 0.25.2) and is kept permanently reproducible by TC-ArgusAgent-INDEX-001-125, which reconstructs the pre-fix loader in memory rather than resting on a pasted transcript. NO VERSION ASSERTION SHIPPED, as the story forbade: the mechanism is a BEHAVIOURAL per-(language, entry point) canary at the real _get_parser_for_lang seam (ARM 5), with the declared range checked as a second, independent signal (-127 proves the bound fires alone; -125 proves the behavioural check fires alone at an in-bound version). Eleven canary expectations were MEASURED before being pinned; the observable is the live intersection of the parsed tree's node types with Argus's own extraction tables, chosen because Ruby's _extract output is ((), ()) and would have been a canary a totally broken grammar satisfies. DF-10-2-A's four zero-definition languages (c, cpp, ruby, rust) pass their canaries and are asserted BY NAME (-123), so the fix cannot ship a mass false INSUFFICIENT_COVERAGE. Degradation routes through the EXISTING floor row: verdict_gate.py and pipeline.py are BYTE-UNCHANGED (git diff --stat empty), no new row, threshold or exit-code mapping. DF-10-4-E CLOSED - _render_grammar_remedy's unconditional fallthrough is now an explicit arm plus a raise, so this story's own fifth cause could not render the core-runtime remedy. AC5 corrections landed struck-not-deleted in architecture.md, pyproject.toml's COMMENT (specifier byte-unchanged) and epics.md's AC; ledger appended append-only (verified programmatically), filing DF-11-4-A and DF-11-4-B. GATES, all LOCAL (Windows/3.11.15), CI NOT ESTABLISHED, nothing pushed/tagged/dispatched and no pip command run: pytest 1395 collected / 1394 passed / 1 failed, the single red being DF-11-1-A carved out BY NODE ID and PROVEN the only failure (deselect run = 1394 passed, 0 failed); mypy clean on 72 source files; bandit 0 High / 0 Medium / 19 Low; argus audit . = RELEASE_READY exit 0 with assessed_deep_ratio UNCHANGED at 61/77. FENCES HELD: git ls-files -- argus = 72 with no A line (no new argus/** file); all three partition_ids BYTE-UNCHANGED (477ef77d7b65/82a3d605e61e/ed6d08f25ce3); unit 2 = 14987 <= 15000, consuming 87 of the measured 100-line budget with 13 to spare - flagged to the operator as the most likely halt for 11.5/12.1. One declared write outside the story's write set: tests/test_release_surface_honesty.py, to register the new CHANGELOG section in the note-section registry it turned red - the guard working as designed, with the placement reasoned and 11.1's disclosure not demoted. ORIGINAL create-story NOTE RETAINED BELOW: create-story 2026-08-12: story file written (stories/11-4-wrong-grammar-version-cannot-produce-false-green.md), backlog -> ready-for-dev. PREMISE CORRECTION, MEASURED: the flip below is NOT reproducible as stated - py-tree-sitter 0.26.0 changes nothing Argus uses (Language()/Parser()/children/child_by_field_name/has_error/grammar ABI all unchanged), and total AST loss lands on INSUFFICIENT_COVERAGE/exit 3 via the floor row, not RELEASE_READY. What IS demonstrated by execution: above the 60% deep gate, a drifted extraction vocabulary flips exit 2 -> exit 0 with deep_ratio UNCHANGED (5/6 both runs), the eligible vacuous_test_ast degrading to advisory vacuous_test_heuristic. Story mandates a BEHAVIOURAL per-language canary at the real loader seam (a version-string assertion is ruled VACUOUS - the demonstrated drift is at an in-bound version), degrading into the existing floor row so verdict_gate.py and pipeline.py stay byte-unchanged. Scope: extends GrammarFailure with a fifth cause and CLOSES DF-10-4-E. Fences: no new argus/** file (git ls-files -- argus = 72); unit-2 LOC cliff measured exactly - +100 safe, +101 moves two partition_ids; DF-11-1-A carved by node id; nothing published. ORIGINAL EPIC NOTE, retained verbatim: 🟠 · Engineering Lead. tree-sitter <0.26 is LOAD-BEARING: on 0.26.0 the cartridge self-audit flips NOT_READY_FOR_RELEASE -> RELEASE_READY (AST corroboration stops firing) — a false NEGATIVE from an assurance tool, the PRD-fatal direction (inversion F1). A metadata bound constrains a RESOLVER, not an already-installed environment. Assert at RUNTIME; degrade to a typed finding + non-vouching verdict; NEVER a RELEASE_READY under an unvalidated parser.
  11-5-published-artifact-is-complete-and-true: done  # code-review 11-5 2026-08-12: PASS, review -> done (independently reverified: build+import probe, partition-plan re-run, full suite/mypy/bandit/audit/sha256 all reproduced; no High/Medium findings). Epic 11 is 5/5 done. | dev-story 11-5 2026-08-12: ready-for-dev -> in-progress -> review. DF-9-2-A CLOSED: the registry import in argus/precision/replay_harness.py is now LAZY and a FRESHLY BUILT WHEEL imports 72 of 72 shipped modules (baseline 67 of 72, 5 fail with ModuleNotFoundError: No module named _registry). THE HEADLINE FINDING WAS REPRODUCED, NOT ASSERTED: TC-ArgusAgent-RELEASE-001-11 walks the SOURCE TREE with ast and stayed GREEN across the entire fix, so it is NARROWED with a docstring stating what it can and cannot see, _NOT_IMPORTABLE_FROM_DISTRIBUTION is measured EMPTY, and the distribution claim moved to TC-ArgusAgent-RELEASE-001-20 (tests/test_built_distribution.py) which builds the wheel AND the sdist locally and imports every shipped module out of the built artifact - with -21 (probe refuses when it cannot prove provenance; the .venv editable install makes a naive probe report a false 72 of 72), -23 (inject a module-level import _registry into the BUILT tree, observe RED, restore, observe green) and -24 (missing build front-end yields a named release_preflight.Unevaluable and a SKIP, never a silent pass) as controls. Published figures corrected AND asserted against the live build by TC-ArgusAgent-DOCS-001-54: README 76->77 wheel entries, 75->76 sdist files, "66 of the 71"->72 of 72; CHANGELOG 71->72 modules. Tag disclosure mechanised in BOTH directions (-55, which found a third caveat spelling the first registry missed). Seven /audit slash commands marked FORTHCOMING against Story 12.7/FR35, enforced both ways (-56). All 21 bare-word Minions occurrences re-derived by closure over git ls-files -- argus and classified one by one: 19 true-historical KEPT, 2 FALSE-SUBJECT rewritten (the FR34 disclosures printed on stderr by every argus audit run), with the single-sourced README.md:10 / CHANGELOG.md:52 copies updated in the same change (-57, -58). BUDGET: +10 of 13 unit-2 lines (replay_harness.py 381->391); unit 2 14987 -> 14997/15000; all three partition_ids BYTE-UNCHANGED (477ef77d7b65 / 82a3d605e61e / ed6d08f25ce3) re-measured AFTER git add; NO dogfood artifact regenerated. FENCE: git ls-files -- argus = 72 before AND after staging, no A line. GATES: suite 1405 collected / 1404 passed / 1 failed / 0 errors / 0 skipped - the one red is DF-11-1-A (tests/test_evidence_citation.py::test_TC_ArgusAgent_DOCS_001_22_the_status_document_set_is_closed), carved out by node id for the fifth consecutive story, and there is NO second red; mypy clean 72; bandit 0 High / 0 Medium / 19 Low; argus audit . = RELEASE_READY blocking_findings=0 assessed_deep_ratio=61/77 scope=application exit 0, with deep_ratio 61/168->61/169 and held_out 91->92, +1 each for exactly one new test file. argus/pipeline.py, argus/verdict/verdict_gate.py and argus/dogfood/proof_run.py byte-unchanged (sha256 quoted in the story). FIFTH CONSECUTIVE _NOTE_SECTIONS EDIT (DF-11-4-D) - a pure zero-deletion insertion registered fifth with its ordering reason stated and a promotion above 11.3 considered and declined; stated plainly for the retrospective. DF-10-2-A / AI-E10-4 RULED OUT and RESTATED AS OPEN - Epic 11 closes with them still needing a dated decision. DF-11-5-A opened: the unit-2 budget is down to 3 lines and now needs an owner (12.1). CI evidence: NOT ESTABLISHED - every figure is LOCAL Windows / CPython 3.11.15 under AI-E10-1. NOTHING PUBLISHED: no push, tag, release, dispatch or index upload; git tag -l still empty; no build artifact added to the repository. | create-story 11-5 2026-08-12: the LAST Epic-11 story is contexted, backlog -> ready-for-dev. Story file: stories/11-5-published-artifact-is-complete-and-true.md. Epic 11 now stands 4 done + 1 ready-for-dev; the epic retrospective runs immediately after 11.5 closes. THE DECIDING QUESTION WAS SETTLED BY BUILDING THE FIX AND MEASURING IT, not by argument. Story 11.4's dev PREDICTED 11.5 will not fit; measured, it FITS with 5 lines spare. The whole of DF-9-2-A is ONE file, argus/precision/replay_harness.py (381 lines, UNIT 2 - the constrained one). Cliff re-measured to the line by perturbing the in-memory LOC map: unit 2 +13 -> 15000 with all three partition_ids BYTE-UNCHANGED, +14 -> two ids move (58dbc3251926/14743 and 9408a0fe1acf/4374); units 1 and 3 absorb +3000 with ids unchanged; tests/**, action.yml, .github/**, pyproject.toml, README/CHANGELOG are outside the audited population entirely (scope_prefix=argus/). THE MINIMUM WORKING FIX WAS BUILT AND RUN IN A REVERSIBLE ON-DISK EXPERIMENT: a lazy _registry_module() helper + registry=None default + keyword-only floor_n on precision_gate_status_for = +8 PHYSICAL LINES (381 -> 389), unit 2 14987 -> 14995/15000 with all three ids byte-unchanged (477ef77d7b65 / 82a3d605e61e / ed6d08f25ce3), mypy clean 72, full suite 1394 tests / 0 failures / 0 errors / 0 skipped (DF-11-1-A deselected), and 72 of 72 modules importing from a FRESHLY BUILT WHEEL with this repository off sys.path, against a baseline of 67 of 72 (5 fail, ModuleNotFoundError: No module named _registry). The file was then RESTORED and sha256 ROUND-TRIPPED byte-identically (be468fdfe9ea646c before AND after; git status shows no M line for it). A PEP-562 __getattr__ variant that also preserves the VALIDATION_SET_FLOOR_N re-export was measured at +17 and is OVER THE CLIFF - rejected on BUDGET, and the re-export dropped instead after measuring ZERO importers anywhere in argus/**, tests/** or scripts/**. Overrun consequence measured rather than inherited: at +17 exactly ONE test goes red (tests/test_dogfood_plan.py::test_committed_partition_plan_artifact_exists_and_matches_live_derivation), NOT five - the five-staleness-tests figure describes 10.4's NEW-FILE trigger, not the LOC trigger. THE HEADLINE NON-VACUOUSNESS FINDING: the existing guard is VACUOUS, and README publishes the claim that it is not. TC-ArgusAgent-RELEASE-001-11 walks the SOURCE TREE import graph with ast, so it stayed GREEN across the entire fix (an import _registry inside a function body is still an ast.Import node) - it is structurally incapable of seeing the one thing the fix changes. DF-9-2-A's own close condition (pinned in BOTH directions, so a fix that leaves the record stale goes RED) is FALSE AS WRITTEN, and README.md:166-167 publishes that false claim verbatim. The record ALSO already drifted while the guard was green: README.md:151 and CHANGELOG.md:397,402 still publish 66 of the 71 shipped modules import when the measured truth is 67 of 72 - Epic 9's retro named this exact class and it rotted again. AC2 therefore mandates a guard over a LOCALLY BUILT wheel AND sdist (nothing published - 12.9 owns that), proven RED-first, with the vacuous guard narrowed or replaced EXPLICITLY, never silently deleted. AI-E10-3 DISCHARGED WITH THREE RE-MEASUREMENTS: (1) 5 of 71 is stale in BOTH numerator and denominator - the wheel holds 72 argus modules (77 entries = 72 .py + 5 dist-info) and 67 of 72 import, though the five-module list is exact; (2) 22 bare-word Minions across 14 modules is 21 across 14, enumerated site-by-site with its unit (6 in unit 1 free, 6 in unit 3 free, 9 across 5 modules in unit 2 where every line costs budget); (3) TWO of the three README falsehoods are ALREADY CORRECTED on this tree - the INTERIM tag path carries an explicit does-not-resolve-today / git tag -l is empty block at README:51-57, and the CLI example was fixed by Story 10.3 (DF-AUD-APAA-E) - so AC4 MECHANISES the tag disclosure in both directions instead of re-fixing it, and only the slash-command claim at README:189 is still live (measured: pyproject:73-76 ships 3 console aliases all -> argus.cli:main, and the wheel carries ZERO non-.py assets). HIGHEST-VALUE FINDING FOR A CONSUMER: two of the 21 Minions claims are printed on a user's terminal today - argus/verdict/negative_assurance.py:174 and :183, 11.1's FR34 instrument disclosure, which tells every user their findings rest on the Minions dogfood corpus four words before the same sentence says it is a self-audit of THIS repository. A false SUBJECT, not a false claim; already single-sourced and compared against README:10 / CHANGELOG:52 / pyproject / action.yml by 11.1's guard, and living in unit 3 which has 3000 lines of headroom. A MEASUREMENT TRAP IS DOCUMENTED SO THE DEV DOES NOT REPEAT IT: .venv carries an editable argus.pth, so probing with python -I (which drops PYTHONPATH) silently resolves argus FROM THE REPO and reports a false 72-of-72; the probe must assert argus.__file__ lands inside the extracted wheel before trusting any result. Baseline re-measured this session: 1395 collected / 1394 passed / 1 failed (DF-11-1-A, carved out BY NODE ID for the FIFTH consecutive story) / 0 errors / 0 skipped; mypy clean 72; git ls-files -- argus = 72; git tag -l EMPTY; units 1330 / 14987 / 4116; argus audit . RELEASE_READY deep_ratio=61/168 assessed_deep_ratio=61/77 held_out=91 exit 0. DF-10-2-A RULED OUT with reasons (AI-E10-4 offered 11.5 as a home; it is a detector-coverage change that moves classification on real repos, and there is no unit-2 budget to do it safely) and RESTATED AS OPEN for the retrospective. DF-11-2-A/-B (12.5), DF-11-4-A/-B/-C, DF-9-2-C (git rm --cached trips DF-10-4-D) and the untracked argusdemo/ also ruled out with reasons. DF-11-4-D FLAGGED EXPLICITLY: this story will be the FIFTH consecutive edit to tests/test_release_surface_honesty.py's _NOTE_SECTIONS registry - stated in the story rather than discovered at review. AI-E10-1 carried forward, not re-taken: no CI run covers this tree, every figure LOCAL Windows/3.11.15, CI evidence NOT ESTABLISHED. NOTHING PUBLISHED - no push, no tag, no release, no dispatch. || (ORIGINAL EPIC DEFINITION, retained verbatim) DF-9-2-A + DF-9-2-B · 🟠 · Engineering. (a) 5 of 71 wheel modules fail to import (No module named '_registry', from argus/precision/replay_harness.py:87-90); make it lazy/optional, re-measure from a FRESHLY BUILT wheel, pin _NOT_IMPORTABLE_FROM_DISTRIBUTION in BOTH directions. (b) 22 bare-word 'Minions' subject claims across 14 argus/** modules — classify each true-historical (keep) or false-subject-claim (rewrite), NEVER a blanket replace. (c) README falsehoods: the 'INTERIM — resolve at a tag' path (git tag -l is EMPTY), the slash-command registration claim (NO mechanism ships; pyproject:59-62 has 3 console aliases to one main), and the CLI example missing the audit subcommand + required repo positional. The slash-command claim is MARKED FORTHCOMING, not deleted — 12.7 delivers it.
  epic-11-retrospective: done  # retrospective epic-11 2026-08-12 (epic-11-retro-2026-08-12.md): COMPLETE, backlog -> done. 5/5 stories done; outcome clause MET - five defects that each get worse on publication are closed with adversarially-controlled guards, and NOTHING was published (no push, tag, release, dispatch or index upload, re-verified live). Delivery: +69 tests (1336 -> 1405 across 88 files, 0 removed/skipped/weakened), 5 new test modules, mypy clean on 72, bandit 0H/0M, ZERO new argus/** files (git ls-files -- argus = 72 throughout), all three partition_ids byte-unchanged and no dogfood artifact regenerated, `argus audit .` RELEASE_READY/blocking_findings=0/assessed_deep_ratio=61/77/exit 0 invariant. Reviews: 3/5 PASS at iteration 1 (11.2 CONCERNS->PASS, 11.3 FAIL->PASS); both fix rounds caught something real, so this is NOT a regression from Epic 10's 5/5 (AI-E11-12, record only). Ledger 4 closed + 1 partial, 10 filed, 10/10 named owners and 6/10 with a target story. THE EPIC'S FINDING: VACUOUS GUARDS ARE THIS PROJECT'S DOMINANT DEFECT CLASS - five instances in five stories (11.3's committed guard blind to 102 of 108 legal `run:` block headers; 11.5's source-tree walk asserting a claim about the DISTRIBUTION, green across its own fix while README published the claim that it was not vacuous; 11.4's rejected version assertion, vacuous by construction because the drift is at an IN-BOUND version; 11.3's inherited resolver blind to single-line `run:`; 11.2's guard sound but its PREMISE expired). Four caught at create-story time by AI-E10-3's re-measurement, one at review, NONE shipped. AI-E10-3 FIRED 5 OF 5 and is recommended permanent. SIGNIFICANT DISCOVERY - SD-1, A HARD STOP: Story 12.1 CANNOT EXECUTE under the regime that carried Epic 11. Re-derived live: dogfood unit 2 = 14997/15000 (3 LINES OF SLACK; the epic consumed 204 of 207), argus/pipeline.py = 1331/1200 and IS IN UNIT 2, git ls-files -- argus = 72. 12.1's extraction creates a new argus/** module (moving the audited population at `git add`) AND moves >=131 lines out of a unit-2 file - BOTH DF-10-4-D triggers at once, in the FIRST story of Epic 12. The Epic-11 blanket prohibition (no new argus/** file) is LOGICALLY INAPPLICABLE to a story whose deliverable is creating one; extending it does not constrain Epic 12, it cancels it. Recommended (NOT applied): pre-authorise implement -> commit -> regenerate through the artifacts' own renderers at a truthful sha -> re-run, the exact sequence the operator already ruled for 10.4 and which IS commit 93adc94. AI-E11-2. OTHER SIGNIFICANT DISCOVERIES: SD-2 third consecutive epic with no executed gate, and the next one ENDS in an irreversible publish (12.9) - the 2026-08-11 risk acceptance was scoped to one epic and has now silently covered ten stories across two; SD-3 this retro DOUBLES the DF-11-1-A standing red because epic-*-retro-*.md is the guard's own glob (it is written in the shape the honest-sentence test accepts, so registering both documents is a two-line edit that goes green); SD-4 four Epic-11 filings converge on Story 12.5, which was scoped before any of them existed; SD-5 DF-10-2-A still unowned for a second epic; SD-6 NFR-M1 breached with no repo-wide sweep while Epic 12 lands in pipeline.py three times. PREVIOUS-RETRO FOLLOW-THROUGH: 2 done / 3 partial / 4 NOT ADDRESSED / 1 not due - worse than Epic 10's 5/3/2, and ALL FOUR unaddressed items are human runbook steps (push, commit, dispose a ledger entry, tidy a config), two of them marked critical. The one item converted into a RULE executed perfectly five times. ZERO COMMITS were produced by this epic (HEAD unmoved at 93adc94; all five stories staged in one tree), so AI-E10-7 was ignored for a second epic at increasing cost. DF-11-4-D RULED: the _NOTE_SECTIONS registry is rotting on COST, not correctness - keep the closed-set half, replace the order-pinned half (which verifies agreement with a typed tuple, not the ordering principle its own comment states) with a machine-checkable impact rank, before Epic 12 adds nine more sections. argusdemo/demo.sh: recommended DELETE (its transcript is already preserved verbatim in the tracked 11.3 A.2, so nothing is lost). The resumed-session recovery after a dev transport error was CLEAN and the review verified it correctly (working tree == index, sha256 matched); recommended only that the check become standing rather than incidental. 12 action items filed as AI-E11-1..AI-E11-12 in the action_items register below. THE RETROSPECTIVE APPLIED NO RE-PLAN: no epic, PRD, architecture or story scope was edited by it; every item is a RECOMMENDATION. epic-11 itself is NOT rolled up here - that is the operator's step. || (ORIGINAL NOTE, retained verbatim) Epic 11 does NOT close by shipping — it publishes nothing. The retro restates DF-7-2-A (attested gate, uncleared) and H0 (unowned) as OPEN rather than letting release-readiness read as plan closure; the epic-9 retro declared the plan FINAL once already.

  # ── Epic 12: The Useful Tool — what a developer gets on the first run (delta, Argus repo) ──
  # Covers FR36 (12.2), FR27/NFR-D1 DELIVERY (12.3), FR37 (12.4), NFR-P3 (12.5), FR35 (12.6, 12.7)
  # Depends on Epic 10 (all five) AND Epic 11 (all five). 12.1 FIRST — hard enabler.
  epic-12: done  # ROLLED UP 2026-08-15 by the dev-loop orchestrator: all 9 of 9 stories done (12.1-12.9) and epic-12-retrospective done (epic-12-retro-2026-08-15.md). NOTE: the epic's outcome clause is only 4/5 met — "the result is published" is NOT met; all seven outward-facing publish acts remain halted under DF-12-9-A, and the derived gate citation reads NOT ESTABLISHED. PRIOR: TALLY CORRECTED 2026-08-15 (code-review 12.5): 5 of 9 stories done (12.1, 12.2, 12.3, 12.4, 12.5). PRIOR: 3 of 9 stories done (12.1, 12.2, 12.3), TALLY CORRECTED 2026-08-13 (code-review 12.3). PRIOR: 2 of 9 stories done (12.1, 12.2); 12.3 ready-for-dev. The "0/9" below is the original planning figure, kept as written. ORIGINAL: added 2026-08-10b; 0/9 stories. backlog -> in-progress 2026-08-12 by create-story 12.1 (first story of the epic). Epic 11 is done and committed (ca37283); the Epic-11 "no new argus/** file" fence is LIFTED for THIS EPIC by the operator ruling of 2026-08-12 (see last_updated). NO STORY IN THIS EPIC PUBLISHES ANYTHING until 12.9, and the orchestrator halts before it.
  12-1-pipeline-stops-breaching-its-own-limit: done  # code-review 12-1 iteration 2 (Sonnet): review -> done. NARROW CONFIRMATION of fix round 1 (cap 2, not a full re-review) — did not re-litigate the pipeline_stages.py extraction boundary, the NFR-M1 sweep's/dogfood-currency closure's non-vacuousness, the append-only ledger check or the provenance-ancestor chain (already independently re-derived and passed in iteration 1). VERDICT: PASS. Independently re-derived Finding 1 with a fresh AST-span extractor over the git blobs (not the working tree): 29 top-level definitions in ca37283:argus/pipeline.py, 16 moved / 13 retained, 0 dropped/duplicated/added, 28 of 29 byte-identical, sole exception _assessment_scope_paths — exact match. Independently reproduced the working-tree-vs-blob trap (binary read off disk is CRLF, the ca37283 blob is LF, so 0 of 13 retained match on a naive working-tree read) and all three replacement digests (e478ec09... / 5ce703e8... / 9d72d850...) by joining moved-then-retained blob bytes in pre-split file order — all three match exactly. Confirmed Finding 2's docstring fix and Finding 4's pipeline_stages.py correction (only a stale .pyc references pipeline_resume.py). Read tests/test_pipeline_split_surface.py in full and attacked it with a SELF-CONSTRUCTED mutation outside the dev's four (addition/reorder/deletion/laundering): a SUBSTITUTION — renamed "resume_audit" to "resume_audit_v2" in argus/pipeline.py's __all__ (same length/position, unlike any of the dev's four) — all three new tests (-14/-15/-16) went RED correctly, reverted with git checkout --, sha256 round-tripped to the exact recorded value (28f11817...). Confirmed no other test references pipeline.__all__, supporting the "0 of 1415 pre-existing tests saw the addition" claim. Confirmed "29 of 29" appears 7x in the story and 2x in sprint-status.yaml, every occurrence a quotation of the original error, none asserting current truth. GATES INDEPENDENTLY RE-RUN: pytest -q 1418 passed in 142.23s, 0 failed/error/skipped; mypy clean 73; bandit 0H/0M/19L; argus audit . = RELEASE_READY deep_ratio=21/58 blocking_findings=0 assessed_deep_ratio=63/79 scope=application held_out=95 exit 0 — byte-identical to the claim; git ls-files -- argus = 73 (unmoved). WRITE-SET CLAIM VERIFIED PROGRAMMATICALLY: git show --stat 2bea92f touches only sprint-status.yaml, the story file, tests/test_classification_word_boundary.py, tests/test_module_size_ceiling.py and tests/test_pipeline_split_surface.py — no argus/, scripts/, README.md, CHANGELOG.md, architecture.md or deferred-work.md touched this round; git diff --numstat ca37283 HEAD -- deferred-work.md still 301/0. PUBLICATION FENCE HELD: git tag -l empty, origin/master unmoved at 00c8d1b, no push/tag/release/workflow_dispatch/index upload. All figures LOCAL, Windows/CPython 3.11.15; CI evidence NOT ESTABLISHED. No High/Medium finding, no unresolved decision-needed/patch item, all ACs met. || dev-story 12-1 FIX ROUND 1 (2026-08-12, Opus): in-progress -> review. ALL THREE filed [Review][Patch] findings RESOLVED, plus a FOURTH of the same class this round found in the story's own write set and the review did not file. NOT PROSE-ONLY - it ships a committed guard. (1) THE BYTE-IDENTITY COUNT WAS RE-DERIVED BY EXECUTION AGAINST ca37283, NEITHER OF THE REVIEW'S TWO NUMBERS TRANSCRIBED: the truth is 28 OF 29, denominator = the 29 top-level definitions of ca37283:argus/pipeline.py (16 moved into argus/pipeline_stages.py + 13 retained; 0 dropped, 0 duplicated, 0 newly added - each count asserted, not eyeballed). The review's '15 of 16 moved' and its suggested '28 of 29' never actually conflicted: 15/16 moved AND 13/13 retained IS 28/29 - it simply never stated the denominator that joins them, and stating it was the fix. BOTH SIDES ARE READ AS GIT BLOBS: this checkout is core.autocrlf=true, so a working-tree read reports a FALSE 0 of 29 (one byte per line) and would have buried the one real change inside 28 artefacts - the first attempt did exactly that. The single exception, _assessment_scope_paths (1962B -> 2206B), is INTENTIONAL: this story's own DF-8-3-C dedup, already disclosed under AC4. Corrected in the story AND in BOTH sprint-status occurrences; the '29 of 29' strings that remain are the REVIEW'S OWN quotation of the error inside its finding text and are deliberately preserved, per the same DOCS-001-59 rule that keeps architecture.md's corrected false claim quoted inside its correction - deleting the quotation deletes the lesson. The digest c6edd6fa9ddd10... is WITHDRAWN, not restated: unreproducible under 48 span/line-ending/joiner conventions against either blob, and a both-sides-equal digest is arithmetically impossible over a set containing a changed body. Replaced by digests whose convention is stated (15 unchanged moved = e478ec09edb501..., 13 retained = 5ce703e8ca2e7b..., all 28 = 9d72d850905c31..., each equal on both sides). (2) PIPELINE-002-11's docstring said 'ENUMERATED FROM GIT' while _pipeline_family_sources() globs the FILESYSTEM - corrected to 'ENUMERATED FROM THE FILESYSTEM' with the DF-12-1-D reason inline so the two docstrings name ONE mechanism; prose only, 4/1, entirely inside the docstring, no predicate or population touched. (3) AC1's 'Pinned by test' HAD NO TEST; it does now: tests/test_pipeline_split_surface.py, TC-ArgusAgent-PIPELINE-002-14..-16, a DEDICATED file because the named DOGFOOD-001-45/-46 precedent is a dedicated file and the alternative homes would have coupled an import-surface pin to a file whose stated single subject is something else. PROVEN RED-FIRST WITH THE FINAL COMMITTED CODE against FOUR mutations of the real seam - addition, reorder, deletion and LAUNDERING (live __all__ and the pinned literal widened together) - with argus/pipeline.py sha256 ROUND-TRIPPED to 28f1181742b7f6... after every one. MUTATION A IS THE FINDING'S PREMISE PROVEN BY EXECUTION: full suite under it = 1418 collected / 1413 passed / 5 failed, and of the 1415 PRE-EXISTING tests exactly TWO went red - the two committed-artifact checks, which saw the added LINE (live LOC moved off the artifacts, i.e. this story's own widened -03/-20 working), NOT the added export. ZERO pre-existing tests saw the __all__ addition. Mutation D is why -15 exists: it re-reads __all__ OUT OF the immutable ca37283 blob by ast, so editing the literal to match a changed surface makes -14 green again and -15 stays RED - the pin cannot be laundered. -16 GENERATES 4n-1 = 27 mutants from the LIVE surface (every deletion, adjacent transposition, addition and duplication) and drives each through THE SAME predicate -14 asserts with, count asserted, never a hand-listed sample - under mutation A it correctly produced 31 for the 8-name surface, which is how it was confirmed to close over live structure rather than a constant. Both directions asserted (true surface ACCEPTED before mutants REJECTED); -15 fails rather than SKIPS when the blob is unreachable. -15 also pins two properties AC1 asserted in prose only: every re-export is the SAME OBJECT as pipeline_stages' (the no-fork property that keeps monkeypatch.setattr(pipeline, '_detect_per_file', ...) intercepting the real call) and none of the 16 private helpers LEAKED into __all__. Two warts in the new file were found by its own red output and NARROWED EXPLICITLY, never quietly: -16's assertion order (it misattributed 'the guard does not bite' when the truth was 'the live surface is wrong') and a -15 assertion whose message described the live surface while its predicate compared the blob to the literal - it now compares the LIVE MODULE TO THE BLOB, which is strictly stronger and is what makes mutation D red. (4) SELF-FOUND, NOT FILED: the MAINT-001 sweep's failure-message remedy told the next reader to follow the 'argus/pipeline_resume.py (Story 12.1) precedent' - a module that DOES NOT EXIST (it is the name SA.1 recommended; the shipped sibling is argus/pipeline_stages.py). Same defect class as findings 1 and 2, inside this story's own new guard, so it was fixed with them (1/1). GATES RE-RUN ON THE FINAL TREE, NOT CARRIED, all LOCAL Windows/CPython 3.11.15: pytest 1418 collected / 1418 passed / 0 failed / 0 error / 0 skipped (+3 = exactly the three new tests, no red anywhere); mypy clean on 73 (unmoved); bandit 0 High / 0 Medium / 19 Low (unmoved); argus audit . = RELEASE_READY deep_ratio=21/58 blocking_findings=0 assessed_deep_ratio=63/79 scope=application held_out=95, exit 0 - a POPULATION move, not a behaviour move: 21/58 IS 63/174 reduced (the Fraction effect this story already documented at 31/39), held_out 94->95 and total 173->174 are the ONE new test file, and assessed_deep_ratio is 63/79 UNCHANGED because a held-out file leaves 174-95=79 assessed and adds no deep-gradable source; verdict and blocking findings IDENTICAL. git ls-files -- argus = 73, UNMOVED - no argus/** file was added, changed or deleted this round, so all three partition_ids (45cbaf975494 / 477ef77d7b65 / 9408a0fe1acf), the unit LOC (14938 / 1341 / 4385), source_file_count 73 and total_loc 20664 are ALL UNMOVED, NO REGENERATION WAS REQUIRED OR PERFORMED, no artifact was hand-edited, and the AC3 currency guard stays green (git diff --quiet c4bd769 HEAD -- argus/ still empty). Write set PROVEN, five files: the new 290-line guard, two prose-only test edits (4/1 and 1/1), the story and this file - argus/, scripts/, README.md, CHANGELOG.md, architecture.md and deferred-work.md UNTOUCHED (git status --porcelain over the first four returns zero lines; deferred-work.md was not opened, so append-only holds trivially). NOTHING WEAKENED: no assertion anywhere deleted, loosened, skipped, xfail-ed or re-baselined; the only two assertions touched were STRENGTHENED, inside the new file. NOTHING PUBLISHED: git tag -l EMPTY, origin/master unmoved at 00c8d1b, no push/tag/release/dispatch/upload. CI EVIDENCE: NOT ESTABLISHED - every figure LOCAL, AI-E10-1's dated acceptance carried forward and NOT re-taken. || code-review 12-1 iteration 1 (Sonnet): CONCERNS, status review -> in-progress. Independently re-derived on disk, not transcribed: full suite `1415 passed in 361.39s` (matches 1415/1415/0/0/0); mypy clean on 73 sources; bandit 0H/0M/19L; `argus audit .` = `RELEASE_READY deep_ratio=63/173 blocking_findings=0 assessed_deep_ratio=63/79 scope=application held_out=94` exit 0, byte-identical to the claim; `git diff --numstat ca37283 HEAD -- deferred-work.md` = `301 0` (append-only PROVEN programmatically, zero deletions, not eyeballed); `git diff --quiet a9cc933 93adc94 -- argus/` empty and `a9cc933` an ancestor of HEAD, both re-verified; `git tag -l` empty, origin/master unmoved at 00c8d1b, HEAD 10 ahead — NOTHING PUBLISHED. Ran `scripts/regenerate_dogfood_artifacts.py` against the live tree as an idempotency probe: only the provenance-sha stamp changed (expected — the renderer stamps literal `git rev-parse HEAD` and HEAD had moved via a docs-only commit since e5a8a88); reverted before finishing, tree left clean. AST-diffed all 16 functions moved into argus/pipeline_stages.py against `git show ca37283:argus/pipeline.py`: 15 of 16 structurally byte-identical; `_assessment_scope_paths` is not (see Finding 1). Both new test files read in full and are genuinely non-vacuous (MAINT-001-05 generates adversarial variants from the live 173-file population with a counted floor; DOGFOOD-001-52 classifies real reachable commit history requiring both current/stale classes non-empty; the exemption registry -04 is proven to shrink). No High/Medium functional, security or spec-conformance defect found; the derivation-stages extraction boundary is well-reasoned and its stated import-cycle justification against SA.1's resume-family recommendation was independently confirmed. THREE Low findings filed as [Review][Patch] in the story's Review Findings section (same class the 11-2 review called "a prose inaccuracy only"): (1) AC1 Completion Notes claim "29 of 29 byte-identical" moved-and-retained definitions, but `_assessment_scope_paths` was intentionally rewritten (not preserved verbatim) as part of this story's own DF-8-3-C dedup — correct figure is 28 of 29, the change is real, tested and disclosed accurately elsewhere (AC4), only this one summary sentence overclaims, and the same "29 of 29" figure is repeated in this very sprint-status comment history; (2) `tests/test_classification_word_boundary.py`'s widened `-11` docstring says the pipeline*.py family population is "ENUMERATED FROM GIT" but `_pipeline_family_sources()` globs the FILESYSTEM (its own docstring says so) — the call-site docstring names the wrong mechanism; (3) AC1's "public import surface unchanged … Pinned by test" has no dedicated `__all__` byte-identity assertion (verified byte-identical by direct diff, but only indirectly pinned via existing imports, unlike the `DOGFOOD-001-45` precedent for `proof_run.__all__`). None block shipping; core is correct and independently verified; cleanups are warranted before `done`. || dev-story 2026-08-12: in-progress -> review. Two LOCAL commits, NOTHING PUBLISHED (git tag -l EMPTY, origin/master unmoved at 00c8d1b, no push/tag/release/dispatch/upload). c4bd769 = the implementation; e5a8a88 = the regeneration, a SEPARATE commit of renderer output only, exactly as 93adc94 did for 10.4, under the operator ruling of 2026-08-12. AC1: argus/pipeline.py 1331 -> 944 (256 lines of headroom for 12.2/12.3/12.4) by extracting argus/pipeline_stages.py. THE BOUNDARY IS NOT THE ONE THE STORY RECOMMENDED, and the reason is measured rather than preferred: an ast walk showed the resume family (SA.1's 357-line recommendation) needs ELEVEN names that would stay behind while pipeline.py must keep re-exporting resume_audit from __all__ - a module-level import cycle survivable only by an import-order trick - whereas the derivation stages need THREE, all constants, which moved with them, so the dependency points strictly downward, there is no cycle, and 403 lines are shed. PURITY PROVEN MECHANICALLY: 28 of 29 moved-and-retained definitions byte-identical (figure RE-DERIVED BY EXECUTION in fix round 1 against ca37283, NOT transcribed; denominator = the 29 top-level definitions of ca37283:argus/pipeline.py, 16 moved + 13 retained, 0 dropped / 0 duplicated / 0 added; both sides read as GIT BLOBS because a core.autocrlf working-tree read falsely reports 0 of 29). The ONE exception, _assessment_scope_paths, was INTENTIONALLY rewritten by this same story's DF-8-3-C dedup and is disclosed under AC4. Re-derived digests, convention stated: the 15 unchanged moved bodies e478ec09edb501... and the 13 retained 5ce703e8ca2e7b..., both sides equal; the previously published c6edd6fa9ddd10... is WITHDRAWN - unreproducible under 48 span/line-ending/joiner conventions, and both-sides equality is impossible over a set containing a changed body), __all__ byte-identical, ZERO import sites edited, and monkeypatching argus.pipeline._detect_per_file still intercepts the real call. DF-8-2-A's OWN named remedy was measured INSUFFICIENT (~59 lines vs the >=131 required, leaving ~1272) and that is recorded in the ledger so the next reader does not repeat it. AC2: tests/test_module_size_ceiling.py (TC-ArgusAgent-MAINT-001-01..-05, a NEW verification area) sweeps EVERY tracked .py from git ls-files. LANDED RED-FIRST WITH THE FINAL COMMITTED CODE - 'NFR-M1 breach - argus/pipeline.py (1331 lines, 131 over)' - and with the registry emptied in-process it independently reproduced all four repo-wide breaches (1331/1326/1308/1203 of 169). The three test-file breaches are NAMED, DATED, REASONED exemptions filed as DF-12-1-A/-B/-C with owners and live target stories (12.2/12.3/12.5), never a narrowed population, and the registry SHRINKS (an exemption for a file that is gone OR no longer over the cap fails; argus/pipeline.py is asserted unaddable). Non-vacuity: the enumeration is asserted non-empty and two-sided, the 1200/1201 boundary is driven through the sweep's OWN predicate, and an over-cap adversarial variant is GENERATED from every one of the 173 files in the live population. AC3: THE 0.5 FINDING IS CONFIRMED INDEPENDENTLY - all three artifacts were stale at ca37283 while all five DF-10-4-D assertions were GREEN. tests/test_dogfood_artifact_currency.py (TC-ArgusAgent-DOGFOOD-001-49..-52) closes both directions: an artifact is current iff the sha it cites is a real ancestor of HEAD and git diff --quiet SHA HEAD -- argus/ is empty. RED FOR FREE on the live defect with the final code ('cites a9cc933; argus/ has moved since (7 files changed, 749 insertions, 78 deletions)'), and -52 refutes both degenerate predicates from LIVE HISTORY on every run by classifying every commit reachable from HEAD and requiring both classes to be non-empty. -03 and -20 were WIDENED to the derived figures their docstrings always promised (-20's red was the live 20152 LOC against the published 19783); NOTHING was loosened or deleted. All five committed-artifact failure messages now name scripts/regenerate_dogfood_artifacts.py, a REAL committed entry point that renders through the artifacts' own renderers, asserts each file equals the renderer's return value, and REFUSES to run on a dirty argus/ tree. DF-10-4-D's provenance/enumeration question is DECIDED and recorded: the label now tells the truth (population = git INDEX, descriptor = git rev-parse HEAD) rather than pinning the enumeration, because pinning is a verdict-adjacent behaviour change AC5 forbids - and the guard makes the divergence detectable rather than merely described. AC4: deferred-work.md +301/-0, APPEND-ONLY PROVEN. CLOSED: DF-8-2-A; DF-8-3-C (one partition_application_files helper in argus/detectors/vacuous_test.py called from both former verbatim copies - its recorded coordinates were stale AGAIN, the fourth in three days); DF-8-5-B + DF-10-4-D TOGETHER per DF-10-4-D's own instruction; DF-11-1-A; DF-11-5-A; DF-9-2-C (already true on arrival - zero tracked pycache under argus, verified, no git rm --cached run). RE-RECORDED: DF-8-3-A -> 12.4, its 'no room in pipeline.py' blocker EXPLICITLY DISCHARGED, the remaining reason being scope (a report-content change AC5 forbids by construction). RULED OUT OUT LOUD: DF-10-2-A stays OPEN and UNOWNED (AI-E11-7 wants a dated OPERATOR decision, type (H)); DF-11-4-D/AI-E11-6 re-targeted to 12.4 with its alternative DoD explicitly NOT taken. OPENED: DF-12-1-A/-B/-C (the exemptions), DF-12-1-D (the sweep's git-INDEX blind spot, which bit during this implementation), DF-12-1-E (three pipeline*.py siblings and no layering guard). AC5 BEHAVIOUR PROVEN UNTOUCHED, NOT ASSUMED: the in-place figures necessarily move (63/173, 63/79, held_out=94) because this story ADDS 4 tracked .py files (+2 deep-gradable source, +2 held-out test files), so the two variables were SEPARATED. The NEW code over the PRE-STORY population (a pristine detached worktree at ca37283) returns verdict=RELEASE_READY deep_ratio=61/169 blocking_findings=0 assessed_deep_ratio=61/77 scope=application held_out=92 exit 0 - byte-identical to the Task 0 fixture. An A/B over that one tree, pre-12.1 code vs post-12.1 code with an identical --report-dir, gives 4 BYTE-IDENTICAL report files and 848 BYTE-IDENTICAL .argus/ files, with no caveat and nothing to explain away. ZERO import-path edits were needed. Four test files were touched and every change is a STRENGTHENING: PIPELINE-002-11 went red because code it ast-parses BY NAME moved, and its REACH was widened to the globbed argus/pipeline*.py family rather than its CLAIM narrowed. AC6: DF-11-1-A closed FIRST and ALONE - suite 1405/1404/1 -> 1405/1405/0 BEFORE the extraction, ending a five-story node-id carve-out; registration verified inert (_status_assertions() = 0 for both retrospectives); NO retrospective edited and NO citation minted (that half of the close condition deliberately NOT taken, with the reason in the ledger). architecture.md registers both new rules AND CORRECTS its false claim that NFR-M1 was enforced by file-size CI under a tests/apaa/ directory that does not exist - TC-ArgusAgent-DOCS-001-59 holds both and permits the false sentence only as a quotation inside the correction blockquote. README/CHANGELOG derived figures 72 -> 73 shipped modules (DOCS-001-54: the artifact is the fact); no user-visible claim changed and no release-note section added (SA.6 honoured). REGENERATION: all three artifacts produced by scripts/regenerate_dogfood_artifacts.py at provenance c4bd769, which git merge-base --is-ancestor confirms is an ancestor of HEAD and git diff --quiet c4bd769 HEAD -- argus/ confirms is EMPTY. Every changed line is a DERIVED FIGURE or the new honest label: population 72->73, LOC 19783->20664, X 450->456, cut edges 57->67, critical set 51->52, deep 56/72->57/73, findings 108->111, plus the bundle hash and byte length. Unit count STAYS 3; two of three partition_ids moved to 45cbaf975494 and 9408a0fe1acf - EXACTLY the ids SB.3's simulation predicted for a new argus/pipeline_stages.py. Not one character was hand-edited. The frozen Story-7.2 superseded record was deliberately NOT regenerated. FINAL GATES, ALL LOCAL Windows/CPython 3.11.15: 1415 collected / 1415 passed / 0 failed / 0 error / 0 skipped (1405 baseline + 10 new); mypy clean on 73 source files (was 72, exactly as predicted); bandit 0 High / 0 Medium / 19 Low; argus audit . RELEASE_READY exit 0. git ls-files -- argus = 73; all tracked .py = 173; NO argus file over 1200 (next largest 944). CI EVIDENCE: NOT ESTABLISHED - committing is not evidence, and AI-E10-1's dated acceptance is carried forward, NOT re-taken (re-taking it for Epic 12 is AI-E11-4, the operator's). Publication is Story 12.9 alone.  # PRIOR: ready-for-dev -> in-progress 2026-08-12 (dev-story). # backlog -> ready-for-dev 2026-08-12 (create-story; story file: stories/12-1-pipeline-stops-breaching-its-own-limit.md). Six ACs, seven tasks, sequencing fixed by DF-10-4-D's bootstrap hazard (baseline -> close DF-11-1-A -> sweep RED-first -> extract -> commit -> regenerate through the renderers -> commit -> land the currency guard). ABSORBS DF-8-5-B AND DF-10-4-D (closed together, never separately, per DF-10-4-D's own instruction) and DF-11-1-A. Original planning note follows. # 🟠 · Engineering Lead. ENABLER, now a GATE — 12.2 and 12.3 both land here. pipeline.py is 1331 vs the NFR-M1 cap of 1200; DF-8-2-A recorded 1199 and warned 'the next edit of any size breaches NFR-M1'. Add the REPO-WIDE sweep test (no assertion covers pipeline.py today). Close/re-scope DF-8-2-A, DF-8-3-A, DF-8-3-C. ABSORBS DF-8-5-B (2026-08-10b triage): the 3 committed-artifact rot checks re-break on any argus/** composition change with no warning (-03/-06 RED across 4 consecutive commits; 8.5 re-derived 3x) — close with a named regeneration entry point in all three failure messages, or a CI regenerate-and-fail-on-drift step. Epic 12 changes argus/** more than any epic since Epic 6. Pure restructuring: full suite unchanged + dogfood verdict IDENTICAL.
  12-2-deep-audit-is-wired-opt-in-and-honest: done  # code-review 12-2 iteration 2 (Sonnet 5): review -> done. VERDICT: PASS. Narrow confirmation of the fix round; full independent re-verification detailed at last_updated above (same date, same review). Suite 1441 passed / 0 failed; mypy clean 74; bandit 19 Low / 0 Med / 0 High identical set vs a fresh 2bea92f worktree; argus audit . RELEASE_READY exit 0; argus population 74 unmoved; ledger append-only 250/0 cumulative, 84/0 this round; publication fences intact (tag empty, origin unmoved, HEAD 15 ahead). TC-ArgusAgent-AUDIT-001-73/-74 attacked with two self-constructed mutations, both directions independently reproduced RED, both reverted clean. AC2.4 byte-identity re-derived from scratch with identical repo/report-dir paths - stdout/stderr/exit/report-tree/.argus-state all byte-identical. One Low, non-blocking finding: a sha256 transcription mismatch in the Dev Agent Record's fix-round prose (deep_pass.py figure does not match; open_llm_adapter.py figure does) - not a code defect, underlying revert-clean property independently re-verified by direct mutation. Process ruling: the story file's untracked git status (unlike 12.1's) is a retrospective item, not a done blocker. CI evidence: NOT ESTABLISHED. || PRIOR: dev-story 12-2 FIX ROUND iteration 1 (Opus 5): in-progress -> review. 1 of 1 blocking review findings resolved (DF-12-2-D); DF-12-2-E re-ruled and left deferred with reasons. THE DECISION OF THE ROUND - is the wiring VACUOUS? RULED NOT VACUOUS: adapter-limited and fail-safe. Option (a) (populate structured_output in the adapter) was MEASURED and rejected on five findings, not assumed: (1) _build_messages never asks the model for structured output - there is no claim grammar and nothing well-formed to parse; (2) structured_output's own contract in ports.py is claim/locator-shaped strings, NEVER raw prompt/response bytes (NFR-S1), so tipping completion text in is a documented contract VIOLATION, not a fix; (3) tests/test_open_llm_adapter.py asserts structured_output == () with the explicit rationale 'NFR-S1 - metadata only', so closing it requires re-baselining a committed NFR-S1 SECURITY assertion on the product's only egress path; (4) doing it honestly needs a claim grammar + prompt contract + redacting parser + a DEEP_PROMPT_TEMPLATE_VERSION bump, which is an AR5 cache-key closure input; (5) it could not be validated (Sec 0.3 forbids live dispatch; CI evidence NOT ESTABLISHED) and deep_audit.py and _claim_is_ast_grounded BOTH already name full claim-grammar grounding as Story 6.2's. Option (b) SHIPPED. Why not vacuous, asserted rather than argued: a vacuous guard is green when it should be red (over-claims); this is the mirror image. TC-ArgusAgent-AUDIT-001-73 asserts the egress path GENUINELY FIRES - the real pipeline builds the real adapter and the request really reaches the transport, with the right per-file targets and URL - so only the FAVOURABLE outcome is unreachable, and FR36's 'never produces a false deep claim' is made UNCONDITIONAL by the gap rather than weakened by it. TC-ArgusAgent-AUDIT-001-74 is the POSITIVE CONTROL required by this story's own Sec 0.6/AC3.3 rule: same adapter, same substituted transport, same response bytes, same real pipeline, same repo, the ONLY change being the discarded completion carried onto structured_output - and it DELIVERS. So the gap is exactly one field wide, it is the adapter's, and everything wired downstream of the port is proven on real provider-shaped input rather than only on a purpose-built fake. Both landed RED-FIRST WITH THE FINAL COMMITTED CODE, BOTH DIRECTIONS: mutating _dispatch_httpx to populate structured_output fails -73 while -74 stays green; mutating _claim_is_ast_grounded to return False fails -74 while -73 stays green; each mutation reverted and the file sha256 round-tripped (open_llm_adapter.py 4a820e77..97ca, deep_pass.py 6cc79bf3..3ff7). -73 is DESIGNED TO GO RED the day DF-12-2-D closes, with a failure message that says so, so the record cannot rot. HONESTY MADE EXPLICIT at all three sites a reader would be misled: the deep_pass.py module docstring, the FR36 _Delivery note WHERE THE WORD 'wired' IS ASSERTED (now scoped: wired means the seam is reached and the safety properties hold on it, NOT that the capability completes end to end through the shipped adapter), and a user-facing KNOWN LIMITATION in CHANGELOG.md telling an operator not to pay a provider for depth they will not get. Also removed 'assert ... or True' from -69 - an assertion that cannot fail, i.e. a vacuous guard, in the story chartered against vacuous guards; the property is held more strongly in a fresh subprocess by TC-ArgusAgent-PIPELINE-001-11. NO assertion was weakened, deleted or re-baselined. GATES RE-RUN, NOT CARRIED: python -m pytest (bare) 1441 passed / 0 failed / 0 error / 0 skipped (142.03s; 1439 -> 1441, +2); mypy Success on 74 source files; bandit 19 Low / 0 Med / 0 High, content-diffed against a 2bea92f worktree = 0 new, 0 removed, IDENTICAL SET (B105x6 B603x5 B404x4 B607x4); argus audit . = RELEASE_READY deep_ratio=64/177 blocking_findings=0 exit 0; git ls-files -- argus = 74 (unchanged); dogfood total LOC 21530 -> 21569 (+39, all partition 2) and ALL THREE partition_ids UNCHANGED (1a31dc9a9559 / 619a713d53ca / aaec0673cdcf); ledger append-only +84/0 this round, +250/0 vs 2bea92f. AC2.4 RE-DERIVED with iteration 1's one caveat ELIMINATED rather than attributed: the differing .argus/state record was first shown to differ in EXACTLY ONE LINE (report_dir), then the confound was removed by re-running both binaries against the SAME repo with the SAME --report-dir - stdout+stderr byte-identical, exit 0 both, all 2 report files and all 9 .argus/ files byte-identical with identical filenames. Commits: 7074c31 (fix) -> 58c8f6b (three dogfood artifacts regenerated THROUGH THEIR OWN RENDERERS at provenance 7074c31, merge-base ancestor-verified, argus/ diff empty, no hand-edits). The regeneration commit message was amended on the local unpublished tip because it stated 21540 where the artifact's prior figure was 21530 - corrected rather than left as an untrue committed figure. NO EGRESS OCCURRED: the two new tests substitute httpx.Client AND point at a .invalid host (RFC 6761), two independent fences. NOTHING PUBLISHED: git tag -l empty, origin/master 00c8d1b unmoved, HEAD 15 ahead, no push/tag/release/workflow_dispatch/index upload. New variance recorded: tests/test_deep_pass_wiring.py now 1104 lines (96 under the NFR-M1 cap) and tests/test_v1_commitment_closure.py 1412 -> 1419 (DF-12-2-C's figure now stale by 7; NOT edited - the ledger is append-only - the true number is in the story for 12.3 to size against). CI evidence: NOT ESTABLISHED (LOCAL, Windows / CPython 3.11.15). || PRIOR: code-review 12-2 iteration 1 (Sonnet 5): review -> in-progress, verdict CONCERNS. Independently re-derived on disk, not transcribed: python -m pytest (bare) 1439 passed / 0 failed (141.29s); mypy clean on 74 source files; bandit 19 Low / 0 Med / 0 High, and a byte-for-byte diff of the bandit JSON against a 2bea92f worktree shows the IDENTICAL 19 findings (same test_id/file/line/text) - ZERO new, confirmed by content not count. git ls-files -- argus = 74; 8a0bebc confirmed an ancestor of HEAD; git diff --quiet 8a0bebc HEAD -- argus/ empty. git tag -l empty; origin/master = 00c8d1b, unmoved. git diff --numstat 2bea92f HEAD -- deferred-work.md = 166 insertions / 0 deletions (append-only proven programmatically). AC2.4 byte-identity RE-DERIVED independently on a fresh synthetic repo across a 2bea92f worktree vs HEAD: stdout, stderr, exit code and every rendered report file byte-identical; the sole .argus/state/*.json filename difference was fully attributable to the different --report-dir paths used for the two runs. All three named judgement calls ADJUDICATED CORRECT: (1) AC5/AC5.4's re-grade-to-audited_shallow-via-grade_entry(claim_present=False) reading verified against the function's own code and confirmed live (a --deep-audit run with no provider configured against an otherwise-RELEASE_READY repo returns INSUFFICIENT_COVERAGE, deep_ratio=0, exit 3 - the downgrade demonstrably moves the FR16 input, no row/threshold/exit-code mapping touched); (2) advisory/non-blocking degradation findings verified structurally (is_verdict_blocking keyed on depth_supported, which _degradation_finding sets None) AND behaviourally (the adversarial repo the review brief asked for was constructed and run: the coverage downgrade IS the only signal and the operator is NOT misled - verdict/summary/depth-meaning callout all state the deep pass was requested but not completed); (3) the two removed production asserts left nothing load-bearing missing (mypy clean, explicit control flow in place, bandit-diff re-run confirms the RULE_DEGRADED_DEEP_READ rename held the security-scan delta at zero). ONE SUBSTANTIVE FINDING, not previously filed: [Medium, non-blocking] the deep pass's 'delivered' branch is provably unreachable through the real, unmodified OpenLLMAdapter - neither _dispatch_litellm nor _dispatch_httpx ever populates LLMRecording.structured_output (confirmed by reading both methods, independently corroborated by the PRE-EXISTING tests/test_open_llm_adapter.py:208 assertion that a real dispatch returns structured_output == ()), so deep_pass.py::_dispatch_one routes every real dispatch - success or failure - to REASON_EMPTY_RESPONSE before grounding is even checked. Fails safe (AC5.1's own 'malformed/empty response' mode is exactly what fires; no false claim is possible) and violates no written AC (every AC and test in this story is, correctly per Sec 0.3, restricted to injected fakes), but the positive half of the wired capability is dead code against the shipped adapter and this gap was not measured or filed alongside DF-12-2-A/-B/-C. Filed at review as DF-12-2-D (a [Review][Patch] item: file the DF entry + add a mocked-transport test) and DF-12-2-E (Low, PROVIDER_ENDPOINT_VARIABLES duplicates the adapter's _api_base derivation instead of deriving it - the [Review][Defer] item, already appended to deferred-work.md). Everything else reviewed and found sound: CLI wiring, derived forbidden-surface/entry-point populations, the AC2.3 environment gate, the AC2.5 ordering proof, the AC3 positive control, the FR36 not_built_refutations closure, the DOGFOOD_EXTERNALIZATION_GUARD repair, and every ledger ruling - no vacuous guard found among the ones this story added, no other engineering-principle violation found. VERDICT: CONCERNS (one Medium disclosure/filing gap on a pre-existing adapter limitation, not a defect in the code this story wrote; core delivery, safety properties and every measured claim otherwise hold). See the story's Review Findings section. || PRIOR: dev-story 12-2 (Opus 4.6): ready-for-dev -> review. FR36 DELIVERED: the deep pass is wired behind `--deep-audit` (store_true, default False, registered in the LOCKED invocation contract) — NOT a --passes token, NOT an env var, NOT a packaging extra, all three foreclosed by measurement. The LIVE FALSE DEEP CLAIM was reproduced on 2bea92f and closed RED-FIRST end-to-end with the final committed test code: the disclosure is now derived from WORK PERFORMED (three honest states), not from a CSV token. The seam is reached only via the injected LLMDispatchPort through a FUNCTION-LOCAL import — statically reachable (FR36 flipped not-built -> wired, PROVEN by the live import closure) yet runtime-inert (NFR-S6) — and AC3's POSITIVE CONTROL landed in the same change because that green is otherwise obtained by not executing. Forbidden-surface and entry-point populations DERIVED (the hand-written ones had omitted open_llm_adapter.py, the only module that can open a socket, and two thirds of the pipeline family); non-vacuity proven by GENERATING a new argus/audit module. Env-var population derived by ast from the adapter's own os.getenv calls (6). Honest degradation over a typed-error surface derived from the class tree, driven through the REAL pipeline entry point; an unconfigured provider REFUSES TO CONSTRUCT an adapter, so the fabricating _dispatch_httpx branch cannot reach the verdict (residual filed DF-12-2-B). Spend through the EXISTING --budget ceiling: no new module/threshold/schema/default. AC6 ANSWERED YES by independent re-derivation (bare `argus audit` reaches NOT_READY_FOR_RELEASE, exit 2, via vacuous_test RULE_AST) and COMMITTED as TC-ArgusAgent-VERDICT-001-30/-31 — the escalation branch does NOT fire, and the externalization guard's causal claim is refuted and repaired (honesty language strengthened, not softened). AC7.2's not_built_refutations FIRED on the live registry before the flip. Two local commits: 8a0bebc (implementation) + 64164bd (artifacts regenerated THROUGH THEIR OWN RENDERERS at provenance 8a0bebc, verified an ancestor of HEAD with an empty argus/ diff; no hand-edits). Suite 1439 passed / 0 failed / 0 error / 0 skipped (baseline 1418, +21). mypy clean over 74 files; bandit 19 low / 0 med / 0 high — ZERO new. DEFAULT RUN BYTE-IDENTICAL vs a detached 2bea92f worktree: stdout, stderr, exit code and all 14 artifact files (rollup 558c6852...). Ledger append-only proven by --numstat (166 insertions, 0 deletions); DF-12-1-A re-recorded to 12.3, DF-12-1-E did not fire, DF-10-2-A RULED OUT and ESCALATED to the Epic-12 retrospective (third consecutive story; needs a dated OPERATOR decision). NO EGRESS OCCURRED — every dispatch went through an injected FakeDispatch, zero tokens, no provider contacted. NOTHING PUBLISHED: git tag -l empty, origin/master unmoved at 00c8d1b. CI evidence: NOT ESTABLISHED (LOCAL, Windows / CPython 3.11.15).
  12-3-a-re-run-returns-the-recorded-result: done  # code-review 12-3 iteration 1 (Sonnet 5): review -> done. VERDICT: PASS. Full independent re-verification detailed at last_updated above (same date, same review). No unresolved decision-needed/patch item, no unresolved High/Medium issue, tests/mypy/bandit/publication-fences all independently re-run and green, all ACs met. One non-blocking [Review][Decision] item recorded in the story's Review Findings section (DF-12-3-C severity reconsideration for the Epic-12 retrospective). || PRIOR: dev-story 12-3 (Opus 5) 2026-08-13: in-progress -> review. DELIVERED FR27/NFR-D1: argus/cache/stage_memo.py is a production call site that derives the corrected 10.2 key and consults MemoStore around the deterministic detect/grade+orphan stage; argus.cache.memo_store FLIPPED to reachable from argus.cli (closure 58/74 -> 60/75), which is AC1.1. THE VACUITY GATE WAS THE PRIMARY DESIGN CONSTRAINT AND ALL FOUR CONTROLS SHIPPED. Control 2, the POISON POSITIVE CONTROL (TC-ArgusAgent-CACHE-001-82), is the assertion that decides the story: a validly enveloped, integrity-correct, schema-valid but DIFFERENT recorded result carrying a verdict-BLOCKING finding is written through the REAL store into the slot the next run reads (key read off the LIVE slot filename, never recomputed), and the run's verdict MOVES OFF RELEASE_READY - proving the served value reaches the verdict fold. CONTROL 3 PROVED THE THESIS BY MEASUREMENT: mutating MemoStore.lookup_stage to always MISS (the permanently-cold cache) left Epic 3.5's already-green re-run byte-identity test TC-ArgusAgent-PIPELINE-001-37 GREEN - it cannot see the defect at all - while 9 of this story's guards went RED; a second mutation that memoizes deep-pass output was REFUSED by the AC6.1 fence naming the two-model slot-collision hazard; both reverted with sha256 round-tripped in BOTH stated conventions (CRLF working tree AND LF git blob). MEMO_STORE_SCHEMA_VERSION 1->2 (payload widened to entries+findings+candidates; findings-only would re-run the same loop and save nothing while every byte-identity test passed). CACHE_KEY_SCHEMA_VERSION UNCHANGED at 3 (DN-4) and NO CLI FLAG (DN-3). SCOPE HELD, NOT WIDENED: memoization covers the deterministic stage only; the deep pass runs downstream and is never served, so PRD:501 is NOT delivered - filed as DF-12-3-A and disclosed at four sites with Story 12.4 named explicitly. AC6.3 BOTH HALVES: FR27 re-derived delivered-differently -> wired (superseded text kept), AND the refutation hole closed by delivered_differently_refutations, a fourth direction as narrow as 12.2's not_built one, proven RED-FIRST with the FINAL code against the REAL pre-fix registry state. DF-12-1-B's stated trigger independently re-confirmed MEASURABLY FALSE by executing the guard's own code. GATES on d040cad, all LOCAL Windows/CPython 3.11.15 (AI-E10-1 carried, NOT re-taken, no AC depends on CI): bare pytest 1466 passed / 0 failed / 0 error / 0 skipped (baseline 1441); mypy clean over 75 files; bandit 19 before / 19 after CONTENT-diffed = 0 NEW; NFR-M1 green with NO new exemption (the sweep caught the new guards at 1388 lines and they were SPLIT on a cohesion boundary, not shaved); cold-run A/B vs a detached 58c8f6b worktree at a SHORT path is byte-identical across exit code, stdout, stderr and all 10 artifacts, the SOLE difference being the cache slot this story exists to create; deferred-work.md append-only PROVEN by --numstat (203 insertions, 0 deletions); argus audit . = RELEASE_READY deep_ratio=65/181 exit 0. Dogfood artifacts regenerated THROUGH THEIR OWN RENDERERS in a SEPARATE commit at truthful provenance fab36e7 (ancestor of HEAD, argus/ unchanged since), no hand edits; population 74->75, LOC 21569->22126. Reds NOT mine, attributed: test_grammar_runtime_validation -125 (three toolchain legs shared one repo's cache; fixed by wiping between legs - the sanctioned lever - with NO assertion weakened) and a PRE-EXISTING test_secret_containment isolation-collection bug proven byte-identical to 58c8f6b and filed as DF-12-3-C rather than fixed here. Every §F ledger item ruled IN or OUT with reasons; AC5.4 ruled OUT LOUD as option (b), filed DF-12-3-B. NOTHING PUBLISHED: git tag -l EMPTY, origin/master UNMOVED at 00c8d1b. Prior dev-story note follows. # ready-for-dev -> in-progress. Baseline RE-MEASURED on this tree, not inherited: bare python -m pytest = 1441 passed / 0 failed / 0 error / 0 skipped in 137.81s; HEAD 58c8f6b; origin/master UNMOVED at 00c8d1b; git tag -l EMPTY; git ls-files -- argus = 74; argus audit . = RELEASE_READY deep_ratio=64/177 blocking_findings=0 assessed_deep_ratio=4/5 scope=application held_out=97 exit 0; .argus/cache/ holds 0 files. Closure probe re-run against the guard's OWN builder: 74 graph nodes, 58 reachable from argus.cli, argus.cache.memo_store reachable=False (the flip this story makes), argus.cache.key reachable=True. FALSE-TRIGGER PROBE REPRODUCED: reachability_refutations(FR27 delivered-differently, memo_store REACHABLE) == () while the library-seam control fires — DF-12-1-B's stated trigger is measurably false, exactly as the story records. Prior create-story note follows. # backlog -> ready-for-dev 2026-08-13 (create-story, Opus 5; story file: stories/12-3-a-re-run-returns-the-recorded-result.md). Seven ACs, eight tasks. Full measured record at last_updated above. HEADLINES: (1) BASELINE RE-MEASURED BY EXECUTION — 1441 passed / 0 failed / 0 error / 0 skipped, NO sanctioned red, 74 argus files, pipeline.py 1007 lines (193 headroom, NOT 12.1's 256 — 12.2 spent 63), dogfood artifacts CURRENT at 7074c31, .argus/cache/ holds ZERO entries so the zero-migration premise HOLDS. (2) DF-12-1-B's STATED TRIGGER IS MEASURABLY FALSE, proven by running the guard's own code: FR27 is disposed `delivered-differently`, NOT `library-seam`, and reachability_refutations never refutes that disposition — so wiring the store turns NOTHING red and the registry would go on asserting "the memoization MECHANISM is unwired ... deferred to Story 12.3" with no test able to notice. AC6.3 corrects the entry AND closes the refutation hole. (3) STALE PREMISE: key.py:100 and architecture.md 395-396 infer "no production caller" from "pipeline.py imports neither module" — 12.2 made argus.cache.key production-imported (deep_audit.py:49, ast_index.py:66) and reachable from argus.cli; the substantive claim survives (reachable != derives-a-key, which is still ZERO) but the sentence is stale — AC7.4 corrects both sites. (4) THE --deep-audit QUESTION IS SETTLED BY MEASUREMENT: the deep pass runs DOWNSTREAM of the hook (pipeline.py:416-430), and the key's model_checkpoint/prompt_template_version are FIXED SENTINELS that do not vary with the model dispatched, so memoizing deep output would let two DIFFERENT MODELS collide on ONE slot. Memoization is SCOPED to the deterministic stage, the exclusion is FENCED (AC6.1), and the honest residual — PRD:501 is NOT delivered, the deep component of a verdict is not reproducible via this store — is FILED (AC6.4) and DISCLOSED (AC6.5) with a note for 12.4, never implied away. (5) PROPERTY IS STRICTER THAN AND NOT COVERED BY EITHER PREDECESSOR: 3.5's -37 varies the tree, 12.2's AC2.4 varies the binary, and BOTH have run 2 execute the SAME code; 12.3 varies nothing and run 2 takes a DIFFERENT path (serve vs compute). memo_store is NOT in the 58-module closure from argus.cli and no test exercises a cache read/write on the pipeline path. (6) VACUITY IS THE DOMINANT RISK: a re-run byte-identity test is GREEN ON THIS TREE TODAY WITH NO CACHE — four controls are mandatory, above all THE POISON POSITIVE CONTROL (a validly-enveloped but DIFFERENT entry must CHANGE the verdict; it is the only assertion a permanently-cold cache cannot pass). (7) SCOPE RULED: unit-level hook, payload extended to entries+findings+candidates with MEMO_STORE_SCHEMA_VERSION 1->2, NO CLI flag, CACHE_KEY_SCHEMA_VERSION stays "3". Original planning note follows. # 🟠 · Engineering. DELIVERS FR27/NFR-D1 — adds NO capability. argus/cache/memo_store.py exists; pipeline.py never imports it. DEPENDS ON STORY 10.2: the key folds ONE grammar_version from tree-sitter-python while the index parses 10 languages; 10.2 makes it per-grammar and explicitly declines to wire the store. Wiring first persists a key wrong for 9 of 10 languages and costs a CACHE_KEY_SCHEMA_VERSION bump + migration to undo. Hit and cold run must be BYTE-IDENTICAL.
  12-4-every-outcome-names-its-next-action: done  # code-review 12-4: review -> done. VERDICT: PASS.  # dev-story 12-4: in-progress -> review. FR37 DELIVERED: every terminal outcome (RELEASE_READY, NOT_READY_FOR_RELEASE, INSUFFICIENT_COVERAGE, AUDIT_FAILED) names why it was reached and its next action. Added dynamic 3-population ingestion boundary disclosure from AUDITABLE_SUFFIXES. INSUFFICIENT_COVERAGE names specific unmet gates with measured figures. FR16 decision table immutable. Integrated DF-8-3-A, DF-10-4-B, DF-11-4-D / AI-E11-6, DF-12-3-A. mypy clean 75, bandit 0H/0M/19L, full pytest suite green. || PRIOR: FR37 · 🟠 · Engineering. Every terminal outcome names WHY and the NEXT ACTION, enumerated over RELEASE_READY / NOT_READY_FOR_RELEASE / INSUFFICIENT_COVERAGE / AUDIT_FAILED, test FAILS on an unenumerated outcome. INSUFFICIENT_COVERAGE is load-bearing: name the SPECIFIC unmet gate (floor / ratio / critical subsystem) with measured figures. GOVERNS EXPLANATION, NEVER CLASSIFICATION — FR16's table untouched, asserted by test. Must reflect 12.2's measurement, not imply a blocking verdict that is not reachable. ADDED 2026-08-10b — THE INGESTION BOUNDARY: name the file classes NEVER OPENED, not only the coverage ratio within those that entered. Motivating incident: `argus audit .` returned RELEASE_READY deep_ratio=57/149 held_out=76 exit 0 while this repo held an action.yml shell injection, 5 non-importable wheel modules and a false README claim — none a missed defect, because AUDITABLE_SUFFIXES (argus/shared/source_languages.py:80) ingests only the 10 SOURCE languages, so .yml/.md/.toml were NEVER OPENED. Three populations by construction: never-ingested / ingested-but-held-out / assessed. NO RATIO CAN DISCLOSE THE FIRST. Derive from AUDITABLE_SUFFIXES, never hand-list; assert on RELEASE_READY SPECIFICALLY (the false-green direction, inversion F1).
  12-5-default-install-grounds-languages-it-claims: done  # code-review 12-5 iteration 1 (Sonnet 5): review -> done. VERDICT: PASS. Full independent re-verification detailed at last_updated above (same date, same review). No unresolved decision-needed/patch item, no unresolved High/Medium issue, tests/mypy/bandit/uv-lock all independently re-run and green (pytest 1477/0/0/0 incl. both dogfood-currency guards now cleared post-second-commit; mypy clean 75; bandit 19/0/0), all four ACs independently verified met against the real diff. One noise-level dismiss recorded (a "exactly one renders" vs. "at most one renders" phrasing overclaim confined to internal dev-record prose, never in a user-facing artifact).  # PRIOR: dev-story 12-5: ready-for-dev -> in-progress -> review. NFR-P3 DELIVERED. AC1: the 9 non-Python tree-sitter grammars promoted to [project.dependencies] at the specifiers the extra already declared (uv.lock consistent); `pip install argus-agent` now grounds all 10 supported source languages with nothing to discover. AC3: [project.optional-dependencies] languages RETAINED as a backward-compat alias and pinned equal to the default requirements specifier-for-specifier, so it can never become a second source of truth. AC2 was NOT a no-op audit: the remedy renderer already named the package, but the TRIGGER fired only when NOTHING parsed (`if eligible: return []`), so a polyglot repo whose Python parsed learned nothing about its failed Go grammar - DF-10-4-A, handed to this story BY NAME by generator.py:398-401 and test_grammar_diagnosis -002-29. Closed with a SEPARATE surface, deliberately not by widening 10.4's trigger (that callout's sentence 'No file could be parsed' is FALSE for a partially-parsed repo): new generator._render_grammar_downgrade_section names each downgraded file, the depth it reached and the package that would have grounded it; new plain_english.render_grammar_downgrade_summary states the same facts once per failure class in the human register, REUSED by the report rather than re-authored. Exactly one of the two surfaces renders per run (both directions pinned). Exhaustive-with-raise per DF-10-4-E; classification always through the shared classify_reason, never a prefix slice. AC3 docs: README/CHANGELOG/architecture.md/pyproject.toml reconciled with superseded wording STRUCK not deleted (§3.4) - the partial start in the tree had DELETED architecture.md's two open-decision paragraphs and had left the suite RED (emptied _EXEMPT_BY_DESIGN vs -26's `assert _EXEMPT_BY_DESIGN`); both corrected, -27 re-armed on the struck sentences. Redundant tests/test_default_install_languages.py (invented PACKAGING-001 area) deleted, assertions moved and strengthened into the AC4-named home. AC4 guards: TC-ArgusAgent-DOCS-001-61, TC-ArgusAgent-REPORT-002-35/-36/-37. GATES: mypy clean 75 files; bandit 19/0/0 content-compared against the stashed tree = 0 new; all touched files under the NFR-M1 1200 cap (test_grammar_runtime_validation.py at 1148 - 52 lines of headroom, split it next). SCOPE HELD: argus/cli.py untouched - the grammar diagnosis does not travel on AuditVerdict and epics.md:2431 gives Story 12.8 the operator-error ('missing grammar') CLI surface by name; no unwired keyword was added to render_ship_readiness (12.4's degraded_conditions is already in that state, DF-10-4-B). tree-sitter<0.26 bound RETAINED - pyproject.toml:33 recorded that widening as owned by 12.5; decided: it stays (NFR-P3 is about which grammars ship, not the core's range). !! NOT GREEN, STATED NOT HIDDEN: test_dogfood_plan/-proof artifact-currency guards fail on the physical-LOC figure because argus/ grew; scripts/regenerate_dogfood_artifacts.py was RUN and REFUSED BY DESIGN on a dirty argus/ tree (the DF-10-4-D bootstrap). Nothing was loosened or xfailed. REQUIRED FOLLOW-UP IN ORDER: (1) commit the argus/ delta, (2) re-run the regeneration script, (3) commit the 3 regenerated artifacts as a SEPARATE commit - then both guards pass.  # create-story 12-5: backlog -> ready-for-dev. Story file: stories/12-5-default-install-grounds-languages-it-claims.md. NFR-P3 · 🟡 · Engineering. Reconciles tree-sitter language grammar dependencies and output disclosures so all 10 supported source languages are grounded by default without requiring an optional extra.
  12-6-coding-agent-can-run-audit-and-read-verdict: done  # code-review 12-6 iteration 1 (Sonnet 5): review -> done. VERDICT: PASS. Independently re-derived on disk, not transcribed: pytest 1492 passed / 0 failed / 0 error / 0 skipped (junit-xml confirmed); mypy clean 78 source files; bandit 19 Low / 0 Medium / 0 High, matches claimed baseline exactly; a fresh `python -m build` measured wheel 83 entries / sdist 82 files, matching README's re-derived figures; the official `mcp` PyPI wheel's METADATA was downloaded and inspected directly, confirming starlette/uvicorn/sse-starlette are REQUIRED (unconditioned on any extra) -- DN-1's SDK-refusal reason is factually verified, not merely asserted. test_v1_commitment_closure.py's own build_import_graph/reachable_from_any were re-run directly: 78 modules, 392 edges, 63 reachable from the [project.scripts] union (60 from argus.cli alone) -- all above both the old (55/150/35) and new (58/290/47) floors, confirming no floor was loosened; the union adds exactly the three argus.mcp.* modules and no existing library-seam (FR23/24/26/29 confirmed still library-seam against the live graph). No transcribed copy of the FR34 constant exists under argus/mcp/** (grepped directly) -- every render routes through render_instrument_disclosure(INSTRUMENT_STATUS). All seven ACs independently verified against the real diff: AC1 protocol surface + closed version enum with exhaustive raise; AC2 no-listener (real process, socket sentinel) + import-isolation extension + inward-only dependency arrow + no credential-shaped schema property; AC3 parity by construction (build_tool_argv -> cli.build_parser/resolve_passes/build_request, DN-8 divergence demonstrated live: a hand-built AuditRequest yields coverage_scope=None where the real MCP path narrows to scope=application held_out=1); AC4 stdout purity (contextlib.redirect_stdout/stderr wraps the entire run_audit call, proven non-vacuous by an injected print() landing on stderr); AC5 FR34 disclosure on both tools/list and every result, -49's corrected ast routing closure re-derived and confirmed; AC6 every falsified gate corrected not loosened (confirmed file by file); AC7 AR4 no-float structural via canonical.dumps (CanonicalSerializationError demonstrated), NFR-M1 all files under 1200 (DF-12-1-B exemption re-measured 1685, confirmed by wc -l), NFR-R1 typed failure -> isError:true with CLI's own wording, server keeps serving. No unresolved decision-needed/patch item, no unresolved High/Medium issue, nothing dismissed as noise. || PRIOR: dev-story 12-6: ready-for-dev -> review. FR35 HALF ONE DELIVERED: `argus/mcp/` (PURE protocol.py + IMPURE server.py) ships behind the fourth console alias `argus-mcp = argus.mcp.server:main` in the SAME distribution — JSON-RPC 2.0 over stdio, BOTH protocol eras (legacy `initialize` + modern `server/discover` with per-request _meta version checking against ONE closed constant rendered exhaustively-with-raise), exactly ONE tool `audit_repository`. ZERO new dependencies: the `mcp` SDK stayed refused (DN-1) and the argus.* NOT-IMPORTS fastapi/uvicorn/starlette gate is GREEN with all three new modules appended to _MODULES_UNDER_GUARD. PARITY BY CONSTRUCTION (AC3): inputSchema derived from build_parser and the request built through the CLI's own resolve_passes/build_request, so CLI defaults govern — proven by driving BOTH real entry points over one fixture repo, with the DN-8 divergence DEMONSTRATED (a direct AuditRequest yields coverage_scope=None where the CLI narrows to scope=application held_out=1). AC4: stdout redirected to stderr for the duration of any audit, proven non-vacuous by injecting a print() mid-audit at the real seam and asserting the noise lands on stderr. AC2.1: no-listener asserted BOTH statically over argus/mcp/** and against the REAL PROCESS with a socket.bind/listen sentinel that is itself shown to fire. GATES CORRECTED, NONE LOOSENED (AC6): test_v1_commitment_closure entry points now DERIVED from [project.scripts] with the reachability closure unioned and floors re-measured 58/290/47 (the old prose 'it is the ONLY entry point' was FALSE with nothing red); FR35 _Delivery flipped not-built -> wired naming argus/mcp/server.py + `def serve(` AND the residual 12.7 half so it cannot over-claim; NO library-seam flipped (FR23/24/26/29 verified still unreachable); _CONSOLE_SCRIPTS derived + parse_failure dispatched on the alias TARGET; _MCP_DISCLOSURE_SURFACES populated and -49's NEVER-EXECUTED assertion CORRECTED from a source-text substring check (which would have forced a transcribed copy of the FR34 constant into argus/mcp/** — the exact AI-E9-7 drift it exists to prevent) to a DERIVED ast routing closure with a non-vacuity floor and both-direction positive controls; FORTHCOMING marker deliberately LEFT INTACT (removing it would claim 12.7's delivery); README's pre-existing 80-vs-77 self-contradiction repaired by deleting the second remembered figure rather than correcting it, and all measured figures re-derived (wheel 83 / sdist 82 / 78 of 78 importable); architecture.md's two FR35 sites struck-and-resolved (§3.4). New verification area ArgusAgent-MCP-001 (-01..-15), every guard written to AI-E11-1 with a demonstrated defect. DN-9: the wire routes through argus.store.canonical.dumps — the ONE serializer — so AR4's no-float is STRUCTURAL on this transport, not merely tested. VERIFICATION: pytest 1490 passed / 2 failed, and the two are ONLY the DF-10-4-D dogfood artifact-currency guards, red BY DESIGN until the argus/ delta is committed and the artifacts regenerated (nothing loosened, skipped or hand-edited); mypy clean 78; bandit 19L/0M/0H with a `git archive HEAD` control proving NO NEW finding. Story file: Dev Agent Record §1-§5. ||  # create-story 12-6 (2026-08-15): backlog -> ready-for-dev. Story file: stories/12-6-coding-agent-can-run-audit-and-read-verdict.md. SCOPE = FR35 HALF ONE ONLY — the MCP stdio server as a fourth console entry point (argus-mcp -> argus.mcp.server:main) in the SAME distribution, plus exactly ONE tool (audit_repository). The packaged assistant command assets are 12.7's; operator-error diagnosis wording is 12.8's by name (epics.md:2431); publishing is 12.9's. DECIDED (DN-1): the official `mcp` Python SDK is REFUSED — it hard-requires starlette/uvicorn/sse-starlette, which would break the argus.* NOT-IMPORTS fastapi gate this story's own AC2.2 requires to still pass, and AR2's zero-new-dependency rule; the JSON-RPC layer is hand-rolled on stdlib json, synchronously, with no asyncio (the adapter may not introduce a concurrency model of its own). DECIDED (DN-2): DUAL-ERA — legacy `initialize` (what installed hosts speak) AND modern `server/discover` + per-request _meta version checking (the 2026-07-28 revision is stateless and has no handshake); the supported-version set is ONE closed constant rendered exhaustively-with-raise. PARITY BY CONSTRUCTION (AC3): the tool derives its inputSchema from and builds its request through argus/cli.py::build_parser, so the CLI defaults govern — constructing AuditRequest directly would inherit coverage_scope='repository' against the CLI's 'application' (Story 10.3 / DN-8, pinned by TC-ArgusAgent-CLI-001-37b) and silently assess a different population. AC4: stdout carries JSON-RPC and NOTHING else — the CLI prints its FR18 summary to stdout today, so the adapter redirects stdout to stderr for the duration of an audit and a guard parses EVERY emitted line. GATES THIS STORY FALSIFIES, to be CORRECTED not loosened (AC6): test_v1_commitment_closure.py's _ENTRY_POINT ('argus.cli ... is the ONLY entry point') -> derive the entry-point set from [project.scripts] and re-measure the non-vacuity floors, and flip FR35's `not-built` _Delivery (whose reason 'argus/mcp/** does not exist on this tree' goes false) WITHOUT over-claiming 12.7's half — note it names no seam_modules, so nothing fires automatically; test_instrument_disclosure.py's _MCP_DISCLOSURE_SURFACES and -49, whose NEVER-EXECUTED registered-surface loop demands the disclosure constant be TRANSCRIBED into argus/mcp/** (the AI-E9-7 drift the FR34 regime exists to prevent) and must be corrected to an ast routing closure; _CONSOLE_SCRIPTS; _MODULES_UNDER_GUARD (append, never fork); README's 'three console aliases' + wheel-figure paragraphs (re-measurement found the README ALREADY self-contradicts: 80 entries at :154 vs 77 at :224). The FORTHCOMING marker STAYS — this story ships no data asset. Adding modules re-trips the DF-10-4-D dogfood-currency two-step: commit argus/ first, regenerate, commit artifacts separately. Carries the FR34 disclosure on every verdict-bearing result AND in the tools/list description. Publishes nothing.  # PRIOR (planning, 2026-08-10b): FR35 · 🟠 · Engineering Lead. MCP server, STDIO ONLY, as an entry point in the SAME distribution (inherits release workflow, version, gate evidence — not a separate channel). No network listener, no bound port, no HTTP stack, no credentials (--deep reads its credential through the existing adapter's env contract, never through this surface). argus.* ⊬ fastapi and ADR #20 hold unchanged. ADAPTER LAYER ONLY: same AuditRequest -> AuditVerdict as the CLI, dependency arrow inward. VERDICT PARITY pinned by test. Carries the FR34 disclosure.
  12-7-commands-the-readme-promises-actually-exist: done  # code-review 12-7 iteration 1 (Sonnet 5): review -> done. VERDICT: PASS. Independently re-ran the full gate set rather than trusting the Dev Agent Record: python -m pytest -q = 1505 passed / 0 failed (both commits applied, so the DF-10-4-D artifact-currency six-red class the dev pass reported is gone); mypy argus clean on 83 files; bandit -r argus -q = 19 LOW, and a fresh `git archive HEAD argus` control run reproduces the IDENTICAL 19 LOW / distribution (B105x6/B404x4/B603x5/B607x4); bandit -r argus/commands argus/assets -q = ZERO findings. Built a REAL wheel + sdist with `python -m build`: all three .md command assets present in BOTH (91 wheel entries / 90 sdist files), matching README:154-156's re-derived figures exactly. Reasoned through the three corrected guards against the attacks they exist to catch rather than reading the docstrings' word: -56's delivered branch (traced the `mechanism_ships` code path - it no longer returns early, asserts set equality both directions with a non-vacuity floor, and the floor genuinely catches "delete the README commands while the wheel ships assets"); derive_arguments (confirmed the default parameter is None = closure over every _SubParsersAction.choices member at all five production call sites, with "audit" appearing only in three synthetic-parser positive controls); SECURITY-001-30 (confirmed it now closes over every tracked .py file via _repository_python_sources(), not one named file). Deletion honesty (risk area 1): grepped the whole tree for adapters/ references - zero live references remain, every hit is struck-not-deleted historical record; all three installer scripts (install.sh/install.ps1/uninstall.sh) delegate to `argus install-commands` and contain no copy verb on a live line; DF-11-5-C closed and DF-3-4-A left untouched (append-only) in deferred-work.md, resume/subsystem/repo/architecture removals correctly cite DF-3-4-A/DF-10-5-C/Story 12.8 rather than silently vanishing. Test-file split (risk area 3): assert-count diff old file (33) vs new test_invocation_contract.py (35) + invocation_sources.py (2) = 37 - nothing dropped, more added; all 9 original test_ functions present by name; both files now under the 1200-line NFR-M1 ceiling (968/319). DN-7/FR34 disclosure (risk area 6): grepped every asset under argus/assets/** for the rendered disclosure text - absent from all three; drove the REAL install_commands() into a fixture tmp_path - disclosure text present in all three written files; zero mcp/model-context-protocol tokens under argus/commands/** or argus/assets/**, matching DN-8. Set-equality (risk area 7): README/audit-commands.md/audit-skill.md all publish the identical derived set (/argus-audit, /argus-audit-security, /argus-audit-report). Installer correctness (risk area 5): install-commands confirmed a sub-command (subparsers.add_parser), not a fifth [project.scripts] alias - only four aliases remain, unchanged targets; containment tested both halves (pure name-validation pre-join, impure realpath re-check post-join); --remove keyed on marker not filename (adversarial variant: same-named user file survives, un-overwritten); no absolute host path in rendered messages; "no host detected" exits 1 via cli.main directly. No unresolved decision-needed/patch finding, no unresolved High/Medium issue, no gate loosened - three were corrected with reasoning recorded in both test docstrings and the Dev Agent Record. See story's Review Findings section for the full verification trail. PRIOR: dev-story 12-7 (RESUMED after a transport-killed run): in-progress -> review. FR35 half two SHIPPED. Packaged command assets as DATA under argus/assets/commands/ (three: argus-audit, argus-audit-security, argus-audit-report) present in the built WHEEL and SDIST with pyproject.toml untouched (flit_core needs no configuration - verified by execution, not assumed); the documented step is a SECOND SUB-COMMAND per DN-1, argus install-commands [--host ...] [--dest ...] [--dry-run] [--remove], with a PURE fold in argus/commands/installer.py over a CLOSED verification-gated host registry in argus/commands/hosts.py and a thin impure write - no new console alias, so the [project.scripts] closures, the reachability floors and 12.6's audit-scoped tool schema are untouched. INHERITED from the killed run and JUDGED rather than trusted: package shape, three assets and host registry KEPT; the two format markers promoted from prose to constants so the installer could not spell them twice; installer.py written from scratch (it did not exist); everything staged so the sdist carries it. install.sh/install.ps1/uninstall.sh now DELEGATE and copy nothing (both previously created ~/.claude/commands/ and copied BESIDE it; uninstall removed nothing); adapters/** removed with its record struck-not-deleted (DN-10), including the codex-cli stub that published `argus --budget 500`, which the real parser rejects and which now lives on as a positive control. Three disagreeing published lists (README 7, audit/commands.md 10, audit/skill.md 6) reconciled to set-equality with what ships, derived by test over a scanned surface population; 12-vs-8-vs-4 report figures, the seven-host sentence, the zero-data-assets paragraph, the adapters/install.sh rows and the clone-install block all corrected struck-not-deleted; FORTHCOMING marker removed. TWO NEVER-EXECUTED GUARDS CORRECTED, NOT LOOSENED: -56's delivered branch returned after one assertion (so it could have been passed by DELETING the commands from the README) and now asserts set equality both ways with non-vacuity floors and no pragma; derive_arguments was audit-scoped at all five call sites (so this story's four flags would have been accepted and specified nowhere, whole suite green) and is now a closure over every sub-command with a collision check - both demonstrated RED at the real seam first. A third found in passing: SECURITY-001-30's single-resolver claim was file-scoped, now repository-scoped. New area TC-ArgusAgent-ASSETS-001-01..-13 in tests/test_command_assets.py. FR34 disclosure RENDERED at install time from the one constant and asserted absent from every committed asset AND present in every written one. Containment proven both ways (.., absolute name, symlinked config dir). test_invocation_contract.py crossed the NFR-M1 ceiling at 1213 and was SPLIT along its own cohesion boundary into tests/invocation_sources.py with every import path preserved - not shaved. GATES: mypy argus clean (83 files); bandit -r argus 19 LOW identical to a stashed-argus/ control, no new finding, ZERO in the new modules, no suppression; pytest 1499 passed / 6 FAILED. The six failures are ONE class - the DF-10-4-D committed-artifact currency guards (test_dogfood_plan.py x3, test_dogfood_proof.py x3), red because argus/ went 78 -> 83 modules. The remedy script was RUN and REFUSED BY DESIGN pending the commit; NOTHING was loosened, xfailed, skipped or hand-edited. Remedy is the two-step: commit the argus/ delta, run scripts/regenerate_dogfood_artifacts.py, commit the regenerated artifacts separately. Filed: DF-12-7-A (one verified host ships; the other five are one reviewed registry entry each), DF-12-7-B (a stale installed asset is detectable and re-installable but nothing TELLS the user - owner 13.3). CLOSED: DF-11-5-C. Nothing published (12.9's); no dependency, extra or build-backend key added. PRIOR: create-story 12-7: backlog -> ready-for-dev. Story file: stories/12-7-commands-the-readme-promises-actually-exist.md. FR35 half two (12.6 delivered half one and deferred this half by name in four places). Scope: packaged command assets under argus/assets/commands/ (architecture.md:970 already reserved the path), a documented install step that actually places them (argus install-commands — a SECOND CLI sub-command, not a fifth console alias: cli.py:29 records the audit sub-command as "an additive seam for future ones"), every shipped command proven to resolve through the REAL parser, and set-equality between what ships and what is published. PREMISES RE-MEASURED on ddeb30d — THREE corrections to the original note below: (a) its line numbers are stale, the seven commands are now README:264-279 under the FORTHCOMING block at :264; (b) pyproject ships FOUR console aliases now, not three (12.6 added argus-mcp); (c) "NO registration mechanism exists" is true of the DISTRIBUTION but FALSE of the repository — install.sh, install.ps1, uninstall.sh and six adapters/** stubs exist and are BROKEN (both installers create ~/.claude/commands/ and then copy the assets BESIDE it; uninstall.sh leaves every copied file behind), so this story is a repair plus packaging, not greenfield. Also measured: THREE published command lists that disagree (README 7 / audit/commands.md 10 / audit/skill.md 6); FOUR of the seven cannot resolve to any invocation (repo, architecture, subsystem, resume — resume is DF-3-4-A, open since Story 3.4, and building a CLI entrance is fenced to 12.8, so they are REMOVED not implemented); a seventh host (RooCode) claimed at README:31 with no adapter directory at all; the report count published as 12 (README:33, :277) vs 8 (:327) vs the FOUR generate_reports actually renders. TWO NEVER-EXECUTED guards named for correction, both the class 12.6 found twice: TC-ArgusAgent-DOCS-001-56's delivered-state branch carries "# pragma: no cover" and RETURNS after one assertion, so nothing would hold the documented set to the shipped set once assets ship; and derive_arguments() in test_invocation_contract.py is scoped to the "audit" sub-command alone, so a second sub-command's flags would be specified nowhere and -35/-37/-38 would not notice. Fences held: no --help prose or operator-error diagnosis (12.8), nothing published (12.9), no new assurance capability. ORIGINAL SPRINT-PLANNING NOTE, kept as written: FR35 · 🟡 · Engineering. README:138-150 claims slash-command registration in Claude Code/Cursor/Cline and lists SEVEN; pyproject:59-62 ships THREE console aliases to one main and NO registration mechanism. Ship packaged command assets (DATA, not code — no execution path of their own) + a documented install step. Remove 11.5's forthcoming marker. Any documented-but-undelivered command is REMOVED in the same change; shipped set == documented set, asserted by test.
  12-8-the-tool-explains-itself: done  # code-review 12-8 iteration 2 (Sonnet 5, RE-REVIEW, final iteration): review -> done. VERDICT: PASS. Scope was narrowed to (1) verifying the ONE Low finding from iteration 1 is genuinely resolved, not reworded away, and (2) a no-regression sweep. Read `git diff 6efa306..de05dec` in full (3 files, docs-only, +94/-6). Verified BY EXECUTION that every claim the new opening sentence makes is actually enforced: test_TC_ArgusAgent_DOCS_001_63 (verdict vocabulary) and -64 (exit codes) both pass and genuinely check the page against the live Verdict enum / exit_code_for_verdict; extract_documented_invocations() genuinely extracts and parses all three (and only the three) `argus ...` lines via the docs/*.md glob + _CONSOLE_SCRIPTS filter -- the `pip install` line's first token is `pip`, not a console script, so it is correctly EXCLUDED from "every `argus ...` command line", never overclaimed. "The rest is prose" is accurate: grepped test_release_surface_honesty.py and confirmed no guard checks anything else on the page besides the DOCS-001-62/63/64/65/66 properties. The opening and the install caveat at :32-35 now AGREE (they contradicted before). BOTH REJECTED ALTERNATIVES JUDGED SOUND, NOT RATIONALISED AVOIDANCE: widening the derivation to the pip-install line would need to resolve a real git+...@v0.1.0 ref, and that tag is explicitly Story 12.9's fence (this story's own "What it is NOT" table, epics.md:2446-2473), not 12.8's to create, and this repo's tests are independently established network-free (test_mcp_server.py: "Offline, deterministic, no network"; test_workflow_input_containment.py: "no network"), so a guard could only pattern-match the URL's shape -- transcription wearing a test's clothes, exactly what AI-E9-7 forbids. Pinning the new sentence's prose in a guard was also correctly rejected: it would compare prose against prose and go RED on a rewording rather than a falsehood, the identical reasoning this story's own DN-2 already used to reject "a registry of expected default sentences (a second hand-list -- the instrument that has been wrong three stories running)". NO REGRESSION: full suite re-run with --junit-xml: tests="1527" errors="0" failures="0" skipped="0" (parsed from the XML, not the terminal), including the three DF-10-4-D dogfood-currency tests, unmoved (correct: this change touched no argus/** composition); mypy argus clean, 83 source files; bandit -r argus -q 19 Low / 0 Medium / 0 High, byte-identical to iteration 1's count. Story's own [Review][Patch] item is checked off with a dated resolution note and a RE-REVIEW CONFIRMATION note added beneath it; sprint-status.yaml's STATUS DEFINITIONS block and full prior-round history verified intact by direct read. No unresolved decision-needed or patch item remains; no new finding raised. All nine ACs re-confirmed met. || PRIOR: dev-story 12-8 fix iteration 1: in-progress -> review. The ONE Low [Review][Patch] finding is RESOLVED: docs/first-run.md's opening claim ("Everything on it is checked by a test against the code, not transcribed from it") was a published claim no test backed - contradicted 20 lines later by the page's own install caveat - so the prose was NARROWED to exactly what the guards enforce (verdict vocabulary, exit codes, every `argus ...` command line: DOCS-001-63/-64 and -28/-39), plus a sentence stating the rest is prose and that an unexercised documented command is flagged AT that command. Widening the derivation instead was rejected on principle: the unchecked line is the pip install at tag v0.1.0, which 12.9 alone may create and whose resolution needs network egress the test contract forbids. Docs-only, 4 lines, no argus/** composition change, so the DF-10-4-D artifact-currency guards stayed GREEN with no regeneration. Gates: pytest -q 1527 passed / 0 failed / 0 errors / 0 skipped; mypy argus clean (83 files); bandit -r argus -q 19 Low / 0 Medium / 0 High (control-matched). Nothing loosened, xfailed or skipped; published nothing (12.9's fence). || PRIOR: code-review 12-8 iteration 1 (Sonnet 5): review -> in-progress. VERDICT: CONCERNS. Independently re-verified rather than trusted: pytest -q = 1527 passed / 0 failed (junit-xml confirmed tests="1527" errors="0" failures="0" — the three DF-10-4-D dogfood-currency tests are green after the 6efa306 regeneration commit); mypy argus clean on 83 files; bandit -r argus -q = 19 Low / 0 Medium / 0 High, matching the Dev Agent Record's claimed stashed-control count exactly, confirming the ...MARKER/...SELECTOR rename genuinely cleared B105 with zero new findings and no # nosec added anywhere. Verified by EXECUTION, not by reading: `argus audit . --passes securty` now refuses with exit 1 (was RELEASE_READY exit 0 on 2f84a0b, every pass silently disabled — the false-green channel is closed on every reachable path: CLI, the MCP audit_repository tool (confirmed it drives the same build_parser().parse_args and rejects with INVALID_PARAMS, not a fabricated verdict), the shipped command assets, action.yml, and the workflow file); `--skip-pass securty` and `--reports vacuous-tests` both now exit 1; `argus audit . --budget 1.5` now exits 1 (was 2) with build_parser().parse_args left byte-identical (the mapping lives in main() only, confirmed by reading); `--help` renders every argument's live default and the three pinned operator-consequence substrings. Read the diff for every highest-risk item named: pipeline.UnexpectedStageError is a PipelineError subclass excluded from the two genuinely-typed except clauses ahead of the wrap sites, so it cannot misclassify a real RepoIntakeError/SourceStateError/WorkspaceContainmentError, and both directions are pinned by test (-60); the four DN-4 run_audit_detailed monkeypatch sites (test_cli.py x2, test_instrument_disclosure.py x2) are all correctly updated, the fifth (test_mcp_server.py, patches server.run_audit) is correctly untouched; the NFR-M1 split into tests/test_help_contract.py drops no assertion and narrows no population (positive control -53 proves the guard bites); the DF-12-1-C exemption re-record and the DF-8-4-D/DF-10-4-C ledger closures are append-only and the target_story strings agree exactly as -04 requires. ONE Low finding, written to the story's Review Findings as [Review][Patch]: docs/first-run.md's opening line claims "everything on it is checked by a test against the code", which the page's own install-command caveat twenty lines later contradicts (verified by execution that extract_documented_invocations() correctly does NOT extract or check the pip-install line, exactly as the page's caveat admits) — no functional/security/AC impact, a one-line reword. No unresolved High/Medium issue, no loosened gate, no re-merged split found anywhere hunted; verdict is concerns rather than pass solely because of the one open Low item, left unchecked for the next dev round. || PRIOR: dev-story 12-8: ready-for-dev -> review (the OPERATOR-FACING EXPLANATION SURFACE shipped, and the load-bearing half is AC3's false-green closure: --passes/--skip-pass/--reports now REFUSE an unknown token inside parse_args against the ONE definition of each, so `--passes securty` - measured on 2f84a0b returning RELEASE_READY exit 0 with EVERY detector pass silently disabled - is a typed refusal instead of a fabricated green. generator.py gained the single RENDERED_REPORT_TYPES constant AC3 required (there was none, which is why nothing could validate a report token and why this repo's own argus-student-audit.yml:48 shipped `vacuous-tests`, now corrected). AC8: an argparse usage error returns the reserved 1 in main() only, so a typo no longer publishes verdict=NOT_READY_FOR_RELEASE assessed=true through action.yml; --help still exits 0 and build_parser().parse_args is byte-identical for every guard and for the second invocation surface. AC4/AC5: FR37's render_audit_failed_next_action has production callers for the first time (both CLI arms + the second surface), dispatching per typed class and raising on an unregistered one; DF-8-4-D CLOSED at the WRAP SITE via pipeline.UnexpectedStageError, because pipeline.py was pre-disguising internal defects as expected PipelineErrors and a CLI-only except split could not have closed it. AC6: the NFR-S1 absolute-host-path leak at source_state.py:122 removed and pinned as a property over BOTH streams in every spelling a path can wear. AC7: 12.5's handover wired - the grammar downgrade reaches the DEFAULT run from the SAME renderer via an additive AuditResult field, nothing new persisted; DF-10-4-C surface half CLOSED, class-payload half re-recorded with a reason. AC1/AC2: docs/first-run.md ships, linked, every checkable claim derived by test and registered in both release-surface registries + the -28 corpus by glob; every argument's --help now states its LIVE default, derived by formatter, parity asserted over derive_arguments' closure. TWO guards that could not fail as written were corrected with their new forms proven to bite, and REPORT-002-31's write-point floor was RE-DERIVED (not lowered) after the render loop. NFR-M1 forced 12.7's cohesion-split remedy -> tests/test_help_contract.py, no line shaved, no population narrowed. GATES: pytest 1524 passed / 3 failed - all three the known DF-10-4-D artifact-currency class (test_dogfood_plan.py x1, test_dogfood_proof.py x2) awaiting the commit-then-regenerate bootstrap this story may not perform; mypy argus clean on 83 files; bandit control-matched with ZERO new findings after two constants were renamed off the ...TOKEN suffix rather than suppressed (no nosec anywhere). Published NOTHING - 12.9's fence held: no tag, no index upload, no release, no release-status claim). PRIOR: create-story 12-8: backlog -> ready-for-dev. Scope: the OPERATOR-FACING EXPLANATION SURFACE - a lean first-run page, --help parity, and operator-error diagnosis - with every premise re-measured by EXECUTING the real CLI on 2f84a0b rather than read from the epic. Four of the epic's premises are FALSE and one partly false: docs/ holds a BMad tooling stub, not an integrator README, and nothing links to it; two of the four operator errors the epic names (--deep-audit with no provider, --strict refusals) are ALREADY handled well and must not be rebuilt; missing-grammar prose renders only into a report that needs --report-dir, which is exactly what 12.5 handed over by name; and DF-8-4-D's arm has moved to cli.py:758 with THREE such arms now, not two. New measurements the story is built from: EIGHT operator-error surfaces are entirely SILENT, one of them a false-green channel (--passes securty disables every detector pass, prints nothing, returns RELEASE_READY exit 0, and this repo's own argus-student-audit.yml:48 ships an unrendered --reports token); render_audit_failed_next_action, shipped by 12.4 for FR37, has ZERO production callers, so both CLI failure arms print a cause and stop; source_state.py:122 leaks the ABSOLUTE HOST PATH into the stderr line whose own docstring promises it never will (NFR-S1); and DF-8-4-D cannot be closed at the CLI alone because pipeline.py already pre-disguises internal defects as typed PipelineErrors. ABSORBED with a recorded reason (AC8): an argparse usage error exits 2, which action.yml:129 publishes as verdict=NOT_READY_FOR_RELEASE assessed=true - a typo fabricates an assessment for a run that never happened, at the one surface whose sibling (mcp/server.py:107) already rules a parse rejection is NOT a verdict. Owns DF-8-4-D and DF-10-4-C; CITES and does not build DF-3-4-A (resume), DF-10-5-C (evidence export) and DF-12-7-A (more hosts), all unscheduled. 9 ACs, no new flag, no new sub-command, nothing published (12.9's). PRIOR (planning note, 2026-08-10b, superseded not deleted): 🟡 · Engineering. docs/ holds ONE integrator-shaped README. Lean first-run page: install, first audit, reading the ledger, what each verdict means — NO tutorial prose beyond that (this persona is a developer, not a novice). --help states purpose + default for every flag 10.3 blessed, with a parser-vs-help parity test alongside 10.3's parser-vs-contract test. Operator errors (bad path, unreadable repo, missing grammar, absent key under --deep) name CAUSE and FIX — extends 10.4's diagnosis principle to the user surface. ABSORBS DF-8-4-D (2026-08-10b triage, RELEASE-RELEVANT): cli.py:295-299 catches BASE ValueError while its comment names five typed subclasses, and Pydantic ValidationError IS a ValueError subclass — an internal defect surfaces as an expected 'audit failed' degradation. Split the arms; pin both directions. Filed as low severity when every user could read a stack trace; not low for a public CLI (a masked bug costs the user a next action, which FR37 forbids, and costs the maintainer the bug report).
  12-9-release-is-published-and-cites-its-gate: done  # code-review 12-9: review -> done. CLEAN PASS, independently re-verified by execution: pytest 1543 passed/0 failed (161.6s, matches claim exactly); mypy argus clean 83 files; bandit 19 Low/0 Med/0 High with `git diff --stat -- argus` empty (stronger than a stash control, confirms DN-7: argus/**, action.yml, pyproject.toml, dist/ all byte-unchanged); git tag -l empty, origin/master unmoved at 00c8d1b, gh release list empty, repo view still isPrivate:true -- nothing outward-facing occurred. gh run list --workflow=audit-ci.yml confirms only 3 runs ever existed on master and none covers de05dec or 4ef82a0, so AC2's NOT ESTABLISHED is the measured truth, not a dodge, and citing 31341363300 is correctly forbidden. Read scripts/release_notes.py (stdlib-only, never imports argus), tests/test_installed_artifact.py (genuinely installs+uses the wheel, PROBE-INVALID demonstrated to catch a real false pass), the widened -55/-55b tag-state closure (all 4 pins/3 surfaces + release.yml header, both directions proven with no real tag created), release.yml's injection-invariant generator invocation, and the _CITATION_DENIAL_MARKERS stricter-never-looser addition (concrete before/after demonstrated) -- all deliver exactly what they claim. deferred-work.md diff confirmed +95/-0 programmatically; DF-10-3-A correctly closed against real evidence with target_story corrected append-only; DF-12-9-A correctly filed once with a named owner; DF-3-4-A/DF-10-5-C/DF-12-7-A/DF-10-3-B/DF-10-3-C cited not re-filed. 0 decision-needed, 0 patch, 0 defer, 0 dismissed; no High/Medium findings. AC9's HALT is the designed terminal state (DN-8) and does not block this pass -- DF-12-9-A remains open, awaiting explicit human act-by-act authorisation, unrelated to this story's dev-complete status. Full record in the story file's Review Findings section. || PRIOR: dev-story 12-9: ready-for-dev -> review. TERMINAL STATE AS DESIGNED: AC1-AC8 COMPLETE, AC9 HALTED PENDING EXPLICIT HUMAN AUTHORISATION -- that is the intended outcome (DN-8), not a failure. ZERO outward-facing acts were performed and it is re-asserted BY EXECUTION at hand-off: git tag -l EMPTY; origin/master still 00c8d1b (0/34, unmoved); gh release list EMPTY; gh repo view -> isPrivate: true (still PRIVATE); no push, no tag, no GitHub Release, no index upload, no marketplace listing. DELIVERED: (AC1) tests/test_installed_artifact.py RELEASE-001-25..-28 builds sdist+wheel, installs the WHEEL into a fresh venv (python -m venv --without-pip + uv pip install --no-deps + one .pth for the deps; --system-site-packages was tried and REJECTED by measurement), REFUSES with PROBE-INVALID unless argus resolves inside that env, then exercises every [project.scripts] alias BY CLOSURE (4, fifth covered with no edit, unknown target RAISES), argus/argus audit --help, a fixture audit to a real verdict whose summary line parses and whose exit code matches the AR3 map, and a REAL MCP JSON-RPC exchange through the installed argus-mcp shim with the tool name and its required argument DERIVED from tools/list. CHANGELOG:23-29's unguarded hand-made claim is struck and replaced by one naming that guard and stating MCP. (AC2) scripts/release_notes.py::derive_release_status is the ONE derivation; README.md and CHANGELOG.md render its value verbatim; DOCS-001-24/-25/-25b extend 10.1's rule to the IMPORTED _RELEASE_SURFACES population without forking its policy tables. STATUS = NOT ESTABLISHED (DN-3, expected and passing): no audit-ci.yml run covers the release commit; run 31341363300 (00c8d1b) is named as SUPERSEDED, never cited. REAL FINDING FOUND AND CORRECTED: the honest NOT ESTABLISHED sentence itself parsed as a well-formed citation (it must name the superseded run WITH its sha), which would have excused every other claim on that surface -- the citation reader was made STRICTER via _CITATION_DENIAL_MARKERS, never looser. (AC3) the release note is GENERATED: the hand-typed literal is gone from release.yml's run: block and every claim is derived (version, AR3 map incl. the reserved 1 from cli.py, the canonical FR34 disclosure selected by INSTRUMENT_STATUS, the install command, the AC2 status). DN-4 RECOMMENDED ROUTE TAKEN: stdlib ast parsing of the single sources, generator never imports argus, runs before any install; the pip-install-then-import alternative is recorded as REJECTED. DOCS-001-67..-70 render and assert both directions and go RED at the real seam (an exit code moved, the instrument status flipped, a single source renamed -> raises). (AC4) DOCS-001-55 WIDENED from README-only to a closure over the registered surfaces with both non-vacuity floors; -55b proves both directions through the pure seam WITHOUT creating a tag, and release.yml's 'HAS NEVER EXECUTED' header is folded into the same rule instead of getting a second guard. Visibility struck-and-corrected on README/CHANGELOG and ADDED to docs/first-run.md, single-sourced and dated with its command (DOCS-001-71). (AC5) DF-10-3-A CLOSED against 12.8/AC8's delivered remedy, VERIFIED BY EXECUTION first (bare argus -> exit 1, no-verdict sentence), its non-existent target_story id corrected in append-only form; DF-12-9-A files the seven unperformed outward acts ONCE with a named human; DF-3-4-A, DF-10-5-C, DF-12-7-A, DF-10-3-B, DF-10-3-C cited and NOT re-filed; deferred-work.md diff is +95/-0, verified programmatically. (AC6) architecture.md's four stale §I cells struck-and-corrected and now HELD by DOCS-001-72; the index exit condition re-affirmed with a date in architecture.md, CHANGELOG.md and README.md (DN-1). (AC7) RELEASE-001-29 rehearses validate-tag/pre-build/post-build through the real CLI over a real local build and asserts coverage of EVERY RELEASE_EDGE_CASE_IDS member (floor, not a sample); E4's three outcomes all exercised and UNKNOWN never becomes ok; E2 re-decided and now DRIVEN to a refusal locally against a throwaway fixture repo, with CI_UNREACHABLE re-stated with a date and reason (RELEASE-001-30). (AC8) RELEASE DOSSIER in the story's Dev Agent Record: sdist/wheel sha256, the derived note verbatim, the derived status, the E1-E6 preflight report, the fresh-env proof, and the exact ordered publish commands QUOTED NOT EXECUTED. DN-7 HELD: argus/** is BYTE-UNCHANGED (git diff --stat -- argus empty), so no DF-10-4-D dogfood regeneration is owed and none was performed. GATES, ALL LOCAL: pytest 1543 passed / 0 failed / 0 errors / 0 skipped (was 1527; +16 new); mypy clean, 83 source files; bandit 19 Low / 0 Medium / 0 High -- and the control is stronger than a stash run here because the scanned population is byte-identical to de05dec. NFR-M1: worst non-exempt file 1181/1200 (test_built_distribution.py); _EXEMPT_BY_DESIGN not grown. CI EVIDENCE: NOT ESTABLISHED -- every figure above is LOCAL and does not discharge architecture.md §H. AWAITING: explicit human authorisation, act by act, for DF-12-9-A's seven acts. || PRIOR: create-story 12-9: backlog -> ready-for-dev. Scope: STAGE, PROVE, DERIVE and GUARD the release -- and HALT before publishing it. Premises re-measured on de05dec by execution AND by live read-only GitHub API reads (gh 2.92.0), because two of them are facts OUTSIDE the repository. SIX DIVERGENCES, two of them outside the tree. (1) THE EPIC'S CENTRAL AC IS IMPOSSIBLE TODAY: the latest audit-ci.yml run on master is 31341363300 at sha 00c8d1b (2026-08-09, success) and origin/master is 34 COMMITS BEHIND local master (gh repo view pushedAt: 2026-08-09) -- every Epic-10/11/12 commit is LOCAL ONLY, so NO executed gate covers the release commit and none can exist until master is pushed (an outward-facing act). The honest status is NOT ESTABLISHED, and citing 31341363300 is forbidden: architecture.md:614-616 uses THAT EXACT run id as its worked example of a half-truth. (2) THE REPOSITORY IS MEASURABLY PRIVATE (gh repo view -> isPrivate: true), which README:76-79 and CHANGELOG:627 both admit they never measured -- so the documented `pip install ...@v0.1.0` cannot resolve for ANY consumer with or without the tag, and the marketplace channel is blocked for a reason unrelated to 11.3 (which is done; precondition MET). (3) TC-ArgusAgent-DOCS-001-55, built by Story 11.5 so the tag caveat "cannot rot in EITHER direction", reads README.md ONLY (test_built_distribution.py:604) -- the tag pin appears on THREE tracked consumer surfaces (README x3, CHANGELOG x2 at :15/:625, docs/first-run.md x1 at :26, the last added by STORY 12.8 AFTER -55 was written), so creating the tag turns three further caveats into published falsehoods INVISIBLY. That is exactly the hand-over 12.8 recorded twice in its Change Log, and AC4 closes it BEFORE any tag exists. (4) release.yml:174-190 builds the GitHub Release note as a HAND-TYPED STRING LITERAL inside a `run:` block, transcribing the exit-code wire contract, the install command and a paraphrase of the FR34 disclosure -- and 12.8 changed exit-code semantics without that literal moving, because nothing checks it (-17 scans it for over-claims only). (5) Story 10.1's citation rule CANNOT SEE the consumer surfaces: _STATUS_DOCUMENTS is change-proposals + retros only, and README/CHANGELOG/release.yml are not excluded with a reason -- they are simply outside the guard. (6) DF-10-3-A -- the ledger entry that NAMES this story -- was already resolved by 12.8/AC8 (its own FIRST candidate remedy: map usage errors to the reserved 1) and never closed, and its target_story names a story key the tracker does not have. DECISIONS RECORDED RATHER THAN ASSUMED: the INDEX (PyPI) channel does NOT ship (DN-1 -- four committed statements, one a Story 9.2 locked decision at sprint-status.yaml:350, and epics.md:2465 is permissive not mandatory); the MARKETPLACE channel does NOT ship (DN-2 -- private repo, not 11.3); the version STAYS 0.1.0 (9.2/D1); and NOT ESTABLISHED is a PASSING outcome (DN-3). NINE ACs. AC1-AC8 are completable with ZERO OUTWARD-FACING ACTS: build and then USE the artifact from a genuinely installed fresh environment (every [project.scripts] alias by closure, --help, a fixture audit, and a real MCP JSON-RPC exchange through the installed argus-mcp shim -- CHANGELOG:23-29 already PUBLISHES that claim with no guard behind it, on a distribution that has since gained argus-mcp, 3 command assets, 9 grammars and a changed exit-code contract); derive the gate citation or NOT ESTABLISHED and extend 10.1's rule to the surfaces a stranger reads; GENERATE the release note instead of typing it, every claim derived; mechanise the tag-state and visibility disclosures across ALL FOUR pins on THREE surfaces; close DF-10-3-A; correct every falsified gate and document (architecture.md's four stale §I cells and release.yml's "HAS NEVER EXECUTED" header among them); re-prove E1-E6 against the channel that actually ships; and assemble a RELEASE DOSSIER with the publish commands QUOTED, NOT EXECUTED. AC9 ALONE IS OUTWARD-FACING AND IS WRITTEN AS A HALT: it enumerates all seven irreversible/outward acts (push master 34 commits, tag, push tag, GitHub Release, make the repository public, Marketplace, PyPI) with blast radius and reversibility, and NONE may be performed without explicit human authorisation naming the act -- per this epic's own standing operator instruction above ("the orchestrator halts before it"), Story 9.2's "escalate rather than assume", and release.yml's own header: a publish "is an operator decision taken with credentials in hand -- not a decision a story author may take unilaterally". An orchestrator or agent message is NOT authorisation. The expected terminal state is AC1-AC8 COMPLETE / AC9 HALTED, and that is a SUCCESS. Owns DF-10-3-A; CITES and does not build DF-3-4-A, DF-10-5-C, DF-12-7-A, DF-10-3-B, DF-10-3-C. PRIOR (planning note, 2026-08-10b, superseded not deleted): 🟠 · Delivery Orchestrator. MOVED from the superseded 11.7. GATED ON 10.1's evidence standard: cite the CI run id on the RELEASED COMMIT or record NOT ESTABLISHED. release.yml deliberately abstains from index publish (its own header) and has NEVER executed — git tag -l is EMPTY; adding publish is a reviewed, deliberate scope change. If 11.3 has not landed the MARKETPLACE channel does NOT ship; the index channel may ship independently. BOTH listings carry the FR34 disclosure. Fresh-env proof: argus --help + a fixture audit + an MCP invocation all succeed. Re-prove 9.2's release edge cases (dirty tree, existing tag, re-tag, silent overwrite) against the index channel, which 9.2 could not exercise.
  epic-12-retrospective: done  # retrospective epic-12 2026-08-15 (epic-12-retro-2026-08-15.md): COMPLETE, backlog -> done. 9/9 stories done. OUTCOME CLAUSE: FOUR OF FIVE MET - the two built-and-unwired capabilities are connected (FR36 12.2, FR27/NFR-D1 12.3), the output stops being a dead end (FR37 + the three-population ingestion boundary 12.4, the operator-error surface 12.8), the tool is reachable from the coding agent (argus-mcp 12.6, packaged command assets + argus install-commands 12.7) - AND THE FIFTH IS NOT: "the result is published" did not happen, and the framing clause "Argus becomes worth installing" is not yet true for anyone outside XAgents, because the repository is measurably PRIVATE so the documented install resolves for nobody with or without a tag. HEADLINE DELIVERIES: two false-green channels closed in a tool whose entire product is an honest verdict - `--passes <typo>` was accepted, silently disabled EVERY detector pass and returned RELEASE_READY blocking_findings=0 at exit 0 (live since before Epic 8, passed by every review gate that ever saw the flag), and every argparse usage error exited 2, which action.yml:129 publishes as verdict=NOT_READY_FOR_RELEASE assessed=true, so a typo fabricated an assessment for a run that never happened. NFR-M1 closed: pipeline.py 1331 -> 1111 with a repo-wide sweep over both trees (197 tracked .py, three named/dated/filed exemptions). AI-E11-5 ADOPTED: 28 commits where Epic 11 produced ZERO. Tests 1405 -> 1543 (+138, none removed/skipped/weakened/xfailed, attributed to Story 12.9's dev and reviewer; NOT re-run by the retrospective). Review: 6 of 9 PASS at iteration 1; 12.1, 12.2 and 12.8 needed a second round. Eleven vacuity instances found across nine stories, ten at create-story/dev and one at review, NONE shipped, and every one CORRECTED rather than loosened. RELEASE STATUS: NOT ESTABLISHED - re-measured live by read-only git and gh: git tag -l empty, gh release list empty, origin/master unmoved at 00c8d1b with local master 35 ahead, repository PRIVATE, and the newest audit-ci.yml run (31341363300 at 00c8d1b, 2026-08-09) is superseded and does not cover the release commit. THE EPIC'S OWN WEAKEST DIMENSION IS ITS GOVERNANCE RECORD: Stories 12.4, 12.5 and 12.6 wrote NOTHING to deferred-work.md while 12.4 and 12.5 claim four closures between them (DF-8-3-A, DF-10-4-A, DF-10-4-B, DF-12-3-A, all still OPEN), and AI-E11-6 was assigned to 12.4, marked done, and is measurably NOT implemented - no rank vocabulary exists in tests/test_release_surface_honesty.py. 14 action items (AI-E12-1..-14), 8 critical-path items, 6 significant discoveries. ⚠️ THIS DOCUMENT MATCHES _STATUS_DOCUMENT_PATTERNS AND TURNS TC-ArgusAgent-DOCS-001-22 RED UNTIL REGISTERED - AI-E12-1, one line, written to be inert. ⚠️ SD-1 SHOULD BE RULED ON BEFORE EPIC 13 STORY 1 IS CONTEXTED: epics.md:2484 makes Epic 13 depend on "a published tool" and nothing is published. NO RE-PLAN WAS APPLIED - no epic, PRD, architecture or story scope was edited, and development_status[epic-12] was deliberately NOT rolled up by the retrospective (the Epic-11 retro set the same precedent); that roll-up is the orchestrator's to take.

  # ── Epic 13: Earn the Gate — remove the disclosure by measuring, not by deleting (delta) ──
  # Covers DF-7-2-A, DF-6-6-A/-P1/-P2, and the architecture's OPEN validation-set input (L152-154, L328)
  # Depends on Epic 12. STRICTLY SEQUENTIAL — each story's output is the next one's input.
  epic-13: done  # dev-loop roll-up 2026-08-19: in-progress -> done. All five stories `done` (13-1..13-5) and epic-13-retrospective `done` (epic-13-retro-2026-08-19.md, which SUPERSEDES the INTERIM by name; the INTERIM is untouched per §3.4). THE EPIC ACHIEVED ITS PURPOSE AND THE GATE IS NOT CLEARED - those are not in tension. 13.5 re-measured through Epic 14's corrected instrument and returned outcome=BLOCKED, precision UNEVALUABLE over a pin-verified five-of-five zero (1,960 files read, 5,129 test functions scored, 1,249 flagged, 4,284 advisory, 0 blocking; expert_hours None, never 0). An empty precision denominator is UNEVALUABLE by construction, which is the PRE-REGISTERED expected outcome, not a shortfall. THE FR34 DISCLOSURE STANDS and INSTRUMENT_STATUS is unmoved. DF-13-5-A's ONE-round stopping rule is cited, not executed - Epic 15 IS that round. TWO DECLARED BLOCKERS ON THE FINAL PASS ARE UNMET (AI-E13-1, AI-E13-3; AI-E13-2 landed as story 13.4) - the retrospective ruled in-domain that it may close anyway, recorded the ruling, and made it REVERSIBLE: set this and the retrospective key back to in-progress if the operator disagrees. || create-story 13-1 (2026-08-16): backlog -> in-progress, first story of the epic contexted. || added 2026-08-10b; 0/3 stories. ✅ ADJUDICATOR NAMED 2026-08-10b: XAgent007 (Engineering Lead role, precision-validation-protocol.md §2 primary). The start condition that blocked this epic is MET. The gate itself remains NOT CLEARED — naming an owner is not a measurement.
  13-1-decide-what-validation-set-is-then-build-it: done  # code-review 2026-08-16 iteration 1: VERDICT=PASS after fixes, review -> done. Three adversarial layers (Sonnet, no shared context); 9 findings, 1 dismissed, ALL 9 RESOLVED, none deferred, none loosened. ALL THREE LAYERS INDEPENDENTLY RAISED R1 (HIGH): the corpus figure was STALE at N=0 in prd.md, precision-validation-protocol.md, ALL THREE architecture.md resolution sites and DF-13-1-A, while the manifest returned 5 — AC1/AC2/AC6 were written pre-ratification and only the DERIVED surfaces followed. WORST PART: TC-ArgusAgent-DOCS-001-75 REQUIRED the literal 'N = 0 eligible members' in prd.md, so a guard written to protect the record was ENFORCING THE FALSEHOOD and holding the suite green. It now DERIVES the count from the manifest; proven RED against a planted 6th member. DF-13-1-A CLOSED by an APPENDED note (never an edit); deferred-work.md +85/-1 where the -1 is a newline-at-EOF marker that erases no content (verified by --word-diff, recorded in the file). R2 (HIGH): measure_validation_corpus() conflated two substrates — validation_floor_n() routes through registry_module(), so a REGISTRY failure was blamed on the MANIFEST and published a real n=5 beside 'NOT CONSULTED' = DF-8-5-C reproduced inside the fix for DF-8-5-C; split into three try blocks, floor read from the registry directly, and only ImportError now means absent (a broken manifest row propagates instead of masquerading as absence). R3 (HIGH): a NON-REPRODUCIBLE member rendered in blocking-worklist.md as '0 blocking / nothing to adjudicate', byte-identical to a genuinely clean member, persisted BEFORE the exit-2 fired — now an explicit WITHHELD block. R4-R9 (MED/LOW, corpus runner): typed DogfoodProofError on an unresolvable floor; CorpusRefusal replaces SystemExit (which derives from BaseException, was never caught, and exited 1 not the documented 2 — the isinstance branch was unreachable); absolute --map paths refused (pathlib discards the root); .git-as-a-file worktrees/submodules accepted; malformed --map refuses cleanly; decode uses errors='replace'. Refusal paths re-verified BY EXECUTION (exit 2 each). Corpus re-run after fixes: IDENTICAL results, 5/5 byte-reproducible, 31 blocking all vacuous_test_ast. GATES UNCHANGED FROM BASELINE: pytest 1561 passed / 0 failed / 0 skipped, mypy Success 83 files, bandit 19 Low / 0 Med / 0 High. Nothing outward-facing. || dev-story 2026-08-16 COMPLETE, in-progress -> review. AC3b RATIFIED AND EXECUTED (commit 1d044af): operator XAgent007 named five repositories, each MEASURED before admission (pin resolved, language mix folded through source_languages, licence checked). N=5, floor MET. ai_body_runtime had NO .git and was therefore unpinnable/unadjudicable — ESCALATED, not worked around; git init performed only on explicit instruction, and the row records that the pin has content but no history. All five BYTE-REPRODUCIBLE across two runs. MEASURED: ai-body-runtime RELEASE_READY 0 blocking/13; agent-markovich INSUFFICIENT_COVERAGE 0/272; minions NOT_READY_FOR_RELEASE 24/2946; xagents-webapp INSUFFICIENT_COVERAGE 0/1507; agent-smith NOT_READY_FOR_RELEASE 7/1280. 31 BLOCKING findings = the precision denominator for 13.2, and EVERY ONE is vacuous_test_ast (the moat detector); 5987 advisory recorded but out of the denominator per protocol §4. Delivered validation-corpus/adjudication-set.json + blocking-worklist.md; NOTHING pre-adjudicated (TP/FP fields null, -31 asserts they stay null). CORPUS LIMITATIONS RECORDED PER MEMBER: minions carries the OVERFITTING caveat (Argus was developed against it, began life inside it as minions_core/apaa/, Story 7.2 ran over it) so a high score there is the least transferable evidence; xagents-webapp is the real multi-language test (810 TS files vs a Python-built detector suite); "independent" means NOT the tool auditing itself, NOT third-party — all five are XAgents repos and four are agent-authored, which is what the PRD specifies and is the corpus main limitation. Gates LOCAL: pytest 1561 passed / 0 failed / 0 skipped, mypy Success 83 files, bandit 19 Low/0 Med/0 High. NOTHING OUTWARD-FACING. || dev-story 2026-08-16: ready-for-dev -> in-progress, baseline_commit bc55e36 (== HEAD, so §0 was measured on the implementation baseline). AC1/AC2/AC3a/AC4/AC5/AC6 COMPLETE (commits 59a2ad4 argus delta, 2e2e089 artifact regeneration, a7ee3c9 documents+ledger). DN-1 ADOPTED: the PRD governs; the >=80% gate is measured over REAL REPOSITORIES and precision-validation-protocol.md §5's cartridge floor is STRUCK IN PLACE (change log V1.1); the cartridges are re-labelled as the FR20 RECALL instrument and tests/cartridges/ is byte-unchanged. The architecture's last OPEN input is CLOSED BY DECISION at ALL THREE sites in identical words (:205 / :800-807 / :1244) + registered in §Enforcement; TC-ArgusAgent-DOCS-001-73 COUNTS occurrences (== 3) because a presence check passes with the 2026-08-10b contradiction intact. NEW: tests/corpus/_manifest.py (closed schema, self-validating rows, floor DERIVED from the reused VALIDATION_SET_FLOOR_N, exclusions recorded IN the manifest), tests/test_validation_corpus.py (PRECISION-001-21..-30, DOGFOOD-001-53..-55), tests/test_validation_set_decision.py (DOCS-001-73..-76). DF-8-5-C CLOSED BY DERIVATION, but NOT as the ledger's close condition specified (DN-7, recorded): passing populated_planted_defect_count()=7 would publish "N=7 ... floor N=5", reading as FLOOR MET for a gate the cartridges do not gate — the over-claiming direction. The gate's N is the REPOSITORY corpus, measured 0; precision is None ("NOT COMPUTED BY THIS RUN") because this generator never invokes the replay harness, and precision=None with provisional=False now RAISES. MEASURED CORRECTION to DF-10-2-A, ruled here (DN-6): its premise "C/C++/Ruby/Rust extract zero definitions" holds for THREE of four — Rust extracts struct_item and misses only functions, because the extractor's vocabulary entry `fn_item` is NOT a node type tree-sitter-rust emits (the real one is function_item); C/C++ match function_definition then drop it for having no `name` field (they use `declarator`); Ruby is a pure vocabulary gap. Rust STAYS ineligible on the narrower correct ground. Detector half OPEN+owned+unscheduled. ALSO MEASURED: sprint-status's own "pipeline.py is 1005 lines" is an UNDERCOUNT — 1005 is what `Get-Content | Measure-Object -Line` returns (it scores blank lines as zero); the true physical count is 1111, still under the 1200 ceiling, so AI-E10-8 remains already-fixed. AC3b (populate to N>=5) is RATIFIED by the operator 2026-08-16, who elected to name the repositories; THE MEMBER LIST IS OUTSTANDING, so the manifest holds ZERO eligible members and nothing was fabricated — filed as DF-13-1-A (🟠 critical-path, owner XAgent007). deferred-work.md +166/-0. GATES ALL LOCAL: pytest 1560 passed / 0 failed / 0 skipped (baseline 1543, +17), mypy Success 83 files, bandit 19 Low / 0 Medium / 0 High (baseline identical); CI evidence NOT ESTABLISHED. NOTHING OUTWARD-FACING: git tag -l empty, origin/master still bc55e36. || create-story 2026-08-16 on HEAD bc55e36: backlog -> ready-for-dev. Story file stories/13-1-decide-what-validation-set-is-then-build-it.md. §0 premise re-measurement found FOUR STALE CITATIONS in this epic's ACs and THREE DIVERGENT CITES OF ONE DEFECT: PRD 'N ~ 5-10 real repos' is at prd.md:196 not L161; 'independent senior engineer' at :191 not L156; the architecture's OPEN-input sentence is at architecture.md:205 (with a SECOND, DISAGREEING statement at :800-807 saying still-open and a third mention at :1244) not L152-154; the rendered gate line is minions-dogfood-proof.md:88 not :87; and DF-8-5-C's literals are at proof_run.py:643-644 per epics.md and the tree, NOT :764-765 as this entry said (the file is 611 lines). CONFIRMED BY EXECUTION: populated_planted_defect_count()=7 across distinct_rule_class_count()=5, so the epic's arithmetic holds; proof_run.py:643-644 literals exact; git tag -l empty and origin/master == HEAD. ALSO MEASURED STALE, do not redo: AI-E10-8 (argus/pipeline.py is 1005 lines, not 1331 — Story 12.1 closed it) and AI-E12-1's first half (epic-12-retro-2026-08-15.md IS registered at test_evidence_citation.py:125). AC3b (populate the corpus to N>=5) is GATED on one operator ratification act and may terminate HALTED per the 12.9/AC9 precedent; AC1-AC2-AC3a-AC4-AC5-AC6 are fully autonomous. || 🟠 · Engineering Lead. PRD L161 ('N ~ 5-10 REAL repos') + L156 (judged by an independent senior engineer) vs precision-validation-protocol.md §5 ('N >= 5 labeled planted-defect CARTRIDGES', VALIDATION_SET_FLOOR_N = 5) — TWO CORPORA, NEVER RECONCILED. Recommendation of record: the PRD governs; cartridges measure RECALL (FR20, delivered), precision measures blocking findings on UNPLANTED code. CLOSES the architecture's OPEN input (L152-154 / L328) — closed by implementation, never by decision. Rebuild from repos Argus did NOT author; the 8.5 self-audit is EXCLUDED from N with the exclusion recorded. Repos may be SOURCED from public users but ONLY ADJUDICATED FINDINGS COUNT — usage is not evidence. ABSORBS DF-8-5-C (2026-08-10b triage, RELEASE-RELEVANT): proof_run.py:764-765 passes precision=Fraction(0,1), n=0 as LITERALS, publishing 'N=0 labeled cartridges populated, floor N=5' into minions-dogfood-proof.md:87 while the shipped registry measures 5 rule classes across 7 populated rows. Derive from the registry, regenerate the artifact, record it as a correction — the figure UNDERSTATED so it never made a gate look cleared.
  13-2-adjudicate-every-finding-by-a-named-human: done  # code-review 2026-08-17 (Sonnet): review -> done. CLEAN PASS after independent adversarial re-verification (Blind Hunter / Edge Case Hunter / Acceptance Auditor layers) of commits e991a00+4c6c76d+411d891 (1816524..HEAD) against the story's seven ACs. AC7's HALT is a legitimate designed terminal state, not an unmet-AC waved through: the story's own ESCALATION section explicitly designs this outcome BEFORE dev-story ran, citing the verified Story 12.9/AC9 precedent (an accepted CLEAN PASS with an outward-facing act HALTED) -- and DN-6 (no agent adjudicates) makes AC7's HALT a STRONGER case than 12.9's, since the deliverable is definitionally a human act. Protocol V1.3 amendment independently checked against loosening: strike-never-erase preserved, N>=5 floor and 80% threshold literals unchanged, the clean-repo condition was NOT weakened (made explicit/NOT-APPLICABLE rather than falsely satisfied), and the finding-level unit is STRICTER (31 rows) than the rejected per-class alternative (denominator of 1) -- no goalpost movement found. adjudication-record.json independently parsed: 31 rows, 100% UNADJUDICATED, adjudicator=null on every row -- matches the dev's claim exactly. DN-2b (fold shares arithmetic, not compute_precision) verified NOT a DRY violation: the shared objects (precision_fraction, gate_is_provisional, PRECISION_GATE_THRESHOLD, precision_gate_status_for) are genuinely reused; the un-shared piece is a golden-key MEMBERSHIP diff vs a direct per-row tally, which are different algorithms, not duplicated arithmetic. Windows-only-shipping-POSIX-bugs risk independently checked: pathlib throughout, explicit encoding='utf-8', explicit newline='\n' on write, locator regex rejects drive letters/backslash/leading-slash/'..', record file verified 0 CRLF bytes on this Windows machine (single-line canonical JSON sidesteps the whole class), .as_posix() used at every path-to-string boundary -- no Windows-only defect found; CI evidence still NOT ESTABLISHED (noted, not blocking). DF-10-4-B correction spot-checked against argus/reports/generator.py:420-422's own pre-existing docstring (unmodified by this diff) and 12-4's story file, which does check off DF-10-4-B as delivered -- the correction is accurate, not overreach. INDEPENDENTLY RE-RUN: full pytest suite 100% green (all dots, 0 F/E/s, exit 0); mypy clean 84 source files (confirmed); bandit 19 Low/0 Med/0 High (confirmed); new-module line counts (adjudication.py 923, replay_harness.py 786, test_adjudication_record.py 768, test_gate_flip_path.py 354, test_governance_record_integrity.py 311, build_adjudication_record.py 228, test_instrument_disclosure.py 1194) all match the Dev Agent Record exactly. No High/Medium findings. git tag -l empty, origin/master unmoved -- nothing outward-facing. See the story file's Review Findings section for the full record. || dev-story 2026-08-16 on HEAD 1816524: ready-for-dev -> review. AC1-AC6 + AC8 COMPLETE; AC7 HALTED BY DESIGN awaiting the named adjudicator XAgent007 (Story 12.9/AC9 precedent) -- filed DF-13-2-A. All three §0.1 flip-path defects REPRODUCED VERBATIM on this sha and closed ADDITIVELY: (a) a 2-member injected registry reported N=7, (b) a corpus emitting nothing reported precision=1/1 / provisional=False / gate 'cleared', (c) §5's clean-repo blocking-FP condition was VACUOUS over the corpus that gates externalization. The 6.6/7.1 contract tests (DOGFOOD-001-11/-12, PRECISION-001-08/-09) pass BYTE-UNEDITED. Protocol V1.3 amended BEFORE the record was generated; unit DECIDED = FINDING (§7 upheld, not softened -- 31 blocking findings are 1 rule class on this corpus, so a per-class fold would gate on a denominator of ONE); a committed guard fails any record whose protocol_version is not the change-log head. Adjudication record committed IN GIT (validation-corpus/adjudication-record.json): 31 rows, ALL UNADJUDICATED, ZERO judgements, no adjudicator id anywhere -- an UNADJUDICATED row carrying one now RAISES at construction. Fold over it: Unevaluable, residual 31, precision NOT COMPUTED, gate 'unevaluable'. THE GATE DID NOT MOVE: protocol_cleared still False and still never True from argus/**; 1 of §5's 4 conditions holds (N=5 from 13.1). AC8: ledger +186/-0; AI-E12-6's ledger-claim guard LANDED and found 19 unbacked closure claims on its first run (reproducing AI-E12-3 independently), registered as a dated, owned, SHRINKING registry -- 2 removed the same day; AI-E12-3's four entries ALL RULED BY EXECUTION (DF-8-3-A CLOSED, DF-10-4-A CLOSED with its divergence, DF-10-4-B NOT DELIVERED with two false story records corrected, DF-12-3-A split); GUARD-ADEQUACY CLAUSE REGISTERED in architecture.md §Enforcement at the FOURTH request (AI-E12-5); AI-E11-8 re-homed with a named owner. Gates LOCAL (WINDOWS ONLY; CI evidence NOT ESTABLISHED): pytest 1585 passed / 0 failed / 0 errors / 0 skipped in 421s (1561 -> +24), mypy clean 84 source files, bandit 19 Low/0 Med/0 High -- all identical to the 13.1 baseline except the +24 new guards and the +1 module. FOUR guards went RED on the full run and every one was a guard WORKING, none loosened: DOCS-001-46 caught a second unregistered protocol_cleared=True test file; RELEASE-001-11 forced a deliberate decision about the new module naming the repository-only tree; DOCS-001-54 caught four stale published built-artifact figures in README/CHANGELOG; and DOCS-001-78 -- the guard this story LANDED -- fired on this story OWN record, which is the strongest available evidence it is not vacuous. Nothing outward-facing: git tag -l empty, origin/master unmoved. || create-story 2026-08-16 on HEAD bc55e36: backlog -> ready-for-dev. Story file stories/13-2-adjudicate-every-finding-by-a-named-human.md. START CONDITION RE-VERIFIED: XAgent007 named at :414/:416 and in deferred-work.md:1553-1560 -- MET. THREE DEFECTS IN THE GATE-FLIP PATH FOUND BY EXECUTION, all reproduced in the story's SS0.1 and owned by AC1: (1) compute_precision derives n from populated_planted_defect_count() and IGNORES the injected registry -- a 2-member population still reports N=7; (2) an empty precision denominator returns Fraction(1,1), passes the >=4/5 threshold and renders gate_status 'cleared' -- measured with 0 TP / 0 FP / 8 FN; (3) SS5's 0-clean-repo-blocking-FP condition is VACUOUS on a repository corpus because _is_clean_repo needs an empty golden key and max_blocking==0. Together: a 2-member corpus that emitted NOTHING reports 'cleared' the moment protocol_cleared=True is passed -- and 13.2 is the story that produces the value 13.3 will pass. ALSO MEASURED: the protocol locks 'precision over FINDINGS' (SS7) while the harness counts distinct match-key CLASSES (tp = len(tp_keys)) -- different quantities, must be amended BEFORE the run (AC2); the 6.7 DecisionRecordWriter is NOT reusable as a class (accepts only EscalationResolution, and writes under .argus/ which .gitignore:19 ignores) so the record must be a committed artifact (DN-3); the live Argus self-audit has ZERO blocking findings (all three classes advisory) so it cannot yield a precision measurement at all. THREE EPIC-12 RETRO ITEMS NAMED AS EPIC-13 PRECONDITIONS ARE ALL UNLANDED, verified live: AI-E12-6 (ledger-claim cross-check guard -- retro ranks it #7 'before 13.2 files its adjudication record') does not exist; AI-E12-3's four entries (DF-8-3-A, DF-10-4-A, DF-10-4-B, DF-12-3-A) are still open and unowned; AI-E12-5's guard-adequacy clause is unregistered (grep GUARD-ADEQUACY architecture.md -> 0 over 16 SSEnforcement rules). Do NOT redo AI-E12-1 (already registered at test_evidence_citation.py:125). HARD PRECONDITION: 13.1 must be done first (strictly sequential per epics.md:2484) and its AC3b may HALT -- if the corpus is absent, AC7's terminal state is 'Unevaluable, recorded', NEVER a fabricated disposition. AC1-AC6 + AC8 are fully autonomous; AC7 is the human act and is ESCALATED to XAgent007. Baselines on bc55e36: pytest 1543 collected / exit 0, mypy clean 83 files, bandit 19 Low / 0 Med / 0 High. NFR-M1 WARNING: test_evidence_citation.py 1199/1200 and test_instrument_disclosure.py 1179/1200 -- new guards need a new module. Nothing outward-facing performed; git tag -l empty, origin/master == HEAD. || 🟠 · ✅ ADJUDICATOR: **XAgent007** (named 2026-08-10b; Engineering Lead role, protocol §2 primary). DF-7-2-A was open and unowned since Epic 7 and restated as unowned by 9.2 and by the 2026-08-10 proposal — the start condition is now MET. Follow the COMMITTED protocol as written: §2 roles (Engineering Lead primary / QA Lead / external tie-break), §4 FULL-CORPUS EXHAUSTIVE (not sampled), borderline -> locator re-examination -> golden-key correction -> external tie-break. Where 13.1 changed the corpus definition, AMEND the protocol BEFORE the run, never reinterpret during it. Record ACTUAL expert-hours against §3's <=4h estimate. Close or re-scope DF-6-6-A/-P1/-P2. Record is APPEND-ONLY (§3.4).
  13-3-record-the-result-and-let-it-decide: done  # code-review 13-3 (Sonnet) close-out (2026-08-17): in-progress -> done. VERDICT: pass. Both Low findings from the two prior re-verification rounds are now fixed and independently re-verified: (1) the ledger delta hand-typed as +165/-0 was corrected to the measured +190/-0 in the story's Change Log and this entry — confirmed via git diff --numstat on deferred-work.md (190/0); the one remaining "+165" in each file is this review's own finding text quoting the defect, correctly left rather than erased. (2) epic-13-open-items.md:136's "82 distinct / 51 open" ledger total was corrected to 84 distinct / 31 closed / 53 open, and the orchestrator's fix ALSO found and corrected a THIRD error in the same sentence that neither review round had named — the sentence cited "Measured ledger totals on `6c59115`", but deferred-work.md's Story-13.3 +190/-0 append landed in commit `0889fc0`, AFTER 6c59115, so the totals could not have been measured on the tree cited; the line now correctly cites `0889fc0` (the commit that last changed deferred-work.md, confirmed byte-unchanged from there through HEAD) and documents a reproducible derivation (_DF_ID -> 85 raw -> 84 excluding the confirmed "DF-8" regex artifact from the "DF-8-*" wildcard prose reference -> 31 closed via the checked-in ledger_closed_ids analyzer -> 53 open) in place of the hand count; the original wrong sentence is struck, not deleted, per §3.4, naming all three errors. Independently re-verified by execution: reproduced the documented derivation exactly (85/84/31/53, all four assertions pass); confirmed deferred-work.md byte-unchanged 0889fc0..HEAD; confirmed the struck text preserves rather than erases the original claim; full pytest suite re-run green (exit 0, 1597 passed / 0 failed / 0 skipped); swept epic-13-open-items.md end-to-end for a third hand-typed total and found none (the file's other per-id closure-status citation at "6c59115" is substantively accurate since deferred-work.md was provably unchanged across that window). No unresolved findings remain; no High/Medium severity issues at any point across three review passes; all applicable ACs (1,2,3,3b,6,7,8) independently verified met across every pass; AC4/AC5 correctly not performed. See development_status[13-3-record-the-result-and-let-it-decide] and the story file's Review Findings for the full three-round record. || code-review 13-3 (Sonnet) re-verification, iteration 1 close-out (2026-08-17): stays in-progress. Finding 1 (ledger delta hand-typed as +165/-0 vs the measured +190/-0) was fixed by the orchestrator (two-site prose correction, no code touched) and independently re-verified: git diff --numstat on deferred-work.md measures 190/0, both corrected sites now agree, and the one remaining "+165" reference in each file is this review's own finding text quoting the defect (correctly left, per agreement with the orchestrator's call — rewriting it would erase the record of the finding). HOWEVER a SECOND instance of the same class was found during this re-verification sweep and is NOT yet fixed: epic-13-open-items.md:136's "82 distinct DF-* ids ... 51 do not [carry closure]" does not match an independent recount against the committed ledger (84 distinct / 53 open) using the same _DF_ID pattern test_governance_record_integrity.py defines, after excluding one confirmed regex artifact ("DF-8-*", a wildcard prose reference, not a real id); the 31-closed sub-figure IS confirmed correct via the real checked-in analyzer (ledger_closed_ids). Low severity — purely descriptive ledger accounting, gates nothing, no guard depends on it — but left as an open [Review][Patch] item per instruction not to wave through a remaining issue because it is small. Full pytest suite re-run green (exit 0, unchanged — no code touched by the fix), sprint-status.yaml re-confirmed to parse via yaml.safe_load. No other hand-typed figure checked (finding counts, AC3b concentration figures, residual bound, README packaging figures, NFR-M1 line counts) disagreed with its artifact. VERDICT stays concerns; status stays in-progress. See development_status[13-3-record-the-result-and-let-it-decide] and the story file's Review Findings for the full record. || code-review 13-3 (Sonnet) on HEAD f069471 (2026-08-17): review -> in-progress. VERDICT: concerns, not fail — every adversarial claim in the review brief was independently re-verified BY EXECUTION and found correct: the BLOCKED-vs-NOT_CLEARED ruling is right (protocol §4/AC1 confirmed, orchestrator's spawn-brief premise was the one that was wrong); the three re-derived guards (-39/-47/-52) are non-vacuous and each can still fail; residual_completion_bound arithmetic (5/31 < 4/5) confirmed exact and recorded as a bound, never a decision; AC3b concentration figures (24 minions / 7 agent-smith / 0 elsewhere / 1 rule class) confirmed derived, not typed, matching the committed record exactly; precision-validation-protocol.md confirmed byte-unchanged (0-line diff); INSTRUMENT_STATUS/decide_gate(literal False)/DOCS-001-46 confirmed intact; expert_hours confirmed null; the agent-smith unreachable-pin ledger entry (DF-13-3-A) confirmed accurate including its own self-reported evidence_deviation schema gap; README's 3 figures (94/93/86) independently reverified against a fresh build via DOCS-001-54/-58, both green; platform neutrality (encoding, newline, no CRLF, no backslash) confirmed on all 4 new files. Independent re-run: pytest 1597 collected / 1597 passed / 0 failed / 0 skipped (green — the one red the dev left open at hand-off was closed by the orchestrator's later commit f069471), mypy clean 86 files, bandit 19 Low/0/0, deferred-work.md +190/-0 (append-only, confirmed). ONE Low finding: the story's own Change Log / Completion Notes / this entry's prior text stated the ledger delta as "+165/-0"; measured +190/-0 — a hand-typed count that disagrees with the artifact, low-stakes (prose only) but flagged as an instance of exactly the class (DF-8-5-C) this story is otherwise scrupulous about. No High/Medium findings, no unresolved decision-needed items, all applicable ACs (1,2,3,3b,6,7,8) independently verified met; AC4/AC5 correctly not performed. Findings written to the story file's Review Findings section. Sent back to in-progress per the concerns->in-progress mapping so the one-line correction is picked up; the orchestrator may treat concerns as pass-eligible per its own cap. || dev-story 13-3 on HEAD 6c59115 (2026-08-17): ready-for-dev -> in-progress -> review. THE BLOCKING PREREQUISITE IS DISCHARGED AND THE DECISION IS RECORDED: the named human (XAgent007, Engineering Lead) adjudicated ALL 31 emitted blocking findings on 2026-08-17 (commit 6c59115) — 0 TP / 26 FP / 5 BORDERLINE / 0 UNADJUDICATED, expert_hours still null (a report, never a gate). ⛔ GATE OUTCOME = BLOCKED, **not** NOT_CLEARED, and the distinction is the whole point of AC1: protocol §4 states in its own text that BORDERLINE 'makes the run non-exhaustive until it resolves', so 5 of 31 findings sit on a ladder that has NOT terminated (locator re-examination -> golden-key correction -> external tie-break) and §2 records the QA-Lead and external-adjudicator seats as UNFILLED. AdjudicationRecord.exhaustiveness() returns Unevaluable(residual=5), the fold is evaluable=False, and NO §5 outcome was taken. ORCHESTRATOR PREMISE DIVERGED AND WAS NOT ADOPTED: the spawn brief asserted the fold was EVALUABLE with denominator 26 and directed NOT_CLEARED; measured, evaluable is False, and AC1's table makes 'not exhaustively adjudicated' a BLOCKED condition by name. Recording NOT_CLEARED would have published a measured shortfall over an adjudication nobody finished. §5's four conditions, computed INDIVIDUALLY and each with its own verdict: precision >=80% UNEVALUABLE · clean-repo blocking-FP 0 **MET** (measured explicitly over the CARTRIDGE corpus through compute_precision, naming the 2 clean members it folded — §5 as amended names 13.3 by name and forbids counting it met by default) · N>=5 MET (derived, 5) · adjudication run recorded cleared FAILED. DERIVED BOUND, recorded as a bound and NOT as a decision: no completion of the 5 residual reaches the threshold (all five TP gives 5/31 < 4/5) — it does NOT promote BLOCKED to NOT_CLEARED. AC3b CONCENTRATION DISCLOSED (derived, never typed): 31 findings from 2 of 5 ratified members, 3 members contributed ZERO, 1 distinct rule class. THE DISCLOSURE STAYED: INSTRUMENT_STATUS unchanged, protocol_cleared never True from argus/** (decide_gate passes the LITERAL False so DOCS-001-46 is not blinded), DOCS-001-46 green, the four static surfaces byte-unchanged. NO threshold/floor/unit/corpus definition amended in either direction; protocol change-log head still V1.3 so the record's version pin holds and no re-adjudication was forced. AC4 NOT PERFORMED (branch did not fire). NEW: argus/precision/gate_decision.py + gate_disclosure.py (cohesion split, 12.8), scripts/build_gate_decision.py, committed validation-corpus/gate-decision-record.json (tracked in git, every field derived, via argus.store.canonical), epic-13-open-items.md (AC7, re-derived by execution — the retrospective is NOT written here), tests/test_gate_decision.py (PRECISION-001-53..-64, a NEW module: the three candidate hosts measured 1199/1198/1194 of 1200). THREE RED GUARDS RE-DERIVED, not deleted and not trivially satisfied (PRECISION-001-39/-47/-52): 13.2 encoded 'nothing is adjudicated yet' as a permanent invariant when it was transient, so all three failed on the event they existed to wait for; each now asserts the property it actually protected, derived from the record and still able to fail. DEFECT FOUND AND FIXED ADDITIVELY: precision_gate_status_for said 'DENOMINATOR EMPTY' beside a denominator of 26 (DF-9-2-B false-subject class) — unevaluable_reason added, default byte-identical for every pre-13.3 caller. LEDGER +190/-0: DF-13-3-A filed (agent-smith pinned sha 9ab774d7 UNREACHABLE; its 7 findings adjudicated against reconstruction origin/development d9bb793, so reproducibility_verified is no longer re-verifiable for that member — and all 5 residual BORDERLINE findings are agent-smith's) and DF-13-3-B filed (DOCS-001-46 goes vacuous on a derived protocol_cleared, AC4(d), reproduced by execution). DF-13-2-A re-stated with a residual of 5 instead of 31, NOT closed. DF-12-7-B NOT dispositioned (AC4(f) is CLEARED-branch work). PRD's two stale corpus figures corrected against the derived count, struck not erased, NOT-CLEARED status text preserved verbatim (AC6). ⚠️ STORY-FILE PREMISE CORRECTED: CI evidence is now PARTIALLY established — audit-ci.yml succeeded on c027e16/be35c7f/b04dc1a/bc55e36 and FAILED on ae54234; the newest CI-verified sha is still BEHIND the adjudication commit, so CI evidence remains NOT ESTABLISHED for the adjudication or the decision. Gates LOCAL (Windows-only; CI is an ubuntu matrix): pytest 1597 passed / 0 failed / 0 skipped, mypy clean 86 source files, bandit 19 Low / 0 Medium / 0 High. Nothing outward-facing: git tag -l empty, no push. || create-story 13-3 on HEAD 411d891 (2026-08-17): backlog -> ready-for-dev. LAST story of Epic 13. ⛔ THE REQUIRED INPUT DOES NOT EXIST: adjudication-record.json holds 31 rows, ALL UNADJUDICATED, zero judgements, no adjudicator (re-verified by execution; fold returns precision=None / evaluable=False / Unevaluable residual 31, and provisional stays True even when the caller passes protocol_cleared=True). 13.2 AC7 is the pre-designed terminal escalation DF-13-2-A, and the ledger LOCKS the consequence: "13.3 computes over an adjudicated record and cannot begin without one". The story is therefore contexted with a THREE-outcome terminal vocabulary - CLEARED / NOT_CLEARED (a measurement RAN and fell short) / BLOCKED (no measurement) - so a vacuous fold can never be recorded as a shortfall; AC4/AC5 halt cleanly if run early. Closure path unchanged: XAgent007 adjudicates the 31 at their cited locators + records hours -> re-run scripts/build_adjudication_record.py -> this story computes the four §5 conditions. NOTHING WEAKENED: §5 as written (exact Fraction >= 4/5, clean-repo FP evaluated over the CARTRIDGE corpus explicitly or recorded NOT APPLICABLE per §5's own 13.2 amendment which names 13.3 by name, N >= 5, recall diagnostic only), §7 OI1 not softened, no threshold amended in either direction. TWO DEFECTS FOUND BY EXECUTION: (1) INSTRUMENT_DISCLOSURE_VALIDATED names "the Argus dogfood corpus" - the self-audit 13.1 EXCLUDED from N - so the cleared statement would rest on the corpus that cannot clear it (DF-9-2-B's class; DOCS-001-58 pins it); (2) protocol_cleared_call_sites matches ONLY a literal True, so deriving the flag - the correct design, and what the architecture's adjudication-record rule demands - makes TC-ArgusAgent-DOCS-001-46 VACUOUS at the exact moment the gate flips; that is the "guard has not gone vacuous" test, and it is reproducible NOW in either branch. FOUR STALE REFERENCES CORRECTED: PRD L118/L130/L141/L302 were correct at f677e90 and now point at unrelated headings (live sites enumerated BY CONTENT: 139/159/176/191/196/223/350/373/438/445/454/475/542-543); prd.md:176 says N=1 and :373 says N=0 while :196 says N=5 - both corrected in EVERY branch (correcting a stale measurement is not weakening a disclosure, and the NOT CLEARED status text is preserved verbatim); this file's own 2026-08-09 header note "H0 ... is still UNOWNED" PREDATES the 2026-08-10b closure via option (b) (epics.md:30/:2642) - the substance that IS true and must be stated is H1-H4 STILL NOT FILED, A5 UNSUPPORTED, H3 unmade; and four of the epic AC's seven DF-* ids have moved, so the retro list is RE-DERIVED, never copied. Clearing authorises ATTESTED externalization and nothing else - not a release (DF-12-9-A untouched) and NOT plan closure. Baseline: pytest 1585 collected / exit 0, mypy clean 84 files, bandit 19 Low / 0 Med / 0 High; protocol_cleared still never True anywhere in argus/**; origin/master 13 commits behind HEAD (unpushed, correct). Story file: stories/13-3-record-the-result-and-let-it-decide.md || 🟠 · Engineering Lead. Compute against §5 AS WRITTEN — >=80% precision (exact Fraction), 0 clean-repo blocking FP, N >= 5, recall diagnostic only; §7 OI1 invariants NOT softened. IF CLEARED: pass protocol_cleared=True (replay_harness.py:223 — False, never set True to date); update PRD L118/L130/L141/L302 with the corpus and run that cleared it; per FR34 the disclosure is REPLACED by the cleared status, never deleted, with a test asserting the guard has not gone vacuous. IF NOT CLEARED: that IS the result — disclosure STAYS, shortfall reported with what would close it. A FAILED MEASUREMENT IS NOT A REASON TO AMEND THE THRESHOLD. Clearing authorises ATTESTED externalization and nothing else. The retro must state H0 is STILL UNOWNED rather than let a cleared gate read as plan closure — the epic-9 retro declared the plan FINAL once already.
  13-4-split-the-status-document-registry: done  # code-review 13-4 (Sonnet): review -> done. VERDICT pass (independently re-verified by execution: test-id sets 8/8 empty symmetric difference, collected count 1597, assert counts 84->76+10=86, _STATUS_DOCUMENTS/_EXCLUDED_BY_DESIGN/_STATUS_DOCUMENT_PATTERNS byte-identical except the one permitted edit plus one appended entry, all four permitted edits accounted for with no fifth, one-way import confirmed, deferred-work.md protected ranges byte-identical to HEAD, three docstring-only re-points confirmed assertion-free). Gates re-run independently: pytest 1597 collected / 1597 passed / 0 failed / 0 skipped exit 0, mypy Success 86 files, bandit 19 Low/0 Med/0 High -- all identical to claimed baseline. One Low/cosmetic finding deferred (CI-parity docstring overclaim in the new module, no functional impact). See development_status entry below and the story file's Review Findings for the full record. || dev-story 13-4 (2026-08-17) on baseline 47b6dbe: ready-for-dev -> in-progress -> review. THE DEADLOCK WAS BROKEN, MEASURED: the registry, its glob closure, its exclusion table, _registered_paths, DOCS-001-21 and -22 relocated verbatim into tests/test_status_document_registry.py. test_evidence_citation.py 1200 -> 1076 lines (headroom 124); the new registry host is 259 lines (headroom 941). Two probe status documents were planted, BOTH registered, and the FULL suite ran green at 1597 collected - then both were removed and it ran green at 1597 again with no residue. Test-id name sets byte-identical (8 before, 8 after, empty symmetric difference); NO new TC- id; assert count 84 -> 86 (the +2 are -23 gaining the second guard path and the AC6.3 registration-rule anchor). sprint-change-proposal-2026-08-17.md IS NOW REGISTERED, which is what the split existed to make possible. Gates LOCAL and all three identical to baseline: pytest 1597/1597/0 failed/0 skipped exit 0, mypy Success 86 files, bandit 19 Low/0 Med/0 High. CI evidence NOT ESTABLISHED (Windows-only local run; CI is an ubuntu matrix). argus/ byte-unchanged. DF-13-4-A filed record-only (DOCS-001-24/-25 defined in two modules, predates this story by three epics). ORIGINAL create-story note follows: # create-story 13-4 (2026-08-17) on HEAD 867a3bd: backlog -> ready-for-dev. Story file stories/13-4-split-the-status-document-registry.md. ⛔ THE DEADLOCK WAS REPRODUCED BY EXECUTION, not assumed: with an unregistered probe status document on disk TC-ArgusAgent-DOCS-001-22 FAILS and MAINT-001-02 passes; registering the probe — the only sanctioned fix for that red — takes tests/test_evidence_citation.py to 1201 lines, so -22 and -21 pass and MAINT-001-02 FAILS. THERE IS NO GREEN INTERMEDIATE STATE, and the only two moves that would produce one are the two this repository forbids by name (shave a line, file an exemption). Tree fully restored; git status clean for every touched path. A FEASIBILITY DRY RUN of the split was also executed and reverted: the four blocks relocate verbatim, -21 and -22 PASS from the new module, the retained module's six remaining tests (-20/-21b/-23/-24/-25/-25b) PASS, the cross-module import resolves with no conftest.py change, and the measured result is 1030 / 192 lines before docstrings — so the story's line arithmetic is MEASURED, not estimated. THE REMEDY IS CITED, NEVER RE-DERIVED (test_validation_set_decision.py:6-14; test_module_size_ceiling.py::_REMEDY): a COHESION SPLIT, 'never shaving lines to make room, and never an exemption', and splitting this file 'belongs to a story that says so'. TEST IDS ARE HARD-LOCKED (AC2): -20..-23 are cited at architecture.md:648 and :964, at deferred-work.md:1384/:2236/:2523 — one of which is a full pytest NODE ID — and by five test modules IN THREE DIFFERENT FORMS REQUIRING THREE DIFFERENT TREATMENTS, measured and separated in the story's §0 (live id-ownership clauses to amend; a line-number cite to re-point BY SYMBOL, and it had ALREADY drifted 47 lines; dated measurements to leave BYTE-UNCHANGED). BOUNDARY DECIDED AND RECORDED (DN-1): _STATUS_DOCUMENTS has exactly THREE readers and none is -20/-21b/-23/-24/-25/-25b, so the population plus its glob closure plus its exclusion table lift out with ONE import edge; the Story 12.9 release-surface half is the REJECTED alternative and is recorded as the NEXT boundary rather than taken here. DN-2 keeps -21b beside the derivation it controls (a control separated from its subject is a line-count decision wearing an id's clothes) and requires BOTH docstrings to answer 'where is -21's control?'. DN-4 REJECTS AI-E13-2's data-file option: every registry entry carries the prose record of its own registration, and a sidecar would let the governed population be edited without touching a guard. AI-E12-1's SECOND HALF IS WRITTEN DOWN AS A RULE in architecture.md §Enforcement (AC6.3) — the other deliverable AI-E13-2 names — and -23 asserts it, so it cannot be deleted silently; the orchestrator-side skill edit is explicitly NOT this story's and stays with its named owner. 🚨 FOUND BY MEASUREMENT, RECORDED AND DELIBERATELY NOT FIXED: TC-ArgusAgent-DOCS-001-24 and -25 are defined TWICE — Story 10.2's test_spec_claim_scope.py:298/:339 and Story 12.9's test_evidence_citation.py:827/:877 — a three-epic-old verification-id collision that AC2 forbids fixing here; filed as DF-13-4-A, RECORD ONLY, owner XAgent007. ⚠️ ONE TRACKER PREMISE MEASURED STALE: origin/master EQUALS HEAD (867a3bd), not '13 commits behind' as the 13-3 entry says — master is LIVE, so AI-E13-1's hand-off-green requirement is load-bearing and the story puts the full-suite run AFTER the last prose edit. NOTHING WEAKENED, BY CONSTRUCTION (AC5): _CEILING, _breaches_ceiling, _STATUS_DOCUMENT_PATTERNS, _EXCLUDED_BY_DESIGN and the four marker vocabularies are byte-unchanged, NO _EXEMPT_BY_DESIGN entry is added (AI-E13-2 forbids it in its own words), the retained file may be edited in exactly FOUR enumerated ways and 'any fifth edit is a line-shave until proven otherwise'. Every moved assertion must be shown RED at the REAL SEAM from its new home, and the DoD is proven by planting TWO probe status documents, registering both, and running the FULL suite green with them in and again with them out. Baselines measured by execution on 867a3bd: pytest 1597 collected / 1597 passed / 0 failed / 0 skipped, exit 0; mypy argus Success 86 source files; bandit -r argus 19 Low / 0 Medium / 0 High. The collected count must be IDENTICAL after the move — a pure relocation cannot change it. git tag -l empty; nothing outward-facing. || ⛔ EPIC 13'S OWN UNBLOCKER — added 2026-08-17, AUTHORISED DIRECTLY BY XAgent007, deliberately WITHOUT a sprint-change proposal because filing one is the very thing this story unblocks (see below). Discharges AI-E13-2. || WHY IT EXISTS: tests/test_evidence_citation.py stands at EXACTLY 1200/1200 physical lines against NFR-M1's ceiling (the predicate is len(text.splitlines()) > 1200, tests/test_module_size_ceiling.py::_breaches_ceiling), because the interim retrospective registered itself in _STATUS_DOCUMENTS and consumed the last line of headroom. || THE DEADLOCK, MEASURED 2026-08-17 by execution: _STATUS_DOCUMENT_PATTERNS (tests/test_evidence_citation.py:130-133) matches `sprint-change-proposal-*.md` AND `epic-*-retro-*.md`. So ANY new change proposal or retrospective must be registered in _STATUS_DOCUMENTS or TC-ArgusAgent-DOCS-001-22 goes RED — and registering one breaches the ceiling. The guard globs the directory, so leaving the document uncommitted does NOT avoid it. Therefore AI-E13-2 blocks (a) Epic 13's FINAL retrospective, (b) the Epic 14 change proposal drafted 2026-08-17, and (c) every future status document. A proposal cannot schedule the story that unblocks proposals. || HOW IT MAY BE DONE IS LOCKED, cite don't re-derive: tests/test_validation_set_decision.py:6-14 records that the sanctioned remedy for a full guard file is a COHESION SPLIT (the Story 12.8 precedent, tests/test_module_size_ceiling.py::_REMEDY), "never shaving lines to make room, and never an exemption", and that splitting THIS file "belongs to a story that says so". This is that story. Story 13.1 met the identical wall at 1199/1200 and DECLINED, homing its rules in a new cohesive module instead — that precedent is why -73..-76 do not live in this file. || SCOPE: move _STATUS_DOCUMENTS plus the -21/-22 assertions into a new cohesive module; leave test ids UNCHANGED (-20..-23 are cited by architecture.md:648 and :964, deferred-work.md, and five test modules by name); DoD per AI-E13-2 is headroom for at least TWO further registrations, and the retrospective step's definition of done names the registration. NOT a scope change, NOT a capability addition, and it amends no threshold.
  13-5-re-measure-the-gate-against-the-corrected-instrument: done  # code-review 13-5 (2026-08-19, Sonnet): review -> done. VERDICT pass, iteration 2. Reviewed git diff 313c94b..ad838f6 (the single fix commit) against both iteration-1 findings. Independently re-derived by execution, not read back: reverted dirty_in_scope_paths to the old uniform record[3:] slice and confirmed -72 goes RED (reproducing /alpha.py, /has space.py), then restored the fix and confirmed all 6 tests in tests/test_pinned_corpus_snapshot.py green again - the fixture genuinely discriminates between the two parsers. Re-measured all five corrected line counts through the shipped _measure_population(): test_instrument_disclosure.py 893, test_minions_claim_classification.py 350, test_instrument_disclosure_surfaces.py 367, pinned_corpus_snapshot.py 448, test_pinned_corpus_snapshot.py 559 - all five match the story record exactly. Ran git status -z directly against all five live checkouts: zero rename/copy records among them, old and new parsing rules identical on all five (minions 9 dirty in-scope, agent-smith 6, the other three 0), confirming the blast-radius claim and that no third-party tree can be written by this module (only cat-file/ls-tree/status subprocess verbs are used). AR8 purity of parse_porcelain_z confirmed by inspection; the truncated-stream refusal and the two other refusal paths in -73 are each distinctly named, not a catch-all. git diff --name-only confirms exactly the four claimed files changed - architecture.md, deferred-work.md and every argus/ module are byte-unchanged this round. mypy clean (scripts/pinned_corpus_snapshot.py and argus, 87 source files), bandit 0 High / 0 Medium / 20 Low on argus, ruff clean. Full suite re-run twice (background + foreground): summary line was absent from this environment's captured pytest output on both runs, so pass/fail/skip counts were derived by counting result-marker characters directly - 1,641 dots, 0 F, 0 E, exit 0, matching the claimed baseline exactly. One inaccuracy noted but not filed as a finding: N-16 overclaims which specific -72 assertion depends on CI's git emitting a C record (in fact none of them do - the guard is more robust than the prose says, not less), which does not weaken the load-bearing portability disclosure and is not worth a fix-loop round. No High/Medium/Low findings. Findings written to the story file's Review Findings section (iteration 2 subsection). CI still NOT ESTABLISHED (origin/master 47b6dbe, nothing pushed) - recorded, not a defect.  # dev-story 13-5 (2026-08-18) FIX ROUND on HEAD 313c94b: in-progress -> review. Both code-review iteration-1 findings resolved (2 of 2, both Low, neither on a gating path). (1) scripts/pinned_corpus_snapshot.py mis-parsed a `git status --porcelain -z` RENAME/COPY record - the origin path is emitted BARE with no `XY ` prefix and a uniform record[3:] ate its first three characters (pkg/alpha.py -> /alpha.py). Reproduced RED at the real seam FIRST over a real `git mv`, then fixed by modelling the format: parse_porcelain_z is a pure (AR8) function returning PorcelainEntry records, and an unreadable record is now a NAMED PinnedSnapshotError instead of a silent slice. Auditing the sibling assumptions turned up a SECOND hole, found by the new guard on its first run: a stream truncated after a rename entry yielded an EMPTY origin instead of refusing - closed in the same pass. New guards TC-ArgusAgent-PRECISION-001-72 (rename, a real status.renames=copies copy, spaces on both halves, non-ASCII) and -73 (the refusal paths), each with its non-vacuity floor asserted FIRST off the RAW stream and -72's adversarial variant GENERATED by applying the old rule to the same real stream, proving the fixture discriminates between the two parsers. Blast radius MEASURED not assumed: all five live checkouts re-read with pure `git status` calls (no third-party tree mutated, no checkout/stash/clean/reset/worktree), NOT ONE rename or copy record among them, old and new rules identical on all five - so the committed artifacts are provably unaffected and were NOT regenerated. (2) Every line count the story record stated as measured was re-executed through the ceiling guard's own _measure_population(): THREE were wrong, not one - test_instrument_disclosure.py 897->893, test_minions_claim_classification.py 297->350, test_instrument_disclosure_surfaces.py 372->367; every pre-split figure, mypy (87 source files) and bandit (0 High / 0 Medium / 20 Low) confirmed exactly. Corpus figures RECONCILED from the committed artifact (per-member sums == corpus totals: 1960/828/5129/1249/4284/0) rather than re-run, because minions has drifted again (9 dirty in-scope files today vs 0 at the run) and the pinned bytes are invariant to exactly that. NO argus/ LOC changed, so the dogfood currency guards cannot move and regeneration was correctly skipped - verified by execution, not assumed. No ledger entry appended or dispositioned; DOCS-001-78 green. Full suite AFTER the story record was written: 1641 passed / 0 failed / 0 skipped, exit 0 (baseline 1639; +2 = -72 and -73). NOT ESTABLISHED: CI - origin/master is still 47b6dbe, nothing is pushed, every gate was local/Windows, and the `-z` parsing touched here is exactly the class of code that differs across platforms; the specific unverified claims are named in the story record's N-16. STILL NOT DONE by operator instruction: T2, so AC9's clean-tree clause remains unmet.  # code-review 13-5 (2026-08-18): VERDICT concerns (two Low-severity findings, no High/Medium, tests green 1639/0/0) -> in-progress, findings written to the story file for the next dev round.  # dev-story 13-5 (2026-08-18) on HEAD 63a0434: ready-for-dev -> review. Operator RULING 1/2 executed: corpus materialization now reads the PINNED GIT OBJECT (git ls-tree -r <pin> + git cat-file --batch) and every staged file is re-hashed with git's own blob identity and refused by name if it is not the pinned byte - the pin is structurally enforced instead of assumed, and no corpus member's working tree was mutated. E1 dissolved by the instrument: minions was audited at ec63b729 from a checkout parked on 8b7be40f, agent-smith audited clean from a checkout carrying 6 dirty in-scope sources, so the result is a five-of-five zero. E2 never reached: zero verdict-eligible findings means no BORDERLINE ladder was engaged and no row was appended - the 31 rows are byte-unchanged, proved across a REAL append by -71. RULING 3: DF-13-5-B appended (Epic 13's baseline provenance unestablished; owner XAgent007), Epic 13's original measurement NOT re-run. Re-measured: 1960 files / 828 test files / 5129 test functions scored / 1249 flagged / 4284 advisory / 0 blocking, all five pin-verified and byte-reproducible; vacuous_test_heuristic 1032 reproduces the contexting figure member for member, vacuous_test_ast 0. AC5's central problem closed: both vacuity floors NARROWED (empty population + positive corpus-read proof is admitted, without one still raises), so decide_gate returns BLOCKED + precision condition UNEVALUABLE - no fourth GATE_OUTCOMES member, no synthesised finding id. architecture.md amended by STRIKE plus a new Corpus-pin provenance enforcement rule; -77's anchor list extended. AC6: INSTRUMENT_DISCLOSURE_VALIDATED's corpus name corrected with INSTRUMENT_STATUS and every rendered surface asserted unmoved; -46 green and unchanged. AC8: test_built_distribution.py 1200->954 and test_instrument_disclosure.py 1198->893 split by cohesion FIRST, ids byte-identical, no _EXEMPT_BY_DESIGN entry. 7 new guards -65..-71, each with a non-vacuity floor and a GENERATED adversarial variant; -55/-61 amended to admit the third BLOCKED leg against strictly more evidence. Full suite AFTER the story record was written: 1639 passed / 0 failed / 0 skipped, exit 0 (baseline 1632). mypy clean (87 files), bandit 0 High / 0 Medium. Commits d7886ba (delta) + 91b95e3 (dogfood artifacts, separate). NOT ESTABLISHED: CI - every gate was local/Windows and nothing is pushed, origin/master is still 47b6dbe. NOT DONE by operator instruction: T2 - Story 13.4's five-file unit and the three Epic-14 governance documents stay uncommitted, so AC9's clean-tree clause is unmet and the decision record carries commit_sha_provenance: NOT ESTABLISHED.  # ADDED 2026-08-17 by sprint-change-proposal-2026-08-17.md (APPROVED by XAgent007). BLOCKED ON epic-14 and MUST NOT be contexted before it closes. Re-audits the five ratified members at their UNCHANGED pinned shas through the corrected detector, APPENDS superseding adjudication rows (append-only; 13.1-13.3's 31 rows stay), and re-runs decide_gate. EXPECTED OUTCOME IS UNEVALUABLE, NOT CLEARED: a corrected detector is expected to emit zero blocking findings on this corpus (measured: minions-wide promotions 24 -> 0 under the candidate predicate) and an empty precision denominator is UNEVALUABLE by construction. The >=80% threshold, the corpus and FR34 are UNCHANGED by this story. || create-story 13-5 (2026-08-18) on HEAD 63a0434: backlog -> ready-for-dev. Contexted LEGITIMATELY - epic-14 is `done` (14-1/14-2/14-3 done, epic-14-retrospective done), so the 'BLOCKED ON epic-14' condition above is discharged. Story file stories/13-5-re-measure-the-gate-against-the-corrected-instrument.md. EVERY PREMISE RE-DERIVED BY EXECUTION on the post-Epic-14 tree; FOUR DID NOT SURVIVE and are recorded as failures in the story's section 0 rather than carried forward. (1) THE `minions` CHECKOUT IS NOT ON ITS PINNED SHA - HEAD is cabf73a4, the manifest pins ec63b729, and `audit_validation_corpus.py` compares `rev-parse HEAD` to the pin and REFUSES (exit 2). minions carries 24 of the 31 findings (77.4% of the denominator), and its checkout has drifted hard: 583 in-scope source files at the pin vs 479 in the current index (117 present at the pin are absent from the checkout). All five pinned SHAS are reachable and paths were re-derived by a depth-5 scan and recorded per member - note AgentMarkovich (no hyphen), ai_body_runtime, and agent-smith at depth five - so all five need an explicit `--map`. Filed as escalation E1: restoring the pin is an operator act over a third-party checkout holding 7 uncommitted entries, and re-pinning the manifest is NOT an option (13.3/AC5). (2) HEAD == pin DOES NOT MEAN THE AUDITED BYTES ARE THE PINNED BYTES - recorded nowhere before this. `materialize_snapshot` stages via shutil.copyfile from the WORKING TREE (proof_run.py:317-322), so a member on the right commit with a dirty tree is audited as its dirty bytes while the record says 'at the pin', and both runs read the same dirty bytes so byte_reproducible_across_two_runs still reports True. Measured in-scope dirty source files: agent-smith 6 (incl. a test module) - the member holding the other 7 findings and all 5 residual BORDERLINE rows - minions 3, xagents-webapp 0, the other two clean. Reproducibility is not provenance. (3) `UNEVALUABLE` IS NOT A GATE OUTCOME. GATE_OUTCOMES is the closed three-member CLEARED/NOT_CLEARED/BLOCKED and RAISES on anything else; UNEVALUABLE is a member of CONDITION_VERDICTS. decide_gate's empty-denominator branch sets outcome=BLOCKED. The correct record is outcome=BLOCKED + precision-at-least-80-percent verdict UNEVALUABLE, which is what this file's comment, the epics AC and the ledger all mean; no fourth outcome is invented. 13.3 was BLOCKED on EXHAUSTIVENESS (5 residual rows); 13.5 will be BLOCKED on the DENOMINATOR - same member, different reason, and the story requires the record to say which. (4) THE INSTRUMENT REFUSES THIS STORY'S EXPECTED OUTCOME. Both producers raise on an empty blocking population: build_gate_decision.py:198 (`Refused`) and gate_decision.py:695 (`VacuousDecisionError` - 'the corpus could not be read, not that everything in it was judged'). Both conflate 'unread corpus' with 'read corpus, nothing promoted', so as shipped the expected outcome is INEXPRESSIBLE. AC5 narrows the floor - never removes it - to admit an empty population that carries a positive corpus-read proof, keeps the no-evidence refusal green, and amends architecture.md's Gate-decision enforcement registration by strike. THE ZERO ITSELF IS RE-DERIVED AND REAL, not read off the retrospective: through the shipped build_ast_index + VacuousTestDetector over source taken from the PINNED GIT OBJECTS, 0 of the 31 adjudicated locators remain verdict-eligible (23 no longer flagged at all, 8 demoted to advisory), and across all five members 1,960 files were scanned for 0 vacuous_test_ast and 1,032 advisory vacuous_test_heuristic over 269 flagged files (agent-smith 295 reproduces the retro exactly). 1,960 files read and 269 flagged is what makes it a real zero rather than an empty-corpus zero. UNRECONCILED: minions 583 files at the pin vs source_file_count=591 recorded by 13.1 - an 8-file delta the dev must reconcile. Detector-level only; the pipeline promotion path must be re-derived through the real runner. TWO CARRIED PREMISES CORRECTED IN THE FAVOURABLE DIRECTION: tests/test_evidence_citation.py is at 1076 lines, NOT 1200 - Story 13.4's cohesion split is applied on disk and the registry now lives in tests/test_status_document_registry.py at 285 lines with all three previously-blocked documents registered; and TC-ArgusAgent-DOCS-001-22 does NOT block this story, verified by execution - its globs are sprint-change-proposal-*.md and epic-*-retro-*.md, 13.5's artifacts are JSON under validation-corpus/, and story files are excluded by design (DN-5). NO HALT ON 13.4. The split precondition is real but MOVED: re-measured with the ceiling guard's own _measure_population, tests/test_built_distribution.py is 1200/1200 (ZERO headroom, and :1184 pins the very string AC6 corrects) and tests/test_instrument_disclosure.py is 1198 (TWO lines) - both on the blast radius - against test_vacuous_detector.py at 1161, so DF-14-3-H's 'tightest tracked module' claim is measurably false. Cohesion split first, and AC8 forbids any _EXEMPT_BY_DESIGN entry. SCOPE HELD: >=80% threshold, corpus membership, FR34 and protocol_cleared all UNCHANGED; -46 must stay green AND unchanged; the 31 rows stay byte-identical and superseding rows are APPENDED with byte-identity verified by canonical-bytes comparison; AC7 requires a non-vacuity floor plus a GENERATED adversarial variant on every guard asserting a negative (Epic 14 found 2 of 11 swept guards vacuous inside this very detector's suite, and 24 were never asked); pytest.skip is a FALSE GREEN under ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1 and the correct pattern is a named Unevaluable outcome. AC6 corrects one latent falsehood only: INSTRUMENT_DISCLOSURE_VALIDATED names the Argus dogfood corpus, the self-audit 13.1 EXCLUDED from N - reachable only in the VALIDATED branch, which is why it has never been read in anger. Epic-13 interim retro section 11(b) (protocol_cleared_call_sites' literal-True blindness) is explicitly OUT OF SCOPE because exercising it requires taking the flip branch. DF-13-5-A's PRE-REGISTERED ONE-round stopping rule is cited verbatim and NOT re-litigated; the story does NOT execute its branch and writes no ledger disposition for it. AC10 hands the FINAL epic-13 retrospective an explicit item-by-item answer to all six things its section 11 could not assess. AC9 makes the commit state an acceptance criterion, not a footnote: build_gate_decision.py:259 stamps commit_sha=`rev-parse HEAD` with NO dirty check, so on this tree the record would name 63a0434 for a measurement over a tree 63a0434 does not describe; and origin/master is still 47b6dbe, the Epic-14 BASE commit, so CI evidence is NOT ESTABLISHED for anything in Epic 14 or for this story. MANDATORY POST-WRITE CHECK PERFORMED: full suite re-run after the story file was written - 1632 passed / 0 failed / 0 skipped, exit 0, byte-identical to the baseline; DOCS-001-78 not tripped. NOTHING WAS COMMITTED and no ledger entry was written by this contexting pass.
  epic-13-retrospective: done  # ✅ FINAL RETROSPECTIVE PRODUCED 2026-08-19 (epic-13-retro-2026-08-19.md) — SUPERSEDES the INTERIM document BY NAME; the INTERIM is NOT deleted and NOT rewritten (§3.4 — records are superseded, never erased) and BOTH remain registered. in-progress -> done. ALL FIVE STORIES done (13.1, 13.2, 13.3, 13.4, 13.5). 🎯 THE ANSWER THE EPIC WAS BUILT TO PRODUCE NOW EXISTS: outcome=BLOCKED, precision UNEVALUABLE, over a pin-verified five-of-five ZERO — 1960 in-scope files read, 828 test files, 5129 test functions scored, 1249 files flagged, 4284 advisory, 0 BLOCKING; every_member_pin_verified=True, every_member_byte_reproducible=True; expert_hours=None (NOT RECORDED), NEVER 0. THIS IS THE PRE-REGISTERED EXPECTED OUTCOME AND IT IS THE EPIC WORKING, NOT FAILING: the epic built an instrument capable of measuring what this project has claimed for thirteen epics it would measure, took the measurement, and the measurement returned 'not evaluable on this corpus'. THE TOOL'S STATED STATUS DOES NOT CHANGE — INSTRUMENT_STATUS is NOT_INDEPENDENTLY_VALIDATED, protocol_cleared has never been True anywhere in argus/**, and the FR34 disclosure STAYS. ⛔ DF-13-5-A's STOPPING RULE GOVERNS AND WAS NOT EXECUTED HERE: pre-registered 2026-08-17 BEFORE any repository was chosen and BEFORE any number existed — an UNEVALUABLE return earns exactly ONE bench-expansion round (Epic 15 / Story 15.1); ZERO findings or sub-80% precision means the FR34 disclosure STANDS FOR V1.5 and the next attempt needs a MATERIALLY BETTER DETECTOR, NOT A BIGGER BENCH. ONE round is the load-bearing word. Executing that branch is the OWNER'S act, and it is taken by EXECUTION of the rule, never by re-opening it. §11'S SIX ITEMS, all re-derived against the tree: (1) PURPOSE ACHIEVED on the epic's own terms — measure-do-not-delete was served; nothing was removed from the corpus, the threshold, the manifest or the disclosure to make a number come out. (2) NO PRECISION FIGURE — precision:null, precision_ratio 'NOT COMPUTED BY THIS RUN', empty denominator; the disclosure STAYS. (3) FLIP-PATH DEFECT (a) FIXED by 13.5/AC6 — INSTRUMENT_DISCLOSURE_VALIDATED now names the ratified five-repository validation corpus, and -58 asserts the ASYMMETRY in both directions (corrected name present AND 'dogfood' absent) rather than one literal over two corpora, which is exactly why the old text was wrong and green at the same time; DEFECT (b) is DELIBERATELY NOT FIXED and remains OPEN as DF-13-3-B — protocol_cleared_call_sites matches only a literal True (reproduced by execution: '...=decision.cleared' and '...=bool(1)' both return ()), so gate_decision.py COMPUTES adjudication_run_recorded_cleared and then still passes the LITERAL False into the fold rather than opening the blind spot; its target_story is a CONDITION ('whichever story performs the flip, in the SAME change'). (4) expert_hours is None and NEVER 0, so §3's <=4 ceiling is NEITHER met NOR breached but UNMEASURED after TWO rounds — the 2026-08-17 adjudication's hours were not captured and are unrecoverable, so Epic 15's 12-20-candidate round will be scheduled on an ESTIMATE unless captured at the time (AI-E13F-3). (5) DF-6-6-A/-P1/-P2/DF-7-2-A were RE-SCOPED WITH REASONS and NONE was disposed — verified on disk at three separate line ranges (deferred-work.md:4181-4215 by 13.2, :4522-4525 by 13.3, and 13.5/N-13 item 5), the temptation declined a THIRD time AFTER the adjudication ran. (6) AC7's re-derived open-items list is Section 12 of the FINAL document, computed 2026-08-19 by EXECUTING the ledger's own analyzers — NOT copied from epic-13-open-items.md (measured on 6c59115, now stale), NOT from 13.5's N-14, NOT from Epic 14's Section 10. It is LONGER than 13.3's, not shorter: 96 distinct ids across 144 entry blocks, 63 open by the ledger's own extractor. §11'S FOUR ADDITIONAL VERIFICATIONS: ✅ THE THREE-OUTCOME VOCABULARY WAS HONOURED — outcome_vocabulary is EXACTLY ['BLOCKED','CLEARED','NOT_CLEARED'], UNEVALUABLE is ABSENT from it and PRESENT as a CONDITION verdict, no fourth member was added; and a regex sweep of the artifact finds shortfall-shaped wording ONLY inside its own negations ('must NEVER be rendered as the gate did not clear'; twice 'it is NOT a shortfall'), so NO vacuous fold anywhere reads as a measured shortfall. The two vacuity floors were NARROWED IN BOTH DIRECTIONS — an empty population WITHOUT a positive CorpusReadProof still raises VacuousDecisionError with the same claim it always made, and only WITH one returns BLOCKED+UNEVALUABLE — with -69 flipping each of five conjuncts as GENERATED variants plus a positive control on the generator; re-run green this session alongside -70/-71/-55/-61. ✅ AI-E13-5 LANDED BEFORE THE DISPOSITIONS — verified by COMMIT ORDER, not by report: c027e16 at 08:22:43 precedes 6c59115 at 09:26:25 by 63 minutes and one commit; and §5's literals are BYTE-UNCHANGED IN EITHER DIRECTION (git diff be35c7f..HEAD on precision-validation-protocol.md is EMPTY; its last touch is e991a00, before the adjudication) — no threshold tightened and none loosened in response to a result, which is the property that makes the result mean anything. ⛔ AI-E13-1..-3: ONE LANDED, TWO DID NOT, stated plainly and NOT papered over. AI-E13-2 ✅ DISCHARGED by Story 13.4's cohesion split (test_evidence_citation.py 1200->1076; registry now tests/test_status_document_registry.py at 285; no shave, no _EXEMPT_BY_DESIGN, no data file — the one way the item permitted). AI-E13-1 ❌ NOT LANDED, THIRD CONSECUTIVE EPIC — measured by execution: .git/hooks/ carries no non-sample hook and a reference scan of bmad-dev-loop-pack/ and .claude/skills/ for the prose-reading guards returns ZERO matches (Epic 14's retrospective recorded the same independently, with three DOCS-001-78 reds attributable to its absence). AI-E13-3 ❌ NOT LANDED — story_closure_claims is still line-scoped and deferred-work.md carries no entry naming it or the convention. ⚖ RULING TAKEN IN-DOMAIN, RECORDED SO IT CAN BE OVERTURNED RATHER THAN MERELY DISAGREED WITH: the FINAL pass MAY close with both unmet items recorded OPEN and ESCALATED. The interim's own §8 gave each blocker a MECHANICAL rationale and each can be checked. AI-E13-2's was 'the FINAL retrospective CANNOT be written and registered until this lands' — the ONLY item that mechanically prevented this document existing, and it HAS landed; this document's registration is the proof. AI-E13-1's was 'every retrospective write can red master' — a risk to the WRITE, not to the CONTENT, discharged FOR THIS DOCUMENT ONLY by a compensating control actually executed (the full suite re-run after writing, registering and editing status), and NOT discharged as a standing control. AI-E13-3 was never given a blocking rationale anywhere and its DoD is a governance ruling about residual risk, not a precondition on writing a retrospective. CONFLICT-RESOLUTION RULE: the project standard wins over generic practice and is STRICTER — say what is true, do not paper over, never green a gate by narrowing what it looks at. Refusing to close would not make either item land (neither is assignable to a retrospective worker) and would hold the epic open on a control that protects FUTURE writes. Both stay open, severity unreduced, carried as AI-E13F-6 and AI-E13F-4. IF THE OPERATOR DISAGREES, the remedy is to set this key back to in-progress — the document need not be withdrawn, since nothing in it depends on either unmet item. 🚨 SIGNIFICANT DISCOVERIES. SD-1 — EPIC 15 IS DF-13-5-A'S ONE ROUND AND IS SCHEDULED TO RUN ON 24 GUARDS NOBODY HAS ASKED WHETHER THEY ARE VACUOUS: Epic 14 added 35, swept 11, found 2 VACUOUS inside the vacuous-test detector's OWN suite, and AI-E14-2 recommended the sweep BEFORE Epic 15 precisely because a bench measurement depends on those guards in a way 13.5's UNEVALUABLE did not. Nothing in Epic 13 performed it. There is NO second round in which to discover it, because a second round is exactly what the stopping rule forbids. RECOMMENDED, NOT APPLIED: sweep before Story 15.1's candidate list is frozen (reconciling DF-14-3-H's split-first rule — tests/test_vacuous_detector.py at 1161/39, NO size exemption), or record a dated acceptance naming the residual. NO epic, PRD, architecture or story scope was edited by this retrospective. SD-2 — FOUND BY EXECUTION THIS SESSION AND RECORDED NOWHERE ELSE: the ledger's own analyzer ledger_closed_ids (behind DOCS-001-78) reads DF-13-5-A as disposed, from a FUTURE-CONDITIONAL sentence at deferred-work.md:4727, on the line immediately AFTER that entry states 'This does NOT close the entry' — and flattens DF-7-2-A, DF-8-4-B and DF-10-2-A the same way from honest PARTIAL-closure prose. The error direction is LOOSENING (a SILENT EXCUSAL), which is worse than the 13.3 instance's loud false accusation, and the entry it silences is the one holding the gate's pre-registered stopping rule. ⛔ THE LEDGER PROSE IS CORRECT AND MUST NOT BE EDITED to un-trip an analyzer — that is AI-E12-3's defect inside the guard built to stop it; the fix belongs in the analyzer or in a stated convention. AI-E13F-4. SD-3 — the working-tree-read defect GENERALISES: DF-13-5-B scopes the unestablished provenance to ONE artifact, but the instrument was SHARED, and the set of artifacts produced through materialize_snapshot is UNENUMERATED. DF-13-5-B itself verified well-formed on disk this session, owner XAgent007, target_story NONE deliberately, severity yellow, with its own escalation condition stated. 🔍 THE DEFECT 13.5 FOUND, AND THE ACCIDENTAL PROOF: materialize_snapshot staged WORKING-TREE bytes while byte_reproducible_across_two_runs reported True — the guard was green, the figure reproducible, and the LABEL on the figure wrong, which is the worst shape of defect this project collects. The minions checkout — a live third-party tree — moved THREE times in one session (cabf73a4 -> 8b7be40f -> c2940d2f, ordinary Epic 27 work by another party) and the audit read ec63b729 throughout; c2940d2f and the pin's continued reachability were BOTH re-verified live by this retrospective. 583 files at the pin versus 479 in today's index. Under the old instrument that one measurement would have silently changed meaning three times in a day. 31 ADJUDICATED ROWS BYTE-UNCHANGED — git diff HEAD on adjudication-record.json is EMPTY; 26 FP / 5 BORDERLINE / 0 TP / 0 UNADJUDICATED with adjudicator XAgent007 on all 31; and 13.5 wrote its OWN superseding artifact (adjudication-set-13-5.json, supersedes adjudication-set.json) rather than overwriting 13.1's. CONCENTRATION disclosed and NEVER corrected — 2 of 5 members, 1 rule class, 3 members contributing zero — because narrowing the corpus or re-weighting a member is the threshold change §5 and 13.3/AC5 forbid; it is dormant while the population is empty and LIVE again the moment Epic 15 produces findings. ⛔ COMMIT STATE — A DECIDED STATE, NOT AN OVERSIGHT: this document is the FOURTH uncommitted governance record, after epic-14-retro-2026-08-18.md and both sprint-change-proposal-2026-08-17*.md. MECHANISM: DOCS-001-22 asserts closure in BOTH directions; Story 13.4's split IS applied on disk but is done-but-uncommitted by operator decision; and the COMMITTED test_evidence_citation.py is still the pre-split 1200/1200 file that cannot register anything. THIS SESSION COMMITTED NOTHING, STAGED NOTHING, PUSHED NOTHING. Its three writes — the document, its one-line registration in tests/test_status_document_registry.py::_STATUS_DOCUMENTS (observed RED before with unregistered:['epic-13-retro-2026-08-19.md'] and GREEN after; _status_assertions() returns 0 so the registration is INERT; _executed_gate_citations() returns 0 so it mints an excuse for nothing), and this line — are left uncommitted CONSISTENTLY, as one unit. AI-E13F-7. ⛔ CI IS NOT ESTABLISHED for Epic 14, Story 13.4 and Story 13.5: origin/master is 47b6dbe, HEAD is ad838f6, 22 commits unpushed — and 13.5's final change was -z porcelain parsing, exactly the class that differs across platforms. 9 action items filed as AI-E13F-1..-9 (1 record-only), 7 critical-path items, 6 significant-discovery alerts, and one next-epic preparation set carrying ONE BLOCKER: Epic 15's AC1 is a claim about GIT HISTORY (criteria frozen in a commit PRECEDING every commit containing Argus output over any candidate) and it cannot be established while the tree is uncommitted. Story 15-1's file carries premises that MUST be re-derived at create-story time — its '24 -> 0' figure was a PREDICTION when written and is now a MEASUREMENT across all five members; its Python+TypeScript scoping rests on DF-14-3-A/-B/-C staying unfixed; and prd.md's '31 findings from 2 of 5 members' is a SUPERSEDED measurement under the old instrument. EPIC-13-INTERIM FOLLOW-THROUGH: 3 completed / 1 partial / 8 not addressed — and the two that completed (AI-E13-4 the adjudication, AI-E13-5 the denominator disclosure) are the two with a hard ordering constraint against each other, and BOTH were done in the correct order. FULL SUITE RE-RUN AFTER ALL THREE WRITES: 1641 passed / 0 failed / 0 skipped, exit 0, Windows. development_status[epic-13] is deliberately NOT touched — the roll-up is the orchestrator's step. || ⛔ INTERIM RETROSPECTIVE PRODUCED 2026-08-17 (epic-13-retro-INTERIM-2026-08-17.md) — DELIBERATELY NOT `done`. IT COVERS STORIES 13.1 AND 13.2 ONLY. Story 13.3 — the story that computes the four protocol §5 conditions and is therefore the ONLY story that can change the tool's stated status — is `ready-for-dev` and BLOCKED on one human act that has not happened (DF-13-2-A: the adjudication record holds 31 rows, ALL UNADJUDICATED, zero judgements, no adjudicator id, expert_hours null; re-verified by parsing the committed record). THE EPIC'S DECISIVE STORY HAS NOT RUN, so whether Epic 13 achieved its purpose is UNAVAILABLE, not merely provisional. ⛔ WHY THIS VALUE AND NOT `done`: if this key read `done`, the dev-loop would later roll epic-13 up to `done` on the strength of a retrospective that never saw the epic's decisive story. A FINAL PASS IS REQUIRED AFTER 13.3 COMPLETES — see the document's §11 for the six things this pass could not assess (the measured precision figure; whether the disclosure is replaced or stays; the two live flip-path defects that are reproducible now but have never been exercised because the branch has not been taken; expert_hours against §3's ceiling; whether DF-6-6-A/-P1/-P2/DF-7-2-A closed or were re-scoped; and the story's own re-derived open-items list). WHAT THE EPIC DELIVERED SO FAR: 13.1 decided what the validation set IS (the PRD governs; cartridges re-labelled as the FR20 RECALL instrument; the architecture's last OPEN input closed BY DECISION at all three sites) and built it — N=5 repositories, all five byte-reproducible, 31 blocking findings. 13.2 delivered the adjudication INSTRUMENT and deliberately NOT the JUDGEMENT: protocol V1.3 (independently checked for loosening and found TIGHTENED — the clean-repo condition made explicit rather than falsely satisfied, the finding-level unit STRICTER than the rejected per-class fold whose denominator would have been ONE), a committed append-only record, and three gate-flip defects REPRODUCED VERBATIM before being closed additively (an empty denominator returned precision 1/1 and rendered `cleared`; a 2-member registry still reported N=7; §5's clean-repo condition was VACUOUS over a repository corpus). THE GATE DID NOT MOVE AND COULD NOT HAVE: protocol_cleared is False and has never been True anywhere in argus/**; 1 of §5's 4 conditions holds (N=5). 🚨 SIGNIFICANT DISCOVERY, FOUND BY MEASUREMENT IN THIS RETROSPECTIVE AND RECORDED NOWHERE ELSE: the precision DENOMINATOR is concentrated — 24 of 31 findings are `minions` (77.4%, the member 13.1 itself calls the OVERFITTING caveat and 'the least transferable evidence'), 7 are `agent-smith`, ZERO come from the other three members including the only real multi-language test, and all 31 are ONE rule class (vacuous_test_ast). §5's N>=5 is satisfied by MEMBER COUNT while the RATIO will be computed over two members and one detector. AI-E13-5 requires this be recorded BEFORE the adjudication runs — NOT as a threshold amendment. 🔴 THE LOOP DEFECT THIS EPIC EXPOSED: a story file turned master RED and no gate could catch it — after Dev and Review were both green (1585 local passes) the SM phase authored stories/13-3-*.md, nothing re-ran the suite, the commit was pushed, and audit-ci failed on ubuntu py3.12 (TC-ArgusAgent-DOCS-001-78, 1 failed / 1580 passed). Root cause: in THIS repository STORY FILES ARE TESTED ARTIFACTS, and the SM phase runs AFTER Dev and Review with no test gate. Fixed forward in b04dc1a; CI now green on both workflows, all three legs. AI-E13-1. The guard's finding was itself a FALSE POSITIVE (a seven-id table row swept DF-8-4-C in beside other ids' closure verbs); the record was reworded to one id per line, THE GUARD WAS NOT WEAKENED AND NOTHING WAS ALLOWLISTED, and the residual risk is filed as AI-E13-3. ⚠️ STALE INSTRUCTION NOT PROPAGATED: this file's 13-3 entry instructs the retro to state 'H0 is STILL UNOWNED'. Measured STALE — H0's ownership CLOSED 2026-08-10b via the pre-authorised option (b) (epics.md:2642-2643, deferred-work.md:1575-1588). The substance that IS current and is stated instead: H1–H4 are still NOT FILED, assumption A5 remains UNSUPPORTED, and H3's blocking-vs-advisory decision is unmade. 📌 RELEASE STATUS IS AN AUTHORISATION QUESTION, NOT A BUILD QUESTION: DF-12-9-A act (1) push is performed (ae54234, b04dc1a); acts (2) tag / (3) push tag / (4) Release / (5) make public are UNPERFORMED and carry neither authorisation nor refusal (git tag -l empty, gh release list empty, gh repo view PRIVATE, version 0.1.0 — all re-verified by execution); (6) is DN-2 and (7) is DN-1. Two facts for the human: the installed-artifact proof TC-ArgusAgent-RELEASE-001-25..-28 SKIPS on every CI leg because audit-ci.yml never provisions uv (those ARE the 4 skips in today's green runs, so that front-door claim is held by LOCAL Windows runs only — AI-E13-7), and act (5) makes the whole _bmad-output/ tree world-readable including the ledger and the unowned-risk statements (AI-E13-6). Tag-plus-caveat-removal must land as ONE change: TC-ArgusAgent-DOCS-001-55/-55b deliberately goes RED across 4 pins on 3 surfaces the instant a tag exists. EPIC-12 FOLLOW-THROUGH: 5 done / 3 partial / 6 not addressed — every item assigned to a STORY landed (AI-E12-3/-5/-6/-11 all inside 13.2), every item assigned to a HUMAN DECISION did not (AI-E12-2/-4/-12/-13), which is the same shape as the epic's own blocker. 12 action items filed as AI-E13-1..-12 in the register below; 7 critical-path items; 5 significant-discovery alerts; 0 next-epic preparation tasks BECAUSE THERE IS NO NEXT EPIC — epics.md ends at Epic 13, so after the FINAL retrospective there is no scheduled reader for any target_story: NONE entry (AI-E13-12). NO RE-PLAN WAS APPLIED: no epic, PRD, architecture or story scope was edited. The ONE write made outside the retrospective document was registering that document in tests/test_evidence_citation.py::_STATUS_DOCUMENTS — AI-E12-1's second half and this step's own DoD; TC-ArgusAgent-DOCS-001-22 was observed RED before it and GREEN after, _status_assertions() returns 0 for the document so the registration is inert, and the FULL SUITE was re-run to green afterwards (1585 passed / 0 failed / 0 skipped, Windows) rather than repeating the very defect the document reports. ⚠️ THAT REGISTRATION PUT tests/test_evidence_citation.py AT EXACTLY 1200/1200 LINES — the FINAL Epic-13 retrospective CANNOT be registered until that module is split (AI-E13-2). development_status[epic-13] is deliberately NOT rolled up: 13.3 is not done, and the roll-up is the orchestrator's step in any case. (was: backlog)

# -----------------------------------------------------------------------------
# ACTION ITEMS - added 2026-08-11 by the Epic 10 retrospective (epic-10-retro-2026-08-11.md).
#
# SCOPE WARNING, so a partial register is not misread as a complete one: this
# section was created by the Epic 10 retrospective and contains EPIC 10's items
# ONLY. Epics 1-9 raised their action items inside their own retrospective
# documents (epic-N-retro-*.md section 7 or equivalent) and those were never
# mirrored here; their absence below is a gap in this section's history, NOT
# evidence that they are closed. The register of record for deferred DEFECTS
# remains deferred-work.md; this section tracks retrospective process and
# governance items.
#
# Every entry below is a RECOMMENDATION to the operator/orchestrator. The
# retrospective applied no re-plan: no epic, PRD, architecture or story scope
# was edited by it. Status vocabulary: open | in-progress | done | retired.
# -----------------------------------------------------------------------------
  epic-14: done  # dev-loop roll-up 2026-08-18: in-progress -> done. All three stories `done` (14-1, 14-2, 14-3) and epic-14-retrospective `done` (epic-14-retro-2026-08-18.md). The retrospective key DID NOT EXIST and was created by that retro alongside epic-15-retrospective; epics 1-13 each had one and the correct-course proposals that added epics 14 and 15 never created theirs - the key is the gate that stops an epic rolling up before it has been reviewed. Instrument repaired, gate NOT cleared and not clearable here: this UNBLOCKS story 13-5, which is expected to return UNEVALUABLE. || create-story 14-1 (2026-08-17) on HEAD 47b6dbe: backlog -> in-progress, first story of the epic contexted. || ADDED 2026-08-17 by sprint-change-proposal-2026-08-17.md (APPROVED by XAgent007). 'Make the Moat Hold' - fixes the instrument Epic 13 measured. Epic 13 returned 0 TP / 26 FP over 31 findings in ONE rule class (vacuous_test_ast); the measurement worked, and what it found is that the detector's AST corroboration tests whether a test CONSTRUCTS A MOCK rather than whether the asserted values DERIVE FROM THE SUT - a conformance gap against cross-cutting concern #6, which has required audited_deep corroboration AND Prosecutor sign-off since the architecture was written. epics.md previously ENDED AT EPIC 13 (AI-E13-12); this is a structural addition. BLOCKS story 13-5 and therefore Epic 13's closure. THIS EPIC DOES NOT CLEAR THE GATE AND CANNOT.
  14-1-a-verdict-eligible-vacuous-finding-proves-vacuity-not-mocking: done  # code-review 14-1 (Sonnet, iteration 3, 2026-08-18): review -> done (PASS; fix-loop cap reached). Independently re-verified by execution: git diff --name-only 47b6dbe..966ceba touches exactly the File List's eleven files, nothing else (pipeline.py, cartridges, validation-corpus, governance artifacts untouched). Full suite 1611 collected / 1611 passed / 0 failed / 0 skipped exit 0; mypy Success 87 files; bandit 19L/0M/0H. vacuous_test.py diff against a727a80 is empty (byte-unchanged, confirmed directly, not read back). -111/-112/-117 independently reproduced RED against the actual pre-fix provenance_scan.py (swapped in via git show, restored after): -111 failed (2,0)!=(1,1); -112 failed on the SAME assertion, not order-invariance -- the pre-fix code is already order-invariant (always searches from column 0) and simply wrong, confirming -112's stated purpose (an order-invariant-but-wrong fix still fails it) against the real pre-fix code; -117 failed with the semicolon-compound arm promoted to a blocking verdict. AC3 re-scored independently (own script, own staging via git show at the pinned shas through build_ast_index + VacuousTestDetector): 0/31 verdict-eligible, confirmed. Cartridge recall 7/7. VacuousTestScore confirmed still frozen extra=forbid, same fields. Dogfood/signature-demo modules re-run green. Order-invariance argument adjudicated and holds (distinctness of the edge-to-occurrence assignment, not its order, is what the counts depend on). Both moved behaviours (x = 1; sut(2, 3) -> corroborated; bare nested sut(sut(1,2),3) -> advisory) are defensible; the nested-call move is in fact a second, previously-unreported instance of the same column-blindness defect being fixed as a side effect (pre-fix both edges resolved to the outer occurrence and both scored discarded -- a hidden false-positive, not a new judgement call). Adversarial hunt for a third false-accusation mechanism: 27 fixtures across walrus, lambda, f-string, async/await, try/except, with-as, chain-assignment, tuple/starred unpacking, global/nonlocal, decorators, comment/string lookalikes, CRLF, deep indentation, long lines, match/case (incl. class pattern), dict-comprehension, run through the real detector at the gate-clearing three-mock/two-call shape -- none produced a false promotion; two recall misses found, both in the safe direction. No third mechanism found (reported as a negative result). No High/Medium findings. See development_status[14-1-...] "Review Findings - iteration 3" for the full record. || dev-story 14-1 iteration 3 (2026-08-17): 14-1 in-progress -> review; the ONE open High finding (column-blind occurrence resolution) is resolved. Occurrence resolution is stateful per (line, callee) and the classification unit is now the ;-delimited SIMPLE statement; CodeEdge and the 1.4 index are byte-unchanged. The suggested fix's premise that the index emits edges in source order was MEASURED FALSE (_extract walks right-to-left) and reported rather than converted into a schema change - the counts are bijection-invariant, so distinctness is all that is required, pinned by -112. Predicate not weakened: recall 3/3, adjudicated locators 0/31 verdict-eligible at the unchanged pinned shas, flag rate byte-identical (1,848/3,551 and 681/1,122). Guards -111/-112/-117 confirmed RED pre-fix. pytest 1611 passed / 0 failed / exit 0, mypy Success 87 files, bandit 19L/0M/0H. || code-review 14-1 (Sonnet, iteration 2): review -> in-progress (FAIL). Full suite (1608 passed / 0 failed, exit 0), mypy (Success, 87 source files) and bandit (19 Low / 0 Med / 0 High) independently re-run; cartridge recall (7/7 via test_cartridge_selfaudit.py) and precision (0/31 verdict-eligible across all 31 adjudicated locators, independently re-scored by execution against the unchanged pinned shas via git show) independently reproduced, matching the story exactly. Extraction sizes (vacuous_test.py 697, provenance_scan.py 541, pipeline.py untouched at 1111), the AC6 byte-diff (_ASSERTION_CALLEES/_MOCK_CALLEES/_count_statements/both thresholds unchanged), both departures (provenance_scan.py naming vs NFR-M1, the callee tables passed as parameters) and the README/CHANGELOG figure provenance (TC-ArgusAgent-DOCS-001-54, currently green) all independently confirmed as claimed. ONE NEW HIGH finding, distinct from the continuation-syntax defect this round fixed: provenance_scan._locate_call resolves a callee's position via unconditional pattern.search(), which always returns the FIRST occurrence of callee( on a physical line; CodeEdge carries no column, so when the SAME callee is called MORE THAN ONCE on ONE physical line (e.g. a semicolon-compound statement), every edge for that (callee, line) pair borrows the first occurrence's classification. Reproduced END TO END through the REAL tree-sitter index: "sut(1, 2); captured = sut(3, 4)" followed by "assert captured == fake.other()" scores vacuous_test_ast (verdict-eligible) even though captured is bound to the genuine sut(3, 4) result and the assertion constrains it against a mock - a direct AC1.3 violation, uncovered by -101..-110/-116. See development_status[14-1-...] "Review Findings - iteration 2" for the full reproduction and suggested fix. || dev-story 14-1 iteration 2 (2026-08-17) on HEAD ae2fb28: in-progress -> review. Both review findings resolved. HIGH (false accusation) fixed AT THE ROOT and its scope was WIDER than recorded: fact (b) read only the call's own PHYSICAL line, so EVERY continuation syntax defeated it - the backslash the finding named AND the parenthesised form PEP 8 prefers. Reproduced END TO END on the -30 corpus through a default zero-token run_audit_detailed (plain -> INSUFFICIENT_COVERAGE exit 3; paren and backslash -> NOT_READY_FOR_RELEASE exit 2 with a verdict-eligible finding). An orchestrator correction claiming backslash was already handled does NOT reproduce and was not implemented - recorded beside the finding. Fix: classification is now over the whole LOGICAL STATEMENT (provenance_scan.logical_statement_starts), one rule for brackets and backslashes, unresolvable -> CONSUMED. NOT weakened: recall 3/3, adjudicated locators 0/31 verdict-eligible at the unchanged pinned shas, flag rate byte-identical (1848/3551 minions, 681/1122 agent-smith). New guards -109/-110 (predicate) and VERDICT-001-116 (end to end), all RED before green. MEDIUM (cohesion) done this round not deferred: argus/detectors/provenance_scan.py extracted, vacuous_test.py 1072 -> 697 (headroom 128 -> 503), pipeline.py untouched at 1111, no size exemption added. Gates LOCAL, CI NOT ESTABLISHED: pytest 1608 collected / 1608 passed / 0 failed / 0 skipped; mypy argus Success 87 source files (+1 = the new module); bandit 19 Low / 0 Med / 0 High. AI-E12-11's commit -> regenerate -> commit sequence RUN, so the dogfood-currency guards iteration 1 handed off are GREEN; nothing hand-edited, no assertion loosened. || create-story 14-1 (2026-08-17) on HEAD 47b6dbe: backlog -> ready-for-dev. Story file stories/14-1-a-verdict-eligible-vacuous-finding-proves-vacuity-not-mocking.md. Feasibility MEASURED out of tree before contexting: 3/3 planted cartridges keep corroboration, 30/31 adjudicated findings lose it, minions-wide promotions 24 -> 0. MEASURED ESCALATION recorded at story §0.3 and it is NOT the dev's to resolve alone: TC-ArgusAgent-VERDICT-001-30 - the test proving a zero-token no-sign-off blocking path exists AT ALL - plants a test whose SUT result is BOUND AND ASSERTED, so it does NOT survive the corrected predicate; its own message says "Do NOT adjust a gate to make this pass - report it and escalate". Two named options recorded with arguments both ways. Blast radius measured: 21 modules reference the rule id, 10 run the detector end-to-end, and test_pipeline_signature_demo.py is the FR32 demo the PRD calls "the product". vacuous_test.py 623/1200 (headroom 577) but pipeline.py is 1111/1200 (headroom 89) and NOT exempt - do not add to it. || ADDED 2026-08-17. THE MOAT. Replaces _ast_corroborated fact (b) - currently 'assertion_sites >= 1 and mock_sites >= 1', measured equivalent to mock_sites>=1 in 1835 of 1836 flagged tests - with a signal that discriminates. Feasibility MEASURED out of tree before the story was written: 3/3 planted cartridges keep corroboration (incl. the holdout_vacuous anti-overfitting control and the non-ASCII one), 30/31 adjudicated findings lose it, minions-wide promotions 24 -> 0. TC-ArgusAgent-DETECT-001-86 must be RE-AUTHORED as an intended behaviour change, never silently adjusted. Not done on a Windows-only pass. || dev-story 14-1 (2026-08-17) on HEAD 47b6dbe: in-progress -> review. Fact (b) REPLACED (DN-1) by a provenance-shape predicate - SUT result DISCARDED, no consumed SUT call, and an assertion referencing a mock-bound name; fact (a) unchanged. MEASURED at the unchanged pinned shas through the shipped detector: cartridge recall 3/3 (incl. the holdout and the Cyrillic-pathed nonascii control, no cartridge or golden key edited); 0 of 31 adjudicated locators remain verdict-eligible (minions promotions 27 -> 0 over 3551 test functions); heuristic flag rate IDENTICAL 1848/3551 = 52.0%; agreement with the bare mock_sites>=1 term 34/36 -> 0/36 on the only sub-population where the two terms can differ. The 14.2 surface (_count_statements, _ASSERTION_CALLEES, _MOCK_CALLEES, both thresholds) proven byte-identical by diff; VacuousTestScore did NOT change shape; argus/pipeline.py untouched. The story's SS0.3 escalation was RESOLVED as option (A) HARDENED on the operator ruling: TC-ArgusAgent-VERDICT-001-30 now measures BOTH arms on the same default path - the new witness is promoted, the OLD fixture (kept verbatim) is demoted to advisory - so the fixture edit is itself a measurement of the intended change. -86/-94 re-authored the same way; -87/-88 (the false-accusation guard) pass unchanged; eight new cases -101..-108 reach every branch incl. CRLF and non-ASCII identifiers. Gates LOCAL, CI NOT ESTABLISHED: pytest 1605 collected / 1603 passed / 2 failed / 0 skipped; mypy Success 86 files (d0); bandit 19 Low / 0 Med / 0 High (d0). HAND-OFF: the 2 failures are test_dogfood_plan.py and test_dogfood_proof.py - artifact-currency guards WORKING, caused by this story's +449 uncommitted lines under argus/ (committed proof 28038 LOC vs live 28487), NOT by detector behaviour. The regenerator refuses on a dirty argus/ tree by design and AC7.5 forbids committing, so this is routed to AI-E12-11's owner (the dev-loop orchestrator) with the pre-authorised commit -> regenerate -> commit sequence. test_dogfood_artifact_currency.py and test_pipeline_signature_demo.py (the FR32 demo) are GREEN. No assertion loosened, skipped, xfailed or narrowed; nothing committed; git tag -l empty; deferred-work.md unmodified. || code-review 14-1 (Sonnet, iteration 1): review -> in-progress (FAIL). Full suite (1605 collected, exit 0), mypy (Success, 86 files) and bandit (19 Low/0 Med/0 High) independently re-run; cartridge recall (3/3) and precision (0/31 verdict-eligible, re-scored by execution at both pinned shas) independently reproduced, matching the story exactly. Two-commit close verified by re-running scripts/regenerate_dogfood_artifacts.py against the clean tree: only the HEAD descriptor differs, content/LOC byte-identical, so the artifacts were script-generated not hand-edited. The 0.3 hardening (both arms on -30) confirmed present and correct. AC6 boundary (assertion/mock callee tables, thresholds) confirmed byte-unchanged. One HIGH finding: a backslash line-continuation between an assignment and the SUT call lets _provenance_evidence misclassify a CONSUMED SUT result as DISCARDED, so a test that genuinely constrains the real SUT result via a mock-mixed assertion is wrongly promoted to verdict-eligible - a reproduced violation of AC1.3 in the exact false-accusation direction the story exists to close. One MEDIUM finding: the new ~15-function provenance micro-parser bolted onto vacuous_test.py (623 -> 1072 lines, headroom now 128) is a distinct concern from the detector's scoring job and should be extracted to its own module before Story 14.2/14.3 land in the same file. See development_status[14-1-...] Review Findings for full detail.
  14-2-the-density-scorer-counts-statements-and-knows-the-assertions: done  # code-review 14-2 (Sonnet, iteration 3, 2026-08-18): review -> done. VERDICT pass. Scope: this iteration is documentation-only -- dev pass 3 changed only story-file prose; HEAD e2e278c is UNCHANGED since iteration 2 (git diff e2e278c -- argus/ is empty; the four modified tests/*.py plus tests/test_status_document_registry.py are the pre-existing SS0.8 unrelated dirty work, not this story's). Re-measured the four module sizes independently via len(Path(p).read_text(encoding='utf-8').splitlines()) -- the ceiling test's own method: provenance_scan.py 926, vacuous_test.py 929, test_vacuous_density.py 1060, test_vacuous_detector.py 1128 -- all four match the story's corrected AC9/File-List figures exactly, and every surviving 919 in the record is explicitly labelled as vacuous_test.py's PRE-fix-pass count at a9cf25d, never presented as a post-pass number. deferred-work.md's working diff re-measured at 287 insertions / 0 deletions (git diff --numstat), matching the story's corrected claim; the load-bearing 0-deletions append-only property holds. DF-14-2-B confirmed still OPEN in the ledger (deferred-work.md:4824-4831, id/origin_story/owner/target_story/category/severity all present, named owner = next dev to open provenance_scan.py). Adjudicated the dev's reasoning as sound: appending a ledger closure to green a governance guard over a defect that is genuinely still unfixed would be AI-E12-3's defect (a false closure) committed inside the very guard built to stop it -- correctly left open instead. Confirmed the story's own prose now describes DF-14-2-B truthfully as OPEN, and that no remaining DF-14-2-B occurrence sits on a line matching the CLOSED/Closes/closes closure-verb pattern test_governance_record_integrity.py's story_closure_claims checks. Independently re-ran the full suite AFTER writing this review's own findings into the story file (the process hazard this review round was warned about, from iteration 2's own hand-off prose tripping TC-ArgusAgent-DOCS-001-78): 1621 passed / 0 failed / 0 skipped, exit 0 (JUnit XML tests=1621 errors=0 failures=0 skipped=0), exact match to baseline; test_governance_record_integrity.py's 3 tests green, including DOCS-001-78. Nothing else moved: no argus/ or tests/ file opened by this review, no test added or altered, nothing committed; sprint-status.yaml's 14-2 key and last_updated are the only keys this pass touches, all comments and STATUS DEFINITIONS preserved. Story's substance was independently verified twice already (iterations 1-2, both CLOSED); this iteration closes the sole remaining documentation-accuracy item. No High/Medium findings; no unresolved decision-needed or patch items; all ACs independently re-confirmed met; tests/mypy/bandit green. || dev-story 14-2 fix pass (Opus, dev pass 3, 2026-08-18) on HEAD e2e278c: in-progress -> review. Review iteration 2's ONE Low finding CLOSED, 1/1. DOCUMENTATION ONLY -- no argus/ or tests/ file opened, no test added or changed, no corpus measurement re-run, no dogfood artifact regenerated, nothing committed. The finding was reproduced before it was accepted rather than taken from the reviewer's table: each figure re-derived with len(Path(p).read_text(encoding='utf-8').splitlines()) -- the same method test_module_size_ceiling.py uses -- at HEAD e2e278c and cross-checked at every commit this story produced via git show. CORRECTED: provenance_scan.py 903 -> 926, vacuous_test.py 919 -> 929, test_vacuous_density.py 1053 -> 1060; test_vacuous_detector.py confirmed already exact at 1128. Root cause named rather than rounded off: 919 is vacuous_test.py's PRE-fix-pass count at a9cf25d transcribed forward as a post-pass number, and the whole fix-pass table was assembled from the pass-1 table rather than from the files; every remaining 919 in the record now carries its baseline explicitly and the File List entries read 815->926, 919->929, 814->1060. SWEEP of the same defect class across the record (the finding's real subject, not just the four line counts) found TWO FURTHER LIVE DEFECTS, one of them RED ON DISK. (i) deferred-work.md's working diff, claimed 269 insertions, re-measured 287, load-bearing 0 deletions (append-only) confirmed to hold. (ii) THE FIRST FULL-SUITE RUN OF THIS PASS CAME BACK 1620 PASSED / 1 FAILED -- TC-ArgusAgent-DOCS-001-78, 'a story record claims a ledger closure that deferred-work.md never received' -- and the claimant was the 14-2 story file itself: review iteration 2's own hand-off prose put the ledger id DF-14-2-B on the same line as the closure verb, which the line-scoped story_closure_claims analyzer reads as a claim the ENTRY was closed. It was not, and must not be: that entry is deferred and OPEN with a named owner, as both prior passes verified. Same defect class as the finding I was sent to fix (the record asserting what is not true of the world) through a different door, and the exact trap the story's own previous-story-intelligence names. It went unseen because iteration 2's gates ran BEFORE its findings were written into the story file, so the red was created by the hand-off prose itself and nothing re-ran -- AI-E13-1 in miniature. FIXED IN THE PROSE, DELIBERATELY NOT IN THE LEDGER: appending a ledger closure to green a guard would be AI-E12-3's defect committed inside the guard built to stop it, so deferred-work.md was NOT opened; the sentence now reads 'closed as a review item, explicitly not a ledger closure, the entry remains OPEN', annotated with what changed and why. (My first attempt reproduced the pattern by quoting the bad sentence verbatim and re-tripped the guard; recorded, because a fix that had to be fixed is part of the record.) The rest of the sweep re-derived as CORRECT: pass-1's four module sizes exact for their baseline (697->919, 610->815, 814 new, 1084->1128), argus/pipeline.py 1111 at all five commits and absent from the diff, thresholds byte-identical by git diff -U0 | grep (empty), dogfood provenance sha 2eeaace confirmed an ancestor of HEAD, git tag -l empty. NO CEILING IMPLICATION: all four files <=1200 (tightest test_vacuous_detector.py at 1128, 72 spare), test_module_size_ceiling.py green, no exemption added, registry unmoved. NO CURRENCY-GUARD IMPLICATION, and it is CONFIRMED rather than assumed: git status --porcelain -- argus/ is EMPTY at e2e278c, so the artifacts' inputs cannot have moved; the regenerator was deliberately NOT run, since re-rendering with no input change would rewrite a provenance sha for nothing. Full suite re-run after the last prose edit (AI-E13-1's lesson, third time): 1621 passed / 0 failed / 0 skipped / 0 errors, exit 0 -- delta 0 against the fix pass, the expected result for a change touching no executable line, and green only after defect (ii) above was repaired. LOCAL ONLY; CI evidence is NOT ESTABLISHED (Windows 11 / CPython 3.11; the ubuntu matrix and the 3.10/3.12 legs reasoned about, not executed). The story's substance, confirmed correct by two independent review passes, is byte-unchanged; only the record's own arithmetic about itself was repaired. Story file and sprint-status.yaml remain UNCOMMITTED per SS0.8, as in the previous two rounds. || code-review 14-2 (Sonnet, iteration 2, 2026-08-18): review -> in-progress. VERDICT concerns. Scope: the fix round only (2eeaace code, e2e278c artifacts, against a9cf25d) -- iteration 1's own re-derivation stands, not repeated. Independently re-derived from a FRESH harness (not the dev's script, not iteration 1's script): CPython grounding confirmed for all five cases (ExceptHandler/match_case are not ast.stmt; elif is a nested ast.If, correctly excluded from the exclusion; a decorator adds no stmt). DN-14-2-5 verified on all three claims: (a) _scan_span/_statement_texts/logical_statement_starts sit outside every changed diff hunk; (b) logical_statement_starts confirmed byte-identical by direct call against -122's fixture; (c) a hand-built WRONG alternative that applies the exclusion inside _scan_span was shown to mis-attribute lines 6 and 8 of that same fixture, i.e. -122 does catch the regression it exists to catch. RED-first reproduced row for row against a9cf25d (try/except/else/finally 8-vs-5; if/for/while-else and wrapped-except-header 5-vs-4; match/case 6-vs-4; decorator and wrapped-decorator 5-vs-4; -122 7-vs-5 while logical_statement_starts already matched). Structural GAINED=0 argument adjudicated sound by source: _counted_statement_texts is a strict subset filter (denominator cannot rise), mock_ratio never reads statement_count. Corpus-wide re-derivation from scratch (own line-number-matched CPython ground truth -- caught and fixed a name-only-matching bug in my own harness first, since tests/security/test_a2a_missing_token_audits.py defines the same test method name in eleven different TestCase subclasses) reproduces the headline claim EXACTLY: n=4673, exact=4672, over=0, under=1 (minions 3551/3551 exact; agent-smith 1121/1122, sole residual in test_structural_conformance_set_verification). Gates re-run independently: pytest JUnit XML tests=1621 errors=0 failures=0 skipped=0 exit 0; mypy Success 87 files; bandit 19L/0M/0H conf 0/0/6/13 -- all exact, no repeat of iteration 1's worktree-naming artifact. 15 hand-built adversarial predicate shapes (except*/tuple/as, wrapped except-as, conditional-expression else, match class-pattern + guard, case-as-identifier, string/comment containing except:/case:, decorator with f-string colon, annotation-only case, CRLF, non-ASCII identifier) all matched CPython exactly through the real detector -- no predicate defect found. Both iteration-1 findings CONFIRMED CLOSED: DF-14-2-B's _ASSIGNMENT_RE is byte-identical since 966ceba and the ledger entry is well-formed with a named owner; argus/pipeline.py and tests/test_vacuous_detector.py both absent from the fix round's diff, confirming the claimed scope. ONE NEW Low finding: the Dev Agent Record's own AC9 module-size table/File-List numbers for this pass do not match the files as committed (provenance_scan.py claimed 903 vs measured 926; vacuous_test.py 919 vs 929, the claimed figure being this pass's PRE-edit count carried forward unmeasured; test_vacuous_density.py 1053 vs 1060; test_vacuous_detector.py exact at 1128) -- no ceiling implication, no functional impact, flagged only because the story's own stated method is measured-not-asserted. Findings written to the story file's Review Findings section. Sent back to in-progress per the concerns->in-progress mapping; the orchestrator may treat concerns as pass-eligible per its own cap. || code-review 14-2 (Sonnet, iteration 1, 2026-08-18): review -> in-progress. VERDICT concerns. Independently re-derived by execution in two isolated git worktrees (966ceba/a9cf25d), own harness (not the dev's script): headline flags EXACT match (minions 1848/3551=52.0%% -> 660/3551=18.6%%, agent-smith 681/1122=60.7%% -> 297/1122=26.5%%, GAINED 0 both members, 1572 lost); CPython ast ground truth re-derived over both full before-flagged populations, 1.9071x -> 1.0052x confirmed exact, 1784/1848=96.5%% exact-match confirmed exact, agent-smith ground-truth divergence (6608 vs §0's 6600) independently reproduced. DN-14-2-1 load-bearing claim reproduced literally: corroboration path patched to read the widened table in an isolated worktree -> only -115 goes red, test_default_path_blocking_verdict.py (-30/-116/-117) stays 100%% green, revert restores byte-identical diff. Gates independently re-measured in ArgusAgent's own venv: pytest JUnit XML tests=1619 errors=0 failures=1 skipped=0 (the one failure is a verification-harness artifact -- a worktree literally named "after" trips a project-name-leak assertion, confirmed absent at 966ceba unpatched); mypy Success 87 files; bandit 19L/0M/0H conf 0/0/6/13. Thresholds byte-identical by diff (empty); pipeline.py diff empty; module sizes 919/815/1128/814 all <=1200, ceiling test diff empty (no exemption); cartridge/critical-eligibility suites clean; DF-14-2-A confirmed a real, well-scoped, honestly-bounded deferral with a named owner; stories/1-5-...md's 2026-08-18 correction independently checked -- struck text untouched, every re-measured figure matches exactly. ONE Low finding kept open: the AC1 docstring's claim about the residual error's DIRECTION is measurably wrong -- the dominant measured mechanism is an OVER-count from bare except/else/finally/case continuation-clause headers (each counted as its own statement though CPython's ast gives the whole compound ONE stmt), not the claimed under-count; reproduced with a minimal repro (try/except/else/finally scores 8 vs ast 5) and two real corpus instances named in the story's Review Findings. Severity capped Low because it can only feed the advisory heuristic path -- ast_corroborated/RULE_AST is independent of density and reads the FROZEN vocabulary via DN-14-2-1's separate path, so this cannot manufacture a verdict-eligible false accusation. One Low item deferred pre-existing (DF-14-2-B, hBcanchored _ASSIGNMENT_RE contradicting its own docstring, byte-identical since 14.1, not exploitable given call-site usage). No High/Medium findings; no unresolved decision-needed or patch items above Low; all ACs independently verified met; tests/mypy/bandit all green. Findings written to the story file's Review Findings section; DF-14-2-B filed in deferred-work.md. Sent back to in-progress per the concerns->in-progress mapping so the two Low items are picked up; the orchestrator may treat concerns as pass-eligible per its own cap. || create-story 14-2 (2026-08-18) on HEAD 966ceba: backlog -> ready-for-dev. Story file stories/14-2-the-density-scorer-counts-statements-and-knows-the-assertions.md. ALL PREMISES RE-DERIVED BY EXECUTION against the POST-14.1 tree (both members staged at the unchanged pinned shas ec63b729 / 9ab774d7, shipped build_ast_index + shipped VacuousTestDetector; both shas confirmed reachable). Baselines: pytest 1611 collected / 1611 passed / 0 failed / 0 skipped exit 0; mypy Success 87 files; bandit 19L/0M/0H. Population reproduces 14.1 exactly (minions 3,551/1,848 flagged = 52.0%; agent-smith 1,122/681 = 60.7%; 0 of 4,673 corroborated; 31/31 adjudicated locators resolve, 31/31 still flagged, 0/31 verdict-eligible). TWO PROPOSAL FIGURES DO NOT REPRODUCE AND ARE SUPERSEDED IN THE STORY: the denominator inflation is 1.907x, not 2.04x (1.933x both members, 1.676x whole population; top-level-only ast counting gives 2.664x, so 2.04 is neither) -- and the 'corrects 14 of 31 / 4 of 31 / 13 of 31 spans' attribution reproduces EXACTLY only for a design that (i) tracks multi-line STRING literals in the denominator and (ii) admits project helpers by NAMING CONVENTION; the weaker table-only design measures 11/31 spans and 3/31 corrected. The numbers therefore PIN the design and are not decoration. ONE PREMISE IS FALSIFIED, and it is the story's centre: 'widening _ASSERTION_CALLEES cannot move corroboration' is FALSE on the post-14.1 tree. The table is PASSED IN to provenance_scan and read in TWO places -- the SUT loop and _assertion_statement_lines -- so provenance_scan.py:22-27's claim that 'nothing in this module can grow a dependency on a table it cannot see' does not hold; DN-4 guarantees independence from the assertion COUNT, not the TABLE. REPRODUCED END TO END through the real tree-sitter index and the real detector: an ordinary mock-interaction test (SUT result discarded, only assertion is fake.calculate.assert_called_once_with()) goes asserts=0 density=0 CORROBORATED=False -> asserts=1 density=1/5 CORROBORATED=True when the table widens -- still under the 1/4 floor, so still flagged, and now VERDICT-ELIGIBLE. A new false accusation manufactured by the fix for defect 2, the exact class Epic 14 exists to close, arriving by a different route. The corpus measurement shows 0 movement ONLY because 0 of 4,673 tests are corroborated at all -- an EMPTY denominator, UNEVALUABLE, not a confirmation. Resolved in-story as DN-14-2-1 on the locked asymmetry (a false red is the lethal failure): the CORROBORATION path (fact (a) AND fact (b)) reads a FROZEN vocabulary pinned to 14.1's 23 names; only the density NUMERATOR reads the widened one -- with a RED-first guard required (AC6.3) because a corpus guard here would be green over nothing (AI-E3-1). Also recorded: the candidate denominator must carry cross-line STRING state or it manufactures 2 new flags from a ';' inside docstring prose (measured, both named); with that state it lands at 1.005x of CPython's own statement count and matches it exactly on 96.5% of flagged tests. Expected flag rate after: minions 18.6%, agent-smith 26.5%. tests/test_vacuous_detector.py is 1,084/1,200 (was 324 pre-14.1) -- 116 lines of headroom is this story's real size constraint, and a cohesion split is prescribed over any exemption. Guard predictions recorded with mechanisms (-87/-88/-93 must not move; -109/-110 are the most likely to break, via _corroborated's 'must still FLAG' assertion, and the failure would be misleading; -30's two arms and -116/-117 must survive). 14.3 boundary drawn explicitly (flat, language-agnostic set; no language field; no cross-language vocabulary here). Windows-only discharge made AC8 with six concrete obligations incl. a CRLF regression test for the DENOMINATOR and a named-Unevaluable-not-skip rule. Dogfood close routed to AI-E12-11. epic-14 already in-progress; unchanged. || ADDED 2026-08-17. _count_statements counts LINES not statements (measured 2.04x inflation over 1,812 flagged tests; corrects 14 of 31), and _ASSERTION_CALLEES is documented as 'unittest family + pytest helpers' while containing NO pytest helper (present in 13 of the 31 spans; corrects 4 of 31). AMENDS THE STORY 1.5 LOCKED DECISION on the denominator - already recorded struck-not-deleted in stories/1-5-heuristic-vacuous-test-detector-tier-a-vacuous-path-ast-subset.md. THRESHOLDS ARE NOT CHANGED by this story. Dogfood artifacts need regeneration. Not done on a Windows-only pass. || dev-story 14-2 (2026-08-18) on HEAD 966ceba: ready-for-dev -> in-progress. || dev-story 14-2 (2026-08-18) on HEAD 966ceba -> 26e6fb6/a9cf25d: in-progress -> review. Denominator re-authored over LOGICAL statements (provenance_scan._scan_span, ONE scan read by both consumers); assertion vocabulary widened + a separate naming-convention predicate; DN-14-2-1 BUILT -- the corroboration path reads a FROZEN 23-name vocabulary, only the density numerator reads the widened one. Re-measured by execution at the unchanged pinned shas (minions@ec63b729, agent-smith@9ab774d7, both git cat-file -t -> commit): inflation 1.9071x -> 1.0052x of CPython ast ground truth, exact on 1784/1848 spans; flag rate minions 1848/3551 52.0% -> 660 18.6%, agent-smith 681/1122 60.7% -> 297 26.5%; flags GAINED 0, lost 1572; ast_corroborated byte-identical on all 3101 spans whose flag is unchanged (stated with its limit -- 0/4673 corroborated is an EMPTY denominator, UNEVALUABLE, not a confirmation). The 31 adjudicated locators re-scored: 13 gain assertion sites, attribution 14/4/23/8, verdict-eligible 0/31. Every cell of the story's SS0.3 prediction reproduced exactly. SS0.4's false accusation reproduced RED first through the real index (asserts=0 density=0 corroborated=False -> asserts=1 density=1/5 corroborated=True) and pinned by DETECT-001-115, which goes RED against the one-table design while the ENTIRE existing suite stays green -- measured proof no existing guard would have caught it. Guard predictions written before measuring: SS0.5's headline -109/-110 prediction was WRONG and is recorded as wrong; -101/-102 moved instead, on the assertion TABLE not the denominator, and their fixtures were strengthened -86-style with the reason (neither predicate nor vocabulary touched). Cartridge golden-key recall unchanged; clean_control still unflagged; VERDICT-001-30 both arms, -116, -117 unmoved. test_vacuous_detector.py was 1084/1200 so new cases went to a NEW tests/test_vacuous_density.py on a cohesion boundary -- a split, NOT a size exemption. Thresholds proven byte-identical by diff; argus/pipeline.py untouched. AC8.5 honoured: no pytest.skip -- the new module reports a named UNEVALUABLE FAILURE; suite reports 0 skipped. Gates 1619 passed / 0 failed / 0 skipped exit 0 (delta +8), mypy Success 87 files (delta 0), bandit 19L/0M/0H conf 0/0/6/13 (delta 0) -- LOCAL ONLY, Windows/CPython 3.11; CI evidence is NOT ESTABLISHED and five specific ubuntu-matrix behaviours are named as unverified in the Dev Agent Record. Story 1.5's 2.04x/1812 corrected on the record to 1.907x/1848, struck text untouched. Dogfood artifacts regenerated through their own renderers via AI-E12-11 (26e6fb6 -> a9cf25d), provenance sha confirmed an ancestor of HEAD; nothing hand-edited, no assertion loosened. DF-14-2-A filed with a named owner (Story 14.3 dev). git tag -l empty; nothing pushed. Pre-existing dirty files left unstaged. | dev-story 14-2 fix pass (Opus, 2026-08-18): in-progress -> review. Review iteration 1's 2 findings closed: 1 FIXED, 1 confirmed-deferred (DF-14-2-B, ledger entry verified well-formed with a named owner, code untouched). Finding 1 fixed at the MECHANISM not the prose: a bare except/else/finally/case clause header and a decorator no longer open a statement in the density denominator (CPython builds no ast.stmt for either). Re-measured by execution at the unchanged pinned shas (ec63b729 / 9ab774d7): denominator vs CPython ground truth 1.0052x -> 1.0000x on the 1848 flagged minions tests, exact 1784/1848 -> 1848/1848; agent-smith 0.9997x, exact 680/681; corpus-wide 4672/4673 exact with 0 over-counts and 1 under-count, so the residual's direction claim is now TRUE as measured (64/64 and 27/28 of the previous non-exact spans were OVER-counts, the opposite of what the docstring asserted). Flag rate minions 660 -> 653 = 18.4%%, agent-smith 297 -> 295 = 26.3%%; flags GAINED 0 against BOTH 966ceba and a9cf25d (structural: the denominator can only shrink, density can only rise, the floor fires from below, mock_ratio is over call_sites). The 31 adjudicated locators did NOT move: 13 rise, 14/4/23/8 attribution, verdict-eligible 0/31. Cartridge recall 4/4; ast_corroborated identical on all 4664 unchanged-heuristic spans; corroborated 0/4673. Applied at the COUNT and not in _scan_span, so fact (b)'s logical_statement_starts is byte-identical (DN-14-2-5), pinned by -122. Guards -121/-122 confirmed RED first against a9cf25d (8-vs-5, 6-vs-4, 5-vs-4) with four negative controls (elif, inline clause body, case-as-a-name, a @ b) already green. 14.1's -87/-88/-109/-110/-111/-112, VERDICT-001-116/-117 and -30's two arms all green. Gates LOCAL only, CI evidence NOT ESTABLISHED: pytest 1621 passed / 0 failed / 0 skipped (delta +2), mypy Success 87 (delta 0), bandit 19L/0M/0H conf 0/0/6/13 (delta 0). Thresholds byte-identical, pipeline.py untouched, no size exemption (903/919/1053/1128 all <=1200). Dogfood artifacts regenerated via AI-E12-11: 2eeaace (code) -> e2e278c (artifacts).
  14-3-the-assertion-vocabulary-crosses-the-languages-the-installer-ships: done  # create-story 14-3 (2026-08-18) on HEAD e2e278c: backlog -> ready-for-dev. Story file stories/14-3-the-assertion-vocabulary-crosses-the-languages-the-installer-ships.md. ALL PREMISES RE-DERIVED BY EXECUTION against the post-14.1/post-14.2 tree; TWO DID NOT SURVIVE and are recorded as failures rather than carried. (1) The named JS fixture is NO LONGER FLAGGED: 14.2's logical-statement denominator scores a K&R-brace JS function body at statement_count=0 and the flag predicate requires >0, so the epic's 're-measured and is NOT flagged' criterion is now VACUOUSLY TRUE and would pass with zero code change. The false accusation survives only in the ALLMAN-brace shape (brace on its own line -> stmts=1, asserts=0, FLAGGED) - reproduced RED and measured closed by the widening. Target re-stated as DN-14-3-4. (2) The proposal 17b section 2.3 corpus prediction is REFUTED, not merely unmeasured: over all three members at their pinned shas the widening moves 0 flags GAINED and 0 LOST - agent-smith scores 0 TypeScript test functions of 169 extracted, xagents-webapp exactly 1 of 410, because DF-14-3-A (case-sensitive startswith 'test') and DF-14-3-C (callback blocks yield no definitions) keep TS tests out of the scorer entirely. The story's rationale therefore narrows to the MECHANISM and the FIXTURE, which is the branch the proposal's section 3.3 pre-registered. Baselines re-measured: 1621 passed / 0 failed / 0 skipped exit 0, mypy Success-87, bandit 19L/0M/0H; flag rates 653/3551=18.4%, 295/1122=26.3%, xagents-webapp 17/73=23.3%; _ASSERTION_CALLEES holds 53 names (NOT 23 - that figure predates 14.2); _CORROBORATION_ASSERTION_CALLEES frozen at 23. DN-14-2-1 vs NFR-P2 resolved WITHOUT conflict and without a halt: 14.3 widens ONE table (the density numerator); the two tables are partitioned by QUESTION, never by language, so no language field enters the detector. The JS/TS verdict-ineligibility limitation was measured to be caused by _MOCK_CALLEES being Python-only and by fact (b)'s Python-shaped machinery - demonstrated to hold even under a hypothetical ONE-TABLE design carrying 14.3's full vocabulary - so it is NOT a trap DN-14-2-1 laid for this story; it is made AC6 (a new ledger entry with a named owner) rather than left unrecorded, because it bounds Epic 15: a TypeScript bench member can contribute advisory findings only. TC-ArgusAgent-DETECT-001-116 identified by execution as the ONLY guard that will go red (its cross_language literal is 14.2's 'this has not happened yet' assertion) - to be re-authored -86-style, never deleted. Dogfood artifacts PREDICTED UNCHANGED (0 delta over Argus's own 1,777 test functions), reversing the usual expectation. DF-14-2-A and DF-14-2-B both name this story as target_story and are in scope. DF-14-3-A/B/C cited, not fixed; A and B move together or not at all. || ADDED 2026-08-17 by sprint-change-proposal-2026-08-17b.md (APPROVED by XAgent007). RUNS STRICTLY AFTER 14-2, which owns the same frozenset - do NOT context it before 14-2 is done or it will context against a _ASSERTION_CALLEES that is about to change under it (the DN-4 discipline, applied to a second pair). _ASSERTION_CALLEES holds 23 names and every one is unittest, while the shipped index emits expect/toBe/assertEquals as ordinary edges. MEASURED BY EXECUTION over six fixtures (proposal 17b section 1.3): a JS test containing expect(r).toBe(5) scores assertions=0 density=0 and IS FLAGGED - a FALSE ACCUSATION, the same failure class Epic 14 exists to close, arriving by a different route. Fix is STRICTLY FLAG-REDUCING (assertion_sites is the numerator of a ratio whose floor fires from below), so nothing passing today can start failing. NFR-P2: the set stays FLAT and language-agnostic on the _UNAMBIGUOUS_TEST_SUFFIXES precedent - NO language field enters the detector. Two ratified corpus members are typescript (xagents-webapp, agent-smith) and the predicted flag delta over them is EXPLICITLY UNMEASURED - an AC measures it first and records a wrong prediction AS WRONG. Does NOT fix DF-14-3-A/B/C; Go and Java stay unscored after it lands. Not done on a Windows-only pass. || DEV 2026-08-18 (dev-story) on HEAD e2e278c -> commits e252043 (delta) + d339076 (dogfood artifacts, SEPARATE): ready-for-dev -> review. ONE table widened, _ASSERTION_CALLEES 53 -> 89 names; _CORROBORATION_ASSERTION_CALLEES byte-unchanged at 23 and both thresholds byte-unchanged. RED FIRST: the Allman-brace fixture measured asserts=0 stmts=1 FLAGGED=True in JS AND TS on the unmodified tree with expect/toBe provably already in the edge set (a vocabulary gap, not a parse failure), asserts=2 FLAGGED=False after, and its byte-identical assertion-free twin STAYS flagged. The epic's criterion was NOT discharged against the fixture it names: plainfn.test.js scores statement_count=0 and is silent for 14.2's denominator reason (DN-14-3-4), pinned by -125 which also asserts the two fixtures differ ONLY in brace placement. AC3 MEASURED per member before and after: 0 flags GAINED and 0 LOST on all three, and the flagged SETS are BYTE-IDENTICAL as sorted file::name@line fingerprints, so 0/0 is an identity and not two equal totals hiding a swap (653/3551, 295/1122, 17/73; corroborated 0 -> 0, which is an EMPTY DENOMINATOR and therefore UNEVALUABLE on its own - the mechanism, DN-14-2-1, is the evidence). Section 0.4's refuted corpus prediction REPRODUCES on a LARGER vocabulary than it tested, so none of the 36 added names removes a real Python flag across 4,746 test functions. Vocabulary MEASURED not copied (expect 6876, toBe 2560, toHaveBeenCalledWith 696, equal 1548, ok 758, deepEqual 506); match/doesNotMatch EXCLUDED with their reason and objectContaining excluded as an argument builder. -116 predicted RED BEFORE the suite ran and RE-AUTHORED not nudged: the 12-name literal is byte-identical, only the expectation inverted, plus a new arm pinning the frozen table - and it was the ONLY guard that moved. NEW cohesion module tests/test_vacuous_cross_language.py (DETECT-001-123..-132, 738 lines); no size exemption, every module under 1200 (tightest: test_vacuous_detector.py 1161, headroom 39). DF-14-2-A and DF-14-2-B both discharged; the sweep written for the latter (-130) immediately found a SECOND caret anchor, _LEADING_CHAIN_RE, that the ledger entry never named - equivalence demonstrated over 218,017 lines / 25,649 matches / 0 disagreements. DF-14-3-D filed with a named owner: no non-Python test can reach verdict-eligibility, measured to be caused by _MOCK_CALLEES and NOT by DN-14-2-1 (so 14.2 laid no trap), bounding Epic 15 to advisory-only TypeScript evidence; DF-14-3-E and DF-14-3-F also filed. GUARD PREDICTION P7 WAS WRONG AND IS RECORDED AS WRONG: the dogfood currency guards DID fire, but NOT for the reason predicted - the flag delta was 0 as measured; the artifacts also record the BUILD-COST PROXY (total physical LOC), which rose by exactly the +141 lines added to argus/, so a comment-only commit would have moved them identically. Discharged via AI-E12-11; no artifact hand-edited, no assertion loosened. Gates: 1631 passed / 0 failed / 0 SKIPPED exit 0 (baseline 1621, +10 = this story's new cases), mypy Success-87, bandit 19L/0M/0H - all LOCAL (Windows); CI evidence is NOT ESTABLISHED. DF-14-3-A/B/C cited and NOT fixed: Go and Java stay unscored, callback suites stay invisible, and A and B move together or not at all. || REVIEW 2026-08-18 (code-review, iteration 1) on HEAD d339076: review -> in-progress. VERDICT fail. Baseline reproduced exactly by execution (1631/0/0 exit 0, mypy Success-87, bandit 19L/0M/0H; 89/23 tables; AC3 0 GAINED/0 LOST with byte-identical fingerprint sets re-derived independently over freshly materialized git-archive trees of all three pinned shas; AC1 RED-then-green; -116 re-authoring; the DF-14-2-B regex equivalence; module sizes; the P7 dogfood LOC-proxy explanation) - no finding on any of that. Two findings did not survive adversarial reproduction and are written to the story file's Review Findings: (1) six of the 36 added _ASSERTION_CALLEES names (ok, equal, Error, Equal, and less commonly throws/rejects) collide with ordinary Python identifiers, reproducibly un-flagging synthetic zero-assertion Python tests that were correctly flagged before this story - DN-14-3-2's own exclusion principle (used correctly for match/doesNotMatch) applied inconsistently; flag-reducing only, 0 impact on the ratified-corpus measurement and on tests/cartridges/, but unresolved and needs a human call. (2) TC-ArgusAgent-DETECT-001-131 does not exercise the mechanism it and DF-14-3-D claim: the shipped fixture's own widened assertion already clears the density floor, so heuristically_vacuous is False and _ast_corroborated short-circuits before calling provenance_evidence; reshaping the fixture to clear the mock_ratio ceiling instead does reach it and reproduces CORROBORATED=False for the claimed reason, so the underlying DF-14-3-D conclusion holds, but the shipped guard does not test it, and the ledger's cited widened-table sut=3 does not reproduce against the shipped fixture (measured sut=2; frozen-table sut=4 does reproduce). No High/Medium finding resolved; not eligible for done this round.  # dev-story 14-3 FIX round 1 (2026-08-18) on HEAD d339076: in-progress -> review. Review iteration 1 returned FAIL with two High findings; BOTH resolved, neither argued away. (1) The exclusion principle had been applied to match/doesNotMatch and to nothing else, leaving six ordinary non-assertion Python identifiers (ok, equal, Error, Equal, throws, rejects) in the density numerator - a wrong match RAISES density and REMOVES a flag, so the risk is silent recall loss, contained by the frozen corroboration table. DN-14-3-5 replaces the judgement with a RULE: admit a name when its measured Python collision is materially below its measured JS/TS benefit, counted with the stdlib ast module over 4046 independent Python files (three pinned members, Argus, site-packages, CPython 3.11 stdlib). It REPRODUCES both previously-ratified decisions (match out at 706 vs 476, expect in at 29 vs 6876) and drops exactly one name: Error, 164 measured Python call sites against a benefit of ZERO. ok (10 vs 764) and equal (34 vs 1548) KEPT with their cost recorded and asserted by execution; throws/rejects/Equal kept on 0 measured collisions. New guard -133 demonstrated RED against the shipped table. (2) TC-ArgusAgent-DETECT-001-131 was ITSELF a vacuous test: its fixture cleared the density floor, so _ast_corroborated returned at 'if not heuristically_vacuous' and provenance_evidence was never called. Re-authored with a non-vacuity floor mirroring -115, a fixture reaching the predicate through mock_ratio 4/7 > 1/2, and the mechanism asserted DIRECTLY (_mock_bound_names binds nothing in 'const fake = Mock()' and binds fake in 'fake = Mock()'). The sweep of -123..-132 found a SECOND instance, -132, which concluded silence from what an unparsed file also produces; floors added there and on -124. Re-measured: 0 flags GAINED / 0 LOST with byte-identical file::name@line fingerprint sets on all three pinned members, cartridge recall unchanged, _CORROBORATION_ASSERTION_CALLEES byte-unchanged at 23, thresholds byte-unchanged, pipeline.py untouched at 1111. The unreproducible sut=3 figure corrected in deferred-work.md and story 0.7(c), named to a fixture. DF-14-3-G and DF-14-3-H filed with named owners; test_vacuous_detector.py judged NOT an acceptable steady state at 1161/1200 but deliberately not split inside a fix round, zero lines added. Commits cce9897 (delta) + d563fa9 (dogfood artifacts, separate; LOC proxy only, NFR-C1 numerator unchanged at 435). Full suite 1632 passed / 0 failed / 0 skipped, exit 0. Gates LOCAL, CI evidence NOT ESTABLISHED. || REVIEW 2026-08-18 (code-review, iteration 2) on diff d339076..d563fa9 (commits cce9897 code, d563fa9 artifacts): review -> done. VERDICT pass. Both iteration-1 High findings independently re-verified as closed by execution, not by re-reading the record. DN-14-3-5's collision table reproduced EXACTLY on a from-scratch 4046-file population (Error 164, match 706, expect 29, ok 10, equal 34, Equal/throws/rejects/doesNotMatch 0 - digit for digit); the RED-first claim for -133 reproduced exactly against iteration-1's shipped table (assertion_sites=1, heuristically_vacuous=False); -131's non-vacuity floor confirmed load-bearing by mutation (scoring iteration-1's own weaker fixture against the current detector fails the new floor); the -123..-133 sweep checked case by case, no unfloored negative found; the corrected consumed_sut figures (frozen=3, one-table=1) re-derived and matched deferred-work.md exactly; deferred-work.md's working-tree diff confirmed append-only (0 removed lines); DF-14-3-H's deferral (39-line headroom, target 13-5, owner XAgent007) judged sound rather than a defect to fix now. Full suite re-run independently: 1632 passed / 0 failed / 0 skipped, exit 0. No decision-needed, patch, or unresolved High/Medium finding remains. Epic 14 closes; Story 13-5 is unblocked.
  epic-14-retrospective: done  # KEY CREATED 2026-08-18 BY THIS RETROSPECTIVE - it did not exist. Epics 1-13 each carry an `epic-N-retrospective` key; sprint-change-proposal-2026-08-17.md, which added epic-14, created the epic and its three stories and NOT this key, and the operator decision that added epic-15 did the same (epic-15-retrospective is created below, at `backlog`). THIS MATTERS AS A GATE, NOT AS BOOKKEEPING: the retrospective key is what stops an epic rolling up to `done` before it has been reviewed - precisely the mechanism epic-13-retrospective is DELIBERATELY holding open at `in-progress` pending story 13-5. An epic with no retrospective key has no such brake at all. The structure the plan already assumed is restored here; nothing new is invented. epic-13-retrospective was NOT touched. || RETROSPECTIVE COMPLETE 2026-08-18 (epic-14-retro-2026-08-18.md), created -> done. 3/3 stories done (14-1, 14-2, 14-3) at HEAD d563fa9. THE QUESTION IT WAS ASKED, ANSWERED: the instrument IS now trustworthy enough for story 13-5 to mean something - fact (b) is a provenance-shape predicate rather than "the test constructs a mock", agreement with the bare mock_sites>=1 term went 34/36 -> 0/36 on the only sub-population where they can differ, 0 of 31 adjudicated locators remain verdict-eligible at unchanged pinned shas, recall 3/3 on planted cartridges including the anti-overfitting and non-ASCII controls, and three separate false-accusation mechanisms were closed. WHAT IT DID NOT ESTABLISH: that the predicate is correct on unseen code - it has never been human-adjudicated against a population containing real vacuous tests, and DF-14-1-A records the residual (a NAME-LEVEL proxy, not dataflow provenance; full provenance stays story 6.2 scope). EPIC 14 CHANGED NOTHING ABOUT THE GATE: protocol_cleared has never been True, the FR34 disclosure stands, and 13-5 is expected to return UNEVALUABLE rather than CLEARED because a corrected detector emits zero blocking findings on the five ratified members and an empty precision denominator is unevaluable by construction. That is the measurement working, not a shortfall; DF-13-5-A pre-registered the stopping rule on 2026-08-17 BEFORE this outcome existed. DELIVERY: 14 commits (47b6dbe..d563fa9), 7 of them dogfood regenerations; 35 new test ids re-derived by diffing the id set (1146 -> 1181, zero removed) - 14.1 fourteen, 14.2 ten, 14.3 eleven; 8 review iterations; full suite 1632 passed / 0 failed / 0 skipped exit 0, re-run in the retrospective session. THE FINDING MOST WORTH CARRYING: TWO of the eleven guards story 14.3 added were THEMSELVES VACUOUS (-131 short-circuited at `if not heuristically_vacuous: return False` before ever reaching the mechanism it cited; -132 concluded silence from `scored == []` / `findings == ()`, which is also exactly what an UNPARSED file produces) - vacuous tests inside the vacuous-test detector own suite, in the epic built to stop exactly that. Non-vacuity floors added, -124 hardened, -123/-125..-130 re-checked and recorded as CONFIRMATIONS. RE-DERIVED HERE AND NOT IN THE STORY RECORD: the sweep covered 11 ids; the other 24 (14.1 -101..-112 plus VERDICT-001-116/-117, 14.2 -113..-122) HAVE NEVER BEEN ASKED THE QUESTION - filed AI-E14-2, and 14.1 guards a predicate behind three sequential short-circuits, which is exactly -131 shape. PROCESS: reviews found 2/2 in 14.1 (both AFTER the dev declared done - line-continuation misclassification made a genuine test verdict-eligible purely by PEP 8 wrapping, then column-blind occurrence resolution did the same via a semicolon compound), 1/1 in 14.2, 2/2 in 14.3; the SM contexting control refuted FOUR premises before any code was written, including an ACCEPTANCE CRITERION THAT WOULD HAVE PASSED WITH ZERO CODE CHANGE (14.3 named JS fixture is no longer flagged at all - 14.2 denominator scores a K&R-brace JS body at statement_count==0) and a corpus prediction REFUTED outright (0 gained, 0 lost on all three members). THE DEV PASS FOUND ZERO OF ITS OWN SUBSTANTIVE DEFECTS IN ANY OF THE THREE STORIES; both independent re-derivation phases found nine between them. FIGURES CORRECTED RATHER THAN PROPAGATED: 14.2 removed 1,581 flags (1,195 + 386), not 1,572 - that was iteration 1; final rates minions 653/3551 = 18.4% and agent-smith 295/1122 = 26.3%, denominator 1.0000x exact on 1,848/1,848 spans. LEDGER RE-DERIVED AGAINST deferred-work.md ON DISK, not from a summary: DF-14-2-A and DF-14-2-B genuinely CLOSED with status fields and demonstrated evidence (218,017 lines / 25,649 matches / 0 disagreements for -B, which also caught a SECOND offender its own entry never named); DF-14-3-D/-E/-F/-G/-H filed OPEN with named owners; DF-14-3-A/-B/-C and DF-14-1-A OPEN and UNASSIGNED - A and B are COUPLED and the one-character fix to A alone would convert Go harmless silence into a fresh false accusation across an entire language. THREE SIGNIFICANT DISCOVERIES, RECOMMENDED NOT APPLIED: (SD-1) the ENTIRE GOVERNANCE RECORD of this epic is UNCOMMITTED - both change proposals, story files 14-2 and 14-3, deferred-work.md (+494), epics.md (+284) and story 13.4 own tests/test_status_document_registry.py are untracked, and origin/master is 47b6dbe, THE EPIC BASE COMMIT, so NO CI RUN COVERS ANY COMMIT IN THIS EPIC and the green suite measures a tree no commit describes; story 13-5 premise re-measurement will read artifacts master does not contain. (SD-2) 24 of 35 new guards unswept for vacuity. (SD-3) DF-14-3-H "tightest tracked module in the repository" claim is MEASURABLY FALSE - re-derived with the ceiling guard own _measure_population(): tests/test_built_distribution.py is at 1200/1200 (ZERO headroom, MAINT-001-03 pins 1201 as the failure) and tests/test_instrument_disclosure.py at 1198 (TWO), against test_vacuous_detector.py 1161 (39); ALL THREE are on story 13-5 blast radius and 13-5 must exercise the INSTRUMENT_DISCLOSURE_VALIDATED defect the epic-13 interim section 11 records, so the split-first precondition covers three modules, not one - and argus/detectors/vacuous_test.py is at 1113 after growing +490 lines this epic. 10 action items (AI-E14-1..-10, one record-only), 6 critical-path items, 6 significant-discovery alerts. NO EPIC-13 ACTION-ITEM STATUS WAS MODIFIED - epic-13-retrospective is still in-progress and its FINAL pass reserves that assessment; ruling on another epic ledger from outside it is the AI-E12-3 defect with a governance boundary crossed on top. This document is registered in _STATUS_DOCUMENTS (tests/test_status_document_registry.py) as part of writing it - AI-E12-1 second half, second consecutive application; DOCS-001-22 closes in BOTH directions so the document and its registration line MUST land in ONE commit. Suite re-verified GREEN after every write in this session: 1632 passed / 0 failed / 0 skipped, exit 0. epic-14 itself deliberately LEFT in-progress - the roll-up is the orchestrator act, not this retrospective.
  epic-15: done  # CREATED 2026-08-17 by OPERATOR DECISION (XAgent007), NOT by a change proposal - recorded as such because sprint-change-proposal-2026-08-17b.md section 4.4 states 'no epic is created', which was true OF THAT PROPOSAL. Admitted by direct authorisation on the Story 13.4 precedent. 'Make the Gate Evaluable - a bench with the defect class in it.' Epic 14 repairs the instrument and CANNOT clear the gate: a corrected detector is expected to emit zero blocking findings on the current five-member corpus and an empty precision denominator is UNEVALUABLE by construction. Clearing needs findings that are REAL, which needs a bench that CONTAINS THE DEFECT CLASS. Covers the section-2.5 operator decision's option (a) - DF-13-5-A. DEPENDS ON epic-14. THIS EPIC DOES NOT CLEAR THE GATE EITHER - it makes the gate EVALUABLE, and the stopping rule was PRE-REGISTERED in DF-13-5-A on 2026-08-17, before any repository was chosen and before any number existed. ONE ROUND.  # create-story 2026-08-19: backlog -> in-progress. FIRST story of epic 15 to be CREATED (15-2; 15-1 remains at backlog). The STATUS DEFINITIONS block above keys this transition on story CREATION - 'backlog -> in-progress: Automatically when first story is created (via create-story)' - not on story NUMBER, and 15-2 is the first of this epic's two stories to leave backlog. The epic charter, its Covers line and DF-13-5-A's ONE-round stopping rule are UNCHANGED; this epic still does not clear the gate, it makes the gate evaluable.  # dev-loop roll-up 2026-08-19: in-progress -> done. Both stories `done` (15-1, 15-2) and epic-15-retrospective `done` (epic-15-retro-2026-08-19.md). 15-1 closed at code-review iteration 2 (Sonnet) PASS after one documentation-only fix round; 15-2 closed at iteration 2 on 79a78cf. Roll-up written by the ORCHESTRATOR, which is the only sprint-status write it makes - every other transition in this run was written by the worker that earned it. THE ROLL-UP CLOSES THE EPIC, NOT THE WORK IT NAMES: epic 15 SELECTED a bench and RATIFIED NOTHING. All 14 candidate rows stay eligible_for_n=False, N stays 5, MANIFEST_FIELDS stays closed at 9, the >=80% precision gate stays NOT CLEARED, FR34 and protocol_cleared are untouched, and DF-13-5-A's ONE permitted round is UNSPENT. The retrospective raised SIX significant discoveries needing an operator decision (SD-1 protocol section 6 R2 ratification untaken, which leaves this epic's deliverable inert; SD-5 epics.md ENDS AT EPIC 15 so the post-R2 work has no container; SD-6 two NFR-M1 splits are preconditions of that round; SD-2 the bench is Python-only in fact while scope is bilingual on paper) plus AI-E15-1..-15. NONE were acted on - they exceed worker and orchestrator authority alike. Epic 15 is the LAST epic in this plan and no epic now carries a non-done item; that is the plan exhausted, NOT the gate cleared. UNCOMMITTED at roll-up time: the retro artifact, this file, and tests/test_status_document_registry.py must land in ONE commit - DOCS-001-22 closes in both directions.
  15-1-a-bench-with-the-defect-class-in-it-chosen-before-anyone-looks: done  # dev-story 2026-08-19: ready-for-dev -> in-progress, and HELD there - NOT advanced to review. HALTED ON AN OPERATOR ACT, measured rather than asserted. AC6.1 asks for 12-20 candidates; criterion 7 requires each candidate's pin to resolve at a path ON THIS MACHINE and criteria 2/3/5 read the pinned tree, so a repository that is not already here cannot be measured - and AC5 forbids fetching. The whole locally-resolvable universe was swept: 11 repositories, ZERO qualify, every one failing criterion 2. Exactly two are genuinely third-party (microsoft/qlib @ 79633dd9: 36 test files, 0 co-occurrence; Windows-universal-samples @ 0db108e9: 0 Python test files) and both fail. The highest co-occurrence count anywhere on this machine is 3, by the Argus repository itself, which is permanently ineligible. Assembling the bench therefore needs third-party source FETCHED, and protocol section 6 R2 says verbatim that choosing members and fetching third-party source are not autonomous acts. That act was named with options, not performed. WHAT DID LAND: the criteria frozen in git as executable code in their own commit 16d7100d73261c759d6176351f2caeff3d1fe172 (AC1.1), the structural argus.detectors import ban (AC2.2, TC-ArgusAgent-PRECISION-001-74), the mechanical ordering guard with all three non-vacuity preconditions and ancestry driven to both outcomes (AC1.2/1.3, -75), the AC4.3 candidate-row checker with five executed-mutation REDs plus a tripwire that goes RED when the first candidate lands (-76), the criteria-are-code guard (-77), and the full 11-repository sweep with every rejection naming the criterion that rejected it (AC6.2/6.3). AC1.4's epics.md residue is RESOLVED - 762a73e committed epics 14 and 15, so git history now shows this epic exists; epics.md was not edited. TypeScript stays in scope behind the AC3.2 floor - collapsing to Python-only remains the operator's call at R2 and was not pre-empted. tests/corpus/_manifest.py and tests/test_validation_corpus.py are BYTE-UNCHANGED, MANIFEST_FIELDS stays closed at 9, N is unchanged at 5, argus/ is byte-unchanged (so no dogfood regeneration - the currency guards track argus/ LOC and this delta contains none). Nothing was ratified, no detector was run over any candidate, nothing was adjudicated, no threshold / corpus floor / FR34 / protocol_cleared moved, and DF-13-5-A's ONE permitted round is NOT consumed - no bench was expanded because none could be selected. Every ledger entry cited remains open and un-dispositioned; deferred-work.md was not touched.  # ADDED 2026-08-17 by operator decision. SELECTION ONLY - it does NOT ratify (protocol section 6 R2 is an operator act), does NOT run Argus over any candidate, and does NOT adjudicate. Story file stories/15-1-a-bench-with-the-defect-class-in-it-chosen-before-anyone-looks.md, written from the authorising conversation - every premise in its section 0 is CARRIED FORWARD and must be re-derived by execution at create-story time, which is why it is backlog and not ready-for-dev. THE ORDERING IS THE POINT: criteria and candidate list land in a commit that PRECEDES every commit containing Argus output over any candidate, and that sha is recorded in the story - git history is the evidence, an asserted intention is not. Candidates enter tests/corpus/_manifest.py with eligible_for_n=False + ineligible_reason='candidate - awaiting operator ratification (protocol section 6 R2)', so the guard is STRUCTURAL (CorpusMemberSpec.__post_init__ raises) and no field is added - MANIFEST_FIELDS stays closed. SCOPED PYTHON + TYPESCRIPT ONLY: DF-14-3-A/B leave Go and Java unscored and DF-14-3-C leaves callback-style JS/TS suites invisible, so admitting them would inflate the N that satisfies the floor while contributing nothing to the N that gates. Selection MAY proceed in parallel with epic-14 (it runs nothing); a RUN may not. Not done on a Windows-only pass.  # create-story 2026-08-19 (Scrum Master): backlog -> ready-for-dev. Story file stories/15-1-a-bench-with-the-defect-class-in-it-chosen-before-anyone-looks.md REWRITTEN as a contexted story on HEAD f2189c1 (= origin/master, 0 ahead / 0 behind). PREMISE RE-MEASUREMENT (AI-E13F-2 / AI-E14-8), run out-of-tree read-only with git status --porcelain argus tests empty before and after, and no corpus checkout mutated. SIX PREMISES SURVIVED EXACTLY: VALIDATION_CORPUS 7 rows / 5 eligible / all independent / 3 python + 2 typescript; MANIFEST_FIELDS closed at 9 fields; __post_init__ raises on eligible_for_n=False with no reason; validation_floor_n() resolves to 5; the pre-registered ONE-round rule present verbatim and un-dispositioned; SOURCING_RULE intact. EIGHT PREMISES DID NOT SURVIVE and are corrected in the story rather than propagated, each with the original named as wrong: (1) THE BIG ONE - the Python+TypeScript scoping rests on a premise measured FALSE for TypeScript. Re-derived at the pins through the real ast_index and the shipped _is_test_function: xagents-webapp@33a86525 has 279 TS test files yielding 1 scorable test function, agent-smith@9ab774d7 has 88 yielding 0 - so 367 TS test files across both ratified TS members yield ONE scorable test function, because idiomatic Jest/Vitest/Mocha describe/it callbacks extract ZERO definitions (DF-14-3-C) and _is_test_function requires a lowercase 'test' prefix. TypeScript is therefore in the SAME position as Go and Java. SCOPE IS NEITHER WIDENED NOR NARROWED: TS stays in scope carrying a measured extractor-visibility floor (DN-15-1-3 / AC3.2, >=25 scorable test functions), the measurement goes to the operator, and collapsing the bench to Python-only stays the operator's call at R2. (2) prd.md:190's '31 findings from 2 of 5 members' is SUPERSEDED (DF-13-5-B) and is cited as history, not as current corpus state. (3) 'a corrected detector is EXPECTED to emit zero blocking findings' was a FORECAST and is now a MEASUREMENT - 13.5's five-of-five pin-verified zero over 1,960 files. (4) the Epic-13 FINAL retrospective's AC1 blocker ('cannot be established from an uncommitted tree') is RESOLVED for the code tree; residue named - epics.md at HEAD ENDS AT EPIC 13, so Epic 14, Epic 15 and both its stories exist only in an uncommitted working copy. epics.md NOT edited (not this story's file, and it carries unrelated uncommitted work). (5) the guard range -21..-30 actually runs to -31 plus DOGFOOD-001-53/-54/-55; -31 verified undisturbed by candidate rows because it closes over eligible_members(). (6) 'the guard is STRUCTURAL' is true of the promotion path and FALSE of three checks - __post_init__ RETURNS EARLY for eligible_for_n=False, so an 8-char sha, a non-hex sha and a go/ruby language all CONSTRUCT SILENTLY on a candidate row; AC4.3 adds those guards and AC4.4 requires each observed RED by an executed mutation. Promotion still takes TWO deliberate edits (flipping the boolean alone raises on the retained ineligible_reason), so AC4's own claim stands. (7) criterion 7's DF-13-3-A citation taught the wrong lesson - that entry's premise was WITHDRAWN 2026-08-17 and the pin was never unreachable; the real lesson is that a path scanned at the wrong depth is indistinguishable from an unreachable pin. (8) premise 5 is MOOT - epic-14 is done, so the parallel-with-epic-14 dispensation is dead. CORPUS PATHS RE-DERIVED, all five pins reachable, read-only: ai-body-runtime D:/ProjectX/XAgents/XAgents/ai_body_runtime; agent-markovich .../XAgents/AgentMarkovich (NO hyphen); minions .../XAgents/Minions (checkout HEAD c2940d2f, DRIFTED off pin ec63b729, pin still reachable); xagents-webapp .../XAgents/XAgents-WebApp (depth 4); agent-smith .../XAgents/XAgents/Agent-Smith (depth 5). DF-14-3-F's 'same extra nesting as agent-smith' wording is itself wrong - the two sit at DIFFERENT depths, so each path must be resolved individually. THREE DECOY TREES found beside the real ones carrying the SAME origin URL and the WRONG bytes ('Minions - Copy', 'XAgents-WebApp - Temp', 'AgentMarkovich-old'), all three failing cat-file on the pin: matching the remote is not matching the tree. A Git-Bash /d/... path form fails exit 128; pass the Windows form. NEW MEASUREMENT the story turns on, taken by TEXT PATTERNS ONLY with the detector NOT imported: across 315 Python test files at three pins, files carrying BOTH a mock binding AND a mock assertion number 1 under the strict predicate and 6 under a looser one (both reported, both predicates stated verbatim, conclusion invariant to the choice). That is WHY the gate returned zero - the bench does not contain the defect class - and it is established without running Argus over anything. Criterion 3 is consequently rewritten as mock-assertion CO-OCCURRENCE (DN-15-1-1), never 'uses mocking', which is the refuted 0 TP / 26 FP definition. THE PICK-BEFORE-YOU-LOOK RULE IS MADE MECHANICAL: AC2.2 permits argus.index.* (the instrument's REACH) and bans argus.detectors.* (its OUTPUT) by an ast walk carrying -28's non-vacuity floor. AC1's ordering guard asserts the criteria sha resolves, is an ancestor of HEAD, and touches no candidate-output path - with all three non-vacuity preconditions pinned and the ancestry predicate driven to BOTH outcomes, because every guard this story adds asserts a NEGATIVE and this project shipped 4 of 35 unreal guards in epic 14. SCOPE UNCHANGED: no ratification, no run over any candidate, no adjudication; MANIFEST_FIELDS stays closed and no field is added; N stays 5; the >=80% threshold, the corpus floor, FR34 and protocol_cleared are untouched; argus/ is byte-unchanged and argus/detectors/vacuous_test.py (1196/1200, DF-15-2-D) is NOT on the write set, so its split trigger does not fire. DF-13-5-A's ONE-round rule is quoted verbatim and cited, and the story is written so it cannot be read as licence to keep expanding. External prior art checked and rejected with reasons (Zhu et al. mock-assertion dataset, ATLAS, Defects4J - all Java, which DF-14-3-A/-B leave unscored, and all measuring recall or supplying extracted snippets rather than pinned repositories). NOTHING was disposed of in deferred-work.md and the story yields ZERO closure claims (verified by executing story_closure_claims over it, so TC-ArgusAgent-DOCS-001-78 gains no new claim). epic-15 NOT touched - it is already in-progress from 15-2's create-story. Full suite re-run after the story file was written. Nothing committed, staged or pushed.  # dev-story pass 2 2026-08-19: in-progress -> review. THE OPERATOR (XAgent007) AUTHORISED THE FETCH - protocol section 6 R2's own path; option B (relax the floor) and option C (take the pre-registered branch) both DECLINED, recorded as a DATED STRIKE on AC5, never a deletion. The criteria stayed FROZEN: applied exactly as committed in 16d7100, which precedes every fetch; COOCCURRENCE_FILE_FLOOR still 10, TEST_FILE_FLOOR still 50, HISTORY_SPAN_DAYS_FLOOR still 730, no predicate touched. 20 repositories fetched to D:/_bench (outside this repo, --no-checkout, read only through the git object database at the pin, origin AND pin verified individually on all 20); 14 admitted, 6 rejected, all six on criterion 2 and each recorded with the criterion that rejected it. Bench carries 2316 test files and 614 co-occurrence files against the current corpus's 1 - an order-of-magnitude fact, explicitly NOT a prediction of yield. Harness proved non-vacuous by reproducing section 1.1 EXACTLY over minions@ec63b729 (286/21/3/1/6). TWO INTENDED BEHAVIOUR CHANGES, each re-authored with its reason and neither relaxed: -76's tripwire (population asserted EXACTLY empty) FIRED as designed and now carries AC4.4's real population arm; -27 was conflating INTRINSIC ineligibility (permanent, arm preserved exactly) with PENDING ineligibility (the R2 act itself, must remain possible) and its candidate arm is strictly stronger. NEW guards -78 and -79 at the true next-free ids. Nine executed mutations drove them RED before trust; manifest restored byte-for-byte, sha256-verified. NOTHING RATIFIED, no detector run over any candidate, nothing adjudicated: all 14 rows eligible_for_n=False, MANIFEST_FIELDS closed at 9, N still 5, -31/-25/-22 green without amendment. argus/ byte-unchanged so NO dogfood regeneration - confirmed by git diff and by zero argus/ files in the code commit, not assumed. vacuous_test.py untouched at 1196, no _EXEMPT_BY_DESIGN entry. TypeScript stays in scope behind DN-15-1-3's floor; all 20 fetched are Python so no TS candidate was tested, and collapsing to Python-only remains the operator's call at R2. DF-13-5-A's ONE round is NOT consumed - 15.1 selects and never reaches the branch point; the rule is cited verbatim and its branch is not executed. Every ledger entry cited stays open and un-dispositioned; deferred-work.md was not touched. Suite 1651 passed, 0 failed, 0 skipped, exit 0 with ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1. CI GREEN on run 32270312088 over sha c028da5, ubuntu-latest x 3.10/3.11/3.12, all three legs success; Security Shield 32270311904 success.  code-review 15-1 (2026-08-19, Sonnet): review -> in-progress. VERDICT concerns, iteration 1, on e5499ac (git diff 762a73e..e5499ac; commits 16d7100 criteria-as-code, 4f4db78 guards, 08788c4 docs/CI, c028da5 bench lands, e5499ac story record). Independently re-verified by EXECUTION: 16d7100 is an ancestor of HEAD and touches only scripts/candidate_selection.py + the story file (git show --stat, no manifest, no candidate output); c028da5 touches only tests/corpus/_manifest.py and the two guard modules, argus/ untouched (git diff --stat argus/ empty); full suite via --junitxml = tests=1651 errors=0 failures=0 skipped=0, exit 0, ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1; git status --porcelain argus/ tests/ empty; gh run view 32270312088 confirms all three legs success over headSha c028da5 exactly as claimed. Read CorpusMemberSpec.__post_init__ directly and confirmed the early-return, single-edit-raises, two-edit-succeeds and INTRINSIC/PENDING partition-by-provenance claims exactly against the live 21-row corpus (5 eligible + 2 intrinsic + 14 pending). Read guards -74/-75/-76/-77/-78/-79 and re-authored -27 in full: each pins a real non-vacuity precondition before the absence and is demonstrably driven to both outcomes in the test body itself. D11/D12's summed figures (614 co-occurrence files, 2,316 test files) reproduce exactly by arithmetic. deferred-work.md confirmed untouched by all four commits; every cited ledger entry (DF-13-5-A/-B, DF-14-3-A/-B/-C/-F, DF-13-3-A, DF-15-2-A/-B/-C/-D) confirmed OPEN and un-dispositioned on disk. ONE Low finding, written to the story's Review Findings as an unchecked [Review][Patch] item: D15's Debug Log understates two module line counts (test_validation_corpus.py claimed 915, actual 927; test_candidate_selection.py claimed 697, actual 698) - no gate or AC depends on the prose figure and the headroom conclusion is unchanged, but it is a fresh instance of the exact stated-figure-wrong anti-pattern this story's own section 1 names five times. No High/Medium finding; all ACs independently confirmed met; tests and CI green.  dev-story 15-1 fix round 1 (2026-08-19): in-progress -> review. Code review iteration 1's ONE Low [Review][Patch] finding addressed; documentation only, no code changed. The finding (D15's debug log understating two module line counts) was RE-DERIVED before it was adopted, not copied from the review: wc -l, the NFR-M1 ceiling guard's own _physical_line_count, and git show --numstat arithmetic off the object database all three agree - tests/test_validation_corpus.py 859+76-8 = 927 (claimed 915) and tests/test_candidate_selection.py 439+284-25 = 698 (claimed 697), undercounts of 12 and 1, matching the reviewer's figures exactly. The other four figures in the same sentence were re-measured rather than assumed innocent (_manifest.py 886, vacuous_test.py 1196, and the 859/546/439 baselines at c028da5^) and all verify. Corrected by a dated strike rather than a deletion in D15 and both File List lines, with the measurement and its instruments in the new D16. No gate or AC read the prose figure - NFR-M1 measures the real file - and the headroom conclusion is unchanged. Also recorded: the review checkbox was found already [x] on disk while its correction had not been applied, named in D16 so the box is not mistaken for the evidence. UNCHANGED: argus/** and tests/** byte-unchanged (git diff --stat argus/ empty, git status --porcelain tests/ empty), scripts/candidate_selection.py and COOCCURRENCE_FILE_FLOOR/TEST_FILE_FLOOR/HISTORY_SPAN_DAYS_FLOOR untouched, MANIFEST_FIELDS closed at 9, N = 5, all 14 candidate rows still eligible_for_n=False, nothing ratified, no detector run over any candidate, nothing adjudicated, every threshold / corpus floor / FR34 / protocol_cleared where it was, deferred-work.md not opened and no ledger entry dispositioned. Suite after the record: 1651 passed, 0 failed, 0 errors, 0 skipped, exit 0, ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1 - identical to the pre-fix baseline, as a documentation-only round must be. Local gates are Windows-only while CI runs an ubuntu matrix; this round changed no path and no code, so nothing new is exposed to that gap.  # code-review 15-1 iteration 2 (2026-08-19, Sonnet): review -> done. VERDICT pass. Scope: the uncommitted fix-round-1 delta on top of e5499ac (story file only; git diff e5499ac -- argus/ tests/ scripts/ empty). The corrected figures (927 and 698) were independently re-derived by three instruments - wc -l, the NFR-M1 ceiling guard's own _physical_line_count run directly, and git show --numstat plus baseline arithmetic - all agreeing, and confirmed applied everywhere the wrong figures previously appeared (every remaining 915/697 is a struck span or a direct quote, never a live figure). No code moved: argus/, tests/, scripts/candidate_selection.py byte-identical to the prior commit, the three frozen constants (10/50/730) unchanged by direct read. The dev's two self-raised items adjudicated: the checked-box-over-unapplied-fix observation is accurately described and is a minor, self-corrected process deviation, not a deliverable defect; the CRLF repair is fully confirmed - this file re-checked byte-for-byte, 923 lines all CRLF, 0 lone CR/LF anywhere, yaml.safe_load succeeds, 100 entries, STATUS DEFINITIONS intact. Scope containment re-confirmed: MANIFEST_FIELDS = 9, 21 rows / 5 eligible / 14 candidates all eligible_for_n=False, deferred-work.md 0 diff lines against e5499ac. Full suite re-run for real (first attempt used a wrong-repo python3 on PATH, producing spurious grammar-package failures; re-run with this repo's own .venv): 1651 passed, 0 failed, 0 errors, 0 skipped, exit 0, ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1. CI re-confirmed green: gh run view 32270312088, all three legs success over headSha c028da5 (unchanged, no new code commit this round). No new finding; the single iteration-1 Low finding is genuinely resolved.
  15-2-the-detector-and-the-index-agree-on-what-a-line-is: done  # code-review 15-2 (2026-08-19, Sonnet): review -> done. VERDICT pass, iteration 2, on 79a78cf (git diff c66a065..79a78cf; commits be3ff0a code+ledger, bc4bce9 dogfood artifacts, 79a78cf story record). Iteration 1's own re-derivations (index_aligned_lines newline-based by construction, the 324-file sweep, the -137/AC1 contingency) left undisturbed, confirmed by diff. Independently re-verified: full suite 1645 passed exit 0 (236.57s); CI green on bc4bce9 (run 32221995572 ubuntu-latest x 3.10/3.11/3.12, run 32221995581 security shield, both confirmed via gh run view); pipeline.py 1111 / secret_scan.py 575 byte-unchanged over the whole story. All three iteration-1 findings confirmed genuinely resolved: DF-15-2-D well-formed, OPEN, owner XAgent007, target_story NONE, not present in ledger_closed_ids(), story yields zero closure claims; DN-15-2-2's "three pre-existing sites" re-derived independently (test_vacuous_density.py:121, test_vacuous_cross_language.py:203, test_vacuous_detector.py:320) and confirmed exact; zero placeholder (X) markers remain in argus/ or tests/ (only the two legitimate pytest.raises(X) examples survive, byte-identical). Two Low, non-blocking notes recorded in the story's Review Findings, not gating this verdict: the "nine markers resolved" count in the commit message/Completion Notes undercounts by one (actual: 10 touched - 3 numbered + 7 dropped, not 6 - the resolution is more complete than claimed, not less); and deferred-work.md:5205-5206 (the DF-15-2-D entry itself) contains two literal raw CR bytes where escaped `\r`/`\r\n` text was clearly intended - cosmetic only, no guard reads the ledger that way (Path.read_text() universal-newline-normalises). Headroom adjudicated squarely: vacuous_test.py at 1196/1200 (4 lines) is tight and the cost is real, judged correctly deferred rather than a defect in this story - MAINT-001-02/-03 is a loud, unavoidable CI gate backing DF-15-2-D's trigger rather than a comfortable form of words; placing Fix 2's content in the production docstring (not the test file or a new shared module) was the right call under AC2.2 and Sec0.15's prohibition on a new shared module; and a production-module cohesion split has no already-named boundary the way the test module's did, so filing a ledgered trigger rather than inventing a split now is the more disciplined choice. Status review -> done.  # code-review 15-2 (2026-08-19, Sonnet): review -> in-progress. VERDICT concerns, iteration 1, on c66a065 (git diff 72a95ef..c66a065). Independently re-verified by execution: full suite 1645 collected exit 0, mypy 87 files success, index_aligned_lines correctly desynchronises from splitlines() on all eight separators and agrees with it on every all-\n edge case plus a fresh 324-file tracked-tree sweep (0 disagreements); the -137/AC1 claim reproduced from scratch (granting direction does NOT reproduce against shipped code, DOES reproduce under all eight separators once provenance_scan's defensive consumed += 1 line is removed with the pre-fix decomposition, and does NOT reproduce even with that line removed once the fix is applied); secret_scan.py/pipeline.py/deferred-work.md byte-unchanged, no _EXEMPT_BY_DESIGN entry added, CI green (32217374903) independently confirmed via gh. No functional defect found. Three Low-severity documentation/process findings written to the story's Review Findings section as unchecked [Review][Patch] items for the next dev round: (1) production-module headroom (1186/1200) recommended-but-not-recorded as a deferred-work.md entry, (2) DN-15-2-2's rejected alternative recorded only in story-file prose rather than in argus/detectors/vacuous_test.py per this story's own pattern for DN-15-2-1/-3/-4, (3) nine unfilled (X) docstring placeholder markers. # ADDED 2026-08-19 by sprint-change-proposal-2026-08-19.md, APPROVED by XAgent007 on that date. Repairs the LINE-NUMBERING CONTRACT between argus/detectors/vacuous_test.py (which scores over source.splitlines()) and the story 1.4 tree-sitter index (which numbers lines by newline). Measured at HEAD 57946a8 through the REAL index: eight characters survive the production read path at argus/pipeline_stages.py:124 and desynchronise the two views - x0b VT, x0c FF, x1c FS, x1d GS, x1e RS, x85 NEL, u2028 LS, u2029 PS - each shifting the scored span by one line and dropping an assertion off the end. Two form feeds turn a genuine, fully-asserted, MOCK-FREE test into assertion_sites=1 statements=7 density=1/7, below the 1/4 floor: a FALSE ACCUSATION, the class epic 14 spent three stories closing, arriving by a third route. Also makes TC-ArgusAgent-DETECT-001-107 real (it is VACUOUS - lf.splitlines() == crlf.splitlines() is True, so its headline assertion is f(x) == f(x) on a pure function) and strengthens -118 (WEAK - on Windows write_text(newline=None) writes its LF arm as CRLF and its CRLF arm as CR CR LF, so the arm it is named for cannot fail).  # ORDERING, carried as a CONSTRAINT and not as a number: this story must be `done` BEFORE any commit containing Argus output over any epic-15 candidate. Story 15-1 is SELECTION ONLY, so 15-1 is NOT blocked by this and this is NOT blocked by 15-1 - the two may run in either order or concurrently. 15-1 was NOT renumbered: an id here is a citation (stories/15-1-*.md, this file, DF-13-5-A and the epic-13 FINAL retrospective section 14 all name it), and the precedent is story 14.3, appended after 14.2 carrying a 'strictly after 14.2' constraint on its own record.  # PRECONDITION: tests/test_vacuous_detector.py is at 1161 of NFR-M1's 1200 (39 lines, DF-14-3-H, re-pointed at THIS story on 2026-08-19 because 13-5 closed `done` without the split). The COHESION SPLIT comes FIRST, before any case is added - no _EXEMPT_BY_DESIGN entry and no shave, MAINT-001-04's registry may only shrink. argus/pipeline.py at 1111 must not be added to. argus/detectors/vacuous_test.py measured 1113, NOT the 1041 this proposal was briefed with.  # OPEN, deliberately: whether the shifted line view can reach VERDICT-ELIGIBILITY through fact (b) is NOT established in either direction - every reproduction used a fixture with no mock-bound assertions, so the corroboration path was never exercised. Determining it is AC1 of this story, not a claim of the proposal. Severity as MEASURED at HEAD: a false heuristic flag, ADVISORY.  # NOT in scope: DF-14-3-A / -B (COUPLED - fixing A alone converts Go's silence into a language-wide false accusation), DF-14-3-C, and argus/detectors/secret_scan.py, which carries the SAME breach (:334 counts newlines, :447 indexes splitlines()) and is filed as DF-15-2-B.  # create-story 2026-08-19 (Scrum Master): backlog -> ready-for-dev. Story file written to stories/15-2-the-detector-and-the-index-agree-on-what-a-line-is.md (1156 lines), contexted on HEAD 72a95ef. PREMISE RE-MEASUREMENT (AI-E14-8 / AI-E13F-2), run out-of-tree against the shipped modules with git status --porcelain argus tests empty before and after: THE DEFECT SURVIVED INTACT - the form-feed table reproduces byte-for-byte through the real index (3 sites/9 stmts=1/3 not flagged; 2/8=1/4 exactly on the floor; 1/7 FLAGGED; 0/6 FLAGGED), all EIGHT exotic separators desynchronise the two views, and CR / CRLF are confirmed normalised at argus/pipeline_stages.py:124 so they cannot reach the detector. -107's tautology re-executed (lf.splitlines() == crlf.splitlines() is True; its only live assertion is a byte-identical duplicate of -104) and -118's Windows byte counts re-measured exactly (LF arm 11 CRLF / 0 bare CR; CRLF arm 11 CRLF / 11 bare CR). FOUR PREMISES DID NOT SURVIVE and are corrected rather than propagated: (1) argus/detectors/secret_scan.py is 575 lines, NOT the 583 the proposal's section 2.4 states - 575 at HEAD and at 57946a8, so the briefed figure was never measured; (2) tests/test_vacuous_detector.py holds 24 TC-ArgusAgent ids, not DF-14-3-H's 'roughly thirty-five' - 24 is the number the by-execution id inventory must return; (3) DF-15-2-B's 'the scanner reports line 3' is fixture-dependent - a minimal fixture reports 2 - while the mechanism and the wrong-line direction reproduce exactly; (4) the briefed WORKING-TREE list was stale by ten paths, all of them landed by f1ab81c / 57946a8 / 72a95ef, and argus/ and tests/ are both CLEAN (git status --porcelain argus tests empty) - corrected in section 0.17 of the story. TWO NEW MEASURED FACTS the proposal did not have: the audited population contains ZERO of the eight separators (argus/ 87 files, tests/ 120, minions 757, agent-smith 852 with one NEL hit that sits under both node_modules and .next and is excluded by intake), which turns the corpus flag-delta AC into a falsifiable prediction of 0 and 0; and the 'drop one trailing empty' newline decomposition is byte-identical to splitlines() on all 219 tracked .py files plus seven edge cases, which establishes the inertness AC is satisfiable and names the phantom-trailing-element trap. AC1 (whether the corrupted span can reach VERDICT-ELIGIBILITY) is PRESERVED AS OPEN IN BOTH DIRECTIONS as approved: every reproduction returns ast_corroborated=False because the mock-free fixture short-circuits fact (a), so the corroboration path has still never been exercised - and the chain that would make a yes lethal was established by reading (RULE_AST -> depth_supported non-None -> verdict_gate.py:86-96 -> VERDICT-001-30 arm 1 -> NOT_READY_FOR_RELEASE on a DEFAULT run with no flags). The cohesion split is PRESCRIBED (DN-15-2-1: lines 838 -> EOF, the real-tree-sitter-substrate subject the module's own docstring already names) with two rejected boundaries recorded, and where every new case lives is decided before it is written. The structural root cause of -107's vacuity is named (_score takes list[str], so the caller owns the decomposition and the guard re-implemented it) and turned into an AC. The DF-15-2-B scope decision is taken explicitly with the argument for folding it in stated fairly and the price of excluding it recorded. NOTHING was closed, no threshold moved, no epic charter amended; argus/ and tests/ are byte-unchanged and nothing was committed, staged or pushed.  # dev-story 15-2 review-fix 1 (2026-08-19, Opus 5): in-progress -> review. 3 of 3 Low findings from review iteration 1 addressed; DOCUMENTATION AND LEDGER ONLY, no behaviour changed and none needed to - the review found no functional defect and no unmet AC. (1) DF-15-2-D FILED in deferred-work.md (OPEN, owner XAgent007, target_story NONE) for argus/detectors/vacuous_test.py's headroom, the entry this story recommended in prose and never filed (AI-E12-3/AI-E12-6/AC12.9). Append-only; NOTHING disposed of, and DOCS-001-78 re-run against both analyzers to prove it. (2) DN-15-2-2's rejected alternative moved out of story prose into index_aligned_lines' docstring (AC2.2), and its blast-radius rationale recorded as OVERSTATED: only three pre-existing sites hand _score a list they built, one internal line each, no assert changed - the decision stands on decoupling, the number did not (DF-8-5-C class). (3) Nine unfilled (X) markers resolved; provenance_scan.py:764 and test_vacuous_detector.py:348 left alone as legitimate pytest.raises(X) examples; delta swept for other placeholders, none found. COST STATED: vacuous_test.py 1,186 -> 1,196 of 1,200 (headroom 4), no _EXEMPT_BY_DESIGN entry, no AC-required content shaved, and DF-15-2-D carries the new number. pipeline.py 1,111 and secret_scan.py 575 byte-unchanged; DF-15-2-B still excluded; no threshold, corpus, FR34, protocol_cleared or GATE_OUTCOMES touched. Suite 1645 passed exit 0; mypy Success 87 files. Dogfood currency guards re-closed through AI-E12-11's order (be3ff0a -> regenerate -> bc4bce9). CI GREEN on bc4bce9: run 32221995572 success on ubuntu-latest x 3.10/3.11/3.12, run 32221995581 success. Pushed.
  # dev-story 2026-08-19: ready-for-dev -> in-progress -> review. Line-numbering CONTRACT implemented (`index_aligned_lines`, newline-based BY CONSTRUCTION - no separator set, no \x0c special case); run() points at it. Cohesion split FIRST (DN-15-2-1, 838->EOF): test_vacuous_detector.py 1161->791 + new test_vacuous_detector_index.py, id union 24->24 BY EXECUTION with none lost, suite green before any case was added. RED observed on all 24 separator rows before green; every docstring-claimed mutation EXECUTED - and one of the dev's own claims (M4 reddens -137) was measured FALSE and recorded as false, docstring corrected. AC1 determined by execution over 6 layouts x 8 separators x 2 counts: corroboration wrongly WITHHELD reproduces; wrongly GRANTED does NOT reproduce against shipped code - recorded as 'no reproduction found', never 'cannot happen' - and the absence is CONTINGENT: removing only provenance_scan's conservative `consumed += 1` makes the granting direction reproduce under all eight separators, so the margin was one line of defensive coding that nothing had pinned (-137 is that pin). The dev's AC1 prediction ('yes, both directions') was WRONG and is recorded as wrong. -107 REBUILT (was VACUOUS: f(x)==f(x)) and relocated; -118 re-authored with its three load-bearing assertions byte-unchanged and its arm now asserting the NORMALISATION (AC6.5). Corpus flag delta measured both directions, corpora read-only: 0 GAINED / 0 LOST on minions 540/540, agent-smith 334/334, argus 275/275 - the PREDICTED outcome. Inertness proven over all 219 tracked .py files. AC10.3 measured not reasoned: statement counting identical to control in all 16 separator-position cases; stale prose corrected in place. No threshold moved; argus/pipeline.py and argus/detectors/secret_scan.py BYTE-UNCHANGED; no _EXEMPT_BY_DESIGN entry; deferred-work.md NOT touched. Gates: pytest 1645 collected exit 0 (+4 = the four new ids), mypy Success-87, bandit 0H/0M/20L - all unchanged from baseline. Dogfood LOC guards fired (29755->29776) and were closed through AI-E12-11's order: 3acb028 -> regenerate -> e5a9e76. FLAGGED FOR REVIEW: vacuous_test.py ends at 1186/1200 (14 headroom) - the DF-14-3-H condition now on the production module; recommend a ledger entry, no exemption added. CI matrix ESTABLISHED GREEN on e3b9b52: run 32217374903 succeeded on ubuntu-latest x Python 3.10/3.11/3.12, plus security-shield run 32217374974 - so the non-ASCII separators were exercised under a UTF-8 locale and a case-sensitive filesystem, which a Windows-only pass could not establish. AI-E14-2 NOT discharged; DF-13-5-A cited, never executed.
  epic-15-retrospective: done  # KEY CREATED 2026-08-18 BY THE EPIC-14 RETROSPECTIVE - it did not exist, for the same reason epic-14-retrospective did not: epic-15 was created 2026-08-17 by operator decision and the epic plus story 15-1 were added without it. CREATED AT `backlog`, NOT completed - epic 15 has not started and no retrospective has been run for it. Restores the structure epics 1-13 all carry, so that epic-15 cannot later roll up to `done` on the strength of a retrospective nobody ran. See epic-14-retrospective above for why this key is a GATE rather than bookkeeping. It stays blocked until story 15-1 completes, and story 15-1 must in turn consume DF-14-3-D (a TypeScript member contributes ADVISORY findings only, therefore NEVER a data point for the >=80% precision gate) and DF-14-3-C (callback-style JS/TS suites yield zero definitions, so a member size floor must be read against it) when it sizes and selects bench members.  # retrospective epic-15 (2026-08-19): backlog -> done. epic-15-retro-2026-08-19.md written and REGISTERED in tests/test_status_document_registry.py::_STATUS_DOCUMENTS (AI-E12-1 second half, fourth consecutive application) - TC-ArgusAgent-DOCS-001-22 observed RED against the document before the registration line and GREEN after, on the live tree; _status_assertions() and _executed_gate_citations() both return 0 for it, so the registration is INERT rather than load-bearing. Suite re-run this session before the write: 1,651 passed / 0 failed / 0 errors / 0 skipped, exit 0, ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1 - identical to both story records. BOTH STORIES done (15-1, 15-2); +10 test ids re-derived by diffing the id set 57946a8..HEAD (1,188 -> 1,198, zero removed), which reconciles exactly with 1,641 -> 1,651. THE EPIC ASSEMBLED THE BENCH AND DID NOT EVALUATE THE GATE - those are not in tension. 14 candidates from 20 fetched, chosen by criteria frozen as executable code in 16d7100, which is an ancestor of HEAD and touches no manifest and no candidate output (re-verified here by git merge-base --is-ancestor and git show --stat). NOTHING IS RATIFIED: all 14 rows eligible_for_n=False, eligible_member_count() re-measured live = 5, MANIFEST_FIELDS closed at 9, no detector was run over any candidate, nothing was adjudicated, and no threshold / corpus floor / FR34 / protocol_cleared value moved. DF-13-5-A's ONE pre-registered round is UNSPENT - 15.1 selects and never reaches the branch point. THE EPIC'S REAL FINDING, established WITHOUT running Argus over anything: the ratified corpus carries 1 mock-binding/mock-assertion co-occurrence file across 315 Python test files (0.32%) against the bench's 614 across 2,316 (26.5%) - which is WHY the gate read zero. Loose predicate re-derived here: 658 vs 6, a 15x density ratio rather than 83x; direction invariant, magnitude not. ZERO ledger entries dispositioned and deferred-work.md NOT touched by this retrospective; zero planning documents edited (epics.md, prd.md, architecture.md, precision-validation-protocol.md all untouched). 15 action items appended below (AI-E15-1..-15, one record-only). SEVEN SIGNIFICANT-DISCOVERY ALERTS need an OPERATOR decision and are BEYOND the retrospective worker's authority - chiefly SD-1 (protocol section 6 R2 ratification is untaken, so the epic's deliverable is inert), SD-5 (epics.md ENDS AT EPIC 15; the post-R2 work has no container) and SD-6 (argus/detectors/vacuous_test.py has 4 lines of NFR-M1 headroom and tests/test_vacuous_density.py has 41 with NO ledger entry - both splits are preconditions of the round, not afterthoughts). epic-15 itself NOT touched - the roll-up is the orchestrator's step. Nothing committed, staged or pushed.
  epic-16: in-progress  # CREATED 2026-08-20 by sprint-change-proposal-2026-08-20.md, which is AWAITING OPERATOR APPROVAL - filed at `backlog` so the plan has the container the epic-15 retrospective's SD-5 found missing (epics.md ENDED AT EPIC 15, so the post-R2 work had nowhere to live). NOTHING HERE MAY START BEFORE THAT APPROVAL: proposal section 4.3 amends protocol section 5 - in the STRENGTHENING direction only, which is why it is permitted at all - and story 16.4 spends DF-13-5-A's ONE pre-registered round. THE RETROSPECTIVE KEY IS CREATED HERE, WITH THE EPIC: epics 14 and 15 were both created without one and the epic-14 retrospective had to patch that twice; creating it up-front is what stops a third time. The gate is UNCHANGED by this entry - still BLOCKED on an empty denominator, protocol_cleared still False and never True, the >=80% threshold / VALIDATION_SET_FLOOR_N / FR34 / the five ratified members all untouched, all 14 candidate rows still eligible_for_n=False, MANIFEST_FIELDS still closed at 9.  # ✅ APPROVED 2026-08-20 by XAgent007 (Engineering Lead) - a SEPARATE act after the proposal was committed at 7f54506 in the AWAITING state, not a self-approval folded into authoring. The 'AWAITING OPERATOR APPROVAL' wording ABOVE is left unedited (section 3.4) because it was true when written. UNBLOCKED: 16-1, 16-2 and 16-3 may start and may apply the three new protocol section 5 conditions, each DERIVING and recording its own constants. STILL BLOCKED: 16-4, which opens by HALTING on the protocol section 6 R2 operator act - approving this epic is NOT approval to ratify, to fetch, or to spend DF-13-5-A's ONE round. Approval moves no gate: protocol_cleared stays False, the >=80% figure / VALIDATION_SET_FLOOR_N / FR34 / the five ratified members stay untouched, and the three new conditions may only make clearing HARDER.
  # 2026-08-20 create-story: backlog -> in-progress on the standard first-story transition (16-1 contexted). This records that work has STARTED on 16.1/16.2/16.3 under the operator's 2026-08-20 approval; it is NOT a second approval and it does not unblock 16-4, which still opens by HALTING on the protocol section 6 R2 operator act.
  16-1-a-score-drawn-from-one-repository-is-not-a-score: done  # Breadth as a section-5 CONDITION, not a disclosure. The 2026-08-18 population was 31 findings from 2 of 5 members across 1 rule class and the record already computes is_concentrated=True while saying in terms it is 'not a threshold'. Reads the EXISTING computed fields so disclosure and threshold cannot disagree. Constants derived and recorded, never typed. MUST precede any Argus output over a bench member.
  # 2026-08-20 dev-story 16-1 review-fix round (iteration 1 findings): in-progress -> review. The [Med] Patch finding is FIXED, not reworded: the review proved by execution that -55's re-authored `elif not breadth.holds` clause was structurally unreachable (its fixture is the committed record, which carries 5 BORDERLINE rows and is therefore never Exhaustive, so the first clause always fires) and stayed GREEN through both of the reviewer's mutations. The section-5 dispatch mirror is now written ONCE as expected_section_5_outcome() in tests/test_gate_breadth.py, imported by -55, and DRIVEN in BOTH directions over GENERATED populations by a new guard TC-ArgusAgent-PRECISION-001-86 whose breadth term is derived from the fixture's own contributing-member count and the derived floor, never read back out of assess_breadth. The reviewer's two mutations were RE-RUN against it: holds=True -> -86 RED ('1 contributing member(s) against a floor of 3: the preconditions dictate BLOCKED and the live decision recorded CLEARED'); both breadth dispatch branches disabled -> -86 RED (NOT_CLEARED vs BLOCKED); plus holds=False -> -86 RED in the MET direction (BLOCKED vs CLEARED) and deleting the clause from the mirror -> -86 RED. 4 mutations, 4 observed REDs, production restored byte-exact after each (git diff over argus/ empty). -55 no longer claims AC1.5 for the clause its own fixture cannot reach and says so in its docstring, and carries a tripwire assert if the committed record ever becomes exhaustive. The [Low] deferred finding is also RESOLVED by restoration: the undisclosed byte-level edit to the Story-15.x entry at deferred-work.md:5205 is reverted to its 0a6e121 bytes (git diff 0a6e121 over that file is now 121 insertions / 0 deletions - a pure append), the reviewer's ledger entry is left standing and annotated append-only, and the edit is disclosed in the story's File List. NO production code changed this round: argus/ is byte-unchanged, so gate_decision.py stays at 1197/1200 (DF-16-1-B), no artifact was regenerated and no dogfood/README/CHANGELOG figure moved. Gates: ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1 pytest 1658 passed / 0 failed / 0 skipped / 0 errors, exit 0; mypy argus Success 88 files; bandit -r argus --severity-level medium 0 medium 0 high; build_gate_decision.py --check exit 0; build_adjudication_record.py --check exit 0. No threshold, floor, corpus, MANIFEST_FIELDS, protocol version, ratification state or bench member moved; DF-13-5-A's one round is UNSPENT; the BINDING ordering constraint holds. NOT pushed - AC7.4 stays OPEN.
  # 2026-08-20 code-review 16-1 iteration 1 (Sonnet): review -> in-progress. VERDICT fail. Both operator HALTs (rule-class arm deferred to DF-16-1-A; no V1.4 re-version) independently re-verified and CONFIRMED correct by execution - not reopened. Full suite ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1: 1657 passed / 0 failed / 0 skipped, exit 0 (the one red DOCS-001-22 the story left open is green on this tree because the out-of-scope commit 6766df7 registered the foreign document); mypy Success 88 files; bandit 0 medium/high. Hand-mutated and observed RED, then restored clean (git status --porcelain empty before/after every mutation): -80, -81, -82, -83, -84, -85, -58, -54, -56 all correctly go RED; -62 only after regenerating gate-decision-record.json (it reads the static committed artifact, like -55). ONE Medium finding, triaged patch: TC-ArgusAgent-PRECISION-001-55's re-authored breadth clause (AC1.5 / DN-16-1-4) is DEAD CODE and could NOT be driven RED by ANY mutation tried, including disabling both breadth branches in gate_decision.py - its local recomputation always short-circuits on fold.exhaustiveness (5 BORDERLINE rows in the committed record) before reaching the breadth clause, and it reads the static committed artifact rather than calling decide_gate live, so AC1.5's 'drive its new branch to both outcomes' and AC4.1's guard-adequacy obligation are not actually discharged for -55 despite the story's Completion Notes and Tasks list claiming 'BOTH DONE'. Of the three latent traps the review re-checked, -56 and -58 are genuinely re-authored and confirmed RED by execution; -55 is only reworded. One Low finding deferred: an undisclosed byte-level edit to a pre-existing Story-15.x-era deferred-work.md entry (line ~5205), outside this story's declared write set - logged to deferred-work.md rather than fixed here. Findings written to the story file's Review Findings section for the next dev round; -55 needs a generated fixture (reuse the _spread()/_population() pattern already in this PR) that actually exercises both directions of the breadth clause, or an honest docstring/ledger note if the branch is being kept only as forward-looking dead code for 16.4.
  # 2026-08-20 create-story: backlog -> ready-for-dev. Contexted at HEAD 0a6e121 into stories/16-1-a-score-drawn-from-one-repository-is-not-a-score.md. Two things the story file adds that the epic text does not: (a) MEASURED from the shipped code, exactly ONE rule class can reach verdict-eligibility over the repository corpus - vacuous_test.py:1067 is the sole non-None depth_supported site on the corpus-audit path and the Prosecutor's promotion is unreachable because pipeline.py:534 passes no sign_offs - so a rule-class floor above 1 would be a SHUTDOWN rather than a strengthening, and AC2.4 requires a HALT to the operator rather than a silently unclearable gate; (b) two artifact-currency couplings that turn the tree red in files the dev did not edit - a V1.4 protocol change-log row invalidates the committed adjudication record's V1.3 stamp (decide_gate raises; -45 and -63 go red), and a fifth section-5 condition invalidates the committed gate-decision record (-54). Both are resolved by re-running the two builders, neither of which executes a detector or touches a bench candidate. Gate UNCHANGED by contexting: protocol_cleared still False, the >=80% figure / VALIDATION_SET_FLOOR_N / FR34 / the five ratified members untouched, all 14 candidate rows still eligible_for_n=False, MANIFEST_FIELDS still closed at 9, DF-13-5-A's round UNSPENT.
  # 2026-08-20 dev-story 16-1 (Opus 5): ready-for-dev -> in-progress, ⛔ HALTED to the operator at AC2.4 and NOT complete. TWO ESCALATIONS. (1) AC2.4's halt condition is LIVE: §0.2 was reproduced by two independent instruments - an AST walk of all 7 build_recording sites in argus/**+scripts/** (exactly one, vacuous_test.py:1067, passes a non-None depth_supported, and its rule_id is bound to vacuous_test_ast by the same `corroborated` boolean) plus the single production prosecute() call site at argus/pipeline.py:535 (MEASURED :535, not the briefed :534) which supplies no sign_offs so DN-PROMOTE is unreachable; and a direct count over both committed adjudication sets (2026-08-16: 6 rule classes emitted, 1 verdict-eligible; 2026-08-18: 5 emitted, 0 verdict-eligible, 0 blocking). MAXIMUM ACHIEVABLE VERDICT-ELIGIBLE RULE-CLASS COUNT = 1, so a floor of >=2 is a SHUTDOWN and a floor of 1 CANNOT FAIL over any population derive_concentration accepts - both closed by rules this project already holds. The floor is the operator's decision and was NOT taken; four options with their costs are recorded in the story. (2) NOT ANTICIPATED BY §0: the committed adjudication record is JUDGED - 31 human judgements (26 FP / 5 BORDERLINE, XAgent007 (Engineering Lead), 2026-08-17), NOT the 'all UNADJUDICATED, zero human judgements' §0.1 states - so §2.1's authorisation to re-version the protocol over an unjudged record does not hold as written, and a V1.4 row would re-stamp 31 judgements made under V1.3. DERIVED AND RECORDED BUT DELIBERATELY NOT LANDED: the contributing-member floor (VALIDATION_SET_FLOOR_N + 1) // 2 = 3, shown against all five AC2.2 tests with four rejected alternatives - landing half a two-armed condition would take the arm-dropping decision AC2.4 reserves. LANDED (the one task independent of the halted constant, and a latent false green in shipped code today): AC1.3 - decide_gate's conditions[3] positional read replaced by section_5_condition() BY ID with a typed MissingSection5Condition, RECORDED_CLEARED_CONDITION_ID named once, guarded behaviourally AND structurally by TC-ArgusAgent-PRECISION-001-80/-81 in a NEW module tests/test_gate_condition_lookup.py, each observed RED by 3 EXECUTED mutations (M3 exhibits the defect: the positional read publishes adjudication_run_recorded_cleared=True while the condition it names reads FAILED). -70 audited, needs no change (already id-keyed); -55's trap CONFIRMED and recorded, re-authoring deferred WITH the condition rather than asserting over a structurally absent term. GATES: 1,653 collected, 1,651 passed, 2 failed, 0 skipped, 0 errors (ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1); the 2 failures are the AI-E12-11 dogfood LOC-currency guards, PROVED by stash-isolation to be caused only by this story's +66 production lines and resolved by the mandatory commit -> regenerate -> commit order at the real landing; mypy Success 87 files; bandit clean at medium+; BOTH builders --check exit 0 (the gate-decision record is CURRENT - §2.2's coupling was never triggered because SECTION_5_CONDITIONS is still four). NOTHING COMMITTED, staged or pushed - HEAD unmoved at 0a6e121, so there is NO landing sha for 16.4's ancestry guard and NO CI run id yet. NO threshold, corpus, floor, MANIFEST_FIELDS (9), GATE_OUTCOMES (3), CONDITION_VERDICTS (4), protocol_cleared, protocol version (V1.3), ratification state (all 14 candidates still eligible_for_n=False, eligible_member_count() still 5) or committed artifact moved; the gate outcome is BYTE-IDENTICAL. DF-13-5-A's one round is UNSPENT and no ledger entry was dispositioned. NEEDS AN OPERATOR DECISION BEFORE 16.1 CAN RESUME.
  # 2026-08-20 dev-story 16-1 ROUND 2 (Opus 5): in-progress (HALTED) -> review. BOTH HALTS RESOLVED BY OPERATOR DECISION (XAgent007, 2026-08-20), recorded verbatim in the story file as the round's first act. HALT-1: land the contributing-member arm ONLY - protocol section 5 gains a FIFTH condition, appended, requiring the precision ratio to be EVALUABLE only over a population drawn from at least (VALIDATION_SET_FLOOR_N + 1) // 2 = 3 distinct CONTRIBUTING members; below it the precision condition is UNEVALUABLE and the outcome BLOCKED with a countable closure path, while the breadth condition's own verdict is MET/FAILED. The RULE-CLASS arm was NOT landed and is filed as DF-16-1-A with the blocking measurement verbatim (max achievable verdict-eligible rule classes = 1; a floor of >=2 is a shutdown, a floor of 1 cannot fail). HALT-2: NO V1.4 change-log row and NO re-stamp - the amendment sits under the EXISTING V1.3, so the record's 31 human judgements (26 FP / 5 BORDERLINE, 2026-08-17) keep their original provenance, adjudication-record.json was NOT regenerated, and -45/-63 stay green with section 2.1's coupling never armed. The false 'zero human judgements' premise in the story's section 0.1 is corrected append-only, struck not erased. LANDED: argus/precision/gate_breadth.py (new, 436); SECTION_5_CONDITIONS +1 appended; derive_concentration HOISTED so threshold and disclosure are one set of counts; effective evaluability rendered through the EXISTING precision_gate_status_for; the section-5 messages count-derived; the positional read repaired by id in production AND in -62. AC3.5 VERIFIED BY EXECUTION - outcome, outcome_reason, closure_path and the precision gate_status are byte-identical across the amendment; the record gained a fifth condition reading FAILED and nothing else moved. A THIRD latent trap the story had not named was found by execution (-58, whose CLEARED variant runs over the committed 2-member population) and re-authored over a GENERATED breadth-satisfying population; -55 and -56 re-authored as intended behaviour changes; -70 audited, unchanged. New guards TC-ArgusAgent-PRECISION-001-82..-85. 10 executed mutations / 10 observed REDs this round, 13 across both. GATES: 1656 passed / 1 failed / 0 skipped / 0 errors of 1657 - the ONE red is DOCS-001-22 over an UNTRACKED, UNAPPROVED, FOREIGN change-proposal document (sprint-change-proposal-2026-08-20-amendment-A.md) that is in NEITHER of this story's commits, isolated by execution (moving it aside makes the guard pass) and DELIBERATELY NOT absorbed, because registering it would mean committing an unapproved MAJOR proposal; it is green on CI, which does not have the file. mypy Success 88 files; bandit clean; both builders --check exit 0. COMMITS 2ac1078 (code + protocol + architecture + ledger) and 11f40cb (regenerated artifacts) - NEITHER contains Argus output over any bench member, so the BINDING ordering constraint HOLDS and these are the shas 16.4's ancestry guard cites. NOT PUSHED (instructed), so AC7.4's CI run id is recorded OPEN rather than claimed - the only AC not satisfied. DF-16-1-B files gate_decision.py at 1197/1200 with a hard split-first trigger for 16.2. No threshold, corpus, floor, MANIFEST_FIELDS, protocol version, ratification state, adjudication set or bench member moved; DF-13-5-A's one round is UNSPENT.
  16-2-part-of-the-bench-is-sealed-before-anything-is-run: done  # code-review round 1 2026-08-20: review -> done. PASS. Adversarial re-review independent of the dev's transcript: RE-DERIVED the 6/8 partition, the override, PRE_SEAL_MEMBER_IDS and MANIFEST_FIELDS=9 by execution; re-ran git ancestry (zero commits reachable from f89f028038dcd9881204f36bc404267c876b18f7 touch CANDIDATE_OUTPUT_PATHS); re-diffed gate-decision-record.json field-by-field and confirmed AC3.5's byte-identity claim exactly (four keys moved, all this story's own); confirmed D1's circular-import justification for the three-way gate_conditions/gate_evidence/gate_decision split is real, and independently recomputed all twelve touched/adjacent NFR-M1 line counts exactly. CENTRAL ADVERSARIAL TASK: independently mutated the shipped code (PYTHONDONTWRITEBYTECODE=1, cleared __pycache__, git checkout restore after each, git status --porcelain clean throughout) - bisection flip, override deletion, pin-check disable, seal predicate stuck both ways, the seal dispatch branch removed (confirmed decisive), the seal term removed from _precision_condition, the pin check un-hoisted (RED on both -92 and -76), the citation predicate forced both directions, the seal floor forked, the sixth condition id dropped from SECTION_5_CONDITIONS - every one observed RED. Reproduced the dev's own R5/R5b finding: the table-row-flip mutation is GREEN against test_gate_breadth.py and RED against -89, confirming the two guard families are a genuine independent cross-check. Gates re-run independently: 1,667 tests collected (exact match), full suite exit 0 with no observed failures; mypy Success on 91 files; bandit --severity-level medium No issues identified; module-size ceiling green; both builders --check exit 0; byte-unchanged claims spot-checked and confirmed. Two notes recorded, neither actioned: AC7.5's own file count is stale by the final story-record commit's own edits (candidate-output paths still zero either way); sealed-intersect-ratified=empty today is judged an honest, countable strengthening (16.4's R2 act restores reachability), not a repeat of 16.1's HALT-1. Zero decision-needed, zero patch, zero defer findings.  # The gating corpus has NO holdout; only the cartridge corpus does (holdout_vacuous). Pre-committed sha-ordered partition frozen before any output exists, partition as a validated manifest field so the seal is STRUCTURAL, opened once as a recorded act. Partitions - does NOT narrow, drop or re-weight a member. MUST precede any Argus output over a bench member.  # create-story 2026-08-20: backlog -> ready-for-dev, contexted at HEAD 6128466 with the baseline measured GREEN by execution (1,658 tests exit 0, mypy 88 files, bandit clean, both builders --check exit 0). THE RULE IS DERIVED AND ITS SPLIT IS MEASURED: partition = f(pinned commit sha) per row - int(sha,16) % 2 - with a PRIOR-OUTPUT OVERRIDE, giving 6 SEALED / 8 open of the 14 bench candidates (sealed side holds 431 of 614 co-occurrence files; all 6 clear Story 15.1's floor of 10). Set-relative rules (sort-and-alternate) were REJECTED because the ratified set is unknown until 16.4 and a rank-within-set rule silently re-partitions under partial ratification - i.e. it is re-derivable after the fact, which is what 'pre-committed' forbids. The override is LOAD-BEARING, not decorative: xagents-webapp and agent-smith both carry ODD pins and the bisection alone would have declared two already-audited members 'sealed'. THE SHUTDOWN QUESTION 16.1 ESCALATED ON IS ANSWERED BY MEASUREMENT: 16.1's breadth floor is 3 and is invariant under corpus size, six sealed candidates stand against it (slack 3), so the seal does NOT make the breadth condition unsatisfiable - but sealed-and-ratified is EMPTY today, so the countable consequence for 16.4 is recorded up front: R2 must ratify at least 3 of the six named sealed candidates or CLEARED is unreachable. MANIFEST_FIELDS QUESTION ANSWERED BY EXECUTION, NOT ASSUMED: -22 compares dataclasses.fields, so a derived partition property is not a field and the schema stays CLOSED at 9; if anyone judges AC2 to require a tenth field that is an OPERATOR escalation, recorded as such rather than assumed. AC2's 'validated at construction' has a measured subject: a candidate row accepts commit_sha='NOT-A-SHA' today because the sha check sits inside the eligible_for_n branch, and all 21 committed rows already satisfy the check, so hoisting it breaks nothing. DF-16-1-B's SPLIT-FIRST trigger is raised to TASK 1 (gate_decision.py 1,197/1,200; boundary measured by AST at 173 contiguous lines, four importers enumerated, re-export so no import line moves); tests/test_gate_decision.py is at 1,193/1,200 and gets the same rule. Both narrowing designs were PROVED BY EXECUTION to raise (VacuousDecisionError / VacuousDisclosureError), which is why the seal lands as a sixth section-5 CONDITION and not as a filter - it partitions, it does not narrow. Coupled guards enumerated by AST walk into two tiers, with the first looser scan's over-report corrected in place. NOT authorised and NOT taken: no run, no ratification, no fetch, no stage, no adjudication, no V1.4 row (the 31 judgements of 2026-08-17 keep V1.3 provenance), DF-16-1-A's rule-class arm stays unlanded, DF-13-5-A's ONE round stays UNSPENT, and sprint-change-proposal-2026-08-20-amendment-A.md remains registered and UNAPPROVED and out of scope. Nothing was committed, staged or pushed by contexting; argus/, tests/ and scripts/ are byte-unchanged.
  16-3-a-detector-that-finds-nothing-has-not-passed: done  # code-review 16-3 (2026-08-20): PASS, zero decision-needed/patch/defer findings. Every load-bearing claim independently re-derived by execution: Task 1 byte-identity re-extracted and diffed from both commits; the floor's inverse specifically probed (mutated PRECISION_GATE_THRESHOLD itself, floor moved to 4, confirming the general form is real, not a disguised constant); sizes 3/4 driven CLEARED at a checked-out 1ecf618 tree and BLOCKED at HEAD in the same process; AC3's both directions driven directly through decide_gate; the AC2.4 AST guard driven RED against the actual production file (an inserted total_fn reference); the OI1 amendment confirmed to touch protocol line 313 only, byte-for-byte prefix-preserving append; five independent production mutations (floor forked/stuck, holds stuck, dispatch branch removed, precision_evaluable conjunct dropped, seventh id dropped) each observed RED; full suite re-run with bytecode caching disabled - 1673 passed 0 failed, mypy 92 files, bandit clean, ceiling 6 passed, both builders --check 0; 107 development_status keys and the status-definitions header block confirmed preserved. Tree left git status --porcelain clean. review -> done.  # dev-story 16-3 (2026-08-20): 16-3-a-detector-that-finds-nothing-has-not-passed ready-for-dev -> in-progress -> review. Protocol section 5 gained a SEVENTH condition, the YIELD floor: the precision ratio is evaluable only over a verdict-eligible population of at least ceil(q/(q-p)) findings over PRECISION_GATE_THRESHOLD = p/q, which is 5 at the shipped 4/5 - the smallest denominator at which '>=80%' is not silently '100%'. Derived, never typed; stated in the general form because it equals q only when q-p==1 (it is 4 at 5/7 and 5 at 7/9); NOT derived from VALIDATION_SET_FLOOR_N, whose equality is a disclosed coincidence. Premise reproduced first: at HEAD 1ecf618 populations of 3 and 4 findings, all TP, returned CLEARED at 1/1 with all six conditions MET; they are BLOCKED now. Landed in 48e8ea6 (the sha 16.4 cites), after the UNDOCUMENTED split-first trigger on tests/test_gate_decision.py (1191/1200, no ledger entry) was discharged FIRST and alone in 01a2f48; artifacts regenerated in d26ffb6; an unreal guard my OWN mutation run caught was repaired in a0c74ae. THIRD dated block under V1.3 - no V1.4 row, adjudication-record.json byte-unchanged. The OI1 recall bullet at section 5 line 313 amended explicitly, struck-not-erased, saying what the floor is and is not; line 312 and section 7 untouched; the boundary enforced STRUCTURALLY by an AST walk of gate_yield.py. Full suite 1673/0/0/0 exit 0 with ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1 (baseline 1667 + the six new -95..-100), mypy 92 files, bandit clean, both builders --check exit 0, ceiling green with no new exemption; 15 executed mutations all observed RED with the tree restored byte-exact. PRE-ROUND DISCLOSURE carried forward, NOT resolved: the corrected detector's verdict-eligible yield over the gating corpus is 0 of 4284, the only population ever above this floor (31) came from the refuted pre-14 rule and adjudicated 0 TP / 26 FP / 5 BORDERLINE, and the sealed partition's achievable yield is UNMEASURABLE without the forbidden run - so a BLOCKED-on-yield round is the likely outcome and DF-13-5-A already routes it to option (b). A search for a STRUCTURAL cap found none: unmeasured, not impossible. DF-16-3-A filed (tests/test_gate_seal.py at 1145/1200), pure append. AC8.3 OPEN - instructed not to push, so no CI run covers these shas and the local gates are Windows-only. Nothing ratified, fetched, staged or run over any bench member; DF-13-5-A's ONE round UNSPENT; DF-16-1-A stays unlanded; amendment-A stays UNAPPROVED and unacted-on; no ledger entry disposed of; zero candidate-output paths touched, so the BINDING ORDERING CONSTRAINT is intact. No other key moved.  # dev-story 16-3 (2026-08-20): 16-3-a-detector-that-finds-nothing-has-not-passed ready-for-dev -> in-progress. Task 1 (the UNDOCUMENTED SPLIT-FIRST trigger: tests/test_gate_decision.py at 1191/1200 with no ledger entry) taken FIRST and in its own commit. No other key moved.  # create-story 16-3 (2026-08-20): 16-3-a-detector-that-finds-nothing-has-not-passed backlog -> ready-for-dev, contexted at HEAD 1ecf618 with the baseline measured GREEN by execution (1,667 collected exit 0 with ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1, mypy 91 files, bandit clean, both builders --check exit 0, tree clean, 20 ahead / 0 behind). THE PREMISE IS PROVED, NOT ASSERTED: driven through the SHIPPED decide_gate, a population of exactly THREE verdict-eligible findings - one per sealed member, all TP - returns CLEARED today with all six section-5 conditions MET, precision 1/1, and an outcome sentence reading 'Clearing authorises ATTESTED externalization'. Size 4 does too. That is the story title, measured. THE FLOOR IS DERIVED FROM THE ONE LOCKED THRESHOLD, NOT TYPED: ceil(q/(q-p)) over PRECISION_GATE_THRESHOLD = 4/5, i.e. the smallest denominator at which the >=80% gate admits ONE false positive - below 5, '>=80%' is silently a 100% requirement (executed: 0 FPs affordable at d=1..4, 1 at d=5). = 5. Stated in the GENERAL form because it equals the denominator only when q-p==1 and diverges at 5/7 and 7/9 - 16.1's 'strict majority' correction, pre-applied. VACUITY MEASURED FROM THE OTHER SIDE AND THE TWO AGREE: derive_concentration raises on an empty population so a floor of 1 could never fail, and the smallest population passing BOTH breadth and seal is 3, so any floor <=3 is an unreachable branch; 5 fires on exactly the sizes 3 and 4 that were measured wrongly CLEARED. NOT resolved from VALIDATION_SET_FLOOR_N although both are 5 - a disclosed coincidence of two independent locked quantities, not a derivation. THE SHUTDOWN CHECK IS ANSWERED AS FAR AS IT CAN BE, AND ITS LIMIT IS RECORDED UP FRONT: the corrected post-Epic-14 detector's verdict-eligible yield over the gating corpus is ZERO - counted directly, 4,284 findings across all five ratified members, verdict_eligible 0, blocking 0 - and the only population that ever exceeded a yield of 5 (31) came from the REFUTED pre-14 corroboration rule and adjudicated 0 TP / 26 FP. Achievable yield over the sealed partition (6 members, 431 co-occurrence files) is NOT measurable without running the detector, which is exactly the forbidden act, so unlike 16.1 and 16.2 this question cannot be closed by measurement. Recorded as a PRE-ROUND DISCLOSURE rather than a halt, because unmeasured is not impossible-by-construction: a low-yield 16.4 is already DF-13-5-A's pre-registered option (b), and the yield floor is that stopping rule made arithmetic. The dev escalates ONLY if a STRUCTURAL cap bounds yield below the floor. OI1 ANSWERED, NOT PAPERED OVER: recall needs FN, which is unknowable over a corpus with no golden key; this floor has no FN term and no bench-content term - it is a floor on the DENOMINATOR of the precision ratio derived from the THRESHOLD, i.e. a statement about the measurement's resolution, not about what was missed. That holds ONLY because of where the number comes from, so AC2.4 makes it MECHANICALLY CHECKABLE by an AST walk of the new module's own imports and names, and AC2.5 makes the bench-content framing an OPERATOR ESCALATION rather than a story decision. The OI1 bullet amended is section 5's Recall row at protocol line 313, EXPLICITLY and struck-not-erased; line 312 (cartridge corpus) and section 7 (which carries no recall bullet) are NOT touched. AC3 COMPOSITION IS A DRIVEN GUARD IN BOTH DIRECTIONS: 40 findings from 1 sealed member is BLOCKED today and must stay BLOCKED (quantity without breadth still fails), and sizes 3-4 with breadth AND seal MET must become BLOCKED on yield - with a mixed population pinning breadth and seal TRUE so the yield clause is DECISIVE and not lockstep. AN UNDOCUMENTED SPLIT-FIRST TRIGGER WAS FOUND AND IS TASK 1: tests/test_gate_decision.py is at 1,191/1,200 - NINE lines - with NO ledger entry at all (16.2 flagged it at 1,193, split only the production module, and filed no DF-16-2-* entry), and the edit is unavoidable because the section-5 dispatch mirror takes REQUIRED keyword terms so a seventh condition forces every caller to be edited; the cohesion boundary is measured by AST at -59..-64 (353 contiguous lines, the artifact guards) and must be re-confirmed by the dev. Guards go in a NEW tests/test_gate_yield.py at TC-ArgusAgent-PRECISION-001-95.., because test_gate_seal.py has 65 lines and shaving a file to fit is what MAINT-001's remedy forbids. NOT authorised and NOT taken: no run, no ratification, no fetch, no stage, no adjudication, no V1.4 row (the 31 judgements of 2026-08-17 keep V1.3 provenance - this is the THIRD dated block under V1.3), DF-16-1-A's rule-class arm stays unlanded, DF-13-5-A's ONE round stays UNSPENT, 16.4 is NOT contexted or started, and sprint-change-proposal-2026-08-20-amendment-A.md remains registered and UNAPPROVED and out of scope. Nothing was committed, staged or pushed by contexting; argus/, tests/ and scripts/ are byte-unchanged. No other key moved.  # UNEVALUABLE closed the EMPTY-denominator hole; a TINY one clearing at 100% is still open. Yield floor as a section-5 condition over a bench selected BECAUSE it carries the defect class. Amends the OI1 recall-is-diagnostic bullet EXPLICITLY, struck-not-erased, rather than contradicting it in passing. Composes with 16.1 - quantity without breadth still fails. MUST precede any Argus output over a bench member.
  16-4-ratify-run-adjudicate-and-let-the-arithmetic-decide: done  # CLOSED BY DECISION 2026-08-22, not by result: HALT-1 declined for now, HALT-2 moot, HALT-3 resolved 2026-08-21. Nothing ratified (N still 5), nothing fetched, no detector run over a bench member, no disposition written, no protocol edit. DF-13-5-A OPEN and UNSPENT with a re-review trigger (16.6+16.7 done, or 2026-11-22). Tasks 3-8 never authorised and left unchecked.  # 2026-08-21 dev-story 16-4 (Opus 5): ready-for-dev -> in-progress, and STOPPED AT TASK 1 exactly as the story requires - AC1.4 calls that the story succeeding, not stalling. NOTHING was ratified, fetched, run over a bench member, or dispositioned; Tasks 3-8 untouched. THREE halts now stand, not two. HALT-1 (section 6 R2 ratification + fetch) and HALT-2 (record scoping) were raised with re-executed figures and NOT chosen, and no plumbing was written for either arm. HALT-3 IS NEW AND WAS FOUND BY EXECUTION: section 0.4 says FOUR shipped guards go RED when N moves; the MEASURED number is THIRTEEN. Measured by a fully-reverted three-row flip (full suite, PYTHONDONTWRITEBYTECODE=1, git checkout restore, porcelain clean, nothing committed, no detector run) - so it is the guard-breakage measurement Task 0 demands, not the ratification AC1.2 forbids. Nine of the thirteen are unnamed by section 0.4 (-79, -54, -59, -89, -25, -31, DOGFOOD-001-53, DOGFOOD-001-54, DOCS-001-75), and for the two it does name (-76, -78) the assertion that fires FIRST is the pre-registered 12-20 CANDIDATE BAND, not the eligible_member_count() == 5 literal section 0.4 cites - so amending the N literal alone leaves both red. THE MECHANISM: bench_candidates() folds on `not eligible_for_n AND 'candidate' in ineligible_reason`, the exact two fields AC1.1 says ratification edits, so ratifying three members drops the candidate population 14 -> 11, breaching DF-13-5-A's OWN pre-registered band and breaking SEALED_PARTITION_TABLE's both-directions equality with it. AC7.1 IS THEREFORE INTERNALLY INCONSISTENT WITH RATIFICATION: DOCS-001-75 requires prd.md to state the LIVE eligible-member count ('Ratify a sixth member and this goes red until the PRD is updated - which is the whole point') and -89 requires the frozen table to track bench_candidates(), while AC7.1 declares BOTH byte-unchanged. No guard was loosened and none was touched. Section 0.2's central premise SURVIVED re-execution exactly (3/16 appended vs 1/1 superseding over an identical six-TP population); two further premises were corrected - minions carries 12 dirty entries not 10 (immaterial), and arm A is evaluable=True ONLY because the new run's emitted population excludes the old run's five BORDERLINE findings, so its exact shape is exhaustiveness judged over 6 findings with the ratio computed over 32. TASK 2 LANDED at 3a99eed: tests/test_gate_ordering.py (new, 275 lines, TC-ArgusAgent-PRECISION-001-101/-102) proves from the object database that 16.1/16.2/16.3 precede this story's first output commit and that none touches a candidate-output path, with constants IMPORTED, three non-vacuity preconditions, ancestry driven both ways, and a GENERATED SEAL..HEAD population whose count is asserted. A bug in that guard was caught BY EXECUTION (--is-ancestor is reflexive and rev-list A..HEAD includes HEAD) and repaired by stating the reflexive case, never by softening the assertion; four executed mutations, four observed REDs, sha256 restored identically after each. tests/test_gate_seal.py byte-unchanged at 1,145 - DF-16-3-A's 55 lines intact. Gates: suite 1675 collected exit 0 / 0 skipped, mypy 92 files, bandit clean over 25,419 LOC, both builders --check exit 0, ceiling green with no new exemption. CI run id OPEN - nothing pushed, and the local gates are Windows-only. || 2026-08-21 HALT-3 REFACTOR LANDED at f1e3be8 under the operator's 'Yes, proceed' - read as HALT-3 ONLY and recorded as such; it is NOT a section 6 R2 authorisation, nothing was ratified, N is still 5, Tasks 3-8 unstarted. BENCH_MEMBER_IDS + BENCH_COMMIT_SHA (c028da5, MEASURED to be three commits AFTER CRITERIA_COMMIT_SHA where the manifest held only 7 rows) make bench membership HISTORICAL and CLOSED, modelled on PRE_SEAL_MEMBER_IDS; unratified_bench_candidates() splits the LIVE pending state from the frozen bench. A frozen constant rather than a manifest column, so MANIFEST_FIELDS stays 9 per AC6.2, and SEALED_PARTITION_TABLE is byte-unchanged. This COMPLETES 16.2's own stated intent: 16.2 froze the PARTITION to survive R2 while membership still keyed on the two fields R2 edits. AR7 finding not in section 0.4: test_candidate_selection.py carried a VERBATIM FORK of the old predicate and now delegates. New guards TC-ArgusAgent-PRECISION-001-103 (re-derives BENCH_MEMBER_IDS by AST-parsing the manifest as read out of git at BENCH_COMMIT_SHA under the ORIGINAL predicate, plus the criteria -> bench -> seal ordering) and -104 (applies R2's two edits at run time and asserts the bench does not move). Four mutations, four observed REDs, manifest restored byte-identical after each; M8 restores the pre-16.4 predicate and reddens -104 with 'ratifying the bench CHANGED it: 14 -> 0'. A portability bug in this story's OWN Task 2 code was caught by execution: _git() used text=True, which decodes with the LOCALE codec (cp1252 here), so -103 died on a UTF-8 blob and would have PASSED on the ubuntu CI leg - the usual Windows/POSIX asymmetry REVERSED. THE DEV OVER-CLAIMED AND IT IS RECORDED AS WRONG: predicted 13 -> ~9 breakages, re-measured 13 -> 12, only -89 green. That one still removes ONE of AC7.1's two outright contradictions (SEALED_PARTITION_TABLE no longer has to move); prd.md / DOCS-001-75 STANDS UNRESOLVED and is the operator's. -76/-78/-79 assert over the PENDING population by design and re-pointing them is the dated amendment section 0.4 reserves to the story carrying ratification authority - deliberately NOT taken. Behaviour preservation asserted by execution: bench 14, unratified 14, N 5, both builders --check exit 0, git diff over validation-corpus/ EMPTY. Gates: 1677 collected exit 0 / 0 skipped, mypy 92 files, bandit clean, ceiling green with no new exemption. || DF-13-5-A's round remains UNSPENT; DF-16-1-A unlanded; amendment-A UNAPPROVED; the seal unopened; protocol_cleared still False.  # NOT AUTONOMOUS. Opens by HALTING on the protocol section 6 R2 operator act (ratification and third-party fetch are not autonomous acts) and must not be driven to completion without the operator in the loop. Carries the git-ancestry guard proving 16.1/16.2/16.3 precede its first output commit. CONSUMES DF-13-5-A's ONE round and records the pre-registered fallback verbatim: if the outcome is UNEVALUABLE or below threshold, the answer is a better detector, NOT a bigger bench - and this story proposes no further expansion. || create-story (2026-08-20): contexted at HEAD 3022415 on a clean tree, baseline measured GREEN by execution (1673 collected, exit 0, 0 failed, 0 skipped; mypy 92 files; bandit clean; both builders --check exit 0). backlog -> ready-for-dev. THE STORY IS STILL NOT AUTHORISED PAST TASK 1 - contexting a story is not approving its acts. TWO HALTS are filed for the operator, not one: HALT-1 is the section 6 R2 ratification and fetch (sealed AND ratified is empty; six sealed candidates named; >=3 must be ratified AND contribute, and >=5 verdict-eligible findings must survive adjudication; no candidate checkout exists on this machine). HALT-2 is NEW and was found by execution: section 5's breadth, seal and yield arms AND the precision ratio are all folded over the LIVE ROWS of adjudication-record.json rather than over the run's emitted population, and the two have already drifted apart - so an identical six-finding, six-TP round folds to 3/16 (NOT_CLEARED, evaluable=True) if appended to the committed record and to 1/1 if written to a fresh superseding one. Appending folds 26 FPs from the corroboration rule Epic 14 REFUTED into this round's denominator; superseding removes them and brushes against section 5's 'no change that makes clearing easier'. Both arms are defensible, neither is a dev's call, and no plumbing for either may be written before the operator answers. Also recorded: the four shipped guards that go RED the moment N moves (-76, -78, -82, -92), each CORRECT as written and to be amended by dated authorisation rather than loosened; PRE_SEAL_MEMBER_IDS must NOT absorb this story's adjudication set, which would declare the newly-audited members pre-seal and destroy the seal condition in the act of satisfying it; no V1.4 row (the fourth application of the 2026-08-20 operator decision, and section 4 forbids amending DURING a run); and 13.5's escalation E1 is CLOSED - minions has drifted further off its pin and it does not matter, because the runner reads the pinned git object. DF-13-5-A's round is UNSPENT at contexting; DF-16-1-A stays unlanded; amendment-A stays UNAPPROVED; the seal stays closed.
  16-5-the-record-says-who-judged-and-whether-they-were-independent: done  # 2026-08-23 dev-story (review fix round 1): in-progress -> review. BOTH review findings resolved, 2 of 2; none rebutted, none deferred, no guard weakened. argus/** and scripts/** are BYTE-UNCHANGED by this round (git diff HEAD -- argus/ scripts/ empty), so section 2.5's artifact-currency order is NOT re-armed and gate_decision.py stays at 1132. FINDING 1 (High, AC6.4 append-only): the literal CR byte 9aea1be dropped from the historical deferred-work.md:5257 entry is RESTORED - 16.1's remedy, restoration, not an amended guard. THE AUDIT WENT PAST THE ONE BYTE: a byte-level difflib alignment of EVERY blob in the arc (52143eb -> 9aea1be -> c5ca6a7 -> cd4cbe4 -> ca0dee2 -> 927548d -> 028c3c8) plus a whole-file baseline comparison found EXACTLY ONE change in the entire arc that modified a pre-existing line - that CR. Every other change is a pure INSERT (22 lines: the DF-15-2-D status: field; 45 lines: DF-16-5-A), and four of the six commits did not touch the file at all. Proven twice: the alignment reports HISTORICAL LINES DELETED OR REPLACED: 0, and the 424300-byte baseline was RECONSTRUCTED BYTE-EXACTLY by removing only the inserted ranges. CR count restored to 1, matching 52143eb. ROOT CAUSE, and it is a trap rather than a slip: core.autocrlf=true with no .gitattributes, and A LONE CR MAKES GIT CLASSIFY THE FILE AS BINARY (convert_is_binary returns 1 on lonecr), silently disabling CRLF normalisation - so an editor rewriting the file as CRLF consumed the lone CR, after which git resumed normalising and booked one net deletion. The fix's own first attempt hit the same trap in reverse (restoring the CR into a CRLF worktree would have staged 5643 CRLF terminators into the blob), so the restoration was rebuilt FROM THE BLOB; worktree bytes == staged blob bytes asserted. Also restored the EOF newline 927548d dropped - not a historical-byte edit (the old content stayed a strict prefix) but a POSIX text-file violation on an ubuntu CI matrix - reported here rather than absorbed silently. TC-ArgusAgent-DOCS-001-78 and ledger_closed_ids UNAMENDED AND UNWEAKENED: the guard was right, the record was wrong. FINDING 2 (Medium, AC1.3/AC5.4 guard adequacy): NEW TC-ArgusAgent-PRECISION-001-114 with a _superseded_population builder extending _population (every pinned term inherited, section 2.8 lockstep preserved), striking one row via a real supersedes correction whose author differs IN ROLE from the live replacement. 2 GENERATED adversarial variants, one per registered non-Engineering-Lead role, each forging a DIFFERENT status - exactly the 'quietly upgrading a NOT_INDEPENDENT record' leak the review named. PROVEN NON-VACUOUS BY THE REVIEWER'S OWN MUTATION: gate_decision.py:856 live_rows() -> rows drives BOTH -114 cases RED while every other guard in test_gate_independence.py, test_gate_decision.py, test_adjudication_record.py and test_gate_decision_artifact.py stays GREEN - so the guard is targeted at exactly the defect that previously escaped. Run with PYTHONDONTWRITEBYTECODE=1 and cleared __pycache__, RESTORED IN THE SAME COMMAND so the tree could never be left mutated, git status --porcelain empty after. _decide gained one defaulted per_finding keyword (a correction shares a finding_id with the row it strikes; a real emitted population is per finding, never per row) - every pre-existing caller byte-identical. DF-16-5-B FILED as a pure append: tests/test_gate_independence.py 962 -> 1127, crossing section 0.5's 1100-1150 filing band - not split (< 1150) and NOT SHAVED, since the -114 docstring carries the GUARD-ADEQUACY content the clause mandates. DF-16-5-A neither closed nor amended. Re-verified after the fix: SECTION_5_CONDITIONS still 7, precision_evaluable still 4 conjuncts, no V1.4 row, adjudication-record.json byte-unchanged, gate_breadth.py:366-368 byte-as-shipped, no _EXEMPT_BY_DESIGN entry added, nothing ratified, no detector run, no disposition written, no role filled, DF-13-5-A OPEN and UNSPENT. GATES: full suite 1688 passed exit 0 with ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1 (1686 + the 2 new -114 cases); mypy argus 94 files clean; bandit 0 medium 0 high; module-size ceiling 6 passed; build_gate_decision.py --check exit 0; build_adjudication_record.py --check exit 0.  # 2026-08-23 code-review (Sonnet 5, iteration 1): review -> in-progress. VERDICT FAIL - two Patch findings written to the story's Review Findings section: (1) High - commit 9aea1be (the Task-0 baseline-repair commit, the story's own flagged highest-value review target) silently dropped a literal \r byte from a historical, pre-16.5 deferred-work.md entry while its own commit message and new status: field claim 'not one byte of the entry above it is edited' - a real AC6.4 append-only violation the diff itself falsifies (24 insertions(+), 1 deletion(-)). (2) Medium - no generated population in tests/test_gate_independence.py exercises a superseded row, so nothing in the targeted guard surface would catch decide_gate deriving independence from all rows instead of live rows only (record.live_rows() vs record.rows mutation produced zero test failures across the targeted surface) - a guard-adequacy gap against AC5.4, not a live mis-derivation (0 superseded rows on the committed record today). Everything else independently re-verified GREEN: full suite 1686/1686 exit 0, mypy 94 files clean, bandit 0 medium/high, both builders --check exit 0, ceiling guard green, AC7.1 must-not-move list byte-unchanged, AC7.1a budget exactly +4/+3/+3/+3 with no gate_independence import in the four forwarders, gate_breadth.py:366-368 byte-as-shipped, protocol edit +57/-0 under V1.3 with no V1.4 row, gate_decision.py 1132 with DF-16-5-A filed not split, README/CHANGELOG module-figure claims verified against a real build. Four mutation spot-checks run and restored (git status --porcelain clean after each): a fifth precision_evaluable conjunct - RED; the raising vocabulary lookup replaced by a silent default - RED; the byte-identical-when-omitted default widened to inject text - RED; record.live_rows() -> record.rows - RED on nothing (finding 2). Nothing operator-owned touched: no ratification, no detector run, no disposition written, no role filled, no V1.4 row, seal unopened, DF-13-5-A still OPEN and UNSPENT.  # 2026-08-23 dev-story: in-progress -> review. DERIVED ANSWER OVER THE LIVE RECORD IS NOT_INDEPENDENT (31 of 31 live human judgements by 'XAgent007 (Engineering Lead)') and it now rides ON the gate_status sentence itself, after the precision figure, in all three renderer branches and on all four renderers precision_gate_status can return, plus a structured 'independence' payload block. NEW argus/precision/gate_independence.py (328) and tests/test_gate_independence.py (962, -105..-112) plus -113 in test_gate_decision_artifact.py. NOTHING IS GATED, proven by execution: SECTION_5_CONDITIONS still 7, precision_evaluable still 4 conjuncts, and flipping the status through all three non-empty members leaves outcome, reason, all seven verdicts, precision_evaluable, meets_threshold and the closure path byte-identical over both an otherwise-CLEARED and a BLOCKED population. AC7.1a budget held exactly (+4/+3/+3/+3, ZERO deletions) and NFR-P1 proven BY RENDERING - 26 surfaces, sha256 27dde086258766f5, byte-identical against the pre-story tree. TEN mutations all RED at the real seam, tree restored byte-exact after each. gate_decision.py 1084 -> 1132 landed in the 1100-1150 band so DF-16-5-A was FILED, not split; no _EXEMPT_BY_DESIGN entry added. gate_breadth.py:366-368 left byte-as-shipped per DN-16-5-7 (correction declined by operator). Nothing ratified, no detector run, no disposition written, no role filled, no threshold moved, no V1.4 row, adjudication-record.json byte-unchanged, DF-13-5-A OPEN and UNSPENT. GATES: full suite 1686 passed exit 0 with ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1; mypy argus 94 files clean; bandit 0 medium 0 high; build_gate_decision.py --check exit 0; build_adjudication_record.py --check exit 0; module-size ceiling green.  # 2026-08-23 dev-story: ready-for-dev -> in-progress at HEAD 52143eb. Task 0 re-derived every SS0 row EXACT (31 rows, 26 FP / 5 BORDERLINE, ONE distinct adjudicator, V1.3, both builders --check exit 0, all 21 line counts exact) and FOUND ONE FALSE PREMISE: the baseline suite was RED, not green. TC-ArgusAgent-DOCS-001-78 failed on the story file and its round-1 validation report. Two governance-record defects, both repaired without touching the guard: (1) DF-15-2-D's real, dated, evidenced 2026-08-22 closure was written in a shape ledger_closed_ids cannot see, so a TRUE story claim read as unbacked - repaired by a PURE APPEND of the machine-readable status field, closing nothing new; (2) the round-1 report put four OPEN ids and one CLOSED id on one physical line and the analyzer is line-scoped BY DESIGN, so the row was split with every word preserved.  # 2026-08-22 create-story: backlog -> ready-for-dev. Contexted at HEAD 52143eb into stories/16-5-the-record-says-who-judged-and-whether-they-were-independent.md. THE EPIC TEXT IS STALE ON THIS STORY'S FIRST PREMISE and the story says so in section 0.2: epics.md:3191 opens 'section 2's QA Lead and External adjudicator are both unfilled', which was true on 2026-08-20 and is FALSE now - commit 1bb7088 (2026-08-22) filled the QA Lead with Veer Pratap Singh. The consequence the story carries forward is that FILLED and JUDGED are now different facts: the QA Lead is filled and has authored ZERO dispositions, so a rendered 'QA Lead: absent' would be read as 'unfilled' and would be false - the DF-9-2-B false-subject class on the surface that publishes the gate (DN-16-5-4). MEASURED for the story rather than assumed: GateDecision.adjudicators has been computed and published since 13.3 and is read by NOTHING - zero assertions over it anywhere in tests/ or scripts/ - so the derivation source already exists and is simply unguarded; over the live committed record all 31 live rows carry one adjudicator, 'XAgent007 (Engineering Lead)', so the derived answer is NOT_INDEPENDENT and that is the story's expected output, not a failure. THE ONE WAY THIS STORY IS UNLIKE 16.1/16.2/16.3, flagged because the dev will pattern-match: it adds NO eighth section 5 condition. SECTION_5_CONDITIONS stays at SEVEN byte-unchanged, precision_evaluable keeps exactly four conjuncts, and AC4.3 proves inertness by driving the status through every vocabulary member over both a CLEARED and the live BLOCKED population. Nothing ratified, no detector run, no disposition written; DF-13-5-A stays OPEN and UNSPENT.  # 2026-08-23 validate + create-story (AMENDMENT MODE): value UNCHANGED at ready-for-dev - no transition, and none is implied. Story-readiness validation returned FAIL with 3 BLOCKING + 4 minor findings, ALL in the acceptance criteria; every section 0 numeric premise was re-derived off the tree and measured CLEAN, so the story was AMENDED IN PLACE (7 targeted edits), never re-contexted or rewritten. B-1: AC2.3 was UNSATISFIABLE inside AC7.1's fence - none of its four renderers renders in gate_decision.py (they are precision_gate_status_for calls at adjudication.py:963, gate_breadth.py:381, gate_seal.py:698, gate_yield.py:493) and the three arm renderers short-circuit on 'return fold.gate_status', so a keyword on precision_gate_status_for alone reaches NONE of them; those four modules moved AC7.1 -> AC7.2 under a NEW AC7.1a 'one forwarded keyword, default byte-identical' budget, and Task 1's headroom projection now covers them (973/777/560/436 against 1200 - NO new NFR-M1 trigger; gate_decision.py at 1084 is still the only one to watch). B-2: AC4.3 contradicted AC5.3 - section 5(4) _recorded_cleared_condition ALREADY takes adjudicators and fails on an empty set, so NOT_ESTABLISHED necessarily moves the outcome; the sweep is now scoped to the NON-EMPTY members and the pre-existing coupling is recorded as a FOUND FACT. B-3: the motivating claim 'Nothing in the repository reads it' measured FALSE (section 5(4) reads it and renders the names at gate_decision.py:665); reframed to 'published and UNGUARDED BY ANY TEST' - the narrower zero-assertions-in-tests/scripts claim IS true - and _recorded_cleared_condition added to section 0.7. Minors: artifact root qualified once in section 0.0 (validation-corpus/ is under _bmad-output/design-artifacts/ArgusAgent/, NOT the repo root, so Task 0's first command would have ENOENTed); replay_harness.py:788's registry_module() indirection marked DELIBERATE and out of scope so it is not misread as a DF-9-2-A defect; epics.md cite tightened :3191 (heading) -> :3199 (the 'both unfilled' Given); AC3.1's no-second-renderer walk given its exclusion set (the 'precision=' surface is the predicate; ConditionResult.measured and the three *_blocked_reason renderers are OUT). NO DN-* decision overturned - DN-16-5-4 and DN-16-5-5 in particular stand unchanged; SECTION_5_CONDITIONS still SEVEN; precision_evaluable still four conjuncts; scope unchanged; no argus/, tests/ or scripts/ file touched.  # AMENDED 2026-08-23 create-story (amendment mode, 2ND PASS) after round-2 validation returned CONCERNS (stories/16-5-...-validation-2026-08-23-round-2.md). STATUS VALUE UNCHANGED: ready-for-dev. SIX targeted edits, no re-contexting, no re-research, no scope change. Round 2 confirmed all THREE round-1 blockers genuinely resolved and re-derived every line projection EXACT; none of that was disturbed. C-1: NEW DN-16-5-7, OPERATOR-AUTHORISED, recording BOTH halves - DN-16-1-1's stated rationale ('a signature shared with the cartridge path', shipped in argus/precision/gate_breadth.py:366-368) is FALSE as applied to the fold, re-verified: fold_adjudicated_precision has EXACTLY ONE production caller (gate_decision.py:811) and replay_harness.compute_precision returns PrecisionResult (replay_harness.py:386) without calling it, the two paths sharing precision_fraction (:291) and precision_gate_status_for (:729) instead - WHILE DN-16-1-1's HOLDING ('the fold is NOT forked') STANDS UNREVERSED, since one inert keyword with a byte-identical default is not a fork. Cross-referenced from the 'cites rather than reopens' list and carved out of AC7.4 so a dev meeting the shipped docstring at Task 4 knows the conflict is already adjudicated instead of stopping. gate_breadth.py:366-368 DELIBERATELY LEFT AS SHIPPED - correcting it was considered and DECLINED by the operator; DN-16-5-7 lives in the story only. C-2: AC4.3 limb 2 (and its AC5.3 repeat) ordered a MEASURABLY WRONG mechanism into a guard docstring as a 'found fact' - AdjudicationRow.__post_init__ (adjudication.py:376-377) refuses any TP/FP/BORDERLINE row without a registered adjudicator, so an empty adjudicator set implies ALL live rows UNADJUDICATED, making the record NON-EXHAUSTIVE, so decide_gate blocks at :956 on EXHAUSTIVENESS AHEAD of the empty-denominator branch at :977 the AC named; cite :1021 -> :1022 (:1021 is closure = yield_closure_path(...)). AC4.3's scoping decision is correct and UNTOUCHED. L-1: section 0.5 prose 640 -> 560 (gate_yield.py's HEADROOM had been transcribed as its line count; the table row and Task 1 were already right). L-2: Task 4 + AC7.2 now state the live/adjudicators derivation at :820-823 must MOVE ABOVE the fold call at :811 - a re-order, not a one-line forward. L-3: section 0.0 / Task 0 working-tree expectation now reads 'plus this story's validation reports' (FOUR entries, not two). L-4: Task 4's byte-identity confirmation list gained the three direct precision_gate_status_for callers - tests/test_gate_flip_path.py, tests/test_precision_replay.py, tests/test_validation_corpus.py. NO DN-* overturned; DN-16-5-4 and DN-16-5-5 remain LOCKED and untouched; SECTION_5_CONDITIONS still SEVEN with no eighth condition; precision_evaluable still FOUR conjuncts; scope unchanged; NO argus/, tests/ or scripts/ file touched. # 2026-08-23 code-review (Sonnet 5, iteration 2): review -> done. VERDICT PASS - both iteration-1 findings independently re-verified resolved, not merely re-asserted. Finding 1 (AC6.4): re-ran a byte-level difflib alignment of every blob in the arc (52143eb -> 9aea1be -> c5ca6a7 -> cd4cbe4 -> ca0dee2 -> 927548d -> 028c3c8 -> a4de7e7 -> ca3853e -> 5f194da) independently and confirmed the ONLY pre-existing-line edit in the whole arc is the CR at deferred-work.md:5257 (9aea1be), restored by a4de7e7 together with the EOF newline 927548d dropped; HEAD carries exactly 1 lone CR matching 52143eb's exactly 1; a4de7e7 touches only deferred-work.md; TC-ArgusAgent-DOCS-001-78/ledger_closed_ids untouched by the fix round. Finding 2 (AC1.3/AC5.4): mutated gate_decision.py:856 (live_rows() -> rows) myself and ran the four targeted files (42 tests) - exactly 2 failures, both -114 parametrised cases, all 40 others green; restored via git checkout --, git status --porcelain empty; _superseded_population confirmed to genuinely inherit _population's pinned terms (built via _population(...) plus one struck row + one correction row, not a fork). Also verified: DF-16-5-B filed for test_gate_independence.py 962 -> 1,127 (in-band, not split); gate_decision.py still 1,132 and argus/ byte-unchanged across the fix round; zero stray lone CRs in any of the four touched files, all end with newline; sprint-status.yaml parses UTF-8 YAML with 109 development_status keys and STATUS DEFINITIONS intact; gate-decision-record.json independence block shows roles_present=[Engineering Lead] only, gates_anything=False, N=5; adjudication-record.json and gate-decision-record.json byte-unchanged by the fix round; DF-13-5-A still OPEN and UNSPENT; no V1.4 row, no role filled, no detector run. GATES re-run independently: full suite 1688 passed exit 0 with ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1; mypy argus 94 files clean; bandit 0 medium/high; module-size ceiling 6 passed; both builders --check exit 0.
  16-6-the-assertion-vocabulary-recognises-the-assertion-that-is-a-raise: done  # 2026-08-23 code-review (Sonnet 5): CONCERNS; review -> in-progress. Independently re-derived by execution rather than trusted: full suite 1,695 passed/0 failed/exit 0, mypy clean 94 files, bandit 0 medium/0 high, ceiling 6 passed, governance-record integrity green, both builders --check exit 0, CORPUS artifacts byte-unchanged (git diff empty), DOGFOOD regeneration re-run and confirmed renderer-produced (script refuses dirty argus/, re-read equals write), DF-16-6-B byte checks all hold (lone-CR 1->1, +207/-0, HEAD-blob strict prefix). The -7 delta was RE-DERIVED FROM SCRATCH by materializing every one of the 1,032 pinned findings from their five corpus checkouts' pinned shas and re-scoring under both tables: exact match, all seven named rows to the Fraction, 0 newly flagged. The "structurally impossible" newly-flagged claim verified by reading _score's arithmetic (assertion_sites monotonically non-decreasing, mock_ratio/call_sites unaffected by the wide table, so heuristically_vacuous cannot flip False->True). `pytest.raises(AssertionError)` non-edge claim reproduced live (edges are ['raises','check'], no AssertionError edge). Both mutations (remove the name; case-insensitive convention) independently re-run: 6/7 RED then 3/7 RED exactly as claimed, tree restored byte-exact both times, porcelain byte-identical before/after. The cp1252 TypeError in tests/test_gate_seal.py::_git was reproduced by execution in an isolated scratch repo (UnicodeDecodeError in the reader thread -> stdout None -> cites_partition(None) raises TypeError) and confirmed real, Windows-only, and untripped by this story's three (pure-ASCII, verified) commit messages - correctly left unfixed as out of scope. The Evidence-partition:open citation for a pre-seal-only corpus is a genuine disclosed protocol gap (SEAL_CITATION_VALUES has no pre-seal member) that does not corrupt any computed state (corpus_partition_counts is independent of commit trailers) - deferred to the seal-protocol owner, not a defect. ONE finding withheld status: Completion Notes' gate table misreports "gate_* 36 passed" where an independent re-run of tests/test_gate_*.py collects and passes 58 (breadth 5 + condition_lookup 2 + decision 8 + decision_artifact 7 + flip_path 7 + independence 10 + ordering 4 + seal 9 + yield 6), none of those files touched by this story - a reporting-accuracy miscount (same shape as the already-known 182-vs-109 sprint-status miscount), not a functional defect; AC3.3's discharge is unaffected since the suite is green either way. Findings written to the story file's Review Findings subsection: one [Review][Patch] (the 36-vs-58 miscount, trivial one-line text fix) and two [Review][Defer] (the trailer-citation gap; the cp1252 guard bug). No decision-needed, no High/Medium severity issue, no failing test, no unmet AC.  # 2026-08-23 dev-story (Opus 5): IMPLEMENTED; in-progress -> review. ONE NAME: "AssertionError" added to _ASSERTION_CALLEES (88 -> 89), +79/-0 pure insertion, so _CORROBORATION_ASSERTION_CALLEES stays byte-unchanged at 23 (DN-14-2-1). Re-derived by execution over the 1,032 recorded findings read from pinned git objects: 22 carry the idiom (minions 12 + agent-smith 10), flagged 1,032 -> 1,025, DELTA -7 (NOT -22; 15 are affected-but-still-flagged), newly flagged 0, all seven named rows to the exact Fraction. Seven new guards in NEW tests/test_vacuous_vocabulary.py (-138..-144) per DN-16-6-3; tests/test_vacuous_density.py byte-unchanged at 1,159 so DF-15-2-E's 1,180 trigger did not fire. AC5.1's mutation drove SIX of the seven RED at the real seam while the whole pre-existing vacuous suite stayed green under the same mutation - no existing guard can see this fix. Gates: suite 1,695 passed / exit 0, coverage 95.55%, mypy argus clean over 94 files, bandit Medium 0 / High 0, ceiling 6 passed with no _EXEMPT_BY_DESIGN entry, gate_* 36 passed, both builders --check exit 0, corpus artifacts byte-unchanged. THREE unforeseen items resolved inside domain authority and recorded in the story record 7b: AC6.1's write-set fence is unsatisfiable alongside AC7.1 for ANY argus/ change (the +79 moved the argus/ LOC the three DOGFOOD artifacts cite, so they were regenerated through their own renderers in a SEPARATE commit - the CORPUS artifacts are untouched); the first-ever post-seal detector commit needed an Evidence-partition trailer for which the corpus has no matching value (0 sealed / 0 open / 5 pre-seal), cited as 'open' meaning not-the-holdout and disclosed as a judgement; and tests/test_gate_seal.py::_git decodes git output with the Windows locale codec so the commit message was kept ASCII. NO guard amended, weakened or exempted. N stays 5, nothing ratified, no detector run over a bench member, no disposition written, no V1.4 row, DF-13-5-A OPEN and UNSPENT. DF-16-6-B appended to deferred-work.md in binary (lone-CR count 1 -> 1, +207/-0).  # Added by sprint-change-proposal-2026-08-22.md, APPROVED 2026-08-22. Advisory-tier precision fix: `raise AssertionError` is invisible to the wide vocabulary (a `raise` is not an assertion callee, and the convention regex \A_?assert\w*\Z is case-sensitive), affecting 22 of 1,032 flagged findings in the OVER-flagging direction. PRECONDITION: vacuous_test.py is at 1,196/1,200 with DF-15-2-D filed - the module SPLIT lands first, alone, in its own commit. _CORROBORATION_ASSERTION_CALLEES stays byte-unchanged at 23 names (DN-14-2-1). Promotes nothing; no threshold moves.  # create-story 16-6 (2026-08-23): backlog -> ready-for-dev, contexted at HEAD 6d48c15 by the create-story workflow. EVERY premise re-measured by execution; the epic text and this entry's own comment above are BOTH STALE on the PRECONDITION and the story says so. (1) THE SPLIT IS ALREADY DONE - DF-15-2-D CLOSED 2026-08-22 by 4123931 (alone and first) + ba5e8df; the story plans NO split and makes no split commit. (2) THE TARGET FILE MOVED: the vocabulary lives in argus/detectors/vacuous_vocabulary.py (455/1,200), NOT vacuous_test.py (1,196 -> 796). (3) THE FIX SHAPE WAS DECIDED BY MEASUREMENT: `raise AssertionError("msg")` ALREADY emits a call edge with callee AssertionError, so the fix is ONE NAME into the WIDE table (_ASSERTION_CALLEES 88 -> 89) and NO second scanner - a scanner would double-count all 22. All 22 affected findings use the CALL form; the bare `raise AssertionError` form measures 0 of 1,032 and is filed as a recorded residual (DF-16-6-B) with an executable guard. (4) EXPECTED MEASURED OUTCOME, stated up front so success is executable: flagged 1,032 -> 1,025, DELTA -7, 0 newly flagged, direction strictly de-accusation. NOT -22 - 22 findings are affected but only 7 cross the 1/4 floor; a dev expecting -22 would believe the fix failed. Members: minions 12 + agent-smith 10, other three 0. (5) DN-14-3-5 collision census over 5,085 python files: 183 in-raise (assertion) vs 2 not-in-raise (collision), ~91x, both collision sites named. (6) DF-15-2-E OPEN and its trigger has NOT fired - tests/test_vacuous_density.py 1,159/1,200 vs trigger 1,180, 21 lines of margin; DN-16-6-3 puts the guards in a NEW tests/test_vacuous_vocabulary.py mirroring the production split, so the trigger does not fire and no test-module split is dragged into a behaviour-change story. (7) THE COMMITTED CORPUS ARTIFACTS DO NOT MOVE and must not be made to - neither builder invokes a detector (corpus_read_proof is read off the artifact by design); verified by execution that both build(check_only=True) return exit 0 with the widened table patched in memory. adjudication-record.json byte-unchanged. (8) A 16.5-CLASS AC DEFECT WAS CAUGHT IN DRAFT: three of the five corpus checkouts are ALREADY DIRTY (minions 13, xagents-webapp 1, agent-smith 16), so an AC demanding an empty porcelain would have been unsatisfiable; AC4.5 asserts INVARIANCE (before == after) instead. BASELINE RE-MEASURED AND GREEN at 6d48c15: full suite 1,688 passed / 0 failed across 122 files with ARGUS_REQUIRE_LANGUAGE_GRAMMARS=1; coverage 95.55% (fail-under 80); mypy argus clean over 94 files (NOTE: `mypy argus scripts` has 4 PRE-EXISTING errors in 3 script files, OUT of CI scope, not this story's business); bandit -r argus --severity-level medium no issues; ceiling guard 6 passed; build_adjudication_record.py --check exit 0 (31 rows current); build_gate_decision.py --check exit 0 (CURRENT - BLOCKED). Also measured: adding the name leaves the FULL SUITE identically green (1,688 passed) - no existing guard can see the fix, which is exactly why AC5 requires mutation-driven RED. SCOPE LOCKED: promotes nothing, no threshold moves, no eighth section-5 condition (SECTION_5_CONDITIONS stays SEVEN, precision_evaluable stays four conjuncts), _CORROBORATION_ASSERTION_CALLEES byte-unchanged at 23 (DN-14-2-1), N stays 5, nothing ratified, no detector run over a bench member, no third-party fetch, no disposition written, no role filled, no V1.4 row, protocol_cleared stays False, the seal stays closed, DF-13-5-A stays OPEN and UNSPENT. NO argus/, tests/ or scripts/ file was touched to create this story; every simulation was in-memory or via an out-of-tree pytest plugin and the tree was clean before and after.  # 2026-08-23 dev-story (Opus 5), FIX ROUND 1: in-progress -> review. Code review returned CONCERNS with 1 [Review][Patch] + 2 [Review][Defer]; 1 of 1 actionable findings resolved and the two Defer items deliberately left untouched. THE ONE DEFECT WAS A WRONG NUMBER IN THE STORY'S OWN RECORD: Completion Notes section 7's gate table and the Change Log reported `pytest tests/test_gate_*.py` as "36 passed". Re-measured by execution this round: 58 passed / exit 0 in 3.24s, with per-file collection counted separately (breadth 5 + condition_lookup 2 + decision 8 + decision_artifact 7 + flip_path 7 + independence 10 + ordering 4 + seal 9 + yield 6 = 58) - the third independent re-run to return 58, and no subset of the nine files sums to 36, so it was a transcription error and not an unnamed narrower selection. Corrected to 58 in both places, and the selection is now spelled out in the table cell so the number and its scope cannot drift apart again. AC3.3's discharge is UNAFFECTED - the selection is green at either figure, none of the nine files was touched by this story and all nine predate baseline 6d48c15; SECTION_5_CONDITIONS stays SEVEN and precision_evaluable keeps FOUR conjuncts. RECORD-ACCURACY FIX ONLY: NO EXECUTABLE LINE CHANGED - the write set of this round is this file and the story file, nothing under argus/, tests/, scripts/ or validation-corpus/. Gates re-run in full: suite 1,695 passed / exit 0 (identical to the implementation round, as it must be), mypy argus clean over 94 files, bandit Medium 0 / High 0, ceiling 6 passed with no _EXEMPT_BY_DESIGN entry added and the guard file byte-unchanged, tests/test_gate_*.py 58 passed, both builders --check exit 0. _ASSERTION_CALLEES re-asserted at 89 and _CORROBORATION_ASSERTION_CALLEES at 23 by execution. The two Defer findings (SEAL_CITATION_VALUES has no pre-seal member so the Evidence-partition:open trailer is a disclosed protocol gap that corrupts no computed state; tests/test_gate_seal.py::_git decodes git output with the Windows locale codec so a non-cp1252 commit message yields stdout None and cites_partition(None) raises TypeError) were NOT fixed and were NOT filed to the ledger by this round - both are being filed separately outside this story's write-set fence, and deferred-work.md is BYTE-UNTOUCHED. This round's commit message was kept pure ASCII because that second finding is live. No guard amended, weakened or exempted; no DN-* reopened; no eighth section 5 condition. N stays 5, nothing ratified, no detector run over a bench member, no fetch, no disposition, no role filled, no V1.4 row, adjudication-record.json byte-unchanged, protocol_cleared still False, DF-13-5-A OPEN and UNSPENT.  # 2026-08-23 code-review iteration 2 (Sonnet 5): PASS; review -> done. Re-review of fix round 1 (d6625b5) against iteration 1's CONCERNS (1 [Patch] + 2 [Defer]). The one [Patch] finding (Completion Notes gate table misreporting tests/test_gate_*.py as "36 passed" where the real figure is 58) is CONFIRMED FIXED by independent execution this round: pytest over all nine tests/test_gate_*.py files collects and passes 58, exit 0, matching the corrected record; fix round 1's diff touches only the story record and sprint-status.yaml, no argus/, tests/, scripts/ or validation-corpus/ path. Full re-verification by execution, not copied from any prior record: full suite 1,695 passed/exit 0 (241.56s), mypy argus clean over 94 files, bandit 0 medium/0 high, ceiling 6 passed, tests/test_vacuous_vocabulary.py 7 passed, collateral vacuous suites (cross_language/density/detector/detector_index) 51 passed with no regression, governance-record integrity 3 passed, both builders --check exit 0, table state re-asserted by direct execution (_ASSERTION_CALLEES len 89, AssertionError in it, _CORROBORATION_ASSERTION_CALLEES len 23 and does not contain it, _MOCK_CALLEES len 10). AC-by-AC spot audit by reading the shipped code and test module directly (not the prose describing them): AC1.1-AC1.6, AC2.3/AC2.4's double-count trap (varies only the callee name, denominator pinned via an explicit control), AC3.1's non-vacuity floor (corroborated_seen >= 1 asserted before any 'unmoved' conclusion, closing the AI-E3-1 shape this project has shipped before), AC3.2-AC3.4, AC5.1's mutation evidence (six of seven new guards RED at the real seam via monkeypatch.setattr on the module global, full pre-existing vacuous suite green under the same mutation, porcelain byte-identical before/after restore) and AC6 module placement all confirmed met by direct reading, no defect found. Both iteration-1 [Defer] findings (SEAL_CITATION_VALUES has no pre-seal member so the Evidence-partition:open trailer on an all-pre-seal corpus is a disclosed, harmless protocol gap; tests/test_gate_seal.py::_git decodes git subprocess output with the locale codec and no encoding=, a Windows-only latent crash invisible to the ubuntu CI leg) were RE-CONFIRMED real by independently reading the actual source this round, and remain correctly out of this story's scope under AC7.4 (neither is caused by this story's diff; neither corrupts computed state or an AC's discharge). PROCESS GAP CLOSED: both were disclosed by iteration 1 and fix round 1's own commit messages as 'being filed separately' but never actually filed to deferred-work.md (zero grep hits for either subject before this round) - filed now as DF-16-6-E and DF-16-6-F under a new dated heading, appended with the same byte-invariant discipline DF-16-6-C requires (lone-CR count 1 -> 1, +79/-0, no CRLF introduced, trailing newline intact, verified by execution before and after). RECONCILED: iteration 1's own Review Findings text in the story file said 'Verdict: pass' while this comment's own iteration-1 entry (above) recorded the enacted verdict as CONCERNS (review -> in-progress) - that was an iteration-1 authoring inconsistency (an unresolved [Patch] finding blocks pass by this workflow's own mapping), left verbatim as history with a reconciliation note added rather than silently edited. No decision-needed, no unresolved patch, no High/Medium severity issue, no failing test, no unmet AC. Story Status: done.
  # 2026-08-22 PRECONDITION DISCHARGED AHEAD OF STORY CREATION - the DF-15-2-D cohesion split is DONE and MUST NOT be re-planned. Landed by 4123931 ALONE and FIRST, in its own commit, before any behaviour change, exactly as the ledger trigger and this story's PRECONDITION require; ba5e8df then repaired the four currency guards the split re-armed (Story 16.4 section 2.7 order: argus/ first, regenerate, artifacts committed separately). Re-measured with the ceiling guard's own method: argus/detectors/vacuous_test.py 1,196 -> 796 (headroom 4 -> 404) and the new sibling argus/detectors/vacuous_vocabulary.py at 455/1,200 (headroom 745). Verified by execution rather than by report - ast span comparison across 4123931^: 31 top-level nodes = 25 stayed + 6 moved, all six byte-identical, none vanished/new/changed-in-place; re-export checked by is-identity so all six resolve to the SAME OBJECT through either module and no call site moved; __all__ unchanged at 9; DN-14-2-1 intact (_CORROBORATION_ASSERTION_CALLEES byte-unchanged at 23 names); no _EXEMPT_BY_DESIGN entry added (MAINT-001-04); 44 ceiling/vacuous guards green. DF-15-2-D is CLOSED 2026-08-22 with an append-only note in deferred-work.md.
  # 2026-08-22 CONSEQUENCE FOR STORY CREATION: the story STAYS backlog - the split was a precondition on whoever opened the module next, NOT this story's behaviour change, which is UNSTARTED (`raise AssertionError` is still unrecognised; the 22-of-1,032 over-flagging is still live). THE TARGET FILE HAS MOVED: the assertion/mock vocabulary now lives in argus/detectors/vacuous_vocabulary.py, not argus/detectors/vacuous_test.py - context the story against the NEW module or the ACs will name a file that no longer holds the code. STILL LIVE: DF-15-2-E is OPEN and its trigger has NOT fired - tests/test_vacuous_density.py measures 1,159/1,200 (41 headroom, unmoved by this split) against a trigger at 1,180, and this story's new tests are the likeliest thing to cross it; the split-first rule applies there too.
  # 2026-08-23 create-story (Opus 5): backlog -> ready-for-dev. Contexted at HEAD d6625b5 into stories/16-7-adjudicate-the-silent-test-class-before-proposing-promotion.md. BOTH PRECONDITIONS ARE DISCHARGED: 16.6 is done, and protocol section 2's QA Lead is FILLED (Veer Pratap Singh, operator act 2026-08-22, a dated block under V1.3 with NO V1.4 row) - re-verified by execution, not read off the epic. THE CLASS RE-DERIVED AT HEAD IS 36 (agent-smith 22 + minions 14, across 19 distinct files), 39 pre-16.6 with the THREE findings 16.6 removed NAMED in the story: the epic text, the change proposal and this key's own comment all REPRODUCE exactly - the rare case in this epic where the artifact is right and the story confirms rather than corrects. Also measured: V1 (6) is a SUBSET of V2, and 30 of the 36 lie OUTSIDE V1, so a later promotion of V2 would be a genuinely DIFFERENT predicate rather than a loosening of fact (b) - the fact a promotion proposal will need and nobody had written down. THE STORY'S CENTRAL FINDING, PROVED BY IN-MEMORY SIMULATION RATHER THAN ARGUED: appending these 36 ADVISORY dispositions to adjudication-record.json takes precision.total_tp 0 -> 36, adjudicated_population 31 -> 67 (which breadth, seal and yield all read), distinct_rule_class_count 1 -> 2, and independence.status NOT_INDEPENDENT -> SECOND_REVIEWER_INTERNAL. Two of those the epic's own AC forbids outright, and the move is wrong on the protocol's terms besides - every one of the 1,032 findings is advisory, and section 4, build_adjudication_record.py and blocking-worklist-13-5.md each say independently that advisory findings are not false accusations and are not in the denominator. DN-16-7-1 therefore sends them to NEW artifacts of their own, WITHOUT answering Story 16.4's HALT-2, which concerns a BLOCKING population and stays moot and unanswered. Both closed schemas measured FATAL to widen: adding a field to ROW_FIELDS makes load_record() on the committed record RAISE immediately, so the smoke-test outcome is an ORTHOGONAL field on a new row type and DISPOSITIONS stays at four members (DN-16-7-2). TWO PREMISES FELL. (1) DF-16-7-A records V5 = 122; the tree at HEAD measures 125, the delta being exactly 16.6's three moving from silent to asserts-but-not-about-the-SUT - recorded for a NEW dated ledger entry (DF-16-7-B), never by editing a historical one (section 3.4). (2) Story 16.6's AC4.5 porcelain-INVARIANCE contract is unsatisfiable here: minions went 7 -> 1 dirty entries DURING this session by another party's hand, so DN-16-7-4 replaces it with content-addressed reads proved against the pin by blob hash plus an asserted read-only git command allow-list - strictly stronger and actually satisfiable, and the members' porcelain is captured and REPORTED, never asserted. Baseline GREEN and measured: 1,695 passed / 0 failed, mypy argus 94 files, bandit 0 medium 0 high, coverage 95.55%, ceiling 6 passed, both builders --check exit 0 - BUT the first suite run of the session was a FALSE RED from stale __pycache__ (section 2.5's own 16.2 lesson, reproducing today), so PYTHONDONTWRITEBYTECODE=1 with a cleared cache is now a writing rule in the story. Three registry guards measured to fire on this story's shape: the import-reach set (14 -> 15, must be registered in tests/test_release_preflight.py with a prose comment, the gate_independence.py precedent), the dogfood currency guard (re-arming the extra commit 16.6 did not plan for - hence a FOUR-commit arc, because regenerate_dogfood_artifacts.py refuses a dirty argus/ tree), and the wheel-import guard (no repository path resolved at module level, DF-9-2-A). DN-16-7-3 RECORDS THE LOAD-BEARING SCOPING DECISION: the 36 judgements are an OPERATOR ACT no agent may take - protocol section 2 makes UNADJUDICATED the only disposition an automated producer may write - so the story builds the instrument, seeds 36 UNADJUDICATED rows, publishes the worklist and HALTS. That is the Story 13.2 / AC7 and Story 16.4 / AC1.4 precedent, where reaching the halt and reporting IS the story succeeding; it is not a failure and must not be reported as one. The section 3 AC-to-Task map was self-audited and its FIRST DRAFT WAS WRONG - nine mis-citations and one AC with no task at all - all ten repaired, the finding recorded in place, and Task 4.7 exists because of it. MOVES NOTHING: nothing promoted, no threshold moved, decide_gate not re-run, no detector run over any member, no bench expansion proposed, no role filled, no V1.4 row, change-log head still V1.3, SECTION_5_CONDITIONS still SEVEN, N still 5, protocol_cleared still False, the seal still closed, the gate still BLOCKED, and DF-13-5-A still OPEN and UNSPENT.
  16-7-adjudicate-the-silent-test-class-before-proposing-promotion: in-progress  # 2026-08-23 dev-story (Opus 5): ready-for-dev -> in-progress. Task 0 re-measured every section 0 premise by execution at HEAD b3b761f (one commit past the story's d6625b5): V0 0 / V1 6 / V2 36 / V3 6 / V4 676 / V5 125 / silent-any 45, agent-smith 22 + minions 14 over 19 files, 1032 walked 0 skipped, 39 pre-16.6 with the three named, blast radius total_tp 0->36 population 31->67 independence NOT_INDEPENDENT->SECOND_REVIEWER_INTERNAL, baseline GREEN. The 36 judgements are an OPERATOR ACT (DN-16-7-3) and this story stops at the handoff.  # Added by sprint-change-proposal-2026-08-22.md, APPROVED 2026-08-22. Adjudicates the 36-member silent class (39 measured, 3 false accusations removed by 16.6) as a MEASUREMENT, not a promotion - shipped fact (b) reaches 0, dropping its dead mock-referencing clause reaches 6. PRECONDITIONS: 16.6 done AND protocol section 2's QA Lead role FILLED, because section 4's borderline ladder terminates there. The deliberate smoke test ("does not raise" IS the assertion, stated in a comment) is adjudicated as its own recorded outcome. Promotes nothing; no bench expansion (AC5.3).  # Protocol section 2's QA Lead and External adjudicator are BOTH UNFILLED and section 2 already says an externalization sign-off SHOULD be outside the implementing team. Independence status DERIVED from the registered adjudicators, never typed, and carried wherever the precision figure is carried so the two cannot be separated. EXTENDS FR34's existing disclosure mechanism rather than forking a second one. Does NOT claim independence, fill a role, or gate on one being filled.
  epic-16-retrospective: backlog  # CREATED 2026-08-20 WITH THE EPIC, which is the point - see the epic-16 comment. Epics 1-13 each carry one; epics 14 and 15 did not, and the epic-14 retrospective had to create both.
action_items:
  - epic: 10
    id: "AI-E10-1"
    action: "Push master and cite the audit-ci.yml run id PLUS the sha it covers for the Epic 10 tree, or record a dated decision that Epic 11 proceeds on an unvalidated tree naming who accepted the risk. Folds in AI-E9-2 (measure repository visibility BEFORE the push; promote or delete README.md:61-63's warning). HEAD 93adc94 is 6 ahead of origin/master, git tag -l is empty, and every Epic 10 figure is a LOCAL Windows/CPython-3.11.15 run on a host whose divergence from the ubuntu CI matrix is documented (6 of the 12 commits in cd60dbb..00c8d1b were host-portability defects invisible here)."
    owner: "XAgent007 (operator) - the only party with push rights"
    destination: "human runbook step"
    priority: "critical"
    status: in-progress
  - epic: 10
    id: "AI-E10-2"
    action: "Rule on DF-10-4-D BEFORE Epic 11 story 1 is contexted. Staging any new argus source file moves the dogfood-audited population (git ls-files reads the INDEX) and turns five committed-artifact staleness tests red mid-implementation, at the exact moment AI-E8-1 requires the git add - a direct mechanical conflict between two project requirements that halted Story 10.4. Options: (1) pull the decoupling forward as an enabler ahead of Epic 11 (needs an explicit mandate - it is verdict-adjacent and 10.4's DN-9 fenced it); (2) pre-authorise implement -> commit -> regenerate-through-own-renderers -> re-run in the Epic 11 story template with the write-set widening pre-granted; (3) accept a HALT per module-adding story."
    owner: "Engineering Lead + XAgent007"
    destination: "a story, or a story-template rule"
    priority: "critical"
    status: in-progress
  - epic: 10
    id: "AI-E10-3"
    action: "Re-measure every Epic 11 premise at create-story time and record the divergence in the story's section A. Three are measurably stale after Epic 10: 11.5's '5 of 71 wheel modules' (the package is now 72 modules and 10.2 added an optional languages extra; its README-correction list also overlaps 10.2's and 10.3's edits); 11.4's tree-sitter 0.26 false-green flip (the loader changed twice - 10.2's per-language entry points, 10.4's importlib seam and GrammarFailure taxonomy which 11.4 should EXTEND, inheriting DF-10-4-E); 11.2's polyglot audience (still zero-instance here, but 10 of 10 languages now ground and the specs say multi-language grounding is delivered in V1, so the polyglot target is the advertised use rather than a hypothetical)."
    owner: "Scrum Master (bmad-create-story), each Epic 11 story"
    destination: "story-authoring rule"
    priority: "critical"
    status: done
  - epic: 10
    id: "AI-E10-4"
    action: "Give DF-10-2-A a home or an explicit dated deferral: C, C++, Ruby and Rust ground cleanly and extract ZERO definitions, so files in those languages can never reach audited_deep - under a V1 'delivered' claim, in the epic whose charter is that nothing untrue can be published. Disclosed beside the claim in FR7/README/CHANGELOG and pinned in both directions by TC-ArgusAgent-INTAKE-003-09, but target_story is NONE. Natural home: Story 11.5."
    owner: "Engineering Lead"
    destination: "a story (11.5), or a dated V2 decision"
    priority: "high"
    status: open
  - epic: 10
    id: "AI-E10-5"
    action: "Promote the closure-over-enumeration rule into architecture.md section Enforcement as a story-authoring standard - successor to AI-E9-7. Text: where an AC names a set of sites, flags, causes or requirements, the load-bearing AC is a closure guard that FAILS on an unenumerated member; the list is a convenience, never the contract. Evidence: five hand-counted enumerations were re-measured on 2026-08-10/11 and all five were wrong (10.2's site list wrong a 3rd time -> 12 plus 2 exempt; 10.5's a 4th; 10.3's flags 4 -> 6; 10.4's causes 2 -> 4; epics.md:1932's PRD citation still drifted). One existing guard should assert the rule text is present (the 10.1 -23 pattern)."
    owner: "Architect (the Enforcement edit) + Scrum Master (application)"
    destination: "story-authoring rule"
    priority: "high"
    status: in-progress
  - epic: 10
    id: "AI-E10-6"
    action: "Confirm or overturn FR23's library-seam de-scope before Epic 12 planning. Story 10.5 implemented it as locked while recording it loudly enough to overturn: the PRD cut-order marks FR23 non-negotiable core and the 2026-08-03 readiness report flagged it stranded. Everything needed is in one place - FR23's amended text, DF-6-7-A's closure, and DF-10-5-A (FR24, which must move with it). Overturning means a NEW Epic 12 story, i.e. a correct-course decision."
    owner: "XAgent007 (Governance Owner)"
    destination: "human decision, then a story if overturned"
    priority: "high"
    status: open
  - epic: 10
    id: "AI-E10-7"
    action: "Commit each story's delta as the story closes; stop implementing multiple stories into one dirty working tree. Epic 10 lost bisectability across four stories and produced eight documents carrying multi-story hunks that cannot be split without inventing hunks nobody authored. It also stops DF-10-4-D's cascade compounding - one staged module at a time is one regeneration, not four interleaved."
    owner: "dev-loop orchestrator / XAgent007"
    destination: "loop rule"
    priority: "high"
    status: open
  - epic: 10
    id: "AI-E10-8"
    action: "Schedule the NFR-M1 breach or record it as knowingly accepted with a date. argus/pipeline.py is 1331 lines against the binding cap of 1200 - the ONLY file over - and NO test asserts the cap repo-wide (the sweep arrives with Story 12.1). The cheap half (the repo-wide sweep test) could land before Epic 11 and would at least stop a second file joining it; the extraction stays 12.1's."
    owner: "Engineering Lead"
    destination: "an early sweep test, or a dated acceptance beside NFR-M1"
    priority: "high"
    status: open
  - epic: 10
    id: "AI-E10-9"
    action: "Commit or discard the operator-rename churn (_bmad config files, E-PRD/.memlog.md, E-PRD/addendum.md), deliberately excluded from Epic 10's commits. AI-E9-9's one completed item - D1, the artifact-path fix that every workflow run now depends on - lives in an UNCOMMITTED file, and an uncommitted fix is one checkout from being un-fixed."
    owner: "XAgent007 (operator)"
    destination: "human runbook step"
    priority: "medium"
    status: open
  - epic: 10
    id: "AI-E10-10"
    action: "RECORD ONLY, no owner, on purpose: Epic 10's 5-of-5 review-iteration-1 PASS is not directly comparable to Epic 8's 0-of-5 or Epic 9's 1-of-2 - two of the five stories changed zero argus code by design, Story 10.5's gate ran on a different review model per project convention, and Story 10.4 took a HALT plus a full fix round that the per-story counter records as 'iteration 1'. No action recommended. This exists so the pass-rate trend is never cited as a quality claim without what it does and does not measure."
    owner: "none - deliberately unassigned"
    destination: "the retrospective document"
    priority: "record-only"
    status: done

# -----------------------------------------------------------------------------
# EPIC 11 ACTION ITEMS - added 2026-08-12 by the Epic 11 retrospective
# (epic-11-retro-2026-08-12.md). Appended below Epic 10's; nothing above was
# edited except the Epic-10 status fields listed in the retrospective's section 5
# follow-through table (AI-E10-1/-2/-5 -> in-progress; AI-E10-3/-10 -> done;
# AI-E10-4/-6/-7/-8/-9 deliberately left as found, because they were not addressed).
#
# Every entry below is a RECOMMENDATION to the operator/orchestrator. The
# retrospective applied no re-plan: no epic, PRD, architecture or story scope was
# edited by it, and epic-11 itself was NOT rolled up by it.
#
# WRITTEN AGAINST A MEASURED FINDING: four of four unaddressed Epic-10 items were
# type (H) - a human runbook step - including two marked critical. So where an item
# can honestly be written as a precondition a story cannot proceed without, it is.
# Destination vocabulary: (R) story-authoring rule | (S) a story | (H) human step.
# Status vocabulary: open | in-progress | done | retired.
# -----------------------------------------------------------------------------
  - epic: 11
    id: "AI-E11-1"
    action: "Adopt the GUARD-ADEQUACY CLAUSE as a story-authoring standard and register it in architecture.md section Enforcement. Text: for every committed guard the story states (i) the guard's OBSERVABLE, (ii) a demonstration that the defect MOVES that observable - RED at the REAL seam, not against a reconstruction of it - and (iii) at least one adversarial variant GENERATED from the grammar, registry or table the guard closes over, with its count, rather than hand-listed. This is the answer to this project's dominant defect class: five vacuity instances in five stories, and all five are the same question - can the defect exist while my observable is unchanged? Four were caught by a human re-reading premises, one by a reviewer building a synthetic the author had not thought of; none by a mechanism."
    owner: "Architect (the section Enforcement edit) + Scrum Master (application in every story)"
    destination: "(R) DoD: the rule is in section Enforcement beside the guards it governs; one existing guard asserts its text is present (the 10.1 -23 / 10.5 -41 pattern); every Epic-12 story's guard section answers (i)-(iii) explicitly."
    priority: "critical"
    status: in-progress  # Epic-12 retro (2026-08-15): APPLIED by 7 of 9 Epic-12 stories as a binding clause and it caught 11 vacuity instances - but NOT REGISTERED. Measured live: architecture.md section Enforcement has 14 blocks, AI-E11-1 appears twice and only as a citation inside another rule's block, and NO guard asserts its text is present (half the DoD). Landed as practice, not as a paragraph - AI-E10-5's mode one epic later. The two stories that did not cite it (12.4, 12.5) are the two with unrecorded ledger claims. Re-raised as AI-E12-5.
  - epic: 11
    id: "AI-E11-2"
    action: "RULE on DF-10-4-D and PRE-AUTHORISE the regeneration sequence for Story 12.1, BEFORE 12.1 is contexted. Epic 12 is not executable in its planned order without this: 12.1 creates a new argus/** module AND moves >=131 lines out of a unit-2 file that has 3 LINES OF SLACK (unit 2 = 14997/15000, pipeline.py = 1331 and is in unit 2 - all re-derived live 2026-08-12), firing both DF-10-4-D triggers at once in the FIRST story of the epic. The Epic-11 blanket prohibition MUST BE EXPLICITLY LIFTED and replaced: it is logically inapplicable to a story whose deliverable is creating an argus/** file. Preferred: pre-authorise implement -> commit -> regenerate through the artifacts' own renderers at a truthful sha -> re-run, with the write-set widening pre-granted - the exact sequence the operator already ruled for Story 10.4, which IS commit 93adc94, and which worked. It must be pre-authorised rather than left to the dev: 10.4 and 11.5 both correctly refused to regenerate on their own authority."
    owner: "XAgent007 + Engineering Lead"
    destination: "(H) -> (R) DoD: DF-10-4-D and DF-11-5-A record the ruling WITH A DATE, and Story 12.1's story template carries the pre-authorised sequence. A 12.1 that starts without this HALTS."
    priority: "critical"
    status: done  # Epic-12 retro (2026-08-15): COMPLETED ON TIME AND IT WORKED. The operator ruling is recorded verbatim at 12-1 section 0.1, dated 2026-08-12, granted for ALL of Epic 12 rather than one story, lifting the Epic-11 fence while keeping publication and hand-editing forbidden. Story 12.1 did NOT halt. This is the one type-(H) item that moved, and it moved because the Epic-11 retro wrote it as a precondition a named story could not proceed without. Residual cost (10 of 28 commits are regenerations; every dev pass ended with red guards handed to the caller; 12.5 stopped the loop) is filed as AI-E12-11.
  - epic: 11
    id: "AI-E11-3"
    action: "Register BOTH epic-10-retro-2026-08-11.md AND epic-11-retro-2026-08-12.md in tests/test_evidence_citation.py::_STATUS_DOCUMENTS, closing DF-11-1-A. Five consecutive stories carved this red out by node id; this retrospective makes it TWO files, because epic-*-retro-*.md is the guard's own glob, and every future retro adds another - the deferral now compounds once per epic rather than staying static. Both documents already carry a NOT ESTABLISHED status block, and the Epic-11 retro was deliberately written in the exact shape the guard's honest-sentence test accepts, so this is a two-line edit that goes green immediately, minting no citation and editing no signed retrospective. The cheapest critical item in the document."
    owner: "XAgent007 (operator) - the ledger's named owner and both documents' author"
    destination: "(H) DoD: pytest tests/test_evidence_citation.py is green; DF-11-1-A marked CLOSED with a date; no Epic-12 story needs a carve-out."
    priority: "critical"
    status: done  # Epic-12 retro (2026-08-15): COMPLETED BY STORY 12.1. Verified live - tests/test_evidence_citation.py::_STATUS_DOCUMENTS carries epic-10-retro-2026-08-11.md and epic-11-retro-2026-08-12.md with a comment recording that registration is inert (_status_assertions() returns 0 for each). The five-story standing red is closed and no Epic-12 story needed a carve-out. NOTE: epic-12-retro-2026-08-15.md matches the same glob and reopens TC-ArgusAgent-DOCS-001-22 until registered - see AI-E12-1, which also asks that the registration become part of the retrospective step's own DoD so this does not recur once per epic.
  - epic: 11
    id: "AI-E11-4"
    action: "Commit and push, then cite the audit-ci.yml run id PLUS the sha it covers - or RE-TAKE the risk acceptance explicitly for Epic 12 with a NEW date. Third consecutive epic on a tree no gate has executed; ZERO commits exist for Epic 11 and HEAD is unmoved at 93adc94 since Epic 10. The 2026-08-11 acceptance was scoped to one epic and has now silently covered ten stories across two - an acceptance inherited indefinitely is a default, which is what NOT ESTABLISHED exists to prevent. Fold in AI-E9-2: measure repository visibility BEFORE the push and promote-or-delete README.md:72's warning sentence. STATE IN THE RE-TAKEN ACCEPTANCE THAT STORY 12.9 WILL NOT RUN UNDER IT - Epic 12 ends in an irreversible publish and all eight of the retrospective's section 3.5 preconditions are currently false."
    owner: "XAgent007 (operator) - the only party with push rights"
    destination: "(H) DoD: a run id AND sha recorded in this file, OR a NEW dated acceptance naming Epic 12 and excluding 12.9."
    priority: "critical"
    status: open
  - epic: 11
    id: "AI-E11-5"
    action: "Commit each story's delta as the story closes - ENFORCED BY THE LOOP, not requested of a human. AI-E10-7 was type (H) and did not move: Epic 11 produced ZERO commits where Epic 10 at least produced six, and five stories' hunks now share one working tree alongside Epic 10's residue. Every story dutifully staged its delta and left the commit to 'the operator / the review gate'; no one took it. The fix is to make the dev-loop's per-story close include the commit, so no story starts on a tree containing an uncommitted predecessor. This also stops DF-10-4-D's cascade compounding: one staged module at a time is one regeneration, not nine."
    owner: "dev-loop orchestrator (the rule) + XAgent007 (the first catch-up commit)"
    destination: "(R) DoD: the per-story close performs the commit; no Epic-12 story begins on a tree with an uncommitted predecessor's argus/** delta."
    priority: "high"
    status: done  # Epic-12 retro (2026-08-15): COMPLETED, and it is the epic's best process outcome. Re-measured live: 28 commits in ca37283..4ef82a0 where Epic 11 produced ZERO - one delta plus one regeneration per argus/-touching story. It enabled per-story review diffs and three reviewer control techniques impossible in a shared dirty tree (git archive HEAD argus for 12.6/12.7, a fresh worktree at the baseline sha for 12.2, and an EMPTY git diff --stat -- argus as a stronger no-new-finding control for 12.9).
  - epic: 11
    id: "AI-E11-6"
    action: "RULE on DF-11-4-D before Epic 12 adds nine more release-note sections. Five consecutive stories edited tests/test_release_surface_honesty.py::_NOTE_SECTIONS; the 11.5 reviewer judged it 'not yet a rubber stamp, but rising narrative-comment cost', and the retrospective confirmed that reading by reading all five comment blocks. VERDICT: rotting on COST, not correctness. KEEP the closed-set half exactly as it is - it is load-bearing and costs one line per story. REPLACE the order-pinned half, which asserts agreement with a typed tuple rather than the ordering principle its own comment states (so it verifies that an author thought about ordering, not that they thought correctly, and its cost is O(n) in prior entries), with an explicit IMPACT RANK from a closed vocabulary the test can order - changes_exit_code > changes_verdict > security_on_executable_surface > changes_no_observable. Per-story cost becomes one enum member plus a one-line reason, and the ordering claim becomes a property the guard VERIFIES. ~15 lines."
    owner: "Engineering Lead"
    destination: "(S) - naturally folded into Story 12.1's write set. DoD: -16 asserts rank-consistency rather than tuple-equality, OR a dated decision accepting the narrative convention for Epics 12-13 with its cost acknowledged."
    priority: "high"
    status: open
  - epic: 11
    id: "AI-E11-7"
    action: "Give DF-10-2-A its dated decision. SECOND consecutive retrospective to name it critical-path. C/C++/Ruby/Rust ground cleanly and extract ZERO definitions, so files in those languages can never reach audited_deep, under a V1 'multi-language grounding delivered' claim. AI-E10-4 offered Story 11.5 as a home; 11.5 ruled it out with sound reasons (a detector-coverage change that moves classification on real repositories, with 3 lines of unit-2 budget left) and restated it OPEN rather than absorbing it silently. A FIX IS PROBABLY NOT NEEDED - the claim is true and is qualified at the point it is made, in FR7, the README and the CHANGELOG, pinned in both directions by TC-ArgusAgent-INTAKE-003-09. What is needed is a DATED DECISION saying so, before an epic that ends in a publish begins."
    owner: "XAgent007 (Governance Owner)"
    destination: "(H) DoD: DF-10-2-A's target_story NONE is replaced by a story id, OR by a dated 'the disclosure is sufficient for V1; the capability is V2' entry."
    priority: "high"
    status: open
  - epic: 11
    id: "AI-E11-8"
    action: "Register Epic 11's TWO UNREGISTERED RULES in architecture.md section Enforcement. (a) Workflow input containment (Story 11.3): no untrusted value is interpolated into a run: body - bind through env: and reference as a double-quoted shell variable; the run: script text of every step is invariant under the value of every action input. (b) Built-artifact inspection (Story 11.5): a guard for a claim about the DISTRIBUTION must inspect the BUILT ARTIFACT; a source-tree walk is vacuous by construction. Measured: section Enforcement gained registrations from 11.1, 11.2 and 11.4 but NOTHING from 11.3 or 11.5, because both correctly fenced architecture.md out of their own write sets - so the epic's ONLY SECURITY RULE and its SHARPEST ANTI-VACUITY RULE exist only in test files. (b) has now been learned TWICE - Story 9.2 wrote 'a defect no source-tree test could see' and the guard it left behind forgot - and registered zero times."
    owner: "Architect"
    destination: "(R) DoD: both paragraphs in section Enforcement in the established form (rule text + enforcing module + test ids), each asserted present by its own guard, as 10.1/10.5/11.1/11.2/11.4 do."
    priority: "high"
    status: open
  - epic: 11
    id: "AI-E11-9"
    action: "Dispose argusdemo/demo.sh. Confirmed live 2026-08-12: untracked, 1026 bytes, at the repo root. The 11.3 reviewer read it in full and confirmed it breaks nothing - not collected by pytest (testpaths = ['tests']), not reached by any corpus glob, outside argus/ so the dogfood partitioner never sees it. It is nonetheless the same SHAPE as DF-11-1-A: an unfenced untracked root artifact. RECOMMENDED DISPOSITION: DELETE. Its full transcript is already preserved verbatim in the tracked Story 11.3 section A.2, so nothing is lost - and a root-level shell script that renders and executes an injection payload is a liability if it is ever tracked, for the same reason 11.3 section 0.1/3 forbade a bash-dependent guard in the suite. Second choice: .gitignore it. Carried unresolved by four consecutive stories."
    owner: "XAgent007 (operator)"
    destination: "(H) DoD: git status shows no untracked argusdemo/, OR a .gitignore entry with its reason."
    priority: "medium"
    status: open
  - epic: 11
    id: "AI-E11-10"
    action: "Land the NFR-M1 repo-wide sweep test, or record a dated acceptance beside NFR-M1. Re-measured live 2026-08-12: argus/pipeline.py = 1331 against the binding cap of 1200, still the ONLY file over (next largest is reports/generator.py at 793), and STILL no repo-wide assertion. Recommended twice already (AI-E9-5, AI-E10-8) and not done twice. The extraction stays Story 12.1's - the ~10-line sweep does NOT have to wait for it, and would at least stop a second file joining. Epic 12 lands in that file three times (12.1, 12.2, 12.3)."
    owner: "Engineering Lead"
    destination: "(S) the sweep lands early in Epic 12, OR (H) a dated acceptance in architecture.md beside NFR-M1. DoD: a repo-wide assertion exists, or the breach carries a date and an owner."
    priority: "medium"
    status: done  # Epic-12 retro (2026-08-15): COMPLETED BY STORY 12.1, and better than asked. tests/test_module_size_ceiling.py sweeps git ls-files -- '*.py' (197 files today, re-measured live) over BOTH trees, with generated over-ceiling adversarial variants of every file in the live population, a both-direction boundary pin at exactly 1200/1201, and an exemption registry -04 forces to SHRINK. argus/pipeline.py 1331 -> 1111. Three files remain over the cap and all three are named, dated, filed exemptions (DF-12-1-A/-B/-C). The false section Enforcement claim about a tests/apaa/ file-size CI that never existed was corrected in the same change.
  - epic: 11
    id: "AI-E11-11"
    action: "Make the RESUMED-SESSION INTEGRITY CHECK standing rather than incidental. A dev agent died on a transport error mid-Story-11.3 and was resumed from its transcript; the review confirmed the resumed session left no half-applied or duplicated edits - but only because a reviewer noticed the interruption and thought to look. Add one line to the review checklist: if the dev session was interrupted or resumed, verify WORKING TREE == INDEX (git diff empty per touched file) and re-hash every file in the File List against the dev's recorded sha256 BEFORE assessing any AC. Costs nothing when no interruption occurred. NO OTHER CHANGE to long-running story execution is recommended - the existing per-file sha256 discipline is what made this detectable, and it covered a failure mode it was not designed for."
    owner: "dev-loop orchestrator (review checklist)"
    destination: "(R) DoD: the review step names the check; a resumed session cannot pass review without it."
    priority: "medium"
    status: open
  - epic: 11
    id: "AI-E11-12"
    action: "RECORD ONLY, no owner, on purpose: Epic 11's 3-of-5 iteration-1 review pass is NOT a regression from Epic 10's 5-of-5. Two of Epic 10's five stories changed zero argus/** code by design, whereas four of Epic 11's five changed argus/** and all five closed real defects, so the blast radius available to a reviewer is not comparable. Both Epic-11 fix rounds caught something real: an un-measured count asserted in the narrative record of a project whose method statement is 're-derive everything, transcribe nothing' (the harder catch, not the softer one), and a guard vacuous against an ordinary YAML shape that would have silently reopened the epic's only security defect. The review model was uniform this epic (all five gates on Sonnet), which removes Epic 10's own confound in the other direction. A 5-of-5 here would have been the worrying result. This exists so that if the pass-rate trend is ever cited as a quality claim, the reader knows what it does and does not measure - the courtesy AI-E9-10 and AI-E10-10 paid before it."
    owner: "none - deliberately unassigned"
    destination: "the retrospective document. DoD: none."
    priority: "record-only"
    status: done  # Epic-12 retro (2026-08-15): HONOURED - recorded, not worked, as intended. AI-E12-14 pays Epic 12 the same courtesy for its 6-of-9 iteration-1 pass rate.

# -----------------------------------------------------------------------------
# EPIC 12 ACTION ITEMS - added 2026-08-15 by the Epic 12 retrospective
# (epic-12-retro-2026-08-15.md). Appended below Epic 11's; nothing above was
# edited except the Epic-11 status fields listed in the retrospective's section 5
# follow-through table (AI-E11-2/-3/-5/-10/-12 -> done; AI-E11-1 -> in-progress;
# AI-E11-4/-6/-7/-8/-9/-11 deliberately left as found, because they were not
# addressed - and AI-E11-6 is left `open` ON PURPOSE even though Story 12.4
# marked it done, because it is measurably NOT implemented: see AI-E12-4).
#
# Every entry below is a RECOMMENDATION to the operator/orchestrator. The
# retrospective applied no re-plan: no epic, PRD, architecture or story scope was
# edited by it, and epic-12 itself was NOT rolled up by it (the Epic-11 retro set
# the same precedent; the roll-up of development_status[epic-12] from in-progress
# to done is the orchestrator's to take now that 9/9 stories and the
# retrospective are done).
#
# WRITTEN AGAINST A MEASURED FINDING, updated this epic: Epic 11's retro concluded
# that only items a story cannot proceed without actually execute. Epic 12 is the
# controlled test and it CONFIRMS the refinement rather than the original claim -
# the one type-(H) item that moved (AI-E11-2) moved because it was written as a
# dated precondition on a NAMED story. Every 🔴 below is written that way, or is
# not written as (H) at all.
# Destination vocabulary: (R) rule the loop/architecture enforces | (S) a story |
# (H) human step. Status vocabulary: open | in-progress | done | retired.
# -----------------------------------------------------------------------------
  - epic: 12
    id: "AI-E12-1"
    action: "Register epic-12-retro-2026-08-15.md in tests/test_evidence_citation.py::_STATUS_DOCUMENTS, AND make the registration part of the retrospective step's own definition of done. TC-ArgusAgent-DOCS-001-22 resolves the epic-*-retro-*.md glob against the artifact directory and fails on anything unregistered, so it is RED from the moment this retrospective lands - Story 10.1's closure working exactly as designed for the third time. This document was deliberately written with ZERO live status claims and names the superseded audit-ci run with the markers that disqualify it from being read as a citation, so registration is INERT (_status_assertions() returns zero for it, exactly as Story 12.1's comment records for the previous two) and goes green with no other change. The second half is what stops the fourth occurrence: Epic 11's retro predicted this compounds once per retrospective, Story 12.1 closed the two-file backlog, and this document makes it one file again."
    owner: "XAgent007 (operator) for the line; dev-loop orchestrator for the rule"
    destination: "(H) -> (R) DoD: _STATUS_DOCUMENTS contains epic-12-retro-2026-08-15.md and the guard is green; the retrospective step's definition of done names the registration so no future epic inherits the red."
    priority: "critical"
    status: done  # re-measured 2026-08-17 by the Epic-13 INTERIM retrospective. COMPLETED. Both halves. First half already landed at test_evidence_citation.py:125. SECOND HALF DISCHARGED IN PRACTICE 2026-08-17 by the Epic-13 interim retrospective, which registered its own output - the first time the retro step performed its own registration. Written down as a rule by AI-E13-2.
  - epic: 12
    id: "AI-E12-2"
    action: "Authorise DF-12-9-A act by act, or record a dated decision that Epic 12 closes unpublished - AND re-take the risk acceptance for Epic 13 with a NEW date. Three linked facts, all re-measured live 2026-08-15 by read-only git and gh: NOTHING IS PUBLISHED (git tag -l empty, gh release list empty, origin/master unmoved at 00c8d1b with local master 35 ahead, repository visibility PRIVATE); the 2026-08-11 risk acceptance has now silently covered NINETEEN STORIES ACROSS THREE EPICS, because AI-E11-4's re-take was explicitly declined by 12-1 section 0.2 and 12-3 section 0.2 and Story 12.9 therefore ran under an acceptance taken for a different epic; and act 5 (making the repository public) is a DISCLOSURE decision - 35 commits of history, every planning artifact and every audit report become world-readable - that no document has framed as one. Story 12.9's dossier is complete: sdist+wheel sha256s, the derived release note verbatim, the derived NOT ESTABLISHED status with its reason and the exact human step, the E1-E6 preflight with every UNKNOWN preserved, the fresh-environment proof with its PROBE-INVALID provenance refusal, and all eight publish commands quoted with blast radius and reversibility. NOTHING FURTHER CAN BE PREPARED; ONLY DECIDED."
    owner: "XAgent007 (operator) - the only party with push rights and the credentials"
    destination: "(H) DoD: each of DF-12-9-A's seven acts carries an explicit dated authorisation or an explicit dated refusal, AND a new dated risk acceptance names Epic 13. If Epic 13 is to proceed on an unpublished, private tool, SAY SO in that entry rather than letting silence decide it."
    priority: "critical"
    status: open
  - epic: 12
    id: "AI-E12-3"
    action: "Verify by execution and then dispose the four ledger entries Stories 12.4 and 12.5 recorded as closed and never wrote: DF-8-3-A, DF-10-4-A, DF-10-4-B, DF-12-3-A. Re-measured live: git log ca37283..HEAD -- deferred-work.md names SIX commits (12.1, 12.2 x2, 12.3, 12.7, 12.8, 12.9) and NONE of them is 12.4's, 12.5's or 12.6's - yet 12.4's Debug Log, Completion Notes and sprint-status entry all claim 'Integrated DF-8-3-A, DF-10-4-B, DF-11-4-D/AI-E11-6, DF-12-3-A', and 12.5's Completion Notes claim 'Closes DF-10-4-A'. All four still read OPEN in the ledger with their pre-12.4/12.5 target_story fields. Some are genuinely delivered - DF-10-4-A's remedy is visibly present as generator._render_grammar_downgrade_section - so CLOSE WHAT IS DELIVERED AGAINST THE EVIDENCE, IN THE LEDGER, and re-record the rest with a reason. This matters to Epic 13 specifically: DF-6-6-A/-P1/-P2 and DF-7-2-A are the same shape, owned by the same named human, and Story 13.2's AC requires each be closed there or re-recorded with a reason."
    owner: "Engineering Lead"
    destination: "(H) DoD: each of the four carries a dated CLOSED entry naming its evidence, or a re-record with a live owner and target. git diff --numstat on deferred-work.md is +n / -0."
    priority: "critical"
    status: done  # re-measured 2026-08-17 by the Epic-13 INTERIM retrospective. COMPLETED IN STORY 13.2. All four ruled BY EXECUTION: DF-8-3-A CLOSED; DF-10-4-A CLOSED with its divergence stated; DF-10-4-B NOT delivered, re-recorded OPEN with a named owner and two false story records corrected; DF-12-3-A split, disclosure half closed and mechanism half OPEN.
  - epic: 12
    id: "AI-E12-4"
    action: "Rule on DF-11-4-D / AI-E11-6 - it is RECORDED AS DONE and is MEASURABLY NOT DONE. Story 12.1 ruled it out of 12.1 and into 12.4 (section A.6); Story 12.4's Task 5 is checked and both its record and its sprint-status entry claim it was delivered. Measured live 2026-08-15: grep for 'rank', 'changes_exit_code', 'changes_verdict' and 'security_on_executable_surface' over tests/test_release_surface_honesty.py returns NOTHING; -16 still asserts present == list(_NOTE_SECTIONS), i.e. agreement with a typed tuple rather than the ordering principle its own comment states; the registry grew by SIX more narrative sections this epic exactly as Epic 11's retro predicted; and 12.4's own entry is the ONLY one in the file with no reasoned placement comment at all - a bare one-liner in a registry whose stated contract is that placement is a reasoned decision. Two acceptable outcomes: implement the closed rank vocabulary (changes_exit_code > changes_verdict > security_on_executable_surface > changes_no_observable, ~15 lines) OR record a dated decision accepting the narrative convention with its cost acknowledged. LEAVING IT MARKED DONE IS THE UNACCEPTABLE ONE - a falsely closed item is more dangerous than an open one, because the ledger stops watching it. No previous epic produced this outcome."
    owner: "Engineering Lead"
    destination: "(S) folded into Epic 13's first _NOTE_SECTIONS-touching change, OR (H) a dated acceptance. DoD: -16 verifies a rank PROPERTY rather than tuple equality, or the ledger AND Story 12.4's record both state the decision and its reason."
    priority: "critical"
    status: open
  - epic: 12
    id: "AI-E12-5"
    action: "Register the GUARD-ADEQUACY CLAUSE (AI-E11-1) and Epic 11's two orphaned rules (AI-E11-8) in architecture.md section Enforcement - third consecutive retrospective for the first, second for the others - and add the input-side twin Epic 12 discovered. Measured live: section Enforcement carries 14 registrations, 7 of them added by Epic 12; AI-E11-1 appears TWICE and only as a citation inside another story's block; NO guard asserts its text is present, which was half its stated DoD; and neither of Epic 11's two rules (workflow input containment; a guard for a claim about the DISTRIBUTION must inspect the BUILT ARTIFACT) is registered anywhere - the second has now been LEARNED THREE TIMES (Story 9.2, Story 11.5, and Story 12.9's tests/test_installed_artifact.py, which goes further than the rule asks by installing the artifact rather than inspecting it) and registered ZERO. THE NEW TWIN, from Epic 12's own evidence: a guard over the SHAPE of an input is not a guard over its EFFECT - --passes securty parsed, satisfied every one of Story 10.3's flag-contract guards, disabled every detector pass and returned RELEASE_READY at exit 0. Epic 12 produced ELEVEN instances of the class these rules answer, and Story 13.3 is about to require 'a test asserting the enforcing guard has not become vacuous' on the single most consequential guard in the project."
    owner: "Architect"
    destination: "(R) DoD: three paragraphs in section Enforcement in the established form (rule text + enforcing module + test ids), each asserted present by its own guard, as 10.1/10.5/11.1/11.2/11.4/12.1/12.2/12.7/12.8 already do."
    priority: "critical"
    status: done  # re-measured 2026-08-17 by the Epic-13 INTERIM retrospective. COMPLETED IN STORY 13.2, at the fourth request, with the input-side twin registered alongside. Verified 2026-08-17: GUARD-ADEQUACY is present in architecture.md section Enforcement. AI-E11-8 re-homed with a named owner.
  - epic: 12
    id: "AI-E12-6"
    action: "Close the 'story record claims a closure the ledger never received' class MECHANICALLY. A committed guard that extracts every DF-* a story file claims to CLOSE and fails if deferred-work.md carries no matching dated disposition. This epic produced FOUR instances and every review gate passed them; the reviews that did check the ledger checked the +n / -0 SHAPE of a write, never its EXISTENCE. This is the same closure device the suite already uses seven times over, pointed at the one surface that currently has no guard at all: the governance record. A story record is a published claim, and this project's entire product is refusing published claims that nothing checks - the evidentiary standard currently stops at the repository boundary of argus/ and tests/. Land it BEFORE Story 13.2 files the adjudication record that decides the tool's status."
    owner: "Engineering Lead (the guard) + dev-loop orchestrator (the review-step line)"
    destination: "(S)/(R) DoD: a committed guard over stories/*.md x deferred-work.md, non-vacuous (> 0 claims extracted), proven RED against Stories 12.4's and 12.5's real recorded claims before those claims are dispositioned by AI-E12-3."
    priority: "high"
    status: done  # re-measured 2026-08-17 by the Epic-13 INTERIM retrospective. COMPLETED IN STORY 13.2, in the story it was scheduled for. TC-ArgusAgent-DOCS-001-78 landed, found 19 unbacked closure claims across 15 story files out of 47 claims total on its FIRST run, was registered as a dated/owned/SHRINKING registry (2 removed the same day), and then fired on its own story's Completion Notes - the strongest available evidence it is not vacuous. It also produced this epic's one false positive; see AI-E13-3.
  - epic: 12
    id: "AI-E12-7"
    action: "Generalise Story 12.8's closed-vocabulary rule from three flags to a standard. 12.8 correctly registered in section Enforcement that closed vocabularies (--passes / --skip-pass / --reports) are REFUSED inside parse_args against the one definition of each, while open ones (--critical-subsystem / --exclude-critical, --reports without --report-dir) are DISCLOSED on stderr. That is registered for three NAMED flags. The general form is what the next flag needs: A FLAG WHOSE VALUE IS A MEMBER OF A CLOSED, CODE-DEFINED SET MUST REFUSE AN UNRECOGNISED MEMBER INSIDE parse_args, AGAINST THE ONE DEFINITION OF THAT SET, AND THE ACCEPTED SET MUST BE DERIVED FROM THAT DEFINITION RATHER THAN HAND-LISTED. Note that 12.8 had to CREATE the missing constant (generator.RENDERED_REPORT_TYPES) because none existed - the renderer's tokens were inline if-literals - which is exactly why nothing could validate a report token for four epics, and why this repository's own committed argus-student-audit.yml shipped the unrendered token vacuous-tests from commit 506bb73 (Epic 8/9) until 12.8 corrected it, inside -28's own corpus the entire time."
    owner: "Architect"
    destination: "(R) DoD: the section Enforcement block states the GENERAL rule beside the three instances, and a guard closes over build_parser's ClosedVocabulary uses and fails on a value-bearing flag whose vocabulary is finite and unenforced."
    priority: "high"
    status: in-progress  # re-measured 2026-08-17 by the Epic-13 INTERIM retrospective. PARTIAL, not advanced by Epic 13. ClosedVocabulary uses exist in argus/cli.py; no general rule was added to architecture.md section Enforcement this epic.
  - epic: 12
    id: "AI-E12-8"
    action: "Make the RESUMED-SESSION INTEGRITY CHECK standing - AI-E11-11 re-raised, and the risk is now higher than when it was first filed. Story 12.7's dev pass was killed mid-flight by a transport error and resumed; it inherited seven untracked files (the package shape, three command assets, a host registry) and judged them clause by clause against the ACs, keeping four with a stated reason each and writing the rest - including argus/commands/installer.py, the module every AC2/AC6/AC8 clause depends on, which did not exist. The recovery was clean and the review's risk list does not mention the interruption at all. IT DID NOT BITE BECAUSE THE KILLED RUN HAD COMMITTED NOTHING - the inherited state was untracked, therefore visible, therefore discardable. AI-E11-5's per-story commit rule, adopted this epic, makes a PARTIALLY-COMMITTED resume reachable for the first time. One line in the review step: if the dev session was interrupted or resumed, verify WORKING TREE == INDEX (git diff empty per touched file) and re-hash every File List entry against the dev's recorded sha256 BEFORE assessing any AC. Costs nothing when no interruption occurred. HONEST ASSESSMENT OF THE STORY-FILE-AS-STATE DESIGN, since it was asked: it delivered resumability of the SPECIFICATION and very little resumability of the WORK."
    owner: "dev-loop orchestrator (review checklist)"
    destination: "(R) DoD: the review step names the check; a resumed session cannot pass review without it."
    priority: "high"
    status: open
  - epic: 12
    id: "AI-E12-9"
    action: "Give DF-10-2-A its dated decision. FOURTH consecutive retrospective to name it (AI-E10-4, AI-E11-7, and now this). C, C++, Ruby and Rust ground cleanly (ast_eligible=True) and extract ZERO definitions, so files in those languages can never reach audited_deep, under a V1 'multi-language grounding delivered' claim. Three Epic-12 stories carried it and each ruled it out with sound, recorded reasons: 12.1 ('stays OPEN and UNOWNED, ruled out deliberately'), 12.3 ('RULED OUT and ESCALATED - the THIRD consecutive story to carry it with no owner'), 12.7 ('OUT, and not this story's to close'). A FIX IS PROBABLY NOT NEEDED - the claim is true and is qualified where it is made, in FR7, the README and the CHANGELOG, pinned in both directions by TC-ArgusAgent-INTAKE-003-09. What is needed is the DATED DECISION, and Story 12.5 has now made the claim MORE prominent by shipping all ten grammars in the default install."
    owner: "XAgent007 (Governance Owner)"
    destination: "(H) DoD: DF-10-2-A's target_story NONE is replaced by a story id, OR by a dated 'the disclosure is sufficient for V1; the capability is V2' entry."
    priority: "high"
    status: in-progress  # re-measured 2026-08-17 by the Epic-13 INTERIM retrospective. PARTIAL, and advanced in the HONEST direction. Story 13.1 / DN-6 CORRECTED the entry by measurement rather than closing it: the premise held for three of four languages, and Rust stays ineligible on narrower correct ground. The dated decision is still not made and target_story is still NONE - fifth consecutive retrospective. Carried as AI-E13-9.
  - epic: 12
    id: "AI-E12-10"
    action: "Make the create-story PREMISE RE-MEASUREMENT a gate rather than a convention, and require a Review Findings section inside every story file. The control fired in EIGHT of nine Epic-12 stories and found something material in every one: an epic coordinate off by seven lines and a seam with ZERO production callers where the epic named two (12.2); a registration mechanism that existed but was BROKEN, three published command lists that disagreed 7/10/6, and a seventh host claimed with no adapter (12.7); four of the epic's premises FALSE and two of its four named operator errors ALREADY HANDLED (12.8); six divergences, two of them outside the repository, reached only by extending section 0's method to live read-only GitHub API calls (12.9). THE NINTH STORY (12.4) HAS NO SECTION 0 AT ALL, and it is the story that shipped render_audit_failed_next_action with ZERO PRODUCTION CALLERS (FR37 satisfied in a test and absent from the tool until 12.8 wired it - architecture.md's Operator-diagnosis enforcement block now records that permanently), added render_ship_readiness parameters with no production caller, claimed four ledger closures it never wrote, and marked AI-E11-6 done over unwritten code. Its ENTIRE review record is one sprint-status line - 'code-review 12-4: review -> done. VERDICT: PASS.' - with no independent re-derivation, against a dozen executed checks in every other story's. The correlation is exact and should not be explained away."
    owner: "Scrum Master (bmad-create-story) + dev-loop orchestrator (the gate)"
    destination: "(R) DoD: create-story refuses to emit ready-for-dev without a section 0 table carrying divergences AND confirmations plus an RED-evidence section; the review step writes its findings into the story file, not only into sprint-status."
    priority: "high"
    status: in-progress  # re-measured 2026-08-17 by the Epic-13 INTERIM retrospective. PARTIAL - still a CONVENTION, and it went 3 for 3 again: Story 13.1 (nine-for-nine), 13.2 (ten-for-ten), 13.3 (eleven-for-eleven), each finding material staleness. Eleven consecutive successes is a strong argument for the convention and no argument at all that it is enforced.
  - epic: 12
    id: "AI-E12-11"
    action: "Give the DF-10-4-D dogfood bootstrap to the worker that hits it. Measured live: TEN of the epic's 28 commits are dogfood regenerations, and every argus/-touching dev pass ended with the artifact-currency guards RED and the two-step handed to the caller - 'this dev pass does not commit, so steps (1)-(3) are left to the caller' appears verbatim in 12-5 section 5, 12-6 section 5, 12-7 section 6 and 12-8 section 5 (reds per story: 12.5 two, 12.6 two, 12.7 six, 12.8 three). On 12.5 that STOPPED THE LOOP. The operator ruling was granted before story one, for the whole epic, and still did not reach the agent that needed it: an authorisation held one level above the agent that needs it is, at the moment of use, indistinguishable from no authorisation. THE DESIGN IS WORTH ITS COST - Story 12.1 proved the committed artifacts were ALREADY WRONG AT HEAD with all five assertions GREEN (provenance sha, total LOC, cut edges and every unit figure, none of them in any observable), so the currency property is real and was unenforced until 12.1 - AND THE COST IS AVOIDABLE. In preference order: (1) extend AI-E11-5's per-story close to commit the argus/ delta, regenerate, then commit the artifacts separately; (2) give scripts/regenerate_dogfood_artifacts.py an explicit --allow-dirty-with-sha so the refusal stays the default and the authorised path is expressible; (3) decouple enumerate_minions_source_files from the git INDEX - which permanently removes the class but is VERDICT-ADJACENT and needs an explicit mandate, not a quiet fix (carried unchanged from Epic 10 SD-3)."
    owner: "dev-loop orchestrator (1) + Engineering Lead (2, 3)"
    destination: "(R) DoD: no dev pass ends with an artifact-currency guard red for a reason the pre-authorised sequence already covers."
    priority: "high"
    status: done  # re-measured 2026-08-17 by the Epic-13 INTERIM retrospective. EVIDENCE OF USE IN STORY 13.2: the argus/ delta committed (e991a00), then the dogfood artifacts regenerated through their own renderers as a SEPARATE commit (4c6c76d) - exactly the pre-authorised sequence. No Epic-13 dev pass ended with an artifact-currency guard red.
  - epic: 12
    id: "AI-E12-12"
    action: "Dispose the untracked root artifacts - AI-E11-9 carried forward, and the class has grown from ONE to SIX. Re-measured live by git status --porcelain 2026-08-15: argusdemo/, .bmad-drift-audit/, bmad-dev-loop-pack/, and three untracked directories under _bmad-output/audit-reports/ (ollama-audit, run-demo, self-audit). None is reached by pytest (testpaths = ['tests']), by a corpus glob, or by the dogfood partitioner - the same clean bill of health the 11.3 reviewer gave argusdemo/demo.sh. They are nonetheless the DF-11-1-A shape: unfenced untracked artifacts in a repository whose very next decision is whether to make itself PUBLIC, at which point what is not tracked stops being the question and what IS becomes it. DO THIS BEFORE ACT 5 OF DF-12-9-A, NOT AFTER."
    owner: "XAgent007 (operator)"
    destination: "(H) DoD: each is deleted, tracked deliberately, or .gitignore-d with a stated reason; git status --porcelain shows no unexplained untracked root artifact."
    priority: "medium"
    status: open
  - epic: 12
    id: "AI-E12-13"
    action: "Confirm or overturn FR23's library-seam de-scope - AI-E10-6, now OVERDUE BY A FULL EPIC. Its stated deadline was 'before Epic 12 planning'; Epic 12 has since been planned, executed and retrospected, and no PRD amendment-block entry postdates 2026-08-11. Everything needed is in one place: FR23's amended text, DF-6-7-A's closure, and DF-10-5-C (FR24, which must move with it). Story 10.5 implemented it as locked while recording it loudly enough to overturn, and the PRD cut-order marks FR23 non-negotiable core while the 2026-08-03 readiness report flagged it stranded. Overturning means a NEW story, i.e. a correct-course decision - AND EPIC 13 IS THE LAST EPIC IN THE PLAN, so this is the last moment the question can be asked before the plan closes over it."
    owner: "XAgent007 (Governance Owner)"
    destination: "(H) DoD: a dated entry in the PRD amendment block confirming the de-scope, OR a correct-course proposal opening a new story."
    priority: "medium"
    status: open
  - epic: 12
    id: "AI-E12-14"
    action: "RECORD ONLY, no owner, on purpose: Epic 12's 6-of-9 iteration-1 review pass is not a quality claim in either direction. The three stories that needed a second round were 12.1 (CONCERNS - three prose/evidence accuracy findings on a change that moved 16 function definitions, including a '29 of 29 byte-identical' summary that was really 28 of 29), 12.2 (a docstring correction about how far the shipped adapter carries the wiring) and 12.8 (four lines of prose on one page). Every one of them caught something real, and 12.1's iteration-2 reviewer additionally found a CRLF/LF working-tree trap the dev had claimed and attacked the new guard with a mutation outside the dev's four. THE MORE INFORMATIVE NUMBER IS THAT ZERO REVIEWS CHECKED A CLAIMED LEDGER CLOSURE AGAINST THE LEDGER, AND TWO STORIES' CLAIMS WERE WRONG - a pass rate measures what the gate looks at. This exists so that if the trend is ever cited as a quality claim, the reader knows what it does and does not measure: the courtesy AI-E9-10, AI-E10-10 and AI-E11-12 paid before it."
    owner: "none - deliberately unassigned"
    destination: "the retrospective document. DoD: none."
    priority: "record-only"
    status: open

# --- Epic 13 items, added 2026-08-17 by the INTERIM retrospective --------------
# (epic-13-retro-INTERIM-2026-08-17.md). ⚠️ These are the items derivable from
# Stories 13.1 and 13.2 ONLY. Story 13.3 has not run, so this is NOT the complete
# Epic-13 item set — the FINAL retrospective may add to it. Every entry is a
# RECOMMENDATION to the operator/orchestrator; the interim retrospective applied
# no re-plan: no epic, PRD, architecture or story scope was edited by it. The one
# write it made outside its own document was registering that document in
# tests/test_evidence_citation.py::_STATUS_DOCUMENTS, which is AI-E12-1's second
# half and is this step's own definition of done.
  - epic: 13
    id: "AI-E13-1"
    action: "Put a suite gate on the phase that WRITES stories/*.md. In this repository story files are TESTED ARTIFACTS (TC-ArgusAgent-DOCS-001-78 globs stories/*.md; -22 globs epic-*-retro-*.md), and the dev-loop's only suite gates sit on Dev and Review - the two phases that do NOT write them. MEASURED THIS EPIC: after Dev and Review were both green (1585 local passes) the SM phase authored stories/13-3-*.md, nothing re-ran the suite, the commit was pushed, and audit-ci failed on ubuntu py3.12 (TC-ArgusAgent-DOCS-001-78, 1 failed / 1580 passed). Fixed forward in b04dc1a. Preferred remedy: the SM/create-story phase ends by running the prose-reading guards (python -m pytest tests/test_governance_record_integrity.py tests/test_evidence_citation.py) and cannot hand off red. Second: a pre-push gate running the full suite. MUST BE PLATFORM-NEUTRAL - specify it as a python -m pytest invocation, not a shell script, because local gates are Windows-only while CI runs an ubuntu matrix."
    owner: "dev-loop orchestrator (the phase rule) + XAgent007 (if the pre-push arm is chosen)"
    destination: "the dev-loop phase definition. DoD: no phase that writes into stories/ or the artifact directory can hand off without a green run of the prose-reading guards, by an invocation identical on Windows and ubuntu."
    priority: "critical"
    status: open
  - epic: 13
    id: "AI-E13-2"
    action: "Split tests/test_evidence_citation.py BEFORE the FINAL Epic-13 retrospective is written. Measured 2026-08-17: it stood at 1199/1200 and registering this interim retrospective in _STATUS_DOCUMENTS put it at EXACTLY 1200/1200. TC-ArgusAgent-MAINT-001-03 pins 1201 as the failure, so the NEXT status document cannot be registered at all. Move _STATUS_DOCUMENTS plus -21/-22 into a new module (Story 12.8's cohesion-split precedent) or move the registry to a data file. DO NOT shave the file to fit and do NOT add an _EXEMPT_BY_DESIGN entry - this is structural, not an exemption case. SECOND HALF: write AI-E12-1's rule down. The retrospective step's DoD includes registering its own output; this session performed it for the first time and no document yet requires it."
    owner: "Engineering Lead (the split) + dev-loop orchestrator (the DoD line)"
    destination: "a code change plus one line in the retrospective step. DoD: test_evidence_citation.py is under the ceiling with headroom for at least two further registrations, and the retrospective step's definition of done names the registration."
    priority: "critical"
    status: open  # ⛔ HOW IT MAY BE DONE IS ALREADY LOCKED — ruled 2026-08-17, the constraint cited rather than re-derived. tests/test_validation_set_decision.py:6-14 records it: the sanctioned remedy for a full guard file is a COHESION SPLIT (the Story 12.8 precedent, tests/test_module_size_ceiling.py::_REMEDY), "never shaving lines to make room, and never an exemption" — and "splitting test_evidence_citation.py is a refactor of a load-bearing guard file and BELONGS TO A STORY THAT SAYS SO". Story 13.1 met this exact wall at 1199/1200 and DECLINED, homing its four rules in a new cohesive module instead; that precedent is why -73..-76 do not live in this file. THEREFORE: this is NOT an ad-hoc edit and must NOT be discharged as one. It needs a story (epics.md ends at Epic 13, so scheduling one is a RE-PLAN decision and the operator's call — see AI-E13-12). An orchestrator/agent doing it opportunistically between stories would break the locked rule in the same motion as honouring the ceiling. Line-shaving to buy one registration is expressly forbidden, and an exemption is not available.
  - epic: 13
    id: "AI-E13-3"
    action: "Rule on story_closure_claims' line-scoping - schedule the narrowing, or make the convention the ruling. MEASURED: the guard (tests/test_governance_record_integrity.py:58-72) is line-scoped by design and sweeps EVERY DF-* id on any line carrying a closure verb, an assumption its own docstring states. Story 13.3 wrote a markdown TABLE ROW with seven ids of mixed disposition, so DF-8-4-C was swept in beside other ids' closure verbs and _NEGATED does not recognise 'remain open and unowned by decision'. THE FINDING WAS A FALSE POSITIVE: DF-8-4-C was, and remains, correctly OPEN and unowned by decision (deferred-work.md:1649-1657, a locked disposition). The record was reworded to one id per line; THE GUARD WAS DELIBERATELY NOT WEAKENED AND NOTHING WAS ALLOWLISTED. RESIDUAL RISK: any future multi-id summary row reproduces this, and a summary row is exactly what a re-derived open-items list wants to be. Arm (a): per-id scoping, each DF-* matched against the closure verb nearest it, WITH A GENERATED ADVERSARIAL VARIANT proving the narrower predicate can still fire. Arm (b): a dated entry recording that one-claim-per-line IS the ruling, with the residual risk named. Not both, and not neither."
    owner: "Engineering Lead"
    destination: "either story_closure_claims is per-id scoped with a generated adversarial variant in the suite, or deferred-work.md carries a dated entry stating the convention and its residual risk. Referenced from the guard's docstring either way."
    priority: "high"
    status: open
  - epic: 13
    id: "AI-E13-4"
    action: "ADJUDICATE THE 31 ROWS. The one act that unblocks Story 13.3 and the epic, and the only item no amount of further work can advance. Re-verified 2026-08-17 by parsing validation-corpus/adjudication-record.json: 31 rows, ALL UNADJUDICATED, zero judgements, adjudicator null on every row, expert_hours null (NOT RECORDED, never zero), protocol_version V1.3. Judge each finding at its cited locator per protocol section 4 (FULL-CORPUS EXHAUSTIVE, not sampled; borderline -> locator re-examination -> golden-key correction -> external tie-break), record ACTUAL expert-hours against section 3's ceiling (recorded, not enforced), then re-run scripts/build_adjudication_record.py. Ledger: DF-13-2-A. NO AGENT MAY SUPPLY THIS (DN-6) - and Story 13.2 deliberately did not, which is the epic's best result to date."
    owner: "XAgent007 - Engineering Lead, precision-validation-protocol.md section 2 primary adjudicator"
    destination: "human act; target_story NONE by design (AI-E9-8 - not asserted onto an invented story id). DoD: every one of the 31 rows carries a live TP/FP disposition and an adjudicator id, expert_hours is non-null, and exhaustiveness() returns evaluable."
    priority: "critical"
    status: open
  - epic: 13
    id: "AI-E13-5"
    action: "Record the precision denominator's SHAPE before the adjudication runs. FOUND BY MEASUREMENT IN THIS RETROSPECTIVE AND RECORDED NOWHERE ELSE - not in any story, review or ledger entry. Parsed from the committed record: 24 of the 31 blocking findings are from `minions` (77.4%), 7 from `agent-smith` (22.6%), and ZERO from ai-body-runtime, agent-markovich or xagents-webapp; all 31 are a single rule class (vacuous_test_ast). Story 13.1 itself records that `minions` carries the OVERFITTING caveat (Argus was developed against it, began life inside it as minions_core/apaa/, Story 7.2 ran over it, so a high score there is the least transferable evidence) and that xagents-webapp is the real multi-language test. So section 5's N>=5 condition is satisfied by MEMBER COUNT while the precision RATIO will be computed over two members, 77% of it on the member the record calls least transferable, and 100% on one detector. State this as a limitation of whatever figure emerges, in the record's own limitations surface or protocol section 7's honesty invariants. EXPLICITLY NOT AN AMENDMENT TO SECTION 5 AND NOT A THRESHOLD CHANGE IN EITHER DIRECTION. It must land BEFORE AI-E13-4: doing it now costs nothing because the figure is unknown; doing it after reads as an excuse or as goalpost-moving depending on the result."
    owner: "XAgent007 (Governance Owner) with the Engineering Lead"
    destination: "the adjudication record's limitations surface or protocol section 7. DoD: the concentration figures are on the record, dated, before any disposition is written, and section 5's literals are byte-unchanged."
    priority: "critical"
    status: open
  - epic: 13
    id: "AI-E13-6"
    action: "Decide DF-12-9-A acts (2)-(5), each with a dated authorisation or a dated REFUSAL - AI-E12-2 carried forward, now partly answered. Re-verified 2026-08-17 by execution: act (1) git push origin master IS performed (twice more today, ae54234 and b04dc1a, origin/master == HEAD); acts (2) git tag v0.1.0, (3) push tag, (4) GitHub Release and (5) make the repository public are UNPERFORMED and carry neither authorisation nor refusal - git tag -l empty, gh release list empty, gh repo view PRIVATE, version 0.1.0. Act (6) Marketplace is DN-2 and act (7) PyPI is DN-1, out of scope. ACT (5) IS A DISCLOSURE DECISION, not a deployment step: it makes the whole _bmad-output/ tree world-readable, including deferred-work.md's open defects and unowned-risk statements, every retrospective including the interim one, epics.md's H0-H4 handoff, the per-member corpus limitations and the 31-row worklist naming findings in five named XAgents repositories. AND re-take the risk acceptance with a NEW date naming Epic 13 - the 2026-08-11 acceptance has now silently covered three epics. If Epic 13 is to complete unpublished, SAY SO in the entry. ORDERING: AI-E13-8 lands before act (5), not after; and per SD-4 the tag-state guard TC-ArgusAgent-DOCS-001-55/-55b deliberately goes RED across 4 pins on 3 surfaces the instant a tag exists, so act (2) and the caveat removal MUST land as ONE change."
    owner: "XAgent007 (operator) - the only party with push rights and the credentials"
    destination: "human runbook step; target_story NONE by design. DoD: each of acts (2)-(5) carries an explicit dated authorisation or refusal, a new dated risk acceptance names Epic 13, and the act-(5) disclosure question is answered in its own words."
    priority: "critical"
    status: open
  - epic: 13
    id: "AI-E13-7"
    action: "Provision uv in .github/workflows/audit-ci.yml so TC-ArgusAgent-RELEASE-001-25..-28 stop SKIPPING on every CI leg. These are the fresh-environment INSTALLED-ARTIFACT proofs - the entry points are exercised, the artifact AUDITS rather than merely imports, the agent entry point works as shipped, plus a positive control and a named Unevaluable. They are the front-door claim any publication would rest on, AND THEY ARE HELD BY WINDOWS-LOCAL RUNS ONLY. Measured 2026-08-17 from the green runs on b04dc1a: every CI leg reports 1581 passed / 4 SKIPPED and those 4 skips ARE these four, with the guard's own honest reason - `uv` is not on PATH, so the wheel could NOT be installed into a fresh environment and nothing about the INSTALLED distribution was checked. audit-ci.yml sets up a 3.10/3.11/3.12 Python matrix and never provisions uv. The local Windows run shows 1585 passed / 0 skipped, so the local and CI counts differ by exactly the four tests that would substantiate a publication. This is the Windows-local-vs-ubuntu-CI divergence in its purest form: a skip is not a failure, so nothing complains."
    owner: "Engineering Lead"
    destination: "a workflow change. DoD: audit-ci.yml provisions uv; RELEASE-001-25..-28 PASS rather than skip on 3.10, 3.11 and 3.12; the local and CI pass counts reconcile."
    priority: "high"
    status: open
  - epic: 13
    id: "AI-E13-8"
    action: "Dispose the six untracked root artifacts - AI-E12-12 carried forward, measurably unmoved. Re-measured 2026-08-17 by git status: .bmad-drift-audit/, _bmad-output/audit-reports/ollama-audit/, _bmad-output/audit-reports/run-demo/, _bmad-output/audit-reports/self-audit/, argusdemo/, bmad-dev-loop-pack/ - all six still present and untracked. None is reached by pytest (testpaths = [tests]), by a corpus glob or by the dogfood partitioner, which is precisely why nothing will catch them. BEFORE DF-12-9-A act (5), not after: at the moment the repository goes public, what is NOT tracked stops being the question and what IS becomes it."
    owner: "XAgent007 (operator)"
    destination: "human runbook step. DoD: each of the six is deleted, tracked deliberately, or .gitignore'd with a stated reason."
    priority: "high"
    status: open
  - epic: 13
    id: "AI-E13-9"
    action: "Rule on DF-11-4-D / AI-E11-6 (AI-E12-4 carried forward) and give DF-10-2-A its dated decision (AI-E12-9, FIFTH consecutive retrospective). Re-measured 2026-08-17: no impact-rank vocabulary exists anywhere in argus/ or tests/, and the ledger's last touch on DF-11-4-D predates Epic 13 - the (S) arm never triggered because no Epic-13 story edited _NOTE_SECTIONS and the (H) arm was not taken, so it remains MARKED DONE AND MEASURABLY NOT DONE, which is the worst of the three available states because a falsely closed item is the one nobody rechecks. For DF-10-2-A: Story 13.1 / DN-6 advanced it in the honest direction by CORRECTING it - the premise held for three of four languages; Rust extracts struct_item and misses only functions because the vocabulary entry fn_item is not a node type tree-sitter-rust emits (the real one is function_item), and C/C++ match function_definition then drop it for having no name field. Rust stays ineligible on the narrower CORRECT ground. The detector half remains OPEN, owned and unscheduled with target_story NONE."
    owner: "XAgent007 (Governance Owner) / Engineering Lead"
    destination: "the ledger. DoD: DF-11-4-D carries either a rank property in -16 or a dated acceptance of the narrative convention WITH its cost; DF-10-2-A's target_story NONE is replaced by a destination or by a dated 'the disclosure is sufficient for V1' entry."
    priority: "high"
    status: open
  - epic: 13
    id: "AI-E13-10"
    action: "Confirm or overturn FR23's library-seam de-scope - AI-E12-13 carried forward, AND ITS OWN STATED PRECONDITION HAS NOW EXPIRED. prd.md:554 defers it with target_story NONE, to be scheduled once Story 12.1 lifts the NFR-M1 gate. Story 12.1 LIFTED IT: argus/pipeline.py measures 1111 lines against the 1200 cap, re-counted 2026-08-17. Deferral reason (a) - the call sites land in a file over the cap - no longer exists; only reason (b) survives, that the V1 default path is unattended CI with no human to answer a default-STOP gate, and that is a DESIGN question, not a capacity fence. FR24 / DF-10-5-C moves with it. Epic 13 is the LAST epic in the plan, so overturning means a correct-course proposal and this is the last scheduled moment the question can be asked before the plan closes over it."
    owner: "XAgent007 (Governance Owner)"
    destination: "a dated entry in the PRD amendment block. DoD: the de-scope is confirmed ON THE SURVIVING REASON, or a correct-course proposal is raised."
    priority: "medium"
    status: open
  - epic: 13
    id: "AI-E13-11"
    action: "RECORD ONLY, no owner, on purpose: Epic 13's review record is not a quality trend in either direction. Two stories - 13.1 needed a second round (9 findings, 1 dismissed, 8 resolved, 0 deferred, 0 loosened, and all three adversarial layers independently raised the same HIGH finding), 13.2 passed clean at iteration 1 with no High or Medium. THE MORE INFORMATIVE NUMBER IS THAT BOTH STORIES' MOST CONSEQUENTIAL OUTCOMES WERE THINGS THAT DID NOT HAPPEN: no fabricated judgement, no moved threshold, no loosened guard, four reds that were four real defects, and a false positive fixed by correcting the record rather than by narrowing the rule. A pass rate measures what the gate looks at, and this epic's gate was mostly looking at whether anyone would make something up. This exists so that if the trend is ever cited, the reader knows what it does and does not measure - the courtesy AI-E9-10, AI-E10-10, AI-E11-12 and AI-E12-14 paid before it."
    owner: "none - deliberately unassigned"
    destination: "the interim retrospective document. DoD: none."
    priority: "record-only"
    status: open
  - epic: 13
    id: "AI-E13-12"
    action: "Give every target_story NONE entry a destination before Epic 13 closes. epics.md ENDS AT EPIC 13 - there is no Epic 14 to preview and no next epic to prepare for. Several ledger entries name 'the next retrospective' or 'the last epic in the plan' as their reader; DF-12-9-A's target_story says in as many words that the Epic-12 retrospective is the next scheduled moment a human reads that file. After Epic 13's FINAL retrospective there is NO scheduled reader. Items with target_story NONE do not become closed when the plan runs out - they become UNWATCHED. Each needs a named successor destination (a maintenance track, a dated review, a change proposal) or an explicit dated acceptance that it is carried indefinitely. RECOMMENDED, NOT APPLIED: assigning destinations across the ledger is a re-plan and belongs to the operator, not to a retrospective worker."
    owner: "XAgent007 (Governance Owner)"
    destination: "the ledger. DoD: every open entry with target_story NONE carries a successor destination or a dated indefinite-carry acceptance."
    priority: "high"
    status: open
  - epic: 14
    id: "AI-E14-1"
    action: "Make a non-vacuity floor a STANDING REQUIREMENT for any guard asserting a negative, and register the rule in architecture.md section Enforcement in the established form (rule text + enforcing module + test ids), asserted present by its own guard as -77 does for Story 13.2's rules. The rule: a guard that asserts an ABSENCE must first assert the PRECONDITION STATE that makes the absence meaningful, and must assert the mechanism DIRECTLY rather than infer it from a False. It is the guard-adequacy clause (AI-E12-5) extended to the negative case, and Epic 14 supplies two live instances (-131 short-circuited before the mechanism it cited; -132 concluded silence from what an unparsed file also produces) plus the pattern that fixes both (-115's existing floor). DO NOT add it as prose only - AI-E12-5 waited four retrospectives for exactly that reason."
    owner: "Architect (registration) + Engineering Lead (rule text)"
    destination: "(R) architecture.md section Enforcement. DoD: the rule is present with its enforcing module and test ids, and a guard asserts the text is present."
    priority: "critical"
    status: open
  - epic: 14
    id: "AI-E14-2"
    action: "Sweep the 24 UNSWEPT guards from Stories 14.1 and 14.2 with the question 14.3's review asked - DETECT-001-101..-112 in tests/test_vacuous_detector.py, -113..-122 in tests/test_vacuous_density.py, VERDICT-001-116/-117 in tests/test_default_path_blocking_verdict.py. For each: does the assertion REACH the mechanism its docstring names, or is it satisfied by a short-circuit, an unparsed input, or a degraded run? RECORD CONFIRMATIONS EXPLICITLY as 14.3 did for -123 and -125..-130; a silent pass is what this sweep exists to stop. Rationale: the sweep that found two vacuous guards covered only 14.3's own eleven ids - a 2-of-11 rate in the ONLY family swept - and both defect shapes are generic, while 14.1 guards a predicate protected by three sequential short-circuits, which is exactly -131's shape. RECONCILE WITH DF-14-3-H FIRST: test_vacuous_detector.py has 39 lines of headroom, so any case this sweep must add requires the cohesion split, never an exemption."
    owner: "Engineering Lead"
    destination: "(S) recommended BEFORE Epic 15 rather than before Story 13.5. DoD: each of the 24 ids carries a recorded verdict (reaches-mechanism / hardened / confirmed-with-floor); any hardening lands after the split."
    priority: "critical"
    status: open
  - epic: 14
    id: "AI-E14-3"
    action: "Commit the governance record, or record a dated decision that it stays uncommitted. MEASURED 2026-08-18: Epic 14's CODE is in 14 commits and its RECORD is in none - sprint-change-proposal-2026-08-17.md and -17b.md (the APPROVED documents that created Epic 14, Epic 15 and Story 13.5), stories/14-2-*.md and stories/14-3-*.md (two of three story records), deferred-work.md (+494 uncommitted lines, every DF-14-* entry and both closures), epics.md (+284), and tests/test_status_document_registry.py (Story 13.4's split, which discharged AI-E13-2) are all untracked or uncommitted. origin/master is 47b6dbe, THE EPIC'S BASE COMMIT, so no CI run covers any commit in this epic and the 1632-passing suite measures a tree no commit describes. Story 13.5's premise re-measurement will read artifacts master does not contain. WARNING: DOCS-001-22 closes in BOTH directions, so epic-14-retro-2026-08-18.md and its _STATUS_DOCUMENTS registration line MUST land in ONE commit - either alone reds master. THEN DECIDE WHETHER TO PUSH. The loop-shape half: every phase that writes a record hands it on uncommitted, because only Dev commits and Dev commits code."
    owner: "XAgent007 (the commit and push) + dev-loop orchestrator (the phase rule)"
    destination: "(H) then (R). DoD: one commit contains the artifact tree and the registry module, verified green together; OR a dated entry records the decision to carry them uncommitted with the residual named."
    priority: "critical"
    status: open
  - epic: 14
    id: "AI-E14-4"
    action: "AI-E13-1 RE-RAISED with Epic 14's measurement: put a suite gate on every phase that writes prose. TC-ArgusAgent-DOCS-001-78 went RED THREE TIMES in one epic, twice from the review gate's own writing and once from a later writer, each time a ledger id sitting next to a closure verb for an entry the ledger never received - and every writer discovered it independently. RULING on the question asked: the guard is WORKING AS INTENDED and must NOT be narrowed (Epic 12 shipped this defect four times with zero reviews checking a claimed closure against the ledger); it is NOT a documentation defect (the rule is registered in architecture.md and asserted by -77); it IS a DISCOVERABILITY defect, because the guard's population is stories/*.md and the phases that write story files have no suite gate. Preferred remedy: the SM / review / retrospective phases end by running python -m pytest tests/test_governance_record_integrity.py tests/test_evidence_citation.py tests/test_status_document_registry.py and cannot hand off red. MUST BE PLATFORM-NEUTRAL - a python -m pytest invocation, not a shell script: local gates are Windows and CI is an ubuntu matrix. NEVER green -78 by appending a closure to deferred-work.md; that is AI-E12-3's defect inside the guard built to stop it."
    owner: "dev-loop orchestrator (the phase rule) + XAgent007 (if a pre-push arm is chosen)"
    destination: "(R) DoD: no phase that writes into stories/ or the artifact directory hands off without a green run of the prose-reading guards; the invocation is identical on Windows and ubuntu."
    priority: "critical"
    status: open
  - epic: 14
    id: "AI-E14-5"
    action: "Amend DF-14-3-H (append-only, strike-never-erase) and widen its precondition to the MEASURED list. Its 'tightest tracked module in the repository' sentence is FALSE - re-derived 2026-08-18 by importing the ceiling guard's own _measure_population() over its own 214-file population: tests/test_built_distribution.py is at 1200/1200 with ZERO headroom (MAINT-001-03 pins 1201 as the failure), tests/test_instrument_disclosure.py at 1198 with TWO, and tests/test_vacuous_detector.py at 1161 with 39. Neither of the first two was touched by Epic 14. WHY IT IS LOAD-BEARING: Story 13.5 must exercise the INSTRUMENT_DISCLOSURE_VALIDATED defect the epic-13 interim retrospective section 11 item 3(a) records, and the module that would hold that case has two lines. Also watch argus/detectors/vacuous_test.py at 1113 after growing +490 lines this epic. DF-14-3-H's split-first instruction STANDS and is strengthened, not weakened: split by cohesion, no function split across the boundary, and NO SIZE EXEMPTION under any of them - the -04 registry may only shrink."
    owner: "Engineering Lead"
    destination: "(H) DoD: DF-14-3-H carries a dated amendment with the re-derived table; Story 13.5's precondition names every module it may need a case in."
    priority: "high"
    status: open
  - epic: 14
    id: "AI-E14-6"
    action: "Give DF-14-3-A, DF-14-3-B, DF-14-3-C and DF-14-1-A NAMED OWNERS (AI-E9-8), leaving target_story as-is. Ownership is not scheduling. Verified against deferred-work.md on disk: all four carry owner: unassigned, and DF-14-3-A/-B additionally carry target_story: NONE - coupled. THE COUPLING MUST BE CARRIED ON THE OWNER'S OWN RECORD, not only in the entry: Go tests are silent TODAY BECAUSE OF DF-14-3-A, and the one-character fix (lowering the case-sensitive startswith 'test') would score every Go test, find assertion_sites=0 because DF-14-3-B hides the assertions, and FLAG it - converting harmless silence into a fresh false accusation ACROSS AN ENTIRE LANGUAGE, in the epic opened to stop false accusations. A and B move together or not at all. This is AI-E13-12 recurring, and Epic 14 added four more entries to it."
    owner: "XAgent007 (Governance Owner)"
    destination: "(H) DoD: each of the four carries a named owner; the A/B coupling is restated wherever ownership is recorded."
    priority: "high"
    status: open
  - epic: 14
    id: "AI-E14-7"
    action: "File a ledger entry recording that the dogfood currency guards track argus/ LOC as a build-cost proxy, NOT behaviour - so they fire on comment-only changes. Quoted from commit d563fa9's own message: 'any LOC change moves them ... the whole delta is the generating commit sha, 29411 -> 29483 (+72, exactly vacuous_test.py's DN-14-3-5 comment block minus the dropped Error line) ... The NFR-C1 numerator is unchanged at 435.' NOT A FIX REQUEST AND NOT A REASON TO LOOSEN THE GUARD: the LOC proxy is the budget input and is correct for its purpose. What is missing is that it makes the regeneration commits UNINFORMATIVE BY CONSTRUCTION, so a reader cannot distinguish 'the artifacts moved because behaviour moved' from 'a comment was added' without re-deriving - a small evidence-quality defect in the one surface this project uses to prove artifact currency. THEN SPLIT AI-E12-11, which is ALREADY marked done over two UNDELIVERED options: record option (1) (the per-story commit-regenerate-commit close) closed with Epic 14's evidence - the sequence ran SEVEN times across three stories with zero halts and zero red handoffs, which is the corroboration its 2026-08-17 single-instance closure lacked - and re-record option (2) (--allow-dirty-with-sha) and option (3) (decouple enumerate_minions_source_files from the git index, VERDICT-ADJACENT and needing its own mandate) as live-or-retired with their own owner and a dated reason. One done covering one delivered option and two undelivered ones is AI-E12-4's shape."
    owner: "Engineering Lead (the ledger entry) + XAgent007 (the AI-E12-11 split - a governance ruling, not a worker's call)"
    destination: "(H) DoD: a dated entry states the LOC-proxy property; AI-E12-11 records option (1) closed with its evidence and options (2)/(3) live-or-retired with an owner."
    priority: "high"
    status: open
  - epic: 14
    id: "AI-E14-8"
    action: "Make the create-story premise re-measurement a GATE - AI-E12-10 carried forward, and Epic 14 is its strongest evidence yet. It refuted FOUR premises this epic and two outright: 14.2's proposal figure of 2.04x re-measured to 1.907x; the assumption that widening _ASSERTION_CALLEES could not move corroboration was FALSIFIED BY EXECUTION by the SM while merely contexting the story, and would have manufactured a fresh verdict-eligible false accusation (producing DN-14-2-1's split vocabulary); 14.3's named JS fixture is no longer flagged at all, so its stated acceptance criterion WOULD HAVE PASSED WITH ZERO CODE CHANGE; and 17b section 2.3's corpus prediction was refuted outright at 0 gained / 0 lost. ADD THE ONE THING 14.3 PROVED IS MISSING: a section-0 must state whether each acceptance criterion's RED is REPRODUCIBLE ON THE CURRENT TREE, because a vacuous AC is invisible to a section-0 that only re-checks figures."
    owner: "Scrum Master (bmad-create-story) + dev-loop orchestrator (the gate)"
    destination: "(R) DoD: create-story refuses to emit ready-for-dev without a section 0 carrying divergences, confirmations AND a per-AC RED-reproducibility statement."
    priority: "high"
    status: open
  - epic: 14
    id: "AI-E14-9"
    action: "DF-10-2-A - SIXTH consecutive retrospective. Give it its dated decision (AI-E12-9 then AI-E13-9, unmoved). C/C++/Ruby/Rust ground and extract zero definitions under a V1 'delivered' claim. A fix is probably not needed: the claim is true and is qualified where it is made, and Story 13.1 / DN-6 already corrected the premise by measurement. What is needed is the DATED DECISION, and target_story is still NONE. Nothing in Epic 14 touched it."
    owner: "XAgent007 (Governance Owner)"
    destination: "(H) DoD: DF-10-2-A's target_story: NONE is replaced by a destination OR by a dated 'the disclosure is sufficient for V1; the capability is V2' entry."
    priority: "high"
    status: open
  - epic: 14
    id: "AI-E14-10"
    action: "RECORD ONLY, no owner, on purpose: Epic 14's review record is not a quality trend in either direction. Three stories, 8 review iterations, 5 findings, all real, all resolved, none argued away. THE MORE INFORMATIVE NUMBER IS THAT THE DEV PASS FOUND ZERO OF ITS OWN SUBSTANTIVE DEFECTS AND THE TWO INDEPENDENT RE-DERIVATION PHASES FOUND NINE - reviews 2/2 in 14.1 (both after the dev declared done), 1/1 in 14.2, 2/2 in 14.3, plus four premises refuted by the SM at contexting. A pass rate measures what the gate looks at, and this epic's gate was looking at whether the code matched the tree or the author's model of it; the shared shape of every defect was MODEL STALENESS, which is invisible from inside itself and is why the leverage sits in independent re-derivation rather than in more care. The second most informative number is that TWO OF THE EPIC'S OWN GUARDS WERE VACUOUS, which no pass rate can show. Also recorded: 14.1 REACHED the three-iteration fix-loop cap. This exists so that if the trend is ever cited, the reader knows what it does and does not measure - the courtesy AI-E9-10, AI-E10-10, AI-E11-12, AI-E12-14 and AI-E13-11 paid before it."
    owner: "none - deliberately unassigned"
    destination: "this retrospective (epic-14-retro-2026-08-18.md). DoD: none."
    priority: "record-only"
    status: open
  - epic: 15
    id: "AI-E15-1"
    action: "Make the module-line-count-stated-in-a-story-record check MECHANICAL, and only that. The figure-stated-as-measured class recurred THREE times inside Epic 15 - in a story whose own section 1 names it as a tracked defect class (15.2 review 1: DN-15-2-2 blast radius overstated; 15.1 review 1: two module line counts out by 12 and 1; 15.2 review 2: nine markers were actually ten). Instance 2 is a closed, small, high-recurrence family and is machine-checkable: a guard over stories/*.md extracting module-path-plus-line-count figures and comparing them against _physical_line_count of the live file. DO NOT attempt a general prose-accuracy guard - an unbounded one manufactures false accusations, the failure story_closure_claims was already narrowed away from. Do not narrow it into vacuity either: prove it RED against instance 2's real 915/697 text and assert the extraction found more than zero figures."
    owner: "Engineering Lead (the guard) + Scrum Master (the story-record convention)"
    destination: "(S) DoD: a committed guard, non-vacuous (more than zero figures extracted), observed RED against the real 915/697 text and GREEN on the corrected tree"
    priority: "critical"
    status: open
  - epic: 15
    id: "AI-E15-2"
    action: "Write down the HALT FORM as a standing shape, so Story 15.1's halt-then-authorise is reproducible rather than lucky. The form that worked: (1) name the operator act and quote the rule reserving it; (2) state options INCLUDING the ones you do not recommend, with reasons; (3) perform none; (4) record the ruling as a DATED STRIKE preserving the declined alternatives and the operator's reasons. The strike, not a clean edit, is load-bearing - options B and C being visible is what makes the decision auditable. CAVEAT recorded rather than hidden: the story was unusually explicit (AC5 said nothing in this story fetches, in so many words) and the operator was available same-day; neither condition is guaranteed next time."
    owner: "Architect (registration in architecture.md section Enforcement) + dev-loop orchestrator (the phase rule)"
    destination: "(R) DoD: section Enforcement carries the halt form; a story that halts on an operator act is expected to produce all four elements"
    priority: "high"
    status: open
  - epic: 15
    id: "AI-E15-3"
    action: "AI-E14-1 carried forward, SECOND consecutive - register the non-vacuity standing rule AND implement DF-15-2-A's mechanisable half. MEASURED: architecture.md is BYTE-UNCHANGED across the whole of Epic 15, so the rule did not land, even though the practice was applied better this epic than in any epic before it (nine executed mutations in 15.1, predictions written before measurement in 15.2, two guards re-authored rather than relaxed). The gap is DURABILITY, not compliance: the discipline lives in two story files and nowhere a future author is obliged to read. Two halves, both needed - (a) the rule text in section Enforcement with its enforcing module and test ids, asserted present by its own guard; (b) DF-15-2-A's narrow machine-checkable arm, a guard asserting f(a)==f(b) must first assert a!=b, which would have caught -107 and -118 on the day each was written. AI-E12-5 waited four retrospectives because it was filed as prose only."
    owner: "Architect (registration) + Engineering Lead (the (b) guard)"
    destination: "(R) DoD: section Enforcement carries the rule with module and ids and a guard asserts the text is present; DF-15-2-A arm (b) is implemented and proven RED against the -107 pre-fix form"
    priority: "critical"
    status: open
  - epic: 15
    id: "AI-E15-4"
    action: "File the UNFILED headroom row: tests/test_vacuous_density.py at 1,159/1,200 - 41 lines, the second-tightest module in the repository and TIGHTER than the 39 DF-14-3-H was filed about. Re-derived in this retrospective with the ceiling guard's own _physical_line_count; it grew +72 during Epic 15 and NO ledger entry covers it (DF-14-3-H's amendment lists it at the now-stale 1,087 as context, not coverage; DF-15-2-D covers only the production module). File it in DF-15-2-D's form: measured size, guards green, no _EXEMPT_BY_DESIGN entry, and the trigger - the next change of any size performs the cohesion split first. ALSO re-derive the WHOLE population when filing: Epic 15's lesson is that a split MOVES ceiling pressure rather than removing it (test module 39 -> 409 headroom, production module 87 -> 4)."
    owner: "Engineering Lead"
    destination: "(S)/(H) DoD: a dated ledger entry for tests/test_vacuous_density.py with the re-derived population table; the MAINT-001-04 registry unchanged, since it may only shrink"
    priority: "high"
    status: open
  - epic: 15
    id: "AI-E15-5"
    action: "Rule on DF-14-3-H: its stated remedy has been PERFORMED and its status does not say so. Story 15.2 split tests/test_vacuous_detector.py 1,161 -> 791 (39 -> 409 lines of headroom), by cohesion, with no exemption and no shave - exactly what that entry's instruction demanded. The entry remains OPEN by the story's own deliberate and correct choice, and this retrospective did NOT dispose of it and must not: a worker closing a governance entry is the AI-E12-3 shape. What is needed is the named owner's DATED disposition - closed against the split's evidence, or explicitly re-scoped to whatever residue survives."
    owner: "XAgent007 (Engineering Lead) - the entry's named owner"
    destination: "(H) DoD: DF-14-3-H carries a dated disposition naming the split as its evidence, or a dated re-scope stating what remains"
    priority: "high"
    status: open
  - epic: 15
    id: "AI-E15-6"
    action: "Decide TypeScript's position AT protocol section 6 R2, BEFORE ratifying any row, not after. MEASURED at the pins through the real ast_index and the shipped _is_test_function: 367 TS test files across the two ratified TS members yield ONE scorable test function (xagents-webapp@33a86525, 279 files, 1; agent-smith@9ab774d7, 88 files, 0), so TypeScript is in the SAME position as Go and Java. All 20 fetched repositories are Python and all 14 admitted are Python, so no TS candidate was ever tested against DN-15-1-3's 25-scorable-function visibility floor. Ratifying TS members that contribute about zero scorable functions INFLATES the N that satisfies the corpus floor while contributing nothing to the N that gates. If the answer is fix DF-14-3-C and -A first, note the A/B COUPLING: the one-character fix to -A alone converts Go's harmless silence into a fresh false accusation across an entire language."
    owner: "XAgent007 (Governance Owner)"
    destination: "(H) DoD: a dated decision at R2 - Python-only for this round, or TS retained with the disclosure restated, or the capability scheduled - recorded BEFORE any candidate is promoted"
    priority: "high"
    status: open
  - epic: 15
    id: "AI-E15-7"
    action: "Carry the two accepted-but-unfixed review observations so they are not lost with the story file. (i) DF-15-2-D's ledger text carries TWO LITERAL 0x0D BYTES at deferred-work.md:5205-5206 where escaped carriage-return text was intended (confirmed by the reviewer with cat -A and a byte scan). No guard is affected - every consumer reads the ledger through Path.read_text with encoding utf-8, whose universal-newline translation normalises the bytes before any comparison - so it is a RENDERING defect only. Fix on the next edit to that entry. Noted because a ledger entry about a line-terminator contract carrying an uncontrolled line terminator is the AI-E13F class. (ii) The placeholder-marker recount (3+7 = 10, not 3+6 = 9): resolution was MORE complete than claimed, nothing is broken, no action beyond AI-E15-1. Recorded because the failure mode for accepted observations is not severity - it is that they were written into a story file nobody re-opens."
    owner: "Engineering Lead"
    destination: "(S) DoD: both are visible outside the story file; (i) is corrected the next time that ledger entry is touched"
    priority: "medium"
    status: open
  - epic: 15
    id: "AI-E15-8"
    action: "Partition the action-item register by WHO CAN DISCHARGE IT - the structural finding of this retrospective's follow-through. MEASURED across Epic 14's ten items: EVERY item Epic 15 executed was one a WORKER could execute inside a story (AI-E14-2's sweep, AI-E14-5's amendment, AI-E14-8's convention, AI-E14-3's commits); EVERY item it did not requires an architect registration (AI-E14-1), a governance owner's ruling (AI-E14-6, -7, -9) or an orchestrator phase change (AI-E14-4) - none of which any story's definition of done can reach. Score: 2 done, 2 partial, 6 not addressed. Six consecutive retrospectives have re-raised items into that class and then measured them undelivered; DF-10-2-A is at SEVEN. THIS IS A PARTITION, NOT A PRUNE - section 3.4 forbids erasing records and an item's age is evidence, not noise. Three buckets: (S) discharged by a story, (R) by an orchestrator or architect rule change, (H) only by a human decision. Then an (H) item re-raised three times gets a dated carried-indefinitely-accepted or an explicit refusal, rather than a seventh restatement."
    owner: "XAgent007 (Governance Owner) + dev-loop orchestrator"
    destination: "(H) -> (R) DoD: every open item carries an S/R/H class; every (H) item re-raised three or more times carries a dated acceptance or refusal"
    priority: "high"
    status: open
  - epic: 15
    id: "AI-E15-9"
    action: "Close the epic-N-retrospective gap with a GUARD, not a third patch. The key was missing for Epics 14 AND 15 - each created without it, one by a change proposal and one by a direct operator decision - and the Epic-14 retrospective created both. Re-verified live this session: all FIFTEEN keys now exist (epic-1-retrospective through epic-15-retrospective). Also re-derived: NO test anywhere in tests/ asserts the invariant, so the rule lives in one retrospective's prose and in the habit of whoever writes the next epic. It is a roughly 15-line closure over a file the suite already reads: for every epic-N key in development_status, an epic-N-retrospective key exists. NON-VACUITY FLOOR REQUIRED (AI-E14-1): assert the epic-key population is non-empty and at least 15 BEFORE asserting the correspondence, or the guard passes forever on a parse failure. DORMANT, NOT REMOVED: epic 15 is the last epic, so this cannot fire again until the plan is extended - which SD-5 makes likely, through the same two doors that created the gap twice."
    owner: "Engineering Lead (the guard) + dev-loop orchestrator (the epic-creation step)"
    destination: "(S)/(R) DoD: a committed non-vacuous guard asserting the correspondence, proven RED by removing one key from a copy of the tracker"
    priority: "high"
    status: open
  - epic: 15
    id: "AI-E15-10"
    action: "Give AI-E14-2's sweep verdicts and the corpus-emptiness measurement DURABLE HOMES. Two records, both currently outside the repository's own surfaces. (a) DF-15-2-C: the 22 REAL / 1 VACUOUS / 1 WEAK per-id verdict table over Epic 14's 24 unswept guards lives ONLY in sprint-change-proposal-2026-08-19.md - and AI-E14-2 is therefore NOT discharged even though the sweep RAN, found a live false-accusation defect in the shipped detector, and produced Story 15.2. It is the highest-yield governance act this project has run and the project can currently lose the record of it. (b) Story 15.1 section 1.1's measurement - 1 co-occurrence file in 315 ratified Python test files against 614 in 2,316 - is THE REASON THE GATE READ ZERO and appears in NO prd, protocol or architecture surface. It belongs on the protocol's limitations surface so that the 0 TP / 26 FP figure can never again be read as a false-positive rate for the detector: it is a measurement of a corpus that did not contain the defect class, taken through an instrument since replaced, and DF-13-5-B records the other half. RECOMMENDATION ONLY - this retrospective edited none of those documents and should not."
    owner: "XAgent007 (Governance Owner) + Engineering Lead"
    destination: "(H) -> (S) DoD: the 24-id verdict table has a home in the repository; the protocol limitations surface carries the section 1.1 figures with their predicates and their date"
    priority: "critical"
    status: open
  - epic: 15
    id: "AI-E15-11"
    action: "DF-10-2-A - SEVENTH consecutive retrospective. Give it its dated decision (AI-E12-9, then AI-E13-9, then AI-E14-9, unmoved). Re-read on disk this session: still owner Engineering Lead, target_story NONE, unscheduled. C, C++, Ruby and Rust ground cleanly and extract ZERO definitions, so files in those languages can never reach audited_deep under a V1 delivered claim. What is needed is the DATED DECISION, not a fix. This item is the strongest single argument for AI-E15-8: seven restatements have not moved it and an eighth will not either."
    owner: "XAgent007 (Governance Owner)"
    destination: "(H) DoD: the DF-10-2-A target_story NONE is replaced by a destination, OR by a dated entry stating the disclosure is sufficient for V1 and the capability is V2"
    priority: "high"
    status: open
  - epic: 15
    id: "AI-E15-12"
    action: "AI-E14-6 carried forward - name owners for DF-14-3-A, -B, -C and DF-14-1-A. All four re-read on disk this session and all four still carry owner: unassigned. Epic 15 DEPENDED on all three of the 14-3 entries: they are the entire reason TypeScript yields 1 scorable test function in 367 files, and therefore the reason SD-2 is an operator decision at R2 rather than an inherited state. Ownership is not scheduling; leave target_story as-is. CARRY THE A/B COUPLING ON THE OWNER'S OWN RECORD, not only in the entries: the one-character fix to -A alone converts Go's harmless silence into a language-wide false accusation."
    owner: "XAgent007 (Governance Owner)"
    destination: "(H) DoD: each of the four carries a named owner; the A/B coupling is restated wherever ownership is recorded"
    priority: "high"
    status: open
  - epic: 15
    id: "AI-E15-13"
    action: "AI-E13-1 / AI-E14-4 carried forward, THIRD consecutive - gate the phases that write records. Epic 15 produced a NEW instance: a sprint-status write left a stray CR mid-line and took yaml.safe_load RED on this CRLF file (caught, repaired, and re-verified byte-for-byte by the reviewer - 923 lines, 923 CRLF, zero lone CR and zero lone LF anywhere in the file). sprint-status.yaml is NOT prose: a one-byte error makes the whole tracker unparseable, so the gate must include a yaml.safe_load round-trip on that file, not only the prose-reading guards. Must be platform-neutral - a python -m pytest invocation, since local gates are Windows-only while CI runs an ubuntu matrix. SECOND HALF: the review gate writes findings UNCHECKED, so the checkbox is the dev's signal and cannot stand over an unapplied fix - Epic 15 produced exactly that, a checked box found on disk with its correction not applied, self-reported in section D16."
    owner: "dev-loop orchestrator (the phase rule) + XAgent007 (if a pre-push arm is chosen)"
    destination: "(R) DoD: no phase writing into stories/ or the artifact directory hands off without a green run of the prose-reading guards PLUS a yaml.safe_load of sprint-status.yaml; review findings are written unchecked"
    priority: "critical"
    status: open
  - epic: 15
    id: "AI-E15-14"
    action: "AI-E14-7 carried forward - file the entry recording that the dogfood currency guards track argus/ LOC as a build-cost proxy, NOT behaviour. Re-derived this session: no such entry exists in deferred-work.md. Epic 15 produced the cleanest corroboration in BOTH directions - 15.2's guards fired on an argus/ LOC delta of 29,755 to 29,776 and were closed through AI-E12-11's pre-authorised order with zero friction, while 15.1 confirmed BY MEASUREMENT that its delta contained zero argus/ files, so no regeneration was owed and none was performed (2 of Epic 15's 15 commits are dogfood regenerations, against Epic 14's 7 of 14). NOT a request to loosen the guard: the LOC proxy is the budget input and is correct for its purpose. What is missing is that a reader cannot distinguish artifacts-moved-because-behaviour-moved from a-comment-was-added without re-deriving. THEN SPLIT AI-E12-11, which is marked done over two undelivered options."
    owner: "Engineering Lead (the entry) + XAgent007 (the AI-E12-11 split - a governance ruling)"
    destination: "(H) DoD: a dated entry states the LOC-proxy property; AI-E12-11 records option (1) closed with its evidence and options (2) and (3) live-or-retired with an owner"
    priority: "medium"
    status: open
  - epic: 15
    id: "AI-E15-15"
    action: "RECORD ONLY, no owner, on purpose: Epic 15's review record is not a quality trend in either direction. Two stories, 4 review iterations, 5 findings, ALL Low, all real, none argued away, zero High and zero Medium. READ THAT WITH THREE CAVEATS OR DO NOT READ IT AT ALL. (1) Story 15.1 changed ZERO bytes under argus/, and a story that ships no production code has a smaller surface for a reviewer to find High findings in. (2) The most consequential thing the review gate did this epic was NOT a finding: it independently re-derived the ordering claim, the import ban, the INTRINSIC/PENDING partition against the live 21-row corpus and the -137 load-bearingness contingency FROM SCRATCH, and found them sound - a pass rate cannot show that. (3) The most consequential thing found in this epic was found by a SWEEP, not by the review gate: AI-E14-2's vacuity sweep, which produced a whole story. This exists so that if the trend is ever cited, the reader knows what it does and does not measure - the courtesy AI-E9-10, AI-E10-10, AI-E11-12, AI-E12-14, AI-E13-11 and AI-E14-10 paid before it."
    owner: "none - deliberately unassigned"
    destination: "the retrospective document (epic-15-retro-2026-08-19.md). DoD: none"
    priority: "record-only"
    status: open
